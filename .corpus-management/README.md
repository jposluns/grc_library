# Corpus-Management pack (`.corpus-management/`)

**Status: ACTIVE (umbrella 4.1, compile PR-53).** This directory is the structure of a standalone,
adoptable Corpus-Management pack. PR-2 shipped the compiler, the first generated slice (the CLAUDE.md
generated-artefacts instruction block, transferred verbatim), and the mandatory drift gate (grc gate
99); PR-3 transferred the first authoring rule (the corpus language convention) as a generated
file-kind rule, the first live use of the rules-sync gate's compiler-owned recognition; PR-4
transferred the second authoring rule (the corpus authoring conventions) as the second file-kind
rule. The gate register carries forty gate transfers (lint-language, grc gate 2, compile PR-5; lint-unbalanced-fences, grc gate 66, compile PR-6; lint-nested-markdown-links, grc gate 68, compile PR-7; lint-ungated-dashes, grc gate 82, compile PR-8; lint-bare-normative-shall, grc gate 56, compile PR-9; lint-links, grc gate 3, compile PR-10; lint-required-sections, grc gate 19, compile PR-11; lint-citations, grc gate 5, compile PR-12; lint-section-placement, grc gate 38, compile PR-13; lint-shall-near-uncertainty, grc gate 9, compile PR-14; lint-todo-marked-done, grc gate 57, compile PR-15; lint-positional-backlog-tokens, grc gate 69, compile PR-16; lint-stub-documents, grc gate 16, compile PR-17; lint-filename-title-alignment, grc gate 7, compile PR-18; lint-standards-currency, grc gate 6, compile PR-19; lint-directional-dependency, grc gate 53, compile PR-20; lint-roles, grc gate 8, compile PR-21; lint-placeholder-leakage, grc gate 12, compile PR-22; lint-gate-count-consistency, grc gate 39, compile PR-23; lint-date-format, grc gate 14, compile PR-33 (first SHARED-mapped scan-gate, Pattern A shim); lint-license-consistency, grc gate 15, compile PR-34; lint-section-anchors, grc gate 17, compile PR-35; lint-intra-doc-refs, grc gate 18, compile PR-36; lint-secrets-in-content, grc gate 21, compile PR-37; lint-pii-in-content, grc gate 22, compile PR-38; lint-internal-references, grc gate 23, compile PR-39; lint-external-link-domains, grc gate 24, compile PR-40; lint-acronym-consistency, grc gate 20, compile PR-41; lint-cross-doc-numbers, grc gate 25, compile PR-42; lint-cobit-iso31000-citations, grc gate 61, compile PR-43; lint-metadata-line-breaks, grc gate 30, compile PR-44; lint-orphan-documents, grc gate 26, compile PR-45; lint-cross-file-section-refs, grc gate 62, compile PR-46; lint-cross-file-section-names, grc gate 65, compile PR-47; lint-document-date-staleness, grc gate 31, compile PR-48; lint-structure, grc gate 4, compile PR-49; lint-metadata, grc gate 1, compile PR-50; lint-retention-consistency, grc gate 55, compile PR-51; lint-document-control-codes, grc gate 54, compile PR-52; lint-document-iso-annex-a, grc gate 58, compile PR-53); no hooks have been transferred yet.

## What this is
A **thin, adoptable layer** for keeping a documentation corpus internally consistent (metadata shape,
section model, citation currency, cross-links), built on top of the **AIQT generic core** rather than
duplicating it. The generic file/text/date/git/metadata primitives come from the vendored
`aiqt_corpus` module (dependency-model B; see `vendor/aiqt/`); this pack adds only the corpus-specific
policy: the fixed metadata structure, the register grammar, and the corpus gates/hooks. AIQT provides
policy-free primitives, not a corpus compiler; this pack is that compiler's source of record.

## Source-of-record discipline
`.corpus-management/` is the **authoritative source**. Generated outputs (rules, CLAUDE.md blocks,
gate/hook wiring) are produced FROM this tree by the compiler (`tools/corpus_mgmt_compiler.py`,
wrapped by the project's `tools/build-corpus-management.py`) and are **never edited directly**; edit
the source here and regenerate. The drift gate (grc gate 99, the wrapper's `--check` form) enforces
byte-parity with nothing stripped before comparison.

## Scan boundary (why this directory is exempt from corpus-content gates)
This is pack SOURCE, not corpus content, so the corpus-content validators do not select it (a
root-anchored exemption, matched on the first path component only, so nested lookalikes such as
`governance/.corpus-management/` are NOT exempt). The security and cross-cutting operational audits
(secrets, PII, internal-references, external-link-domains, narrative-boundary, and prose dash-style)
**still apply** to this tree; the exemption is from corpus-document-model validation only, not a blanket
skip. (The stdlib-only-imports and unused-import gates scan the pack's `tools/` tree, which ships
Python since PR-2.)

## Structure (fixed) vs vocabulary (configurable)
The metadata STRUCTURE is fixed (every document carries the same field set and section model, which is
what makes the corpus machine-auditable). The VOCABULARY (allowed values, jurisdiction lists, framework
identifiers) is configurable per adopter via profiles under `defaults/`.

## Reference and operational registers
Reference defaults ship under `defaults/grc/` and are overridable by an adopter. Operational registers
(a project's private working state) are **project-only** and are never shipped by this pack.

## Optional semantic add-ons
The reference-dependent semantic audits (matrix-fit, claim-fit, reference-audit) are OPTIONAL add-ons
that require an adopter to supply a reference base; they are listed in the manifest as available but
disabled in the skeleton.

## Rollout
- **PR-1:** inert scaffold + the root-anchored scan-boundary exemption + scope tests. SHIPPED.
- **PR-2:** the compiler + the first generated vertical slice + the mandatory drift gate (grc gate 99). SHIPPED.
- **PR-3:** the first clause-transfer wave: the `language-convention` file-kind rule generated to `.claude/rules/corpus-management/` (first live gate-37 recognition), plus the tree-child containment hardening (routed from #2038). SHIPPED.
- **PR-4:** the second clause-transfer wave: the `authoring-conventions` file-kind rule (the corpus authoring conventions, transferred from the CLAUDE.md `## Conventions` section), the pack's second file-kind rule. SHIPPED.
- **PR-5:** the gate-wave opened: the first gate transfer (lint-language, grc gate 2), the engine moved to the pack tools/ as source of record behind a thin Model-2 wrapper, the gate register populated with compiler validation, and the id-history charter widened to pack-ids. SHIPPED.
- **PR-6:** the second gate transfer (lint-unbalanced-fences, grc gate 66), same Model-2 wrapper shape as PR-5. SHIPPED.
- **PR-7:** the third gate transfer (lint-nested-markdown-links, grc gate 68), same Model-2 wrapper shape as PR-5/6, enforcing the new markdown-link-integrity clause. First transfer sequenced by the gate-homes.toml classification. SHIPPED.
- **PR-8:** the fourth gate transfer (lint-ungated-dashes, grc gate 82), Model-2 pattern, REUSING the existing language-convention clause (no new clause/rule, unlike PR-6/PR-7). SHIPPED.
- **PR-9:** the fifth gate transfer (lint-bare-normative-shall, grc gate 56), Model-2 pattern, enforcing the new normative-wording clause (the FR-44 must-over-shall convention). The grc-specific EXEMPT_FILES filter moved to the wrapper so the engine is REPO_ROOT-free. SHIPPED.
- **PR-10:** the sixth gate transfer (lint-links, grc gate 3, broken-internal-link audit), Model-2 pattern, enforcing the new markdown-link-resolution clause. Engine made repo_root-free (the containment check took a repo_root parameter). SHIPPED.
- **PR-11:** the seventh gate transfer (lint-required-sections, required-sections-by-doctype audit), Model-2 pattern, enforcing the new required-sections clause. The engine takes the section MODEL (required_map) + repo_root as parameters (config-free); the grc REQUIRED_SECTIONS model stays in the wrapper. SHIPPED.
- **PR-12:** the eighth gate transfer (lint-citations, grc gate 5, framework-citation denylist audit), Model-2 pattern, enforcing the new citation-denylist clause. Engine takes the DENYLIST + PATH_EXEMPTIONS + repo_root as parameters (config-free); the grc denylist stays in the wrapper. SHIPPED.
- **PR-13:** the ninth gate transfer (lint-section-placement, grc gate 38, section-order audit), Model-2 pattern, enforcing the new section-placement clause. Engine config-free (PLACEMENT_RULES + repo_root params); grc rules stay in the wrapper. SHIPPED.
- **PR-14:** the tenth gate transfer (lint-shall-near-uncertainty, grc gate 9), Model-2 pattern, enforcing the new shall-near-uncertainty clause. Marker patterns + window in the engine (generic check); grc exempt-files filter in the wrapper (engine repo_root-free) (the marker/window vocab subsequently moved to defaults/grc/uncertainty.toml in Phase-4 PR-I). SHIPPED.
- **PR-15:** the eleventh gate transfer (lint-todo-marked-done, grc gate 57), Model-2 pattern, enforcing the new todo-forward-only clause. Cleanest yet: fully-generic check (no config), only the scan target in the wrapper. SHIPPED.
- **PR-16:** the twelfth gate transfer (lint-positional-backlog-tokens, grc gate 69), Model-2 pattern. FIRST direct-call-coupled transfer: the wrapper keeps a check_file SHIM (exempt-check + engine check) for a direct-call regression test, establishing the pattern for direct-call-coupled gates. SHIPPED.
- **PR-17:** the thirteenth gate transfer (lint-stub-documents, grc gate 16), Model-2 pattern, enforcing the new no-stub-documents clause. Stub-phrase list + threshold in the engine; target selection in the wrapper. SHIPPED.
- **PR-18:** the fourteenth gate transfer (lint-filename-title-alignment, grc gate 7), Model-2 pattern. Most param-heavy yet (synonyms + doctypes + min-overlap params); DOCTYPES stayed a wrapper module attribute for the gate-67 cross-check (migrated to the alignment profile in Phase-4 PR-E). SHIPPED.
- **PR-19:** the fifteenth gate transfer (lint-standards-currency, grc gate 6), Model-2 pattern. Most-involved yet: engine gets compile+check+run (compiled patterns + counts + repo_root params); wrapper keeps the canonical-register parse + the --root fixture-isolation override + error paths. SHIPPED.
- **PR-20:** the sixteenth gate transfer (lint-directional-dependency, grc gate 53), Model-2 pattern. Bespoke marker-aware fence handling + a 9-case self-test moved to the engine (project_gov_dir param); --self-test delegates. SHIPPED.
- **PR-21:** the seventeenth gate transfer (lint-roles, grc gate 8, Owner/Approving-Authority role audit), Model-2 pattern, enforcing the new role-authority clause. Engine holds the pure metadata-value check (patterns + is_placeholder + check_file + reporting run, known + repo_root params, bare relative_to like gate 56); the wrapper keeps the role-authority register parse (load_known_roles) + the EXTRA_KNOWN_ROLES allow-list + the register-prerequisite exit 2 + the --root override (proven by PR-19). SHIPPED.
- **PR-22:** the eighteenth gate transfer (lint-placeholder-leakage, grc gate 12, placeholder-leakage audit), Model-2 pattern, enforcing the new placeholder-leakage clause. PATTERNS marker set + fence-aware scan + reporting run (repo_root param, try/except display fallback) in the engine (the PATTERNS marker set subsequently moved to defaults/grc/placeholders.toml in Phase-4 PR-H); the exempt policy + iter_targets in the wrapper. The lint-pii-in-content cross-reference proved to be prose-only (no code coupling). SHIPPED.
- **PR-23:** the nineteenth gate transfer (lint-gate-count-consistency, grc gate 39), Model-2 pattern, enforcing the new gate-count-consistency clause. The HEAVIEST transfer (569L): PATTERNS + word-number machinery + scan_file (version-history aware; patterns anchored) moved byte-verbatim (diff-verified each block); the wrapper keeps the §6-spec parse + collection dirs + a scan_file shim for the importlib direct-load test. Completes the clean ALLOW-mapped tranche. SHIPPED.
- **PR-24 (Phase-4 PR-A):** the reference-vocabulary profile ENABLER, opening the Phase-4 genericize wave. A concern-agnostic `tools/profile_loader.py` (versioned TOML profiles under `defaults/grc/<concern>.toml`, key-level-replace adopter overlay, fail-closed envelope + regex-table compilation), the first shipped profile (`defaults/grc/citations.toml`, the framework-citation denylist mirrored VERBATIM from gate 5), the `core/profiles.toml` register, gate-99 profile validation (validate-when-declared; profiles are validated SOURCE, never owned/generated), and loader + compiler tests. Behaviourally INERT: gate 5 kept its wrapper config pending PR-B, and a parity test bounded the two-place window. SHIPPED.
- **PR-25 (Phase-4 PR-B):** the first gate MIGRATION onto the loader. Gate 5 (lint-citations) now loads its framework-citation denylist from `defaults/grc/citations.toml` via `profile_loader.load('citations')` (`_citation_config()` converts to the engine shapes, loaded lazily in `main()`); the wrapper literals were removed. Behaviour byte-IDENTICAL (proven before/after); the PR-A parity test reworked to a wiring test; engine unchanged. SHIPPED.
- **PR-26 (Phase-4 PR-C):** gate 19 (lint-required-sections) migrated onto the loader (defaults/grc/sections.toml + `_sections_config()`; 11 doctypes x 8 orientation aliases; byte-identical). gate 67 Check 2 CO-EDITED (it read `REQUIRED_SECTIONS` with a `getattr(...,{})` default that would silently vacuous-pass). SHIPPED.
- **PR-27 (Phase-4 PR-D):** gate 38 (lint-section-placement) migrated (defaults/grc/placement.toml + `_placement_config()`; the tuple/frozenset/None PLACEMENT_RULES shape rebuilt exactly, pickle-baseline verified; byte-identical). No cross-gate reader. SHIPPED.
- **PR-28 (Phase-4 PR-E):** gate 7 (lint-filename-title-alignment) migrated (defaults/grc/alignment.toml + `_alignment_config()`; DOCTYPES set + SYNONYMS map). gate 67 Check 1 CO-EDITED to read the composed doctypes via `_alignment_config()` (DOCTYPES no longer a module attribute). Byte-identical. SHIPPED.
- **PR-29 (Phase-4 PR-F):** the FIRST engine-parameterization. Gate 2 (lint-language): the ise/isation/commonwealth/yse VOCAB moved from the engine into `defaults/grc/language.toml`; the engine's `run()` now takes a `LanguageVocabulary` and COMPILES the ise/yse patterns from it (byte-identical to the former hardcoded patterns, proven by `.pattern`+`.flags` equality); the wrapper loads it via `_language_config()`. gate 82 unaffected. SHIPPED.
- **PR-30 (Phase-4 PR-G):** markers, gate 16 (lint-stub-documents) engine-parameterized. STUB_PHRASES (11) + WORD_COUNT_THRESHOLD (100) moved to `defaults/grc/stubs.toml`; the engine's `run()` takes a `StubVocabulary` (the profile's first INT value); the wrapper loads it via `_stubs_config()`. Plain data (no compiled-from-vocab pattern). Byte-identical. SHIPPED.
- **PR-31 (Phase-4 PR-H):** markers gate 12 (lint-placeholder-leakage) engine-parameterized. The 17 `PATTERNS` `(regex,label)` pairs moved to `defaults/grc/placeholders.toml` as ordered `[[placeholders.patterns]]` (nested `{regex,ignorecase}` table + `label` sibling, loader-compiled, order = policy); the engine's `run()` takes the composed `(pattern,label)` pairs via `placeholder_patterns()`. Byte-identical. SHIPPED.
- **PR-32 (Phase-4 PR-I):** markers gate 9 (lint-shall-near-uncertainty) engine-parameterized. The 8 `UNCERTAINTY_PATTERNS` + the `MANDATORY_PATTERN` alternation + `WINDOW_LINES` (2) moved to `defaults/grc/uncertainty.toml` (loader-compiled `{regex,ignorecase}` tables; the mandatory stored verbatim so its embedded spaces survive; the window an int); `run()` takes an `UncertaintyVocabulary`. Byte-identical. SHIPPED. **This COMPLETES the Phase-4 wave: the profile-loader enabler + 8 gate migrations (5/19/38/7 wrapper-config, 2/16/12/9 engine-parameterized), 8 profiles under `defaults/grc/`.**
- **PR-33 (SHARED/SAFETY lane, first transfer):** gate 14 (lint-date-format) engine transferred into the pack via **Pattern A** (the g69 re-export shim generalized to `scan`): `ISO_DATE_RE` + `validate_date` + `PLACEHOLDER_VALUES` + the fence-aware `scan` moved verbatim to `gate_lint_date_format.py`; the wrapper keeps `main` + the grc scan scope and a module-global `scan` shim delegating to the engine, so the scan-scope test's `patch.object(m,"scan")` still intercepts. New `date-format` clause (gensrc kind=file). Byte-identical (511 files, 0 findings). SHIPPED.
- **PR-34 (SHARED/SAFETY lane):** gate 15 (lint-license-consistency) engine transferred via Pattern A. `CANONICAL_LICENSE` + the raw-line (non-fence-aware) `scan` moved to `gate_lint_license_consistency.py`; the wrapper keeps `main` + the grc scan scope + the `EXEMPT_FILES` set (`NOTICE.md`) + a `scan` shim, reads `CANONICAL_LICENSE` from the engine; dead `LICENSE_LINE_RE` + `import re` removed. New `license-consistency` clause. Byte-identical (510 files, 0 findings). SHIPPED.
- **PR-35 (SHARED/SAFETY lane):** gate 17 (lint-section-anchors) engine transferred via Pattern A. `LINK_RE`+`HEADING_RE`+`slugify`+`extract_anchors`+the anchor-resolution `scan` moved to `gate_lint_section_anchors.py`; the engine `scan` takes `repo_root` as a keyword (for the finding's display path) so it stays repository-root-free, the wrapper shim supplies `REPO_ROOT`. New `section-anchor-resolution` clause. Byte-identical (511 files, 0 findings). SHIPPED.
- **PR-36 (SHARED/SAFETY lane):** gate 18 (lint-intra-doc-refs) engine transferred via Pattern A. `HEADING_RE`+`REF_PATTERNS`+`extract_sections`+`is_cross_doc_context` (the window scan + doctype-word + framework-name heuristic vocabularies, verbatim)+`scan` moved to `gate_lint_intra_doc_refs.py`; the wrapper keeps `main`+the grc scan scope+`EXEMPT_FILES`+a `scan` shim. New `intra-doc-section-refs` clause. Byte-identical (509 files, 0 findings). SHIPPED.
- **PR-37 (SAFETY lane, first):** gate 21 (lint-secrets-in-content) engine transferred via Pattern A. The 14 `SECRET_PATTERNS` + the redacting fence-aware `scan` moved to `gate_lint_secrets_in_content.py`; the wrapper keeps `main`+the grc scan scope (`iter_targets` over `SCAN_SUFFIXES`)+`EXEMPT_FILES`+a `scan` shim. The engine is added to `EXEMPT_FILES` defensively (it now documents the secret formats by design, mirroring the wrapper's self-exemption; it does not currently self-match). New `secret-content` clause. Byte-identical (SECRET_PATTERNS identical; gate clean, 771 files). SHIPPED.
- **PR-38 (SAFETY lane):** gate 22 (lint-pii-in-content) engine transferred via Pattern A. The 5 detection regexes (email/SSN/phone/IPv4/street) + the IP filters + the fence-aware `scan` moved to `gate_lint_pii_in_content.py`; `scan` gained an `example_domains` keyword so the grc example-domain allow-list stays wrapper config. The engine joins `EXEMPT_FILES` defensively (does not self-match). New `pii-content` clause. Check output-identical (regexes+domains identical, main() output text preserved verbatim); gate clean, 769 files. SHIPPED.
- **PR-39 (SAFETY lane):** gate 23 (lint-internal-references) engine transferred via Pattern A. The 5 detection regexes (internal-hostname/AWS/Azure/GCP-region/CIDR) + the documentation-subnet filter + the fence-aware `scan` moved to `gate_lint_internal_references.py`; `main()` copied verbatim (the PR-38 lesson); the engine joins `EXEMPT_FILES` defensively. New `internal-reference` clause. Byte-identical (regexes+scan identical; gate clean, 773 files). SHIPPED.
- **PR-40 (SAFETY lane):** gate 24 (lint-external-link-domains) engine transferred via Pattern A, **completing the SAFETY `scan` set (g21-24)**. `URL_RE` + `is_allowed` + the fence-aware `scan` moved to `gate_lint_external_link_domains.py`; `scan`/`is_allowed` gained an `allow_list` keyword so the grc 142-entry publisher allow-list stays wrapper config; `main()` verbatim; engine self-exempt defensively. New `external-link-domain` clause. Byte-identical (URL_RE+allow-list+scan identical; gate clean, 779 files). SHIPPED.
- **PR-41 (2nd-seam SHARED):** gate 20 (lint-acronym-consistency) engine transferred via Pattern A + a **2nd patched seam**. The scan-scope test patches BOTH `scan` and `parse_glossary`, so BOTH stay wrapper module-global shims; `parse_glossary` is parameterized by the glossary path (the grc `GLOSSARY` path stays wrapper config). Regexes/stopwords/parse/scan moved to `gate_lint_acronym_consistency.py`; `main()` verbatim. New `acronym-consistency` clause. Byte-identical (regexes+glossary+scan identical; gate clean, 473 files). SHIPPED.
- **PR-42 (2nd-seam SHARED):** gate 25 (lint-cross-doc-numbers) engine transferred via Pattern A. Its scan returns a DICT (so the scope-test capture returns `{}` not `[]`), but it patches only `scan`. TERM_PATTERNS + the unit table + `normalise` + the per-file `scan` moved to `gate_lint_cross_doc_numbers.py`; the cross-document aggregation stays in the wrapper's `main` (verbatim). NO engine self-exemption (gate 25 scans `.md` only, so the engine `.py` is out of scope by suffix). New `cross-doc-numbers` clause. Byte-identical (patterns+scan identical; gate clean, 510 files). SHIPPED.
- **PR-43 (SHARED lane):** gate 61 (lint-cobit-iso31000-citations) engine transferred via Pattern A. The COBIT objective/practice + ISO 31000 clause existence checks moved to `gate_lint_cobit_iso31000_citations.py` (reference-register coupled via `cobit_iso31000_reference`); new `cobit-iso31000-citations` clause. SHIPPED.
- **PR-44 (Group-A content-generic lane):** gate 30 (lint-metadata-line-breaks) engine transferred via Pattern A, opening the Group-A content-generic tranche. `META_LINE` + `has_hard_break` + `scan_file` moved verbatim to `gate_lint_metadata_line_breaks.py`; the wrapper keeps `main()` (verbatim) + the grc scan scope (`DEFAULT_TARGETS` incl the splatted `AUDITED_DOMAIN_DIRS` + `iter_target_files`) + a module-global `scan_file` shim delegating to the engine so the scan-scope test (patches `mod.scan_file` + runs `main`) still intercepts; the engine is scope-free and repository-root-free. New `metadata-line-breaks` clause. Byte-identical. SHIPPED.
- **PR-45 (Group-A content-generic lane):** gate 26 (lint-orphan-documents) engine transferred via Pattern A. `LINK_RE` + `normalise_link` + `build_reverse_graph` moved to `gate_lint_orphan_documents.py`, parameterized by `repo_root` so the engine is repository-root-free; the wrapper keeps the grc artefact policy (`is_artefact`/`find_artefacts`/`find_all_markdown`/`ALWAYS_EXEMPT`) + the orphan aggregation + reporting in `main` + a `build_reverse_graph` shim supplying `REPO_ROOT`. New `orphan-documents` clause. Byte-identical. SHIPPED.
- **PR-46 (Group-A content-generic lane):** gate 62 (lint-cross-file-section-refs) engine transferred via Pattern A. `extract_sections` + `resolve_target` + `binding_target` + `adjacent_link` + `check_file` (with the tool-specific `HEADING_RE`/`CLAUSE_RE`) moved to `gate_lint_cross_file_section_refs.py`, parameterized by `repo_root` + the shared `CROSS_*` reference-extraction config so the engine is repository-root-free and scan-scope-free; the wrapper keeps `main` + the grc scan scope (`iter_markdown_targets`/`EXEMPT_FILES`) + the `CROSS_*` config from `lint_common` (shared with the names-phase sibling gate 65) + a module-global `check_file` shim delegating to the engine so the scan-scope test still intercepts. New `cross-file-section-refs` clause. Byte-identical. SHIPPED.
- **PR-47 (Group-A content-generic lane):** gate 65 (lint-cross-file-section-names) engine transferred via Pattern A, the CROSS_* pair sibling of PR-46. `normalize_title` + `extract_titles` + `resolve_target` + `binding_target` + `adjacent_link` + `title_candidate` + `check_file` (with the tool-specific `TITLE_HEADING_RE`/`PAREN_TITLE_RE`/`QUOTE_TITLE_RE`) moved to `gate_lint_cross_file_section_names.py`, parameterized by `repo_root` + the shared `CROSS_*` config (shared with the numbers-phase sibling gate 62); the wrapper keeps `main` + the grc scan scope + the `CROSS_*` config from `lint_common` + a module-global `check_file` shim. The vestigial unused `HEADING_RE` copy (dead since the CROSS_* hoist) is dropped. New `cross-file-section-names` clause. Byte-identical. SHIPPED.
- **PR-48 (Group-A content-generic lane):** gate 31 (lint-document-date-staleness) engine transferred via Pattern A. The PURE check `get_metadata_date` + `future_lead_days` + `commit_lag_days` moved to `gate_lint_document_date_staleness.py` (source of record); it never touches the filesystem, git, or the clock. The wrapper keeps the OBSERVER and policy inherently project- and environment-specific: the `git log --follow` commit-date lookup, the concurrency pool, the today (UTC) clock, the scan scope, the lag/future/baseline thresholds, the reporting, and the module-global `read_text_safe` its scan-scope test patches; it reads each file and passes TEXT to the engine, and preserves the deliberate two-loop order (the future-date check runs before the git lookup, so a future-dated file with no commit history is still flagged; the lag check runs after). New `document-date-staleness` clause. Byte-identical. SHIPPED.
- **PR-49 (Group-A content-generic lane):** gate 4 (lint-structure, structural index integrity) engine transferred via Pattern A. `parse_referenced_paths` + `iter_domain_files` + `collect_findings` moved to `gate_lint_structure.py`, fully parameterized by `repo_root` + the corpus structure config (domain list, index-register relpath, exempt-from-index set, exempt directory prefixes) so the engine is repository-root-free and corpus-policy-free; the wrapper keeps the grc `DOMAINS` + `INDEX_REL_PATH` + `EXEMPT_FROM_INDEX` + `EXEMPT_DIRECTORY_PREFIXES` + the `--root` handling and grouped reporting. Engine uses only `re` + `pathlib` (no aiqt_corpus dependency). New `structural-index-integrity` clause. Byte-identical. SHIPPED.
- **PR-50 (Group-A content-generic lane):** gate 1 (lint-metadata, the core document metadata-block audit) engine transferred via Pattern A. The PURE check (`extract_metadata` + `check_line_break_markers` + `looks_role_based` + `normalize_link_value` + `check_file`) plus the generic field/link/semver/date regexes moved to `gate_lint_metadata.py`, parameterized by `repo_root` + the metadata model as keyword args. The grc METADATA MODEL (`ALLOWED_TYPES` + `TYPE_TO_PREFIX` + `REQUIRED_FIELDS` + the exempt sets + `ROLE_SUFFIXES` + `ROLE_STEMS` + `LICENSE_VALUE`) stays wrapper-side, because it is the project schema and because the doctype-parity gate (67) reads the allowed types from the wrapper; the wrapper also keeps the scan scope and a module-global `check_file` / `iter_markdown_files` shim the scan-scope test calls. New `document-metadata` clause. Byte-identical. SHIPPED.
- **PR-51 (Group-A content-generic lane):** gate 55 (lint-retention-consistency) engine transferred via Pattern A. `normalise` + `register_value` + `procedure_value` + `collect_findings` plus the generic period regex + unit-to-days table moved to `gate_lint_retention_consistency.py`; the wrapper keeps the grc `REGISTER` path + the `RETENTION_CHECKS` register-category-to-procedure pair list + `PROCEDURE_ANCHOR_RE` + the `--root` handling, register-missing terminal, and OK/FAIL reporting. New `retention-consistency` clause. Byte-identical. SHIPPED.
- **PR-52 (Group-A content-generic lane):** gate 54 (lint-document-control-codes, per-document NIST CSF 2.0 control-code validity) engine transferred via Pattern A. `check_code` + `codes_in` + `scan_file` plus the NIST-CSF label/token regexes moved to `gate_lint_document_control_codes.py`; the CSF catalogue is supplied to the engine as two predicates (`is_valid_category`, `relocation_note`) + `repo_root`, so the engine is catalogue-free. The wrapper imports the shared `nist_csf_reference.py` (also used by gate 49) and keeps `collect_targets` (grc scan scope + matrix exclusion) + reporting. New `document-control-codes` clause. Byte-identical. SHIPPED.
- **PR-53 (Group-A content-generic lane):** gate 58 (lint-document-iso-annex-a, per-document ISO/IEC 27001:2022 Annex A validity) engine transferred via Pattern A, parallel to gate 54. `_check_theme` + `_check_range` + `findings_in` + `scan_file` plus the ISO regexes moved to `gate_lint_document_iso_annex_a.py`; the Annex A reference is supplied as `check_iso_token` + `ISO_ANNEX_A_RANGES` + `repo_root`, so the engine is catalogue-free. The wrapper imports the shared `iso_27001_reference.py` (also used by gate 49) and keeps `collect_targets` (matrix exclusion) + a call-time-`REPO_ROOT` `scan_file(path)` shim + reporting. New `document-iso-annex-a` clause. Byte-identical. SHIPPED.
- **Phase-2b: SKIP.** **Gate-98 (vendored-core digest): CORPUS interim.** **Publication: deferred (P4).**

See `core/manifest.toml` for the machine-readable pack descriptor.
