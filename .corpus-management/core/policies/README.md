# Policy sources

Sources of record for compiler-owned policy text. Each file here is spliced or rendered into its
declared target by the compiler (see `../../gensrc.toml`); edit the source here and regenerate
(`python3 tools/build-corpus-management.py` from the project root), never the generated output.
First transferred policy: `generated-artefacts-note.md` (the CLAUDE.md generated-artefacts
instruction block, transferred verbatim in compile PR-2).
