# Deep-assessment run r2 (2026-07-11)

Second whole-project `/deep-assessment`, maintainer-invoked at the sweep97 `/resume`
(2026-07-11), opened fresh-context this resumed session. Prior run r1 (2026-07-10) was
signed off. This file is the per-run detail record; the durable phase-state row is in
[`register.md`](register.md). Terminates only on explicit maintainer sign-off.

## Phase 1: run state, environment integrity, live inventory (COMPLETE)

- **Run state:** register read at open; r1 = `signed-off`, no `in-progress` run, so r2 is
  opened as a new run row. This dated record file created.
- **Environment integrity (mechanically verified):**
  - Full (non-shallow) clone: `git rev-parse --is-shallow-repository` = `false`.
  - All three repos present and readable: `grc_library` (HEAD `41a20db`), `grc_library_ref`
    (sibling, git, readable), `grc_library_scratch` (sibling, git, readable) per
    `tools/detect-env.py`.
  - Concurrency interlock: lease ACQUIRED this session (`Active-session:
    claude/resume-sweep97-validate` at open, now continuing on
    `claude/deep-assessment-r2-open`; `Status: active`); no live sibling session (the
    remote `claude/*` branches were all merged PR branches predating the #821 release).
- **Live instrument inventory (derived from the repo at run time, not remembered):**
  - Audit gates: **68** (runner `tools/run_all_audits.sh` = 68; spec §6 inventory = 68 rows;
    64 `tools/lint-*.py` scripts plus the build/regression/generator gates).
  - Slash commands: **14** (`.claude/commands/*.md`).
  - Pack skills: **21** (`dev-security/claude-rules/skills/*/`).
  - Governance rules: **13** (`dev-security/claude-rules/governance/*.md`).
  - Advisory tools: **9** (`tools/audit-*.py`): brief-freshness, claim-precision,
    delivery-status, gate-blindspots, gate-mutation, matrix-semantic-fit,
    reference-acquisition-gaps, reference-breadth, register-currency.
- **HEAD SHA at open:** `41a20db` (#822).

## Phase 2: mechanical baseline (COMPLETE)

All standalone, unpiped; all exit 0:

- `tools/run_all_audits.sh`: **all 68 gates pass** (EXIT=0), re-confirmed on `main` at
  `41a20db` post-#822-merge.
- Regression suite (gate 36): green within the runner (361 tests OK, per the Sweep 97
  Subagent C run).
- `tools/run-pr-time-checks.sh` (D1-D7 + history-aware trio): green in the #822 pre-push
  guard.
- `python3 tools/build-taxonomy.py --check`: `OK: taxonomy.yml is in sync.`
- `python3 tools/build-portal.py --check`: `OK: portal and scorecard in sync.`
- `grc_library_ref` `tools/validate.py`: `validation OK` (529 catalogue items, 21 topics,
  no watermark PII, extracts well-formed).
- `grc_library_scratch` `tools/validate.py`: `validation OK` (worker-exchange repo, no
  watermark PII, no style issues), confirmed clean AFTER the scratch cleanup merge
  (scratch #153).
- **Green-at `41a20db` (#822) = 68/68** + all sibling and generator checks clean.
- **Asserted-expectations cross-check:** performed in Sweep 97 (this resume's loop-break
  `/validate` over #790..#821): all corroborated, 0 contradicted; loop-break control for
  #821 PASSED. Sweep 97 is this run's `/validate` of record (the r1 precedent, which used
  its session's Sweep 93 as the `/validate` of record).

## Phases 3-8: overview (SUPERSEDED by the per-phase sections below)

This was the Phase-1-2-open placeholder listing Phases 3-8 as the remaining scope. After
the maintainer chose to continue Phase 3 in-session (see `register.md`), Phases 3-7 were
run and are recorded COMPLETE in the per-phase sections below; Phase 8 HOLDS for sign-off.
This block is retained only for the coverage-obligations and standing-constraint notes that
follow; its "not started" framing is superseded by the sections below (the authoritative
phase state is the `register.md` r2 row). The scope it listed: Phase 3 (semantic
instruments: `/full-qa`, `/fitness`, whole-matrix `/matrix-fit`, Tier-A+sample `/claim-fit`,
FULL `/reference-audit`, `/screen-publications`, `/guardrails`, advisory ledger aids),
Phase 4 (audit-programme audit: blind-spot map, mutation probe on a disposable clone,
dead-gate/parity re-derivation), Phase 5 (ground-truth citation sampling), Phase 6
(adoptability + pipeline + QA-ledger meta-audit), Phase 7 (verify / dedupe / tier / route),
Phase 8 (record + maintainer sign-off).

**Coverage obligations carried:** `/guardrails` (gate-60 cadence due) runs in Phase 3, and
its run is the natural point to land the HELD reconciled **gate-69** branch
`origin/claude/positional-token-gate-69` (rebase onto current `main`, re-bump the library
version + re-number its CHANGELOG entry which hardcodes the superseded #821/`2026.07.309`,
run `/guardrails`, record its history row, merge; closes TODO §3.4).

**Standing constraint:** the §4.8 pack adoption-hygiene programme stays HOLD/maintainer-gated
throughout; no phase of it begins and the `inbox/claude-pack-hygiene/` consolidated scratch
drop is not touched.

## Phase 3: semantic instruments (COMPLETE, priority-sampled; remainder tracked)

The `/validate` of record is this session's **Sweep 97** (loop-break control for #821 PASSED,
0 findings A/B/C, all asserted expectations corroborated). The other instruments ran as
parallel adversarial subagents on a priority slice (the AI gaps-and-expansion workstream
#791-#809 as the largest recent surface, plus the machinery), with the full-worklist
remainders explicitly deferred (no silent cap; the run is re-entrant for a fuller pass).

- **`/full-qa` (forensic AI-pattern pass over #791-#810):** 1 warning (F1 below). Refuted
  clean (held-verified): OSFI E-23 dates, NIST AI 600-1 GV-2.1-001, ISO/IEC 42005 Annex D,
  ISO/IEC 5338 6.4.14, Colorado SB26-189/SB24-205 dates, EU AI Act Art 73 (15/2/10) uniform
  corpus-wide, Art 50/43/3(49), ISO year attributions (no drift), OWASP LLM Top 10 v2.0 /
  AIMA v1.0, AIDA lapsed uniform, OECD "2019 updated 2024", multi-surface presence of the two
  newest docs. Three unverifiable-not-findings recorded (Digital Omnibus detail
  `annex-ai-european-union.md:110`, ISO/IEC 12792 clause structure not deep-read,
  `plan-ai-incident-response` portal absence = generator selection not drift).
- **`/guardrails` (machinery-coherence pass of record):** 1 note (F2). Machinery coherent:
  counts 13 rules / 21 skills / 68 gates / 14 commands consistent across every self-reference;
  §5/§6 map 1:1 (68); no dead machinery; all overlaps deliberate and documented;
  rules-without-skills by design. Cadence-reset token: `inventory 68 gates / 13 rules /
  21 skills / 14 commands`.
- **`/matrix-fit` + `/claim-fit` (AI slice):** 1 note (F5). 0 matrix mismatches / 0
  over-assignments / 0 mis-attributions across ~14 AI matrix rows + ~12 AI claims; EU AI Act
  Art 73/3(49)/43, NIST SP 800-218A tasks, ISO 5259-3/4 / 8183 / 42005 clauses all verified
  verbatim against held text; the EU-AI-Act retention `informed-not-prescribed` phrasing
  (post-#630) intact. Deferred (tracked): ~50 non-AI matrix rows, ~135 non-AI Tier-B claims,
  3 non-AI Tier-A (canonical GDPR/CCPA figures, low risk).
- **`/reference-audit` (FULL worklist, AI slice judged):** 0 new routable findings. One
  confirmed-additive pairing (ISO/IEC 5259-5 data-quality governance framework -> the
  data-quality standard + training-data-governance procedure) DEDUPES to TODO §3.42; ETSI SAI
  dedupes to §3.14; ISO 23053/5339/5469 no-fit/out-of-scope. Deferred (tracked): the
  ~447-candidate non-AI-domain tail + the `jurisdictions/` annex pairings (subsumed under
  §3.42).
- **`/fitness` (three personas):** 3 notes (F3, F4, and a dedupe). Corpus in strong shape;
  engineer + auditor lenses essentially clean (the AI impact procedure called exemplary; the
  compliance matrix credible and version-keyed; the FR-44 `shall`->`must` discipline holds,
  6 justified `shall` corpus-wide). The `TODO.md`/`RESUME.md` adopter-facing-scaffolding note
  DEDUPES to the already-open TODO §3.47.
- **`/screen-publications`:** NOT run this slice (17 `pending` publications in the ref
  `SCREENING.md`). Deferred to the FULL continuation; the standing rule holds (a `pending`
  publication never informs corpus work), so this is not a live risk, only an unrun instrument.

## Phase 4: audit-programme audit (COMPLETE, clean)

- **Blind-spot map** (`tools/audit-gate-blindspots.py`): 0 non-exempt markdown files scanned by
  zero scope-derivable gates; the only gate-blind trees are the known-by-design `.claude/` (1)
  and `.working/` (82), reviewed manually here.
- **Mutation probe** (`tools/audit-gate-mutation.py`, on a DISPOSABLE `--no-hardlinks` clone
  with the `DISPOSABLE-COPY-OK` marker, never the working repo): **11 detected / 0 missed /
  5 clean-pass / 0 false-positive** across language, fences, secrets, links, metadata,
  citations, standards-currency, CCM-column variants. Gate detection width confirmed.
- **Four-surface parity** re-derived clean by Sweep 97 Subagent C (68 gates across workflow /
  runner / pre-commit / spec §6, plus §5 grouped list + §6 detailed-prose + docstring +
  fixture). Dead-gate/parity clean (gates 35/39/64 green).

## Phase 5: ground-truth citation sampling (COMPLETE for the AI slice; remainder tracked)

Covered by the `/full-qa` + `/matrix-fit`/`/claim-fit` held-text verification above (extensive
AI-cluster citations checked verbatim against held reference-base full texts, editions
confirmed against the held catalogue). r1's stratified 8-Tier-A sample (all clean, incl. the
FR-120 class) is the cross-domain baseline. A broader non-AI stratified sample is the deferred
FULL-continuation remainder (tracked; no silent cap). No upstream currency re-check performed
this turn (held-edition + register-stamp basis; the currency SOP gates any actual apply).

## Phase 6: adoptability, pipeline integrity, QA ledgers (COMPLETE)

- **Adoptability** (fitness adopter persona): strong; 2 Low adopter-facing notes (F3 README
  map; the §3.47-dedupe TODO/RESUME scaffolding).
- **Branch protection** (platform-verified via `gh api`): ENFORCED via the active ruleset
  `jposluns/grc_library` (pull_request required + 1 approval, required_status_checks strict on
  "Lint markdown corpus", required_signatures, deletion blocked). The classic-protection
  endpoint's 404 is expected (ruleset-based). r1's F20 (no explicit `non_fast_forward`
  force-push rule; the PR requirement makes direct push impossible) PERSISTS, deduped to r1's
  signed-off finding, not re-routed.
- **Secret / PII scan** over commits since r1's baseline (`4f043f6..HEAD`): clean (the lone
  strict-pattern hit is r1's own record prose citing the canonical `AKIAIOSFODNN7EXAMPLE` AWS
  docs placeholder; no private-key body; r1's full-history scan was clean and no history
  rewrite has occurred since).
- **Guard/hook efficacy:** the RM-10 pipe-guard hook FIRES this session (observed: it blocked
  a piped verification command); the mutation probe confirms the content gates detect at width.
- **QA-ledger meta-audit:** no unsanctioned sham-pass markers in `validate-pr/history.md` (the
  "abbreviated"/"spot-check" grep hits are all legitimate sampling-method or defect-description
  prose); `session-metrics.md` is reverse-chronological with current rows (not stale);
  cross-repo delivery reconcile ran at resume (`audit-delivery-status.py`, 42-delivery review
  set, most APPLIED). Gate 50 (bookkeeping-parity) green (it caught and this run fixed the
  omitted #821 handoff-exemption row in #822).

## Phase 7: verify, dedupe, tier, route (COMPLETE)

Every finding orchestrator-verified before routing (F1 re-read against the held 42001 clause
8.4 TOC/body + the carrier lines; the notes are self-grounded with path:line quotes). Deduped
against the backlog and prior runs. **Finding register (0 error / 1 warning / 4 note):**

| ID | Sev | Location | Finding | Disposition |
| --- | --- | --- | --- | --- |
| **F1** | warning | `ai/framework-ai-model-documentation-and-transparency.md:148` (+6 carriers: `guideline-ai-synthetic-content-provenance.md:89`, `procedure-training-data-governance.md:208`, `register-model-registry.md:147`, `template-dataset-datasheet.md:193`, `standard-ai-data-quality-and-readiness-validation.md:85`, `procedure-foundation-model-lifecycle.md:195`) | ISO/IEC 42001:2023 Clause 8.4 is titled "AI system impact assessment" (held line 1150; TOC 127-128; def 3.24), but these framework-alignment table rows label §8.4 as documentation / transparency / data-controls. The 42001 documentation hook is Clause 7.5 "Documented information"; 8.4 only requires retaining the impact-assessment RESULTS. The bare `checklist-ai-algorithmic-compliance.md:54/74/91` uses are in impact-assessment/DPIA contexts and are DEFENSIBLE (no mislabel). | **In-window fix, HOLDS for maintainer sign-off** (per-row judgment: re-point to Clause 7.5 or an Annex A documentation control, or relabel to "AI system impact assessment"; skeptical verifier on the multi-doc change). |
| **F2** | note | `dev-security/claude-rules/skills/guardrail-review/SKILL.md:50,71` | The skill prescribes routing findings "tagged `[guardrails]`", but the backlog carries 0 literal `[guardrails]` tags and 5 `(rN guardrails, ...)` parentheticals. Traceability preserved (Low). | **ROUTE (maintainer-decision):** reconcile the skill prose to the `(rN guardrails, ...)` convention in use, OR adopt the bracket tag in TODO. Protected pack edit; not auto-applied. |
| **F3** | note | `README.md:145-158` | The Repository-structure map omits git-tracked root items `.working/`, `.project-governance/`, `TODO.md`, `RESUME.md`, though the README claims to orient "as a whole". | **In-window fix candidate** (add the tracked root items to the structure map, or note them as maintainer/working scaffolding). |
| **F4** | note | `governance/framework-human-capital-and-ethical-conduct.md:41` | "operates an anti-bribery management system compliant with ISO 37001:2025", the only unhedged conformance phrase in an otherwise-hedged corpus (voice-consistency). | **In-window fix candidate** (reword to target-state/model prose, e.g. "designed to align with ISO 37001:2025"). |
| **F5** | note | `compliance/financial-services/annex-dora-implementation.md:78` | The DORA major-incident windows (4h / 72h / 1 month) rest on the DORA RTS (Commission Delegated Reg (EU) 2025/302), NOT held in `grc_library_ref`. Correctly attributed to "RTS/ITS" (no phrasing fix). | **ROUTE:** reference-base gap + accepted-unverified tracker (ingest RTS 2025/302 or add a verification tracker). |

**Deduped (no new route):** fitness `TODO.md`/`RESUME.md` adopter-scaffolding -> TODO §3.47
(open); reference-audit ISO/IEC 5259-5 additive pairing -> TODO §3.42 (open); r1 F20
force-push-rule note -> r1 (signed-off).

**Refuted / not routed:** the full-qa refuted-clean list (above); the 3 unverifiable-not-findings
(recorded, not asserted, not routed as defects).

## Extended coverage (2026-07-12, maintainer-directed full-program continuation)

The maintainer chose "Fixes + full coverage now" at the Phase-3-remainder decision, so the
non-AI instrument remainders and `/screen-publications` were run this session (the deferred
FULL set), not left as a re-entrant continuation. Results:

- **`/matrix-fit` non-AI (73 of 84 worklist rows, all non-AI domains):** 0 findings. Every
  non-AI code a defensible fit; the five AICM AI-specific controls on ostensibly-non-AI docs
  are each grounded in an explicit AI section (verified by reading).
- **`/claim-fit` non-AI (8 of 8 Tier-A + 10 value-attribution Tier-B):** 0 new findings. GDPR
  Art 33/12(3), CCPA §1798.130, PIPEDA s.8(3), NIS2 lex-specialis, NIST SP 800-63B/800-52r2/
  800-82r3 all verified prescribed against held text. The lone note is the same DORA
  source-not-held (F5).
- **`/reference-audit` FULL non-AI (497 items / 354 docs, whole set triaged):** 3 additive
  findings, F6-F8 below (held-but-unused authoritative sources; not defects).
- **`/screen-publications` (15 genuinely-`pending` of the register's rows; the brief's "17"
  included 2 already-screened):** all 15 recommend `screened` (clean provenance, clean
  mechanical instruction-content scan (0 candidates on every pending extract), corroboration
  anchors held). 0 quarantined, 0 discard. One caveat: the BCR-P application form has no text
  extract (screened provenance-only; any future extract must be re-scanned). Applied to the
  reference base `publications/SCREENING.md` via a separate cross-repo `grc_library_ref` PR.

**Extended-coverage new findings (F6-F8, reference-breadth; F9-F10, citation-fit from the F1
verifier):**

| ID | Sev | Location | Finding | Disposition |
| --- | --- | --- | --- | --- |
| **F6** | warning | `security/procedure-vulnerability-management.md:182` | ISO/IEC 30111:2019 (vulnerability-handling processes), the dedicated standard for the documented process, held but 0 corpus cites (also ISO/IEC 29147 uncited). | Routed to TODO §3.51 (additive citation; per-touch `/reference-audit` apply). |
| **F7** | note | `security/standard-threat-modelling.md:195` | MITRE CAPEC (attack-pattern enumeration) held, 0 cites; additive to the supplementary-frameworks list. | Routed to TODO §3.51. |
| **F8** | note | `resilience/procedure-backup-and-recovery.md:77` | NIST IR 8374r1 (ransomware CSF profile) held, 0 cites; marginal control-prioritization overlay. | Routed to TODO §3.51. |
| **F9** | warning | `ai/jurisdictions/annex-ai-european-union.md:123` (+AU/CA/CO/NYC) | Transparency-to-affected-persons obligation mapped to 42001 §8.4 (the same F1 misconception); 42001 has no clean transparency clause, correct target debatable. | Routed to TODO §3.52 (maintainer/`/matrix-fit` assessment). |
| **F10** | note | `compliance/matrix-grc-compliance-alignment.md:393-395` | The ISO/IEC 27001:2022 column cites §8.4 (27001 clause 8 has no 8.4); pre-existing citation-existence issue in that column. | Routed to TODO §3.52. |

**Updated finding-set disposition (F1-F10):** F1 FIXED (#825, 7 tables + 2 cross-file carriers);
F2 FIXED (#826, guardrail skill reword, closes §3.49); F3 FIXED (#826, README structure map);
F4 FIXED (#826, ISO 37001 hedge); F5 routed (§3.48, DORA RTS ingest pending maintainer); F6/F7/F8
routed (§3.51, reference-breadth additions); F9/F10 routed (§3.52, 42001/27001 §8.4 citation-fit);
15 publications `screened` (ref SCREENING.md PR). The maintainer-directed missing-reference-document
SOP was codified in `.claude/CLAUDE.md` (#826) and its pack distribution routed (§3.53). **Net: the
corpus is near-clean; every warning-tier finding (F1 fixed, F6/F9 routed) and every note is
dispositioned; no error-tier finding in the whole run.**

## Phase 8: record and HOLD for maintainer sign-off (in-progress)

This record written; register row r2 updated (P1-P7 complete, P8 in-progress). The finding set
is surfaced to the maintainer, tiered, for sign-off. **The run HOLDS: it does not self-close on
this near-clean set.** Per the deep-assessment discipline, an empty-or-near-empty set is still
presented for explicit sign-off. The in-window fixes (F1 corpus fix; F3/F4 candidates) and the
coverage-obligation gate-69 merge are proposed but NOT shipped before sign-off, because F1 is a
7-carrier semantic corpus change worth maintainer awareness of the approach, and the run holds
by design. The gate-60 cadence-reset row (this run being the `/guardrails` pass of record) and
the routing of F2/F5 ship with this record PR.

**Coverage honesty (what this r2 did NOT exhaustively cover):** the semantic instruments were
priority-sampled to the AI slice + machinery; the non-AI matrix/claim/reference-breadth
remainders and `/screen-publications` are the tracked FULL-continuation set (the run is
re-entrant via the register). This is disclosed, not silently capped.
