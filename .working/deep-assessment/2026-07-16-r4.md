# Deep-assessment run r4 (2026-07-16) - PARTIAL, re-entrant

**Invocation:** maintainer-directed 2026-07-16 (VM, unattended) - "have the B worker start a deep-assessment ... we've made enough changes lately that there's potential for it to find things."

**First OFFLOADED deep-assessment.** The read-only probe phases (1-6) ran on credit-offload worker `worker-20260716-b` (Opus 4.8) as scratch order `deep-assessment-r4-probe`, pinned to `grc_library` `1246607b` (post-#981) and `grc_library_ref` `b15aa1f`. The worker used its own non-shallow clone (`/tmp/grc_library_working_b`), verified read-only. Phases 7-8 are orchestrator/maintainer; the worker did NOT route, dedupe, write the register, or self-close. The orchestrator re-verified every finding at source before recording.

**Change window assessed:** grc_library #969-#981 + reference-base #85/#86 (the credit-offload machinery + metrics tab, the CCPA §2.23 regulations-alignment, the Quebec Law 25 corrections, the EDPB/Brazil/LatAm ingests).

## Per-phase completion

| Phase | Status | Outcome |
| --- | --- | --- |
| 1 run-state + env + live inventory | COMPLETE | Non-shallow clone verified; all three repos present; HEAD recorded. Live inventory (derived, not remembered): 69 audit gates (per `run_all_audits.sh`), 14 commands, 24 pack skills, ~90 tools. |
| 2 mechanical baseline | COMPLETE (1 finding) | Corpus GREEN 69/69 + PR-time delta (window 37abc69..1246607b) + linter regression + both generators `--check` + scratch `validate.py` all exit 0. **Reference-base `validate.py` RED at `b15aa1f` = F-P2-1.** |
| 3 semantic instruments | NOT-STARTED (deferred whole) | `/validate`, `/full-qa`, `/fitness`, `/matrix-fit`, `/claim-fit`, `/reference-audit`, `/screen-publications`, `/guardrails` deferred rather than run abbreviated (the no-abbreviation rule). |
| 3 advisory aids | PARTIAL | `verify-reference-modules.py` CLEAN (CCM v4.1.0 / AICM v1.1.0 / NIST CSF 2.0 / COBIT 2019 / ISO 31000:2018 all match); `tension-scan` judged 1 flagged block honest (no contradiction); `audit-brief-freshness.py` -> OBS-1; `residual-scan` deferred (needs finding tokens). |
| 4a blind-spot map | COMPLETE | `audit-gate-blindspots.py` exit 0: no un-accounted unscanned surfaces beyond the by-design exempt `.claude/` (1) + `.working/` (153) trees (manual review of those deferred). |
| 4b mutation probe | NOT-STARTED | Needs a disposable copy outside a live checkout; deferred. |
| 4c dead-gate + coverage | NOT-STARTED | Deferred (clone is non-shallow, feasible on resume). |
| 4d parity re-derivation | NOT-STARTED | Deferred (the gate-parity gate itself passed inside the 69; the by-hand re-derivation is deferred). |
| 5 ground-truth sampling | NOT-STARTED | Heavy adversarial-reader fan-out; deferred. |
| 6 adoptability / pipeline / ledgers | NOT-STARTED | Heavy; deferred. |
| 7 verify / dedupe / tier / route | IN-PROGRESS | F-P2-1 + OBS-1 verified at source and routed (below). |
| 8 record + sign-off | HOLDS | Presented for maintainer sign-off; the run does NOT self-close and stays in-progress (phases 3/4b-d/5/6 outstanding). |

## Findings (orchestrator re-verified at source)

- **F-P2-1 (Medium / warning) - reference base gate-RED, FIXED.** `grc_library_ref/tools/validate.py` failed at `b15aa1f` with `[orphan-csv] ingest/gc-ai-register-mvp-registre-de-lia-du-gc-pmv-04-26 (1).csv: CSV extract in a directory with no catalogue presence`. Orchestrator re-verified at source (the file was git-tracked at `b15aa1f`, working tree clean, 0 catalogue entries). Origin: the GC AI Register MVP dataset CSV (maintainer upload) landed in `ingest/` uncatalogued. **FIXED in `grc_library_ref` PR #87** (catalogued the CSV as a dataset under `programs/Canada-AI-Register/`, 412 records); ref `validate.py` now green (696 items).
- **OBS-1 (Low / advisory; scratch-side, not a corpus defect).** `audit-brief-freshness.py` flagged 5 `research/COVERAGE.md` row anchors in `grc_library_scratch` pointing at TODO headings that shifted/closed (§5.1, §2.12, §2.13, §3.17->3.15, §3.24). Advisory; folded into scratch housekeeping (the COVERAGE Korea-eligibility row is also stale, corrected when the §2.18 seed applies). Not a corpus defect.

**Clean confirmations (worker, orchestrator-spot-checked):** corpus green 69/69; reference modules match source extracts; QA ledgers honest (tension-scan found no contradiction); no blind-spot gap.

## Resume note (continuation)

Enqueue the continuation (phases 3 instruments, 4b/4c/4d, 5, 6) as a fresh worker order AFTER the §2.22 Canada corpus apply lands, so the heavy semantic pass assesses the post-Canada corpus (the current high-change area). A non-shallow clone is required (a full non-blobless clone is preferable for 4b/4c/6 full-history scans). The run is re-entrant via the register; Phase 8 holds for maintainer sign-off on the complete finding set.

---

## Continuation delivered 2026-07-17 (worker-20260716-b, elevated delivery-2; orchestrator phase-7 re-verified)

The heavy semantic phases (3 instruments, 4b/4c/4d, 5, 6) ran as scratch order
`deep-assessment-r4-continuation`, pinned to `grc_library` `80499055` (#986), before the
§2.22 Canada apply (so absence of deep Canada engagement is caveated, not a defect).
SARIF-lite source: `grc_library_scratch` `results/deep-assessment-r4-continuation.md`.
Consumed under the elevated-QA window (worker-b delivery-2): mechanical proof-of-run
re-derived (corpus 69/69 at #986 modulo the assessing-behind-origin gate-13 artefact;
mutation probe 15/0/5/0; regression 0 dead gates), and every corpus finding re-verified at
held source by the orchestrator.

**Orchestrator re-verification results (phase 7):**
- **W1 CONFIRMED** at held NIST SP 800-52r2 section 3.3.1.2 (lists only the four AES suites;
  ChaCha20 absent). Routed to TODO §1.17(a).
- **N2 CONFIRMED** at the held CSF 2.0 core CSWP-29 (DE.CM = 01/02/03/06/09, RS.CO = 02/03;
  DE.CM-04/-07 and RS.CO-01 are 1.1-legacy). Gate-blind: gate 49 validates the CSF category,
  not the subcategory number. Held-source nuance: the ref-base OSCAL catalogue appears to carry
  the numbers (likely predecessor/mapping links); CSWP-29 is authoritative. Routed to §1.17(b) +
  a ref-base OSCAL-vs-core note.
- **W2 CONFIRMED at held source** (corrected 2026-07-17 after the maintainer flagged that ISO/IEC
  27002:2022 IS held; an earlier partial-grep wrongly concluded not-held): the held standard
  (`grc_library_ref` `standards/ISO/ISO-IEC-27002-2022--Information-security-controls--full-text.md`)
  has 8.15 "Logging" (no 8.15.3 sub-clause) and 8.17 "Clock synchronization", so the corpus's
  `operations/procedure-security-monitoring-and-alert-management.md:331` "8.15.3" for time
  synchronization is wrong. Fix: 8.15.3 -> 8.17. Routed to §1.17(c) with W1/N2.
- **W3 CONFIRMED** (`.working/credit-offload-design.md` status line stale vs the metrics ledger,
  gate-exempt coherence gap); low-risk prose refresh, routed (fix on sign-off).
- **W4** = already TODO §3.84 (Quebec Law 25 PIA section). **W5** (AICPA TSC edition mislabel) +
  the **OSCAL-vs-CSWP-29 discrepancy** route to the reference-base maintainer.
- **W6 / N1 / N3-N12:** candidates and low notes recorded (route-everything); the Canada
  reference-breadth candidates (N4 PIPEDA principles, N5 BC PIPA) carry the §2.22-deferral caveat.
- **Dedup:** F1 (APO14 "Managed AI") already fixed in #987; not re-routed. **Refuted:** A2/A4 tool
  false-positives.

Phases 1-7 are now COMPLETE. **Phase 8 HOLDS for explicit maintainer sign-off** on the complete
r4 finding set (probe + continuation); the run does not self-close. On sign-off, §1.17(a)+(b) are a
single skeptical-verifier citation-fix PR; (c) awaits the ISO source; the ref-base items go to a
`grc_library_ref` PR; W3 is a working-state refresh.
