# Deep-assessment run r5 (2026-07-18)

Fifth `/deep-assessment`, maintainer-invoked 2026-07-17b (VM, overnight-unattended), the
second OFFLOADED run. Read-only probe phases on credit-offload worker
`worker-20260716-a` (Opus 4.8), scratch order `deep-assessment-r5`, pinned to
`grc_library` `a42a2a0b` (#1006) and `grc_library_ref` `d1e3063`. Worker result of record:
`grc_library_scratch` `results/deep-assessment-r5.md` (~480K tokens; 3 read-only probe
subagents + inline phases). Orchestrator does Phase 7 (routing, this record) and Phase 8
holds for maintainer sign-off.

## Scope note

The Phase-3 FORMAL semantic instruments (`/validate`, `/full-qa`, `/fitness`,
`/matrix-fit`, `/claim-fit`, `/reference-audit`, `/screen-publications`, `/guardrails`)
each write their own `grc_library` record, so they are NOT worker-offloadable; the worker
ran their READ-ONLY deterministic halves + advisory aids and the mechanical baseline they
gate on. Phase 8 (sign-off) is the maintainer's.

## Finding set (all orchestrator-re-verified at source)

### Medium 1 (CONFIRMED) - CSA CCM SEF-02 mislabelled "Incident Management"

- Corpus: `security/sop-incident-escalation-matrix.md:97`,
  `| CSA CCM v4.1 | SEF-02: Incident Management | Cloud incident escalation |`.
- Held source: CSA CCM v4.1.0 catalogue CSV `:601` -> SEF-02 = **"Service Management
  Policy and Procedures"** (an ITSM control). The incident controls are SEF-01
  "Security Incident Management Policy and Procedures" (`:597`), SEF-06 "Event Triage
  Processes" (`:615`), SEF-07 "Incident Management and Response" (`:618`).
- **Orchestrator re-verification (2026-07-18):** read the corpus line (verbatim as
  quoted) and grepped the held CSV; SEF-02 title confirmed = "Service Management Policy
  and Procedures"; the four SEF incident rows confirmed at :597/:615/:618/:622. CONFIRMED.
- Failure scenario: an auditor cross-walking this incident-escalation SOP to CSA CCM is
  pointed at a service-management control; the SOP's subject has no correct CCM anchor.
- **Routing: morning-review decision** (`pending-decisions.md`). The correct replacement
  is a semantic-fit call on an expert-review-facing table, so it is NOT applied
  unattended. **Recommendation: SEF-07 "Incident Management and Response"** (the direct
  incident-handling anchor for an escalation SOP); **alternative SEF-06 "Event Triage
  Processes"** (the triage/escalation angle). Maintainer chooses.
- Dedupe context: the same code (bare, no wrong title) appears at
  `governance/register-document-index-and-classification.md:260`; if the remap changes the
  code (not just the title), that carrier is updated in the same change.

### Low 1 (CONFIRMED) - COBIT "Manage X" vs source "Managed X"

- Corpus: `governance/framework-sustainability-and-responsible-technology.md:97`
  (APO02), `governance/framework-human-capital-and-ethical-conduct.md:113` (APO07),
  `governance/procedure-continuous-improvement-register.md:142` (APO11) use the
  imperative "Manage X"; the COBIT 2019 source uses the outcome-form "Managed X".
- **Routing: folded into §1.16** (the COBIT title-normalization + title-text gate,
  maintainer-approved to normalize to past-participle overnight, guard-first). §1.16's
  corpus-wide re-derive grep covers these three PLUS the DSS02 same-row instance the
  orchestrator spotted at `security/sop-incident-escalation-matrix.md:96`
  ("DSS02: Manage Service Requests and Incidents").

### Low 2 (noted) - hallucination-metrics ledger stale

`.working/hallucination-metrics.md` reads "as of PR #896"; pinned SHA is #1006. The file
self-declares opportunistic update and its note explains the counts legitimately have not
moved (recent sessions dispatched QA-reviewer subagents, not content-drafting workers).
Known/tracked; a Low refresh reminder, no new tracker.

### Low 3 (robustness) - gate 61 uncaught ValueError on an external-path arg -> §3.98

`tools/lint-cobit-iso31000-citations.py:212` calls `path.relative_to(REPO_ROOT)` unguarded
in the findings-print loop; a target outside `REPO_ROOT` with a finding raises ValueError.
Cosmetic (never triggers in the runner/CI, which pass repo-relative paths). New TODO §3.98.

### Low 4 (latent) - parity gate exclusion allow-lists + PR-only delta gates unguarded -> §3.99

The four-surface parity re-derivation reconciles 70/70/70/70, but (i) the three hardcoded
exclusion allow-lists (`WORKFLOW_DELTA_GATE_STEPS`, `WORKFLOW_SETUP_STEPS`,
`PRECOMMIT_NON_GATE_HOOKS`) are themselves unguarded (a real gate mis-added to an exclusion
set is masked); (ii) the 8 PR-only delta gates have no cross-surface parity check on
naming/wiring. No current defect; new TODO §3.99.

### Low 5 (informational, cross-repo-unverifiable)

(a) The `.web/` landing page deploys to Cloudflare Pages but no deploy workflow lives in
`.github/workflows/` (only the `--check` health probe), so the production-deploy hardening
cannot be assessed from this repo. (b) Clean-SHIP `validate-pr` rows cite cross-repo
scratch `results/` evidence, unverifiable from within `grc_library` (findings-producing
evidence lands in-repo and was verified: 59 of 59 detail files intact). Informational.

## Clean surfaces (worker proof-of-run, mechanical facts spot-re-derived by the orchestrator)

- Mechanical baseline: `run_all_audits.sh` 70/70; regression suite 412 tests OK; both
  generators `--check` rc 0; both sibling `validate.py` rc 0.
- Phase 4: blind-spot map 0 uncovered non-exempt files; mutation probe 15 detected / 0
  missed / 5 clean-pass / 0 false-positive (disposable clone, `DISPOSABLE-COPY-OK`); dead-
  gate pass 0 genuine dead gates; four-surface parity 70/70/70/70, 0 ordered mismatches.
- Phase 5: 26 citations sampled across 10 source families / 10 domains, 24 correspond /
  2 mismatch (Medium-1 + Low-1) / 0 unverifiable. Cross-doc coherence spot-checks clean.
- Phase 6: adopter on-ramp resolves; toolchain stdlib-only (no non-stdlib imports); full-
  history secret scan CLEAN; PII watermark CLEAN (no licensee watermark; surname hits are
  intentional public attribution); guard/hook defences wired; QA-ledger meta-audit HONEST
  (809 rows, 59/59 detail files resolve, sampled clean-SHIP rows carry concrete proof-of-
  run, 0 sham).
- Orchestrator elevated-QA re-derivation: gate count = 70 (matches); Medium-1 confirmed at
  held source; the worker's proof-of-run is genuine (path:line + disposable-clone mutation
  probe + SHA-reachability ladder), so the clean/zero-finding results are trusted.

## Phase 8

HOLDS for explicit maintainer sign-off. The run does NOT self-close on a near-clean set.
Presented in the morning-review (the `pending-decisions.md` 2026-07-17b batch carries the
Medium-1 remap decision).
