# FR-48 deferred worklist (entangled docs)

**Date:** 2026-07-01\
**License:** CC BY-SA 4.0

The 38 policy/standard documents that FR-48's safe-subset pass (PR #520) deferred because their section numbers are cited, self-referenced, or coupled to inline prose clause numbers, so a renumber to the canonical fully-numbered model (`## N.` H2 + hierarchical `### N.M` H3) needs a **lockstep citation/clause remap** rather than the purely-additive change the 28 safe docs received. This file is the starting worklist for the dedicated effort TODO §1.1 describes; it is maintainer working state, exempt from corpus audit gates.

**Split note.** The corpus's 66 non-fully-numbered policy/standard docs divide: 28 safe (normalized in PR #520) + 38 deferred (below). The last 5 of the deferred set were initially applied in PR #520 and then **reverted** when the pre-push skeptical verifier caught the inline-clause-numbering coupling (DEFECT 2) plus one missed bare-parenthetical inbound citation (DEFECT 1); they are restored to their pre-FR-48 state and listed here.

**Entanglement forms** (why each doc is deferred):
- **inbound `§N`** / **inbound "Section N"**: another live document cites this doc's section by number (a renumber shifts the target; the citer must be updated in the same PR). Both markdown-link (`[..](doc) §N`) and bare-parenthetical (`(doc §N)`) forms count. "Section N" citers include the [`dev-security/claude-rules/languages/`](../dev-security/claude-rules/languages/) pack files for the mobile-app standard.
- **intra `§N`** / **intra "Section N"**: the doc references its own sections by number in body prose (the reference must be remapped when the headings renumber).
- **inline clause numbering**: the doc's body carries numbered prose clauses (`3.1 Internal Audit must...`, `4.3 CAPA effectiveness must be validated...`) keyed to the OLD local H3 numbering; renumbering the headings without realigning these inline clauses (and any external citation of them, e.g. `procedure-capa.md` -> `policy-compliance-and-audit-management.md §4.3`) breaks the heading<->clause correspondence. The remap must renumber the inline clauses in lockstep with the headings.
- **uses "## Section N:" labels**: the doc's H2s are `## Section N: title`; the strip-and-renumber changes the section number the "Section N" scheme exposes, so it is unsafe wherever that doc is also cited or self-references.

**Build-time discipline.** Recompute the inbound citers at build time (they drift as the corpus changes); for each doc, produce the old-section-number to new-`§N` map and apply it to every citer (corpus-wide grep, not the doc's own input set) in the same PR, then re-parse. **Inbound-discovery recipe (#524 lesson):** discover inbound citers with a LINE-LEVEL grep over the full corpus (`grep -rnE '<basename>' --include='*.md' . | grep -E '§|Section [0-9]|section [0-9]'`) plus a separate `<basename>.md#` anchor grep, never a proximity-bounded (character-window) pattern: a narrow window can return a false-negative when a `§N` sits far from the doc name in a table row (the #524 near-miss, where the first narrow scan missed `register-coverage-gaps §5`). Then distinguish an external-standard `§N` (e.g. `ISO/IEC 42001 §8`, which belongs to the cited standard, not the target doc) from a citation of the target doc's own section, and also run the exhaustive bare-numeric scan (`N.M`, "requirement/section/tier N") for docs whose numbers SHIFT rather than merely gain a prefix. **Inline-clause detection (#527 near-catch):** before classifying a doc as a safe prefix-add, scan the body for plain-text or bold numbered prose clauses (`grep -nE '^\*?\*?[0-9]+\.[0-9]'`) keyed to the section numbers (e.g. `1.1 Data Owners are responsible...` under `## 1.`); when present, the doc is the inline-clause hard class (the clauses must renumber in lockstep with the headings, and any external citation of a clause `§N.M` remapped), not a prefix-add. The near-catch: `security/standard-data-classification-and-handling.md` was carried as "intra §N" but a pre-renumber body scan found inline clauses `1.1`-`7.4` under a hybrid H2 scheme, so it was retagged inline-clause and deferred rather than renumbered. **Bare-external-§ disambiguation (#528 gate catch):** the intra-document-reference gate (`tools/lint-intra-doc-refs.py`) SKIPS a doc with no numbered headings, so a previously-unnumbered doc's OWN bare external `§N` cells (typically a framework-alignment table whose standard name lives only in the column header, e.g. `| Password requirements | §5.1.1 | ...` under a `NIST SP 800-63B` column) are never resolved before the renumber but collide with the new intra-document numbering the moment the doc is numbered, failing the gate. Before committing a renumber of a previously-unnumbered doc, grep the body for bare `§N` cells (`grep -nE '§[0-9]'`) whose LINE does not itself name the external standard, and disambiguate each inline (`SP 800-63B §5.1.1`, matching the corpus convention e.g. `standard-logging-and-monitoring.md`'s `SP 800-92 §4.2` cell) in the same PR. This is a lockstep fix the renumber requires, not scope creep. `run_all_audits.sh` on the committed state is the backstop that catches it if the pre-commit grep misses it (as it did in #528, caught then fixed pre-push). Shape (maintainer directive 2026-07-01, supersedes the earlier "one domain-batch per PR" framing): **one document per PR** with maximal QA, no mass change until certain the renumber leaves zero broken links or references anywhere; each PR renumbers exactly one deferred document plus the citer files its inbound section-number references require. The numbering itself is gate-blind (gate 38 strips numbering when matching), so this is editorial consistency, not error-prevention. **H3-numbering convention (maintainer decision 2026-07-01, option B): number ALL H3s hierarchically (`### N.M`), including descriptive/unnumbered ones (e.g. `### Security and vulnerability testing` under `## 6.` becomes `### 6.1`), not only already-enumerated ones.** This resolves the earlier open A/B question (option A would have left descriptive H3s unnumbered); apply B to every remaining doc. The 5 docs merged before this decision already comply (their H3s were numbered or absent), so no retrofit is required. **H4 numbering (maintainer decision 2026-07-01): number ALL H4s hierarchically too (`#### N.M.K`), including descriptive ones** (e.g. `#### Minimum SBOM content` under `### 4.4` becomes `#### 4.4.1`), consistent with option B extended to the fourth level. **Section-N-label titles (maintainer decision 2026-07-01): when a doc uses `## Section N: lowercase title` H2 labels, convert to canonical `## M. Title` where M is the H2's DOCUMENT-ORDER position (not the old "Section N" number, which usually differs), and CAPITALIZE the stripped title to Sentence-case to match sibling canonical H2s** (e.g. `## Section 1: records retention schedule`, if it is the 5th H2 in document order, becomes `## 5. Records retention schedule`). **Enumerated `### Category N:` (and similar) H3 labels cross-referenced in the body (maintainer decision 2026-07-01, PR #542, Option 1): treat them exactly like Section-N labels one level down.** Strip the `Category N:` prefix, keep the descriptive title, renumber hierarchically to `### M.K` (M the parent H2's document-order position, K the H3's order), and remap every body cross-reference of the old category number to the new `§M.K` in lockstep (e.g. `### Category 2: Incident signals` under `## 3.` becomes `### 3.2 Incident signals`, and a tier-posture cell "Categories 2 and 4 at minimum" becomes "§3.2 and §3.4 at minimum"). Generic prose ("five categories", "each category") with no number attached is not a cross-reference and stays.

## Deferred documents (recompute reasons at build time)

- [x] `ai/standard-ai-access-and-agent-permissions.md` -- intra §N, uses "## Section N:" labels (DONE: Section-N-label doc with an already-hierarchical Section 4 that SHIFTED to document-position 6; 12 H2s -> `## 1.`-`## 12.` (Purpose/Scope 1-2, `## Section 1:`-`## Section 8:` stripped+Sentence-cased+document-order `## 3.`-`## 10.`, Framework/Limitations 11-12); Section 4 -> `## 6.` so its sub-hierarchy shifted `### 4.1`-`### 4.6` -> `### 6.1`-`### 6.6` and `#### 4.1.1`/`#### 4.3.1` -> `#### 6.1.1`/`#### 6.3.1`; verified NO intra-doc reference to the old 4.x numbers exists in body prose, so headings-only; the "intra §N" tag was conservative, only §-ref is external `ISO/IEC 42001 §8`; zero live inbound §/Section/anchor citers, so no citer remap)
- [x] `ai/standard-ai-and-agentic-development-security.md` -- inbound §N, intra §N (DONE #547: the numbered H2s `## 1.`-`## 35.` were ALREADY canonical and did NOT shift, so no intra `§N` ref (§3, §8, §10, §20-§24, §28, §32, etc.) and no inbound `§N` citer (§21, §22, §23, §33 in the coverage-gaps + tooling-landscape registers) needed remapping. The renumber was 9 changes: `## Framework alignment` -> `## 36. Framework alignment`, and 8 descriptive H3s numbered per option B (`### 9.1`-`### 9.4` under AI-assisted development controls, `### 21.1`-`### 21.2` under Security testing requirements, `### 29.1`-`### 29.2` under Incident detection and response). No H4s. Worklist "inbound/intra §N" tag was pessimistic: the refs exist but point at unchanged sections.)
- [x] `ai/standard-ai-inference-cost-governance.md` -- intra "Section N", intra §N, uses "## Section N:" labels (DONE: Section-N-label doc with one intra "Section N" self-ref; 14 H2s -> `## 1.`-`## 14.` (Purpose/Scope 1-2, `## Section 1:`-`## Section 10:` stripped+Sentence-cased+document-order `## 3.`-`## 12.`, Framework/Limitations 13-14); the one numbered H3 `### 3.1` under Section 3 (->§5) shifted to `### 5.1`; the one intra self-reference "Monitored per Section 5" (old Section 5 "monitoring and anomaly response" -> document-position §7) was remapped in lockstep to "per §7"; the "intra §N" tag's §-hits are external `ISO/IEC 42001 §8/§9`; zero live inbound §/Section/anchor citers, so no citer remap)
- [x] `ai/standard-ai-model-risk.md` -- inbound §N (DONE: renumbered to canonical fully-numbered; inbound `register-coverage-gaps.md` §5 -> §3.5 remapped)
- [x] `ai/standard-ai-security-and-risk.md` -- intra §N (DONE: hybrid unnumbered-H2 + locally-numbered-H3 doc whose OWN crosswalk table cites its §1-§11; 6 H2s prefix-added -> `## 1.`-`## 6.` (Minimum requirements is the 4th -> `## 4.`); the 11 H3s `### 1.`-`### 11.` -> `### 4.1`-`### 4.11`; the 11 crosswalk cells `§1`-`§11` remapped in lockstep to `§4.1`-`§4.11` (the intra "sibling-crosswalk §1-§11" self-refs, cited by this doc's own section numbers per its line 42); the Evidence crosswalk row cites by NAME (no §), unchanged; no inbound citers (the privacy-procedure ref is path-only + external ISO 27701 §8.9, the `.working` hits are frozen historical), no anchors, no inline clauses)
- [x] `ai/standard-ai-testing-validation-and-documentation.md` -- intra §N (DONE: renumbered to canonical fully-numbered, hybrid 1-9 with numbers shifting 1->4/2->5/3->6/4->7/5->8 and `### 2.x`->`### 5.x`; the 2 "§8" refs are external ISO/IEC 42001 citations, not self-refs; zero inbound section citations and zero bare numeric cross-refs, so no citer remap needed)
- [x] `architecture/standard-integration-architecture.md` -- intra "Section N", uses "## Section N:" labels (DONE: Section-N-label doc with TWO intra "Section N" self-refs; 19 H2s -> `## 1.`-`## 19.` (Purpose/Scope 1-2, `## Section 1:`-`## Section 14:` stripped+Sentence-cased+document-order `## 3.`-`## 16.`, Operating expectations/Framework/Limitations 17-19); no H3/H4; the two intra self-refs "(Section 3)" (integration classes -> document-position §5) and "Per Section 8" (webhooks -> document-position §10) remapped in lockstep to "(§5)"/"Per §10"; zero live inbound §/Section/anchor citers (the only corpus mentions are the FR-48 worklist/CHANGELOG prose naming it as a deferred example), no bare-§ cells (framework table cites ISO 27001 Annex A), so no citer remap)
- [x] `architecture/standard-technology-radar.md` -- intra "Section N", uses "## Section N:" labels (DONE: Section-N-label doc with TWO intra "Section N" self-refs; 15 H2s -> `## 1.`-`## 15.` (Purpose/Scope 1-2, `## Section 1:`-`## Section 10:` stripped+Sentence-cased+document-order `## 3.`-`## 12.`, Operating expectations/Framework/Limitations 13-15); no H3/H4; the two intra self-refs in the blip-structure table "Per Section 3" (quadrants -> document-position §5) and "Per Section 2" (rings -> document-position §4) remapped in lockstep to "Per §5"/"Per §4"; the compliance-matrix `§6.1` on this doc's row is ISO/IEC 27001 clause 6.1 (external; the ISO column's basis is "Annex A control numbers and §6 to §10 clauses" per the matrix legend, and A.5.20/A.8.30 on the same cell are ISO Annex A controls), NOT a citation of this doc's Section 6, so no citer remap; zero anchor links, no bare-§ cells)
- [ ] `compliance/standard-internal-audit.md` -- inbound §N, intra "Section N", intra §N
- [x] `dev-security/standard-api-security.md` -- intra §N, uses "## Section N:" labels (DONE: Section-N-label doc, 17 H2s -> `## 1.`-`## 17.` + 1 descriptive H3; Purpose/Scope prefix-add 1-2, its `### Relationship to the API design standard...` H3 -> `### 2.1`, the 12 `## Section 1:`-`## Section 12:` labels stripped+Sentence-cased+document-order `## 3.`-`## 14.`, Operating expectations/Framework/Limitations 15-17; the "intra §N" tag was conservative, the two body §-refs are external/other-doc (`encryption-and-key-management §1` is an OUTBOUND ref to another doc, `NIST SP 800-52 §3.3.1` external), NOT self-refs; zero live inbound §/Section/anchor citers, so no citer remap)
- [x] `dev-security/standard-developer-security-requirements.md` -- inbound §N, intra §N (DONE #546: hybrid-shift renumber (`## Purpose` -> `## 1.`, old `## 1.`-`## 17.` -> `## 2.`-`## 18.`, `## Framework alignment` -> `## 19.`); 23 H3s renumbered (`### 1.1`-`### 1.3` -> `### 2.1`-`### 2.3`; `### 2.1`-`### 2.6` -> `### 3.1`-`### 3.6`; 4 descriptive H3s under old §10/now §11 -> `### 11.1`-`### 11.4` per option B; `### 11.1`-`### 11.4` -> `### 12.1`-`### 12.4`; 6 descriptive H3s under old §14/now §15 -> `### 15.1`-`### 15.6`); intra self-ref `§5` -> `§6` (Input validation, line 256); the `§1` on line 153 is an external cross-ref to the encryption policy's `## 1. Encryption standards`, left. Two live inbound citers of `§9` (Dependency and open-source governance = hallucinated-package coverage) remapped to `§10`: [`governance/register-ai-security-tooling-landscape.md`](../governance/register-ai-security-tooling-landscape.md) line 388 and [`dev-security/guideline-ai-coding-assistant-security.md`](../dev-security/guideline-ai-coding-assistant-security.md) line 215 (SCA `§4.5` on the same guideline line preserved); both citers' Version co-bumped. CHANGELOG-referenced `§9`/`:151` mentions left as frozen historical.)
- [x] `dev-security/standard-devops-security-requirements.md` -- intra §N (DONE: renumbered to canonical fully-numbered per option B; hybrid-SHIFT (Purpose/Framework were unnumbered while `## 1.`-`## 11.` were numbered), so all H2s shifted into document order `## 1.`-`## 13.` (Purpose->1, old 1->2 ... old 11->12, Framework->13); 6 `### 1.x` H3s -> `### 2.x`, 7 descriptive H3s numbered `### 3.1`-`### 3.2`, `### 4.1`-`### 4.2`, `### 8.1`-`### 8.3`; the one intra self-reference `per §1.2` (body) was remapped to `per §2.2` in lockstep with CI/CD shifting §1->§2; zero inbound section citations, zero anchors, so no citer remap)
- [x] `dev-security/standard-mobile-application-security.md` -- inbound "Section N", intra "Section N", uses "## Section N:" labels (DONE #548: Section-N-label renumber, document-order: `## Purpose`/`## Scope` -> `## 1.`/`## 2.`, `## Section 1:`-`## Section 14:` labels stripped+Sentence-cased -> `## 3.`-`## 16.`, `## Operating expectations`/`## Framework alignment`/`## Limitations` -> `## 17.`-`## 19.`; no H3/H4. 3 intra "Section N" self-refs -> §N (line 216 "Sections 2 through 10" -> "§4 through §12"; line 220 "Section 2" -> "§4"; line 222 "Section 8" -> "§10"). The 7 claude-rules pack files that cite the standard by "Section N" prose (README language-index rows + 6 rule files [`languages/react-native.md`](../dev-security/claude-rules/languages/react-native.md), [`flutter.md`](../dev-security/claude-rules/languages/flutter.md), [`dotnet-maui.md`](../dev-security/claude-rules/languages/dotnet-maui.md), [`capacitor-ionic.md`](../dev-security/claude-rules/languages/capacitor-ionic.md), [`swift.md`](../dev-security/claude-rules/languages/swift.md), [`kotlin.md`](../dev-security/claude-rules/languages/kotlin.md)) had all 129 "Section N" references renumbered to the new section numbers, KEEPING "Section" wording per maintainer Option A (2026-07-02) (e.g. Section 13 -> Section 15, the enumerated native-layer lists (2,3,4,5,6,7,9,10,14) -> (4,5,6,7,8,9,11,12,16)). swift.md + kotlin.md were initially missed (too-narrow inbound grep) and caught by the pre-push verifier; correct discovery is `grep -rl` for any file referencing the standard.) Pack README Version co-bumped (1.53.9 -> 1.53.10) with a version-history row. Single-pass regex remap with a total map-guard (errors on any unexpected number).)
- [x] `dev-security/standard-quality-assurance-and-testing.md` -- intra §N (DONE: renumbered to canonical fully-numbered, 8 unnumbered H2s -> `## 1.`-`## 8.` + 3 descriptive H3s under `## 6.` -> `### 6.1`-`### 6.3` per the option-B H3 convention; the "intra §N" tag was conservative, the only §-ref is external `ISO 9001:2015 §8.6` in the framework-alignment table (standard named on the row, no bare-§ collision); zero inbound section citations, zero self-refs, zero anchors, so no citer remap)
- [x] `dev-security/standard-software-composition-analysis.md` -- inbound §N (DONE: renumbered to canonical fully-numbered per option B + H4 numbering, 7 unnumbered H2s -> `## 1.`-`## 7.`; 8 H3s under SCA requirements (#4) `### 1.`-`### 8.` -> `### 4.1`-`### 4.8`; 5 descriptive H4s -> `#### 4.4.1`-`#### 4.4.3` (SBOM) and `#### 4.5.1`-`#### 4.5.2` (Supply chain); inbound citer [`dev-security/guideline-ai-coding-assistant-security.md`](../dev-security/guideline-ai-coding-assistant-security.md) `§5` -> `§4.5` remapped (the doc's Supply-chain-attack-mitigations section, old `### 5.`, is now `### 4.5`); the guideline's own Version was co-bumped for the body change; the developer-security-requirements `§9` on the same citer line is a different doc and was left alone)
- [x] `governance/policy-digital-twin-and-simulation-governance.md` -- intra §N (DONE: renumbered to canonical fully-numbered, 5 unnumbered H2s -> `## 1.`-`## 5.` in document order + the 6 already-locally-numbered H3s under Policy statements converted from local `### 1.`-`### 6.` to hierarchical `### 4.1`-`### 4.6` (the all-enumerated-H3 case where convention options A and B coincide); the "intra §N" tag was conservative, the only §-ref is external `ISO/IEC 42001:2023 §8` in the framework-alignment table; zero inbound section citations, zero self-refs, zero anchors, so no citer remap)
- [ ] `governance/policy-exception-and-risk-acceptance-management.md` -- **inline clause numbering** (RETAGGED from "inbound §N, intra §N" 2026-07-01, PR #544): a pre-renumber body scan (`grep -nE '^\*?\*?[0-9]+\.[0-9]'`) found 31 inline prose clauses keyed to the policy's numbered `### N:` H3 statements, so a canonical renumber must realign every inline clause in lockstep with the headings. Also has inbound §N citers (e.g. cited `§N` by the compliance-matrix and enterprise-risk crosswalks), compounding the lockstep. Hard class (inline-clause), defer.
- [x] `governance/standard-records-retention-and-destruction.md` -- intra §N, uses "## Section N:" labels (DONE: first Section-N-label doc; the 7 `## Section 1:`-`## Section 7:` labels were stripped, titles capitalized to Sentence-case, and renumbered to DOCUMENT-ORDER positions `## 5.`-`## 11.` (the old Section number differs from the new position), alongside the 4 leading unnumbered H2s -> `## 1.`-`## 4.` and Framework alignment -> `## 12.`; 5 descriptive H3s numbered `### 5.1`/`### 8.1`-`### 8.2`/`### 11.1`-`### 11.2`; the "intra §N" tag was conservative, the only §-refs are external `ISO/IEC 42001 §9`; live inbound citers reference it by path + external § only (BASC §7.5, SOX §103, ISO 27701 §8.9), and the two CHANGELOG line-number refs are frozen historical, so no citer remap)
- [x] `operations/standard-capacity-and-performance-management.md` -- intra §N, uses "## Section N:" labels (DONE: Section-N-label doc, 17 H2s -> `## 1.`-`## 17.`; Purpose/Scope prefix-add 1-2, the 12 `## Section 1:`-`## Section 12:` labels stripped+Sentence-cased+document-order `## 3.`-`## 14.`, Operating expectations/Framework alignment/Limitations 15-17; no H3/H4; the "intra §N" tag was conservative, only §-ref is external `ISO/IEC 20000-1 §8.6`; zero live inbound §/Section/anchor citers, so no citer remap)
- [x] `operations/standard-observability-and-telemetry.md` -- intra §N, uses "## Section N:" labels (DONE: Section-N-label doc, 19 H2s -> `## 1.`-`## 19.` + 1 descriptive H3; Purpose/Scope prefix-add 1-2, its `### Scope boundary...` H3 -> `### 2.1`, the 14 `## Section 1:`-`## Section 14:` labels stripped+Sentence-cased+document-order `## 3.`-`## 16.`, Operating expectations/Framework/Limitations 17-19; the "intra §N" tag was conservative, only §-ref is external `ISO/IEC 20000-1 §8.6`; zero live inbound §/Section/anchor citers, so no citer remap)
- [x] `operations/standard-production-security-requirements.md` -- inbound §N, intra §N (DONE: hybrid-SHIFT doc (Purpose/Framework-alignment unnumbered while `## 1.`-`## 10.` numbered), so all H2s shift into document order `## 1.`-`## 12.` (Purpose->1, old 1->2 ... old 10->11, Framework alignment->12); the 3 H3s under old `## 3.` (now `## 4.`) shift `### 3.1`-`### 3.3` -> `### 4.1`-`### 4.3`; the 7 descriptive H3s under old `## 9.` (now `## 10.`) numbered per option B `### 10.1`-`### 10.7`; intra self-ref body `§3.2` -> `§4.2`; 2 inbound citer lines in [`operations/procedure-threat-intelligence-and-siem-operations.md`](../operations/procedure-threat-intelligence-and-siem-operations.md) remapped (`§3.1` -> `§4.1`, `§3.1 to §3.2` -> `§4.1 to §4.2`) with its Version co-bump; the compliance-matrix `§8.1, §8.4` on this doc's row is BASC v6 chapter refs (external, BASC column per the matrix legend), not this doc's sections, so no remap; no anchors; the §3.2 `1.`-`8.` list is a plain ordered list, not N.M clauses)
- [ ] `operations/standard-service-level-management.md` -- **inline clause numbering** (RETAGGED from "intra §N" 2026-07-01, PR #541): a pre-renumber body scan (`grep -nE '^\*?\*?[0-9]+\.[0-9]'`) found 32 plain-text inline prose clauses (`1.1`-`1.3`, `2.1`-`2.4`, `3.1`-`3.4`, `4.1`-`4.4`, `5.1`-`5.4`, `6.1`-`6.3`, `7.1`-`7.4`, `8.1`-`8.3`, `9.1`-`9.3`) keyed to the numbered `### 1:`-`### 9:` H3s under `## Requirements`, so a canonical renumber (Requirements becomes `## 3.`, its H3s `### 3.1`-`### 3.9`) must realign every inline clause in lockstep (`1.1` -> `3.1.1` etc.). Hard class (inline-clause), defer.
- [x] `privacy/policy-privacy-and-data-governance.md` -- inbound §N, intra §N (DONE: hybrid unnumbered-H2 + descriptive-H3 + numbered-H3 doc; 5 H2s prefix-added `## 1.`-`## 5.`; the 3 descriptive H3s under Governance (`## 3.`) -> `### 3.1`-`### 3.3` per option B; the 11 numbered H3s under Policy requirements (`## 4.`) `### 1.`-`### 11.` -> `### 4.1`-`### 4.11` (H3 bodies are bullets, NOT inline clauses); the "intra §N" tag was conservative, the only body § are external `ISO 8000-8 §5/§6/§7` (named on their framework-table lines); one live inbound citer remapped: [`privacy/procedure-data-subject-rights-management.md`](../privacy/procedure-data-subject-rights-management.md) `§8` -> `§4.8` (Data subject rights) with its own Version co-bump; the same procedure's `§Governance` name-based reference is left as-is (resolves by name to the renamed `## 3. Governance and accountability`, not broken); `.working`/CHANGELOG mentions are frozen historical)
- [x] `risk/standard-third-party-and-supply-chain-risk.md` -- intra §N (DONE: renumbered to canonical fully-numbered per option B, 11 unnumbered H2s -> `## 1.`-`## 11.` + 8 H3s -> hierarchical (`### 1.`-`### 3.` under Risk assessment became `### 4.1`-`### 4.3`; 5 descriptive H3s numbered `### 5.1`, `### 6.1`-`### 6.2`, `### 8.1`-`### 8.2`); no inline clauses; the "intra §N" tag was conservative, the only §-ref is external `ISO/IEC 42001 §9` in the body (named on line); framework-alignment table has no bare-§ cells; zero inbound section citations, zero self-refs, zero anchors, so no citer remap)
- [ ] `security/policy-acceptance-into-service.md` -- **inline clause numbering** (RETAGGED from "intra §N" 2026-07-01, PR #541): a pre-renumber body scan (`grep -nE '^\*?\*?[0-9]+\.[0-9]'`) found 22 plain-text inline prose clauses (`1.1`-`1.3`, ... keyed to the doc's numbered `### N:` H3 policy statements), so a canonical renumber must realign every inline clause in lockstep with the headings. Hard class (inline-clause), defer.
- [ ] `security/policy-encryption-and-key-management.md` -- inbound §N, intra §N
- [ ] `security/policy-identity-and-access-management.md` -- **inline clause numbering** (RETAGGED from "intra §N" 2026-07-01): a pre-renumber body scan found `### 1.`-`### 10.` H3s under `## Policy statements` PLUS plain-text inline clauses (`1.1`-`1.4`, `2.1`-`2.4`, `3.1`-`3.4`, `4.1`-`4.3`, `5.1`, ... keyed to those H3 numbers), so a canonical renumber (Policy statements becomes `## 4.`, its H3s `### 4.1`-`### 4.10`) must realign every inline clause in lockstep (`1.1` -> `4.1.1` etc.); IAM is also high-connectivity. Hard class (inline-clause), defer.
- [x] `security/standard-authentication-and-password-management.md` -- intra §N (DONE: renumbered to canonical fully-numbered, 9 unnumbered H2s -> `## 1.`-`## 9.` prefix-add, no H3/H4, no inline clauses; the "intra §N" tag was conservative, all §-refs are external NIST SP 800-63B (`§5.1.1`/`§6.3`/`§7.1` in the framework-alignment table); zero inbound section citations (inbound citers path/title-only or external BASC `§8.3`/`§8.5`), zero self-refs, zero anchors, so no citer remap)
- [ ] `security/standard-data-classification-and-handling.md` -- **inline clause numbering** (RETAGGED from "intra §N" 2026-07-01): a pre-renumber body scan found plain-text numbered prose clauses (`1.1`-`1.5`, `3.1`-`3.5`, `4.1`-`4.4`, `7.1`-`7.4`) keyed to a HYBRID H2 scheme (unnumbered Purpose/Scope/Governance/Data-classification-levels/Framework-alignment interleaved with numbered `## 1.`-`## 7.` starting at the 5th H2), so a canonical renumber must shift the H2 numbers AND realign every inline clause in lockstep; the compliance-matrix `§7.5/§8.4` cell for this row is external (the BASC v6 trade-programme column, external BASC clauses), not a citation of this doc's own sections (which stop at §7, with no §8 and no §7.5). Hard class (inline-clause), defer.
- [ ] `security/standard-logging-and-monitoring.md` -- **inline clause numbering** (RETAGGED from "inbound §N, intra §N" 2026-07-01, PR #544): a pre-renumber body scan (`grep -nE '^\*?\*?[0-9]+\.[0-9]'`) found 31 inline prose clauses (`1.1`-`1.4`, `2.1`-`2.2`, `3.1`-`3.3`, `4.1`-`4.4`, `5.1`-`5.4`, `6.1`-`6.3`, `7.1`-`7.3`, `8.1`-`8.3`, `9.1`-`9.3`, `10.1`-`10.2`) keyed to the numbered `### 1:`-`### 10:` H3s under `## Control statements`, so a canonical renumber (Control statements becomes `## 4.`, its H3s `### 4.1`-`### 4.10`) must realign every inline clause in lockstep (`1.1` -> `4.1.1` etc.). Also has inbound §N citers, compounding the lockstep. Hard class (inline-clause), defer.
- [x] `security/standard-personnel-security-screening.md` -- intra §N (DONE: renumbered to canonical fully-numbered, 10 unnumbered H2s -> `## 1.`-`## 10.` prefix-add, no H3/H4; the "intra §N" tag was conservative, the only §-hits are external `BASC v6 §8.3` in the framework-alignment table; zero inbound section citations, zero self-refs, so no citer remap)
- [x] `security/standard-security-awareness-and-training.md` -- intra §N (DONE: fully-unnumbered doc, the simplest class; 9 H2s prefix-added in document order -> `## 1.`-`## 9.` + the 2 descriptive H3s under Mandatory training requirements (`## 4.`) numbered per option B -> `### 4.1`/`### 4.2`; no H4, no inline clauses, no intra-doc section refs; the "intra §N" tag was conservative, the only body § is external `BASC v6 §7.2` (named on its framework-alignment row); the inbound matrix/register `§7.2` hits are ISO clause 7.2 / BASC §7.2 (external, this doc has no §7.2 subsection), path-only links otherwise, so no citer remap)
- [x] `supply-chain/standard-supplier-resilience-monitoring.md` -- intra §N (DONE: fully-unnumbered H2s + 5 `### Category N:` enumerated H3 labels cross-referenced by the tier-posture table; 9 H2s prefix-added -> `## 1.`-`## 9.`; per maintainer decision 2026-07-01 (PR #542, Option 1) the 5 `### Category 1:`-`### Category 5:` H3 labels had "Category N:" stripped and were renumbered hierarchically -> `### 3.1`-`### 3.5`, and the two body cross-refs remapped to §-numbers ("Categories 2 and 4" -> "§3.2 and §3.4", "Category 2" -> "§3.2"); the generic "five categories"/"each category" prose is not a numbered ref and stays; the "intra §N" tag's only actual § is external `EBA/GL/2019/02 §82 to 95`; inbound register/matrix refs are external EBA/BASC clauses + path-only, so no citer remap)
- [ ] `compliance/policy-compliance-and-audit-management.md` -- inbound §N (bare-parenthetical, `procedure-capa.md` §4.3), inline clause numbering (reverted from PR #520)
- [ ] `compliance/policy-legal-and-regulatory-compliance.md` -- inline clause numbering (reverted from PR #520)
- [ ] `dev-security/policy-secure-development-and-engineering.md` -- inline clause numbering (reverted from PR #520)
- [ ] `security/policy-network-communications-security.md` -- inline clause numbering (reverted from PR #520)
- [ ] `security/policy-information-security.md` -- inline clause numbering (reverted from PR #520)
