# /full-qa (deep-qa-review) iter 1: 2026-06-22

## Trigger & state snapshot

- **Trigger**: maintainer-directed trust-recovery escalation after the prior session's three discipline failures, (1) 11 consecutive PRs (#231-#241) recorded "abbreviated spot-check, 0 findings" in [`.working/validate-pr/history.md`](../validate-pr/history.md) without a formal Subagent A dispatch; (2) the post-commit/pre-push audit was skipped on PR #242, producing two CI fix loops; (3) a 60-minute fallback timer was armed where the project mandates 60 seconds. `/full-qa` is run first (smaller, AI-failure-pattern lens), `/fitness` second (ten persona lenses).
- **Window**: PRs #218-#242 inclusive (25 squash commits, range `495d6b0^..8319d32`), plus files referenced from their CHANGELOG-detailed entries.
- **State at HEAD `8319d32`**: Library `2026.06.220`, pack `1.45.2`, README `1.9.91`, 46 gates, 8 governance rules, 13 skills (4 paired). All confirmed from live files by Subagent B.
- **Environment note**: the takeover container was a **depth-50 shallow clone**. This was discovered during apply-time verification of Subagent C's gate-31 finding and corrected with `git fetch --unshallow` (→ 413 commits) before the audit verdict was trusted. See §C and §Synthesis.

## Subagent A: recent-PR deep review

Read the #242 close-out diff and all four Sweep-22-fixed risk files in full; read both canonical treatment-vocab sources; ran the mandated corpus-wide treatment-vocabulary grep; dispatched two sub-reviewers across #218-#241.

**Findings (3 errors, all confirmed by orchestrator against current file state):**

1. `ai/register-ai-risk.md:44`, `| **Treatment Option** | Mitigate / Transfer / Avoid / Accept |`. Four-option set missing the canonical positive-risk options Exploit/Enhance. Canonical-6 per `risk/standard-enterprise-risk-management.md` §6 (Avoid/Mitigate/Transfer/Accept/Exploit/Enhance). The "missed fifth surface" the special-focus directive anticipated. Pre-existing (not touched in-window).
2. `ai/register-ai-risk.md:47`, `| **Status** | Open / In Treatment / Accepted / Closed |`. Carries the exact "In Treatment"/"Accepted" values that `risk/procedure-risk-register.md:59` and standard §7.1 retired in favour of the Open/Closed lifecycle. Same file, same root cause. Pre-existing.
3. `risk/annex-ai-risk-methodology.md:113`, `In addition to standard Avoid / Reduce / Transfer / Accept treatment options, AI risks may be treated through:`. Uses non-canonical "Reduce" (canonical: "Mitigate") and a 4-option set missing Exploit/Enhance. Second missed surface, in the `risk/` domain itself. Pre-existing.

**Verified non-findings**: the four Sweep-22-fixed risk files are internally consistent (procedure-risk-register 1.2.0, template-enterprise 1.1.0, policy-ERM 1.4.3, procedure-methodology 1.1.0, all 2026-06-22). A sub-reviewer's quote of `procedure-risk-register.md:59` with the OLD value was from the pre-#242 diff state; current file is correct. Discipline-failure corrective actions all landed and are mirror-synced. `ai/template-ai-system-register.md:53` and similar "Not Started/Implemented/Verified" values are a distinct field (Impact-Assessment Status), not risk treatment, no finding.

## Subagent B: corpus-wide stale-reference sweep

**Ground truth confirmed from live files**: Library `2026.06.220` (README.md:7), pack `1.45.2` (pack README:5), README `1.9.91` (README.md:8), gates `46` (spec §6), governance rules `8` (`ls`), skills `13` (`ls`). All match. Pre-flight scanner: 0 candidates.

**Finding (1, confirmed):**

1. `tools/lint-collection-enumeration-consistency.py:5`, docstring reads "the pack's **seven** governance rules are listed in the…"; there are **8** (`ls dev-security/claude-rules/governance/*.md` = 8). Stale docstring (`six → seven` was bumped in PR #112 but never to `eight` when the eighth rule landed at pack 1.36.0). No gate guards the docstring (gate 39 targets gate-counts; the collection-enumeration audit parses designated enumeration locations, not its own docstring). Cosmetic/tooling; no adopter impact.

## Subagent C: audit-programme integrity reviewer

**Gate-count parity confirmed at 46 across all four surfaces** (`quality.yml`, `run_all_audits.sh`, `.pre-commit-config.yaml`, spec §6); both parity linters pass. PAIRS registry: 4 pairs, all slash-command files present, no orphans. README skills table = 13 = disk. Pack-rule copies byte-identical (`lint-claude-rules-sync.py` clean). The two #242-touched SKILL.md files pass step-parity. No parity defect introduced by #242.

**Reported finding, RESOLVED AS FALSE POSITIVE at apply-time:** Subagent C reported `tools/run_all_audits.sh` exiting 1 with gate 31 (document-date-staleness) failing on **153 documents** (e.g. `ai/framework-ai-model-risk.md` Date 2026-05-27 vs `git log --follow %cI` 2026-06-21). Orchestrator validation: the container was a **depth-50 shallow clone** (`git rev-parse --is-shallow-repository` → true; only 50 commits; the affected file showed a single `--follow` commit pinned to the shallow-boundary PR #192). `git log --follow` on a shallow clone mis-attributes last-commit dates, manufacturing fake lags. Clinching logic: gate 31 is in CI and is what caught the risk files on #242; had it genuinely failed on 153 docs at `8319d32`, #242 could not have merged green. After `git fetch --unshallow` (→ 413 commits), `tools/run_all_audits.sh` **exits 0, all 46 gates pass**. The 153-doc gate-31 failure does **not** reproduce. Not a corpus finding; recorded as an environment artifact and a methodology lesson for the SKILL.

## Subagent D: citation forensic

Enumerated every external-standard citation introduced/modified in-window; verified each against the canonical-citations register AND authoritative sources via WebFetch (confirmed reachable).

**All citations verify** (zero error/warning): GDPR Art 8(1) 16/floor-13, Art 12(5)(a)/(b)+burden, Art 26(1)-(3), Art 27(1)-(5), Art 36(2) 8+6 weeks, Art 38(3)/(6); PIPL Art 38/39/40; NIS-2 Art 21.2(b); ISO 31000:2018 §§6.4.2/6.4.3/6.5/6.6/6.3/5.3 (the #241 corrections, all six clause-to-subject mappings correct). Register registers all cited standards in matching editions.

**Note (1):** EDPB Guidelines 07/2020 and 3/2018 are cited in-window but have no individual rows in `register-canonical-citations.md` (only WP243 rev.01 at line 285 has a row; line 51 is the soft-law scope extension). Register-completeness item. **Orchestrator dedup: this is already tracked as TODO P7 B2** (Sweep 22 Subagent B surfaced the same five soft-law instruments). Known-issue (K); not a new item.

Declared scope boundaries (not findings): ISO 23894 matrix cells (not touched by #241); national implementing-law citations in the #237 Art 8 per-Member-State table (16+ national statutes; corpus itself flags them adopter-verify); RFC 8725 (not introduced in-window).

## Subagent E: generator-output forensic

Generators discovered: `tools/build-taxonomy.py` (→ `taxonomy.yml`) and `tools/build-portal.py` (→ BOTH `docs/portal.md` and `docs/maturity-scorecard.md`; no separate scorecard generator). Verified two independent ways: generator `--check` exit 0 for both; and regenerate-to-temp + `diff` against committed → empty diff, matching byte counts (taxonomy 248022, portal 42502, scorecard 55382). **No findings.** Independently re-confirmed by the orchestrator: `run_all_audits.sh` exit 0 includes the generator-sync gates.

## Subagent F: discipline-violation forensic

**Scorecard (25 PRs):** CHANGELOG lock-step 25/25 (PASS); retro rows 24/25 (#242 absent by-design, batches to next PR, PASS); Version+Date co-bump clean on sample (no silent co-bump violations); split discipline clean (no genuine unrelated-bundling).

**Findings (synthesized by orchestrator from F's three raw findings):** F's raw findings, (1) sweep22 file:92 unfaithful-reframing, (2) 11 abbreviated rows persist, (3) #240-only annotation, collapse on verification to a single sharper finding. The bare existence of honest "abbreviated spot-check" rows is **not** a new violation: retaining them as the moment-in-time record (rather than rewriting history to hide the lapse) is the correct disposition, explicitly decided in the #241 row. The real finding:

1. **Corrective-record overstatement (1 warning, confirmed):** `.working/validate-sweeps/2026-06-22-sweep22-iter1.md:92` claims the abbreviated rows "now read 'deferred to Sweep 22 close-out per maintainer-directed pause' rather than 'abbreviated spot-check'", but `.working/validate-pr/history.md` rows #221-#239 (lines 19-35) still literally read "abbreviated spot-check", and the per-row "this was abbreviated; Sweep 22 reconciles" annotation was applied to only #240 and #241. The #242 corrective record describes a relabelling that did not happen and contradicts the actual retain-decision, a multi-surface-incompleteness / inferred-as-verified assertion in the discipline-failure record itself (the exact failure class this pass targets). The SUBSTANCE was reconciled (Sweep 22 Subagent A formally scoped #231-#241 and surfaced the 4 real errors); only the artefact's self-description is inaccurate.

## Orchestrator synthesis & apply-time verification

Every subagent finding was re-verified by the orchestrator against current source before routing (research-assistant + apply-time-correction disciplines). Dedupe/adjudication:

- **A's 3 treatment-vocab findings**: confirmed verbatim against current files. Real (R). Out-of-window (pre-existing) but genuine drift from the canonical vocabulary #238/#239/#242 established, the C-3 multi-surface-incompleteness pattern reaching AI-risk surfaces Sweep 22's abbreviated review never touched.
- **B's docstring finding**: confirmed (R). Cosmetic.
- **C's gate-31 finding**: refuted (false positive, shallow-clone artifact). Removed from finding set; recorded as environment caveat + SKILL methodology lesson.
- **D's EDPB note**: confirmed real but K (already P7 B2). Not re-routed.
- **E**: clean (R, double-verified).
- **F's findings**: synthesized to one warning (corrective-record overstatement); the "abbreviated rows persist" raw findings reclassified as correct-disposition, not violations.

Apply-time worker correction logged: **1 worker false positive caught** (C gate-31) and **1 worker over-classification corrected** (F's 3→1). Logged to hallucination-metrics per discipline.

## Findings routed to TODO P1 top (tagged `[full-qa]`)

5 confirmed findings routed (3 treatment-vocab errors, 1 docstring note, 1 corrective-record warning). All routed regardless of severity per trust-recovery convention; maintainer triages. Not routed: C (false positive), D (dedup to P7 B2).

## Resulting PR

Pending. The trust-recovery suite terminates on maintainer sign-off of the combined `[full-qa]` + `[fitness]` P1 additions; codification PRs (deep-qa-review SKILL, 9th pack rule, /fitness amendment, structural-review skill, optional /trust-recovery wrapper) follow sign-off.

## Trust-recovery framing

Prior-session discipline failures this pass was the response to: (1) abbreviated `/validate-pr` over 11 PRs; (2) skipped post-commit audit on #242; (3) 60-min vs 60-s timer. Elevated rules applied at maximum strictness: `validate-inference-before-action` (every "since/because X" fired a validation read, the decisive instance being the gate-31 shallow-clone catch, which a less strict pass would have routed as a 153-doc corpus emergency); `evidence-grounded-completion` (every finding carries a `path:line` quote; worker returns treated as hypotheses until orchestrator re-read); `ai-assistant-workflow-disciplines` §1/§3 (research-assistant + apply-time correction).

Honest limitation: this pass is a process control, not a mechanical gate. The durable backstop for the abbreviation failure remains the queued P4.6 QA-cadence enforcement gate. Two methodology lessons for the `deep-qa-review` SKILL codification: (a) unshallow / verify full clone before any git-history-aware audit; (b) the corrective-record-overstatement finding shows that even discipline-failure remediation records need their own apply-time verification.
