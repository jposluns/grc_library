# Deep-QA Forensic Pass (`/full-qa`): 2026-06-27 iter 1

**Trigger.** Maintainer-invoked trust-recovery escalation: "There were hallucinations at the last run. Do an in-depth analysis beyond `/validate`." Run after the owed #391 `/validate-pr` (the "validate" the maintainer's request follows), at HEAD `aa38ff8`/#391.

**Scope (maintainer-directed).** The maintainer scoped the analysis to *hallucinations*. The dominant hallucination class for the recent window (FR-167 matrix batches) is "valid code, wrong control": a control identifier that EXISTS in the framework catalogue (so gates 48/49/54 pass it) but is the WRONG control for the row's document (semantic-fit failure, which no gate checks). The forensic-citation lens (deep-qa Subagent D) was run **exhaustively across the entire compliance matrix**, not just the named batches 5-9, split across 6 workers (more thorough than the single-D default). The maintainer then chose **"accept findings, broaden first"**, so the remaining `/full-qa` lenses (A recent-PR, B corpus stale-reference, C audit-programme integrity, E generated-artefact, F discipline-violation) were subsequently run as fresh subagents. The `/fitness` persona pass is offered as the second lens pending sign-off (not auto-launched: a 10-persona corpus pass is tangential to the code-hallucination concern). The ISO 27001:2022 Annex A column is a maintainer-acknowledged coverage gap (no source extract; tracked at TODO P3), not asserted clean.

**Clone depth (step 0).** Full clone confirmed (`git rev-parse --is-shallow-repository` = `false`; unshallowed at session start). No shallow-clone history artefact risk.

**Mechanical baseline (step 1).** `tools/run_all_audits.sh` = **54/54 green** at `aa38ff8`. All gate-checked properties (code existence, four-surface parity, link coverage, version monotonicity, CSF-1.1 cleanliness) hold; the findings below are all in the gate-blind semantic-fit class.

## Ground truth used
- CCM v4.1 codes -> titles: `grc_library_scratch/ref/standards/CSA-CCM-v4.1.0-catalogue__CCM.csv` (207 codes). Authoritative; every routed CCM finding quotes the source Control Title.
- NIST CSF 2.0 categories: `grc_library_scratch/ref/standards/NIST-CSF-2.0--CSWP-29--full-text.md`.
- ISO 27001:2022 Annex A: **no source extract in the base** (only ISO AI-standards are present). ISO codes were validated knowledge-only at note level and **none were routed** (flagged as a coverage gap, not asserted clean).

## Subagent (citation-forensic, scaled D) returns
Six workers, each validating every CCM/CSF code in its section against the source title, ISO at note-level:

- **D-Governance (rows 50-68):** 1 finding (row 52).
- **D-Compliance (rows 98-127):** 3 findings (rows 98, 101 x2).
- **D-Security (rows 135-169):** 4 findings (rows 140, 142, 163, 165).
- **D-Operations+Resilience (rows 177-250):** 0 findings (42 rows validated; BCR sub-numbers individually checked; 4 borderline rows opened to the source document and confirmed groundable).
- **D-Risk+Supply-chain (rows 76-90, 205-224):** 0 findings (STA sub-numbers individually checked).
- **D-Privacy+Architecture+Dev-security (rows 258-298):** 0 findings.

## Broadened-lens returns (A/B/C/E/F)
- **A (recent-PR deep review, non-code):** 0 findings. Every sampled CHANGELOG-detailed correction claim matches the actual matrix state (independently re-confirmed batch-9 fixes HRS-09→HRS-04, HRS-06/07→HRS-11/12, A.6.2→A.6.7, DSP-04/UEM-11 DLP, A.8.12 are really in the matrix); per-section row counts match the entries; the one #390 future-tense placeholder was self-caught and fixed in #391 (closed loop). One out-of-window note (`CHANGELOG-detailed.md:1060`, PR #332 future-tense placeholder), not routed.
- **B (corpus-wide stale-reference sweep):** 0 findings. Every stale-looking count/version lives only in frozen `.working/`, append-only CHANGELOG, or the self-documented TODO snapshot; no corpus doc links into `.project-governance/`; matrix citers cite via metadata field, not by quoting volatile facts.
- **C (audit-programme integrity):** 0 findings. Gate-49 (#390) coherent across spec §5/§6, linter docstring, CHANGELOG, runner label; spec `1.16.13`; CCM_V41=207; no stale "CCM out of scope" prose survives; the superseded `test_ccm_column_not_policed_here` is gone.
- **E (generated-artefact forensic):** 0 findings. `build-taxonomy.py --check` and `build-portal.py --check` both exit 0; the #381 register migration correctly reflected (migrated file absent, stayed file present); matrix metadata 1.9.0/2026-06-27 consistent across taxonomy/portal/scorecard.
- **F (discipline-violation forensic):** 1 finding (F-1 below). Otherwise clean: per-PR `/validate-pr`+`/retro` rows present for every non-handoff window PR (#391's owed, batching this session = expected); handoff #386/#389 carry inline loop-break exemption rows; library CalVer strictly monotonic; sweep chain 56-60 unbroken, all full-dispatch no-abbreviation; zero abbreviation anti-pattern occurrences in-window.

## Orchestrator synthesis and apply-time verification (step 3)
Every routed finding was re-verified by the orchestrator: each CCM Control Title re-read from the CSV, each matrix line re-read; F-1 re-verified by header-parity grep (root has #386/#387/#388 headers; detailed mirror has only #388/#389). All nine confirmed (none refuted). Dedupe: GRC-08 appears on two rows (98, 101), two locations of one recurring misapplication, not deduped away. **The broadening result is decisive: of six lenses, only the code-forensic (D) and one discipline check (F) surfaced anything; A/B/C/E clean. The hallucination concern is real but narrowly bounded to the matrix control-code semantic-fit class plus one CHANGELOG-mirror structural slip.**

| # | Location | Code(s) | Source Control Title (the wrong control) | Why wrong | Suggested correct code | Severity |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `compliance/matrix-grc-compliance-alignment.md:52` | DSP-07, DSP-08 | "Data Protection by Design and Default" / "Data Privacy by Design and Default" | Row is *Standard: Records Retention and Destruction*; by-design privacy-engineering controls, not retention/disposal. Sibling row 66 correctly uses DSP-16/DSP-02. | DSP-16 (Data Retention and Deletion), DSP-02 (Secure Disposal) | Medium |
| 2 | `...:142` | IAM-03, IAM-08 | "Identity Inventory" / "Access Review" | Row is *Standard: Authentication and Password Management*; bullseye IAM-13 (Strong Authentication) is omitted. The document's own framework table carries the same confusion. | IAM-02, IAM-13 (Strong Authentication), IAM-14 (Credentials Management) | Medium |
| 3 | `...:98` | GRC-08 | "Special Interest Groups" | Row is *Policy: Compliance, Audit, and CAPA Management*; SIG-contact control unrelated to audit/CAPA. | A&A-05 (Audit Management Process) or A&A-06 (Remediation) | Medium |
| 4 | `...:101` | GRC-08 | "Special Interest Groups" | Row is *Register: BASC IT Compliance Monitoring and KPIs*; same SIG control on a monitoring register. Recurs with #3. | A&A-02 (Independent Assessments) or A&A-04 (Requirements Compliance) | Medium |
| 5 | `...:163` | STA-05 | "SSRM Control Ownership" | Row is *Standard: Penetration Testing and Red Team*; cloud shared-responsibility-matrix control, unrelated to pen testing. TVM-07 (Penetration Testing) is already the anchor. | drop STA-05; if a vendor dimension is wanted, STA-13 (Supply Chain Compliance Assessment) | Medium |
| 6 | `...:140` | (DSP-04 absent) | "Data Classification" | Row is *Standard: Data Classification and Handling*; the bullseye DSP-04 is absent (row uses DSP-01/02/03). Under-mapping, present codes defensible. | add DSP-04 (Data Classification) | Low |
| 7 | `...:165` | SEF-08 | "Security Breach Notification" | Row is *Procedure: Security Disciplinary Process* (HR sanctions); breach-notification function does not match. Thin toehold only. | SEF-01 or HRS-10 (Non-Disclosure Agreements), or drop | Low |
| 8 | `...:101` | GRC-04 | "Policy Exception Process" | Row is the KPI/monitoring register; weak fit, has a toehold (one "exception-register-currency" KPI line). | reconsider vs an A&A / GRC-07 monitoring code | Low |
| 9 | `.working/changelog-details/CHANGELOG-detailed.md:74-116` | (n/a, structural) | detailed-mirror per-PR header destroyed | PR #388's commit overwrote the prior detailed-mirror header in-place; the standalone `## ... PR #386` and `## ... PR #387` headers are gone and those bodies are orphaned under #388's header, while root CHANGELOG carries all three. Gate-blind (D1 checks per-commit presence, not cross-commit header integrity). | Re-insert standalone #386 and #387 headers above their orphaned bodies; consider a mechanical detailed-vs-root per-PR-header-set parity check | Medium |

**Pattern (systemic).** All eight are the same class: a valid code that is the wrong control for the row. Gates 48/49/54 check existence and catalogue membership, not semantic fit, so the entire class is gate-blind. The class is NOT confined to the recent batches: row 52 (governance) and row 142 (security) are pre-existing rows, so the matrix-authoring process produced these across multiple sessions. The #390/#391 work (the `verify-reference-modules.py` parity aid, worker-brief rails 9/10, the standards-validation discipline) is the start of addressing this class but is a dev-aid plus worker guidance, not a mechanical gate; semantic fit is hard to gate mechanically.

## Findings routed (step 4)
Per trust-recovery routing (none dropped; severity governs tier): all nine are Medium/Low, so all route to **Priority 2**, tagged `[full-qa]`. Findings 1-8 form one coherent backlog item (the matrix control-code semantic-fit cleanup, eight locations enumerated); finding 9 (F-1) is a `.working/` CHANGELOG-detailed-mirror header repair. ISO-column semantic validation stays a **coverage gap** (no source extract; existing TODO P3 per-document ISO Annex A validation is the tracker; maintainer chose "leave as known gap"). **Maintainer decisions (2026-06-27):** accept the findings + broaden first (done: A-F run); remediate in **one** PR (matrix fixes + source-doc code cleanup B + the 3-governance-trade-cell redirect + end-overnight-mode bookkeeping + handoff). The TODO routing + corpus fixes are **held for maintainer sign-off** on the complete set; `/fitness` offered as the optional second lens.

## Trust-recovery framing
The prior window's discipline concern was hallucinated control codes the routine QA layer did not catch. This pass confirms the concern is real and bounds it: eight control-code semantic-fit defects (all Medium/Low, all in the compliance matrix, all in a gate-blind class) plus one CHANGELOG-detailed-mirror structural slip (F-1). The full six-lens broadening was decisive: A/B/C/E returned clean, so the defect surface is narrow and known, not diffuse. The maintainer's instinct (validate the codes against the scratch ground-truth source titles, not just the derived module) is the same instinct that produced #390; this pass extends it from the module to the matrix cells, and surfaces that the whole class is gate-blind (existence/membership gates cannot see semantic fit). **The pass terminates on maintainer sign-off, not on this finding set.** Held for sign-off; `/fitness` (the persona pass) offered as the optional second lens.
