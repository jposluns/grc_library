# Deep-QA Forensic Pass (`/full-qa`): 2026-06-27 iter 2

**Trigger.** Maintainer-invoked trust-recovery escalation, the second `/full-qa` of 2026-06-27. The maintainer requested a structured white-box re-examination of the post-#392 window to rebuild certainty before further `grc_library` work. The tier was not self-authorized: the maintainer invoked it. This iteration pairs with the `/fitness` persona pass recorded in [`../fitness-reviews/2026-06-27-r1.md`](../fitness-reviews/2026-06-27-r1.md); the forensic pass ran first per the trust-recovery suite ordering, the persona pass second.

**Window.** Since the last `/full-qa` (iter 1, which closed at #392): the #393 to #409 deltas. Iter 1 found and #392 remediated the matrix control-code semantic-fit class; this iteration re-examines the window that followed.

**Clone depth (step 0).** Full clone confirmed (non-shallow); the binding full-clone methodology rule was satisfied before any history-aware reasoning. No shallow-clone history artefact risk.

**Mechanical baseline (step 1).** `tools/run_all_audits.sh` exit 0, **54/54 green** at `5f0c594`/#409. All gate-checked properties (code existence, four-surface parity, link coverage, version monotonicity, CSF-1.1 cleanliness, dash hygiene) hold; the findings below are all in gate-blind classes.

**No abbreviation.** All 16 subagents of the trust-recovery suite ran (6 `/full-qa` lenses A to F here, plus the 10 `/fitness` personas in the paired record). No lens was skipped, scoped down, or substituted with an informal check.

## Subagent (A) recent-PR deep review
Clean on the non-code axis. Every sampled #393 to #409 CHANGELOG-detailed correction claim matched the actual corpus state; per-section row counts matched the entries; version cascades were consistent. As a targeted cross-check of the iter-1 concern, Subagent A re-examined the FR-167 matrix-expansion batches that landed in the window for any recurrence of the "valid code, wrong control" semantic-fit class, and found **no recurrence** in the batch rows. One matrix finding surfaced outside the batch-row scope (FR-198 below, a weak-fit `DSP-10` on the Pseudonymisation row), routed to the closing whole-matrix `/matrix-fit` candidate set rather than treated as a batch regression.

## Subagent (B) corpus-wide stale-reference sweep
Clean. `tools/nist_csf_reference.py` reported 0 CSF-1.1 carriers out of 311 candidate strings. Gate-count prose, governance-rule counts, skill and command counts were current against the live four-surface state. No corpus document links into a relocated path; matrix citers cite via the metadata field rather than by quoting a volatile fact, so no stale-count drift was introduced by the window.

## Subagent (C) audit-programme integrity
Clean. The 54-gate set is coherent across the four parity surfaces (CI workflow, `tools/run_all_audits.sh`, `.pre-commit-config.yaml`, and the audit-programme specification §5/§6). The advisory `tools/audit-matrix-semantic-fit.py` remains unwired, so the gate count correctly stays at 54. Collection enumerations (gates, governance rules, skills, commands) are internally consistent.

## Subagent (D) citation forensic
Clean except FR-199. The five soft-law citations that the prior window's B2 item (EDPB / WP29, closed in #408) had reconciled were re-verified against the canonical register and confirmed groundable. One cosmetic inconsistency surfaced: the soft-law "Current version" column in [`../../governance/register-canonical-citations.md`](../../governance/register-canonical-citations.md):285-290 mixes format conventions (`rev.01` / `WP216` / `Version 2.1` / `Opinion 28/2024`); recorded as FR-199 (Low, cosmetic).

## Subagent (E) generated-artefact forensic
Clean. `tools/build-taxonomy.py --check` and `tools/build-portal.py --check` both exit 0; the maturity scorecard is in sync. No generated-artefact drift in the window.

## Subagent (F) discipline-violation forensic
Clean. No recurrence of the iter-1 F-1 class (the detailed-mirror per-PR header slip): the detailed CHANGELOG mirror's per-PR `##`-header set is intact across the window. Per-PR `/validate-pr` and `/retro` rows are present for every non-handoff window PR; handoff PRs carry their inline loop-break exemption rows; TODO-to-DONE rotations are intact; library CalVer is strictly monotonic; no abbreviation anti-pattern occurrences in-window.

## Orchestrator synthesis and apply-time verification (step 3)
The forensic pass is clean on the discipline, generator, audit-programme, and stale-reference axes; the only `/full-qa`-originated findings are two matrix/register items (FR-198, FR-199) plus the citation-forensic re-confirmation of the #408 soft-law reconciliation. FR-198 and FR-199 were re-verified by the orchestrator (each cited line re-read; the `DSP-10` weak fit and the register column-format inconsistency confirmed). The substantive volume of this trust-recovery suite came from the paired `/fitness` persona pass, which surfaced 30 cross-document contradictions and editorial items (FR-168 to FR-197); those are routed and verification-tracked in [`../fitness-reviews/2026-06-27-r1.md`](../fitness-reviews/2026-06-27-r1.md). The decisive read mirrors iter 1: the corpus is mechanically clean (54/54), so every finding in this suite is in a gate-blind class (cross-document semantic contradiction, control-code semantic fit, cosmetic register formatting), exactly the class the trust-recovery suite exists to surface.

## Findings routed (step 4)
The `/full-qa`-originated findings are:
- **FR-198** (Low, `[full-qa]`): matrix `DSP-10` weak semantic fit on the Pseudonymisation row, [`../../compliance/matrix-grc-compliance-alignment.md`](../../compliance/matrix-grc-compliance-alignment.md):263; a defensible-but-loose third code (`DSP-08` or `DSP-16` tighter); candidate for the closing whole-matrix `/matrix-fit`.
- **FR-199** (Low, `[full-qa]`): cosmetic soft-law "Current version" column-format inconsistency, [`../../governance/register-canonical-citations.md`](../../governance/register-canonical-citations.md):285-290.

Plus FR-168 (the CCM GRC-12 wrong-citation High, surfaced by both the `/full-qa` and `/fitness` lenses), routed to P1. All other suite findings (FR-169 to FR-197) are `/fitness`-originated and routed in the paired fitness record. Per trust-recovery routing, every confirmed finding routes by severity: High to P1, Medium and Low and FYI to P2, none dropped. All are apply-time-verified or subagent-evidenced with `path:line`, and deduped against the existing backlog (two re-confirmations of existing TODO items, the ERC two-expansion item and the FR-44-generalisation item, cross-referenced not duplicated).

## Trust-recovery framing
The prior trigger was the maintainer's request for certainty (beyond the routine `/validate`) before further `grc_library` work, the same posture as the 2026-06-22 and the 2026-06-27 iter-1 invocations. This iteration confirms the post-#392 window is clean on the AI-failure-pattern axes the forensic lens targets (recent-PR, stale-reference, audit-programme, citation, generator, discipline) except two minor matrix/register items, and the paired persona pass carried the cross-document contradiction findings. The suite ran on a full clone, with all 16 subagents and no abbreviation, satisfying the no-reproduce-the-abbreviation discipline. The pass terminates on maintainer sign-off, not on a finding set; the maintainer reviewed the combined routed additions (the P1 and P2 blocks plus the two dedupe cross-references) and **signed off this session**.
