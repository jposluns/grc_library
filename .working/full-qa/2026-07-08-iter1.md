# Deep-QA (/full-qa) forensic pass, 2026-07-08 iter 1

**Trigger:** maintainer-directed `/full-qa` over the just-merged PR #693 (squash `6e21845`), "Update and clarification of reference information", a large, rapidly-iterated cleanup (~55 files, +403/-521) that genericized ref-holdings/possession phrasing, removed all watermark mentions + the literal watermark PII, removed `Traffic Tech`/`TrafficTech` repo-wide, reframed possession-`licensed`, retired the employer `SANITISATION_TERMS` denylist + its Appendix-A tables, ran the scrub Tier-1 + sanitization->organization-neutrality Tier-2 reframes, bumped 16 versioned docs, and batched #692's QA rows.

**Clone depth (step 0):** `git rev-parse --is-shallow-repository` = false (872 commits). History-aware gates safe.

**Mechanical baseline (step 1):** `tools/run_all_audits.sh` = 66/66, EXIT 0 on merged `main` (`6e21845`).

**Window:** PR #693's diff + the surfaces it touched.

**Trust-recovery framing:** this pass is the AI-failure-pattern half of the trust-recovery tier, invoked proactively (not after a discipline lapse) because #693 was a broad, rapidly-scoped edit across corpus governance docs + tooling + records, with elevated collateral-error risk. All six subagents dispatched (no abbreviation). The elevated rule applied: every finding re-verified against live source before routing; the maintainer-decided KEEP-LIST (domain `licensed`, corpus-correction/records `scrubbed`) excluded.

## Subagent A: recent-PR deep review (#693 diff)
Verified clean: all 16 version bumps complete (Version+Date), Appendix A restructure clean (4 employer tables removed, generic subsections intact), anti-copying reframes parse, no surviving employer identifiers, TODO P2 "11 open items" now correct. Findings: (1) `tools/lint-internal-references.py:7` docstring xref to removed `SANITISATION_TERMS`; (2) `tools/lint-pii-in-content.py:69` stale exemption comment "# contains sanitization-term list"; (3) `specification-ingestion.md:67` "## Organization-neutrality rules" title collides with Appendix A "## Appendix a: organization-neutrality rules"; (4) terminology desync, CONTRIBUTING "organization-specific residue" vs `procedure-library-quality-and-review-cadence.md`/`template-document-review-record.md`/`template-library-health-report.md` still "Sanitization residue/check/sweep".

## Subagent B: corpus-wide residual sweep
Confirmed ZERO survivors: `Traffic Tech`/`TrafficTech` (0), watermark (only `ai/standard-ai-model-risk.md`, the AI-model-watermarking topic, KEEP), possession-`licensed` (0), ingestion-`scrub` (0), ingestion-`sanitization` in the 6 named specs (0, all "organization-neutrality"). Finding: `tools/lint-internal-references.py:7` (dup of A#1). The two `SANITISATION_TERMS` mentions in `.working/changelog-details` are frozen historical entries (accurate-as-of-then) -> KEEP.

## Subagent C: audit-programme + linter integrity
Confirmed clean: `lint-language.py` no dead code / no unused var / parses / runs exit 0; `test_linters.py` 345 tests pass; gate count 66 unchanged. Finding: `governance/specification-audit-programme.md:69` gate-2 narrative still lists "sanitization neologisms" + worked-example exemption triple "heading-case, sanitization, and ensure that" (both stale). Plus dup of A#1.

## Subagent D: citation + reframe coherence
NO DEFECTS. All anti-copying statements still assert the copyright-respecting (no-verbatim-copy) principle; CC BY-SA 4.0 refs intact; licence-compatibility lists unchanged; every cross-reference to the former substitution table now names "organization-neutrality rules" and resolves; no #692 (OWASP register) collision.

## Subagent E: generated-artefact drift
NO DRIFT. `build-taxonomy.py --check` + `build-portal.py --check` both exit 0; all 5 taxonomy-tracked version bumps propagated; scorecard consistent; titles/metadata unchanged. The `2026-07-08` dates are UTC-correct (GMT-4 maintainer; gate 31 within-1-day) -> non-finding.

## Subagent F: discipline-violation forensic
PASS: gate-discipline (legitimate maintainer-authorized retirement, not silencing), change-tracking lock-step, version bumps, #692 QA rows present, TODO P2 fix, bookkeeping-parity (exit 0), D7 tokens (the 5 labelled tokens match), meta-prose (suite 345 + gate 66 measured-correct). Findings: (1) `.working/session-handoff.md:44`, the D7 fix updated the version-token prefix to #693 but left the rest of the bullet as the verbatim #688 narrative (self-contradiction) and a false #688 change-scope claim.

## Orchestrator synthesis + apply-time verification
Six confirmed findings after de-dupe (A#1/B/C = one docstring finding). All re-verified against live source; all doc/comment/records staleness (no corpus-normative-content defect, no functional break). Root cause: multi-surface incompleteness, the `SANITISATION_TERMS` removal + the sanitization->organization-neutrality reframe left describing/vocabulary surfaces stale, and the minimal D7 fix half-updated the handoff.

1. `tools/lint-internal-references.py:7`, docstring xref to removed rule. [reworded]
2. `governance/specification-audit-programme.md:69`, gate-2 narrative + worked-example exemption stale sanitisation. [dropped both]
3. `tools/lint-pii-in-content.py:69`, vestigial exemption (test: lint-pii passes without it). [exemption removed; lint-language.py now actively PII-scanned]
4. `specification-ingestion.md:67`, title collision with Appendix A. [retitled to "Organization-neutrality requirements"; anchor-unreferenced, verified]
5. terminology desync, incomplete Tier-2 (Tier-2 explicitly covered "sanitization residue -> organization-specific residue" + the sanitization-rules pointers). [completed the rename across the 3 quality-review docs, 8 occurrences, for corpus-wide consistency; flagged to maintainer]
6. `.working/session-handoff.md:44`, half-stale #688 narrative + false scope. [reconciled the whole bullet to the #693/#694 state]

Apply-time refutations: none (all 6 confirmed). Non-findings recorded: E's date observation (UTC-correct); B's 2 historical-CHANGELOG `SANITISATION_TERMS` mentions (frozen, accurate-as-of-then).

## Findings routed
All 6 fixed in-window in the follow-up hot-fix PR (per the maintainer's "correct any issues"), not routed to backlog. Severity: all note/warning (doc/comment/records; no normative-content or functional defect). No P1.

## Termination
Per the trust-recovery discipline, this pass terminates only on maintainer sign-off (not empty-delta). Held for maintainer review of the 6 corrections + the group-C completion choice + the pending git-history-collapse decision.
