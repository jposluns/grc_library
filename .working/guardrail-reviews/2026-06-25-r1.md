# Guardrail Review, 2026-06-25 r1

**Trigger.** Maintainer-directed (2026-06-25), auto-prompt cadence: the gate-50 addition (#343) is exactly the "after any PR that adds a gate" trigger the skill names. Closes TODO §4.13.

**Mechanical baseline.** `tools/run_all_audits.sh` 50/50 green; parity gates 35/37/39/41/44 all green; clone non-shallow. Inventory under review: **50 audit gates**, **10 governance rules**, **15 skills**.

**Method.** Three lens subagents (overlap, gap, drift) dispatched in parallel, each reading the §6 gate inventory + narrative, the linters, the 10 rules (+ mirror + CLAUDE.md one-liners), and the 15 skills; every finding required a `path:line` quote; orchestrator apply-time-verified each before routing.

## Overlap lens

**0 accidental-overlap / contradictory findings.** Every candidate pair is deliberate-and-documented, each tail-appended gate carrying an explicit boundary clause:

- Gate 48 vs 49: `lint-matrix-control-codes.py`:40-43 ("This gate does not re-validate CCM codes; doing so would duplicate that gate") + spec §6 ("reuse, not duplicate"). Clean.
- Gate 50 vs parity family 35/39/41/44: disjoint inputs (gate 50 reads `.working/validate-pr/history.md` + `improvement-log.md` + TODO bullets; the parity family reads gate-declaration / count / enumeration / step surfaces). `lint-bookkeeping-parity.py`:14-16 positions it as extending, not duplicating, the #333 pre-push runner. Clean.
- Gate 45 vs 50: both read CHANGELOG + `.working/` history, but gate 45 reads the commit graph (queued-PR-merged, sweep-cursor) and gate 50 reads committed content (record presence, rotation markers); `lint-bookkeeping-parity.py`:16-23 draws the line explicitly. Clean.
- Citation gates 5/6 vs 48/49 (literal-version drift vs staleness vs control-code validity) and internal-ref gates 3 vs 23 (link-target resolution vs internal-deployment-leakage): both layered-and-documented. Clean.

The documentation discipline (each adjacent gate names its boundary in prose) is itself why there is no accidental redundancy.

## Gap lens

4 candidate blocks; **3 are dedup-confirmations of already-queued TODO items**, **1 genuinely unqueued**:

- **gate-49-scope-narrower-than-defect-class** (medium): gate 49 validates NIST CSF only in the central matrix; the live carriers `risk/standard-third-party-and-supply-chain-risk.md`:230 (`ID.SC`) and `operations/procedure-security-monitoring-and-alert-management.md`:333/336/337 (`RS.RP`/`DE.DP`) are ungated. **Already queued: TODO:88** (gate-49 extension) + DD-12 (TODO:87). Cross-referenced, not duplicated. (`security/standard-remote-working-security.md`:256 `PR.AC` is already corrected to `PR.AA`, NOT a live carrier.)
- **directional-dependency-gate-prose-only** (medium): spec §7.3 (`specification-project-governance-separation.md`:188) states the no-inbound-link-to-`.project-governance/` rule with no enforcing linter. **Already queued: TODO:61.**
- **directory-scan-scope-parity-meta-check-prose-only** (medium): spec §7.4 (`:197`) states the explicit-allow-list completeness obligation, backed only by per-gate regression tests, no meta-check. **Already queued: TODO:62.**
- **changelog-hygiene-precommit-preflight-no-aid** (low): **GENUINELY UNQUEUED.** The CHANGELOG-hygiene-caught-at-pre-push pattern recurs (#330/#332/#340/#341, `improvement-log.md`:39); the link-coverage gate runs pre-commit-over-all-files and the em-dash check only PR-time, so neither fires on the first commit's staged CHANGELOG diff. The #341 retro proposed a tiny staged-diff pre-flight helper but gated it on a 3rd recurrence and it never reached TODO. **Routed: new TODO item (low).**

The gate-50 worker-provenance dormant stub's activation dependency IS tracked (TODO:184 + the design-decisions pinned-design entry); no gap.

## Drift lens

**0 drift findings.** Gates 48/49/50 §6 narratives match their linter docstrings + code constants exactly (gate 48 "four checks", gate 49 ISO ranges 37/8/14/34 + NIST 6 functions + 22-Category set, gate 50 INCEPTION=329 + allowlist {300,322,334} + 3 checks). Rule pack bodies match their CLAUDE.md one-liners (spot-checked trust-recovery routing, the five workflow disciplines, change-tracking's three CI gates). No characterization drift.

**One sub-resolution coherence observation (surfaced by the drift lens AND independently by Sweep 47's Subagent C), routed as a Low finding:** gates **49 and 50 are absent from the §5 functional-category index** (`specification-audit-programme.md`:5-11, which runs through gate 48). §5 self-describes as "descriptive, not prescriptive" (line 13) and is not a gate-35 parity surface, so this is not meaning-drift between two surfaces; it is a stale-index coherence gap. It is the concrete substance behind the maintainer's "are the gates in a sensible functional grouping" question.

## Orchestrator synthesis and verification

- All overlap/drift candidates apply-time-verified clean (re-read both surfaces).
- Of the 4 gap blocks, 3 verified as already-queued (TODO 61/62/88, cross-referenced); 1 (CHANGELOG pre-flight) verified genuinely-unqueued by `grep` of TODO.
- The §5-index gap verified by enumerating §5's category membership (gates 1-48 placed; 49, 50 absent).

## Findings routed

Two new low-severity items routed to the backlog, tagged `[guardrails]` (Medium/Low → next-priority tier; both Low):

1. **[guardrails] CHANGELOG-hygiene first-commit pre-flight aid** (P4-adjacent, low): a small `tools/` staged-CHANGELOG-diff pre-flight (em-dash grep + link-coverage on staged root-CHANGELOG lines, PASS/FAIL), runnable before the first commit; OR move the em-dash check to pre-commit. Not a new audit gate (the gates exist); a convenience that moves the existing discipline earlier.
2. **[guardrails] §5 functional-category index currency + gate-ordering decision** (P4, low): bring §5 current (place gates 49 in "Content drift defence" and 50 in "Programme and index integrity"), and record the ordering decision the maintainer's §4.13 raised: keep numeric append-order (renumbering would break every gate-number cross-reference across the corpus) and use §5 as the functional-grouping index over the append-ordered numbers, keeping §5 current as gates are appended.

The three dedup-confirmed gaps (TODO 61/62/88) are NOT re-routed; they are cross-referenced here.

## Verdict

The 50-gate machinery is **coherent**: no accidental overlap, no drift, no unqueued substantive gap. Every gate earns its place; the adjacent-gate boundaries are explicitly documented. The two new items are low-severity coherence/convenience improvements, not defects.
