# Guardrail review r3 (2026-07-03)

Auto-prompted run: gate 60 reached its fail threshold (machinery drift 3, gates 59 to 62)
on the gate-62 branch, firing the GR-6 sequencing rule exactly as designed (a review must
land before or with the gate that hits the threshold). Baseline: all 61 merged gates green
at `b83cb6e` (#609); gate 62 wired on the branch with its corpus-wide test-first run clean;
parity gates 35/37/39/41/44 green on the branch state. Inventory under review: 62 gates,
12 governance rules, 17 skills, 10 slash commands, the five delta checks D1-D5, and the
advisory aids. Three lens subagents dispatched (overlap, gap, drift), foreground-railed;
every finding below was re-verified at its cited source by the orchestrator before routing.

## Overlap lens (3 findings, all Low)

- **O-F1 (Low): gate-18/gate-62 trailing-link seam.** Gate 18's cross-doc filter checks
  only the preceding 60-char window for a `.md` link while gate 62 claims links on either
  side, so a trailing-link phrasing is claimed by both with different resolution targets.
  Latent (the doctype-word scan catches real phrasings). ROUTED (TODO section 3.15).
- **O-F2 (Low): gate-9/gate-12 undocumented layering.** Six shared uncertainty-marker
  tokens across two independently-maintained lists and different exempt sets, with no
  cross-documentation; the only same-token family pair without a stated boundary. The
  layering is real (gate 9 checks a conjunction; gate 12 presence). ROUTED (TODO 3.15).
- **O-F3 (Low): `lint-links.py` boundary note mischaracterized gate 17** ("intra-doc
  anchor resolution"; gate 17 is same- AND cross-document). FIXED IN-WINDOW (one-line
  docstring reword in the gate-62 PR).

Cleared: the full reference family (62/18/17/3 boundaries documented), the
citation-existence family (61 vs 48/49/54/58 disjoint with documented complementarity),
the TODO family (D5/57/45), the language family (2/9/56/51 partitioned by tree with no new
shared surface from the recent gate-2 extensions), a systematic §6 pair skim, and the
rule/skill layering (every adjacent pair carries an explicit delineation).

## Gap lens (5 findings)

- **G-F1 (Medium): the RM-10 pipe-mask class has a feasible, absent mechanical backstop**
  (the guard has no tty detection; three incidents). ROUTED as a maintainer-decision
  machinery proposal (TODO 3.15): a `[ -t 1 ]` fail-loud check with a documented override.
- **G-F2 (Medium): the stall-class subagent rails never reached the worker-brief
  template** (foreground-only, #582/#593/#596; persisted-output no-read, #599), and the
  orchestrator confirmed the #600 census-scope rail was missing too. FIXED IN-WINDOW:
  template rails 11-13 added, template 1.3.2 to 1.4.0.
- **G-F3 (Medium): the names-phase residual was queue-invisible and the design record's
  Status stale** (still PAUSED while the gate ships). FIXED IN-WINDOW: the names-phase
  bullet added to TODO 3.15; the design record's Status flipped to shipped with the two
  as-shipped deltas recorded (the TODO.md exemption; the inline-clause extraction).
- **G-F4 (Low/Medium): GR-8(b) recurrence, live evidence.** The #609 authorized CLAUDE.md
  touch shipped RM-10 but skipped the two carried retro candidates waiting for exactly
  that event (#594 refutation-grep; #593 bare-token fold). ROUTED as a fold-in note on the
  GR-8(b) bullet (a carried-candidates check); the two candidates stay queued for the next
  authorized touch.
- **G-F5 (Low): GR-11's exit-code half refuted at source** (the docstring documents 0/1/2,
  dated to the creation commit). FIXED IN-WINDOW: the bullet amended to the two-parser
  half only.

Plus two D-check candidates ROUTED (TODO 3.15): the D6 TODO-section-close orphan check
(three logged misses) and the pack-README Version/history co-bump check. Convention-only
disciplines adjudicated: separator-tolerance correctly documented-infeasible; the
60-second SOP infeasible-as-gate with the template rails as the partial control (now
landed); the unpiped-guard convention is G-F1's routed proposal. The section-3.15
bullet-by-bullet premise verification found every other bullet accurate (GR-3 wave-2
targets confirmed un-migrated; GR-4 suppression real; GR-6 pair-miss did NOT recur on
gate 62's own prose pair; GR-9/GR-10/S-4 accurate).

## Drift lens (3 findings)

- **D-1 (Medium): the gate-62 design record still said PAUSED while the gate ships** (the
  one actionable field a future session would act on). FIXED IN-WINDOW (same fix as
  G-F3's second half).
- **D-2 (Low): the gate-62 module docstring understated the target set** (heading-only,
  while the code and both spec surfaces include line-initial inline clauses). FIXED
  IN-WINDOW (docstring and failure-message widening).
- **D-3 (Low): two of three pack enumeration surfaces omitted the skeptical pre-push
  verification standard** (the rule's most operationally binding post-1.53 content;
  the project CLAUDE.md bullet carried it, the pack CLAUDE.md paragraph and both pack
  README blurbs did not). FIXED IN-WINDOW (one clause each, pack 1.53.16).

Cleared: eleven areas including the D5 seven-form tri-surface agreement, gates 55/50/2/31/
60/61 docstring-vs-spec fidelity, the guardrail-review skill/command/README suite (the r2
five-agent decomposition adjudicated as implementation latitude, not drift), the
high-assurance enumerations, and the WORKFLOW_DELTA_GATE_STEPS lockstep.

## Synthesis and verification

Eleven lens findings were identified (3 overlap, 5 gap, 3 drift); D-1 and G-F3 name the
same design-record staleness, so the deduped set is TEN findings (3 Medium, 7 Low, 0
High). Every finding re-verified at its cited
source by the orchestrator before routing (the verification batch: the lint-links
wording, the preflight docstring exit codes, the template zero-rail greps, the CLAUDE.md
refutation grep, the design-record Status line). One lens claim was CONFIRMED-with-
correction rather than taken as-is: the gap lens flagged the census-scope rail as
unverified; the orchestrator's grep confirmed it absent and folded it into the G-F2 fix.

## Findings routed

Six distinct fixes landed in-window in the gate-62 PR (O-F3; G-F2; the merged G-F3/D-1
design-record flip; G-F5; D-2; D-3); three findings routed to TODO section 3.15 tagged
`[guardrails]` (O-F1, O-F2, G-F1); G-F4 recorded as a fold-in note on the GR-8(b) bullet.
The two D-check candidates (not defect findings; gap-lens proposals) routed to the same
section alongside. None dropped. Verdict: the machinery
is coherent; the recurring theme of this run is CARRIER FAILURE (lessons adopted
in-session that never reach their durable home: the template rails, the retro candidates,
the design-record status), which the GR-8(b) closure discipline is the queued fix for.
