# Guardrail review r5 (2026-07-04)

Frozen-state record; cross-references accurate as-of write-time. Run on branch `claude/gr4-fence-gate-2r8kn2` (the gate-66 PR, #641), auto-fired by gate 60 at its drift-3 fail threshold (gates 65 to 66, skills 17 to 18, commands 10 to 11). Post-r4-delta focused per the r4 precedent. Inventory at review: **66 gates / 12 rules / 18 skills / 11 commands** (verified against the section-6 table, the governance tree, the skills tree, and the commands directory at the review state).

Mechanical baseline at review start: the branch's post-commit `run_all_audits.sh` failed ONLY on the trigger gate (60) plus a gate-39 count catch fixed mechanically first (the pack growth narrative to sixty-six, pack 1.54.2); all content gates green.

## Overlap lens

Five candidate pairs examined. One fully clean (the RM-10 self-test wiring is layered, not duplicated). One crisp at the delineation level (the claim-fit / matrix-fit / citation-gate boundary is clear in the new skill's own text) but yielding the reciprocal-link Low OV-3. Two deliberate-and-documented pairings with a Low documentation residual each (OV-2, OV-4). One Medium (OV-1):

- **GR5-OV-1 (Medium, ROUTED to 3.15):** the fence-toggle predicate exists in ten-plus carriers in three variants; six private copies are tilde-blind (pre-GR-4 semantics), so a one-backtick-plus-one-tilde file passes gate 66 (even shared-toggle count) while a tilde-blind linter sticks in code mode to EOF. Orchestrator-verified at source (gate 54's `startswith("```")`-only toggle re-read; zero `~~~` tokens in the file). Proposal: shared `is_fence_line()` in `lint_common.py`. Latent (zero tilde fences in the corpus).
- **GR5-OV-2 (Low, ROUTED to the protected-touch bundle):** the close-out checklist's reconcile bullet does not name D7 as the mechanized version-token half (the gate-50 naming pattern is the precedent shape).
- **GR5-OV-3 (Low, FIXED in-window):** matrix-fit's See Also lacked the reciprocal claim-fit link; added in this diff under the same pack 1.54.2 bump.
- **GR5-OV-4 (Low, FIXED in-window):** gate 54's docstring still called per-document ISO coverage "a separate follow-up" after gate 58 shipped; corrected in this diff.

## Gap lens

- **GAP-1 (Medium, ROUTED):** D5's phrase-family design has an open-ended escape tail (nine phrasings across five widenings); the diff-side companion trigger (TODO-heading deletion requires the DONE ledger in the same diff) is folded into the queued eighth-form 3.15 bullet as the design-shape proposal.
- **GAP-2 (Low, split):** gate 66's docstring claimed the exempt trees are unscanned, false for the explicit-paths invocation mode the CLAUDE.md new-pack-prose bullet mandates on `.claude/` files. The docstring honesty fix is IN this diff; the default-population widening is ROUTED as a 3.15 shape call. Live check at review: `.claude/commands` and the pack skills tree all balanced.
- **GAP-3 (Low-Medium, ROUTED):** the figure-drift family's adjacent-enumeration subset is mechanizable as a delta-scoped advisory (count vs its own parenthesized enumeration); routed to 3.15 for maintainer scoping, with the honest subset limits recorded.
- **ADJ-1 (adjudicated, no item):** the no-long-check-ins clause stays convention-only; a `send_later` hook cannot distinguish maintainer-directed reminders from self check-ins and inherits the hook-firing residual. Recorded here as the durable adjudication.
- **ADJ-2 (adjudicated dedupe):** the verifier-fold audit-trail blind spot is convention-class, already folded to the EXPEDITE evidence base at the #640 retro.
- **CLEAN-1:** D7's nine-label set is complete against the live Current-truth line's quoted registers.

## Drift lens

Five surface-sets verified aligned (gate 66's four surfaces vs code including a re-run census; D7's four surfaces; the claim-fit SKILL/command pair including the four-valued vocabulary; gate 54's post-rename carriers; the guardrail-review growth narrative's three counts). Findings:

- **F1 (Medium, ROUTED to the protected-touch bundle, SURFACED in chat):** a live CLAUDE.md self-contradiction: the attended-autonomous rule-3 fallback ("idle on a longer check-in") vs the 2026-07-04 no-long-interval-check-ins clause ("of any shape") in the same file; the maintainer owns the reword or the explicit carve-out.
- **F2 (Medium, FIXED in-window):** the citation-verification spec's coverage narrative predated claim-fit (its out-of-scope acknowledgements read as uninstrumented); a claim-fit pointer sentence added parallel to the matrix-fit one (spec 1.2.8).
- **F3 (Low, DEDUPED):** the missing CLAUDE.md /claim-fit cadence section is the already-staged #630 out-of-window candidate; cross-referenced onto the touch bundle, not duplicated.
- **F4 (adjudicated, no item):** project-only placement of the no-long-check-ins clause is correct; the pack's wait guidance never recommends what the clause forbids.
- **F5 (Nit, FIXED in-window):** the garbled comment above D7's SURFACES table.

## Orchestrator synthesis and verification

Findings deduped by (surface, item, lens): no cross-lens duplicates (F3 deduped against a pre-existing staged item, not another lens). Every routed or fixed finding re-verified at source before action: GR5-OV-1's tilde-blind claim re-read in gate 54's code; GAP-2's explicit-paths claim consistent with the linters' argument handling; F1's two clauses both quoted from the live CLAUDE.md; F2's zero-hit grep reproduced. Fixed-in-window claims verified against the diff before this record was written (the five folds each grepped present).

## Findings routed

Tally: 14 items surfaced across the lenses (4 overlap + 6 gap + 4 drift findings/adjudications with a nit; F3 is recorded as a dedupe against a pre-existing staged item, not a 15th). Dispositions: 5 FIXED in-window in this diff (OV-3, OV-4, GAP-2's docstring half, F2, F5), 4 ROUTED to TODO 3.15 (OV-1, GAP-1's fold, GAP-3, GAP-2's shape half), 1 protected-touch bundle bullet carrying TWO items (OV-2 + F1) with F3 cross-referenced, 3 adjudications recorded here (ADJ-1, ADJ-2, F4), 1 clean check (CLEAN-1). The disposition labels total 15 because GAP-2 appears in both the FIXED and ROUTED lists (one item, two halves); distinct items remain 14. None dropped. Machinery verdict: coherent; the recurring theme this run is DIVERGENT COPIES (the fence predicate) and DOC-VS-INSTRUMENT lag (two specs/docstrings describing pre-instrument or pre-sibling states), both bounded.
