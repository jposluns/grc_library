# Guardrail Review r6, 2026-07-08

## Trigger and state snapshot

- **Trigger**: auto-fired by gate 60 on the `claude/reference-audit-skill` branch (PR B, #707). The machinery inventory drifted 4 (threshold 3) since the r5 review (2026-07-04, token 66/12/18/11): skills 18 to 20 (+2: `deep-assessment` #702, `reference-audit` this PR), commands 11 to 13 (+2, the paired slash commands). This is the sixth consecutive threshold firing of the auto-prompt cadence; the run rides in PR B.
- **Baseline**: `tools/run_all_audits.sh` at the review state = 65/66 with only gate 60 (the trigger) red; the parity gates 35 / 37 / 39 / 41 / 44 all green (the reference-audit wiring is correct: PAIRS entry added, skills count 20, skills tree entry, `derives_from` resolves). Full clone confirmed (not shallow). This is the expected auto-fire baseline.
- **Inventory under review**: 66 gates / 12 rules / 20 skills / 13 commands (verified against the spec §6 table, the governance tree, the skills tree, and the `.claude/commands/` directory at the review state).
- **Scope**: the post-r5 machinery delta: the `deep-assessment` skill + `/deep-assessment` command + register + the three advisory tools (#701/#702); the AIQT apex-rule reframe of `project-integrity.md` + the pack-wide restatements (#705); the `reference-audit` skill + `/reference-audit` command + tool added this PR.

## Overlap lens

**0 findings.** Every candidate overlap examined is deliberate-and-documented, confirmed with `path:line`:

1. `/reference-audit` vs `/matrix-fit` vs `/claim-fit`: same design pattern (recall-oriented advisory tool + cadenced semantic judge above existence gates), disjoint ground (breadth / control-code fit / value precision); explicit delineation at `reference-audit/SKILL.md:69` ("It is to reference breadth what `/matrix-fit` is to control-code fit and `/claim-fit` is to claim precision") and `:210` ("Breadth, fit, precision, and existence are four different questions; this skill only answers breadth"). All three share `derives_from: evidence-grounded-completion.md`.
2. `/deep-assessment` composing the six (now seven) instruments: composition by invocation, not restatement, with an explicit anti-drift rationale at `deep-assessment/SKILL.md:220` ("composes them by invocation and deliberately does not restate their procedures, so they cannot drift") and non-substitution at `:51`.
3. `/deep-assessment` vs `/trust-recovery`: `derives_from` trust-recovery-escalation; inheritance scoped to three named conventions at `SKILL.md:23`, not a blanket clone. The one apparent tension (phase-7 in-window fix PRs vs trust-recovery's hold-until-sign-off) cleared: deep-assessment's trigger is explicitly not a lapsed-confidence hold.
4. Phase 4(d) hand parity re-derivation vs the mechanical parity gates: deliberate belt-and-suspenders that refuses to trust the parity gate (`SKILL.md:112`, red flag `:175`).
5. The three new advisory tools occupy distinct functions, each stamped "not a gate"; blindspots + mutation are complementary halves of phase 4.
6. AIQT apex rule vs the rest of the pack: no contradiction (`project-integrity.md:106` states substance unchanged; the one describing sibling migrated in lock-step).

Out-of-lens note (routed to the drift lens): `.claude/CLAUDE.md:916` retained "**Quality > Speed remains the tiebreaker**", a stale AIQT-phrasing carrier (substance-consistent). Folded into drift finding D1.

## Gap lens

**2 findings**, both apply-time-verified.

```
ruleId: convention-with-phantom-queued-backstop   level: warning (Medium-High)
location: .claude/CLAUDE.md (the per-touch reference-breadth close-out bullet)
finding (G1): the NEW per-touch reference-breadth close-out obligation (added this PR)
  is silently skippable (no gate detects a corpus-body-touching PR that omits the
  per-touch run or the doc-state refresh, the class gate 50 backstops for /validate-pr),
  AND its parenthetical claim that "the mechanical staleness backstop is a queued TODO
  item" was FALSE when written: a grep of TODO.md / design-decisions / the SKILL / command
  / deferred-protected-changes returned zero hits. So the gap was both unbacked and
  untracked, and the "queued" claim breached the accepted-unverified-tracker corollary.
  APPLY-TIME VERIFICATION: CONFIRMED (grep returned zero hits at the review state).
```

```
ruleId: recurring-failure-mode-uncodified   level: note (Low-Medium)
location: .working/improvement-log.md #702 (candidate), #704 (standing)
finding (G2): the "worker-machinery-claim propagated without apply-time verification"
  failure mode (2nd occurrence at #702) reached the adopter-facing root CHANGELOG (the
  gate-42/44 mislabel) and cost the #702/#703/#704 three-PR churn. It is NAMED as a
  candidate in the improvement log (a worker-brief rail + a checklist line) but codified
  into NO durable guard: the worker-brief-template carries control-CODE rails (6, 9) but
  no gate-NUMBER rail. APPLY-TIME VERIFICATION: CONFIRMED (worker-brief-template grep
  found rails 6/9 for codes, none for gate numbers; improvement-log #702/#704 confirm the
  candidate stands uncodified).
```

## Drift lens

**2 findings**, both apply-time-verified; both the same root class (stale AIQT-migration carriers #705 missed because its completion grep was phrasing-specific, not bare-token).

```
ruleId: aiqt-reframe/partial-migration   level: warning (Low-Medium)
location: surface-counterproductive-instructions.md:18 (BOTH trees); pack CLAUDE.md:141;
  + .claude/CLAUDE.md:916 (the overlap lens's out-of-lens carrier, folded here)
finding (D1): #705's /validate-pr asserted the AIQT migration COMPLETE ("every surviving
  Quality > Speed > Cost string is a whitelisted survivor"), but that grep matched only
  the ">" form and missed the variant phrasings "Quality versus Speed versus Cost" (rule
  :18 both trees + pack CLAUDE.md:141) and "Quality > Speed remains the tiebreaker"
  (.claude/CLAUDE.md:916). Meaning preserved (Quality is a co-equal top-tier facet still
  above Speed), so Low-Medium; but it contradicts a claimed-clean #705 surface, so it is a
  genuine miss (the completion-grep pattern-width class the project's own bare-token
  discipline warns against). APPLY-TIME VERIFICATION: CONFIRMED by an independent
  bare-token grep (`quality (>|&gt;|versus|vs) speed`); 4 live carriers found, plus the
  whitelisted provenance/CHANGELOG/version-history survivors.
```

```
ruleId: deep-assessment/description-underenumerates   level: note (Low)
location: deep-assessment/SKILL.md:3 (frontmatter description); .claude/commands/deep-assessment.md:1
finding (D2): this PR extended the phase-3 instrument list to seven (adding
  /reference-audit) on both surfaces (they match each other), but the one-sentence
  description parenthetical and the command opener still enumerated only the original six
  as "the existing semantic instruments composed by invocation". Identical omission across
  both surfaces (not a SKILL-vs-command drift). APPLY-TIME VERIFICATION: CONFIRMED (both
  surfaces listed six; phase-3 body lists seven).
```

Surfaces confirmed consistent (no finding): the two apex-rule copies byte-identical; the bracket form `(Accuracy = Integrity = Quality = Trust)` uniform (25 canonical occurrences); the checkpoint line uniform across the rule, `.claude/CLAUDE.md`, and the README baseline; both new skills' descriptions vs command / README tree / `derives_from` / CLAUDE.md cadence sections agree; the three advisory tools' "not a gate" framing consistent; every gate-number citation (37 / 44 / 59 / 60; no stale "gate 42 = paired-skill") correct against spec §6; the growth counts sixty-six gates / twelve rules / twenty skills match live.

## Orchestrator synthesis and verification

Four distinct findings across the three lenses (overlap 0, gap 2, drift 2), 0 cross-lens duplicates. Every finding orchestrator-re-verified at source before action (the greps above). Severity adjudication: G1 Medium-High (highest); D1 Low-Medium; G2 Low-Medium; D2 Low. None refuted.

Fixed-in-window vs routed:
- **D1** (AIQT partial-migration): FIXED in-window in PR B. All 4 live carriers migrated (`surface-counterproductive-instructions.md:18` in both trees to "the AIQT tier ... Speed ... Cost"; pack `CLAUDE.md:141` to "the AIQT-tier / Speed / Cost tradeoff"; `.claude/CLAUDE.md:916` to "The AIQT tier above Speed remains the tiebreaker"). Re-verified: a post-edit bare-token grep returns zero live carriers (only the whitelisted provenance paragraph and the #705 CHANGELOG entry remain); the two rule copies stay byte-identical (gate 37 `diff` = IDENTICAL).
- **D2** (deep-assessment description): FIXED in-window in PR B. `/reference-audit` added to the six-instrument enumeration on both the SKILL frontmatter description and the command opener, matching the phase-3 body's seven.
- **G1** (per-touch backstop / false claim): the false-claim half FIXED in-window (the backstop is now a real TODO item, section 3.15 `[guardrails]`, so the CLAUDE.md "queued TODO item" phrase is backed); the backstop itself ROUTED to that item as a proposed machinery change (a Dn PR-time delta check, maintainer-decision).
- **G2** (worker-brief rail): ROUTED to TODO section 3.15 `[guardrails]` as a proposed machinery change (a worker-brief-template DO-rail), NOT applied autonomously per the skill's route-machinery-proposals discipline; surfaced in chat with a recommendation to codify.

## Findings routed

- TODO section 3.15 (`[guardrails]`): two new bullets, (r6) `/reference-audit` per-touch staleness backstop [G1] and (r6) worker-brief gate-number-verification rail [G2].
- In-window fixes (D1, D2, the G1 false-claim half) ship in PR B under its per-PR QA cadence.

None dropped. The review is a single pass (structural findings are maintainer-decision proposals); it terminates on routing + chat surfacing.
