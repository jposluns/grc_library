# Guardrail-review r7 (2026-07-09)

**Trigger.** Auto-fired: gate 60 (guardrail-review cadence) hit its fail threshold, machinery drift = 3 since r6 (2026-07-08): gates 66 to 67 (gate 67 Document-Type enumeration parity, #717), skills 20 to 21 and commands 13 to 14 (the new `publication-screening` skill + `/screen-publications` command, the TODO 2.11 build PR). Seventh consecutive threshold firing.

**Baseline.** On branch `claude/pubscreen-2.11-corpus` (working tree, the uncommitted 2.11 build). `tools/run_all_audits.sh` = 66/67 green with only the trigger gate 60 red; parity gates 35 / 37 / 39 / 41 / 44 all green; clone non-shallow. Inventory under review: **67 gates / 12 rules / 21 skills / 14 commands** (verified against the spec §6 table, the governance tree, the skills tree, and the `.claude/commands/` directory at the review state). Scope: the post-r6 delta (gate 67; the `publication-screening` skill + `/screen-publications` command + `scan-publication-instruction-content.py` scanner + wiring).

## Overlap lens

**0 accidental-overlap findings.** The five skill delineations are all deliberate and documented, verified against the neighbours' actual scope: `publication-screening` (admission control for the untrusted publications bucket) vs `reference-audit` (breadth, "held but unused"), `claim-fit` (attribution precision), `matrix-fit` (control-code fit), `citation-quote-verification` (quote-to-source correspondence). The command's "distinct from /reference-audit / /claim-fit / /matrix-fit" clause is accurate. Gates 67 / 41 / 35 are a parity-check family over three DISJOINT collections (the Document-Type set backed by `lint-metadata.py` constants; the rules+skills collections backed by directory listings; the audit-gate names backed by the spec §6 table), no collection checked by two, no redundancy.

One Low reverse-direction note (folded into the gap finding, same root): `reference-audit/SKILL.md:49` said the publications bucket is excluded "pending the publications-assessment process" (reads as not-yet-existing) and carried no back-link to the now-shipped `publication-screening` skill.

## Gap lens

**1 finding.**

- **G1 (Low, the reference-audit half elevated toward Medium): missing bidirectional `## See Also` back-references.** `publication-screening/SKILL.md:196,199` forward-link `reference-audit` and `claim-fit`, but neither reciprocated (`grep "publication-screening\|screen-publications"` returned zero hits in both siblings' See Also). `skill-authoring-discipline/SKILL.md:47` (step 6) requires the reciprocity ("uni-directional cross-references rot; bi-directional ones survive maintenance"), a completion criterion at `:71`, and improvement-log #257 names "bidirectional See Also" as new-skill wiring surface 5. No gate inspects See-Also reciprocity (gate 32 = `derives_from` only; 41 = README tree; 3 = link depth; 44 = PAIRS step-parity), so it is convention-only and slipped. The reference-audit direction is functionally load-bearing: `publication-screening/SKILL.md:131-134` states screening gates reference-audit's publications tier, so a reader arriving at reference-audit had no pointer to the skill governing whether that tier may run.

All other required surfaces confirmed wired: README skills-tree row (gate 41), `derives_from` (gate 32), slash-command pair + PAIRS (gate 44), repo-internal link depth (gate 3). No warranted grc_library-side enforcement gap (the register-completeness gate correctly lives ref-side as `validate.py` check 14; the "pending never steers authoring" / "screening never upgrades trust" disciplines are gate-blind by construction and honestly framed as backstop). The scanner needs no freshness anchor (stateless; its state is the ref-side register) and is auto-covered by `/deep-assessment`'s inventory-deriving phase 3. Gate 67's whole-file-scan limitation is already tracked (TODO 3.23).

## Drift lens

**2 findings, both Low (token/precision, no meaning contradiction).**

- **D1 (Low): ref-gate failure-condition granularity.** `publication-screening/SKILL.md:33-35` (Overview) states the precise three-part form ("fails on a missing row, an unknown status, or an orphan row"); `SKILL.md:124-125` (step 5), `screen-publications.md:11`, and `.claude/CLAUDE.md` compressed it to "missing or malformed row". A superset relationship, not a contradiction ("malformed" subsumes "unknown status" but not "orphan row", a join failure). No reader misled about what the gate broadly does.
- **D2 (Low): CLAUDE.md rendered the fourth verdict as prose.** `.claude/CLAUDE.md` had "discard candidates" (un-hyphenated, un-backticked) where the other four surfaces (SKILL, command, both README locations) use the canonical token `discard-candidate`; a corpus grep for the token found no CLAUDE.md hit.

All five description surfaces otherwise consistent (what the skill does, admission-not-trust semantics, cadence, not-a-gate, verdict vocabulary); gate 67 docstring matches the spec §6 prose; the growth-narrative counts (`twelve` rules / `twenty-one` skills / `sixty-seven` gates at `guardrail-review/SKILL.md:93`) match the live inventory.

## Orchestrator synthesis and verification

All findings re-verified at source before action (the `path:line` quotes confirmed live). Adjudication: G1 and the overlap reverse-direction note share one root (reference-audit's stale relationship to the now-shipped skill), fixed together. Severity: G1 Low-to-Medium (load-bearing reference-audit half), D1/D2 Low. No cross-lens duplicates beyond the G1/overlap fold.

## Findings routed

**Fixed in-window (this PR):**
- G1 + overlap note: added the reciprocal `## See Also` "Related skill `publication-screening`" bullet to both `reference-audit/SKILL.md` and `claim-fit/SKILL.md`; reworded `reference-audit/SKILL.md:49-50`'s "pending the publications-assessment process" to name the now-live `/screen-publications` process (the screening wave plus the maintainer's inclusion decision as the remaining triggers).
- D1 + D2: `.claude/CLAUDE.md` gate-condition parenthetical expanded to the accurate three-part form, and "discard candidates" changed to the canonical `discard-candidate` token.

**Routed to TODO 3.15 `[guardrails]` (maintainer-decision):**
- The bidirectional See-Also parity gate (r7, M/M): a gate that asserts, for each skill A whose See Also links sibling B as a "Related skill", B's See Also links back to A. Closes the one required new-skill surface still on authorial discipline alone (recurring class #213/#257/r7).

None dropped.
