# Guardrail review r9 (2026-07-12)

**Trigger:** the §4.8 Phase-2 pack-hygiene apply (14 skills generalized in place, 2 new derived skills taking skills 21 to 23, the new `rule-provenance.md` register, the pack README rework) tipped gate 60's cadence to the drift-3 fail threshold. Maintainer-invoked as the MASTER-PLAN Phase-2 obligation.

**Baseline:** `tools/run_all_audits.sh` = 68 of 69 green with only the trigger gate 60 red; all mechanical parity gates (35 gate-name, 37 claude-rules-sync, 39 count-consistency, 41 collection-enumeration, 44 paired-skill step-parity) green; clone non-shallow. Inventory: **69 gates / 13 rules / 23 skills / 14 commands** (skills tree on disk = 23 = README-tree entries).

## Overlap lens

No accidental overlap. The two new skills delineate cleanly and every overlap is deliberate-and-documented:
- `validate-inference` vs `surface-instruction-concern`: distinct triggers, each routing its out-of-scope case (`validate-inference/SKILL.md:36-38` routes decision-doubt to `clarify-before-acting`; `surface-instruction-concern/SKILL.md:42-46` routes ambiguity to `clarify` and the AIQT tradeoff to `project-integrity`).
- `validate-inference` vs `validation-sweep`: the per-premise form, reciprocated at `validation-sweep/SKILL.md:272`.
- `rule-provenance.md` vs README `## Version history` vs the removal ledger: division of labour stated at `rule-provenance.md:25-30` and echoed at `README.md:34,36`.

**LOW observation (not a defect):** rule-origin narrative now lives in three surfaces (`rule-provenance.md`, the README version history, and the pack `CLAUDE.md` rollout paragraph). Documented as deliberate; flagged for maintainer awareness as a maintenance cost.

## Gap lens

**MEDIUM (FIXED in-window):** uni-directional skill cross-reference. `clarify-before-acting/SKILL.md:79` links forward to `surface-instruction-concern` as a "Related skill", but `surface-instruction-concern`'s See Also linked only the clarify-before-acting RULE, not the clarify-before-acting SKILL back. This is the reciprocity skill-authoring step 6 (`skill-authoring-discipline/SKILL.md:58`) requires and that no gate enforces (TODO §3.12). **Fixed:** added a reciprocal `Sibling skill [`clarify-before-acting`](../clarify-before-acting/SKILL.md)` line to `surface-instruction-concern/SKILL.md`'s See Also.

Everything else clean: both new skills' `derives_from` resolve to real rules; both present in the README tree; the other new-skill links (`validate-inference` ↔ `surface-instruction-concern`, `validate-inference` ↔ `validation-sweep`) are bidirectional; both new skills correctly unpaired (no command sibling, no PAIRS entry); no leaked project-specific references in either new skill; the generalized-skill body-leak scan found no genuine leaks (the only hits are the fitness-review skill's own output id-scheme, wired, and a generic "N gates" phrase); the provenance register covers all 13 rules.

## Drift lens

No drift. Each new skill's `description` tracks its parent rule's scope; the README tree/count/positioning matches 23 skills; `rule-provenance.md` per-rule dates match the rules on spot-check (`:106` vs the rule's 2026-06-21; `:138-140` vs 2026-07-08); no generalized skill's `## Project wiring` section contradicts its body.

**LOW observation (low confidence):** `rule-provenance.md:5` carries `**Version:** 1.1.0` on a brand-new file (1.0.0 would be conventional). Not clearly wrong (may reflect worker-fragment iteration); left as delivered, flagged for a maintainer glance rather than an arbitrary renumber.

## Orchestrator synthesis and verification

All lens findings apply-time-verified against the live files. The one Medium (See-Also reciprocity) was confirmed by reading both See Also blocks (surface-instruction-concern linked the clarify RULE, not the clarify SKILL; clarify:79 linked this skill forward) and FIXED in-window in this Phase-2 PR. The two Low items are maintainer-awareness observations (three-surface origin narrative; the new register's 1.1.0 version), routed as notes, not defects, not applied.

## Findings routed

- MEDIUM gap (See-Also reciprocity): FIXED in-window (no routing needed).
- LOW overlap (three-surface origin narrative): surfaced to the maintainer as an awareness note; no backlog item (the division is documented-deliberate).
- LOW drift (`rule-provenance.md` 1.1.0): surfaced for a maintainer glance; no backlog item.

## Resulting PR

The §4.8 Phase-2 apply PR (this run rides in it). Gate 60 cadence reset by this row.
