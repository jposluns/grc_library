# Guardrail review r10 (2026-07-13)

The `/guardrails` pass of record for `/deep-assessment` r3 (Phase 3 coverage obligation; resets the gate-60 cadence). Three lens subagents (overlap, gap, drift) dispatched read-only over the live machinery. Mechanical baseline green at `a5d2edd` = 69/69 (parity gates 35/37/39/41/44 all green); clone non-shallow; inventory **69 gates / 13 rules / 23 skills / 14 commands / 9 advisory tools** (derived from the repo, not remembered). Every finding orchestrator-re-verified at source before routing.

## Overlap lens

**0 genuine findings.** 11 candidates examined across the named families (the citation/currency/control-code existence-gate family, the section-reference cluster gates 17/18/62/65, the two `shall` gates 9/56, the numeric-consistency pair gates 25/55, the ageing/cadence trio gates 27/43/60, the skill gates 32/44, the validation-skill family, the semantic trio /matrix-fit + /claim-fit + /reference-audit, and the five inter-related governance rules). Every overlap is DELIBERATE-AND-DOCUMENTED layered defence: each surface carries an explicit bounded-scope or "complementary, not redundant" / "NOT a substitute" note and cross-references its siblings. No accidental redundancy, no contradiction.

- **Sub-note (maintainability, routed):** gate 39's docstring names and mirrors gate 41's collection sources (`# mirror gate 41's collection sources so the two gates agree on what counts`), but gate 41's docstring is silent about gate 39. One-directional documentation; a future editor of gate 41 has no pointer to the shared collection-source logic. Not a coverage defect (the two check genuinely different surfaces: prose count numbers versus enumerated item sets).

## Gap lens

**2 warnings + 3 notes.**

- **G1 (warning): branch-to-main edit unguarded.** `improvement-log.md:62` (#884) states the HARD sequencing rule AND that its proposed hook was never built; `improvement-log.md:333` (#595) records an earlier edit-on-main slip, so #884 is a 2nd+ occurrence. Only `.claude/hooks/block-verification-pipes.py` exists; no hook checks HEAD==main. Current backstop is fail-loud-at-PUSH (refspec mismatch), which fires only after edits accumulate on main. An FP-free PreToolUse hook (warn/block on Edit/Write when `git symbolic-ref --short HEAD` == `main`) is feasible. ROUTED to TODO §3.62.
- **G2 (warning): count-gate idiom-coverage gap.** `improvement-log.md:597` (#272) records a 3rd+ occurrence pattern: a count in a non-standard idiom or word-form escapes gate 39 (spec §5 describes gate 39's word-form coverage as "narrow, anchored" by design). The low-FP digit-idiom half ("N automated audits") is a routable regex extension; the free-prose word-form half is FP-prone and convention-territory. ALREADY-TRACKED as the standing #272/#465 maintainer expire-versus-pursue call; cross-referenced in §3.62, no new item.
- **G3 (note): register row-ordering ungated (one-of-a-pair).** The `deep-assessment/register.md` run-table has no ordering gate; the #888 mis-ordered r3 row reached main (caught post-merge). Sibling structured-bookkeeping files ARE gated (detailed-mirror by gate 59, lease by gate 63). A date-monotonicity check is FP-free feasible; the #888 WATCH's stated objection is low-VALUE, not infeasibility. ROUTED to §3.62 as a maintainer value call.
- **G4 (note): per-touch reference-breadth convention-only.** `.claude/CLAUDE.md:560` self-flags the mechanical staleness backstop as "a queued TODO item"; a known-tracked residual, not a silent gap. Cross-referenced in §3.62, no new item.
- **G5 (note): #376 paired-surface gate candidate persists.** `improvement-log.md:517` labels it the "top routed-not-built candidate", but its mechanizable half shipped as gate 50 Check 1 and its residual is free-prose. ROUTED to §3.62 for a maintainer expire-or-rescope disposition.

## Drift lens

**3 warnings, single-root-cause cluster (FIXED in-window this PR).** The TODO 3.16 root-CHANGELOG reformat to the compact `**date | version | PR #N**` one-line form (#855/#857) updated the parsing CODE of gates 29/50/59 (all now match both header shapes) and the change-tracking rule + CLAUDE.md prose, but left three DESCRIPTIVE surfaces asserting the superseded `## YYYY-MM-DD, Library Version X, PR #N` shape:

- **D1** `tools/lint-bookkeeping-parity.py:28-29` (gate-50 docstring Check-1 narrative). The root file carries zero `## ...` headers (`grep -cE '^## [0-9]{4}-' CHANGELOG.md` = 0); the CODE (`CHANGELOG_HEADER` alternation) matches both forms.
- **D2** `tools/lint-changelog-mirror-header-parity.py:5,9-11` (gate-59 docstring: "lead-paragraph summaries" + "same set of per-PR `## ...` headers"). The code has `HEADER_RE` + `COMPACT_HEADER_RE`.
- **D3** `governance/specification-audit-programme.md` §6 narrative for gate 59 ("matched on the `## <date>, Library Version <version>, PR #<number>` header shape").

Risk: a future maintainer reading these narratives could "correct" the code back to a `##`-only regex, reintroducing the orphaned-header defect (#388) gate 59 exists to catch. Below the parity gates' resolution (gates 35/39 inspect the §6 table identity and row count, never the header-format prose). **FIXED this PR:** all three reworded to name both the compact form and the legacy form; the spec doc bumped `1.17.1`->`1.17.2` with taxonomy + scorecard regenerated. Negatives sampled clean (parity-gate number mappings, step-count claims, subagent/persona counts, collection-enum wiring claim, referenced-tool existence).

## Orchestrator synthesis and verification

Each finding was re-verified at source before action: the three drift surfaces confirmed against the live docstrings/narrative AND the live code (which matches both header forms) AND the root CHANGELOG (zero `## ...` headers); the gap findings confirmed against the cited improvement-log rows, the hook directory listing, and the CLAUDE.md self-flag. The drift cluster is clear-mechanical (documentation-accuracy to match working code, zero behaviour change) and was FIXED in-window per the deep-assessment/trust-recovery clear-mechanical-in-window convention (r1 F1/F3/F6/F12, r2 F1-F4 precedent). The gap warnings/notes and the overlap sub-note are machinery-change PROPOSALS or maintainer disposition calls, ROUTED to TODO §3.62 tagged `[guardrails]`, none applied autonomously.

## Findings routed

- TODO §3.62 (`[guardrails]`): G1 branch-to-main hook (warning, machinery proposal); G3 register-ordering guard (note, value call); G5 #376 candidate expire-or-rescope (note, disposition); overlap gate-41-docstring symmetry (note, trivial). G2 (count-gate idiom) and G4 (per-touch ref-breadth backstop) cross-referenced as already-tracked, no new item.
- Drift D1/D2/D3 FIXED in-window (this PR).

Single-pass termination: findings routed and surfaced; the review does not loop (structural findings are maintainer-decision proposals). Feeds the `/deep-assessment` r3 finding set; disposition at r3 sign-off.
