# Guardrail-review r11 (2026-07-17)

Auto-prompted by the §1.19.6 `/adopt` machinery addition (a new pack skill + slash command),
which drove the gate-60 machinery-inventory drift to 3 (gates 69->70 in #995, skills 23->24,
commands 14->15) and thus mandated a `/guardrails` review before the machinery could grow
further. Rides in the §1.19.6 PR (#998).

- **Baseline inventory (derived from the repo):** 70 gates / 13 governance rules / 24 pack
  skills / 15 slash commands / 9 advisory tools.
- **Parity gates:** 35 / 37 / 39 / 41 / 44 green with the new `adopt` skill+command pair
  registered; the collection-enumeration and gate-count gates green at 24 skills.
- **Method:** three read-only lens subagents (overlap, gap, drift), focused on the `/adopt`
  addition (SKILL.md + command + PAIRS registry + pack README skill-tree/version + the
  `/resume` adopter-path wiring + the `detect-env.py` origin-identity probe), plus a lighter
  whole-machinery scan. Every genuine finding was orchestrator-re-verified at source before
  action. Shared-tree safety observed (git inspected read-only; no checkout/reset).

## Findings and dispositions

### Overlap lens

- **W1 (FIXED in-window).** `/adopt` step 4's self-contained branch instructed bootstrapping
  the in-repo `.ref` from the reference-acquisition manifest, which contradicts gate 70 (the
  stub-guard requires `.ref` to hold only its README) and the skill's own step 7 (green sweep).
  Fixed: step 4 (SKILL + command) reworded so the reference-base bootstrap targets an EXTERNAL
  `grc_library_ref` sibling (the own-siblings model); the in-repo `.ref`/`.scratch`/`.private`
  always stay stub-only; the self-contained model uses the manifest as a bibliography only.
- **N1 (ROUTED §3.92).** The maintainer/adopter/fresh-machine classification + its STOP/HALT
  conditions are restated across four surfaces (detect-env probe_identity + decisions,
  resume.md, adopt SKILL step 1, adopt command step 1) with no parity gate: a coupled set.
- **N2 (FIXED in-window).** Step 3 empties the pending-decisions / verifier-override queues and
  the deep-assessment register, which carry never-silently-drop invariants; the skill did not
  name this as a sanctioned exception. Fixed: step 3 now states it is a sanctioned exception
  scoped to a confirmed adopter clone (the items are the maintainer's), not a breach.
- N3: clean (probe_identity consumes probe_siblings, no duplicate sibling detection).

### Gap lens

- **W (adopt-config not tool-validated; ADDRESSED in-window + mechanical routed §3.92).**
  `.claude/adopt-config.json` is machine-consumed but the "already onboarded?" check was
  assistant-presence-only, with no branch for a malformed config. Fixed in-window: `resume.md`
  now parses/validates the config and re-proposes `/adopt` on a malformed one. Mechanical
  backstop (detect-env emits `adopt_config_present` + a validity flag) routed §3.92(a).
- **W (non-gated-shape not sweep-verified; FIXED in-window).** Step 7's green-sweep control
  only covers the GATED reset surfaces; the gate-exempt `.working/` majority's shape is
  convention-verified only. Fixed: steps 3 and 7 now name the gated-vs-exempt verification
  asymmetry and direct a parse-check of the exempt surfaces (the adopter's first `/resume` is
  the real check).
- **N (no mechanical maintainer-clone guard; ROUTED §3.92(b)).** `/adopt` is
  assistant-executed; the dangerous direction (resetting a maintainer clone) is defended by
  the host-pinned origin match + step-1 confirmation + config short-circuit + git-revertability.
  An optional pre-flight helper that exits non-zero on a non-`adopter` classification is routed.
- **N (resume detect-env-error fallback; FIXED in-window).** `resume.md` now treats an
  unavailable/errored probe as UNDETERMINED identity and asks the operator, rather than
  assuming maintainer-mode.

### Drift lens

- **W (stale forward-pointer; FIXED in-window).** `detect-env.py` :206 (docstring) and :309
  (adopter decision string) still framed the adopter path as "wired in TODO section 1.19.6 /
  a not-yet-built command", false once this PR builds `/adopt`. Fixed: both now name the
  now-built `/adopt` and the `/resume` proposal path.
- **N (frontmatter omitted step 5; FIXED in-window).** The SKILL `description:` omitted "strips
  maintainer-only residue" that the three sibling surfaces carry; added.
- **N (`derives_from: session-lifecycle`; no action).** Defensible: `/adopt` resets the durable
  handoff + working-state registers session-lifecycle §1 defines and is consumed by the
  `/resume` flow that rule governs. The framework-alignment table leans toward
  audit-trail-integrity; flagged for a maintainer glance, assessed no drift.
- Counts clean: CLAUDE.md command-count note = 15; pack skill-tree includes `adopt`; the
  1.62.0 row says twenty-fourth skill / 23->24; no stale live "14 commands" or "23 skills".

## Outcome

Six in-window fixes applied to the `/adopt` surfaces before push; three hardenings routed to
TODO §3.92 as maintainer-decision proposals; the `derives_from` note assessed no-action. The
`/adopt` core design (adopter-clone-only, run-once, product-preserving, honest clean-baseline
reset) is sound. Single-pass review; not a fix-to-fixed-point loop.
