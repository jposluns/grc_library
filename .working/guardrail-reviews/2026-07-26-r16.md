# Guardrail Review r16 (2026-07-26)

**Trigger.** Auto-prompted by the `/restore-broken` command addition in PR #1183, which drove the gate-60 machinery-drift
counter to 3 (gates 76 to 78 = +2 from the prior session's gates 77/78; commands 15 to 16 = +1 this PR) and mandated the
review, resetting the gate-60 cadence.

**Basis.** OFFLOADED to worker `opus-20260726T123931Z-f6b9`, READ-ONLY git at `8bcfadf5` (the #1183 commit), ref
`db08742`. The three lenses were run over the machinery ADDED since r15 (2026-07-24): gate 77
(`tools/lint-gate-citation-inventory.py`, "Gate-name-citation inventory audit"), gate 78
(`tools/lint-todo-number-permanence.py`, "Backlog item-number permanence audit"), and the new command
`.claude/commands/restore-broken.md`. Every positive finding was orchestrator-re-verified at source before routing.

**Run-number note.** The dispatched order was misnamed `guardrails-r12`, but runs r12 through r15 already exist in the
history; the actual next run is r16, which this record uses. The mislabel is on the order id only, not the review.

## Inventory (measured at `8bcfadf5`)

**78 gates / 15 rules / 24 skills / 16 commands.** Worker-measured against the spec section-6 inventory table (78 rows,
contiguous 1-78, no gaps or duplicates; gates 77 and 78 present and wired across all four parity surfaces: spec section 6,
`tools/run_all_audits.sh`, CI `quality.yml`, `.pre-commit-config.yaml`), the governance tree
(`dev-security/claude-rules/governance/*.md` minus README = 15), the skills tree
(`dev-security/claude-rules/skills/*/` = 24), and the commands directory (`.claude/commands/*.md` = 16). Orchestrator
re-verified the four counts. All match the gate-60 as-of expectation.

## Lens: OVERLAP (0 findings)

- **Gate 77 vs gates 35 / 39 / 64.** Deliberately carved, documented, not accidental. Gate 35 checks gate-name parity
  across the DECLARATION surfaces; gate 39 checks the gate-COUNT; gate 64 checks per-gate detailed-prose PRESENCE in the
  spec. Gate 77 checks gate-name CITATION accuracy in the PROSE citation surfaces against section 6, and its docstring
  explicitly excludes the spec ("the ground truth, not a citation surface") and is count-free by construction (the
  gate-39 trap).
- **Gate 78 vs gates 45 / 57.** Distinct TODO-hygiene concerns: gate 45 (stale references / resume cursor), gate 57
  (self-marked-done in place), gate 78 (recycled number / stale counter). No shared coverage.
- **`/restore-broken` vs `/resume`.** Documented counterpart, not redundancy: `/resume` rebuilds from a CLEAN handoff,
  `/restore-broken` from NO clean handoff, and its Phase E hands off TO `/resume` as the compensating control.

## Lens: DRIFT (0 findings)

- Gate 77's docstring matches its code (`_check_citation` flags exactly the two documented shapes; `_is_name_citation`
  skips role-glosses and bare `gate N`); self-test 9/9.
- Gate 78's docstring matches its code (RECYCLE and COUNTER checks both present); covered by the gate-36 regression
  (`TodoNumberPermanenceTests`), so the lack of an in-module `--self-test` flag is a cosmetic convention difference, not
  a gap.
- `/restore-broken`'s frontmatter description maps 1:1 to its five phase headings (A assess, B report/ask/GO, C recover,
  D wind down, E same-session resume). No divergence across surfaces.

## Lens: GAP (1 finding, LOW, maintainer-decision proposal)

**G-1 (LOW, proposal).** The `TODO.md` never-recycle rule is ABSOLUTE (a number maps to exactly one item across the whole
history), but gate 78 (`tools/lint-todo-number-permanence.py`) enforces only the RECORDED-retirement half. Its own
docstring discloses the boundary: a `.working/DONE.md` entry heading carrying no `§N.M` id is invisible to the recycle
check, and most DONE headings state no id, so a number whose retirement was never recorded with its id could be recycled
undetected. The docstring names the closure (adopt a DONE.md heading convention always carrying the retired id, then
extend gate 78 to it), which was UNQUEUED. Re-verified at source: the docstring disclosure is present, and a `TODO.md`
search returns no existing tracker for the closure.

## Synthesis and verification

Every lens finding was re-read at source by the orchestrator before routing. OVERLAP and DRIFT are clean. The single GAP
finding G-1 is a maintainer-decision PROPOSAL (adopt the DONE-heading-id convention and extend gate 78, OR accept the
recorded-retirement half as sufficient and record that decision), not a defect to fix: gate 78 is honestly guard-first
and discloses its own boundary. Machinery verdict: **COHERENT.**

## Findings routed

- **G-1** routed to TODO 3.132 `(r16 guardrails, G-1, L; maintainer-decision)`, tagged `[machinery]` `[needs-decision]`.

Minor observation (not a finding, not routed): `/restore-broken` has zero mentions in the `README.md` / `.claude/CLAUDE.md`
/ pack `CLAUDE.md` narrative (while `/resume` is referenced throughout). Expected for a command shipped in this same PR;
weaving it into the narrative is a natural follow-up, not a structural defect.
