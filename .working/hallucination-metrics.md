# Worker Hallucination Metrics

Tracks the rate at which research-assistant (worker subagent) drafts contain claims that the orchestrator catches and corrects at apply-time, versus the rate at which those errors slip past apply-time and ship to `main`.

This file is **maintainer working state**. It is not a versioned corpus document; it is not subject to corpus audit gates; it is exempt under the `.working/` directory exemption. The file is updated opportunistically as the orchestrator catches and ships work; precision matters less than the trend signal.

The metric is the operational counterpart to the **research-assistant discipline** in [`dev-security/claude-rules/governance/ai-assistant-workflow-disciplines.md`](../dev-security/claude-rules/governance/ai-assistant-workflow-disciplines.md). The discipline says "verify worker claims at apply-time"; the metric says "how often does the discipline catch something". A rising metric suggests the worker prompt needs tightening; a falling metric suggests the discipline is converging on a robust worker model.

---

## Definitions

- **Worker draft**: a research file produced by a background subagent. Used as input by the orchestrator; not pasted unmodified.
- **Apply-time catch**: the orchestrator identifies a worker error during apply-time verification (re-reading target files, running contradiction searches) and corrects it before the PR is committed.
- **Shipped escape**: an error that survived apply-time verification and was caught only after the PR merged (by `/validate`, by CI on `main`, by a subsequent reader, or by the maintainer).

The ratio `shipped escapes / total worker errors` is the discipline's leak rate. The discipline aims to keep apply-time catch rate high and shipped escape rate low.

---

## Current state (as of PR #896, refreshed 2026-07-13 at the deep-assessment r3 session)

Session window fold (#887 to #896, refreshed 2026-07-13; surfaced by the r3 Phase-6 QA-ledger meta-audit noting this file was at its opportunistic-update threshold). This session was a `/resume` (loop-break Sweep 101) followed by the maintainer-invoked `/deep-assessment` r3, run PR-by-PR (#887-#896). **Worker-draft class UNMOVED:** no content-drafting worker applies this session; the many subagents dispatched were QA reviewers (validate-pr Subagent A, the r3 semantic-instrument judges: full-qa, fitness, matrix-fit, claim-fit, reference-audit, guardrails, dead-gate, ground-truth, pipeline, ledger), not content workers, so the worker-draft counts and rates below are unchanged. **Orchestrator-side: zero shipped escapes this session.** Every defect was self-caught PRE-PUSH by the guard/verifier layer or by the immediate next PR's `/validate-pr`, and every one was mechanical-edit-layer, not corpus-value: CHANGELOG-detailed header-clobber (#887, #894; caught by gate 59 + the gate-36 mirror-parity test on committed HEAD, the recurring sub-class whose #894-retro lesson then HELD clean across #895/#896), table-row join-errors (#887/#888/#891; self-caught by the pipe-count/join-residual check), a branch-to-main near-miss (#884; fail-loud at push, `main` never touched), a register-row order slip (#888) and a stale gate-29 docstring carrier (#890) both caught by the next PR's `/validate-pr` and both cosmetic/documentation. The two recurring mechanical classes (header-clobber, table-row-join) are guarded by gate 59 + gate 36 and the pipe-count self-check respectively; both held. **The `/deep-assessment` r3 finding set (FR-200..219 fitness, §3.62 guardrails, §3.63 reference-audit, §3.64 Phase-4/5 incl. the systematic DA-ASVS High) is NOT this-session escapes** but PRE-EXISTING corpus issues the proactive assessment surfaced (the deep-assessment's purpose); they are recorded in the r3 record and routed to the backlog, HOLD for maintainer sign-off, and do not belong in the escape ledger. The prior refresh's text follows.

Session-close window fold (#631 to #643, refreshed 2026-07-05): the window was orchestrator-authored machinery and QA work (gates 66 and the gate-64 extension, the G-4 hoist, the D7 delta gate, renames and hygiene bundles) with NO worker-draft applies beyond the fr-59 batch already counted on the external-collaborator row (the remaining P2 applies are re-tiered to a separate session), so neither worker class moved. Orchestrator-side, every window defect was caught PRE-MERGE by the verifier/D7/sweep layers (the #639 verifier-loop-cap descope; two folded Lows each at #641, #642, and #643 including D7's two duplicate-token blocks; one in-window sweep Low each at #640 and #642, both fixed in the next PR's batch): zero shipped escapes, zero out-of-window findings. The prior refresh's text follows.

Refreshed 2026-07-04 (the P2 worker-delivery-apply boundary refresh the r4 G-8 cadence note calls for) from the "as of #347" state below; the #348-#630 window's catches live in the per-PR `/retro` and `/validate-pr` records per the trend-over-precision protocol, with the salient additions folded into the table, the catches log, and the escapes log (the #482 29134 orchestrator-side recurrence). The window's headline: the worker-draft class stays controlled (no shipped worker escape since #167); the orchestrator-side class now has the gate-50/57/D5 mechanical family plus the tiered pre-push verifier standard catching it pre-merge; and a THIRD worker class opens at this boundary, separate-session external-collaborator deliveries from the scratch inbox (new table row below). Two standing clarifications:

1. **Scope drift in the log.** The file's Definitions scope the metric to **worker-draft errors**, but the catches log has accumulated a distinct class: **orchestrator-side discipline catches** (multi-surface incompleteness, the C-9 mandatory-QA skip, registry/docstring oversights, table-edit slips). These are not worker hallucinations; they are counted on a separate row below rather than inflating the worker-error rate.
2. **Log currency.** The per-event catches/escapes logs below run through Sweep 30 / PR #275 line-by-line; catches since then (PR #277-#347) live in the per-PR `/retro` ([`improvement-log.md`](improvement-log.md)) and `/validate-pr` ([`validate-pr/`](validate-pr/)) records and are **summarized** here per the "trend over precision" protocol, not re-logged line-by-line. The #295-#347 window's salient additions are folded into the table and the catches log below.

| Metric | Count | Notes |
| --- | --- | --- |
| Worker-driven PRs under the research-assistant discipline | ~59 (approx) | ~55 as of #294 + the FR-167 batches 2/3/4 (#313/#317/#341) and the project-governance Phase-1 migration (#336, built via a read-only research subagent). The corpus-wide `/validate` and `/guardrails` sweeps also fan out subagents, but as QA reviewers (not content-drafting workers), so they are not counted here. Approximate per this file's "trend over precision" protocol. |
| Worker-draft apply-time catches (logged through #275) | 10 | Stale version/library bumps (#172/#178/#179), wrong PR/FR cross-refs (#172/#178), confabulated file path (#169), control-identifier hallucination (#275), subagent incomplete-enumeration (Sweep 30). |
| Worker-draft apply-time catches (post-#275, in /retro + /validate-pr records) | ~8 | #290 stale FR-pointer; #294 FR-155 premise + B2 unverifiable-citation deferral; the FR-167 matrix-batch control-code catches (#313 two GRC-05 mis-mappings + a cross-section duplicate with conflicting cells; #317 batch-3 code catches; #341 four weakly-grounded worker codes dropped + AICM kept out of the CCM column). The recurring matrix-batch class is now guarded at source by the worker-brief template's control-identifier rail (1.1.0, #275) plus the in-repo CCM/AICM/NIST reference modules gates 48/49 use. |
| Worker-draft shipped escapes | 2 | ISO/IEC 29134:2023 citation year (#162, caught `/validate` Sweep 15, fixed #167); the #169 file-path (borderline, caught at the apply boundary). **No worker-draft escape logged since #167 (~180 PRs).** |
| Worker-draft apply-time catch rate | ~18 / ~20 ≈ 90% | Catches / (catches + escapes), worker-draft class only. Both escapes predate #167, so the recent-window rate is ~100%. Small sample; trend signal. |
| Worker-draft shipped-escape rate | 2 / ~59 ≈ 3% | Trending down: the worker-brief control-id rail (#275) plus the in-repo reference modules now gate the matrix-batch control-code class at source, so the FR-167 batches 2/3/4 produced zero shipped worker escapes (all catches were apply-time). |
| External-collaborator worker deliveries applied (opened 2026-07-04) | 1 (FR-59 held-source batch PR 1) | First separate-session external-worker delivery applied from the scratch inbox (`worker-20260703-a`, fr-59-privacy-jurisdiction). Zero hallucination catches at apply-time: every relied-on source quote re-verified verbatim against the reference extracts, and the worker's self-flagged currency caveat (Japan article numbers cited from the June-2020 reference text) was resolved by orchestrator upstream confirmation of the consolidated APPI numbering, the flag-not-fabricate shape the brief template mandates working as designed. The #626 QA-report intake from the same worker programme needed six validation-layer corrections (four line-offsets, two counts) plus one verifier-caught inherited imprecision, so the intake/validation layers stay load-bearing for report-shaped deliveries even when research-shaped deliveries arrive clean. |
| Orchestrator-side discipline catches (distinct class) | ~16 | The ~9 through #294 (multi-surface incompleteness #181 ×2 / #294; registry/docstring misses #186 ×3; C-9 mandatory-QA skip #187; Date-bump near-escape #179; citation over-specification #287) plus the #295-#347 window: control-code-from-memory (#329 `RS.RP`→`RS.MA`); the multi-surface-incompleteness pattern that became a confirmed 3-occurrence run (#336 content-linter scan-scope under-enumeration → #338 → #340 find-every-carrier-missed-a-4th-carrier); the CHANGELOG-hygiene em-dash class migrating to the un-gated `.working/` detailed-mirror + records (#344, #346); the mid-PR-scope-change narrative staleness (#343); and a markdown-table-row-edit slip caught by gate 50 + gate 36 (#347). Caught by `/validate`, `/validate-pr`, gate 50, or CI, not by worker review. |

**Trend read.** The worker-draft escape rate is low and still falling (no worker-draft escape logged since #167, now ~180 PRs); the worker-brief control-id rail plus the in-repo reference modules drove the FR-167 matrix batches to zero shipped worker escapes (catches all apply-time). The **active** class remains orchestrator-side, and it has now partly earned a mechanical backstop: the multi-surface / paired-surface incompleteness family the `/retro` log kept surfacing (#181, #278, #294, #336→#338→#340) is the signal that drove the §4.8 codification backlog and, more concretely, **gate 50 (bookkeeping-parity, #343)**, which has already caught real orchestrator-side bookkeeping defects at #342 (a missing handoff `/validate-pr` row) and #347 (a mashed table row). The still-ungated residue is hand-authored `.working/` prose hygiene (the em-dash and bare-code-span classes on the gate-exempt surfaces), which the queued §4.14 pre-flight aid would address. The metric's usefulness going forward is less about worker hallucination (controlled) and more about how much of the orchestrator-side class gets a mechanical gate (gate 50, §4.14) versus relying on the per-PR QA layer to catch it.

---

## Update protocol

When the orchestrator catches a worker error at apply-time:

1. Increment the apply-time-catches count in the table above.
2. Add a one-line note to the **catches log** below, dated and PR-keyed.
3. The catch is also documented in the closing PR's CHANGELOG-detailed entry under a "Discipline observation" or equivalent section (per the research-assistant discipline's tracking convention).

When a worker error is discovered post-merge (a shipped escape):

1. Increment the shipped-escapes count in the table above.
2. Add a one-line note to the **escapes log** below.
3. The fix is shipped in the next regression PR (a `/validate` close-out, a `/fitness` close-out, or a dedicated correction PR). That PR's CHANGELOG entry notes the escape.

The metrics table itself is updated **opportunistically**, not on every PR. A periodic refresh (every 5-10 PRs, or at session-pause) is sufficient. The precision matters less than the trend direction.

---

## Catches log

Reverse-chronological. Each entry: date, PR number where the catch occurred, one-line description.

- **2026-07-04, PR #626 (external-worker QA-report intake, three-layer intake catches; summarized)**: the independent validation subagent corrected four line-offsets and two counts in the external worker's QA report before any finding was routed, and the transcription-fidelity verifier then caught one imprecision the validation layer had inherited verbatim (the FIT-8 row-225 cell characterization), tightened pre-push. First observation of the validation-layer pass-through class; a second occurrence escalates to a tighten-on-transcribe line in the intake shape per the #626 retro. **Worker-brief implication**: none (report-shaped deliveries are governed by the intake shape, not the research brief template).
- **2026-06-26, PR #376 (orchestrator-side paired-surface-incompleteness; the loop-closure half of the worker-brief update protocol, caught by `/validate-pr`)**: PR #376 added DO rail 8 to [`worker-brief-template.md`](worker-brief-template.md) (the code-cell-and-description-cell paired-surface guard, traced to the #371/#374 `RC.IM` to `ID.IM` "recovery" catch) and bumped the template **1.2.1 to 1.2.2**, and the CHANGELOG-detailed claimed compliance "per the hallucination-assessment update protocol", but the orchestrator omitted that protocol's steps 1 and 4 (this catch-log entry + the template-version reference). `/validate-pr` on #376 (Subagent A) caught it (warning/Medium, in-window), apply-time-verified by greping this file for zero `#371`/`#374`/`1.2.2` hits. **Root cause**: the orchestrator performed the template half of the update protocol (add the rail, bump the Version, cite the catch inline) but not the metrics half, the same update-one-of-a-pair / paired-surface-incompleteness class rail 8 itself addresses, committed in the very PR codifying the guard. **Discipline-gap fix applied**: this entry closes the loop (worker-brief **1.2.2**, rail 8, from the #371/#374 catch); added in the session-closing handoff PR #377 per recursion-avoidance. **Pattern + escalation**: paired-surface-incompleteness recurred across #372 (pack Version vs history row), #374 (control code vs description cell), and #376-itself; the recurrence while codifying the convention-level guard is the signal that the durable fix is a **mechanical paired-surface check** (a gate-50 bookkeeping-parity extension, or a new check) rather than more convention, surfaced to the maintainer and routed in the #376 `/retro`. **Worker-brief implication**: NONE beyond this loop-closure (rail 8 is already added; this is its metrics-side reference).
- **2026-06-25, PRs #313 / #317 / #341 (FR-167 matrix batches 2/3/4, worker control-code catches, summarized per the post-#275 protocol)**: across the three comprehensive-matrix batches, research workers proposed framework control identifiers the orchestrator caught and corrected at apply-time before any cell shipped. #313 surfaced two `GRC-05` mis-mappings plus a cross-section duplicate row with conflicting cells; #341 dropped four weakly-grounded worker-proposed codes and kept AICM codes out of the CSA CCM column. All caught by re-reading each source document and validating every code against the in-repo reference modules ([`tools/ccm_aicm_reference.py`](../tools/ccm_aicm_reference.py), [`tools/nist_csf_reference.py`](../tools/nist_csf_reference.py)) that gates 48/49 use, so gates 48/49 then **confirmed** rather than caught. **Zero shipped worker escapes** across the three batches (each batch's `/validate-pr` returned 0 in-window findings). This is the matrix-mapping failure class the worker-brief control-id rail (1.1.0, #275) guards at source: the catches are the rail working. Per-PR detail lives in the `/validate-pr` records and CHANGELOG entries.
- **2026-06-23, Sweep 30 `/validate` (Subagent B incomplete + mis-classified finding set, orchestrator apply-time correction)**: Subagent B reported the "CSA Cloud Controls Matrix v5" hallucinated-version cluster as 4 v5 locations + 1 versionless "sibling". Apply-time verification (orchestrator `grep -rn "Cloud Controls Matrix v5"`) found B's set was **incomplete and partly wrong**: B MISSED a real v5 instance (`security/procedure-key-escrow-and-recovery.md:217`) and MIS-CLASSIFIED a versionless instance (`security/standard-endpoint-hardening.md:225`, which has no version string) as a v5 sibling. The orchestrator grep produced the authoritative set of exactly 5 v5 instances. **Root cause**: the subagent's grep/read coverage of the corpus was incomplete for the spelled-out form. **Discipline held**: subagent findings are research, not findings, until the orchestrator re-verifies; the corpus-wide grep at the routing boundary is exactly the apply-time check the research-assistant discipline mandates, and it caught both the miss and the mis-classification before anything was routed. No shipped escape (finding is out-of-window, surfaced to maintainer, not yet acted on). **Lesson**: for "find all instances of X across the corpus" sweep tasks, the orchestrator's own corpus-wide grep is the authoritative set, not the subagent's enumeration, always re-run the grep at apply-time rather than trusting the subagent's count.
- **2026-06-23, PR #275 (FR-167 batch 1, worker control-identifier hallucination, NEW class, now guard-railed)**: research workers producing candidate compliance-matrix mappings proposed framework control identifiers NOT in established corpus use: `IPY-02` and `DSP-10` (corrected at apply-time to `IPY-01` / `DSP-04` after grepping the corpus), and strict-CSF-2.0 `PR.PS` where the corpus convention is `PR.IP`. All caught at apply-time by re-reading each source doc + greping the proposed code against corpus use; none shipped (clean in-window `/validate-pr`). **Root cause**: workers drew control codes from general framework knowledge rather than from the corpus's established usage. **Discipline-gap fix applied**: [`worker-brief-template.md`](worker-brief-template.md) bumped **1.0.0 → 1.1.0**, added DO rail 6 (verify every framework/control identifier against established corpus use; flag unconfirmed as "needs-verification") and a "Matrix-expansion PR (FR-167)" per-PR-class override codifying the framework-code crib. This closes the §107 self-update loop for the matrix-mapping failure class; with ~10 FR-167 batches remaining, the rail prevents recurrence at source. (Also folded in two long-queued template additions while the file was open: DO rail 7 new-acronym-glossary from PR #229, and the corpus-wide-rename override from PR #218/#219/#220.)
- **2026-06-22, PR #187 hot-fix (orchestrator-side POLICY DEVIATION: judgment-call skipping of mandatory QA step)**: After PRs #185 and #186 merged, the orchestrator deliberately skipped `/validate-pr` invocations on both, citing "circular" or "redundant" rationale (PR #185 was a housekeeping recording PR; PR #186 was a /validate close-out). The maintainer flagged this as a real policy deviation: the orchestrator does NOT have discretion to skip quality-assurance or testing steps; only the maintainer can authorise an exception. **Root cause**: orchestrator made unilateral judgment calls about which PRs "needed" QA, when the discipline says every merge gets it. **Discipline-gap fix applied**: `/validate-pr` SKILL.md and slash command both gain an explicit "No orchestrator-side skip discretion" paragraph naming the failure mode and disclaiming carve-outs for meta/housekeeping/sweep-close-out/circular PRs. Same language added to `/validate` SKILL.md and slash command (forthcoming separately) for consistency. **NEW failure-mode class**: **C-9 Orchestrator-side judgment-call skipping of mandatory QA/testing steps**. This is distinct from worker hallucinations and from the multi-surface incompleteness pattern; it's an orchestrator-side discipline failure that policy must explicitly disclaim. **Future gate candidate**: a mechanical check that every merged PR appears in `.working/validate-pr/history.md`. The gate would catch the skip class definitively. Queued for consideration. **Worker-brief template implication**: NONE (workers don't run /validate-pr; this is orchestrator-side only). The fix is at the rule/SKILL/slash-command layer and at the orchestrator-checklist layer.
- **2026-06-22, Sweep 17 missed by Subagent A (extension of the PAIRS finding)**: Sweep 17 Subagent C correctly identified that gate 44's `PAIRS` registry was missing the new pair, but the SAME root cause produced a sibling defect that Subagent A missed: the docstring of `tools/lint-paired-skill-step-parity.py` at lines 16-19 said "Currently only ``validation-sweep`` has both a SKILL.md and a slash-command counterpart", also stale after PR #176 (library-fitness-review) and PR #183 (validation-sweep-pr-scoped). Caught by `/validate-pr` on PR #186 (returned 1 finding). **Root cause**: Subagent A's scope was the lines in PR #186's diff (the PAIRS list at lines 47-60); the docstring at lines 16-19 was outside the diff window. **Discipline-gap fix applied**: rewrote the docstring to be generic. **Lesson for worker-brief template / Subagent A briefs**: when fixing a file, the subagent should also check the file's docstring for stale claims that the fix might invalidate. Add as orchestrator-side checklist item (not worker-brief, workers don't write subagent briefs).
- **2026-06-21, Sweep 17, PR #186 close-out (orchestrator-side oversight, NEW pattern: gate-registry-incompleteness when shipping a paired-surface skill)**: PR #183 added the `validation-sweep-pr-scoped` SKILL.md + `/validate-pr` slash command but did NOT add the pair to `tools/lint-paired-skill-step-parity.py` PAIRS registry. Gate 44 was therefore not validating step-parity on the new skill, a real defect that would have allowed silent step-drift. Caught by Sweep 17 Subagent C. **Root cause**: orchestrator added two paired surfaces (SKILL + slash command) but missed the downstream registry that the gate depends on. **Discipline-gap fix applied**: PAIRS registry extended; gate 44 now validates 3 paired surfaces (5-step parity confirmed). **NEW orchestrator-side checklist item for future /retro skill**: "when shipping a new SKILL.md + slash-command pair, add to `tools/lint-paired-skill-step-parity.py` PAIRS registry". This is an orchestrator-side item (workers don't ship gate registrations), not a worker-brief template rail.
- **2026-06-21, Sweep 17, PR #186 close-out (orchestrator-side oversight, stale forward-reference)**: PR #183's `validation-sweep-pr-scoped/SKILL.md:151` drafted a forward-reference "queued for PR #185" for the /retro skill. PR #185 became the housekeeping recording PR (renumbered the queue), but the SKILL.md forward-reference was not updated. Caught by Sweep 17 Subagent A. **Root cause**: when renumbering an in-flight queued PR, the orchestrator did not search the corpus for forward-references to the original number. **NEW orchestrator-side checklist item**: "when renumbering a queued PR, search the corpus for forward-references to the original number and update". Captured for /retro skill's improvement-log register.
- **2026-06-21, Sweep 16, PR #181 close-out (orchestrator-side oversight, multi-surface incompleteness)**: PR #176 added the 8th governance rule and updated the pack README inventory, directory-tree comment, available-rules table, and rollout-history paragraph, but missed the paired narrative reference in `dev-security/claude-rules/skills/skill-authoring-discipline/SKILL.md:26` ("one of the seven governance rules"). Caught by Sweep 16 Subagent B. **Root cause**: orchestrator updated structural surfaces but did not search for narrative paraphrases of the count. **Discipline-gap fix applied**: rewrote the line to remove the count entirely ("one of the governance rules under `<directory>`"); the directory IS the canonical authority; citing a count in prose creates a drift-prone paraphrase. **Broader pattern queued**: corpus-wide count-genericization sweep (queued as a follow-up PR per the maintainer's direction).
- **2026-06-21, Sweep 16, PR #181 close-out (orchestrator-side oversight, multi-surface incompleteness)**: PRs #177-#180 advanced the TODO state (session-resume metadata refreshed each time) but the Queued-sequence narrative on `TODO.md:22` was not refreshed in lock-step, it still said "PRs #142-#176 have closed 34 findings" while the actual state was "PRs #142-#180 have closed 42 findings". Caught by Sweep 16 Subagent A. **Root cause**: orchestrator refreshed the structural session-resume snapshot at the top of TODO but did not search for paraphrases of the PR range / finding count elsewhere in the same file. **Worker-brief / orchestrator-checklist implication**: when updating any inventory or count, search the corpus for narrative paraphrases of that count and update in lock-step. Note: this is the same pattern as the B1 finding above, both are "structural surface updated; paired narrative missed".
- **2026-06-21, PR #179, CI catch (gate 31)**: orchestrator bumped per-doc Version on six touched files but missed bumping Date on two of them (`security/policy-information-security.md` Date 2026-05-27, lag 25 days; `resilience/template-tabletop-exercise.md` Date 2026-06-02, lag 19 days). Gate 31 (document-date-staleness) caught this in CI; fixed in a follow-up commit on the same branch (Date bumped to 2026-06-21; per-doc Version bumped again per the version-bump-recency rule). **Discipline gap exposed**: the apply-time checklist needs an explicit "did I bump Date on every file whose body changed?" item alongside the Version bump check. Not a worker error; an orchestrator oversight that CI caught (not a shipped escape, but a near-escape worth recording).
- **2026-06-21, PR #179**: worker drafted library bump as `2026.06.151 → 2026.06.152`; actual state was library `2026.06.157` pre-this-PR. Corrected at apply-time to `2026.06.157 → 2026.06.158`.
- **2026-06-21, PR #179**: worker drafted READme bump as `1.9.22 → 1.9.23`; actual state was `1.9.28` pre-this-PR. Corrected at apply-time to `1.9.28 → 1.9.29`.
- **2026-06-21, PR #179**: worker bundled FR-33 (high[critical]) into a velocity batch alongside six mediums; orchestrator split FR-33 out per "always split when in doubt" (different severity tier; substantively larger scope). Not a hallucination per se but a discipline-judgement catch.
- **2026-06-21, PR #178**: worker drafted library bump as `2026.06.150 → 2026.06.151`; actual state was library `2026.06.156` pre-this-PR. Corrected at apply-time to `2026.06.156 → 2026.06.157`.
- **2026-06-21, PR #178**: worker drafted backlog math as `85 → 83`; actual pre-state was 77 open. Corrected at apply-time to `77 → 75`.
- **2026-06-21, PR #178**: worker drafted target PR number as `#169`; actual is #178. Corrected at apply-time. (Common pattern: workers often anchor to the most recent PR they saw, which drifts over multi-PR sessions.)
- **2026-06-21, PR #172**: worker drafted FR-3 as closed in PR #158; actual closing PR is #147. Corrected at apply-time after cross-checking [`.working/DONE.md`](DONE.md).
- **2026-06-21, PR #172**: worker drafted against library `2026.06.150` / README `1.9.21`; current state was `2026.06.152` / `1.9.23` (advanced via PRs #170 / #171). Corrected at apply-time.
- **2026-06-21, P1.3 (PR #169)**: worker referenced `policy-information-security-incident-management.md` which does not exist in the corpus. Caught pre-apply by `find`; substituted with the correct `procedure-security-incident-response.md`.

(Earlier catches not retroactively logged; the discipline started recording at the moment this file was created.)

---

## Escapes log

Reverse-chronological. Each entry: date, original PR, catching mechanism, one-line description.

- **2026-06-21, original PR #162 (FR-29 DPIA template), caught by `/validate` Sweep 15**: worker drafted `ISO/IEC 29134:2023`; the publicly verifiable edition is 2017 with no recorded 2023 republication. Corrected in PR #167 (Sweep 15 close-out).
  - **RECURRENCE, 2026-06-30, PR #482 (FR-31 privacy-by-design framework), caught PRE-MERGE by the high-assurance dual-verifier harness (NOT an escape; did not ship)**: the orchestrator (not a worker) reintroduced `ISO/IEC 29134:2023` while authoring the framework-alignment table (the FR-31 research proposed `ISO/IEC 29134` with no year; the wrong year was an orchestrator-side generation). Both independent verifiers flagged it against this logged class; fixed to `:2017` in two places (the framework table + the document-index register row it had propagated to) before commit. Signal: the denylist-based currency gate is blind to 29134 (absent from the canonical register), so the harness, not a gate, is the operative control for this class; and the failure mode is not worker-only (the orchestrator reproduces it), so the pre-push verifier / harness is the backstop for net-new authoring, not the worker-brief guard rail alone.
- **2026-06-21, original PR #169 (P1.3 access-control), borderline apply-time**: worker referenced a non-existent file path. Caught by orchestrator's `find` during apply-time verification, before the file was committed. Counted as apply-time catch in the metrics table; logged here in the escapes register because the worker's draft did contain the error (the discipline caught it at the boundary).

---

## Discipline observations

- **Apply-time `find` for file-path verification** is one of the cheapest worker-error catches. Worker confabulations of plausible-sounding-but-nonexistent filenames are common; a single `find` rules them out in milliseconds.
- **Cross-PR / FR references** are a recurring failure pattern. Workers drafting from session memory rather than from the live closed-PR ledger get these wrong. The orchestrator's verification step is to grep [`DONE.md`](DONE.md) for the referenced PR or FR.
- **Stale version numbers** are mechanical and almost universal in workers drafted before the current PR sequence. Apply-time correction is a quick read of [`README.md`](../README.md)'s Library Version + the target file's Version field.
- **External-standard citation years** are higher-cost catches because they require WebFetch or external verification. The canonical-citations register ([`governance/register-canonical-citations.md`](../governance/register-canonical-citations.md)) is the project's defence; the metric here is the gap between the register and what workers draft.

---

## Future signals to watch

- **Apply-time catch rate trending down** could mean the worker prompt has improved (less hallucination at source) OR that the orchestrator is becoming less vigilant (more escapes downstream). Compare with the shipped-escapes count.
- **Shipped escape rate trending up** is a clear discipline-failure signal. Investigate which class of error is leaking (the catches log surfaces patterns).
- **Specific error class clusters** (e.g., several consecutive citation-year escapes) suggest a structural worker-prompt fix is needed.

The metric is a maintenance signal, not a production gate. Use it to calibrate the discipline; do not use it as a CI requirement.

---

## TODO-mining cursor

Records how far this file has been mined for TODO-worthy improvements (the companion to the [`improvement-log.md`](improvement-log.md) `## TODO-mining cursor`). Maintainer-directed 2026-07-10.

**Mined-for-TODO-extraction through the sweep94 pass (2026-07-10, PR #762).** Two forward-references in this file were confirmed already-shipped (stale-as-written, annotated here rather than edited in place, since this file is an opportunistically-updated trend log):
- The #187 catch's "**Future gate candidate**: a mechanical check that every merged PR appears in `.working/validate-pr/history.md` ... Queued for consideration" **SHIPPED** as **gate 50 Check 1** (bookkeeping-parity); the skip-class gate exists.
- The trend-read's "still-ungated residue is hand-authored `.working/` prose hygiene (em-dash and bare-code-span classes) ... the queued §4.14 pre-flight aid" is **partly shipped**: the em-dash + must-be-linked half is `tools/preflight-changelog.py` + gate 51; the remaining dangling-link-resolution half is now **TODO §3.34** (the `§4.14` number is a pre-restructure positional token, superseded). No un-captured TODO-worthy signal remains in this file's "Future signals to watch" (generic monitoring guidance) or escapes log (two pre-#167 worker escapes, the #482 recurrence caught pre-merge).
