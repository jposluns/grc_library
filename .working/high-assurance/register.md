# High-Assurance Verification Register

**Version:** 1.0.17\
**Date:** 2026-07-05\
**License:** CC BY-SA 4.0

The durable, cross-session register for the **high-assurance verification** discipline (the
pack rule [`governance/high-assurance-verification.md`](../../dev-security/claude-rules/governance/high-assurance-verification.md)):
the heavier pre-apply harness invoked for *sensitive* changes, those that are gate-blind on
correctness, delicate at scale, and costly to get wrong. A sensitive change often spans more
than one session (the trigger is recognized in one, the harness runs across one or more, a
closing re-check falls in a later one), so its state must survive a session boundary; this
register is where it does.

Each row records one sensitive item: what it is and which of the three trigger conditions
make it sensitive, the harness stages run and their outcomes (research workers, the
independent adversarial verifier findings, the programmatic invariant checks, the
deterministic apply script and its re-parse result), the status, and any follow-up. New rows
on top.

**Status field**: `pending` (recognized, harness not yet run) / `in-progress` (harness
running, not yet complete) / `verified` (harness complete, retained for the audit trail) /
`deferred` (blocked on a maintainer decision or an external dependency, routed around).

**Resume surfacing**: on `/resume`, the assistant reads this register alongside the other
standing registers and surfaces every `pending`, `in-progress`, and `deferred` row before
starting the queued work, so an in-flight harness is not lost across a session boundary. A
`pending` or `in-progress` row is a standing instruction that the item is not yet cleared; it
is not silently dropped (the same discipline as the pending-decisions queue).

This file is maintainer working state, exempt from corpus audit gates.

## Active items

None currently in-flight. The M1 Commonwealth-spelling harmonization (PR #570) and the §1.5 reference version-currency register harness completed and are recorded under Completed items below. The closing whole-matrix `/matrix-fit` over the 62-row worklist (FR-167 closure, tracked in [`TODO.md`](../../TODO.md)) is the next candidate that may warrant a register row when it runs, if its findings touch sensitive cells.

## Completed items (retained for the audit trail)

| Date | Item | Why sensitive (conditions) | Harness stages and outcome | Status | Follow-up |
|---|---|---|---|---|---|
| 2026-07-05 | **PR #664 gate-49 matrix ISO-header lockstep**: rename the compliance-matrix ISO column header + gate 49's `ISO_HEADER` machine key from `ISO 27001:2022` to `ISO/IEC 27001:2022` (11 headers + legend), coupled with the gate-49 fixtures/docstring/message, the shared `iso_27001_reference.py`, gate 58's diagnostics, the `/matrix-fit` docstring, and the audit-programme spec. | All three: **(1) gate-blind correctness** (a wrong rename makes gate 49 silently skip the entire matrix's control-code validation and still exit 0, the single-header-detection-block failure mode; no gate catches the coverage loss), **(2) delicate scale** (a multi-surface machine-key coupling where a header/constant mismatch silently disables the gate), **(3) high escaped-error cost** (gate 49 protects control-code validity in a cited, adopter-consumed matrix). | **Stage 5** deterministic scripted apply of the matrix (idempotent exact-string replace; dry-run then apply; re-parse: 11 gate-anchor header rows carry the joint label, 0 residual bare, the already-joint line-407 row untouched). **Stage 4** invariant floor + the pending-decisions-mandated proof gate 49 still fires: injected `A.5.99` under the new header is caught (`iso-annex-range`, exit 1), live matrix exit 0, and a negative control confirms the OLD bare header is no longer recognized (exit 0, validates nothing) so an accidental revert surfaces; full 343-test regression suite + all 66 gates green. **Stage 3** two independent blind adversarial verifiers, both SHIP: the false-negative verifier confirmed the constant exactly matches the header, all 11 headers + both fixtures converted, no other gate silently broken, and the spec gate-49/58 split correct (its one miss was the owed bookkeeping, now supplied); the false-positive verifier ran its own injection test (gate functional), confirmed no malformed form, the gate-58 both-form documentation + fixtures preserved, no frozen-CHANGELOG edit, correct version bumps, and surfaced the shared-module side effect (gate 58 output would mix designations) which was resolved by completing gate 58's diagnostic harmonization rather than shipping a self-introduced inconsistency. | verified | None. TODO 3.1 part (a) closed. Option A (exact-string constant) was executed per the maintainer's resolution; a future hardening to option B (a both-forms regex like gate 58's) would remove the exact-string fragility (deferred to the maintainer). The base-unverified gap tail (TODO 3.1 part b) and the authoritative-register+designation-gate (TODO task) remain separately tracked. |
| 2026-07-02 | Gate 61 reference data (tools/cobit_iso31000_reference.py): 40 COBIT 2019 objectives + 231 practice-range counts + the ISO 31000:2018 clause tree | All three conditions: gate-blind correctness (no gate checks the reference data itself), delicate scale (317 identifiers: the 271 COBIT codes plus the 46-entry ISO clause tree), high escaped-error cost (corpus-wide validation relies on it) | Stage 1 research fan-out (extraction worker, closed-set token scan); stage 3 independence: the orchestrator's deterministic parse script derived both sets from the held extracts SEPARATELY from the worker's manual extraction, exact agreement (40 objectives / 231 practices / all 46 ISO clause entries, the 38 structural clauses plus the 8 clause-3 term numbers, APO12=.01-.06); stage 4 floor: eight regression fixtures (one of them the corpus-at-HEAD smoke test); stage 5: the data was generated by script from the extracts, not hand-typed, and the practice-title tier was deliberately excluded (extraction wraps titles, the fabrication class the module prevents) | verified | Refresh on a new COBIT release or ISO 31000 edition via the same deterministic parse (module docstring carries the instruction) |
| 2026-07-02 | **M1 / TODO 7.4 + 3.13 item-25: corpus-wide `-isation` to `-ization` harmonization (~2000 occurrences, ~300 files) + `lint-language.py` noun-form extension** (maintainer-locked overnight decision: full normalization, not a Commonwealth exception). | All three: **(1) gate-blind correctness** (a flip inside a VERBATIM quote of an external instrument, e.g. the GDPR's official English "organisational measures" in the Article 25 quotes, is a real defect no gate catches; the extended gate validates spelling, not quote fidelity), **(2) delicate scale** (~2000-occurrence, ~300-file mechanical sweep), **(3) high escaped-error cost** (every corpus document is citable; a corrupted verbatim quote misattributes an official text). | **Stage 1**: research worker enumerated and classified every occurrence in the gate-2 scan set (FLIP vs KEEP-verbatim-quote vs KEEP-code-fence vs KEEP-other), exhaustive on the KEEP lists. **Stage 2**: quote-context signal pass done (5 quoted lines, 14 "organisational measures" carriers identified pre-dispatch). **Stage 5**: deterministic masked apply (1945+ flips, single Version+Date bump per file, census re-parse clean; the rename coupled-updated). **Stage 4**: extended gate 2 exits 0 post-sweep; all 59 gates; 264-test suite; both generators check-clean. **Stage 3**: two BLIND adversarial verifiers. False-negative verifier REFUTED pass-one completeness: 17 in-scan-set residuals, all in the derivational family (verb "analyses" x5, analyser x3, -isable x5, a fenced sanitiser, three tools emitted-string carriers) that both the sweep's inflection lists and the extended gate were blind to; every finding adopted, the gate extended again (per-stem -isable/-iser/-isers + analyser(s); the grammar-ambiguous verb "analyses" documented as editorially caught), the out-of-scan-set root files flipped, the AEO linter constant aligned. False-positive verifier: ZERO blockers (corruption/quote/proper-noun/URL/mirror checks clean; ~65 flips context-read); its catches adopted: the EU/UK "Authorised Economic Operator" official name restored at the three labelled carriers + allow-span (maintainer-answered), the Australia ATT expansion corrected to "Australian Trusted Trader", the improvisational allowed forms and noun-pattern case-insensitivity fixed. Guard-note: the linter extension and the sweep land in ONE PR (landing the gate first would hold `main` red across the window, which gate-discipline forbids working around; the new-rule-plus-conforming-corpus bundle is the sanctioned tightly-coherent exception). | verified | TODO 7.4 and the 3.13 item-25 line rotated in the landing PR (#570); the scratch CONTRIBUTING/house-style cross-repo note is SR-wave. |
| 2026-06-30 | **§1.5 reference version-currency register** (TODO §1.5): added `Upstream check location` + `Last verified (UTC)` columns to all 16 tables of [`governance/register-canonical-citations.md`](../../governance/register-canonical-citations.md) (151 rows), applied 7 upstream-confirmed corrections, and wired the staleness cadence into [`governance/specification-citation-verification.md`](../../governance/specification-citation-verification.md). | All three: **(1) gate-blind correctness** (version-currency and upstream-URL accuracy are semantic, ungated; a fabricated URL is a real defect under no-decorative-external-links), **(2) delicate scale** (151-row, 16-table single-artefact reshape), **(3) high escaped-error cost** (corpus-cited; gate 6 reads its Superseded column). | **Stage 1**: 4 parallel research workers produced the per-row upstream-URL map, briefed to return only fetch-confirmed URLs (100 confirmed) and `needs-reconfirm` for the rest (51), never a guessed URL. **Stage 2**: the `needs-reconfirm` set is dominated by iso.org / IEC-webstore / nerc / tsa / hhs / planalto / wcoomd HTTP-403-to-fetch (an egress limit, recorded, not a miss). **Stage 5**: deterministic scripted apply ([`scratchpad apply_currency_columns.py`]) keyed on the explicit map, NOT a hand-edit. **Stage 4**: programmatic re-parse confirmed 16 tables / 151 rows / correct column counts / all tail-markers valid / all confirmed keys matched. **Stage 3**: two independent adversarial verifiers (blind). False-negative/structural verifier: 0 misses, 0 structural defects (14 high-profile standards confirmed current). False-positive verifier: 6 of 7 corrections upstream-confirmed correct and all 10 sampled URLs correctly matched; **1 finding** (the awesome-ai-security category count had been "corrected" the wrong direction to ~17; the live README has ~20 content categories) was validated against the verifier's direct `grep` count and **fixed to ~20** before commit. Gates: 57/57 after the `lint-standards-currency.py` parser adaptation and the `lint-external-link-domains.py` allow-list extension. | verified | Three version-upgrades with corpus-citation impact ship as follow-up PRs: ISO/IEC 27033->27033-1:2015 (2 corpus docs cite `27033:2020`), ISO/IEC 27036-2:2014->2022 (1 doc), NIST SP 800-88 Rev.1->Rev.2 (1 doc). ATLAS scratch superseded-archival is MCP/maintainer-gated ([`pending-decisions.md`](../pending-decisions.md)). FYI (not a defect): CSA CCM `Publication date` field reads 2024 while v4.1 released Jan 2026 (version correct; date-field lag, future pass). |
| 2026-06-30 | OT post-ingestion validation: maintainer-directed "follow up with an audit/validation" of the OT corpus against the NIST SP 800-82 Rev. 3 + ISO/IEC 27019:2024 sources newly ingested into scratch, run after the SP 800 ingestion batch (this PR). Scope: the 6 `operations/ot/*` docs read in full + the 6 PR-#494 OT matrix rows. | Conditions (1) gate-blind correctness (whether the OT docs' technical claims hold against ground truth, and whether the matrix CSF choices fit, are semantic properties no gate checks) and (3) high cost (citable OT corpus + adopter-consumed matrix) hold; (2) delicate-scale weaker (a verification pass, not a bulk reshape). | This is the *verification* tier (an audit against newly-held ground truth), not a 5-stage apply harness. One deeply-contextualized audit subagent read the 6 OT docs in full and the 6 OT matrix rows, checking each technical claim against the held 800-82r3 (zone-and-conduit / Purdue, Security Levels attributed to IEC 62443, segmentation / DMZ, active-scanning caution, outage-aligned patching, safety-first IR, OT asset management) and the held ISO 27019:2024 scope. **Verdict: OT work SOUND.** No contradictions of 800-82r3; IEC 62443 correctly positioned primary and 800-82r3 secondary; the 6 matrix-row CSF choices align with 800-82r3; ISO 27019 correctly energy-sector-scoped (the BMS overlay correctly does NOT cite it). Two LOW findings: **GAP-1** (`operations/ot/annex-ot-security-overview.md:46` CIA-priority table omits safety from the ordering) APPLIED in-window: added a clarifying sentence that per 800-82r3 human safety is paramount ahead of protection of the process, so where safety and availability conflict safety governs (annex 1.0.2 -> 1.0.3); **GAP-2** (the same line ranks A>I>C where 800-82r3 line 986 lists "integrity and availability" together ahead of confidentiality) assessed by the audit as IMMATERIAL, NO ACTION (IEC 62443, the docs' primary source, puts availability first; the docs are internally consistent and defensible). Orchestrator apply-time verification: independently confirmed GAP-1's source claim by reading the held 800-82r3 extract lines 2146-2147 ("Human safety is paramount, followed by protection of the process") before authoring the note; confirmed GAP-2 is correctly a no-action finding before declining to reorder the triad. | verified | None. Closes the maintainer's OT "audit/validation" directive. The NIST SP 800 ingestion (the prerequisite) is complete in scratch; the OT corpus is confirmed sound against it. |
| 2026-06-30 | FR-167(b) closure: add 7 compliance-matrix rows for the 6 OT docs (`operations/ot/*`: OT/ICS Security standard, OT Security Overview + BMS Overlay annexes, OT Change Management + OT Incident Response procedures, OT Asset Inventory register) and the AI Security Tooling Landscape register, PR #494 | All three: (1) gate-blind fit (a code can exist, pass gates 48/49/54, and be the wrong control for the row); (2) delicate scale (7-row, 12-cell-each reshape of a cited artefact); (3) high cost (adopter-consumed matrix). | (1) 3-worker research fan-out (OT standards/overviews; OT procedures/register; the 2 substantive docs), each reading the doc + reference base. (2) Mechanical floor: every CCM/AICM/CSF code confirmed in `ccm_aicm_reference.py`/`nist_csf_reference.py`; all ISO Annex A titles confirmed against the scratch 27001:2022 full-text. (3) Two independent adversarial verifiers, blind to each other and to the research rationale. **False-negative** (re-read each doc, challenged every `N/A`): confirmed the blanket customs-column `N/A` is CORRECT for all 7 (OT is plant-process security, explicitly routed to the logistics annex, not customs/cargo; the IT-physical row earns trade columns on a premise that does not transfer), and surfaced grounded missed mappings (row 1 +LOG-03/BCR-08/RC.RP for the §10 monitoring + §13 recovery sections; row 6 +TVM-03/STA-08 for the §6.6 vuln + vendor-EOS lifecycle; row 7 +AICM TVM-13 Guardrails for the §4.1/§4.8 runtime-guardrail tooling), correctly refuting GRC-10 on row 7. **False-positive** (judged every code vs source title): found 6 genuine mis-fits (row 1 A.7.2->A.7.4 physical-monitoring; row 3 STA-08->STA-01 vendor-RM-not-inventory and DSP-04->DSP-03 inventory-not-classification; row 6 remove A.5.10 acceptable-use and A.8.1 endpoint-protection, leaving A.5.9; row 7 remove GV.OC org-context), cleared the DE.CM/GV.SC/SEF-06-07-08 pre-flags. Orchestrator synthesis: applied all 6 FP fixes + the FN adds, each re-verified against the reference module / ISO full-text; the two are consistent (STA-08 is a mismatch on row 3's vendor-governance but a fit on row 6's vendor-inventory). The deprecated stub `privacy/annex-regional-privacy-requirements.md` (its live replacement already rowed) was EXCLUDED on maintainer decision. (4) Mechanical floor: all 57 gates green incl. 48/49/54 on the 7 new rows. (5) Hand-applied at thematic anchors (Operations block + Governance block), then re-parsed: each new row 12 fields / 13 pipes; taxonomy + scorecard regenerated. The 2-verifier adversarial pass IS this batch's `/matrix-fit` (per the AICM-column precedent). | verified | FR-167 CLOSED with this batch (item (a) /matrix-fit done #490; item (b) net-new docs #491, OT + AI register #494). Out-of-window note for a future `/matrix-fit`: the general `operations/register-asset-inventory.md` row (matrix line ~198) carries the same `A.5.10`/`A.8.1` this verifier judged mis-fits for the OT register; pre-existing, not touched here. |
| 2026-06-30 | FR-61: extend `compliance/financial-services/annex-financial-services-sector-requirements.md` regulatory-landscape overview with the missing prudential regulators (Asia-Pacific MAS/APRA/HKMA/JFSA, Switzerland FINMA, US OCC/FRB/FDIC, PR #488) | Conditions (1) gate-blind correctness (whether the named instruments are real and correctly attributed, and whether any ungroundable version is pinned, are semantic properties no gate checks) and (3) high cost (a citable, adopter-consumed sector annex) hold; (2) delicate-scale weaker (a bounded overview extension). The ref base holds NO financial-services prudential source, so this is the no-held-source citation-accuracy case (the sanctions-standard precedent: name structurally, pin nothing). | (1) Authored by strict structural naming (regulator + supervisory domain; iconic stable instrument names only; an adopter-confirm note; no pinned version/year/circular-number). (2/3) Two independent adversarial verifiers, blind to each other: false-positive (citation accuracy, INVITED to confirm upstream) **0 defects**, with all six instruments UPSTREAM-CONFIRMED via WebSearch this turn (MAS TRM Guidelines + Notice on Cyber Hygiene; APRA CPS 234 + CPS 230; HKMA SPM TM-G-1/OR-2; JFSA cybersecurity guidelines; FINMA Circular 2023/1 Operational Risks and Resilience; OCC/FRB/FDIC interagency third-party + GLBA information-security guidance) and all 8 regulator names correct; false-negative (coverage/consistency) **0 content gaps** (all 9 FR-61 regulators now represented; no duplication) + 1 warning (heading-case `### Asia-Pacific` vs the section's sentence-case convention, gate-blind; fixed to `### Asia-pacific`). (4) Mechanical floor: all 57 gates green; lint-citations/language/links exit 0. (5) Bounded overview extension; the heading fix applied and re-read. | verified | None. FR-61's deeper per-regime requirement detail (beyond the landscape map) is out of scope; the overview now maps all the named regulators. |
| 2026-06-30 | FR-144: author `privacy/template-breach-notification-regulator-register.md` + wire into `privacy/procedure-data-protection-and-privacy-breach-response.md` §6.2 (adopter-fillable breach-notification regulator register with an internal-target clock, PR #487) | Maintainer-directed (net-new authoring via the harness). Conditions (1) gate-blind correctness (whether the illustrative deadline rows are correctly framed as non-authoritative rather than asserted as fixed law, whether the internal-clock gap is actually closed, and whether the strictest-applicable-requirement rule is operable are semantic properties no gate checks) and (3) high cost (a citable, adopter-consumed breach-notification instrument; a deadline relied on as fact would be harmful) hold; (2) delicate-scale weaker (one new template + a one-note wire-in). | (1) Authored against the existing §6.2 jurisdiction table (column model + example values) and the TIA template sibling; illustrative rows drawn from §6.2 and explicitly marked non-authoritative. (2/3) Two independent adversarial verifiers, blind to each other: false-positive **0 defects** (illustrative rows non-authoritative and adopter-confirmable; citations mirror the existing §6.2 / register row, none invented; not-legal-advice disclaimer present; no contradiction with §6.2 / §6.1 / §6.3; links resolve; house style clean; procedure co-bumped 1.4.16->1.4.17); false-negative **0 content gaps** + 1 warning (metadata tail-field order deviated from the corpus convention, gate-blind; fixed to Repository Path -> Confidentiality -> License). (4) Mechanical floor: all 57 gates green. (5) New file + one-note wire-in; the field-order fix applied and re-read. | verified | None. Bundles the #486 /validate-pr fix (stale AIGC ethics-review KPI reattributed to the Panel). |
| 2026-06-30 | FR-73: author `ai/charter-ai-ethics-review-panel.md` + edit `ai/charter-ai-governance-council.md` (split independent AI ethics review into a separate panel with a challenge mechanism, PR #486) | Maintainer-directed (net-new authoring via the harness). Conditions (1) gate-blind correctness (whether the split is complete with no residual AIGC self-claim, whether independence is structural rather than asserted, whether the challenge mechanism actually reaches above the AIGC, and whether the Panel's authority is correctly bounded as challenge-not-veto are semantic properties no gate checks) and (3) high cost (a citable governance charter pair other AI documents reference) hold; (2) delicate-scale weaker (one new charter + a surgical edit). | (1) Authored against the AIGC charter (the structural sibling), the Ethical AI Use Guideline, and the recorded Rec-14 plan. (2/3) Two independent adversarial verifiers, blind to each other: false-negative (completeness / independence / split-completeness) **0 error/warning gaps** (independence structural: chair + 4/7 voting majority outside the AIGC, reporting CRO/Board vs the AIGC's ERC line; the 5-step challenge mechanism reaches the Board above the AIGC; the AIGC side fully de-claimed; Charter structural bar met; all 6 ethics dimensions covered; one informational note on CRO dual-membership, not a defect); false-positive (citation / authority / contradiction) **0 defects** (all 4 frameworks registered and cited as the sibling does; the Panel correctly bounded as challenge-not-veto and not under-claimed; new seats hedged as adopter-mapped; no contradiction introduced; reporting lines consistent and non-circular; links resolve; house style clean; AIGC Version+Date co-bumped 1.2.2/2026-06-22 -> 1.2.3/2026-06-30). Apply-time orchestrator catch (pre-verifier): a residual AIGC Purpose line "overseeing the ethical, risk, and compliance governance" reworded to drop the ethics-governance self-claim and reference the Panel. (4) Mechanical floor: all 57 gates green. (5) New file + surgical edit; both re-read post-edit. | verified | None. The "General Counsel" (vs the register's "Legal Counsel") and OECD (2023) citation form are pre-existing corpus conventions shared with the AIGC sibling, not introduced here. |
| 2026-06-30 | FR-167(b): add 7 compliance-matrix rows for net-new session docs not yet rowed (LIA, Privacy-by-Design, TIA, breach-notification register, M&A due-diligence, sanctions/export-control, AI Ethics Review Panel), PR #491 | All three conditions: (1) gate-blind fit (a control code can exist, be in-catalogue, pass gates 48/49/54, and still be the wrong control for the row's document); (2) delicate scale (a 7-row reshape of a single cited artefact, 12 cells each across CCM/AICM/ISO/CSF/5 customs columns, where a mis-assignment or a wrong customs-column claim is easy and gate-blind); (3) high cost (the matrix is a cited, adopter-consumed artefact). | (1) 3-worker research fan-out for the per-doc candidate mappings (privacy x4, compliance x2, ai x1), each reading the doc + the reference base. (2) Mechanical existence floor: all proposed CCM/AICM/CSF codes confirmed present in `ccm_aicm_reference.py` / `nist_csf_reference.py` (GRC-12/15/11/13 confirmed AICM-only). (3) Two independent adversarial verifiers, blind to each other and to the research rationale: Verifier (false-negative, re-read every doc + challenged every `N/A`) found **1 genuine miss** (AICM GRC-13 Explainability omitted on the AI panel row while its mandate-sentence neighbours bias/fairness=GRC-11 and human-oversight=GRC-15 were mapped) plus 2 weaker GV.SC hypotheses; Verifier (false-positive, judged every assigned code against its source title + confirmed all 13 ISO clause titles against the scratch 27001:2022 full-text + attempted upstream confirmation of the sanctions customs columns via WebSearch) found **0 hard mismatches**, 7 loose-supporting flags, and confirmed the sanctions customs columns should stay `N/A` (CTPAT/AEO/PIP require partner-security + AML/TF screening, NOT sanctions/denied-party screening; asserting a mapping would equate distinct things). Orchestrator synthesis: added GRC-13 (FN fix); trimmed the genuine stretches (A.5.26+RS.MA on the breach REGISTER since it is an obligations reference not the response activity; ISO §4.2 on sanctions; ISO A.5.36 on the AI panel); kept defensible supporting codes (DSP-01, §6.1, GRC-02, GRC-07, IAM-04). (4) Mechanical floor: all 57 gates green incl. existence gates 48/49/54 on the new rows. (5) Hand-applied 7 rows at thematic anchors (not a scripted bulk apply, given the modest 7-row scale across 3 domain sections), then **re-parsed**: each new row confirmed 12 fields / 13 pipes, gates 48/49/54 green, taxonomy + scorecard regenerated. The 2-verifier adversarial pass IS this batch's `/matrix-fit` semantic-fit check (equivalent-or-stronger than the worklist tool, per the 2026-06-28 AICM-column precedent). | verified | OT block + library-meta/glossary/crosswalk docs are NOT rowed: their IN/OUT scope is deferred to the maintainer ([`pending-decisions.md`](../pending-decisions.md) FR-167(b) entry). FR-167 stays OPEN until that scope is resolved. |
| 2026-06-30 | FR-72: author `compliance/standard-sanctions-and-export-control-screening.md` (sanctions / OFAC / export-control screening Standard with UBO verification and denied-party-list integration, PR #485) | Maintainer-directed (net-new authoring goes through the harness). Conditions (1) gate-blind correctness (no gate checks whether a regime is correctly named, whether the OFAC 50% rule is accurately stated and attributed rather than asserted universal, whether a list version is improperly pinned, or whether jurisdiction-specifics are over-claimed) and (3) high cost (a citable, adopter-consumed, high-stakes legal-domain standard; the ref base holds NO sanctions/export-control source, so any pinned specific would be ungroundable) hold strongly; (2) delicate-scale weaker (single new doc). | (1) Authored structurally (regimes named, never pinned); the ref base confirmed (via `grc_library_scratch/ref/INDEX.md`) to hold no OFAC/EAR/ITAR/FATF source, so no specific list/edition/CFR/ECCN asserted. (2/3) Two independent adversarial verifiers, blind to each other: Verifier B (false-positive, high-stakes-legal lens) **0 defects** (no pinned version/CFR/ECCN anywhere; OFAC 50% rule attributed-not-universal, unpinned, accurately worded, fenced from other regimes; all four authority names correct OFAC/BIS/DDTC/OFSI; ISO 37301:2021 / ISO 31000:2018 / COBIT 2019 APO12 / NIST CSF 2.0 GV all registered/accurate; not-legal-advice disclaimer prominent; 25% UBO threshold marked adopter-defined; AML scope-fence accurate; paired M&A update complete, no "is planned" residual); Verifier A (false-negative, operability/completeness lens) **0 error-level gaps** + 4 warning-level operability refinements, all folded in in-window (segregation-of-duties on match clearance, list-currency control, entity-level event-driven re-screen trigger, reporting-timeline-recording). (4) Mechanical floor: all 57 gates green. (5) Single new file; the 4 refinements applied and the standard re-read post-edit. | verified | None. The M&A procedure's sanctions dimension (FR-71) is now wired to this standard. |
| 2026-06-30 | FR-71: author `compliance/procedure-mergers-acquisitions-due-diligence.md` (M&A due-diligence procedure, PR #484) | Maintainer-directed (net-new authoring goes through the harness). Conditions (1) gate-blind correctness (citation accuracy of the framework-alignment table, accuracy of 20 cross-reference relationships, role-name validity, and no-phantom-link discipline are semantic properties no gate checks) and (3) high cost (a citable, adopter-consumed compliance procedure) hold; (2) delicate-scale weaker (single new doc). | (1) Authored against the gap source (the GRC programme trigger), the internal-audit hand-off, and a compliance Procedure sibling; framework citations limited to canonical-register-backed ISO/COBIT/NIST. (2/3) Two independent adversarial verifiers, blind to each other: Verifier B (false-positive) **0 defects** (all citations verified, COSO correctly omitted, no phantom sanctions link, no invented thresholds, all 7 roles real, 20 cross-refs resolve); Verifier A (false-negative) **0 error-level gaps** + 2 warning-level completeness improvements applied in-window (divestiture scoped out; sanctions coverage gap noted in Limitations). (4) Mechanical floor: all 57 gates green (one apply-time gate-2 `ensure that` catch pre-verifier). (5) Single new file; improvements applied and re-verified. | verified | None for the procedure. Related open items (separate, not harness-gated): the planned sanctions standard (FR-72) will become the governing instrument for the financial-crime dimension; the TIA cross-reference wiring follow-up. |
| 2026-06-30 | FR-34: author `privacy/template-transfer-impact-assessment.md` (Transfer Impact Assessment template, EDPB Rec 01/2020 / GDPR Chapter V, PR #483) | Maintainer-directed (net-new authoring goes through the harness). Two of three conditions hold strongly: (1) gate-blind correctness (accurate GDPR Chapter V operationalization, correct attribution of the not-held EDPB methodology, and not pinning an unverified SCC version are semantic properties no gate checks); (3) high cost (a citable, adopter-consumed transfer-compliance instrument). Condition (2) delicate-scale is weaker (a single new template). | (1) Authored against the held GDPR Chapter V full text (Arts 44 to 49); the EDPB Rec 01/2020 methodology is NOT held (attributed, own prose) and the SCCs/2021-914 are NOT held (referenced generically, no version pinned). (2/3) Two independent adversarial verifiers, blind to each other: Verifier B (false-positive) found **0 defects** (all Chapter V citations accurate; SCC version correctly not pinned; EDPB attributed not quoted; Schrems II as concept; TIA effect under-claimed); Verifier A (false-negative) found **0 error-level gaps** + 2 warning-level completeness improvements applied in-window (Art 46(2)(e)/(f) tool routes; Art 48 foreign-disclosure assessment). (4) Mechanical floor: all 57 gates green. (5) Single new file; the two improvements applied and re-verified against the held GDPR text. | verified | FR-74 residual (annex/procedure Schrems II deepening) and the TIA cross-reference wiring remain as separate TODO items, not harness-gated. |
| 2026-06-30 | FR-31: author `privacy/framework-privacy-by-design.md` (Privacy by Design Framework, GDPR Article 25, PR #482) | Maintainer-directed (net-new authoring goes through the harness). Two of three conditions hold strongly: (1) gate-blind correctness (whether GDPR Art 25 is accurately operationalized, whether the seven principles are correctly attributed vs over-claimed as law, and citation-edition accuracy are semantic properties no gate checks); (3) high cost (a citable, adopter-consumed privacy framework). Condition (2) delicate-scale is weaker (a single new framework doc, not a bulk reshape). | (1) Authored against the held GDPR full text (Art 25 + Recital 78). (2/3) Two independent adversarial verifiers, blind to each other: Verifier A (false-negative, completeness vs GDPR Art 25 + the consent-framework structural bar) and Verifier B (false-positive, every quote/citation vs ground truth + the canonical-citations register) **both independently caught the same real defect**: a hallucinated `ISO/IEC 29134:2023` citation (verifiable edition is 2017; a logged recurrence of #162/#167; gate-blind, 29134 absent from the canonical register), orchestrator-introduced and propagated to the register row. Fixed to `:2017` in both places pre-merge. All GDPR Art 25(1)/(2)/(3) quotes verbatim-accurate; Recital 78 correctly framed as encouraging (not binding) producers; the seven principles correctly attributed to Cavoukian (not over-claimed as legal force). (4) Mechanical floor: all 57 gates green on the fixed state. (5) Single new file; the 29134 fix applied in-window and re-verified. | verified | None. NB: the catch is the harness backstopping an ORCHESTRATOR-introduced hallucination (not a worker draft); logged in [`hallucination-metrics.md`](../hallucination-metrics.md) as a pre-merge catch. |
| 2026-06-30 | FR-32: author `privacy/template-legitimate-interest-assessment.md` (LIA template for GDPR Article 6(1)(f), PR #480) | Maintainer-directed (net-new authoring goes through the harness). Two of three default conditions hold strongly: (1) gate-blind correctness (whether each cited article/recital is the right authority and whether the three-part test is complete is a semantic/fit property no gate checks); (3) high cost (a citable, adopter-consumed privacy template; a misquote or a missing test-limb propagates). Condition (2) delicate-scale is weaker (a ~130-line single new file, not a bulk reshape). | (1) Authored against the held GDPR full text (`grc_library_scratch/ref/legislation/EU/GDPR-Regulation-2016-679--full-text.md`). (2/3) Two independent adversarial verifiers, blind to each other and to the authoring rationale: Verifier A (false-negative, required-but-missing content vs GDPR + the DPIA structural bar) found the doc substantively complete with **2 warning-level gaps** (compressed sign-off; no Article 21(4) prominence prompt); Verifier B (false-positive, every quote/citation vs the GDPR full text + the canonical-citations register) found **0 error-level defects** (all quotes verbatim, all citations registered/current, EDPB Guidelines 1/2024 correctly not cited). (4) Mechanical floor: all 57 audit gates green on the fixed state. (5) Single new file authored (not a scripted bulk apply); both warning gaps + the FP scope-precision note applied in-window and re-verified (57/57 green, orchestrator re-read). | verified | None; the template is a structural baseline (its Limitations section directs adopters to validate against their supervisory authority's guidance). |
| 2026-06-28 | FR-167: add the CSA AICM v1.1 column to the compliance matrix (PRs #447 gate, #448 data, #449 tool-scoping) | All three: (1) gate-blind fit (a control code can exist, be in the right catalogue, pass gate 49, and still be the wrong control for the row's document); (2) delicate scale (a 261-row single-artefact reshape across all 11 domain tables); (3) high cost (the matrix is a cited, adopter-consumed artefact) | (1) 5-worker research fan-out for the per-row candidates. (2) Mechanical AI-signal grep over the negatives. (3) Two independent adversarial verifiers: Verifier A (false-negative, re-read every title-based `N/A` document) caught **9 genuine misses**; Verifier B (false-positive, judged each assigned code against its AICM v1.1 title) tightened **3 over-assignments**. (4) Programmatic invariant: every code confirmed `is_aicm_only` (34/34); gate 49 validated the column. (5) Deterministic scripted apply (dry-run-validated, idempotent-guarded, keyed on a path-to-cell map) then re-parse of all 68 rendered cells against the verified map (0 mismatches). Guard-first: #447 gate landed before #448 data. | verified | Closing whole-matrix `/matrix-fit` over the 62-row worklist remains (FR-167 closure); semantic-fit assurance for this batch is recorded in [`matrix-fit/history.md`](matrix-fit/history.md). |
