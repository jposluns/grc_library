# Improvement Log Register

**Version:** 1.0.824\
**Date:** 2026-07-24\
**License:** CC BY-SA 4.0

Reverse-chronological register of post-merge retrospectives. One row per merged PR (per the `/retro` skill's discipline). New rows on top.

See the [`pr-retrospective` SKILL](../dev-security/claude-rules/skills/pr-retrospective/SKILL.md) for the column semantics and the six-step process. The slash-command entry point is [`/retro`](../.claude/commands/retro.md).

## Purpose

The register accumulates per-PR retrospectives. Each entry is light-touch (one paragraph per cell). The value emerges over time as patterns surface across many entries:

- First occurrence of friction is **observation**.
- Second occurrence is **signal**.
- Third occurrence is **pattern**, and the pattern earns a "Proposed improvement" candidate that the next planning cycle picks up.

The register pairs with the worker-side `worker-brief-template.md` (codifies guard rails against worker-side failures) and the apply-time-catch tracking in [`hallucination-metrics.md`](hallucination-metrics.md) (logs orchestrator-side verifications). Together the three close the per-PR learning loop:

- **Worker brief**: prevents recurring worker-side failures before they reach the orchestrator.
- **Apply-time catches**: log orchestrator-side verifications of worker output.
- **PR retrospective**: surfaces process-level patterns that warrant pack-rule updates, new gates, or worker-brief additions.

## Convention

- New entry per merged PR. The `/validate-pr` cycle completes; then `/retro` appends one row.
- Pattern and Proposed-improvement entries (if present) are surfaced in chat for maintainer awareness; the register is the archive.
- Empty cells in Pattern or Proposed-improvement are valid (most PRs have neither).
- The register's row commit is **batched into the next PR**, whatever its substantive purpose, per the recursion-avoidance rule (see [`/validate-pr` SKILL.md](../dev-security/claude-rules/skills/validation-sweep-pr-scoped/SKILL.md) Termination section). The retrospective itself runs immediately after `/validate-pr`; only the row commit waits.
- **Third-occurrence-to-gate:** a failure class recorded in the Pattern column of three or more distinct PR rows graduates to a Proposed-improvement gate/convention candidate in the third row's Proposed column, tagged `[3rd-occurrence]`, so a thrice-recurring class is not left to checklist prose. (GR-P3, TODO 4.7.)

- The two removal ledgers, [`claude-md-considerations.md`](claude-md-considerations.md) (the PR #441 CLAUDE.md condense) and [`claude-rules-considerations.md`](claude-rules-considerations.md) (the GR-P2 pack-rule condense), are reviewed as part of each `/retro` (a quick scan of their open RM entries) and the periodic hallucination-metrics pass (a deeper pass): for each open removal, check whether its "evidence the removal was wrong" signal has appeared, and if so advise the maintainer to restore the cut text or to make a new change the removal inspired, recording the disposition in that entry's Status.

The register is maintainer working state, exempt from corpus audit gates per the `.working/` directory exemption.

## Disposition convention

A Proposed-improvement cell is **pending** until it carries a disposition token. When a later PR closes a candidate, `/retro` step 6 (the disposition scan) appends the token to the originating row's cell; the row's original text is never rewritten, the token is appended after it:

- `CODIFIED in <carrier>`: the candidate's durable home shipped (a pack rule clause, a SKILL step, a checklist line, a gate).
- `ROUTED to <destination>`: a backlog bullet or bundle now carries the candidate, and the register row stops being the carrier.
- `WATCH (fires on <n>th occurrence)`: the sanctioned holding state for conditional candidates awaiting a recurrence threshold.
- `REJECTED (<reason>)` and `EXPIRED (<date>)`: maintainer calls. The scan proposes these for aged candidates; the maintainer dispositions them, and a pending candidate is never silently dropped.

The convention is forward-looking (introduced 2026-07-04 with the GR-8(b) closure build): rows older than the introduction are not retroactively tokenized in bulk; each is tokenized when its candidate is dispositioned.

## TODO-mining cursor

The disposition token above records a candidate's OUTCOME; this cursor records the ASSESSMENT FRONTIER, how far the register has been mined for TODO-worthy improvements, so a future pass need not re-mine the whole register end to end. (Maintainer-directed 2026-07-10.)

**Mined-for-TODO-extraction through: PR #761 (2026-07-10).** The sweep94 log-mining pass assessed every row through #761. The aged pre-2026-07-04 pending tail (~239 rows) is owned by TODO §3.7 (the one-pass batch triage); this pass sub-classified the post-2026-07-04 window plus the aged tail's proposal-language rows. Outcome: **3 new TODO items** (§1.9 completion-verification file-type-width axis from #688/#689; §3.34 detailed-mirror link-resolution check from #715; §3.35 path-resolution fixture rail from #634), **1 EXPIRED-vs-pursue maintainer call** (#272/#465 count-gate idiom broadening), and the rest already-shipped / already-routed / reinforcement / not-mechanizable. **Already-shipped candidates confirmed this pass (no new TODO):** #711/#712/#716 → gate 67 (`lint-doctype-parity.py`); #702/#703/#704 → worker-brief DO rail 14; #687 → `tools/detect-env.py` + `/resume` step 3 (former §3.18); #187 (hallucination-metrics) → gate 50 Check 1. **WATCH candidates still latent** (no TODO by design, fire on their recurrence trigger): #685/#683/#682 + #681 (enumeration-parity escape), #680 (background-task SOP refinement), #665 (meta-prose-count recurrence), #626 (validation-layer pass-through, 2nd occurrence).

A future mining pass assesses only rows newer than this cursor (plus any WATCH candidate whose trigger has since fired). The endgame: once a window's rows are fully dispositioned and age out, they cycle to a `grc_library_scratch` archive per the mechanism queued as TODO §3.36 (mirroring the CHANGELOG sweep-to-scratch model), keeping the live register to a recent window.

## Entries

| Date | PR | FR closed | What went well | Friction | Pattern (if any) | Proposed improvement |
| 2026-07-24 | 1137 | Correct the #1135 gate-74-docstring free-prose miss + fix ledger F1 | Promptly fixed a #1135 miss (a stale docstring on main) as a small standalone PR, and folded in the #1136 QA finding F1 (unescaped ledger pipe) in-window; the offloaded /validate-pr confirmed the fan-out is now genuinely complete and the ledgers render uniform. | The #1135 change-impact grep was CASE-SENSITIVE ("fourth") and missed the docstring's "FOURTH"; and I re-hit the §3.73 row-fusion inserting the #1136 retro row (caught by a field-count check). Two mechanical-hygiene slips in one PR area, both caught before push. | Change-impact / contradiction greps must be CASE-INSENSITIVE (`grep -i`), because a stale carrier can differ only in case (FOURTH vs fourth); add `-i` to the bare-token grep discipline. And the §3.73 row-fusion recurs whenever a ledger insert anchors on the NEXT row's prefix; the header-only anchor is the fix and must be the default. | Add "case-insensitive" to the contradiction-grep close-out discipline; bump the standing §3.73 ledger-row-integrity gate (it catches both the fusion and the unescaped-pipe column drift mechanically). |
| 2026-07-24 | 1136 | §5.9 NYC-LL144 candidate reconciled and struck (item h) | The strike rested on delivered research plus orchestrator corroboration (the NYC LL144/AEDT annex exists, the DCWP rule is held); a clean bookkeeping close mirroring the Canada-AIDA shape. The offloaded /validate-pr caught a real pre-existing ledger defect. | Two ledger-integrity issues surfaced on the QA-row batch: (1) a §3.73 improvement-log row-FUSION when I inserted the #1135 retro row by anchoring the Edit on the next row's prefix instead of the header (the guard's gate 50 + gate 36 caught it pre-push; I de-fused surgically); (2) the #1136 /validate-pr then flagged F1, an unescaped Markdown pipe in "Edit\|Write" in the two #1134 rows (8 cols vs 7), pre-existing from my own #1134 ready-to-paste rows, fixed in #1137. | The §3.73 row-fusion class recurred (anchor-on-next-row-prefix); the interim mitigation (anchor the Edit on the HEADER line and append after) must be the DEFAULT for every ledger-row insert, not just remembered under load. And any ledger cell containing a literal pipe character (a tool/matcher name like "Edit\|Write") must escape it, or a strict column-count parser sees an extra column. | Both are the standing §3.73 ledger-row-integrity gate candidate (a structural row-shape check over the .working/ ledgers): it would catch BOTH the fusion and the unescaped-pipe column-count drift mechanically. Bump priority on that deferred gate. |
| 2026-07-24 | 1135 | §3.56a guard 3: rule-provenance register as a gate-41 surface | A one-line gate-41 config addition made a new rule un-shippable without its provenance entry; the passing gate run itself proved the surface non-vacuous (an empty parse would have failed on 15 missing), and a drift-lock unit test made that guarantee permanent. The skeptical verifier empirically confirmed the fail-on-missing behaviour. | The "config-only" change had a real change-impact fan-out: five live prose surfaces described gate 41 as covering "three" rule-enumeration surfaces (and called gate 74 "the fourth"), all needing the three-to-four update. Caught by reading the spec §6 before commit (the change-impact map working as intended), not missed. | A gate-COVERAGE change (adding an enumeration surface to an existing gate, not just a new gate) triggers the same free-prose fan-out as a new gate: grep every "N surfaces"/"the Nth surface" carrier at bare-token width before commit. The change-impact map should note that extending an existing gate's coverage is a type-A-adjacent trigger. | Consider a change-impact-map note that "extending an existing gate's surface set" is a fan-out trigger, not only "new/changed gate". |
| 2026-07-24 | 1134 | §3.62 branch-to-main edit-guard hook (BUILD) | An FP-safe PreToolUse Edit\|Write guard landed with a tight envelope (blocks a project-repo edit only while on main; allows siblings, feature branches, detached HEAD, and fails open on any git/parse error), a 7-case self-test, and regression wiring; the skeptical verifier ran live allow/fail-open probes and found no confirmed defect. | None material. The verifier's one LOW nit (a docstring "does not fire when CLAUDE_PROJECT_DIR unset" note that was imprecise given the hardcoded fallback) was fixed pre-push; it erred in the fail-safe direction. | For a new PreToolUse hook, a docstring "does not fire in condition X" claim must be checked against the actual main() fallback chain (an env-var default can mean it DOES fire); the fail-safe direction still makes it a nit, not a defect. | None new. |
| 2026-07-24 | 1133 | Codify completeness-over-sampling + chat-answer pacing (both trees + CLAUDE.md) | The maintainer's core meta-correction (a set-scoped instruction is a contract to exhaust the set) landed as a portable pack section byte-identical across both trees plus its CLAUDE.md instantiation, and the chat-pacing convention was correctly placed CLAUDE.md-only as project chat-mechanics; the offloaded /validate-pr returned CLEAN. | The pre-push verifier caught a gate-50 blocker: the CHANGELOG asserted the #1132 /validate-pr + /retro rows were "batched" before those rows were actually in the diff (the #1132 QA had not been consumed when #1133 was first committed), so gate 50 failed at HEAD and two CHANGELOG clauses were false-as-written. Resolved by consuming the #1132 result and adding its rows before push. | Third occurrence of the "batched #N rows" claim authored ahead of the actual rows (#1131, #1133; the #1131 retro already flagged it). The recurrence is that the offloaded QA of the just-merged PR is not always delivered by the time the NEXT PR is first committed, so the batch-claim races the delivery. | Do not write the CHANGELOG "Batched" clause until the prior-PR QA rows are physically in the working tree; when the offload is still in flight at first-commit time, commit without the Batched clause and add both the rows and the clause together once the result lands (or gate the claim on a grep of the ledger diff). | None new beyond the above. |
| 2026-07-24 | 1132 | ETSI register section + Canada-AIDA §5.9 strike | Two upstream-verified ETSI register rows (EN 304 223 V2.1.1 normative, TR 104 128 V1.1.1 informative) landed from offloaded research, and the AIDA strike rested on reading the Canada annex (already accurate, AIDA lapsed) rather than inferring a gap. The offloaded /validate-pr returned a CLEAN PASS. | The register add needed two coupled tool surfaces the skeptical verifier could not see (no egress, no gate-run): the gate-24 etsi.org domain allow-list and the gate-72 ETSI-standards heading-tier map; the pre-push guard caught both (gate 24 + gate 36 regression). A transient GitHub runner-provisioning failure also reddened the web-generator-health job (re-run cleared it). | When a PR adds a new register sub-table citing a new external domain, the change-impact set includes the gate-24 domain allow-list AND the gate-72 heading-tier map, not only the register rows; add both in the same PR. A CI job failing with "not acquired by Runner" is infrastructure, re-run it rather than chasing a corpus cause. | Consider a register-subtable-to-gate-72-tier parity check (a new sub-table with no tier mapping is the escape gate 36's regression test caught). |
| 2026-07-24 | 1131 | pack cwd-inference corollary (A2, both trees) | The portable cwd-inference discipline landed clean and byte-identical across both trees (gate 37); the pre-push verifier confirmed coherence with the parent inference-cascade rule and caught two pre-push issues (a premature "batched #1130 rows" CHANGELOG claim written before those rows existed, and a dangling "self-verification companion" phrase), both fixed before push. | The batched-rows CHANGELOG line was authored ahead of the actual rows (the #1130 QA had not delivered when #1131 was committed), and the improvement-log Version bump was initially missed (D2-caught); both fixed via amend before push. | When a PR's CHANGELOG asserts "batched #N rows", write that line only after the rows are actually in the diff (or re-amend when they land); and treat the .working ledger Version bump as part of the row-batch edit, not a separate step. | None new. |
| 2026-07-24 | 1130 | Close §3.108 + §3.36, record 4 decisions (bookkeeping) | Two clean research-to-close loops landed: §3.108 closed as already-satisfied on the offloaded research finding (the specific ASVS codes were already migrated to 5.0.0 by the 1.61.6 remap, no 4.0 residual), and the ETSI-register and §3.36-close decisions were recorded from their respective offloaded deliveries. The DONE entries carry the honest close-rationale (already-satisfied / superseded), not a bare "done". | None material; terse-tier scope held (9 files, working-state only, no corpus/gate/pack change) and the permanent-numbering rule was respected (3.108 retired, counter left at 3.109). | For a close-as-already-satisfied or close-as-superseded item, record the specific rationale in the DONE entry (what made it a no-op) so the closure is auditable, not just marked done; and keep the section counter advanced even when the just-created item closes immediately (numbers never recycle). | None new. |
| 2026-07-24 | 1129 | PQC §6.2 deepening (advanced) | The apply corrected a research imprecision at source: the worker research had led with ML-DSA-44 = category 1 (the conditional reduction); #1129 ships the correct primary claim (category 2, reduced to 1 only under a sub-192-bit RBG), held-FIPS-204-verified, with no residual flat cat-1 claim. All parameter-set/category facts trace to the held FIPS full-text; the three prior #1128 findings were fixed in the same PR. | The FIPS 205 Section 11 extract is Unicode-math-dense and defeats grep; the category facts had to be read (table of contents plus the n = 16/24/32 category sentence) rather than grepped. | For a security-category claim, verify each parameter set against the held primary at the leaf (a conditional reduction is not the primary category); and for a Unicode-dense held extract, read the section and its table of contents rather than trusting a grep that silently returns nothing. | None new. |
| 2026-07-24 | 1128 | §3.66 generic-ASVS-remap apply (closed) | The high-assurance harness landed the substance cleanly: both false-negative-lens findings (the systematic five-row V16.4 gap and the previously-unenumerated validate-inference:110 row) were adopted in the apply, zero residual generic codes in either tree, byte-parity, and 75/75 gates. | Two downstream-consistency items escaped every gate and only surfaced in the manual post-merge QA: a SKILL restatement (evidence-grounded) that did not pick up the parent's new V16.4, and the TODO P3 "Next item number" counter that was not advanced after the new §3.108 was created (a latent number-collision). Neither is gate-checkable today. | When an apply adds an ASVS code to a governance rule that has a SKILL restatement, re-derive the SKILL's code list from the parent's FINAL distinct set (not a mechanical per-code swap); and when a PR creates a new TODO item, advance that section's "Next item number" counter in the same change. Both are candidates for a cheap new gate (SKILL-restatement-vs-parent ASVS parity; Next-item-number-vs-assigned consistency). | Consider gates for the two escape classes above. |
| 2026-07-24 | 1127 | Codify wind-down compaction-gate (.claude/-only) | A one-paragraph operating-context codification landed cleanly and self-consistently: the gate declares "sits ON TOP OF the offer-regime" and forward-references the two OFFER cases, so it narrows the existing regime without contradicting it, and the always-valid degradation trigger is preserved. Terse-tier + batched-QA scope held (7 files, no corpus/gate/pack change). | The one soft spot is a below carve-out ("an offer does not require degradation evidence") now qualified by the gate but not cross-referenced; resolvable top-down, flagged as an optional tighten rather than a defect. | When a new gate paragraph modifies an existing regime described in the same doc, state the precedence explicitly in the new paragraph (as done here) and optionally back-reference from the modified clause; the forward-reference alone is sufficient for correctness but a back-reference removes bottom-up-reader ambiguity. | None new. |
| 2026-07-24 | 1126 | §3.1 ISO/IEC designation reconcile (closed) | The read-only research-then-apply split worked cleanly on a corpus-wide-ish reconcile: research-3001 verified each of the 11 designations against iso.org + the held base (5 fixes, the 12207 edition decision, 23247 confirmed-bare), and this PR applied exactly that with zero residual bare tokens and the durable register-plus-gate correctly left open as §3.2. | None material; the anti-false-completeness §3.1 reconcile that had been flagged corpus-wide-sweep-attended was scoped by the research to just 4 files and 7 edits, making it a clean apply. | For a designation/format reconcile, an offloaded read-only per-standard verification pass turns a scary corpus-wide-sweep into a small, precise, residual-grep-checkable apply; reuse the pattern. | None new. |
| 2026-07-24 | 1125 | Repo-target guardrail hook widened (softened scope) | The widening was verified the right way: the hook's 13-case self-test plus live evasion probes on both lenses (mutating-git shapes all blocked, legit git -C / project-tool / cd-prefixed / read-only / arg-mention all allowed), so the guardrail is proven non-decorative and non-over-blocking against the exact near-miss it targets (a bare git add -A against the wrong repo). | The one live probe that could not fire (the sibling-tool block) was correctly attributed to the worktree having no adjacent sibling repo, not to a hook gap, and covered by the self-test. Separately, the orchestrator's own cd-slip on scratch cwd-guarded tools recurred 5x this session even after shipping this guardrail; mitigated by routing scratch git ops through git -C and relying on scratch CI. | For a PreToolUse guardrail, exercise both the false-negative (must-block) and false-positive (must-allow) lenses with crafted stdin probes in addition to counting the self-test; and note honestly which paths the test environment cannot reproduce live. | None new. |
| 2026-07-24 | 1124 | Terse working-state batch (#1123 QA rows + active-standby wake note) | The bookkeeping PR stayed correctly scoped: working-state only, terse-tier CHANGELOG, and an honest active-standby wake-state block that frames queue-drained as BLOCKED-pending-maintainer (not a wind-down, not an idle) with grouped wake actions, matching the offloaded anti-false-completeness re-triage. | None material; the entry avoided the queue-exhaustion-as-wind-down trap and recorded the read-only research-3001 result as the worker-safe standby work. | For an apply-queue-drained state, record the active-standby posture with the enumerated per-item blockers and grouped wake actions rather than a wind-down; keep the bookkeeping PR terse-tier and working-state only. | None new. |
| 2026-07-24 | 1123 | §3.20 publications-screening close + §4.2 pack language-rule review | Two closes in one PR, each honest about scope: the §3.20 wave's real work is a single cross-repo ref screen (WP260) that the grc_library PR correctly attributes to grc_library_ref #102 rather than claiming a corpus edit, and the §4.2 review shipped only the one genuine clarity fix (the Prisma $queryRaw NEVER-over-Safe contradiction) while routing the discretionary residuals (F1 alignment-table shape to §3.74, F2/F4 positioning to pending-decisions) rather than force-applying them. | The ref-repo CI check-rollup 403s on the token (GraphQL/check-runs), so `gh pr checks --watch` failed on grc_library_ref #102; worked around with `gh run watch <id> --exit-status` (REST) plus a post-merge `validate.py` on merged ref main. Separately the sibling-repo cd-slip recurred repeatedly (the wrong-repo/repeat-failure guards caught each). | For a review-class item, ship only the defect-grade fix and route discretionary residuals to a decision surface; for a wave whose substance lands in a sibling repo, attribute it to the sibling PR and keep the corpus-side PR to the TODO close plus records. For ref-repo PRs: watch CI via `gh run watch --exit-status` (REST) and confirm merged ref main with `validate.py`; sibling-repo shell commands use absolute paths / `git -C`, never a cd prefix. | None new. |
| 2026-07-24 | 1122 | TODO §3.63 (ETSI TR 104 128 informative see-also, §3.63 close) | The offloaded see-also draft carried the one accuracy nuance that mattered (the held TR names its baseline as TS 104 223 while §7 cites EN 304 223, the TS and EN forms of the same ETSI Securing-AI baseline), the orchestrator confirmed the equivalence at the held catalogue.yml, and the shipped row frames the TR as guidance for "the ETSI Securing-AI baseline the EN 304 223 rows cite" rather than over-asserting a verbatim citation. No register row, matching the unregistered normative parent. | None material; the TS-versus-EN nuance was the trap, and flagging it in the research kept the shipped framing accurate. | For an informative see-also whose held text names a predecessor designation of the corpus-cited standard, frame the see-also against the umbrella baseline (confirmed via the held catalogue) rather than asserting the informative doc cites the exact corpus designation. | None new. |
| 2026-07-24 | 1121 | TODO §3.75 parts 1+2 (web-corpus link manifest + gate 75) + r14 guardrail cadence reset | The design-verify-build-verify chain held end-to-end: the offloaded code draft designed and verified the gate and the committed-manifest build change (and corrected the seed's ephemeral-dist "fourth byproduct" framing to a committed artefact), a refute-briefed verifier caught the one overclaim in the draft's comment (the curated list PARALLELS render_llms_txt rather than being a consumed single source), and the shipped prose was corrected accordingly; the post-merge sweep re-ran the gate and proved it is not decorative. | None material; the drift-proof claim being scoped to only the domain/type rows (with the curated-list bind recorded as a §3.75 follow-up) is the honest framing, not an overclaim. | For a generated committed artefact, the taxonomy/portal pattern (committed + a build --check drift guard + a resolve gate) is the right shape; and a re-derivation is drift-proof only for the sources it actually re-derives from, so scope the drift-proof claim to those and route the hand-kept remainder as a bind-it follow-up. | None new. |
| 2026-07-24 | 1120 | §1.19.10a (recurring weekly root-condensation wiring) | The design-to-build loop closed cleanly: the offloaded research proposed extending build-public-changelog.py with an event-driven mode under the sweep's emit-verify-prune contract, and the build did exactly that, with the data-safety re-parse assertion and a --> neutralization the verifier added, all confirmed by a 10-test self-test. Deferring the first outward-facing condensation run to an attended step (it needs an authored weekly paragraph) is the right call for an unattended run. | None material; the wiring-versus-first-run split kept an authorial, adopter-facing change out of the unattended path. | For a mechanism whose FIRST use restructures an outward-facing artefact and needs authored prose, ship the wiring unattended but gate the first run to an attended close-out; reuse this split. | None new. |
| 2026-07-24 | 1119 | TODO §3.15 (ATLAS currency + #654 LLM02 T0024 to T0057 re-map) | The reference-currency loop closed cleanly end-to-end: the offloaded ATLAS map found the corpus citations already current (the prior #635 and pack-1.54.4 re-maps held) and surfaced only the one genuine fit finding (the #654 T0024-to-T0057 LLM02 mapping), which this PR applied to exactly the four rows, with a held-source re-verify confirming T0057 (LLM Data Leakage) is the LLM02 fit and no live T0024 residual. | None material; the map correctly reported "already current" rather than manufacturing stale findings, and the one fit finding was scoped to the four LLM02 rows only. | For a currency-map task, reporting already-current plus the single genuine fit finding (rather than re-litigating prior fixes) is the right shape; keep it. The pack-only re-map (no .claude mirror, no taxonomy) is the correct blast radius for an AI-rules ATLAS code change. | None new. |
| 2026-07-24 | 1118 | TODO §3.42 (ISO/IEC 5259-5:2025 citation, sub-part complete) | The new-ingest reference-breadth discipline held: only the one claim-accurate part (5259-5, governance framework, apt for a governance procedure) was added, and parts 1 (overview) and TR 6 (visualization) were judged off-claim and excluded rather than stuffed in; the item was narrowed (not falsely closed) because its Canadian/international half remains. | None material; the narrow-not-close bookkeeping on a partially-completed backlog item was handled correctly (heading narrowed, MEG row updated, no DONE rotation, CHANGELOG "advances"). | Keep the anti-stuffing habit on new-ingest breadth passes (add only claim-accurate parts, exclude overview/visualization/vocabulary parts that would be citation-stuffing), and keep narrowing partially-done items in place rather than rotating them. | None new. |
| 2026-07-24 | 1117 | TODO §3.106 (OWASP GenAI Red Teaming Guide see-also in §23) | The reference-audit breadth cadence closed its second routed candidate cleanly: the LOW-priority held-but-unused Guide was engaged as a corroborative see-also (not forced into a normative slot), the four blueprint phase names were held-source-verified against the Guide's own checklist, and the previously-held-but-unregistered source gained a register row. Both §3.106 and §3.107 (the two candidates the breadth cadence routed) are now closed. | None material; the see-also-versus-normative distinction was handled correctly, which is what keeps a corroborative anchor from over-claiming. | For a held-but-unused source that is guidance rather than a normative standard, the corroborative-see-also pattern (anchor it, route normative weight to the existing normative source, register it) is the right shape; reuse it. | None new. |
| 2026-07-24 | 1116 | TODO §3.107 (Texas TRAIGA + Illinois HB 3773 in §7.5) | The two-layer verification did exactly its job: the offloaded held-plus-upstream fact-sheet gave the effective dates and structure, and a refute-briefed verifier at authoring time caught two held-source mis-statements in the Texas prohibited-use summary (a "sole intent" phrasing mis-attributed to Sec. 552.052, and an unscoped social-scoring prohibition that the statute limits to a governmental entity under Sec. 552.053) before they shipped. The post-merge validate-pr re-confirmed the corrected text at the held source. | The originating fact-sheet compressed two distinct prohibited-use sections (552.052 manipulation, 552.053 social scoring, 552.057) into looser summary language; a per-section quote-anchor in the fact-sheet would have surfaced the imprecision earlier. | For statute bullets that summarize multiple prohibited-use sections, anchor each clause to its own section number and quote in the offloaded fact-sheet, so the compression happens against quotes rather than paraphrase. | None new; the verifier-plus-validate-pr chain already caught this, which is the intended defence in depth. |
| 2026-07-24 | 1115 | Reference-breadth engagement of the OWASP MCP Top 10 in §12 | The /reference-audit breadth cadence closed its own loop cleanly: the cadence surfaced the held-but-unused MCP Top 10 (a whole §12 section plus TC-09 unanchored), an offloaded mapping dispositioned all ten categories against the held source, and the authored Framework-anchors line applied it with an accounting fix (all ten dispositioned, not a "two-remaining-gap" framing) that the post-merge validate-pr confirmed at source with 0 findings. | None material; the disposition-all-categories discipline is what kept the anchor line from reading as a partial map. | The breadth-to-engagement path (reference-audit finds held-but-unused, offload a held-source mapping, author an anchor line, validate at source) is a repeatable pattern; the two lower-priority candidates were correctly routed to §3.106/§3.107 rather than force-fit. | None new; the D8 root-entry length stayed within the sibling norm this PR. |
| 2026-07-24 | 1114 | OWASP §2.27 PR-2 (§36 ASI column + crosswalk note, §2.27 closed) | The full high-assurance harness paid off on the error-prone derived column: two independent adversarial lenses (false-negative and false-positive) over the held source produced a reconciled conservative column that the post-merge validate-pr confirmed cell-by-cell at the held source with 0 findings, and the reconciliation caught a forced Excessive-agency-to-ASI10 mapping the naive read would have kept. | None material; the derived per-Control-Area-to-ASI bridge is exactly the surface that warranted two lenses, and it held. | The two-lens-then-reconcile pattern is the right default for a derived crosswalk into an expert-facing standard; bank it for the next matrix-column build (for example a future ASI or MCP-Top-10 column). | None new; the D8 root-entry length stayed within the sibling norm this PR (about 599 chars), so the D8-length-gate remains the standing candidate follow-up, not a fresh flag. |
| 2026-07-24 | 1113 | OWASP §2.27 PR-1 (three §6 TC classes) | The offload-verify-author loop closed cleanly: worker-a drafted, the held-source verifier flagged two accuracy corrections (TC-17 origin classes, TC-19 external trigger), and #1113 applied both verbatim, so the shipped threat classes are held-source-accurate; a second refute-briefed verifier then cleared the final prose (REFUTED-clean) and the post-merge validate-pr found 0 findings. | Nothing new; the two-worker draft-then-verify path on an expert-facing standard worked as intended. | Keep the pattern for PR-2 (the §36 column): the two independent adversarial lenses delivered a reconciled conservative column (row 3 ASI02-direct, row 6 drop ASI10, row 9 ASI08-direct, row 8 no-direct-ASI), which #1114 applied and source-validated. | 
| 2026-07-24 | 1112 | §2.17 (California CCPA ADMT annex) | The independent offloaded validate-pr worked as designed: worker-a (not the worker-b that originated the annex research) re-verified all three ADMT quotes verbatim at the held CCPA regulation and the 2027 compliance date at section 7200(b), and the prior skeptical verifier's accuracy fixes (OAL date dropped, privacy-annex ADMT overlap acknowledged, consolidation routed to §3.105) all held; 74/74 gates, 0 findings. | None material; the batched #1111 row carried the recurring D8 root-entry-length LOW, which #1112 itself fixed (root entry back within the sibling norm). | Independent verification of a cross-worker build is the right routing (the verifier is not the originating worker); re-reading the primary source caught a citation the verifier itself initially mis-attributed (section 7010 vs 7200(b)), which is exactly why positive re-verification at source is required. | None new; the D8 length-ceiling gate remains the candidate follow-up #1111 logged. |
| 2026-07-24 | 1111 | Website sidebar clip fix (§3.94 mechanical half) | The offloaded grounded CSS diagnosis paid off: it refuted the obvious box-model cause, identified the overflow-x/sticky interaction, and recommended the exact zero-regression `clip` fix that shipped, with §3.94 correctly kept open for the UX root cause; the diagnosis also averted a speculative unattended CSS change (the item's own body reserves the UX fix for an attended pass). | The D8 root-entry length ceiling drifted on two consecutive PRs (#1110 about 637, #1111 about 647, versus a 423-to-546 sibling norm) because it is prose-only and not gated. | The D8 length drift is a recurring, mechanizable seam: a prose-only ceiling drifts without a gate. | Behavioural fix applied in #1112 (a shorter root entry back within the sibling norm); pinning the D8 concrete bound (a per-entry char/word cap) with a cheap delta check remains a candidate follow-up so the ceiling is gated rather than eyeballed. |
| 2026-07-24 | 1110 | Singapore §2.19 annex + F1/F2 hot-fixes | The annex applied cleanly (nine dimensions held-source-matched, all ISO 42001 anchors real, indexes and generated artefacts in sync) and both qa-1109 findings were hot-fixed in the same PR; the offloaded held-source + upstream verification (currency confirmed this turn) kept the orchestrator's spend on authoring, and a refute-briefed verifier cleared it with two LOW crosswalk-precision refinements applied. | The one recurring drift is root-CHANGELOG-entry length: #1110 is the longest recent entry (about 637 chars), and the D8 ceiling is prose-only with no pinned numeric bound, so it is not mechanically enforced (gate-compliant but eyeballed). | The content-annex build is now a repeatable pattern (offload structural draft + held-source verification, orchestrator authors + re-verifies, skeptical verifier clears); the D8 length ceiling stays a soft prose convention. | Consider pinning the D8 concrete bound (a per-entry character or word cap) with a cheap delta check so the root-entry length ceiling is gated rather than eyeballed (the mechanization §1.18 deferred; a candidate P3 follow-up). |
| 2026-07-24 | 1109 | §1.18 CLOSED (the change-impact surface map) + overnight-run setup | The mechanical §1.18 close ran clean (the section + body + MEG-29 index row deleted, the DONE rotation entry added, the P1-intro three-to-two count update, the §1.1 date breadcrumb reconciled), and the overnight-authorization + lease records were well-formed. The offloaded post-merge validate-pr (worker-b) then caught two closure-sweep misses the mechanical close did not. | The §1.18 close swept the item's OWN section but missed a dangling `1.18` dependency in another live item (4.31's Depends-on) and the origin cites in the CLAUDE.md change-impact map; both pre-existing, surfaced by the offloaded validate-pr and hot-fixed in #1110. | A section-close reference sweep that scopes only the closed item's own section misses OTHER items' Depends-on/Blocks lines and prose origin cites naming the closed number; this is the cross-FILE §N-orphan class the close-out checklist warns about, recurring at a NEW carrier (another item's dependency line). | When closing a numbered backlog item, grep the WHOLE repo for the bare item number including Depends-on/Blocks lines and prose origin cites (not only the item's own `§N` heading), and reconcile each in the closing PR; a cheap mechanical guard would be a closed-item back-reference check in the closure PR. |
| 2026-07-24 | 1108 | §1.1 CLOSED (the discussion-vs-execution gate shipped as the fifteenth pack rule) | The 15th-rule build ran the full offloaded-draft-plus-verify loop over a 20-surface change cleanly: worker-a drafted from the design seed using this session's own change-impact map (§1.18) as the surface checklist; the orchestrator re-verified every surface, authored the rule prose in project voice, and shipped guard-first; the pre-push skeptical verifier (worker-b) SHIP'd (8 targets, byte-diffed trees, ran 74/74) with one trivial finding. The change-impact map made the 20-surface wiring tractable and complete (all four enumeration surfaces, the gate-74 row, both web templates, provenance, the count carriers). | The UTC date rolled over to 2026-07-24 mid-build, so the pre-push guard's D4 forced every Version-bumped file's metadata Date to 2026-07-24 (a fail-then-fix loop caught pre-push); the pre-push verifier's F1 (a one-day breadcrumb) and the post-merge validate-pr's LOW (an omitted verifier note) were both trivial cosmetic items. Also a wrong-repo `gh pr merge` slip (ran in the scratch cwd) was caught immediately and redone with `--repo`. | The multi-surface new-rule change is now a well-worn pattern (the 14th and 15th rules both shipped clean via the change-impact map as the checklist); the midnight-UTC-rollover D4 cascade is a recurring apply-time gotcha for any PR spanning midnight; and the sibling-repo command-cwd slip recurred (all caught, no merged defect). | When a session spans midnight UTC, set new date-bearing fields to the commit's UTC date up front rather than carrying the session-start date into a post-midnight PR's metadata (pre-empts the D4 loop); and continue using `--repo` / `git -C` / absolute paths for every sibling-repo command (the standing fix for the recurring cwd slip). | 
| 2026-07-23 | 1107 | none (§1.18 PR-2, gate 74; pre-merge verifier found 1 LOW, fixed) | The gate-74 build ran the full guard-first loop cleanly end to end: the offloaded draft (worker-a) built on the design seed; the orchestrator re-verified every convention at source (`lint_common.REPO_ROOT`, `run_linter`/`LinterTestCase`, the `--root` convention, the four wiring anchors) and independently ran the gate against the live README (one MISSING before the row, green after) before authoring; the pre-push skeptical verifier (worker-b) then SHIP'd with one LOW (a docstring overclaim plus an over-strict indented-row edge), validated at source and fixed + re-verified; the post-merge validate-pr confirmed the fix effective. The change-impact map this session shipped (§1.18) served as the multi-surface checklist. | The pre-push guard caught two real apply-time misses before push (the spec Version bump when the spec body changed, and the resulting taxonomy/scorecard regen) plus the F1 fix could not be force-pushed (harness-denied), handled cleanly as a soft-reset-plus-follow-up-commit rather than an amend. | The version-bump-and-regen discipline and the fix-as-follow-up-commit (when force-push is denied) are recurring apply-time patterns, both handled without a merged defect. | None new; the guard-first + offloaded-draft + skeptical-verify loop closed cleanly, and the force-push-denied workaround (soft-reset to the pushed head, commit the fix as a fast-forward follow-up) is the standing approach for a post-push fix in this harness. |
| 2026-07-23 | 1106 | none (2026-07-23b resume close-out; offloaded validate-pr found 1 LOW, self-resolving) | The resume ran cleanly through the loop-break control: Sweep 119 offloaded to worker-b (CLEAN 0/0/0), consumed under elevated-QA with an independent baseline re-run (73/73 matched) and the one dismissed positive cross-checked against the recorded §3.68b decision; the handoff prune (keep current+1-prior across three stacks) was boundary-verified by the offloaded validate-pr, and both workers were kept fed (the §1.18 PR-2 gate draft and the P3 survey were offloaded during the CI/validate waits). | A recurring MECHANICAL command-construction slip: the sibling-repo (`grc_library_scratch`) commands kept arriving WITHOUT the intended `cd <repo>` prefix, tripping the wrong-repo and repeated-failure guards several times before I switched structure. It was a read-back lapse (the `cd` lived in my description/reasoning but not the literal command string), not degradation (fresh session, 0 compactions). | Sibling-repo tool/git invocation from the wrong cwd is the same class the 2026-07-19 retros logged; the durable fix is structural, not attentional. | ADOPTED this session: for every sibling-repo operation use `git -C <abs-path>` for git and an absolute script path for a repo-rooted tool (e.g. `python3 /abs/scratch/tools/validate.py`, which `os.chdir`-es to its own root), NEVER a bare `cd`-prefixed command; this sidesteps both the cwd-reset and the wrong-repo guard entirely. Worth considering as a standing worker-brief / CLAUDE.md rail if it recurs. |
| 2026-07-23 | 1104 | none (§1.18 PR-1; verifier found 1 MEDIUM, fixed) | Consuming a worker draft-seed for a multi-surface convention PR worked well: the worker delivered the high-value website-surface inventory (the Q2 half) + candidate map/checklist prose, and the orchestrator re-verified every inventoried surface at source (the pack.html dual-occurrence, the counts, gate-64's actual coverage) before authoring, catching that gate-64 already covers what I'd first proposed as PR-2's target and re-picking the pack-README rule-scope table (a surface that had DEMONSTRABLY drifted, the 14th rule's row missing). The pre-push verifier then caught a type-C over-attribution ("three enumeration surfaces" copied into the skill row where gate 41 checks one), validated at source and fixed. | The map's own accuracy invariant ("name the authority gate for each surface") is exactly the kind of claim a verifier must check cell-by-cell; a copied phrase (three-surfaces) slipped into a row where it was false. | When authoring a table whose whole purpose is accurate per-cell attribution, verify EACH cell against the authority independently, not by pattern-copying a sibling row; and re-verify a proposed gate target's ungated status at source (gate-64 already covered my first candidate). Both caught pre-merge (verifier + the research-assistant re-verify), the layers working. | None new; the verify-each-cell-against-its-authority discipline is the durable takeaway. |
| 2026-07-23 | 1103 | none (two decided P3 closes; offloaded validate-pr found 1 LOW) | The batch-answer-at-boundary pattern worked: two low-stakes pending decisions (3.19, 3.32) surfaced together via AskUserQuestion when the maintainer was present, both taking the recommended option, then closed in one coherent PR. The offloaded validate-pr then caught a stale `.gitattributes` §3.19 pointer the in-PR §N-orphan grep had missed. | The §N-orphan close-out grep (run in #1103) missed `.gitattributes` because the grep's file-type globs were `.md` / `.py` / `.html` / `.yml` / `.json` / `.sh` and `.gitattributes` is an EXTENSIONLESS dotfile none of them match; a closed-section pointer can live in any tracked text file. | Widen the §N-orphan close-out grep to extensionless dotfiles (`.gitattributes`, `.gitignore`) and config files, not just the extension-globbed set. This pairs with the existing file-type-width close-out guard (which added non-`.md` types); the residual was dotfiles. Caught by the offloaded validate-pr, the verification layer working. | None new; the widen-to-dotfiles refinement extends the file-type-width guard. |
| 2026-07-23 | 1102 | none (Task-1 reconciliation close; verifier found 0 defects) | The #1101 retro lesson (verify each copied boilerplate clause against THIS change's scope) demonstrably held: the #1102 pre-push verifier found ZERO defects and the offloaded validate-pr confirmed no fabricated "also-updated-X" claim, versus #1101 where the same verifier caught three. Authoring GAP-1, F1, and the coupling clause as clauses in existing rules (not a new rule) kept the rule count stable and the change small and reviewable. | None material; the FUTURE-tail migration took a judgment call (migrate the 1.14 pointer explicitly; let the general coupling convention carry the 1.18 half), which the offloaded validate-pr flagged as an informational note and confirmed intentional. | Applying the immediately-prior PR's retro lesson at the next PR's authoring prevents recurrence: the verify-each-copied-clause discipline turned a 3-defect PR into a 0-defect one. And declining F2 (surface-counterproductive-instructions) is a legitimate terminal disposition of a listed sub-item when source inspection shows the instruction would degrade the artefact. | None new; applying-the-last-retro-lesson is itself the durable pattern. |
| 2026-07-23 | 1101 | none (new 14th pack rule; the verifier caught 3 authoring defects pre-merge) | The multi-surface wiring for a new rule went cleanly via the surface checklist (gates 37/41/39 as the mechanical net), and the pre-push skeptical verifier earned its cost: it caught 3 real defects (a dangling forward-reference to not-yet-shipped GAP-1 content in the rule body, plus a fabricated "new circuit-breaker clause" and a fabricated guardrail-review-count edit in the README version-history row) that the mechanical gates could not see. All were validated at source and fixed before merge. | The 3 defects shared one root cause: copying a prior new-rule PR's version-history and wiring boilerplate without checking each clause applied to THIS change (the prior rule DID update the guardrail-review count and DID pair with a sibling; this one does neither). | When copying a prior PR's boilerplate wiring or prose, re-verify each carried clause against THIS change's actual scope: a copied clause is a hypothesis, not a fact. The catch confirms the substantive-tier pre-push verifier is load-bearing for a multi-surface rule addition, not optional. | None new; the verify-each-copied-clause pattern is the durable takeaway. |
| 2026-07-23 | 1100 | none (P2 umbrella-series authoring, recording only) | The redirect-stub pattern from #1099 paid off immediately: three existing P2 items folded into series children with zero number reassignment, and the offloaded validate-pr confirmed all four live cross-references were repointed with no stray citer. Authoring the whole two-series structure plus P4.31 and the Task-1 reconciliation item in one coherent recording-only PR (per PR-splitting calibration: one coherent on-topic task = one PR even across many surfaces) kept the backlog restructure atomic and reviewable. | None material; a recording-only backlog PR, the offloaded QA was clean. | A backlog restructure is safest done as one atomic recording-only PR with no corpus/gate/pack change riding along, so the reference sweep and the counter advances are verified as a single unit rather than spread across PRs. | None new; the atomic-restructure-then-execute sequencing is the durable takeaway. |
| 2026-07-23 | 1099 | none (two conventions ahead of the P2-series authoring) | The redirect-stub numbering pattern was the clean synthesis of the maintainer's two competing goals (in-order series vs never-break-references): rather than renumber (which the 2026-07-15 rule forbids) or scatter series members (confusing), an item keeps its number as a forward redirect while content moves, so no number is reassigned and missed references still resolve. Both numbering surfaces were updated together (multi-surface consistency), and the pack-parity coupling was recorded as convention + cadence + deferred-gate (mirroring the L1/L2-now, L3-deferred shape of the saturation guard). | None material; a clean recording-only convention PR (D8 length ceiling caught the first root-entry draft at 110 words, trimmed to under 100 pre-push). | When a maintainer has two goals in apparent tension (here: self-contained series vs reference integrity), the best answer is often a third option that satisfies both (a forward redirect preserves the number AND lets content move), not a tradeoff between them. Surfaced by iterating the numbering approach with the maintainer rather than picking one horn. | None new; the surface-a-third-option-when-two-goals-conflict pattern is the durable takeaway. |
| 2026-07-23 | 1098 | none (worker-saturation guard rail, maintainer-directed) | The guard rail directly answered a maintainer-caught failure (idle workers) with the right shape: an OBSERVABLE (a PreToolUse hook cannot intercept the inaction of not-dispatching, so a block is the wrong tool) plus a narrow boundary checkpoint, L1+L2 now with L3 deferred behind a one-month TF review. The worker-drafted tool was verified line-for-line before applying, the two lint_common helpers confirmed exported, the liveness constants confirmed VERBATIM against credit-offload-queue.py, 12/12 self-test wired into gate 36, and its own live run caught the fleet at IDLE-CAPACITY mid-build (the observable proving itself). The new TODO time-bounded-follow-ups section gives date-gated revisits a home /resume surfaces. | The preflight caught mis-pathed `.claude/` links in the detailed entry (`../CLAUDE.md` instead of `../../.claude/CLAUDE.md`), a recurring detailed-mirror relative-path slip; fixed pre-commit. Several lint commands hit the pipe-guard (piped to tail); re-run unpiped. Both caught by the verification layer, not escapes. | Detailed-mirror links to `.claude/` files need the `../../.claude/` prefix (the mirror sits two levels down under `.working/changelog-details/`); the reflexive `../<name>` is wrong. The observable-not-block insight generalizes: a failure of INACTION cannot be caught by a PreToolUse hook (which intercepts actions), so its guard must be a visible signal plus a checkpoint, not a block. | None new; the detailed-mirror `.claude/`-link-depth slip is a known preflight-caught class, and the observable-not-block principle is now recorded in the new pack-agnostic-worthy CLAUDE.md checkpoint section. |
| 2026-07-23 | 1097 | §3.68 (vuln-remediation-SLA divergent carriers) | The full substantive-tier pipeline ran clean: worker research offloaded, then every quoted line / line number / current Version re-verified against the LIVE files before authoring; a refute-briefed skeptical verifier returned SHIP on all 8 axes; the answered-question guardrail correctly resolved that §3.68 WAS decided (the TODO prose predated the recorded pending-decisions), so I acted on the record instead of re-asking. The 3.68a residual (Critical accelerated tiers / Low) was surfaced honestly rather than silently expanding scope past the maintainer's value-tighten decision. | The recurring preflight bare-path-span friction on the detailed entry (three code-spans: the SoT path, the scratch result path, a portal path); reworded (link the in-repo one, de-path the cross-repo/illustrative ones). Same class every corpus PR. | The answered-question guardrail earned its keep: a stale TODO prose ("maintainer decides per row") that PREDATES a recorded decision would have driven a needless re-ask; decisions-search surfaced the actual decision. Check the decision store, not just the TODO prose, before treating an item as undecided. | None new. The preflight bare-path friction is well-understood (reword at authoring); a possible ease would be a detailed-mirror authoring habit of linking/de-pathing spans on first write, but the preflight catch is cheap and reliable. |
| 2026-07-23 | 1096 | none (maintainer-directed CI-action pin refresh; supersedes Dependabot #1092/#1093) | The maintainer's "review, proceed if quality confirmed" was executed as a real review, not a rubber-stamp: the new SHAs were resolved against the upstream actions/* GitHub API (both = current latest release, supply-chain-clean), the v4->v7 / v5->v7 major bumps were confirmed CI-compatible (the Dependabot PRs' own web-generator-health job ran green on them; #1096's own Lint job then confirmed the full gate), and a real defect was caught (Dependabot #1093 kept a stale `# v4` comment on a v7.0.1 pin). Rather than --admin-override the real D1 CHANGELOG gate the bot PRs fail, the refresh shipped as a disciplined PR with the correct comment. | The pin-bump sed used `#` as the s-command delimiter, which collided with the `# v7.0.1` comment content (sed error); switched to `|` (absent from SHAs/comments). A one-shot self-inflicted slip, caught immediately. | Verifying an automated dependency PR is a real review task: resolve the proposed SHA against the upstream repo's tags (authenticated API, no egress worry for github.com), confirm it is the intended version AND the comment matches, and check CI actually exercised it, before trusting a bot's title. A bot PR that fails a house gate (here D1) is re-done disciplined, not force-merged. | None new; the choose-a-delimiter-absent-from-the-payload sed habit and the resolve-SHA-against-upstream-tags review step are the durable takeaways. |
| 2026-07-23 | 1095 | none (maintainer-directed: add adopt to the site skills list, 23->24) | The adopt-gap surfaced by #1094 was resolved cleanly: surfaced as an AskUserQuestion (not auto-added, since it was an authorial call), the maintainer chose "add", and the follow-up shipped same-session. The count-correction was done at full multi-surface width: a bare-token grep found not just the section-04 header but three more "23 skills" carriers (meta description, sidebar CSS comment, landing CTA), all fixed, 0 residual in templates. Placement handled honestly: adopt fit no existing purpose-group, so a plain new "Fork onboarding" group (not the declined italic set-apart) kept the "grouped by purpose" integrity, with the choice surfaced in the PR for redirect. Offloaded validate-pr, CLEAN. | None material. The number verified at PR-create matched (#1095), avoiding the #1094 rework. | Count corrections span multiple carriers: the visible header is rarely the only one. The bare-token multi-surface grep (meta tags, CSS comments, sibling templates) is what caught the other three "23 skills" carriers a header-only fix would have left stale. | None new; the multi-surface count-correction grep and the surface-authorial-calls-not-auto-decide patterns are already codified. |
| 2026-07-23 | 1094 | §3.78 (website: skill links target SKILL.md, not the bare directory) | A mechanically-verifiable retarget shipped clean: 46 links (23 skills x2 blocks) retargeted by scoped substitution, 0 residual single-skill tree links, the 2 pack-root links left by design, all 23 SKILL.md targets confirmed present, build --check green. Post-merge validate-pr offloaded. The adopt-gap (site 23 vs pack 24) was surfaced to the maintainer as an AskUserQuestion rather than auto-added (an authorial call), and the answer set up the immediate follow-up. | Two. (1) PR-NUMBER MISMATCH: I authored all bookkeeping as "#1092" assuming sequential numbering after #1091, but GitHub assigned #1094; #1092/#1093 had been consumed by the two Dependabot auto-update PRs my own #1091 dependabot.yml opened. Caught at PR-create; corrected via a follow-up commit (force-push was denied by the harness, so a normal fixup commit on top). (2) COMMAND-STRING SLIP: I hit the wrong-repo hook 5x running the scratch validate.py, each time editing the tool-call DESCRIPTION while leaving the command STRING as bare `python3 tools/validate.py` (no cd/absolute prefix); the circuit-breaker fired; the absolute-path form cleared it. | (1) PR numbers are NOT guaranteed sequential when repo automation (Dependabot, bots) can consume intervening numbers; do not bake an assumed number into bookkeeping before the PR exists. (2) The intent-vs-artefact gap the read-back discipline names is real and recurred: I "intended" a cd prefix but the emitted command string lacked it, repeatedly, because I edited the description not the command. | (1) Consider creating the PR first (or reserving the number) before authoring number-bearing bookkeeping, OR write the number as a placeholder and fill it post-create; at minimum, verify the assigned number immediately at create and grep-correct. (2) When a wrong-repo/pipe hook blocks a command, READ the literal command string and change the STRING (the description is not the command); prefer `git -C <path>` and absolute script paths for sibling-repo tools to avoid the cwd class entirely. |
| 2026-07-23 | 1091 | §3.39 (Dependabot for the SHA-pinned CI actions) | A small decided item closed cleanly and confirmed end-to-end: the worker's post-merge sweep observed Dependabot had ALREADY parsed the new `dependabot.yml` and opened its first two auto-update branches (checkout, setup-python), so the config's correctness is proven by the live service, not just by shape-checking. The MEG-34 index row was removed with a head-count-0 + orphan-grep confirmation. Offloaded validate-pr, CLEAN. | The preflight bare-path-span check flagged the new `.github/dependabot.yml` path in the CHANGELOG line (recurring on any new-file PR); reworded to non-path prose. No other friction. | Adding a config for an external automation is verifiable at source: the service itself parsing the file (Dependabot opening auto-update branches) is stronger evidence than a shape check. When closing a config-add item, look for that live-service confirmation. | None new; the preflight bare-path-span friction on new-file PRs is already tracked (the recurring reword). |
| 2026-07-23 | 1090 | none (anti-false-completeness guardrail L3-4, rule + convention) | Completed the 4-layer guardrail: `evidence-grounded-completion` (both trees) now names the set-completeness-is-completion-class and asymmetric-skepticism principles, plus the CLAUDE.md backlog-status convention (with the `_private`-placement clause, maintainer-directed). Offloaded draft seed (worker-a); the orchestrator authored the final prose. The stack mixes mechanical enforcement (L1 tool + L2 hook) with sharpened discipline (L3-4 rule + convention), the right shape when a loaded prose rule was violated. | None; a clean additive rule/convention change. | When a prose rule is violated despite being loaded, the durable fix is MECHANICAL (a tool + a hook that enforce it) PLUS a sharpened named principle, not another paragraph alone. The four-layer pattern (enumerate, block, name-the-principle, project-convention) is reusable for other loaded-but-violated disciplines. | None new; the mechanical-plus-sharpened-rule response to a loaded-but-violated discipline is the durable pattern. |
| 2026-07-23 | 1089 | none (anti-false-completeness guardrail L2, the decision-hook) | The skeptical verifier EARNED its cost: it caught a real Medium false-positive (the exhaustion guard keyed on the loose "blocked" substring without checking the entry was a hold, so it would over-block a legitimate ACT entry reviewing "all 92 items; none is blocked", the exact positive review the control exists to encourage). Fixed in-window by gating on the BLOCKED classification, plus 2 FP-guard regression cases. The hook's item-count regex was aligned byte-for-byte to the layer-1 tool with a drift-proof parity test, and the `_private` validate marker-set parity was confirmed by reading it. | The worker's draft used a loose substring trigger (matching the brief's literal firing condition, but too broad); the false-positive surfaced only under adversarial verification, not the worker's own self-test. | A guard that keys on a text-substring signal ("blocked") over-fires on negation and incidental prose; gate a decision-guard on the STRUCTURED field (the Classification), not a loose substring. A paired tool + hook needs a drift-proof parity test, not merely aligned-at-apply regexes. | None new; classification-gated (not substring-gated) triggers and a mechanical parity test between a coordinated tool + hook pair are the durable patterns. |
| 2026-07-23 | 1088 | none (anti-false-completeness guardrail L1) | The corrective control for the worst failure this session: a false "every remaining backlog item is blocked" claim, made from a PARTIAL review, used to justify stopping the unattended run. Layer 1 is an advisory tool that enumerates all 92 open items and flags the 49 with no blocker token, so an all-blocked claim must confront the full list. The tool's own live run (92 total / 43 blocker-signal / 49 presumed-actionable) is the concrete refutation of the false claim I had made. Offloaded draft (worker-b); the orchestrator re-ran it, spot-checked the 49-item list, and fixed an id-parse quirk. | The maintainer had to catch the false claim; the existing `evidence-grounded-completion` inventory-corollary rule was ALREADY loaded and was violated, which is precisely why the fix is mechanical (a tool + a hook), not another paragraph of prose. | A set-completeness claim ("all / none / every X") over a collection that gates a STOP is a completion-class claim requiring the full enumeration; and the evidence bar to STOP must exceed the bar to CONTINUE, because stopping is the self-serving direction. Being codified mechanically (layers 1-2: tool + count-match hook) and in the rule (layer 3). | The four-layer guardrail (enumeration tool + count-match decision-hook + evidence-grounded-completion rule + CLAUDE.md convention) is the durable mechanism; this is layer 1. |
| 2026-07-23 | 1087 | none (§3.99 gate-35 additive hardening) | The offloaded draft (worker-a) recommended EXTENDING gate 35 rather than adding a new gate (avoiding the gate-count ripple) and handled the subtle `--check` discriminator false-positive (the regen hook shares the build scripts with the `--check` §6 gates); the orchestrator re-read the source and skeptical-verified (0 defects across 8 refute vectors). The additive-only design gives a safe blast radius (a bug reddens the author's own CI or is a no-op, never a masked real gate). | The `(TODO 3.99)` marker in the §6 narrative clause tripped gate 9 (mandatory-requirement-near-uncertainty) plus gate 69 (positional-backlog-token); caught by the pre-push guard and removed before merge. | A §6 narrative clause that cites a TODO `§`-number trips gate 69 (positional token) and can trip gate 9 (uncertainty marker); in gate-scanned corpus prose cite the SHIPPED mechanism, not the TODO `§`-number (the same recycled/positional-`§` lesson as #1085). | None new; additive-gate-extension and cite-shipped-not-TODO-`§` are stable patterns. |
| 2026-07-23 | 1086 | none (decision-cluster surfacing) | Formalized the three decision-blocked items into `pending-decisions.md` (beyond the next-prs catalogue) so `decisions-search.py` indexes them and `/resume` surfaces them for a batch answer, maximizing the maintainer's wake-time throughput (one answer each unblocks a downstream item). A correct clarify-before-acting move under unattended conditions: surface the authorial decisions, do not guess a default. | None. | When the clean quick-clears are drained and the remainder is decision-blocked, surfacing the decisions into the decisions-search-indexed queue (not only next-prs) is higher-leverage than grinding, since it converts the maintainer's wake-time into several unblocks at once. | None new; pre-loading decisions into `pending-decisions.md` is the established "pre-load decisions per merge" discipline. |
| 2026-07-23 | 1085 | none (§3.22 D7-handoff-snapshot rotation close) | A resolved-but-unrotated close: §3.22's defect was ALREADY fixed by #1075/#1076 (the same defect keyed as §3.89/§3.101), so verifying that (the tests cover the stale-token and non-vacuity cases on the new marker; no CLAUDE.md D7 note exists so the deferred "protected part" was moot) turned it into a clean bookkeeping rotation with no new content risk. A recycled-section-number cleanup (a former §3.22 = PCI DSS #649, cited in a test comment) was de-ambiguated by citing the stable PR number. | None; the D7 fix and its tests were already in place, so this was a verify-then-rotate. | Recycled TODO section numbers create stale/ambiguous lineage references: in DURABLE code comments cite the STABLE PR number, not the recycled `§N`, so a later reuse of the `§N` slot does not make the comment misleading (the #649-vs-D7 §3.22 collision this PR resolved). | None new; citing PR numbers over recycled `§`-numbers in durable code comments is the stable de-ambiguation. |
| 2026-07-23 | 1084 | none (§3.34 detailed-mirror full-link-resolution scan) | The offloaded draft (worker-a) reused the PR #934 resolver cleanly via a new backward-compatible `root` parameter (no divergent reimplementation), and the orchestrator's mirror-clean measurement (0/142 dangling) made the item's historical-fix half moot, narrowing the work to the scan-enablement. A skeptical pre-push verifier confirmed no correctness defect across 7 refute vectors and caught one LOW cosmetic double-report, fixed in-window at the source (the added-line resolution sub-check now skips the mirror, which the full-mirror pass covers in full). | A GitHub-wide Actions incident (Actions `partial_outage`, webhooks degraded) blocked #1084's CI for ~70 min: the push and PR triggered NO workflow run at all. Diagnosed via `gh run list` (zero repo-wide runs) plus the GitHub status API, logged to `third-party-issues`, held (did NOT `--admin`-merge past the absent required check), re-triggered on a bounded recovery watch, and merged the moment Actions recovered. | A CI-provider trigger outage presents as "no checks reported" AND zero repo-wide runs in `gh run list`; the diagnostic pair is `gh run list` + the provider status API, and the correct response is a re-trigger plus a bounded recovery watch, never an `--admin`-merge past a never-run required check (the local pre-push guard is equivalent proof, but forcing a batch past absent CI unattended erodes the backstop). | None new; the confirm-via-status-API, log, hold-don't-bypass, re-trigger-and-watch response is the stable pattern for a CI-provider outage. |
| 2026-07-23 | 1083 | none (§3.86 CCPA $25M CPI-adjustment caveat) | The offloaded source-grounded census (worker-a) correctly narrowed a broadly-framed item to its true scope: the §3.86 block anticipated a corpus-wide sweep of CCPA thresholds, but the census plus the held-statute read found the CPI adjustment applies to the $25M MONETARY figure ONLY (Cal. Civ. Code §1798.199.95(d)(1)), so exactly one carrier received the caveat and the record-count triggers were correctly left alone. The orchestrator re-read the held statute (non-circular) before applying; a skeptical verifier and the post-merge validate-pr both re-confirmed the scope. | The dispatch order mis-cited the adjustment authority (§1798.185(a)(5) rather than §1798.199.95(d)(1)); worker-a caught it against the held statute, and the TODO block had it right. A one-carrier result where the item's framing implied many. | Source-grounding a normative caveat against the HELD statute (not a corpus restatement) is what keeps the scope honest: the statute's own CPI-adjust enumeration is the authority on WHICH thresholds are adjusted, so the caveat lands exactly where the source supports it and nowhere else. | None new; the offloaded-census plus held-source-scoping plus orchestrator-re-read pattern is the stable shape for a source-grounded normative-caveat addition. |
| 2026-07-23 | 1082 | none (§3.71 corpus-wide GRC-Manager rename) | The corpus-wide rename (60 occurrences / 5 docs) landed cleanly via the offloaded-census-then-deterministic-apply pattern: worker-a produced the full before/after census (per the worker-brief corpus-wide-rename override) + the plural->title->lowercase substitution order + a zero-residual proof, and the orchestrator applied it as an ordered `str.replace`, asserting the replacement count equalled the census (exactly 60) with zero residual (5 files + full corpus). A skeptical verifier and the post-merge `validate-pr` both re-derived the count (incl. the capa case-sensitive-vs-insensitive reconciliation) and confirmed the distinct roles + anchors. | None. A clean corpus-wide rename. | For a corpus-wide rename: applying via an ORDERED `str.replace` (longest/plural forms first) with a count-equals-census assertion + a full-corpus zero-residual grep is the deterministic, self-verifying apply shape; the offloaded census supplies the before/after and the count to assert against, and the count-match is what proves neither a drop nor a duplicate. | None new; the offloaded-census + ordered-replace + count-assert + zero-residual pattern is the stable shape for a corpus-wide rename. |
| 2026-07-23 | 1081 | none (§3.103 decision-log deferral-keyword widening) | The cross-repo lock-step change (hook + mirrored `_private` validate, exact parity) landed cleanly: both tuples widened identically, the hook self-test proves the widened set blocks a synonym deferral, and the CLAUDE.md §N-orphan cleanup removed the stale forward pointer in the same PR. The offloaded post-merge `validate-pr` correctly verified only the grc_library half + the CLAUDE.md parity assertion (it cannot see `_private`), and flagged that the orchestrator must confirm the `_private` byte-parity. | None. The `_private`-invisibility to the QA worker is a known structural boundary, handled by the orchestrator confirming the byte-identical `_private` tuple + running `_private` validate at apply. | For a cross-repo parity change, the QA worker verifies the visible (grc_library) half and that the public prose ASSERTS the parity; the orchestrator (the only actor who can read `_private`) confirms the non-visible half is byte-identical and its validate passes. This division keeps the offloaded QA honest about what it can and cannot see. | None new; the visible-half-worker + private-half-orchestrator split is the stable shape for a cross-repo parity change. |
| 2026-07-23 | 1080 | none (§3.11 gate-66 default-population close-out clause) | The offloaded investigation+draft (worker-a) handled a DECISION-carrying quick-clear well: it identified gate 66 exactly (the Unbalanced-fence audit), confirmed the concern against the tool's own docstring, evaluated both §3.11 options, and recommended the conservative one (a checklist clause over a gate-code default-walk widening) with sound reasoning; the orchestrator verified gate 66's identity at source before applying. Applied the very discipline being documented (ran both audits on the edited `.claude/CLAUDE.md`). Offloaded post-merge `validate-pr` confirmed the clause matches the tool, 0 findings. | None. A clean decision-carrying convention clear. | The offloaded-INVESTIGATION pattern (worker evaluates the named options + recommends with reasoning, orchestrator adjudicates + verifies at source) works for a quick-clear that carries a design choice, not just a mechanical edit; it keeps the choice evidence-based while staying offloaded. | None new; this investigate-then-recommend offload shape is stable for option-carrying quick-clears. |
| 2026-07-23 | 1079 | none (§3.23 new-annex discoverability override) | A clean offloaded convention-doc add pinned to the current main tip (the #1078 pin-to-main lesson applied; the SHA resolved without the branch-head-unreachable problem). The worker confirmed the order framing matched the actual §3.23 block (no mis-framing this time) and verified the cited decision-tree surfaces exist; the orchestrator re-verified the section numbers live before applying. The offloaded post-merge `validate-pr` confirmed fence balance + the cross-references, 0 findings. | One self-caught pre-push: my first root CHANGELOG entry exceeded D8's length ceiling (a >45-word sentence); compressed and re-verified. | The pin-to-main lesson from #1078 held (no unreachable-SHA issue on #1079). D8's length ceiling bites a dense one-sentence root entry; keep the root summary to short sentences and push detail to the mirror. | None new; both are working backstops (D8 caught the length; the pin-to-main habit is adopted). |
| 2026-07-23 | 1078 | none (§3.35 worker-brief path-resolution fixture rail) | The offloaded draft (worker-a) demonstrated the research-assistant discipline at its best: the dispatch order MIS-FRAMED §3.35 (as link-resolution-coverage), the worker read the ACTUAL §3.35 TODO block, corrected it to the CONFIGURED-PATHS-EXIST (F6) intent, and flagged the divergence, which the orchestrator verified against TODO.md before applying. The worker also caught that the order pinned an unreachable pre-squash branch-head SHA. | Two caught, both by the layered machinery: (1) the order pinned the pre-squash PR branch-head SHA (`a9b583e0`), unreachable after the squash-merge deleted the branch; the worker ran against the content-equivalent main tip and flagged it (a LOW watchdog alert). (2) the pre-push guard's D2 caught that I edited `.working/worker-brief-template.md` (a version-tracked file) without co-bumping its Version; fixed before push. | (1) SHA-pinning: pin offloaded orders to the SQUASH-MERGE / current-main SHA, not the in-flight PR branch-head SHA (it becomes unfetchable once the PR squashes and the branch deletes). Adopted for subsequent orders this session (§3.23 was pinned to main). (2) `.working/worker-brief-template.md` carries a `**Version:**` field, so editing it triggers the D2/D4 version-bump checks; treat it as a versioned document, not a free `.working/` doc. | (1) The pin-to-main habit is adopted; a mechanical order-SHA validator is out of scope for a quick-clear. (2) The D2 check is the working backstop for the worker-brief-version-bump; the orchestrator now co-bumps it by reflex. Both convention-level. |
| 2026-07-23 | 1077 | none (§3.96 test-isolation convention, a tests/README.md section) | A clean offloaded convention-doc add: the worker surveyed the full 8000-line `tests/test_linters.py` for shared-global patches, verified every one already restores (zero live leaks), drafted the exact section prose to the right home (a new `## Global-state isolation` after `## Fixture isolation`), and the orchestrator applied it verbatim after re-reading. The offloaded post-merge `validate-pr` confirmed placement + house style, 0 findings. | None. A smooth XS convention-doc PR. | The worker-surveys-then-drafts-then-orchestrator-applies pattern is the efficient stable shape for an XS convention item: the survey (zero live leaks) both justified the convention and confirmed no in-scope fix was needed, so the change stayed a disjoint doc add. | None new; the offloaded-convention-doc pattern is stable. |
| 2026-07-23 | 1076 | none (§3.89 + §3.101 D7 handoff-snapshot check made non-vacuous) | The offloaded draft (worker-a) fixed the inert D7 check at the CLASS level, not just the instance: beyond re-pointing the marker to the live token line, it added a non-vacuity guard so a located-but-token-less snapshot line fails loud, and corrected the fixtures to the real layout (regressing the bug) plus a new token-less test. The offloaded post-merge `validate-pr` (worker-b) PROVED the guard non-vacuous by a direct pure-core call, not just re-confirming a pass. Clean, no findings. | None new (the #1075-W1 docstring rider rode in cleanly; the D7 fix applied and verified first-try). | Fixing an INERT/silent check: re-pointing it at the right input is necessary but not sufficient, because the silent-pass CLASS survives (a future layout drift re-inerts it); add a fail-loud guard on the silent condition itself (here `checked == 0` -> non-vacuity failure) so the class cannot recur, and prove the guard fires (a direct test on the silent condition), not merely that the check now passes. | None new; the fix-the-class-not-the-instance + prove-the-guard-fires pattern is the reusable lesson, already the shape the high-assurance and guard-first disciplines encode. |
| 2026-07-23 | 1075 | none (§3.77 CHANGELOG link gates recognize .html/.css/.js) | The offloaded 3-line detection-widening candidate (worker-b) applied cleanly and self-verified (fixtures 3/3, widened gate clean on the live corpus, 73/73); the offloaded post-merge `validate-pr` (worker-a, independent) confirmed tuple parity + FP-safety and surfaced one real in-window warning. | Two catches. (1) POST-MERGE W1: the §3.77 item explicitly named the gate module DOCSTRING a paired surface, but I widened the `FILE_EXTENSIONS` tuple + the fixture and left the docstring's extension enumeration at the old 8-set; the offloaded `validate-pr` caught it, fixed in #1076. (2) PRE-PUSH: a combined `cd <scratch> && ... ; git checkout -b <grc_library-branch>` bash block ran the branch-creation in the SCRATCH repo (the `cd` carried over), so #1075 was committed to grc_library `main`; caught loud by the push refspec error, cleanly recovered (branch created at the commit, main reset to origin, nothing bad pushed). | (1) Paired-surface completeness: when a change names its own paired surfaces (here "docstring + fixture"), tick EACH off; a tuple-plus-fixture edit that skips the docstring enumeration is the update-one-of-a-pair miss, and no gate scans a docstring's prose enumeration. (2) Cross-repo write-safety: NEVER combine a sibling-repo `cd` with a grc_library git-state command in one bash block; use an explicit `git -C <path>` per repo (or separate blocks), because the `cd` carries into the later command. | (1) The paired-surface close-out line already covers this; the offloaded `validate-pr` is the working backstop (it caught W1). (2) Reinforce the explicit-`git -C`-per-repo habit for sibling-repo commands; the push-refspec error is the working backstop. Both convention-level. |
| 2026-07-23 | 1074 | none (§1.16 COBIT objective title-text gate 73 + 41-carrier backfill, guard-first) | The sensitive-tier guard-first apply landed as one coherent PR: the new gate + four-surface wiring + fixture + the deterministic `git apply` backfill (41 carriers / 32 docs) + the 72->73 count migration + the guardrail-review r12 cadence reset. The gate self-verified the backfill (0 findings post-apply), and a fresh non-author pre-push skeptical verifier held-source-checked 18 titles against `grc_library_ref` (breaking the gate<->reference<->backfill circle), confirming even the subtle APO01 "I&T" vs BAI07 "IT" distinction. | The pre-push verifier caught a real Medium: `governance/specification-audit-programme.md` was edited for the gate-73 wiring (§6 row + prose) but its OWN `Version`/`Date` were not co-bumped, which the pre-push guard's D2/D4 would have failed on. All 32 backfill docs were co-bumped; the gate-spec doc I also edited was the one dropped, because attention was on the 32-doc backfill set. | When a PR edits a gate-spec / governance doc for gate WIRING (not only the data docs), that spec doc is itself a versioned corpus doc needing its own Version+Date co-bump; the attention on a large backfill set masks the single wiring-doc edit. | The audit-gate-change-completeness close-out line already enumerates the §6-spec surfaces; a reminder there that editing the spec doc's body triggers its own Version+Date co-bump (it is not gate-40/31-exempt) would help. Convention-level; the pre-push skeptical verifier is the working backstop (it caught this one). |
| 2026-07-23 | 1073 | none (§3.102 pack-distribute the unattended-degradation auto-handoff discipline) | The offloaded draft (worker-a) + orchestrator-side substantive-tier skeptical verifier + deterministic dual-tree apply (pack body byte-identical to the `.claude/` mirror, overlay confined to the mirror) landed the §3.102 pack distribution cleanly; the offloaded post-merge `validate-pr` (worker-b) confirmed gate-37 body-parity and zero project-token leak, 0 findings. | Two LOW cosmetic nits, both caught pre-push by the skeptical verifier: em-dashes in the `next-prs.txt` `# then:` line, and the pack README metadata `Date` not co-bumped with the version-history row. | Pack-distribution paired-surface discipline: when bumping a pack README `Version` and adding its version-history row, co-bump the README metadata `Date` in the same commit; and keep project tokens (file names, commands, gate numbers) out of the pack body, confined to the `.claude/` PROJECT-OVERLAY. | Both nits sit in gate-exempt surfaces already covered by the close-out checklist's paired-surface and pack-README-Date lines; no new mechanism warranted. |
| 2026-07-23 | 1072 | none (§1.22.4 cross-repo reference-existence advisory tool) | The offloaded draft (worker-b) + INDEPENDENT adversarial verify (worker-a, SHIP, all 5 concerns refuted) + deterministic `git apply` + `--self-test` 5/5 landed the §1.22.3/§1.22.4 shared-engine pair cleanly; the offloaded post-merge `validate-pr` (worker-a, prior draft-verifier) reviewed the APPLICATION surfaces fresh and confirmed 0 findings. | One catch, pre-push: gate 24 (external-link-domain) flagged an `example.com` URL in the tool's `test_external_and_fenced_skipped` self-test fixture (the gate scans `.py` too); fixed to the allow-listed `github.com` (behaviourally identical under the tool's `^https?:` external regex). | A NEW TOOL's self-test fixtures that embed an external URL must use an allow-listed domain, not `example.com`: gate 24 scans tool `.py` files, so a placeholder domain in a fixture reddens the build. | Add a worker-brief rail / gate-building convention line: test fixtures needing an external URL use an allow-listed domain (e.g. `github.com`), never `example.com`/`example.org`, so gate 24 stays green. |
| 2026-07-23 | 1071 | none (§1.22.7 Maintainer-or-Egress-Gated TODO section) | The offloaded enumeration (worker-b: 47 MEG items against the full TODO + the `_private` egress queue) plus orchestrator authoring produced a clean registry section; the offloaded post-merge `validate-pr` confirmed all refs resolve + the DONE-rotation parity. | Two catches, both pre-merge: (1) gate 62 flagged 2 MEG rows referencing §1.22.8/§1.22.9 (sub-BULLETS, not headings); fixed to the §1.22 parent heading with the sub-item named in parens. (2) The enumeration worker surfaced it could read `_private` this session (a trust-boundary observation, surfaced to the maintainer). | Intra-doc §-ref discipline: a `§N.M` ref must resolve to a HEADING in the same doc, not a sub-bullet (gate 62 enforces it). The worker-`_private`-readability is a §1.19-design-intent deviation worth the maintainer's awareness. | When authoring `§N.M` refs to sub-bullet items, reference the parent HEADING (gate 62 requires a heading match) and name the sub-item in prose. Surfaced the `_private`-readability observation for the maintainer. |
| 2026-07-23 | 1070 | none (§1.22.3 `.working` cycle-out tool build) | The offloaded draft-then-verify-then-apply loop worked cleanly for a DATA-SAFETY tool: worker-a drafted the candidate diff, worker-b independently adversarially verified it (SHIP-WITH-FIXES, catching that the one-off verify-before-prune refusal, the whole guard of the only new destructive path, was not locked under `--self-test`), and the orchestrator folded that one fix in AT apply (extracted the self-tested `oneoff_missing_from_archive` helper) and applied deterministically (`git apply` + full re-read + `--self-test` 7/7). | No friction; the conservative-by-default scope (destructive DONE/pending sweeps NOT enabled, only counted) kept the delicate part out of this cut. | The draft-then-INDEPENDENT-verify-then-orchestrator-apply pattern is the right shape for a delicate data-safety tool: the independent verifier caught a missing self-test the drafter and orchestrator would both have shipped, and folding the fix in PRE-apply (not apply-then-patch) kept it to one PR. | None new; this offload+verify+deterministic-apply pattern is the stable shape for a tool build. Keep deferring the destructive DONE/pending sweeps until the maintainer cutoff + floor + predicate decision. |
| 2026-07-23 | 1069 | none (OWASP Top 10 for Agentic Applications citation; RB-7 residual resolved) | The maintainer-uploaded authoritative OWASP Agentic framework was ingested (`_ref` #101), currency-confirmed upstream this turn, and cited in the corpus + register, resolving the RB-7 residual; the offloaded post-merge `validate-pr` confirmed CLEAN (the ten ASI names verified against the held source). | The pre-push skeptical verifier's one note (a day-level "released 2025-12-10" over-precision in bookkeeping prose, from the WebSearch summary but not the held full-text, which says only "December 2025") was a good catch, softened to "December 2025". Separately, a first broad `grep -r --include` under-reported the carriers (found 1 of 5); the per-file re-grep found all 5. | Two classes: (1) currency-claim precision, prefer the held-source granularity (month) over a secondary-source day claim in bookkeeping prose; (2) grep-claim-fidelity, a single broad `grep -r` can under-report, so confirm a carrier count with a per-file grep before asserting completeness. | When softening or fixing a token across surfaces, confirm the carrier count per-file (not only via one broad `grep -r`), and prefer the held-source granularity over a secondary-source claim. Both were caught pre-merge (verifier + re-grep); no escape. |
| 2026-07-23 | 1068 | none (2026-07-23 resume close-out) | The resume close-out landed clean: Sweep 118 (offloaded worker-a) PASSED as the loop-break control for #1067, the lease was acquired in overnight mode, and the handoff was pruned to current+1-prior; the offloaded post-merge `validate-pr-1068` confirmed CLEAN (0/0/0). | One caught trip, no escape: I initially accepted the Sweep-118 worker note N-A2 (a `validate-pr/history.md` header-date "outlier") and "fixed" it, but the pre-push guard's D4 (Version-Date co-bump) check exposed that the original `2026-07-23` value was CORRECT (the bump-commit UTC date) and my fix was wrong; I reverted it and re-classified N-A2 as a false positive. The guard caught it pre-push. | Elevated-QA lesson: a worker QA note is a hypothesis, and re-verifying it means checking it against the ACTUAL governing rule (here D4's bump-date convention), not just confirming the surface observation (two dates diverged). I confirmed the divergence was real but not which value was correct, and accepted the worker's recommended direction; the mechanical gate was the backstop. | When consuming a worker QA note that recommends a fix, re-derive the CORRECT state from the governing rule before applying the recommended direction, not merely the existence of the flagged condition. The pre-push guard reliably backstops a date/version co-bump slip (D4). |
| 2026-07-22 | 1065 | RB-7 (reference-acquisition track close) | The RB-7 track closed cleanly: TODO->DONE rotation, a `/reference-audit` history row + per-run detail file + doc-state refresh (via `--update-state`, tool-driven not hand-authored), and the #1064 QA batch, all confirmed by the offloaded post-merge `validate-pr-1065` (CLEAN 0/0/0, 8 record-accuracy checks). Classified pure-bookkeeping tier, so no pre-push verifier (correct per the tiering discipline). | The order-status typo (`new` instead of `pending`) meant the offloaded `validate-pr-1065` sat unclaimable for three poll cycles until I verified the order state and flipped it; the worker also logged it as MAINTAINER_ALERT 2026-07-22-a. A one-off (the prior #1057-#1064 orders all used `pending`), caught by checking the order state rather than assuming the offload was live. | Intent-is-not-action: enqueuing an order is not the same as the order being claimable; verify the canonical `status: pending` (not `new`) before assuming an offload is live. The order-authoring reflex carried a wrong initial status. | When enqueuing a credit-offload order, confirm `status: pending` in the pushed order file (a quick `list-pending | grep <id>`) before treating the offload as live; consider a queue-tool enqueue helper that sets `pending` canonically (queued thought, not built). |
| 2026-07-22 | 1064 | none directly (§3.70 pack crypto parity, RB-7-adjacent ancillary) | The corpus-vs-pack layer difference flagged in #1052's pending-decisions (§3.70) was resolved by the maintainer's "tighten the pack too" decision and applied cleanly: both pack Asymmetric-encryption rows (core/cryptography.md + pack CLAUDE.md) dropped below-floor EC P-256 to match the corpus floor, the pack Version + `## Version history` paired surface handled per the paired-surface guard, and both pre-merge verify and post-merge validate-pr confirmed byte-identical. | No friction (clean apply). The pack-versioning paired surface (pack Version + history row) was the only extra surface versus a pure-corpus fix. | Closing a corpus-stricter-than-pack layer difference is a deliberate parity decision the maintainer owns, not an autonomous tighten: §3.70 was routed to pending-decisions in #1052 and resolved only on the maintainer's explicit "tighten the pack too" this session. | None new; the route-to-pending-decisions-then-apply-on-maintainer-decision path for a pack/corpus layer difference worked as intended. |
| 2026-07-22 | 1063 | none directly (RB-7 held-breadth residual) | The residual closed cleanly with two premise corrections caught by the offloaded research: OWASP Agentic Top 10 is NOT authoritatively held (only an untrusted AIUC-1 crosswalk) so no normative cite (routed to egress-acquisition), and NIST IR 8596 is a DRAFT CSF-2.0 profile (not an asset taxonomy) so a draft-watch see-also in the AI risk register, not the RB-7-named system register. The scoping-gap self-catch (the `--ref-since` worklist excluded pre-held items) surfaced the residual before RB-7 was wrongly closed. | The post-merge `/validate-pr` out-of-window note surfaced that `governance/register-canonical-citations.md` already tracks OWASP Agentic by URL (register-only), which the RB-7 research had not cross-checked; not a defect (register-only URL tracking is distinct from a held-source body citation) but a reconciliation item for the RB-7 close. | Two patterns: (1) a `--ref-since` new-ingest worklist by construction misses HELD-but-uncited items a task also names, so a breadth task naming specific held items needs a separate held-item pass alongside the new-ingest pass; (2) "held and citable" in a seed premise must be re-verified against the trust tier (authoritative vs untrusted publication), not taken at face value (RB-7 recorded OWASP Agentic as held+citable when it is held only as an untrusted crosswalk). | When a reference-breadth task names specific items to assess, scope BOTH a `--ref-since` new-ingest pass AND an explicit held-item pass; and re-verify the trust tier of any "held and citable" seed claim before applying. |
| 2026-07-22 | 1062 | none directly (RB-7 N-1/N-2/N-3 breadth sub-items) | The final RB-7 breadth cluster (NYDFS financial-services) landed clean; the research delivered every value verbatim (the exemption-threshold dollars, the annual-filing dates, the amended universal-MFA scope), and the pre-merge verify confirmed each at held source. The 500.12 MFA update aligned the sector annex with the corpus-wide MFA-scope harmonization from #1053. | No friction (clean apply). | The value-specific citations (dollar thresholds, calendar dates) are exactly the class where re-verify-at-source matters most; the research and the pre-merge verify both confirmed them verbatim. | None new. |
| 2026-07-22 | 1061 | none directly (RB-7 L-2/L-3 breadth sub-items) | The LatAm breadth apply landed clean, including the accent-bearing Spanish citation of the Colombia decree (Decreto Unico Reglamentario 1074 de 2015, Capitulo 25): the corpus already permits accented Latin characters, so the proper name was rendered faithfully rather than transliterated, and the pre-merge verify confirmed correct UTF-8 rendering (no mojibake). | No friction (clean apply). The research worker transliterated Spanish quotes to ASCII for the scratch house-style gate; the orchestrator re-quoted the exact source characters at apply time, per the worker's explicit note. | Cross-repo house-style differences matter: the scratch coordination plane's validate.py gate rejects accented characters and curly quotes even inside quotes, so a worker delivery normalizes them, and the orchestrator must restore the exact source characters when the corpus (which permits accents) is the apply target. | None new; the worker's explicit "re-quote exact source characters at apply time" note is the right handoff for accent-bearing citations. |
| 2026-07-22 | 1060 | none directly (RB-7 AI-security integration) | The high-assurance harness ran end-to-end on the maintainer-chosen full-column integration and DEMONSTRABLY improved the result: the two independent adversarial verifiers (blind to each other and to the research rationale) dropped 2 over-assigned cells to N/A (Unsafe-code-gen, Overreliance-SANS), replaced 1 over-generic anchor (Table-3 ISMS to scope-precise categories), and dropped 1 redundant bullet; then a deterministic apply from the reconciled explicit map, a re-parse, and an independent pre-merge apply-correctness verify confirmed no transposition. Every verifier finding fell among the research's own flagged cells, validating the flag-and-scrutinize protocol. | No friction. The full harness (research + false-negative + false-positive + apply-verify) is heavier than a single verifier but appropriate for a gate-blind semantic-fit reshape across 5 docs; the maintainer chose full columns knowing that. | The two-independent-adversarial-verifier stage earns its cost on a semantic-fit mapping: the false-positive lens alone removed 3 over-assignments a single confirm-oriented pass would have shipped, and the distinct lenses (hunt-misses vs hunt-over-assignments) caught different classes. | None new; route full-column / matrix-fit-class semantic mappings through the two-verifier harness, not a single verifier. |
| 2026-07-22 | 1059 | none directly (RB-7 C-1/C-2 breadth sub-items) | The Canada breadth apply landed clean AND surfaced the highest-value catch of the RB-7 wave: the offloaded research worker (`research-canada-breadth`) re-verified the reference-audit's C-1 claim at held source and CORRECTED it (the GC VM Guideline does not publish a 48h/14d/30d remediation matrix; that timed table is scanning frequencies, remediation timing is qualitative), so a fabricated numeric claim never reached the corpus. Both the pre-merge skeptical verify and the post-merge validate-pr confirmed the corrected, held-grounded text. | No friction (clean apply). | The re-verify-at-source step catches OVER-SPECIFIC upstream findings, not only under-citation: a reference-audit finding that asserts a specific value (here fixed remediation hours) must be re-grounded at the held source before apply, because the seed worker can over-attribute a plausible value the source does not state (the FR-120 attributed-value class surfacing inside a breadth pass). | Reinforce briefing research/apply workers to re-verify (not restate) any specific numeric value a seed finding claims; the research-worker-then-apply-verify chain caught this one twice. |
| 2026-07-22 | 1058 | none directly (RB-7 SAIF accuracy sub-item) | The second RB-7 accuracy fix landed cleanly: the SAIF "execution" overreach (a citation the corpus and pack had carried without a held source) was corrected only after the source was held and re-read, the drop confirmed by a full "execut*"-in-context scan of the six held SAIF files, a refute-briefed skeptical verifier returned SHIP, and the offloaded post-merge `/validate-pr` corroborated. The pack-versioning paired surface (pack Version + `## Version history` row) was handled correctly. | No friction (clean apply). The pack README plus version-history-row pairing was the only extra surface versus a pure-corpus fix; handled per the paired-surface guard. | The RB-7 SAIF finding was an OVERREACH on an EXISTING citation, not a breadth gap: holding a previously-uncited source let the audit check the standing citation's fidelity and catch the over-claim. A cite-without-source gap, once the source is acquired, is also a citation-accuracy audit opportunity, not only a breadth one. | None new. |
| 2026-07-22 | 1057 | none directly (RB-7 L-1 sub-item; RB-7 still open) | The first RB-7 apply landed cleanly under accuracy-first sequencing: the Brazil ANPD adequacy currency correction was re-verified at the held Resolution 32/2026 source before authoring, a refute-briefed skeptical verifier returned SHIP, and the offloaded post-merge `/validate-pr` independently corroborated the fidelity line-by-line. The worker-delivered reference-audit candidate (L-1) proved accurate as delivered (a currency defect the corpus genuinely carried), validating the reference-audit's dual breadth-plus-accuracy value. | The verifier surfaced one optional prose nit (a loose pronoun antecedent, "those destinations"), applied as a one-word tightening ("those recognized destinations") before push. Not a defect, a quality polish. | A new-ingest reference-breadth pass doubles as a currency check where the newly-held source post-dates the corpus claim: here a new source (Resolution 32/2026) disproved a standing corpus statement ("not yet issued adequacy decisions"), a currency defect surfaced as a side effect of the breadth pass. | None new; the re-verify-at-held-source-before-authoring plus skeptical-verifier plus offloaded-validate-pr chain worked as designed. |
| 2026-07-22 | 1056 | none (resume close-out; RB-7 assessment opened, not closed) | The 2026-07-22 resume ran cleanly under the offload discipline: the loop-break Sweep 116 `/validate` was offloaded to worker-b (clean pass), the aged L133 follow-up (a multi-day-gap bookkeeping artefact, not a corpus regression) was correctly re-triaged rather than closed, and the handoff prune + lease acquire followed the standard first-PR-of-session pattern. The #1056 per-PR `/validate-pr` was also offloaded (worker-b routine tier) and returned clean. | Two mechanical catches, both the verification layer working as designed: (1) the working-tree `run_all_audits.sh` passed 72/72 but the post-commit pre-push guard caught gate 31 (README Date not co-bumped with the Version) because gate 31 is history-aware; fixed by amend. (2) D8 flagged the root CHANGELOG entry's over-45-word sentence; fixed by splitting into shorter sentences. Both caught pre-push, no escape. | The history-aware gates (31/40) can pass on the working tree yet fail post-commit against HEAD, so the pre-push guard (not the working-tree audit) is the authoritative Version+Date co-bump check, exactly as the version-bump discipline states. Not a new pattern (the known Version-bumped-Date-missed recurring class). | None new; the Version+Date co-bump and the D8 sentence ceiling are existing standing guards that fired correctly. |
| 2026-07-19 | 1053 | §3.69 (MFA scope); opened §4.30 (adopter umbrella) | The MFA harmonization applied cleanly to the type-anchored form across all three governing docs; the terse umbrella-policy editorial choice for the info-sec clause held under adversarial refutation (the offloaded verify confirmed "interactive user" correctly scopes out service accounts). The §4.30 adopter-experience umbrella captured a coherent multi-phase initiative from the maintainer's iterative design (full clean-clone assessment + `.adopt` kit + move DONE/improvement-log to `_private` with gate resolvers). Both the pre-merge verify and the corpus-wide Sweep 115 confirmed clean. | No friction (clean apply). | None new. | None. |
| 2026-07-19 | 1052 | §3.70 (crypto tables) | The offloaded pre-merge refute-verify (worker-a) confirmed the §3.70 change clean at source (both crypto rows match the encryption-policy floor, no stale P-256 carrier, generated artefacts in sync); the offloaded verify is now the standard pre-push flow under the new mandatory-offload rule. The worker's flagged secondary (tighten the prohibited RSA threshold to under 4096) was a sound catch, applied stricter-safe. | No friction (clean apply). The corpus-vs-pack layer divergence (the corpus is now stricter on EC curves than the distributable pack) surfaced and was routed to pending-decisions for confirm-or-redirect, an intended layer choice, not friction. | None new. | None. |
| 2026-07-19 | 1051 | none (maintainer-directed mandatory-offload guardrail) | The OFFLOADED skeptical verify (worker-20260716-a, the first live practice of offloading the pre-push verifier) CAUGHT a real MEDIUM the orchestrator introduced and missed (two disagreeing offloadable-set enumerations in the very PR meant to prevent drift), fixed in-window before merge. First concrete proof that offloading verification catches what the orchestrator's own review misses, validating the primordial-rule redesign. | The orchestrator authored two offloadable-set lists (from the worker draft's two parts, the primordial rule and the operational section) without cross-harmonizing them, a multi-surface-consistency miss. Root of the whole redesign: the orchestrator had self-run two prior `/validate-pr` passes (#1049, #1050) while workers idled, the exact behaviour this guardrail now blocks. | Multi-surface enumeration drift: two lists of the same set introduced in one PR and not cross-checked (the multi-surface-incompleteness class); here the offloaded verify caught it where the orchestrator's self-review did not. | None new. The standing mechanisms now cover it: the offloaded-verify practice (verification is worker-level), the `block-mandatory-offload.py` hook (mechanical backstop), and the multi-surface-completeness checklist. |
| 2026-07-19 | 1050 | none (credit-offload public tail; revised §3.87, added §3.103) | The credit-offload liveness bundle landed cleanly across three repos (scratch #173, two `_private` pushes, #1050); the pre-push `preflight-changelog` caught cross-repo `_private` filenames as unlinked path-shaped code spans before the first commit, so no CI round-trip. | A detailed-mirror entry referencing `_private` files (`credit-offload-design.md`, `orchestrator-claude.md`) tripped the preflight as unlinked path-shaped code spans (they are cross-repo, so unlinkable); reworded to plain prose. | Cross-repo filename in a code span tripping the CHANGELOG link preflight, second occurrence this session (also the #1051 detailed entry needed the same care). | Convention (no gate change): a `_private` / cross-repo file named in a CHANGELOG entry goes as PLAIN PROSE, not a backticked path-shaped code span; the preflight already enforces it pre-commit. |
| 2026-07-19 | 1049 | none (maintainer-directed decision-guardrail self-guard) | The write-before-enact decision-log + `block-unjustified-decision.py` hook + `_private` validate check shipped with exact cross-surface parity (the validate-pr Subagent A confirmed the BLOCKED closed set byte-identical across four surfaces) and a clean 9/9 self-test; the hook's forbidden-phrase check was correctly scoped to deferral entries from the start. | The hook BLOCKED the orchestrator's OWN first (documentation-heavy) log write, because the initial log file embedded `**Classification:**` examples and verbatim forbidden phrases the hook fires on; resolved by keeping the log LEAN (the discipline prose lives in CLAUDE.md; the log holds entries only). A self-guard that guards its own artefact must not embody the patterns it forbids. | none new (first occurrence; relates to the session's intent-vs-artefact theme but distinct). | §3.103 (widen the deferral-trigger keyword set) already routed from the #1049 validate-pr NOTE; no further candidate. |
| 2026-07-19 | #1048 | §1.22.5 (CHANGELOG daily-tier: condense the current-week days to daily summaries); carries the #1047 QA. | The deterministic-apply-with-a-no-drop-assertion approach made a 183-entry PUBLIC CHANGELOG restructure safe: the script asserted every removed per-PR entry is covered by a daily-summary range, and TWO independent skeptical passes (pre-push + post-merge) each INDEPENDENTLY reconstructed the #856-#1038 set and confirmed the 6 ranges partition it exactly (no history lost). The gate-59 coupling was correctly resolved by pruning the mirror to floor the parity cutoff. | ONE MED doc-drift caught POST-merge: the tool's MODULE docstring was not co-updated with the daily-tier behaviour change (the function docstring, inline comments, and prints WERE updated). | Module-header-doc-drift class: when a tool's BEHAVIOUR changes (a new tier/mode), the MODULE docstring (the header describing the model) must be co-updated, not just the changed function's docstring + comments; the module header is the easy-to-miss surface. | Proposed improvement (standing habit, no gate, free prose): on a tool behaviour change, grep the MODULE docstring for the old-model phrase (here 'Three tiers' / 'CURRENT WEEK') and update it in the same PR. The post-merge /validate-pr reliably catches this class; convention-level. |
| 2026-07-19 | #1047 | §1.22.2 (root `.directories` cleanup: delete the 3 sibling stubs, gate 70 guard-if-present-as-stub, `/adopt` creates-on-request); carries the #1046 QA. | The ONE-coherent-PR framing (per the maintainer's just-given PR-splitting calibration) worked: a 26-file multi-surface change (gate logic + fixture + §5/§6 spec + `/adopt` skill/command/bootstrap + `lint_common` + `detect-env` + CLAUDE.md + 3 deletions + generated-artefact regen) landed as ONE clean PR, not fragmented. The completion grep over ALL file types caught the §5 grouped-list surface a §6-only edit would have missed. The pre-push skeptical verifier earned its keep (caught a real HIGH). | TWO process slips, BOTH caught pre-push (the verification layer working, NOT a degradation signal per CLAUDE.md): (1) I ran `run_all_audits.sh` BEFORE the bookkeeping version bumps, so the spec-doc Version bump left `taxonomy.yml` stale (gate 33), caught by the pre-push verifier; (2) I bumped the pack README Version without co-bumping its Date, caught by D4. Both are sequencing slips in the version-bookkeeping step. | Version-bookkeeping-sequencing class (new): the mechanical baseline suite + generated-artefact regen must run AFTER the version bumps, not before; a pack Version bump must co-bump the pack README Date. | Proposed improvement (standing habit; the gates already catch it, so no new gate): after the version-bookkeeping (per-doc Version, pack Version + pack Date, library CalVer), ALWAYS (a) regenerate taxonomy->portal->scorecard if any per-document Version changed, then (b) re-run `run_all_audits.sh`, BEFORE committing, so a gate-33/34 desync or a missed Date co-bump is caught locally rather than by the verifier or CI. The regen-order and version-bump disciplines already exist in CLAUDE.md; the addition is to sequence the suite-run LAST in the bookkeeping step. |
| 2026-07-19 | #1046 | none (working-state / backlog PR: the #1045 QA batch + TODO §4.29 + §2.24) | The QA-plus-captures batch was split OFF the big root-cleanup PR and landed CLEAN (validate-pr 0 findings), directly answering the maintainer's `when does §2.24 merge` by getting it in promptly; the assess-then-scope-then-capture flow for the governance-relationship proposal (two AskUser rounds, all recommendations accepted) produced a turnkey §2.24 with no premature authoring commitment. | The maintainer flagged that I OVER-FRAMED the root-cleanup as a multi-PR effort and over-apply always-split-when-in-doubt; I adapted (root cleanup = one coherent PR), corrected the §2.24 apply-discipline line, and saved a pr-splitting-calibration memory. | Over-split / over-framed-effort class (new): treating a coherent on-topic multi-file task as a multi-PR program is needless overhead; always-split is for GENUINELY UNRELATED work, not for fragmenting one coherent task. | Proposed improvement: SAVED as standing guidance (the pr-splitting-calibration memory + the §2.24 apply-line fix): one coherent on-topic task = one PR even across many non-overlapping files; split only genuinely independent parts. No gate fits (it is a judgment calibration on free work-shaping); convention/behaviour-level. |
| 2026-07-19 | #1045 | §1.22 self-guard bundle (maintainer-directed; not FR-keyed); carries the #1044 `/validate-pr` + `/retro` QA batch. | The layered verification worked end-to-end with NET 0 ESCAPES of the guard machinery to merge: the pre-push adversarial verifier caught a real MEDIUM (GUARD 1's remediation steer was hardcoded to the wrong-repo class and would tell a pipe-guard-blocked command to `add a cd`), FIXED + locked with `test_steer_is_class_specific` BEFORE merge; the post-merge `/validate-pr` Subagent A then independently re-confirmed the hook logic + wiring clean (12/12 self-test). The new intent-is-not-action read-back (git status clean before the PR) worked: 22 staged, 0 unstaged residual. And the bundle mechanically implements the exact intent-vs-artefact class that caused this session's earlier slips. | (1) D8 caught an over-long ROOT CHANGELOG entry (137 words > 100) pre-push, re-compressed to a compact multi-sentence form. (2) F1 (validate-pr) = self-test count-drift: the detailed CHANGELOG said `4 hook self-tests` while the shipped self-test is 12 CASES (I conflated `4 hooks' self-tests` with `the new hook's 12 cases`), caught post-merge. (3) F2 (validate-pr) = a #1044-escape moved-section residual (`## Credit-offload mode` cited in `.claude/CLAUDE.md` at `TODO.md:345`) that BOTH #1044's and #1045's `0 stale pointers` grep missed, because it sat on a historical `APPLIED 2026-07-16` phase-status line rather than a live convention pointer. | (a) **Self-test count-drift in record prose = 3rd distinct PR** (#1041 `10 vs 12`, and the #664 `bare-form fixtures` measured-not-inferred class, now #1045 `4 vs 12`): AUTO-GRADUATES at 3. (b) **Moved-section §N-orphan grep misses a HISTORICAL `APPLIED`-phase status line**: the #1044 trim's whole-repo grep (and #1045's re-grep) prioritized live convention pointers and skipped a descriptive `APPLIED: wired into .claude/CLAUDE.md` line, a stale locus all the same. | Proposed improvement: (a) for the count-drift class (now 3): a fully mechanical gate is false-positive-prone because the referent varies (hooks vs cases), so propose **WATCH (fires on 4th occurrence) -> a targeted preflight assertion** that greps added CHANGELOG lines for an `N (hook )?self-test` figure and cross-checks the hook's `--self-test` `Ran N tests` output; the interim convention is to cite the MEASURED `Ran N tests` count and LABEL what is counted (cases, not hooks). (b) extend the §N-orphan / moved-section close-out grep to include HISTORICAL `APPLIED`/phase-status lines, not only live convention pointers (a convention line for the section-move checklist; no clean gate fits free prose). Both fixes for this PR's F1/F2 batch into the next PR. |
| 2026-07-19 | #1044 | §1.19.12 (CLAUDE.md sensitivity-trim applied); carries the Sweep 114 loop-break close-out for #1043. | The high-assurance method worked end-to-end on a wide sensitive edit to the apex file (1699 to 1262 lines): worker-a produced a deterministic line-range apply script + candidate + re-parse proof, the orchestrator ran it on live source, cross-checked it against the candidate (a single-line watermark diff), adjudicated every removed rule-language line as a legitimate move (not a dropped rule), and confirmed the LOAD content byte-verbatim against the removed spans. TWO independent adversarial verifiers each earned their keep: the in-session full-access verifier returned CONFIRMED-CLEAN (caught 2 cosmetics), and the offloaded worker-b public-side QA caught F1/F2 that the CLAUDE.md-only verifier's scope did not cover. Net 0 escapes. | Two self-slips, BOTH caught pre-merge: (1) F1/F2 = 4 stale `resume.md` pointers to the moved `## Credit-offload mode` / Multi-session content (worker-b) - the trim moved the sections but resume.md still pointed at them in public CLAUDE.md; (2) the 2 verifier-flagged CLAUDE.md nit-fixes were left UNCOMMITTED because the bookkeeping commit's targeted `git add <list>` did not include `.claude/CLAUDE.md` (caught by the `gh` uncommitted-change warning + a `git status` clean-check before merge). | (a) Section-MOVE cross-file class: a section move must grep `.claude/commands/` (not only corpus `.md`) for pointers to the moved section - a COMMAND file (resume.md) pointed at a moved CLAUDE.md section, and a single-scope verifier misses it (the §N-orphan-cleanup guard extended to command-file pointers, plus a two-verifier-scope lesson). (b) Targeted-git-add-drops-a-later-edit class: a file edited AFTER a targeted `git add <list>` can be left uncommitted; the `gh` warning + a pre-PR `git status` clean-check is the backstop. Both are the intent-vs-artefact / paired-surface class the SELF-GUARD BUNDLE (this next PR) targets. | Proposed improvement: (a) SHIPPING the self-guard bundle next (repeat-block hook + diagnosis circuit-breaker + evidence-validated degradation-watch log + read-back + intent-vs-artefact disciplines), which directly addresses the intent-vs-artefact slips this session surfaced [DISPOSITION: (a) CODIFIED in #1045, the self-guard bundle shipped]; (b) standing habit: on a section MOVE, run the whole-repo §N-orphan grep across ALL trees, tool docstrings (`tools/*.py`), `TODO.md`, and `.claude/commands/` included, not only corpus `.md` or the one obvious file, the #1044 post-merge `/validate-pr` (worker-a) found 2 tool-docstring escapes and the orchestrator's follow-up grep found a 3rd (`TODO.md`) that the pre-merge QA all missed because only `resume.md` was grepped; (c) standing habit: `git status` must be clean (or intentionally staged) before opening a PR - a targeted `git add` can silently drop a later edit. Two independent verifiers with DIFFERENT scopes (one CLAUDE.md-internal, one command-file-pointer) is the right pattern for a wide privatization move. |
| 2026-07-19 | #1042 | §1.22.1 (wrong-repo tool guardrail hook); carries #1041 QA. | The hook shipped correct (8/8 self-tests, self-run validate-pr CLEAN 0/0/0) and IMMEDIATELY re-caught in production the exact `credit-offload-queue.py`-from-wrong-cwd slip it targets, a rare same-PR proof-of-value. worker-a's validate-pr-1041 delivery got proper ELEVATED QA (delivery-1 auditor caught F2, a miss the worker made), the elevated-QA layer working as designed. | TWO self-inflicted slips, both self-caught, no escape: (1) I ran `git fetch && git reset --hard origin/main` on a FEATURE BRANCH (scratch-sync reflex misapplied), reverting the settings wiring, recovered by re-adding + committing the core; (2) after wiring the new hook, I repeatedly (5x) failed to prepend the explicit `cd` the hook now requires for sibling-repo tools, updating the command DESCRIPTION without editing the actual command string, so the hook correctly blocked each attempt. | New-guardrail-vs-old-habit friction: a hook I built THIS session (requiring explicit `cd` for sibling tools) fought my ingrained persisted-cwd habit, and I edited command descriptions without editing command bodies (a copy-template-without-editing slip). Plus the scratch-sync-reflex-on-feature-branch class (same cross-context-command hazard §1.15a/§1.22.1 address). | Proposed improvement / lessons (standing): (a) ALWAYS prefix a scratch/`_ref`/`_private` tool invocation with an explicit `cd /home/jposluns/<repo> &&` (the §1.22.1 hook now enforces it; stop relying on a persisted cwd); (b) NEVER run `git reset --hard origin/main` on a `grc_library` feature branch, that sync pattern is ONLY for the wipeable scratch checkout; (c) when re-issuing a blocked command, edit the COMMAND STRING, not just the description. These accumulated into the early-wind-down decision (#1043); the density of self-caught slips in one un-reset marathon turn was the named-degradation signal. |
| 2026-07-19 | #1041 | §1.22.6 (answered-question guardrail); carries #1040 QA. | The guardrail directly fixes a SAME-SESSION failure (I re-asked the maintainer four content forks already recorded in pending-decisions.md). The pre-push skeptical verifier found a real over-block class (the coded-id regex matched corpus control codes like SEF-07 and statute codes like A-2.1), FIXED pre-merge by restricting to known backlog-id prefixes; 12/12 self-tests. The gate-50 guard caught the missing #1040 QA-row batch before push; D2/D8 caught two more bookkeeping misses. Net 0 escapes. | THE motivating friction: I asked the maintainer four decisions they had ALREADY answered (compute-first / check-before-asking failure), which cost their time and eroded trust; the maintainer explicitly demanded a guardrail. Plus the recurring CHANGELOG bare-path-span preflight trips and three bookkeeping-pairing misses (gate-50 QA-row, D2 ledger-version, D8 length), all caught pre-push. | Re-ask-already-decided class (new, high-cost: wastes maintainer time) + the continuing bookkeeping-pairing/bare-path class (caught reliably by preflight/gate-50/D-checks, 0 escapes). | Proposed improvement: SHIPPED the mechanical fix for the re-ask class this PR (the block-answered-question hook + decisions-search tool + the CLAUDE.md "search decisions before asking" discipline, the executed-not-narrated forcing function); the standing habit is to run decisions-search before ANY authorial-decision AskUserQuestion. For the bookkeeping-pairing class the mechanical layer is sufficient (no new gate); the residual is authoring reflex. |
| 2026-07-19 | #1040 | Resume close-out (Sweep 113 recorded, handoff pruned, lease acquired, TODO §1.22 queued). | Clean resume: Sweep 113 offloaded to worker-b and consumed under full ELEVATED QA (mechanical facts independently re-derived, a delivery-1 false-negative auditor returned CLEAN in-delta), loop-break control for #1039 PASSED; the handoff prune + lease acquire were verified correct by validate-pr-1040. | Two paired-surface bookkeeping misses, BOTH caught pre-merge by the mechanical layer: (1) I added top-level P1 §1.22 but did not bump the P1 "Next item number" counter (F1, caught by validate-pr-1040); (2) I first built #1041 WITHOUT batching #1040's /validate-pr + /retro rows, caught by the gate-50 pre-push guard before any push. | Paired-surface / counter-bookkeeping class: adding a numbered section has a paired priority-counter to bump; a merged PR has a paired QA-row pair to batch into the next PR. Both are mechanically caught (validate-pr + gate 50), 0 escapes. | Proposed improvement: the mechanical backstops (validate-pr for the counter, gate 50 for the QA-row pairing) are reliably catching this class pre-merge, so no new gate is warranted (gate-discipline: no redundant machinery). Reinforced standing habit: when adding a numbered TODO section, bump its priority's "Next item number" in the SAME edit; and treat the recursion-avoidance batch (the prior PR's /validate-pr + /retro rows) as a first-class close-out-checklist item, not an afterthought. |
| 2026-07-18 | #1019 | §3.84 (Quebec Law 25 citation corrected to s. 3.3). | The maintainer-authorized Quebec fix landed cleanly + fully evidenced (s.3.3 confirmed at held s.3.4 cross-ref + upstream + the corpus-wide no-residual grep). The offloaded validate-pr-1019 CAUGHT a real adopter-portability test defect I introduced in #1017 (§1.20): the live-smoke test hardcodes rc-3 (refuse), which fails on an adopter clone where the guard correctly proceeds. The worker correctly framed it as an environment artefact orthogonal to #1019; I verified it's a genuine portability regression and routed it (§1.20, fresh-session first-fix). | I wrote an environment-fragile live-smoke test in #1017 (`test_live_repo_refuses_on_maintainer_clone` assumes the runner classifies non-adopter). It passes on the maintainer machine + CI but breaks the adopter-clone portability invariant. Not caught by #1017's own verifier (which ran on the maintainer machine where it passes). | Environment-dependent test assertion: a live-smoke test hardcoding an outcome (rc, count) that actually depends on the runtime environment (operator classification, egress, sibling presence). Passes where authored, fails elsewhere, exactly the adopter-portability class §1.19 protects. | Proposed improvement (in §1.20's fix + as a standing lesson): a live-smoke test whose result depends on the environment must assert the CONTRACT (consistency with the environment's actual detected state) rather than a hardcoded outcome, e.g. "rc 0 iff classification is adopter, else rc 3", not "rc 3". The offloaded QA running in a DIFFERENT environment (the worker's adopter-classified clone) is what surfaced it, an incidental benefit of cross-environment QA. |
| 2026-07-18 | #1018 | §3.93 (closed, rotated to DONE). | Clean close-out consolidation: recognized §3.93(a)+(b) were ALREADY applied in CLAUDE.md (a prior session), so §3.93 was fully complete once (c) shipped scratch-side, avoiding redundant re-application; rotated §3.93 to DONE with both D5 closure forms + the §N-orphan grep (no dangling 3.93 pointer). Terminated the overnight substantive tail cleanly. | The bare-path-span-in-CHANGELOG issue recurred AGAIN (5 path-spans: `.claude/CLAUDE.md`, `credit-offload-queue.py`, an `origin/main:...` git-ref, `validate.py`), all caught pre-commit by preflight, all de-path-shaped. 4th session-occurrence (#1013/#1015/#1016/#1018). | FIRM pattern: freshly-authored CHANGELOG detailed entries keep including bare path-shaped code spans, especially for NON-resolving refs (scratch tools, `.claude/` files, git refs). Always caught pre-commit by `preflight-changelog.py`, so it never escapes; the cost is only a fix-loop. | Proposed improvement: the mechanical backstop (preflight) is reliably sufficient (0 escapes in 4 occurrences), so no new gate is warranted (gate-discipline: do not add redundant machinery). The residual is authoring reflex; the standing habit (write a CHANGELOG file-ref as a `[`path`](path)` link if in-repo, or prose if it is a scratch/`.claude/`/git-ref that will not resolve) is the fix, reinforced here. Accept preflight-as-guard; stop re-proposing new mechanism. |
| 2026-07-18 | #1017 | §3.92(b) tool-side (§3.92 stays open for c). | The security fail-safe was designed correctly (exit 0 only on adopter; every failure -> refuse) and proven three ways: code read, 5 tests incl. an undetermined-refuse case, and an empirical run. Reusing detect-env's classification (one source of truth) kept the guard thin. The pre-push skeptical verifier CAUGHT a doc/code mismatch (a documented exit code 2 the code never produced) before push, an accuracy defect, not a safety one; fixed pre-push (docstring + commit message) and re-confirmed. | I documented an exit code 2 ("internal error") in the docstring + commit message that `main()` never returns (the probe-missing case returns None -> exit 3). A spec/impl mismatch in my own authoring; the `3` description already covered probe-failure, so the `2` line was internally contradictory. Caught by the verifier. | Authoring-accuracy slip: documenting behavior the code lacks (a phantom exit code). Distinct from the CHANGELOG path-span/dash recurrences; this is a docstring-vs-code contract mismatch. First occurrence of this specific shape this session. | Proposed improvement: when a tool docstring enumerates exit codes (or any contract the code must honor), grep the code for each enumerated value (`return 2` / `sys.exit(2)`) before shipping to confirm it exists; an unreachable documented code is an accuracy defect. Cheap grep, catches the phantom-contract class the verifier caught here. |
| 2026-07-18 | #1016 | §3.92(a) tool-side (§3.92 stays open for b/c). | The ADDITIVE design (add two emitted fields, never touch the classification computation) made the /resume-critical detect-env change low-risk, and both the pre-push verifier and the offloaded validate-pr independently proved correctness across 11 cases (incl. crash paths) with the classification unchanged. Choosing detect-env (runs only at the next /resume) over the in-session-used credit-offload-queue tool avoided live-session risk. The one-source-of-truth pattern held (the guard in #1017 reuses this classification). | The CHANGELOG detailed entry tripped preflight on bare path-shaped code spans (`detect-env.py`, `.claude/adopt-config.json`, `resume.md`, `credit-offload-queue.py`), the same bare-path recurrence as #1013/#1015; fixed by de-path-shaping the non-resolving ones (scratch tool, `.claude/` config) and linking the in-repo one. Caught pre-commit by preflight. | Continuing recurrence of bare-path-spans in freshly-authored CHANGELOG prose (#1013, #1015, #1016), all caught pre-commit by `preflight-changelog.py`. The mechanical backstop is doing its job; the authoring reflex is the gap. | Proposed improvement: when a CHANGELOG detailed entry names a file, decide at authoring time: an in-repo file -> write it as a `[`path`](rel/path)` link; a non-resolving file (a sibling-repo tool, a `.claude/` file that may not exist in the maintainer clone) -> refer to it by prose name, NOT a backticked path. This is the same habit as the #1015 retro's dash-grep, extended to path-spans; preflight remains the backstop. |
| 2026-07-18 | #1015 | §1.14 Layer A (gate 72; §1.14 stays open for Layer B). | Guard-first held: the gate shipped green (175 rows within window). The WARN-mode decision was well-reasoned from the register's own "advisory, not a gate" note (a FAIL staleness gate reintroduces the blocked-egress-fails-CI problem), so the authorial policy was a defensible stricter-safe default, logged and dormant, not a guess forced through. The offloaded seed-114 gave a complete design base (schema survey + guard-first analysis) that re-authoring built on. The pre-push skeptical verifier CAUGHT an em-dash in the pending-decisions note before push (gate 51 would fail CI); fixed and re-confirmed. | I introduced an em-dash in a NEW `.working/pending-decisions.md` note ("DEFAULTS -- confirm"). Caught pre-push by the verifier + gate 51, but it is the same new-prose-dash slip that preflight catches on CHANGELOG lines: em-dashes creep into freshly-authored working-state prose. | Recurrence signal (2nd-3rd this session of a dash/hygiene slip in freshly-authored prose, all caught pre-push by preflight or gate 51 or the verifier: the #1013 CHANGELOG path-span, the #1015 CHANGELOG generated-artefact spans, this em-dash). All caught before escape, so the verification layer is working; the authoring habit is the gap. | Proposed improvement: extend the first-commit hygiene habit beyond CHANGELOG (which `preflight-changelog.py` covers) to ALL freshly-authored prose in a PR, `.working/` notes included: a quick `grep -nP '[\x{2014}\x{2013}]'` on the touched non-CHANGELOG prose before commit (gate 51 covers `.working/`, but grepping pre-commit avoids the verify-fix-recommit loop). Convention-level (the mechanical gate 51 is the backstop). |
| 2026-07-18 | #1014 | none (§1.19.12/13/18 prep-drafts staged; not closed). | The credit-offload research-assistant discipline ran cleanly end-to-end: the §1.19.12 CLAUDE.md trim-list was OFFLOADED to worker-b as a research seed, re-authored (not pasted) after spot-checking against the live CLAUDE.md structure, and the paired validate-pr-1014 came back CLEAN. This closed the maintainer-requested overnight prep-drafts gap that a prior turn had skipped (going straight to P3). Bundling all four .working/ staging payloads into one coherent "overnight prep" PR avoided serializing four tiny PRs on the worker QA cycle. | The initial turn of this resumed session went to P3 before the maintainer-requested §1.19.12/13 prep; self-corrected by re-reading the overnight scope and prioritizing the prep-drafts. No mechanical miss (the drafts are working-state, gated by preflight + the offloaded validate-pr). | Not a recurring pattern (a one-turn scope-ordering slip inside a resumed session, self-corrected). The standing lesson (explicit maintainer asks precede discretionary P3) is already covered by the priority-ordering + next-prs queue disciplines. | Proposed improvement: none; the fix was re-reading the recorded overnight scope, which the next-prs `# then:` queue already carries. |
| 2026-07-18 | #1013 | §1.15a part (a) (not closed; part b staged for daytime). | TWO independent verifications confirmed repo-guard.sh's load-bearing property (never run the wrapped command on a repo mismatch): the orchestrator's pre-push refute-briefed verifier AND the offloaded validate-pr-1013, the latter with 7 empirical exit-path tests. The changelog preflight correctly BLOCKED the first commit on a path-shaped `.claude/CLAUDE.md` code span (the `&&` chain held), reworded and re-ran clean. Offload-and-wait held: I waited through a slow worker run rather than self-running, conserving orchestrator credits. | worker-b's registry AND order-file heartbeats both froze at 03:56:38 for ~3.5 min during a legitimately-slow run (the QA re-fetches the pinned SHA from real upstream, then runs all 71 gates), which momentarily read as a possible stall. I probed the order-file status (`done` vs `claimed`) before concluding, and it resolved as delivered (slow, not stalled). | §3.88 recurrence (registry-heartbeat-lag) PLUS a new note: a no-egress QA is NOT necessarily fast, because the pinned-SHA upstream fetch + the 71-gate run dominate (~4 min), so the "typical duration" for the stall heuristic is longer than the diff size suggests. The stall tell is a `claimed` (not `done`) status with a frozen heartbeat PAST ~5 min, not ~3 min. | Proposed improvement: none new; probing the order-file `status` before declaring a stall (rather than the registry heartbeat alone) is the right habit and worked. Note for the background-task SOP: calibrate the QA-order stall threshold to ~5 min, since the upstream-fetch + full-gate-run baseline is ~4 min even for a tiny diff. |
| 2026-07-18 | #1012 | §3.84 reversed (not closed); §3.100 opened. | The pre-push skeptical verifier CAUGHT a backwards change before it shipped: my §3.84 attempt (rewrite the corpus "3.3" -> "23.3") rested on a held-PDF-extract header ("23.3.") that is actually margin-note corruption. The verifier proved s. 3.3 is the correct in-force PIA duty (the held s. 3.4 body reads "referred to in section 3.3"; even-header series clean, odd headers corrupted). I abandoned the branch, kept main's correct "3.3", and shipped an escalation documenting the real error (the #973-shipped `annex-privacy-canada.md` "s. 23.3"). The offloaded validate-pr-1012 then INDEPENDENTLY re-confirmed at the held source AND upstream LegisQuebec (which I could not reach). | I trusted a held-PDF-extract section HEADER at face value in my earlier "re-verification", and so did the #973 worker QA; both were fooled by the same corrupted "23.3." header. Only the adversarial verifier (briefed to refute) broke the shared error. | **Third-plus occurrence of "a held-PDF extract is not ground truth on its own"** (prior: OCR/margin-note artefacts in ingested sources). A PDF-extract header/number is the LEAST reliable token in an extract (margin notes, line numbers, and running heads rotate into the text). Pattern: for any load-bearing statute SECTION NUMBER from a held extract, cross-check the statute's OWN internal cross-references (a "referred to in section N" body clause) AND upstream before citing; never trust the extracted header alone. | Proposed improvement: §3.100 (re-ingest a clean P-39.1 source) is opened. Candidate worker-brief / claim-fit note: a held-source section-number claim requires an internal-cross-reference OR upstream corroboration, not the extracted header. The `/claim-fit` cadence already judges attributed VALUES against source text; extend its guidance to treat an extract's section-NUMBER header as corroboration-requiring. |
| 2026-07-18 | #1011 | none (§3.90/§3.91: sibling-reaching advisory-tool graceful degradation). | Ground-truth-first (RAN each candidate tool sibling-free before editing) showed only 3 of the 6 crashed, scoping the fix precisely; the guard fires only on default+absent (explicit bad path still errors). THE catch: the §3.91 item ENUMERATED six tools but the check-portability header named only three; I initially investigated the three, then caught the full six by reading the item's own enumeration. | Reading a secondary surface's partial list instead of the item's enumeration would have missed 2 crashers. | Pattern: read the ITEM's enumeration as authoritative, re-verify EVERY enumerated case (find-every-carrier). Self-caught mid-build. | Proposed improvement: none; the read-the-item + ground-truth-first habits worked. |
| 2026-07-18 | #1010 | none (§3.98: gate-61 external-path ValueError guard). | A clean small robustness fix: read the flagged line, added a `_display_path` helper with an absolute-path fallback, verified gate 61 still clean, and consumed the validate-pr-1009 delivery which caught (offload independence) my own credit-offload-metrics validate-pr-1008 worker mis-attribution (b->a) that I then re-verified at source and fixed in this same PR. The §3.98 regression test needed the synthetic module registered in `sys.modules` before `exec_module` so its `@dataclass` annotations resolve under Python 3.14 (a load-mechanics quirk, de-registered in a `finally`). | Recurring this session (3rd time): the D8 root-CHANGELOG length ceiling caught my root entry as too long (>100 words), each time trimmed pre-push. The habit fix is to write the root summary tersely from the start (~75 words), reserving detail for the mirror. | Pattern: (a) a worker QA delivery independently catching an orchestrator bookkeeping slip is the offload-independence value working; re-verify at source + fix in-window. (b) Write compact root CHANGELOG entries first-time to avoid the D8 trim loop. Both caught, no escape. | Proposed improvement: none mechanical; noted the terser-root-entry habit (D8 already enforces it at pre-push). |
| 2026-07-18 | #1009 | none (gate 71 stdlib-only import audit; closes §3.95). | Built the gate stdlib-only from the start (AST, allowlist = stdlib union first-party-stems union ALLOWED_THIRD_PARTY), full 4-surface wiring + §5/§6 prose + a save/restore-isolated test; the independent verifier confirmed the AST logic across alias/dotted/relative/lazy imports and found no missed third-party import. The guard + verifier BOTH caught the same real slip: I edited the audit-programme spec body (the gate-71 §5/§6 prose + table row) without bumping the spec's OWN `Version`/`Date` (gate 40, version-bump discipline); fixed by bumping 1.17.9->1.17.10 + Date + regenerating taxonomy/portal/scorecard (the history-aware gate 40 only saw it after the bump was committed in the same commit as the body change). Also caught pre-push: a gate-20 false-trip (a "must ... TODO" adjacency in the new §6 prose, reworded declarative) and a D8 root-entry-length overrun (trimmed). | The recurring class this session: a new-gate PR that edits the SPEC to wire the gate must remember the spec is ITSELF a versioned corpus document, so the gate-71 body edit needs the spec's Version+Date co-bump + the taxonomy/portal regen, in the same commit. Easy to miss because the mental frame is "adding tooling", not "editing a versioned doc". | Pattern: wiring a gate into `governance/specification-audit-programme.md` is a versioned-corpus-doc body change, so co-bump the spec Version+Date and regenerate the derived artefacts in the same commit (gate 40/33/34). Guard-caught, no escape. | Proposed improvement: none mechanical (gate 40/33/34 already enforce it at pre-push); noted the "the spec is a versioned doc too" reminder to the new-gate close-out habit. |
| 2026-07-18 | #1008 | none (deep-assessment r5 routing; working-state only). | The QA-completion SOP worked end-to-end on the r5 delivery: consumed on arrival, both corpus findings re-verified at source (SEF-02 held title confirmed, gate count re-derived), the Medium risk-gated to the morning-review pending-decision (semantic remap not applied unattended), the COBIT Low folded into §1.16, robustness items to §3.98/§3.99, register + dated detail written, Phase 8 held for sign-off. The pre-push guard's gate 50 caught a register row-order slip (I inserted the r5 row ABOVE r4, but the deep-assessment run-table is STRICTLY ASCENDING oldest-first, unlike most reverse-chron ledgers); fixed by swapping the rows. | Two recurrences worth naming: (1) the register-table order convention (ascending, newest-at-BOTTOM) is the opposite of the DONE/improvement-log/validate-pr ledgers (reverse-chron, newest-at-top), so the reflex to prepend was wrong here, gate 50 caught it. (2) 3rd time this session (#1006/#1007/#1008) the preflight caught a detailed-CHANGELOG link to a `.working/` sibling written without the `../` prefix (from `changelog-details/`, a `.working/X` target needs `../X`); each caught pre-commit, none shipped. | Pattern: register-row-order is append-ASCENDING (a distinct convention from the reverse-chron ledgers); and detailed-mirror links to `.working/` siblings need `../`. Both are preflight/gate-caught, so no gate change needed; noted to the habit. | Proposed improvement: none mechanical (gate 50 + preflight already enforce both); the recurring `../`-link slip is a candidate for a muscle-memory note but is reliably caught by the TODO-3.34 preflight aid. |
| 2026-07-18 | #1007 | none (§1.19.7(c): the `/adopt` `.ref` bootstrap planner; closes §1.19.7 / Phase 1). | The §N-section-close discipline worked end-to-end: closing §1.19.7 + §1.12 + §3.18 in TODO, the pre-push guard's gate 18 (intra-doc-ref) caught 3 residual `§1.19.7` references (L55 range + SHIPPED-list, L413 §3.95 body) that no longer resolved to a heading; fixed by dropping the `§` per the gate's own guidance (the L67 refs stayed clean via cross-doc framework-word context, and L419/421's `§1.19.7c` via the "library" substring in `grc_library_ref`). The independent skeptical verifier INDEPENDENTLY confirmed the same one finding (it ran on the pre-fix commit), corroborating the guard. New tool built stdlib-only from the start (the #1006 yaml lesson applied); the planner is network-free + write-free by design so the copyright boundary sits in the assistant layer. | The intra-doc-ref orphan was in the FIRST commit (caught by the guard, not shipped), i.e. the §N-orphan cleanup grep was not run BEFORE the first commit for the newly-added §1.19.7 references, only the pre-existing ones. | Pattern: the §N-orphan close-out grep must cover references ADDED in the same PR (the SHIPPED-list line I added itself introduced a §1.19.7 token), not only pre-existing carriers; the guard caught it, but running the whole-repo `§N` grep after drafting (before first commit) would have caught it one step earlier. Single occurrence; guard-caught, no escape. | Proposed improvement: none mechanical (gate 18 already enforces it at pre-push); noted the "grep same-PR-added refs too" refinement to the close-out habit. |
| 2026-07-18 | #1006 | none (§1.19.7 (a)+(b): the reference-acquisition manifest generator). | The verification LAYER worked exactly as designed: TWO real defects were caught pre-merge, not shipped. (1) The post-commit `check-portability` run caught a test-isolation regression, `test_degrades_when_ref_absent` monkeypatched the shared `lint_common.resolve_sibling` without restoring it, so the alphabetically-later `ResolveSiblingTests` saw the stub; a differential clone (origin/main passed the identical sibling-free clone) cleanly isolated it as mine, fixed with a `finally` restore. (2) CI (stdlib-only) caught the generator's `import yaml`; replaced with a stdlib `_parse_catalogue` proven byte-identical to `yaml.safe_load` (`--check` zero-drift across all 632 rows + a yaml-blocked load test). Force-push was blocked in-session, so I avoided it cleanly (soft-reset to the remote tip + one fast-forward commit). | `check-portability.sh` did NOT catch the `import yaml` (it tests sibling-ABSENCE, not the stdlib-only environment; the local Python has PyYAML), so a non-stdlib import passed every local gate and failed only in CI, the local proxy has a stdlib-only blind spot. | Pattern (2nd occurrence of "local proxy misses what CI catches"): a local CI-proxy must exercise the SAME constraints CI does (here: stdlib-only, not just sibling-free), or a class slips to CI. The fix is a static gate that catches the class at run_all_audits time (earlier than either), queued as §3.95 (stdlib-only import gate). Pattern (test hygiene): a monkeypatch of a shared module global must restore, queued as §3.96 (mock.patch convention). Both caught pre-merge = the guard + CI working, NOT a degradation signal. | Proposed improvement: §3.95 stdlib-only import gate (maintainer-approved, lands after §1.19.7c) + §3.96 test-isolation mock.patch convention. |
| 2026-07-17 | #1005 | none (§1.19.7 part: the `_ref`-required loud gate). | The maintainer's correction (graceful degradation is adopter-only; for the maintainer a missing `_ref` must fail LOUD) was implemented identity-keyed off the existing detect-env classification, so it added the loud maintainer-side check WITHOUT weakening the adopter sibling-independence invariant. The skeptical verifier confirmed the gate SOUND on 8 axes and flagged one defense-in-depth note (an unlabeled fallthrough branch); I hardened it (unknown identity -> fail-safe LOUD) + added a test in the same PR before push, matching the PR's own "loud not silent for a non-adopter" philosophy. | The gate's helper originally relied on an upstream KeyError to foreclose the unknown-classification fallthrough (correct but implicit); the verifier rightly flagged it as fragile-under-future-refactor. | Pattern: a "fail loud, never silently degrade" gate should be fail-safe in its OWN logic (unknown -> loud), not rely on an upstream guard; a verifier's defense-in-depth note on such a gate is worth acting on even when currently-unreachable, because it IS the same defect class the gate exists to prevent. Single occurrence. | Proposed improvement: none beyond the applied hardening; the verifier-note-to-same-PR-hardening loop worked. |
| 2026-07-18 | #1038 | Wired the maintainer-alert watchdog SOP into the orchestrator discipline across four surfaces (maintainer-directed, co-designed mid-session). | A maintainer-co-designed SOP was implemented consistently across all four surfaces (CLAUDE.md `## Credit-offload mode`, resume.md step 3, the `_private` design-of-record, the scratch convention) and refined live as the maintainer sharpened it (from an orchestrator-auto-clear to the stronger maintainer-decides-clearing-via-a-choices-question, with an explicit overnight carve-out that respects the no-blocking-prompt-unattended rule). The SOP's FIRST exercise (assess + advise + clear the three real 2026-07-18 watchdog alerts on maintainer authorization) demonstrated it end-to-end. | Preflight caught path-shaped filename tokens (`MAINTAINER_ALERT.md`, cross-repo `.md` names) in both the root and detailed CHANGELOG added lines (de-path-shaped to prose, since scratch/`_private` files are not repo-internal-linkable); D8 caught the root entry at 123 then 103 words (compressed to 93). Both pre-push, no escape. | Pattern: (a) a multi-surface prose SOP must be self-checked for consistency across EVERY surface it touches (here four, two of them cross-repo); (b) root CHANGELOG entries avoid path-shaped filename tokens for non-repo-internal files (de-path-shape to prose or link only repo-internal targets) and stay <=100 words with no sentence over 45; (c) a maintainer refining a directive across several messages means re-editing the same surface more than once, so the FINAL state must be reconciled (the auto-clear text was fully replaced, not appended to). | Proposed improvement: none mechanical (preflight + D8 caught the CHANGELOG-hygiene slips; the multi-surface consistency is a self-check the tiered-verifier standard already covers). |
| 2026-07-18 | #1037 | §1.19.10 slice 2: the DESTRUCTIVE tiered public-CHANGELOG migration + full 1020-entry per-PR source moved to grc_library_private; closes §1.19.10. | The emit-verify-then-prune ordering held on a destructive public-facing change: the full source was preserved verbatim in _private FIRST (byte-identical, validate-clean), THEN the public root was pruned, so data was never at risk. The judgement-heavy authoring (6 weekly summaries condensing 839 per-PR bullets) was offloaded as a worker research-seed DRAFT and re-authored by the orchestrator against the source bullets (research-assistant discipline), which made it tractable while keeping accuracy. A pre-push skeptical verifier CONFIRMED both data-safety (byte-identical current block, perfect range partition, 0 loss vs the _private source) AND per-claim accuracy of the authored prose. | THREE defects caught pre-push, 0 escapes: (1) inserting the #1037 detailed-mirror entry OVERWROTE #1036's entry (my Edit old_string matched only #1036's header+lead, replacing them with #1037 and orphaning #1036's body); gate 59 caught it, #1036 restored. (2) D8 PR-time check caught the #1037 compact entry's 47-word sentence (>45); split. (3) the verifier caught a 5-sentence weekly paragraph (spec cap is 4); merged. | Pattern: (a) when PREPENDING a detailed-mirror entry above an existing one, the insertion must not swallow the prior entry's `##` header, or the prior entry is orphaned (header-less body); gate 59 is the backstop but the habit is to anchor the insert on the prior header and re-verify it survives. (b) A destructive public-facing migration warrants preserve-to-durable-store-FIRST + a full skeptical verifier on BOTH conservation and authored-prose accuracy. (c) Public-facing authored summaries must meet the sentence-COUNT cap (weekly paragraphs) AND the per-sentence word cap (D8 compact entries), which are different limits on different tiers. | Proposed improvement: none mechanical (gate 59 + D8 + the verifier caught all three pre-push). Reinforced: anchor a mirror-entry prepend on the prior header and confirm it survives; emit-verify-prune for destructive moves; author to both the sentence-count and word-length limits. |
| 2026-07-18 | #1036 | §1.19.10 slice 1b: broadened the projection tool's ENTRY_RE so the PR-number segment is optional, fixing the drop of the 34 earliest pre-PR-number entries. | The defect (surfaced in slice 2 by examining the scaffold buckets) was fixed at the width of the class: the regex was made PR-number-optional and conservation was re-defined over ALL 1019 entries (180 current + 839 weekly), not the PR#-bearing subclass. The slice-1b verifier returned CONFIRMED-CORRECT on every axis (regex adversarially safe, conservation holds, 0 drop / 0 dup), and the offloaded #1035 validate-pr (worker-a, routine) INDEPENDENTLY confirmed the drop and found no other projection defect, corroborating both the catch and that the fix is complete. | Minimal: guard green, verifier clean, worker corroboration clean. Preflight caught the usual path-shaped command spans in the drafted detailed entry; fixed pre-push, no escape. | Pattern: a fix for a "conservation" defect must widen the conservation universe itself (full entry set), not just patch the one dropped range; and examining a generated artefact's actual buckets before a destructive migration is the habit that surfaces such a defect (reinforced from the #1035 retro). | Proposed improvement: none mechanical; the examine-before-destructive-migration and full-universe-conservation habits are the durable controls. |
| 2026-07-18 | #1035 | §1.19.10 slice 1: tiered public-CHANGELOG projection scaffold tool (non-destructive). | The scaffold-tool design cleanly separated the mechanizable part (grouping + tier structure + PR-range headers + raw per-entry bullets) from the authored part (the <=4-sentence weekly summaries), so the tool is testable + the human authors only the condensation. The "reshape D8" spec assumption was source-verified UNNECESSARY (D8's ENTRY_RE already skips paragraph headers), documented rather than changed. | The slice-1 verifier returned CONFIRMED-CORRECT but MISSED that the projection dropped the 34 pre-PR-number entries (no `\| PR #N`): it defined "conservation" over PR#-bearing lines only (984=179+805, true for that subclass but blind to the 34). I caught it in slice 2 by EXAMINING the scaffold's weekly buckets before migrating (the oldest bucket was week 06-15/#38, nothing for 05-31..06-14); fixed in slice 1b (#1036). | Pattern: a "conservation / no-loss" verification is only as complete as its UNIVERSE definition; the verifier's conservation held over PR-entries but the true universe was ALL 1019 entries incl. 34 no-PR ones. Same class as the completion-grep scope-width discipline (verify over the FULL set, not the matched subclass). The examine-the-output-before-a-destructive-migration habit is what surfaced it. | Proposed improvement: none mechanical; reinforce (a) define a conservation check's universe as the full entry set, not a regex-matched subclass, and (b) examine a generated artefact's actual buckets before running a destructive migration on it. |
| 2026-07-18 | #1034 | §1.19.9 PR B2: ran the dated-archive MIGRATION (aged working-records -> grc_library_private), closes §1.19.9. | The guard-first sequence culminated cleanly: a large DESTRUCTIVE cross-repo migration (1217 rows + 189 entries + 53 files) ran on already-verified machinery (PR B1's data-safety verifier), the emit-verify-prune worked, gate 50's floor + gate 59's cutoff absorbed the shrink, and a dedicated data-loss verifier confirmed row-by-row conservation (0 lost / duplicated). Zero data loss, gate-clean 72/72. | The guard's D2 caught 4 MORE registers (claim-fit/guardrail-reviews/matrix-fit/validate-sweeps history.md) that the prune's link-DELINKING changed but whose Version I had not bumped; the _private validate flagged the canonical AWS example key (from the deep-assessment secret-gate probe) in swept content. Both caught pre-push, fixed, no escape. | Pattern: (a) a prune that DELINKS history.md links (not only row-sweeps) also changes those registers, so EVERY touched register with a Version field needs a bump (D2 catches at PR-time); (b) swept `.working/` QA content carries example / mutation-test secrets, so the receiving `_private` store must inherit grc_library's `.working/` secret-exemption for the archive subtree (done). | Proposed improvement: none mechanical (D2 + the _private validate + the verifiers caught everything pre-push). The guard-first + deterministic-tool + emit-verify-prune + independent-data-loss-verifier stack made a 1459-artefact destructive cross-repo migration safe on a very long turn, reaffirming the high-assurance principle (apply-correctness independent of orchestrator in-context precision). |
| 2026-07-18 | #1033 | §1.19.9 PR B1 (tool-only): generalize the working-records sweep tool (rename to -to-private + aged roll-up-row sweep for the 2 gate-50 registers). | Guard-first paid off end to end: verifying the sweep TOOL's row-partition data-safety (a dedicated data-loss-focused pre-push verifier, CONFIRMED-CORRECT on every axis) BEFORE PR B2 ran it destructively meant the 1217-row migration ran on already-proven machinery. The deterministic partition + re-parse assertions + emit-verify-prune made apply-correctness independent of orchestrator in-context precision (the high-assurance principle) on a very long turn. | F1: the tool-name repoint (`-to-scratch.py` -> `-to-private.py`) missed the destination-NAME "scratch" prose tokens in 3+ changed files (a README "the scratch archive", the docstring); the pre-push verifier's bare-token `scratch` pass caught them all pre-push, no escape. The recurring multi-surface / bare-token-width incompleteness class. | Pattern: a rename that changes a CONCEPT (destination scratch->private), not just an identifier, must bare-token-sweep the CONCEPT NAME ("scratch") across the changed prose, not only the identifier (the filename); repointing the path token leaves the destination-name tokens stale. | Proposed improvement: none mechanical (the verifier's bare-token pass + the close-out checklist's bare-token-width line already cover it; reinforced). Reaffirms guard-first: prove destructive machinery on a verifier before running it. |
| 2026-07-18 | #1032 | §1.19.9 PR A (guard-first): gate 50 per-register dynamic floor for the dated-archive sweep. | The guard-first sequencing worked cleanly: landing gate 50's dynamic floor BEFORE the sweep (PR B) means the sweep can move aged rows without the gate flagging them, and the floor is inert (equals INCEPTION) until a sweep runs, so the PR is behaviour-neutral and low-risk. A read-only mapping subagent + independent source-verification of each gate CORRECTED the spec's inexact "gates 50, 60, D5" list (only gate 50 reads aged roll-up rows; gate 60 is newest-row-only; D5 reads only the PR diff), avoiding two wrong/no-op cutoffs. Pre-push verifier CONFIRMED-CORRECT on all lenses. | Minimal. The offloaded #1031 validate-pr (worker-b) ran the review in its main loop rather than the requested Subagent A; result was correct + independently corroborated, so accepted as a clean elevated pass but flagged (watch for recurrence). | Pattern: source-VERIFY a spec's mechanical claims (here "which gates read the swept rows") before implementing them; a co-designed spec can name the wrong surfaces, and a mapping subagent + independent at-source re-derivation is the control that caught it (mirrors the research-assistant discipline: the worker's map is a hypothesis until re-verified). | Proposed improvement: none mechanical. Reinforces: guard-first (land the gate change before the data change) + source-verify spec mechanics before building. |
| 2026-07-18 | #1031 | Deep-assessment findings-fix (maintainer-directed §1.19.8 QA follow-up; not a numbered TODO closure). | The maintainer-directed §1.19.8-change-set deep-assessment gave the intended peace-of-mind: 7 low-severity findings, 0 High, all fixed in-window; the pre-push skeptical verifier + a class-width re-grep then caught 6 SAME-CLASS stragglers the deep-assessment's `.working/`-prefix reference hunt had missed, all fixed before push. Net zero escapes across the whole §1.19.8 arc. | The pre-push guard's D2 check caught 2 `.working` registers (improvement-log, validate-pr/history) whose Version I had not bumped after adding rows (they carry Version fields even though `.working/` is corpus-gate-exempt); preflight caught path-shaped command code-spans in the detailed-CHANGELOG entry. Both caught pre-push, no escape. | Pattern: (a) a reference/completion hunt keyed on a PATH PREFIX misses the same doc cited by bare basename or via a paired command, so a class-width (bare-token + file-type + sibling-command) grep is the control; (b) a `.working` register with a Version field still needs a per-PR Version bump (D2 enforces it at PR-time though the corpus gates exempt `.working/`); (c) freshly-authored detailed-CHANGELOG entries recurrently include path-shaped command spans that preflight flags. | Proposed improvement: none mechanical (D2 + preflight + the pre-push verifier reliably catch all three; more machinery would be redundant per gate-discipline). Standing habits reinforced: run moved-doc cleanups at class width; bump `.working` register versions when adding rows; write CHANGELOG file-refs as links or prose. |
| 2026-07-18 | #1030 | §1.19.8 (CLOSED, rotated to DONE) via the `_private`-pointer minimization (approved hybrid). | The hybrid minimization landed clean: ONE CLAUDE.md delegation directive replaced ~20 inline `_private` pointers + 4 overlay + 1 spec pointer (collapsed to bare concern-names), the 3 commands kept functional reads + gained a one-line opener, and the read-evidence discipline was added; the whole-repo bare-token re-grep confirmed no residual dead pointer at merge. The maintainer-directed §1.19.8-change-set deep-assessment then gave independent peace-of-mind, confirming the close was substantively clean (7 low-severity findings, 0 High), all fixed in #1031. | The minimization's `.working/`-prefix-oriented reference hunt (and the deep-assessment's) missed BARE-BASENAME references to moved docs (`session-metrics.md` in CLAUDE.md + resume.md; the `/guardrails` paired command; the W1 sibling docstrings) that only a bare-token + file-type-width grep catches, the recurring pattern-width/scope-width class. All caught by the #1031 pre-push verifier + class-width re-grep; none escaped. | Pattern: a completion/reference-hunt grep keyed on the `.working/<path>` prefix systematically misses the same doc cited by BARE BASENAME or bare concern-stem, and the paired COMMAND of an edited SKILL is a recurring sibling carrier. The class-width (bare-token + file-type + sibling-command) grep is the control the prefix-scoped hunt lacks. | Proposed improvement: none mechanical (these surfaces are gate-exempt `.claude/`/`.working/`/pack by design; a gate would be decorative per gate-discipline). Standing habit reinforced: on any moved-doc/rename cleanup run the completion grep at BARE-TOKEN + FILE-TYPE width AND sweep the paired command of every edited skill (the close-out checklist's file-type-width + §N-orphan lines). The #1031 pre-push verifier catching what two prior QA passes missed reaffirms the independent-adversarial-verifier value. |
| 2026-07-18 | #1029 | §1.19.11 (closed) + the layered `_private`-required assurance built. | The high-assurance-style build worked end to end: the sensitive tool-blocking hook was designed fail-OPEN-on-uncertainty from the start (deny ONLY on confirmed maintainer-origin + confirmed `_private`-absent), self-tested per-path (7 cases incl. a lookalike-owner), and a dedicated skeptical verifier traced every path confirming NO bricking risk; the offloaded validate-pr-1029 (worker-a, elevated) independently corroborated CLEAN. Part 0's `_private`-gate verifier caught a REAL dead-`-ise`-stem defect (the gate silently not enforcing 5 spellings) BEFORE it could give false confidence. | The concurrent-verifier fixture race fired again (a mid-verifier-run amend caused a transient `tests/tmp/*` regression FAIL in the guard), confirmed a concurrency artefact by a standalone re-run (the documented shared-tree class); and the D8 root-entry ceiling fired once (101 words, trimmed to 97). Both caught pre-push, no escape. | Pattern: (a) for a session-tool-blocking hook, fail-OPEN-on-uncertainty + deny-only-on-positive-confirmation is the design that makes it un-brickable, and a per-path self-test + an adversarial verifier are the proof; (b) the concurrent-verifier + orchestrator-amend fixture race is a well-understood artefact (a standalone re-run confirms), so avoid amending mid-verifier-run where practical, or re-run the guard clean after the verifier finishes. | Proposed improvement: none mechanical; the hook's fail-open design + self-test + verifier are the controls. Reaffirms a durable principle: a mechanical guardrail should fail OPEN on its OWN malfunction (fail loud only on the real condition it guards), so a guardrail bug is never worse than the risk it prevents. |
| 2026-07-18 | #1028 | §1.19.8 relocation (relocation + gate-critical repointing done; the _private-integration wiring deferred, then built as the layered assurance in #1029). | Shipped with heavy pre-merge verification: two adversarial verifiers (missed-reference + repoint-correctness) PLUS the orchestrator's own whole-repo bare-token re-grep caught FOUR classes before push, four missed PROJECT-OVERLAY repoints, a fabricated "49" count (true measured 51), two live handoff/TODO strays the missed-reference verifier had WRONGLY cleared, and a stale draft count. The offloaded post-merge validate-pr-1028 then returned CLEAN, corroborating. Net zero escapes. | The link-form repoint transform UNDER-REACHED three ways (missed the PROJECT-OVERLAY blocks not in its target-file set, a bare-code-span TODO ref, and two handoff link-pointers), and the drafted accounting prose ("49") was unmeasured. The D8 length ceiling also fired on 5 historical entries pulled into the diff by the dead-link fixes, each compacted. All caught in-window. | Pattern: a deterministic link-form transform is only as complete as its target-file set AND its link-form assumption; references also live as bare code-spans, in gate-37-stripped overlay blocks, and as links in live `.working/` files. The orchestrator's OWN bare-token whole-repo re-grep (trusting neither the transform NOR a verifier's clear, the missed-ref verifier wrongly cleared the handoff) is what caught the residue. And the meta-prose measured-not-inferred guard recurred (the "49"). | Proposed improvement: for any repoint/rename, run the whole-repo bare-token re-grep across ALL reference forms (link, bare code-span, prose) and ALL file types BEFORE trusting a transform's per-file counts, and MEASURE any count claim from that grep output (never draft a count from a transform's changed-line total). Both are existing disciplines (file-type-width grep; measured-not-inferred); this is reinforcement, no new machinery. A verifier's "clear" on a surface is itself a hypothesis to re-grep, not a fact. |
| 2026-07-18 | #1027 | none (record the completed §1.19.12 CLAUDE.md-trim review; working-state + versions). | The one-by-one AskUserQuestion review confirmed all 32 verdicts with a single change (Date/timezone -> KEEP whole), producing a maintainer-confirmed FINAL classification (2 MOVE / 17 KEEP / 13 SPLIT); the two #1026-added sections were classified in the same pass. Clean bookkeeping; the self-run `/validate-pr` (batched into #1028) surfaced only 2 Low working-state items, both fixed in-window in #1028. | No material friction at #1027 build time. The 2 Low items THIS PR's validate-pr surfaced were both working-state: a stale "15 KEEP" count left in the draft's retained #1025-era LOCKED paragraph after a new "17 KEEP" FINAL block was prepended above it, and a next-prs.txt first line grown to 150 chars (> the ~120 statusline guideline). | Pattern: the stale-count-in-a-retained-paragraph is another instance of the **#619 append-not-reconcile class** (a new FINAL/status block prepended above an older LOCKED paragraph whose count is left stale), now re-seen inside a status DRAFT rather than a snapshot line; the class recurs across surface types. | Proposed improvement: when prepending a new FINAL/status block above a retained LOCKED paragraph, reconcile OR explicitly mark-historical the old paragraph's counts in the SAME edit (the append-not-reconcile guard, extended from snapshot lines to in-doc status blocks). Caught in-window by the self-run validate-pr, so no escape; a drafting habit, no new machinery. |
| 2026-07-18 | #1026 | §1.15 (cross-repo write-safety guardrail, part b, closing the two-part item). | The protected CLAUDE.md + pack bundle landed clean under the new-pack-prose discipline: `lint-language` run on the new pack section BEFORE the first commit, and the two pack copies confirmed byte-identical before push, so gate 37 passed first try; the skeptical verifier confirmed 7/8 targets clean (the one DEFECT-FOUND was the deliberately-not-yet-added #1025 QA rows, added on its return). The item-11/12 additions were also fed straight into the §1.19.12 re-assessment per the maintainer's "re-run the assessment once we add items" directive. | Two recurrences of the SAME class this PR, both caught in-window: (a) preflight tripped SIX times on detailed-mirror PROSE that backticked file paths (`.claude/CLAUDE.md`, `lint-language.py`) or used a wrong relative link target, and (b) next-prs.txt accumulated a stale second `# then:` line because I edited only its first line across PRs. | Pattern: **detailed-CHANGELOG-mirror prose keeps backticking bare file paths/names that preflight flags as unlinked references** (recurred across #1023/#1024/#1026; the mirror IS preflight-scanned per TODO 3.34). And **next-prs.txt drifts when only its first line is edited** (the `# then:` line goes stale / duplicates). | Proposed improvement: (a) in detailed-mirror PROSE, either markdown-link a path-shaped reference or write the name WITHOUT backticks (reserve backticks-with-links for the `### Changed` bullets); run the changelog preflight (`&&`-gated, never `;`-chained) before EVERY commit so it blocks, which it now does. (b) When refreshing next-prs.txt, REWRITE the whole file (both the `next:` and the single `# then:` line), not just line 1, so no stale `# then:` accumulates. Both are drafting habits; no new machinery. Also reaffirmed: chain preflight with `&&` not `;` (a slip earlier this session let two commits through on a non-zero preflight; the `&&` form is now consistently used). |
| 2026-07-18 | #1025 | none (lock the §1.19.12 CLAUDE.md-trim classification + flip to attended-autonomous + log :333; working-state). | The AskUserQuestion-blocked-by-stale-mode-field problem was diagnosed and fixed cleanly: the hook reads the on-disk `Operating-mode`, so flipping the working-tree field (committed on the branch) unblocked AskUserQuestion mid-session without waiting for merge; confirmed by putting the real :333 decision through it successfully. The §1.19.12 classification lock was surfaced with per-decision-point resolutions (provenance KEEP, /screen-publications KEEP after a live re-read, session-migration bullet-wise). | The self-run validate-pr caught a #619 append-not-reconcile slip in `.working/session-state.md`: I reconciled the FRONT of the multi-clause `Current-task` line (maintainer-returned, §1.19.12-locked) but left three stale tail clauses ("pending-decisions EMPTY" while the same PR added :333; "§1.19.12 HELD" after locking it; "daytime-unattended" after the flip), a within-line self-contradiction. Caught in-window, fixed in #1026. | Pattern (a RECURRENCE of the #619 class, now re-seen): reconciling only the FRONT of a long multi-clause snapshot line and leaving stale clauses in the tail. The `Current-task` line is a dense single line with many clauses, so a front-edit misses the tail. | Proposed improvement: when reconciling a multi-clause snapshot line (session-state `Current-task`, the handoff `Current truth`), re-read the WHOLE line end-to-end and reconcile EVERY clause to the state the PR produces, not just the lead, per the close-out checklist's handoff-reconcile line (#619/#622/#628). The `Current-task` line is a candidate to break into bullet clauses so a stale tail clause is visible; noted, no change this PR. Caught by the self-run validate-pr, so no escape. |
| 2026-07-18 | #1024 | §1.21 (three non-existent ISO/IEC 27002:2022 sub-controls corrected in the logging standard). | Guard-first held-source verification: the fix was derived by reading the held ISO/IEC 27002:2022 (8.15.x confirmed non-existent; 8.17 = Clock synchronization; 8.15 = Logging with a "Protection of logs" subsection + a 5.28 retention cross-ref), so the two semantic-fit `->8.15` remaps rested on the standard's own structure, not inference. The skeptical verifier and the self-run validate-pr both returned SHIP; the #1023 count-gloss re-enumeration held (validate-pr independently re-counted exactly five SEF-02 carriers, no recurrence of the count-gloss drift). | Nothing material: a clean PR (0 verifier findings, 0 validate-pr findings). The count-gloss habit introduced after #1022/#1023 held on the first test. | Pattern: none new; a POSITIVE confirmation that the two recurring session patterns (D-check drafting slips, count-gloss drift) did NOT recur on #1024, so the drafting habits (pre-commit D8 check; enumerate-exactly-N glosses) are working. | Proposed improvement: none; the guard-first held-source method + the now-applied drafting habits produced a clean PR end-to-end. Reaffirms, no new machinery. |
| 2026-07-18 | #1023 | §1.20 (adopt-preflight-guard smoke test made adopter-portable). | The fix was designed from the guard's actual contract (read `classify()` returns `str | None`; `main()` returns 0 iff `"adopter"` else 3), so the rewritten test asserts rc-consistency with the live classification and is portable across maintainer / CI / adopter clones, restoring the adopter-portability invariant validate-pr-1019 flagged. The self-run validate-pr confirmed the rewrite is sound and not trivially-passing (asserts on every branch, would catch a `main()` regression). | The self-run `/validate-pr` caught a second count-gloss issue in my bookkeeping (a 2nd-occurrence signal after #1022's "seven"->"five"): the abbreviated SEF-02 gloss in DONE + the #1022 validate-pr row named ~4 carriers while the count said "five" (the incident-response *procedure* row was ambiguously folded into "incident-response rows"). Count correct, gloss under-enumerated; fixed in #1024. | Pattern (2nd occurrence this session, now a SIGNAL): **abbreviated count-glosses in audit-trail prose drift from the stated count** (#1022 "seven" over-counted the total-vs-subset; #1023 gloss under-enumerated 5-as-4). Both are the meta-prose measured-not-inferred guard's count-and-label-granularity sub-shape, both narrative-only, both caught in-window by the self-run validate-pr (no escape). | Proposed improvement: when an audit-trail clause pairs a COUNT with an abbreviated enumeration/gloss, make the gloss enumerate EXACTLY that many distinct items (or drop the gloss and cite the authoritative enumeration, e.g. the detailed-CHANGELOG line-numbered list) - a drafting habit, no new machinery; the self-run validate-pr already catches it. If it recurs a 3rd time, consider a mechanical count-vs-enumeration check (already tracked as TODO §3.32). |
| 2026-07-18 | #1022 | none (the deep-assessment r5 Medium-1 routed fix: CSA CCM `SEF-02` -> `SEF-07` on the incident-escalation SOP + its register row; a pending-decisions item, not a numbered TODO). | The corpus-wide bare-token `SEF-02` grep was load-bearing: it distinguished the ONE wrong carrier (the incident-escalation SOP, where SEF-02 = "Service Management Policy and Procedures" is the wrong control) from FIVE correct ones (the matrix IT-Service-Management / Service-Level-Management rows genuinely ARE SEF-02), preventing an over-reach that a naive "replace all SEF-02" would have caused. The skeptical verifier independently confirmed SHIP against held CCM v4.1.0 (title exact-match, no over/under-reach). | The `/validate-pr` (self-run) caught a count error in my OWN audit-trail narrative: I wrote "seven other SEF-02 carriers left untouched", but seven was the PRE-FIX total; the untouched subset is five (5 + 2 fixed = 7). Off-by-2, narrative-only (no corpus/gate effect), fixed in #1023. | Pattern: the **meta-prose measured-not-inferred guard's count-and-label granularity sub-shape** (a count stated next to an enumeration at a different granularity): "seven OTHER carriers" conflated the pre-fix TOTAL (7) with the untouched SUBSET (5). I inferred "seven" from the grep's total-hit memory rather than recounting the specific subset the sentence describes. Single clear occurrence; the verifier caught it in-window (no escape). | Proposed improvement: when an audit-trail clause states "N other/remaining X left untouched", recount that exact SUBSET with a same-turn `git grep` of the post-change state (not the pre-change total), per the meta-prose measured-not-inferred guard. No new machinery; a measure-the-subset drafting habit. Reaffirms the standing guard rather than adding to it. |
| 2026-07-18 | #1021 | none (the loop-break Sweep 112 `/validate` close-out; first PR of the 2026-07-18 resumed session: consume Sweep 112 CLEAN + prune handoff + acquire lease + route S112-1 to new TODO §1.21 + add the #1020 handoff-exemption row). | The elevated-QA consume of the offloaded Sweep 112 worked end-to-end on worker-a's delivery-1-this-session: proof-of-run genuine, independent re-derivation of every mechanical fact (HEAD / 72-72 maintainer-clone / counts / parity / versions all exact-match), and a dedicated delivery-1 false-negative auditor that not only confirmed the delta CLEAN but surfaced a REAL out-of-window citation defect (S112-1, three non-existent ISO 27002:2022 sub-controls in `standard-logging-and-monitoring.md`) that both the worker's sweep (correctly, out-of-window) and my own scope-limited residual grep (which covered only `operations/`+`dev-security/`) had missed. The auditor earned its keep. | Two in-window slips, BOTH caught pre-push by the guard (the verification layer working): (a) the #1020 session-closing-handoff PR omitted its OWN validate-pr handoff-exemption row, so gate 50 flagged #1020 the moment this PR demoted it from highest-numbered (the #445/#450 recurrence class), fixed in-window here; (b) the #1021 root CHANGELOG entry ran over the D8 100-word ceiling, caught at the pre-push guard and compressed (the 3rd D-check drafting slip this session after #998 D4 and #1001 D8). | Pattern: (a) **the session-closing handoff PR recurrently omits its own validate-pr exemption row** (#445/#450 class, recurred at #1020) - the row IS knowable at close (SKIPPED + compensating control = next-resume `/validate`), but the closing session keeps forgetting it, and gate 50 only catches it one PR into the NEXT session; the catch is reliable and the in-window fix cheap, so no escape, but the recurrence persists. (b) **D-check drafting slips are now a 3rd-occurrence session pattern** (D4 #998, D8 #1001, D8 #1021), each guard-caught, each costing an amend. (c) my own residual-grep for a citation sweep was **scope-limited** (2 dirs), and the auditor's corpus-wide check caught the carrier I would have missed - the file-type/scope-width lesson, applied to my own verification not just the corpus. | Proposed improvement: (a) add the session-closing handoff PR's OWN validate-pr exemption row as an explicit line in the close-out checklist's handoff-PR step (it is knowable at close; currently added only retroactively at the next resume when gate 50 flags it) - a candidate for the protected-apply item 12 QA-completion codification, or a small mechanical check that the highest-numbered PR, when it is a session-closing handoff, carries its own marker row. (b) reaffirm the #1001 shift-left habit: run the D8 word/sentence count on a drafted root CHANGELOG entry BEFORE the first commit (I did not, and hit it again). (c) run citation residual-greps corpus-wide (no dir scoping) - the same bare-token/file-type-width discipline the corpus grep guard already mandates, applied to the orchestrator's own pre-push verification. |
| 2026-07-17 | #1004 | none (website: reduce the `.hero h1` clamp so the For-AI title fits the band on tablets). | Evidence-grounded root-cause: rather than just shrinking the font blindly, I read the CSS and found the real interaction (`&nbsp;`-joined 31ch token defeating `max-width:18ch` + `text-wrap:balance`, overflowing at tablet width), which let me size the clamp reduction with margin and explain it precisely. Deploy confirmed LIVE by fetching the site (new clamp in the shared inlined CSS, old value gone). | I cannot render the maintainer's iPad viewport, so the fix's SUFFICIENCY on the exact device is unverified by me (flagged to the maintainer to confirm; calculation gives comfortable margin at 768-1024 px). A cache-buster query string on the fetch hit Cloudflare's bot-challenge page (563KB), a red herring; the plain URL fetched correctly. | Pattern: for a visual/CSS fix the orchestrator cannot see rendered, the honest close is "fixed by calculation + deploy-confirmed-in-DOM, maintainer confirms the pixels" rather than asserting it looks right. Single occurrence; the flag-and-confirm handling was correct. Also: avoid arbitrary query strings when curling the Cloudflare-fronted site (they trigger the challenge page); fetch the plain URL. | Proposed improvement: none material; noted the plain-URL curl habit for the site. The residual `&nbsp;`/`max-width` root cause (phone widths) is tracked (§3.94-adjacent) as the follow-up if the clamp alone proves insufficient. |
| 2026-07-17 | #1003 | none (§3.93 parts a+b: codify the sync-scratch-every-PR discipline). | Closed the loop on a 3rd-occurrence recurrence (the stale-scratch-read): rather than write another memory (one already existed and did not prevent recurrence), the fix became a forcing function in the three standing surfaces (close-out checklist, credit-offload section, /resume step 3). When the maintainer suggested folding the small §3.93 into "the next PR", I surfaced that the next queued item (§1.19.7) turned out to be substantial (a 696-source manifest + /adopt + copyright guardrail), so combining them would make a large mixed PR contrary to the "small, no confusion" intent; the maintainer then chose §3.93-as-its-own-small-PR (with the QA batch) + §1.19.7 separate. | None material. The §3.93 prose spans two `.claude/` surfaces (CLAUDE.md close-out + credit-offload) plus resume.md; keeping the three consistent needed care, but they are convention-guarded (no parity gate on `.claude/` prose), so a light self-review sufficed for a discipline-codification prose change. | Pattern: a maintainer instruction to combine a small item can rest on an out-of-date view of the OTHER item's size; surfacing the changed premise (the next item is larger than assumed) before executing is the surface-counterproductive-instructions discipline, and it let the maintainer re-decide cleanly. Single occurrence this session, noted. | Proposed improvement: none; the proactive-surfacing worked. The §3.93(c) mechanical backstop (the queue tool auto-fetching origin) remains queued as a `grc_library_scratch` PR to complete the recurrence-prevention. |
| 2026-07-17 | #1002 | none (website: "For AI" site-wide footer link, a maintainer-flagged sidebar-overflow mitigation; + TODO §3.94). | Evidence-grounded diagnosis before any fix: the maintainer reported the sidebar links "missing", but fetching the live site + a fresh build confirmed both were in the DOM, and the CSS pinpointed the real cause (the sidebar's `max-height: 100vh` inner-scroll fold as the nav grew), so the fix targeted the actual problem (a persistent footer link) rather than the misread symptom. The fix was confirmed LIVE by curling the deployed page (footer For AI at line 901). | When I tried to defer #1001's recursion-avoidance QA rows to a later PR, the pre-push guard's gate 50 (bookkeeping-parity) correctly failed the build (#1001 demoted from highest-numbered with no `/validate-pr` row), forcing the rows into #1002 as the discipline intends. The website detailed-CHANGELOG entry also tripped the preflight on an em-dash + unlinked path-shaped references, fixed before commit. | Pattern: gate 50's "prior PR needs its QA row in THIS PR" is not deferrable across a PR (a batch-to-next-next-PR shortcut fails the guard); the recursion-avoidance batch must land in the immediately-next PR. Single clear occurrence (a useful confirmation the guard enforces the batching, not just convention). | Proposed improvement: none beyond the existing guard, which worked exactly as designed (caught the deferral pre-push, no escape). The lesson (recursion-avoidance batch rides the immediately-next PR, offload the prior PR's `/validate-pr` early so it is consumed by that boundary) is already the standing close-out convention; reaffirmed, no new machinery. |
| 2026-07-17 | #1001 | §1.17 (r4 confirmed gate-blind citation-accuracy fixes W1/N2/W2 + S110-1; the r4 Phase-8 sign-off). | The pre-merge skeptical verifier confirmed all four fixes correct at held source with concrete quotes (800-52r2 §3.3.1.2 lists the AES suites and 0 ChaCha20; every remapped CSF 2.0 code exists in CSWP-29; 8.17 is Clock synchronization; ID.AM-02 is software inventory), including a corpus-wide grep proving 0 surviving 1.1-legacy IDs. The one genuinely-authorial cell-set (the N2 CSF 1.1->2.0 mapping, where Time-synchronization has no clean 2.0 subcategory) was surfaced to the maintainer with a concrete recommended mapping and confirmed before any table edit, so the fix landed right the first time. | The root CHANGELOG #1001 entry's longest sentence was 46 words, one over the D8 45-word ceiling; the pre-push guard's D8 check caught it and it was trimmed and the commit amended before push (no escape). | Pattern (2nd occurrence this session, a SIGNAL: #998 had a D4 co-bump miss, #1001 a D8 sentence-length miss): a drafted root CHANGELOG entry recurrently trips a D-check at pre-push (length, dash, co-bump). Each is caught by the guard, so no escape, but each costs an amend cycle. | Proposed improvement: when drafting a root CHANGELOG entry, run the D8 word/sentence count (and the D3 dash grep) on the entry BEFORE the first commit, not just at pre-push, the same shift-left the preflight-changelog aid already gives for dashes and links; extend the pre-commit habit to a quick sentence-length check. No new machinery (D8 already gates it); a drafting-order habit. |
| 2026-07-17 | #1000 | none (the loop-break Sweep 111 `/validate` close-out; first PR of the 2026-07-17b resumed session: lease acquire + resume-cursor advance + TODO §3.93). | The offloaded Sweep 111 came back a clean pass and the elevated-QA consume confirmed it end-to-end: independent re-derivation of every mechanical fact (HEAD / 70-70 / pre-flight 422-32-11 / counts / versions all exact-match) plus a dedicated delivery-1 false-negative auditor (10 adversarial checks) returned CLEAN VERDICT HOLDS. The loop-break control for #999 passed; the pre-push guard was green first try. | At `/resume` I read the scratch coordination plane (`list-workers` / `list-pending` / `results/`) from the UN-SYNCED local scratch checkout, which was 16 commits behind `origin/main`, and so wrongly reported both workers stale and the pre-positioned Sweep-111 order unclaimed, and asked the maintainer to restart the workers, when worker-a had already claimed and delivered Sweep 111 on `origin/main`. Self-caught after a scratch `git fetch`, but it cost the maintainer a needless round. | Pattern (3rd occurrence, now firmly a PATTERN): reading scratch queue/worker/results state from the local checkout without fetching first (flagged 2026-07-16; recurred here). A dedicated memory (`read-origin-not-stale-worktree`) already existed and did NOT prevent recurrence, so a note is proven insufficient; the control must be mechanical / procedural. The local scratch checkout does not auto-sync, but the credit-offload reads operate on it. | Proposed improvement: routed as TODO §3.93, three layers: (a) the maintainer-directed PRIMARY rule, **sync scratch every PR at close-out** (`git fetch && git reset --hard origin/main`), delivery pending or not, added to the CLAUDE.md close-out checklist; (b) mandate the scratch fetch (and a per-tick fetch in any poll loop) before any coordination read at `/resume` step 3 + the credit-offload discipline; (c) the mechanical forcing function, `credit-offload-queue.py` `list-workers`/`list-pending` auto-`git fetch` origin before reporting. Codification is the next PR (§3.93). |
| 2026-07-17 | #998 | none (§1.19.6 `/adopt` skill + command + matcher hardening + `/resume` adopter-path; the largest Phase-1 item). | The layered QA worked end-to-end on the session's hardest item: the gate-60 cadence backstop CORRECTLY mandated a `/guardrails` review (the skill+command drove drift to 3), which caught 9 genuine findings on the new machinery (6 fixed in-window, 3 routed §3.92); the skeptical verifier then found 0 residual defects; the pre-push guard caught the last slip (D4). Net zero escapes across the whole 4-PR turn (#995-#998). The `/adopt` core design proved sound under three independent lenses. | #998 was slip-dense (all caught pre-merge): building on `main` before branching (2nd occurrence this session), a D4 pack-README Version-Date co-bump miss (bumped Version to 1.62.0, left Date at 2026-07-14), the scratch-enqueue not chained to its validate, and the initial `/adopt` prose needing 6 guardrail fixes + gate-9/18/22/preflight fixes before green. | Pattern: (a) **building on `main` after merge+sync is now a 2nd-occurrence SIGNAL** (§1.19.4 and §1.19.6): the post-merge `git checkout main && pull && branch -d` leaves HEAD on main, and the next item's edits land on main unless a branch is created first. (b) **adding a new pack skill triggers a CASCADE of ~7 surfaces** (PAIRS, collection-enum tree, gate-count, pack README version AND Date, gate-60 `/guardrails` review, CLAUDE.md count); the D4 miss is the pack-README-Date sub-case. | Proposed improvement: (a) BRANCH-FIRST habit, run `git checkout -b claude/<item>` immediately after the post-merge sync, before any edit (no new machinery; the gates catch it at pre-push but the habit avoids the mid-build branch-move); recorded in the session-state repo-safety line. (b) new-pack-skill drafting checklist (PAIRS + tree + gate-count + pack Version+Date + `/guardrails` + CLAUDE.md count); each surface is already gate-caught (35/39/41/44/60 + D4), so this is a drafting-order aid, not new machinery. |
| 2026-07-17 | #997 | none (§1.19.5 detect-env origin-identity probe). | Read-before-editing detect-env.py gave a clean, minimal probe; the pre-push verifier found 0 errors (only 3 low notes, all handled in-window: the Current-task refresh, and two theoretical origin-matcher false-positives documented + queued to §1.19.6). The offloaded validate-pr-997 (worker-b, routine) independently confirmed clean with a 13-URL adversarial truth-table incl. the owner-suffix attack. | The full-sweep PII gate caught an email-shaped `git@github.com` literal in an SSH-URL docstring example, twice this session (in #997's detect-env probe, then again in #998's hardened-matcher docstring), each fixed by rewording to a `git@<host>` / host-described placeholder. Both caught pre-push by the sweep (no escape). | Pattern (2nd occurrence this session, now a SIGNAL): an SSH-clone-URL EXAMPLE (`git@<realdomain>.tld:...`) written into a gate-scanned `.py`/`.md` trips the PII email-pattern gate; the corpus's own SSH-URL illustrations are the recurring carrier. | Proposed improvement: when illustrating an SSH clone URL in any gate-scanned file, write the host as a non-domain placeholder (`git@<host>:owner/repo`) or describe it (host `github.com`, path `owner/repo`) rather than a literal `user@domain.tld`; the PII gate already catches the slip pre-push, so this is a drafting habit, no new machinery. |
| 2026-07-17 | #996 | none (§1.19.2 resolve_sibling helper + advisory-tool graceful-degradation). | Read-before-editing caught the design's scope error (6 named tools -> only 3 reach a sibling today), avoiding dead no-op code; the fix was scoped correctly and surfaced. The pre-push verifier caught three real items pre-push (D8 overrun, ref-holds corrupt-sibling masking edge, portability over-claim), all fixed before push. The offloaded validate-pr-996 (worker-a elevated) independently confirmed clean incl. a corpus-wide completeness grep of the §3.91 list. | My scope-correction examined only the SIX NAMED design tools (found 3 reach a sibling), but did NOT grep the WHOLE tools/ dir for the sibling-reach class, so I missed SIX OTHER `grc_library_ref`-reaching tools; the verifier's completeness grep found them (routed to §3.91). Also the PII gate caught an email-shaped `git@github.com` SSH-URL docstring example (fixed by rewording to `git@<host>`). | Pattern: a SCOPE analysis of a "tools that do X" class must grep the WHOLE tool set for X, not just the design's named candidate list, exactly like the corpus-wide-completion-grep discipline (an input-set grep confirms only the named set, never the whole set). Single occurrence (scope-analysis variant). Also: email-shaped `git@host.tld` literals in a scanned .py trip the PII gate; use a non-domain placeholder in illustrative SSH URLs. | Proposed improvement: when correcting or asserting the scope of a tool-class ("the N tools that reach a sibling"), run a bare-pattern grep over ALL of `tools/*.py` (not the named list) and reconcile, before writing the scope claim. No new machinery; a scope-claim-grep habit, the tool-set analogue of the corpus-wide completion grep. |
| 2026-07-17 | #995 | none (§1.19.4 hard sibling-repo stub-guard gate 70 + validate-pr-994 consume). | The pre-push skeptical verifier earned its keep: it caught TWO real defects pre-push, one CI-blocking, both fixed before push (net zero escape). The gate shipped proven both directions (clean PASS + injected-payload FAIL + 7 unit tests); the offloaded validate-pr-995 (worker-b delivery 3, elevated) then independently confirmed 0/0/0 and enumerated the payload-bypass vectors as all caught. | (1) I edited the audit-programme SPEC body (gate-70 §6 row + detailed prose + §5) but did NOT co-bump its per-document Version, so gate 40 (history-aware, sees only COMMITTED state) failed AFTER commit while my pre-commit 70/70 was a false-clean; the verifier caught it. (2) The §1.19.4 rotation reworded the item-definition clause but left the sibling "Build order: §1.19.4 (next)" clause in the same paragraph stale (a dangling token + a stale "(next)" on the just-shipped item), which also falsified the CHANGELOG "reworded to drop the dangling token" claim; the verifier caught it. | Pattern: (a) a "gate-wiring" PR that feels like tooling still edits `governance/specification-audit-programme.md`, which IS a versioned corpus doc, so it needs a Version co-bump + taxonomy/scorecard regen; the pre-commit run_all_audits is a FALSE-CLEAN for gate 40 because gate 40 is history-aware (single occurrence). (b) a rotation that rewords one clause must re-grep the SAME paragraph for other clauses citing the rotated token (incomplete-paired-clause class; the §N-orphan guard at intra-paragraph scope). Both single occurrence. | Proposed improvement: (a) checklist habit "if this PR touches the audit-programme spec body, co-bump its Version + regen" (mechanical control already exists via gate 40 + the pre-push verifier + guard, so no new machinery); (b) on any §-section rotation, bare-token grep the WHOLE containing paragraph (not just the definition line) for the rotated token before commit. No new machinery. |
| 2026-07-17 | #994 | none (§1.19.3 sibling-repo placeholder stubs + exemption + full Phase-1 design lock + validate-pr-993 consume). | The full Phase-1 design (§1.19.2-§1.19.7) was locked with the maintainer up front (their choice) before any build, so the placeholder stubs shipped to a settled spec; the offloaded validate-pr-993 confirmed §1.19.1 clean. The maintainer set the timestamp-prefix hook this stretch (a real automated forcing-function for a preference memory alone kept slipping). | validate-pr-994 caught a real in-window stale-docstring lag (I added 3 dirs to DEFAULT_EXEMPT_DIRS but left the lint_common.py module docstring at 5), fixed in #995. This is the paired-surface-lag class (a code change with a docstring half left stale). | Pattern: adding to a shared constant (DEFAULT_EXEMPT_DIRS) needs its module docstring's enumeration updated in the same edit; the inline comment at the edit point was added, but the module-level Scope-notes summary lagged (a distinct surface). Single occurrence; watch. | Proposed improvement: when editing an enumerated shared constant, grep the module docstring for a stale enumeration of it in the same commit. No new machinery; a close-out-grep habit. |
| 2026-07-17 | #993 | none (§1.19.1 adopter-clone portability check + validate-pr-992 consume + worker-a trust-tier correction + §3.89 route). | The new tool (`tools/check-portability.sh`) shipped with a PROVEN correctness property (PASS + non-vacuous FAIL, demonstrated directly), which the offloaded validate-pr-993 elevated consume + the worker's adversarial false-negative hunt independently confirmed clean (0/0/0). The self-caught worker-a trust-tier miscount was corrected across all six carriers and re-confirmed by the worker. | The D2 guard caught two working-file Version bumps I missed (credit-offload-metrics + validate-sweeps re-touched in #993 without re-bumping) - the SAME "re-touch needs re-bump" class the D8 guard caught on #986 the PR before. Both caught pre-push, no escape. | Pattern (2nd occurrence this session, now a SIGNAL): RE-TOUCHING a versioned working file in a NEW PR (already bumped in a PRIOR PR this session) needs a fresh Version+Date bump; D8 (#986) then D2 (#993) both caught the class. Also an FYI from validate-pr-993: `check-portability.sh` covers relative sibling-reaches only (routed §3.90). | Proposed improvement: the D2/D8 guards already catch the re-touch-needs-rebump class reliably at pre-push, so the mechanical control exists; the durable habit is "if I edit a versioned working file in this PR, bump its Version even if I bumped it in a prior PR this session." No new machinery needed. |
| 2026-07-17 | #992 | none (resume Sweep-110 loop-break `/validate` close-out: sweep record + S110-2 CHANGELOG fix + S110-1 route to §1.17(d) + §1.19.x locks + §3.87 socket + handoff prune + lease acquire). | The offloaded Sweep 110 was consumed under full elevated QA (worker-a; independent re-derivation of base #984/baseline/pre-flight/counts + every finding re-verified at source), loop-break control for #991 PASSED, and the two findings were triaged correctly (S110-2 in-window fix, S110-1 out-of-window route to §1.17(d)). The §1.19.x design discussion produced clean, mutually-consistent locks across TODO / session-state / next-prs / CHANGELOG. | Two self-caught slips, both corrected in-window before any escape: (1) the S110-2 fix RE-TOUCHED the pre-D8 #986 root entry (147 words), tripping D8 at the pre-push guard, so I had to compress it to <=100 words mid-build; (2) I mislabeled worker-a "re-graduated to routine" by counting LAST-session elevated passes, corrected in #993 (worker-b's validate-pr-992 + the delivery-1 auditor both independently confirmed the miscount). Also: offloading validate-pr-992 (~132K conserved) then spending ~255K on the delivery-1 auditor was net-negative for this pure-bookkeeping self-authored PR (a one-time worker-b trust-establishment cost). | Pattern (if any): (a) D8 fires on any RE-TOUCHED merged CHANGELOG entry, not only new ones, so a one-word fix to an old over-length entry forces a compress (single occurrence). (b) the SESSION-SCOPED elevated-QA window is easy to miscount by carrying prior-session passes forward (single occurrence; a 2nd would make it a signal). | Proposed improvement: none yet (all single-occurrence). Calibration note for later: for a small SELF-AUTHORED bookkeeping PR, consider self-running its `/validate-pr` rather than offloading, since the delivery-1 elevated-QA auditor cost can exceed the offload saving; and the correct trust-count check is always "clean elevated passes THIS session," never a cross-session total. |
| 2026-07-17 | #990 | none (mistake-prevention fixes: ref-holds.py + AskUserQuestion-unattended hook + Operating-mode field + #989 QA batch). | The two maintainer-flagged session errors were each converted from a discipline hope into a MECHANICAL guard: the ISO-27002 partial-grep error -> `ref-holds.py` forcing-function (held/not-held from the index, quoted) + the executed-not-narrated rule; the ~7h-idle AskUserQuestion-in-overnight error -> a gate-63-validated Operating-mode field + a PreToolUse hook that blocks the call in unattended mode. The pre-push skeptical verifier returned SHIP (could not break either tool), and worker-b's validate-pr-989 delivery (which graduated it to routine trust) had already caught the paired-surface lag this PR fixed. | The preflight-changelog gate caught four unlinked path-shaped code-spans (`ref-holds.py`, `.claude/settings.json`) in the #990 CHANGELOG draft, self-fixed before commit. | **Pattern (4th occurrence, CHANGELOG path-spans at depth: #985/#986/#988/#990):** bare path-shaped code-spans reintroduced in CHANGELOG prose; preflight-changelog catches it deterministically each time when run before commit. **Meta-pattern (the session's theme):** a flagged error should become a forcing function / gate, not a promise, and this session did that for both flagged errors. | **CHANGELOG path-span class ->** consider wiring `preflight-changelog.py` as an actual pre-commit hook (it is currently a manual `&&`-chain habit); low priority (the habit + the D3/link-coverage gates catch escapes). The bigger structural lessons are carried by §1.18 (surface map) and §1.19 (privatization + adopter portability). **Disposition scan:** closes no register candidate; #990 touched protected files (`.claude/CLAUDE.md`, `.claude/settings.json`, `.claude/hooks/`, `tools/lint-session-state.py`), authorized this session; created §1.18/§1.19. |
| 2026-07-17 | #989 | none (D8 CHANGELOG-length gate + deep-assessment r4 consume + §1.18 surface-map + #988 QA batch). | Layered verification earned its keep twice on one PR: the pre-push skeptical verifier caught four accuracy/bookkeeping slips (r4-routing misattribution §1.16-vs-§1.17, a stale P1 count five->seven, the P3-counter description drift, an imprecise "no corpus content" claim), all fixed in-window; then the offloaded validate-pr-989 (worker-b, delivery-3, now graduated to routine trust) caught a fifth (the r4 W2 paired-surface lag between TODO/CHANGELOG and the r4 detail/register), also fixed in-window. | Several accuracy slips concentrated in one PR (the misattributions + the paired-surface lag), all authored at depth. Every one was caught by the verification layer (verifier + validate-pr) before escape, so the friction is authoring density, not an escaped defect. | **Pattern (paired-surface lag at depth, recurring this session):** correcting or adding one surface (a routing pointer, a count, the W2 disposition) without propagating to its paired surfaces; caught by the #989 verifier and validate-pr-989's NOTE-1. This is exactly the multi-surface-incompleteness class §1.18 (the change-impact surface map) scopes, and the class the maintainer flagged for the pack/website/MD dimensions. | **-> §1.18 (change-impact surface map) is the carrier** for mechanizing the paired-surface-completeness class. This session's mistake-prevention PR (#990) directly prevents two of the flagged errors (the AskUserQuestion-in-unattended-mode hook + the ref-holds.py held/not-held forcing function). **Disposition scan:** #989 touched no `.claude/` or pack protected file (its edits were the D8 tool, the audit-programme spec, and `.working/`); the mistake-prevention PR #990 DOES touch protected files (`.claude/CLAUDE.md`, `.claude/settings.json`, `.claude/hooks/`), authorized this session. |
| 2026-07-17 | #988 | none (§2.22 A2 TBS agentic-AI comparator row + #987 QA batch + local-VM-transport design §3.87 + N1 routing §1.16). | The elevated-QA discipline caught a CONFIRMED worker miss the way it is designed to: worker-a's validate-pr-987 in-window SHIP verdict was correct (I re-derived the self-test 5/5, the APO14 canonical title, and both changes), but the mechanical re-derivation of its out-of-window N1 enumeration found it undercounted the COBIT-title carriers (32/25 vs the true 35/28; a title-case-only grep missed 3 lowercase-form sites). Independent re-derivation of a mechanical fact (elevated-QA step 2) is exactly the guard, and it fired. Worker-a's window reset + escalated per the discipline; the finding itself routed to §1.16 with the corrected count. | The preflight-changelog gate caught two unlinked path-shaped code-spans in the #988 detailed-CHANGELOG draft (a cross-repo held-source path and an in-repo tool), self-fixed before commit. Same authoring-reflex-at-depth class as #985/#986. | **Pattern A (THIRD occurrence, gate-blind COBIT-title/fit class -> AUTO-GRADUATES):** MF1 (#986 APO14->EDM01 mis-fit), Note-1 (#987 APO14 "Managed AI"->"Managed Data"), N1 (#988 corpus-wide "Manage X"->"Managed X" title-verb drift, 35 sites) are all COBIT-citation defects gate 61 (code-existence) misses. Three distinct PRs -> graduates to a gate proposal. **Pattern B (worker false-negative via narrow grep):** worker-a's N1 enumeration used a title-case-only pattern and undercounted; the elevated-QA re-derivation caught it (the worker-brief bare-token/pattern-width discipline applies to worker enumerations too). **Pattern C (3rd, CHANGELOG path-spans at depth):** preflight catches it each time (#985/#986/#988). | **Pattern A -> §1.16 CREATED** this PR (COBIT title-text validation gate + one-time normalization backfill, guard-first): the auto-graduation carrier. **Pattern B ->** add a "grep enumerations at bare-token/pattern-family width, not one case form" line to the worker-brief when §1.16 or item-1 next touches it. **Pattern C ->** the recurring CHANGELOG drift (this session's other directive) is addressed by making the length guard a REAL gate (§1.12, next PR). **Disposition scan:** N1 routed to §1.16; closes no register candidate; #988 touched no protected file (design-decisions/TODO/CHANGELOG/session-state are .working/root, not `.claude/` or `dev-security/claude-rules/`). |
| 2026-07-17 | #987 | none (§3.61 delivery-status low-confidence flag + fix validate-pr-986 Note-1 [COBIT APO14 title] + batch #986 QA). | The offload + QA-first model caught its SECOND gate-blind COBIT citation defect of the session: consuming validate-pr-986 surfaced Note-1 (APO14 title "Managed AI" -> "Managed Data" in the dev-security register), a title error gates 48/49/61 (code-existence) structurally miss; fixed in-window. §3.61 shipped with a self-test (5/5) that also caught my own tuple-return break of the existing `test_classify` (fixed before push). | The offloaded validate-pr lags the next content PR's readiness, so each PR waits on the prior's QA row (pipeline latency); mitigated by the ~5-min worker poll but real. | **Pattern (recurring, gate-blind COBIT/framework-title class):** MF1 (APO14 -> EDM01 semantic mis-fit, #986) and Note-1 (APO14 "Managed AI" -> "Managed Data" title error, #987) are both COBIT-citation defects gate 61 (code-existence) passes; `/matrix-fit` + `/validate-pr` catch them, the gates do not. | A COBIT/framework TITLE-accuracy check (assert each cited code's title matches the canonical module title) is a gate candidate, folding naturally into the §3.74 standards-reference-format gate work; low priority (the `/matrix-fit` + validate-pr cadences catch the class). **Disposition scan:** closes no register candidate; #987 touched no protected file. |
| 2026-07-17 | #986 | none (QA-consume PR: MF1 fix + 4 QA-pass consumptions + #985 QA batch). | The maintainer-directed QA-passes-first ordering paid off twice: consuming the 4 delivered QA passes surfaced MF1 (a real COBIT semantic mis-fit) fixed ahead of §2.22, and the offloaded validate-pr-986 then surfaced Note-1 (a real pre-existing APO14 title error, `Managed AI` for `Managed Data`) fixed in #987 - both gate-blind citation defects (gates 48/49/61 check code existence, not semantic fit or title accuracy) the QA layer caught. Offload independence earning its keep again (worker-a elevated delivery-2 PASS). | The preflight-changelog gate caught an em-dash + two unlinked path-shaped code-spans in my #986 CHANGELOG draft (self-caught pre-commit). | **Pattern (recurring, CHANGELOG-authoring dashes/path-spans at depth):** em-dashes and unlinked path-shaped code-spans reintroduced in CHANGELOG prose deep in a long session, caught by `preflight-changelog.py` each time (#985 em-dash + path-spans; #986 the same). The preflight gate is the reliable pre-commit control. | No new gate needed: `preflight-changelog.py` already catches this class deterministically before the first commit (running it before commit is standing discipline). The residual is purely authoring reflex at depth; the gate holds. **Disposition scan:** closes no register candidate; #986 touched no protected file. |
| 2026-07-17 | #985 | none (Sweep 109 loop-break `/validate` close-out over #969..#984; offloaded to worker-b + elevated-QA-validated; fixed S109-N1; pruned handoff; acquired the 2026-07-16c lease). | The elevated-QA layer earned its keep: worker B's A/B/C sweep returned 0, but the delivery-1 adversarial false-negative auditor surfaced a REAL precision gap (S109-N1: the US annex section 7120 cyber-audit threshold parenthetical rendered only the (b)(2) prong, omitting the (b)(1) 50%-revenue trigger and the (b)(2) >$25M conjunct), verified at the held CCPA Regulations + Civil Code 1798.140(d)(1)(A)/(C) and fixed in-window; the reworded parenthetical then passed an independent skeptical verifier (SHIP). Worker B's mechanical claims (baseline, pre-flight, parity, counts) were independently re-derived to an EXACT match (elevated QA, delivery 1 = PASS). Offload worked once the worker clones were resynced (worker B, GitHub-based). | A UTC-midnight rollover mid-assembly: the close-out commit crossed into 2026-07-17 UTC, so the version-bumped docs' Date fields (set to 2026-07-16) were a day stale; D4 flagged the co-bump, and after the Date fix gate 33 flagged the taxonomy (regenerated before the Date change). Both fixed pre-push (the guard working). Also a stale-working-tree read on the scratch queue (grepped local files after a bare `git fetch`, reported the Sweep 109 order "still pending" when worker B had already claimed it on `origin/main`); self-caught, maintainer-flagged. | **Signal (2nd+ occurrence, UTC-date-boundary):** dating close-out artefacts to the session's opening UTC day when the commit has crossed into the next UTC day (a prior guard-caught occurrence this session-family was the UTC-midnight date rollover in the #964-#967 session); the D4 + gate-31 + gate-33 layer catches it deterministically pre-push. **Observation (1st, stale-worktree-read):** characterizing remote state from an un-refreshed local working tree after a bare fetch (a `validate-inference-before-action` / read-before-characterizing instance). | UTC-boundary: convention nudge (set close-out Date fields from `date -u` at push time, not the session's opening date); already backstopped deterministically by D4/gate-31/gate-33, so low priority. Stale-read: CODIFIED in the assistant memory `read-origin-not-stale-worktree` (read `git show origin/main:<path>`, never the post-bare-fetch working tree) + adopted this session; watch for a 2nd occurrence. **Disposition scan:** closes no register candidate; #985 touched no `grc_library` protected file (the two maintainer-directive codifications, worker resync-check + staggered check-ins, landed in `grc_library_scratch`, not here). |
| 2026-07-16 | #983 | none (session bookkeeping batch for the unattended run: r4 register + metrics + #982 QA + frozen-§7121 fix + wind-down-/validate codification + pre-loaded decisions). | The durability-checkpoint batch landed clean under independent worker QA (validate-pr-983 SHIP, 0 findings; the ~1.98M metrics roll-up independently arithmetic-corroborated). Recording session state (r4 register, metrics, the pre-loaded decisions) in a dedicated PR BEFORE the delicate/high-stakes work protected the unattended run against compaction, and the split (bookkeeping #983, then the delicate helper work separately) kept the checkpoint clean and mergeable. | The batch was large (r4 register + dated record + metrics + #982 QA + the frozen-#976 fix + the codification + egress + decisions + session-state + next-prs) - a lot of intricate bookkeeping in one PR, at a depth where the caught-slip rate was already elevated (the frozen-#976 fix needed several targeted edits + a preflight loop for bare cross-repo paths). | The elevated caught-slip rate through this stretch (status:queued, §1.15 cwd-persist 3x, §7120->§7121, and immediately after #983 the VACUOUS item-1 test) is the tracked signal; the item-1 false-self-verification is the one that graduated to the maintainer-authorized WIND-DOWN (a false "test-proven" claim on delicate git-logic this deep). | For a session-closing bookkeeping batch this large, consider splitting durable-state recording (register / decisions / metrics) from QA-row batching so each PR is smaller + lower-slip-risk. Low priority. The deeper takeaway: at an elevated caught-slip depth, delicate git-logic + high-stakes work should move to a fresh context (the wind-down this session took). |
| 2026-07-16 | #982 | none (CCPA §2.23/#976-NOTE-1 precision fix: US privacy annex per-tier cyber-audit measurement years). | The pre-push skeptical verifier caught a citation-precision slip BEFORE merge: I had attributed the cyber-audit PHASING to 11 CCR §7120 in the #982 records prose, but the phasing/timing is §7121 ("Timing Requirements"); §7120 is the audit-requirement/threshold section. Re-verified at the held source and corrected in-window (the annex body itself was always accurate, citing only the "7120 to 7124" range). Offloaded validate-pr-982 (worker-b) then confirmed the operative fix complete and surfaced the two FROZEN #976 records that still said §7120 (corrected in #983). | I introduced the §7120 misattribution by carrying forward the validate-pr-976 NOTE-1's own "§7120" framing without re-confirming the exact sub-section at the held text; the range (§7120-7124) was right but the specific timing section (§7121) was not. | **Pattern (2nd occurrence, sub-section citation precision):** attributing an attributed VALUE to the wrong sub-section/paragraph of the right instrument. First this session: the `1798.185(a)(16)`->`(a)(15)` statute-sub-paragraph fix (#977). Here: the phasing §7120->§7121. Both CCPA, both caught (verifier / worker), both the exact-sub-section-not-the-range class. | When citing the SOURCE sub-section of an attributed value, confirm the exact sub-section (not the article/range or an adjacent section) against the held text at authoring time. The pre-push verifier + `/claim-fit` catch it, but the authoring reflex cites the range; a first-commit "confirm the exact §" habit would pre-empt it. Low; the verifier layer already catches it. |
| 2026-07-16 | #981 | none (ref-acquisition grc_library-side closeout: egress-blocked items + metrics-ledger consume rows + batched #980 QA). | The offloaded `validate-pr-980` (worker-b, routine consume) caught a REAL 3-surface session-tally figure-drift (`~1.37M` lagging vs the `~1.44M` ledger) that the orchestrator had introduced, and it was fixed in-window: offload independence earning its keep again (after the validate-pr-974 F1 catch). The EDPB/Brazil/LatAm ref-acquisition packages consumed and merged (ref #85/#86) cleanly, closing the maintainer's chosen task. | The running "estimated orchestrator credits conserved" tally increments every time a worker delivery is consumed, so every SECONDARY surface that restates the number (session-state, next-prs, retro rows) drifts stale the moment the next delivery lands; validate-pr-980 caught it, but the authoring keeps re-introducing it. | **Pattern (2nd occurrence, session-tally variant):** a single running figure duplicated across multiple prose surfaces is a recurrence of the CHANGELOG-count-reflex / meta-prose-state-claim class (a figure restated in N places goes stale in N-1 of them). First seen as the mid-PR count-drift guard; here as the credit-conserved tally. | Consider making the metrics-ledger roll-up the ONE quoted figure and having secondary surfaces point at it ("see the credit-offload-metrics roll-up") rather than restating the number, so there is a single place to update. Low priority; the per-PR /validate-pr already catches the drift. |
| 2026-07-16 | #980 | none (credit-offload metrics ledger + convention; maintainer-requested feature). | Delivered the maintainer's requested worker-productivity/savings tab as a proposal-with-options FIRST (AskUserQuestion, pausing the scroll for their read/response), then built the chosen shape (a `credit-offload-metrics.md` ledger + a per-major-activity chat tally; metric = estimated orchestrator credits conserved; no per-DONE line). The metric was designed HONEST from the start: worker figures are best-effort estimates (no exact in-session counter), and the standing shift-not-reduce caveat is baked into the ledger, the design-of-record, and the CLAUDE.md convention. Backfilled this session (~1.44M est. orchestrator tokens conserved across the this-session-consumed set). | The backfill's token figures had to be reconstructed from the workers' proof-of-run subagent-budget notes (two formats: a "Token spend" section for worker-b, a "Proof-of-run" subagent-budget line for worker-a); four research deliveries carried no figure (recorded as `not captured`, an honest gap). | A worker-onboarding refinement surfaced: the two token-reporting formats should converge on one "Token spend" section so the ledger backfill/parse is uniform. Low-priority; the going-forward orders now explicitly request the token-spend estimate. | Add a one-line "report your token spend in a `## Token spend` section" to the scratch `/credit-offload` worker command (aligns worker-a's format to worker-b's), folded into the credit-offload thread; not blocking (the orchestrator already reads both formats). **Disposition scan:** closes no register candidate; #980 touched the protected `.claude/CLAUDE.md` (a maintainer-requested convention line, authorized on the local VM). |
| 2026-07-16 | #979 | none (CCPA §2.23 slice 4 breadth: 4 framework-alignment tables; batched #978 QA). | Completed the CCPA §2.23 regulations-alignment (primary + breadth) cleanly. The breadth adds were batched into one efficient PR (4 low-risk citation-adds, same shape) rather than 4 separate PRs, with every section anchor (s. 7004/7025/7026/7070-7072/7021/7221-7222) re-verified at the held regs before authoring and a single skeptical verifier confirming all 9 subjects + the minors age-bands. | No friction; verifier SHIP first-pass, guard green first-pass. | No new pattern. Batching same-shape low-risk citation-adds into one PR (with one verifier) is the right proportionality call for breadth-tier work, versus a PR-per-doc that would multiply the offload/consume ceremony for minimal-risk changes. | None. **Disposition scan:** closes no register candidate; #979 touched no protected file. |
| 2026-07-16 | #978 | none (CCPA §2.23 slice 3: jurisdiction index + notice template + DSR procedure; batched #977 QA). | Completed the CCPA §2.23 primary-carrier set (slices 1-3) with every anchor and timeline re-verified at the held regs (the 15-business-day opt-out §7221(n), the §7021 10-business-day/45-calendar-day/90-max timeline, the pre-use-notice-in-Notice-at-Collection permission), all newly verified this slice, plus a skeptical verifier SHIP. The per-domain slicing kept each PR reviewable and each verifier's scope tight. The pipe-guard hook caught my one reflexive `guard | grep` (defence-in-depth working alongside the restored unpiped habit). | No friction on the edits; verifier SHIP first-pass, guard green first-pass. | No new pattern; consistent with the slice-1/2 approach (source-verify + verifier + confirmed currency). The CCPA §2.23 regs-alignment is now at its primary-carrier completion milestone; the remaining breadth carriers are lower-priority citation-adds. | None. **Disposition scan:** closes no register candidate; #978 touched no protected file. |
| 2026-07-16 | #977 | none (CCPA §2.23 slice 2: ADM register to final CCPA regs; batched #976 QA). | A genuine external-facing citation ERROR was corrected with source proof: the register cited the CCPA ADMT enabling paragraph as Cal. Civ. Code s. 1798.185(a)(16), but the held statute shows (a)(15) is the ADMT access/opt-out regulations paragraph and (a)(16) is the law-enforcement-investigation paragraph (the regs themselves cite "(a)(15) and Article 11", closing the loop). The GDPR-framed subject-rights list was extended with the distinct CCPA ADMT rights. The acronym-collision guard held (only the California CCPA/CPRA row touched, not the Canadian CPPA/Bill-C-27 row); skeptical verifier SHIP; validate-pr-976 consumed clean (worker-b now routine). | No friction on the edit; first-pass clean through the verifier, guard, and preflight. The per-PR offload/consume chain (each validate-pr gates the next PR's gate 50) adds a short worker round-trip wait per slice, but that is the credit-offload design working, not friction. | No new pattern. The per-domain slicing of a large multi-carrier reference-audit delivery (slice 1 = annex, slice 2 = register, slice 3 = DSR/index/notice) keeps each PR reviewable and each verifier's scope bounded, working well. | None. **Disposition scan:** closes no register candidate; #977 touched no protected file. |
| 2026-07-16 | #976 | none (CCPA §2.23 slice 1: US privacy annex to final California CCPA regs; batched #975 QA). | Content quality HELD on an external-facing, currency-material apply despite the session's earlier mechanical-slip cluster: every CCPA regulation § anchor was independently re-verified at the held source (grep of the article/section headings, the "significant decision" enumeration, the thresholds, the phasing dates) BEFORE authoring; currency was confirmed upstream at the fetchable cppa.ca.gov (unlike canada.ca); and a refute-briefed skeptical verifier returned SHIP with only one minor fidelity note ("or households"), applied. The large 11-finding + 9-carrier §2.23 delivery was split into per-domain slices (this = the US-annex cluster) rather than a mega-PR. Mechanical discipline recovered: the scratch order push this turn was validate-gated and unpiped. | One guard iteration: gate 24 (external-link-domain) flagged `cppa.ca.gov` as not allow-listed; resolved by adding the legitimate publisher domain (the California Privacy Protection Agency) to the allow-list, the sanctioned action the gate message directs, not gate-weakening. The verifier's "or households" fidelity note was the only content nit, applied. | No new pattern. Adding a new authoritative publisher domain to the external-link allow-list is a normal, expected step when the corpus first cites a new authoritative source; the gate did its job (flagged an off-allow-list domain) and the fix is the documented add-with-justification path. | None. **Disposition scan:** closes no register candidate; #976 touched a tool (`lint-external-link-domains.py`) only to extend its allow-list data, not a protected `.claude/`/pack file. |
| 2026-07-16 | #975 | none (maintainer-egress channel + TODO §2.22 defer; consumed validate-pr-974 + fixed its F1; batched #974 QA). | The maintainer-egress channel gives a durable, honest path for egress-blocked sources (no fabricated URLs; instruments listed by verified title + held edition). The canada.ca block was confirmed FIRST-HAND (WebFetch WAF rejection) before asserting it. worker-b's delivery-2 F1 (a real design-doc overclaim I introduced in #974 and my own pre-commit cross-check missed) was re-verified at the scratch-command source and fixed in-window, and the #974 CHANGELOG echoes corrected transparently, the offload-independence catching what the orchestrator missed. Confirmed CCPA/California currency IS fetchable (unlike canada.ca), de-risking §2.23. | A cluster of MECHANICAL slips, all caught + corrected on the non-authoritative scratch repo, none reaching grc_library: (1) a pipe-masked scratch push (`git push \| tail` masked the exit, so a `PUSH_OK` printed on a non-fast-forward failure, the RM-10 class applied to a scratch push, not a verification gate); (2) TWO self-inflicted scratch-CI reds because I committed+pushed before gating on `validate.py`'s exit code (a bare "ensure" in worker-b's result prose, then a bare "ensure" in my OWN pre-deliver-instruction text). Also the F1 overclaim itself (introduced in #974, caught in #975). | (a) The design-doc overclaim class RECURRED (F1 = 2nd instance after the #974 "oldest-queued" one), now caught by the offload QA rather than my own cross-check, a signal my design-claim-vs-implementation cross-checks are not catching everything. (b) A NEW cluster: scratch-side mechanical-hygiene slips (pipe-masking, push-before-validate-gate) at rising density late in a long session, all scratch-only + caught, but observable. | (a) Root-caused the recurring worker house-style trip: ADDED a pre-`deliver` `validate.py` step to the scratch `/credit-offload` command (closes the §3.83 validate-before-deliver nit, done this turn). (b) MECHANICAL DISCIPLINE (self, restated): run every push/verification UNPIPED, gate every push on its exit code (`RC=$?; if [ $RC -eq 0 ]`), and re-read my own edits before committing. (c) Design-claim cross-checks: diff design-doc claims against the implementation line-by-line, not by recall. **Disposition scan:** closes no register candidate; the §3.83 validate-before-deliver nit is CODIFIED in the `/credit-offload` command this turn. |
| 2026-07-16 | #974 | none (credit-offload design-of-record: worker-allocation + ref-read models; TODO §3.85; batched #973 QA + §3.84 refine). | The design-of-record codification was cross-checked against the ACTUAL scratch serve-loop before commit, catching and correcting my own over-specific "then oldest-queued" claim-precedence overclaim pre-commit (the design doc now matches the implementation, not an idealized version of it). worker-b's FIRST QA delivery (`validate-pr-973`) validated clean under FULL elevated QA: independent re-derivation of SHAs + diff scope EXACT-MATCH, all four Law-25 figures re-verified at the held source, and a dedicated adversarial auditor returning WORKER-CLEAN-CONFIRMED, establishing worker-b trust 1-of-2-to-3. | Two, both caught in-window: (1) **#974 shipped WITHOUT touching `next-prs.txt`** (the "every PR touches next-prs" close-out line missed; the first `next:` line happened to stay accurate since it already pointed at Canada §2.22, but the item-4 "design-doc" entry is now a stale done-item, the #847-era drift shape). (2) **worker-b's delivered result prose carried house-style em-dashes + a bare "ensure"** that tripped scratch `validate.py` (red scratch CI), fixed post-hoc; a worker-onboarding gap (the worker did not run `validate.py` before committing its result, contra the scratch CLAUDE.md invariant). | (a) Bookkeeping-authoring-slip class CONTINUES (the next-prs miss is another in-window instance this session; all caught by a QA layer, none escaped). (b) NEW class: **worker-delivered-result house-style violations** (worker-b's dash-laden result prose) - the `/credit-offload` delivery step does not force a pre-deliver `validate.py` run, so a worker can push a style-tripping result that reddens scratch CI. | (a) next-prs miss: reinforce the "every PR touches next-prs.txt" close-out line (WATCH; not gated, first line stayed accurate this time). (b) worker-result house-style: add a "run `python3 tools/validate.py` and fix any finding BEFORE `deliver`" step to the scratch `/credit-offload` command's delivery flow (concrete, actionable; route as a scratch-side follow-up in the credit-offload thread, near §3.82/§3.83). **Disposition scan:** closes no register candidate; #974 touched no protected file. |
| 2026-07-16 | #973 | none (Canada annex Quebec Law 25 corrections; batched #972 QA; added TODO §3.84). | The research-assistant discipline worked exactly as designed on the FIRST worker-sourced CORPUS edit: the `canada-annexes-source-verification` delivery was treated as a hypothesis, and every Quebec Law 25 figure and section citation (the CAI s. 90.12 single-max, the s. 91 penal bands, the s. 23.3 + s. 17 PIA scope) was independently re-verified at the held source in `grc_library_ref` before the orchestrator authored the edit; a substantive-tier skeptical verifier returned SHIP; the edit was clean pre-push (preflight + guard + verifier all first-pass). The verifier's out-of-scope §3.84 finding (a pre-existing Law-25 PIA-citation inconsistency in three OTHER docs) was ROUTED as a follow-up rather than scope-crept into this PR. | No friction on the edit itself (first-pass clean through preflight, the 69-gate + PR-time guard, and the verifier). The only fixes carried here were the two BATCHED #972 findings (its counter + session-state staleness), which belong to #972's authoring, not this PR's. | The worker-sourced-apply consume path is now proven for CORPUS edits (re-verify every attributed value at the held source, author, verify, ship), complementing #971's proven consume path for QA-kind (`/validate`) deliveries. Not friction; a maturation of the credit-offload pipeline (the bottleneck is confirmed shifted from workers to orchestrator apply-throughput). | None new. The still-owed worker-efficiency item (the credit-offload design-of-record update + the §3.85 worker-ref-read-access / shared-`/tmp` resync gap) remains the next prioritized worker-efficiency candidate, carried from the #972 row (still pending). **Disposition scan:** closes no register candidate; #973 touched no protected file, so no carrier-phrase pending cells applied. |
| 2026-07-16 | #972 | none (credit-offload backlog items §3.82/§3.83). | The worker-usage design was worked out (not just captured): the maintainer's hard QA/research split was refined into a soft split (prefer-role, fall-back-to-any) to avoid idling, and §3.82's self-restart leverages the reconcile-on-re-register hook shipped the same session. The #971 verifier's two LOW notes were both closed in-window (workers/README field list + the pass-role-on-every-register note). | None material; a small bookkeeping PR. | Worker-efficiency work is now a maintainer-prioritized class (elevated above ordinary P3); the credit-offload thread (§3.80-3.84) is the cluster. | Continue treating worker-efficiency improvements as prioritized (they multiply orchestrator productivity via offload); track the ref-read-access / per-worker-ref-clone resync item (the shared /tmp gap) as the next such item. |
| 2026-07-16 | #971 | none (Sweep 108 `/validate` close-out; loop-break control for #968). | The FIRST offloaded corpus-wide `/validate` was consumed end-to-end and recorded: the worker's result was independently re-derived (SHAs/counts/gate-54/pre-flight all exact-match) + adversarially audited (WORKER-CLEAN-CONFIRMED) before the sweep row was written, and the two validated worker findings applied cleanly (phase-1 date to UTC; `verify` enum). The credit-offload consume path is proven. | A D4 miss: `.working/validate-sweeps/history.md` had its Version bumped but not its Date co-bumped; the pre-push guard (D4) caught it and it was fixed pre-push. | Version-bumped-without-Date-co-bump on a `.working/` bookkeeping file with a Date field (the same class the version-bump discipline covers for corpus docs, here on a working-state ledger). Guard-caught, so low; noted as the 2nd-ish occurrence of a working-file Date-co-bump slip this session. | None mechanical (D4 already catches it pre-push); the fix is the standing "bump Version AND Date together" habit applied to `.working/` ledgers, not only corpus docs. |
| 2026-07-16 | #970 | none (credit-offload new-worker QA-trust-tier policy codification). | The elevated-QA-window policy was co-designed with the maintainer (advise-then-codify) and landed in both surfaces (CLAUDE.md + design-of-record) consistently on the first pass; the worker-validation that motivated it produced hard evidence (independent re-derivation exact-match + a WORKER-CLEAN-CONFIRMED adversarial auditor), so the policy rests on a demonstrated case, not a hypothetical. | The session has generated a rapid cascade of small process PRs (phase-3 wiring, policy codification, this close-out), each carrying mandatory per-PR QA, while the single credit-offload worker is dedicated to the maintainer's priority Canada + CCPA-ingestion work and cannot absorb the process-PR QA. So the per-PR `/validate-pr` for the process PRs is SELF-RUN (orchestrator subagent spend), which erodes the credit-conservation the offload scheme exists to achieve. | **Process-PR QA spend vs a busy single offload-worker** (first clear occurrence): when the one worker is on priority work, offloading process-PR QA behind it is impractical, so the orchestrator self-runs it, and the credit saving inverts. The bigger the process-PR cascade, the worse. | **Batch remaining deferred work into fewer, larger PRs** (less per-PR QA overhead), and/or **defer non-urgent deferred items to a session with spare worker capacity**, and/or a **second worker** to absorb process-PR QA. Surfaced to the maintainer as a pacing decision after this close-out. |
| 2026-07-16 | #969 | none (credit-offload phase 3 orchestrator-side wiring; deferred-protected item 10). | The credit-offload machine was exercised end-to-end for the first time: both the mandatory Sweep 108 `/validate` and this PR's `/validate-pr` were OFFLOADED to the live worker and delivered in ~3 min, and the orchestrator validated the worker under elevated QA (independent re-derivation + adversarial false-negative auditor for delivery 1). The offload's independence paid off immediately: the worker caught a real orchestrator error (a one-char SHA-field typo in the `validate-pr-969` order) that the inline pre-push verifier would not have seen (it was in the scratch order, not the #969 diff). | A one-char SHA-field typo (`110fde6b` for `110fde6d`) in a hand-written scratch order; caught by the worker rather than at authoring. The mid-PR `/retro`-vs-`/validate-pr` offloadable-set wording in the #969 CHANGELOG-detailed overclaimed (`/retro` is not offloadable); caught by the inline pre-push verifier and fixed before push. | First occurrence of "hand-written order-field typo"; the worker's fencing/param-disambiguation handled it gracefully, so no mechanical guard is warranted yet (single occurrence, self-corrected downstream). | none this turn; the elevated-QA-window policy (codified this PR) is the systemic response to worker-trust, and the order-typo is a candidate for a future order-schema SHA-resolves check if it recurs. |
| 2026-07-16 | #967 | none (Canada.ca reference-utilization TODO §2.22 + #966 QA batch + 2 design-doc fixes). | The Canada.ca reference-utilization plan (§2.22) was recorded with its research half seeded as three priority-1 credit-offload orders for tomorrow's workers (offload-first, serving the Canada expert-review priority at low orchestrator cost); the two #966 design-doc self-consistency notes were fixed cleanly and #967's own sweep was clean. | No new friction; the two #966 design-doc notes (a broken section-anchor + a build-status overstatement) were the `/validate-pr` catch, fixed here. | The design-doc self-consistency-note class (a broken internal section-anchor and a build-status overstatement in a newly-authored design doc) is a sub-shape of the meta-prose-state-claim guard, caught by the `/validate-pr` layer (design docs are gate-exempt working state, so no gate fits). Consistent with the session's elevated-but-all-caught authoring-slip pattern. | WATCH (the authoring-slip class): the `/validate-pr` + guard + preflight layer caught every slip this session pre-escape; no new false-positive-free gate fits gate-exempt working prose. **Disposition scan:** closes no register candidate. |
| 2026-07-16 | #966 | none (credit-offload design record + backlog + phase-3 staging). | The credit-offload scheme was co-designed with the maintainer across many rounds and recorded cleanly as a design of record before any build; the phase-3 orchestrator wiring (touching `/resume` + CLAUDE.md) was correctly STAGED for maintainer review rather than applied unattended overnight; the scheme's own worker command was placed in scratch, avoiding a grc_library count-parity ripple. | Three authoring slips, all guard-caught pre-push: (1) an Edit anchor dropped a table-row prefix, MERGING the #965 and #964 improvement-log rows onto one line (caught by gate 50 + the regression suite at the guard); (2) the UTC-midnight rollover meant 4 bumped-doc Date fields needed 2026-07-16 not 2026-07-15 (caught by D4); (3) the detailed-CHANGELOG links needed `../` relative paths (caught by preflight-changelog). All fixed pre-push; net zero escapes. | The bookkeeping-authoring-slip class continues (WATCH). The row-MERGE slip (1) is a NEW sub-shape: an Edit whose anchor consumes a table-row prefix. This is EXACTLY the class TODO §3.73 (ledger-table-row-integrity check) would catch mechanically, so §3.73 gains a concrete recurrence. The date-rollover slip (2) is a UTC-midnight-crossing artefact (rare, self-correcting via D4). | WATCH continues; the row-merge sub-shape REINFORCES TODO §3.73 (a ledger-row-integrity gate would catch it) - cross-referenced there. No new gate proposed here (§3.73 already covers it; D4 + preflight cover slips 2 and 3). **Disposition scan:** closes no register candidate; reinforces §3.73. |
| 2026-07-15 | #965 | none (batch #964 QA + task-1 close-out). | Read-before-asserting paid off: rather than run the maintainer-chosen `/matrix-fit` on the AICM-code defect, checked git history first and found it was ALREADY fixed in #939 via exactly that remap, then re-verified all 7 codes against the held AICM v1.1.0 titles, saving a redundant subagent sweep under the credit-conservation directive. The stale pending-decisions entry was resolved-in-place. | No friction; pure working-state bookkeeping, `/validate-pr` SHIP 0. | The pending-decisions-entry-staleness class (a fix ships in a PR that does not rotate its pending-decisions entry out) recurs as a process observation (#964 retro noted it; #965 was the live instance) - **signal (2nd)**. Distinct from the TODO->DONE rotation (which is disciplined); pending-decisions has resolve-in-place but no staleness DETECTION. | WATCH (fires on 3rd occurrence): if a third stale pending-decisions entry surfaces, propose a lightweight detection aid (e.g. an advisory scan flagging an `open`/`Pending` entry whose named fix-PR has merged). Low priority; the resolve-in-place convention holds meanwhile. **Disposition scan:** closes no register candidate. |
| 2026-07-15 | #964 | none (Sweep 107 /validate close-out; resume from #963). | The loop-break Sweep 107 caught one genuine (low-severity) in-window miss the closing session's asserted-clean claim had over-reached on: three `(TODO section 2.4)` prose-orphans in `.py`/`.yml`/runbook files that #960's `§`-glyph-only, TODO-scoped residual grep could not see. The cross-check discipline worked exactly as designed (a contradiction of an asserted-clean surface flagged + fixed in-window); loop-break control for #963 PASSES. Also surfaced, before burning credits on a redundant sweep, that the maintainer-chosen task-1 `/matrix-fit` target (the AICM-code defect) was already fixed in #939 and its `pending-decisions.md` entry is stale, and independently re-verified all 7 remapped AICM codes against the held v1.1.0 titles. | Two authoring slips, both guard-caught pre-push (the QA layer working, not degradation): (1) bare backticked path-spans (`DONE.md`, `build-taxonomy.py`, `build-portal.py`) in the detailed-CHANGELOG entry, caught by `preflight-changelog.py`; (2) the `.working/cloudflare-pages-setup.md` Version not bumped after its title-line edit (D2) plus a D5 false-positive on the narrated-past-closure line needing a `TodoRotation:` trailer. All fixed pre-push; net zero escapes. | The bookkeeping-authoring-slip-in-close-out-PRs class recurs (consistent with the #950/#951/#953-era WATCH): version-bearing `.working/` docs and CHANGELOG path-linking are the recurring trip points on records-heavy close-out PRs. Each is guard/preflight/D-check caught, so the WATCH holds; no new gate needed (the guards already extinguish the class pre-escape). | None (the existing guard/preflight/D-check layer catches this class pre-escape; WATCH continues). Separately, a process observation: a `pending-decisions.md` entry can go stale when its fix ships in a PR that does not rotate the entry out (the AICM/#939 case); the standing rotation discipline covers TODO->DONE but pending-decisions rotation is convention-only. **Disposition scan:** closes no register candidate. |
| 2026-07-15 | #962 | none (For-AI page opaque-citation fix + §3.78/§3.79 P3-prioritized TODO items). | The For-AI Mitchell/Gebru author-citations were reworded to plain source references (maintainer's "name the source plainly"), removing opaque academic citations from a general-reader page; `/validate-pr` SHIP 0. The corpus-first dependency for §3.79 (the "named, not yet covered" instruments need corpus coverage before the page can claim it) was surfaced to the maintainer as advice. | No friction; small website copy + TODO additions, `.web/build.py --check` verified. | No new pattern (the 2026-07-15 website copy-fix cadence, #957/#961/#962). | None. **Disposition scan:** closes no register candidate. (Note: PR #963, the session-closing handoff, takes no `/retro` per the loop-break.) |
| 2026-07-15 | #961 | none (two public-site copy/style tweaks: about-page cert half-size + landing §01 positive reframe). | The two maintainer-flagged website copy fixes applied cleanly (about ", CGEIT, CISSP" at half the name's height via a `.m-certs` 0.5em span; landing §01 reworded from the "not X; it is Y" contrast to the maintainer's chosen positive Option-B form); `/validate-pr` SHIP 0 findings; the landing reframe removed a house-style-discouraged contrast framing. | No friction; small verified website copy changes (`.web/build.py --check` clean). | No new pattern; consistent with the 2026-07-15 website copy-fix cadence (#957, #961). | None. **Disposition scan:** closes no register candidate. |
| 2026-07-15 | #960 | none (maintainer-flagged TODO stale-item cleanup: closed §2.4 grclibrary.ai + §2.6 Cloudflare watch-paths to DONE, reworded 6 dangling §2.4 refs; batched #959 QA + Sweep 106 tally fix). | The stale-website cleanup was done thoroughly and verified orphan-free (§2.4/§2.6 residual 0 across the whole repo, the six dangling refs reworded, both sections rotated to DONE); the `/validate-pr` confirmed SHIP 0 findings. | An em-dash authoring slip in the session-state Current-task, caught by the working-prose-hygiene gate at the pre-push guard (the `preflight-changelog` aid only scans CHANGELOG, so the guard's gate is the backstop for `.working/` prose dashes). Third `.working/` authoring slip this session, all caught in-window. | Bookkeeping-authoring-slip class (auto-graduated at #953): three in-window instances this session (the #957-row clobber, the Sweep 106 tally, this em-dash), each caught by a DIFFERENT QA layer (pre-push verifier, `/validate-pr`, pre-push guard gate). No false-positive-free gate fits free prose. | Reinforce: for `.working/` prose avoid em/en dashes (the working-prose-hygiene gate is the backstop, since `preflight-changelog` scopes to CHANGELOG only), and re-read assembled multi-row tables before commit. WATCH; the QA layer catches each occurrence. **Disposition scan:** closes no register candidate. |
| 2026-07-15 | #959 | none (Sweep 106 loop-break `/validate` close-out: register "third-review"->"fourth-review" hyphenated 3rd carrier; batched #958 QA). | The loop-break Sweep 106 found and fixed a real hyphenated-separator third carrier of the #955/#956 ordinal fix (the separator-tolerance lesson in action), and the pre-push skeptical verifier caught a #957-row clobber I introduced in the #958-retro insert BEFORE it escaped (gate 50 would have failed CI). | TWO `.working/` authoring slips this PR, both caught in-window: (1) the #957-row clobber (pre-push verifier via gate 50); (2) the Sweep 106 history-row tally "0 note" vs body "1 note" (post-merge `/validate-pr`). Both gate-exempt free-prose bookkeeping. | Bookkeeping-authoring-slip class: a multi-row/multi-cell `.working/` edit producing a row clobber or a count-vs-body mismatch. Already auto-graduated at #953 (the "bookkeeping-surface authoring/refresh" class); two more in-window instances here. No false-positive-free gate fits free prose; the QA layer (pre-push verifier + `/validate-pr`) catches each. | Reinforce (restate at close-out): when authoring a multi-row `.working/` table edit, re-read the assembled rows before commit (row-boundary integrity + tally-vs-body), and anchor an insert on the target row's FULL header cell. WATCH; caught by the QA layer each occurrence. **Disposition scan:** closes no register candidate. |
| 2026-07-15 | #958 | none (date-anchor the annex AIA question counts to the 2026-05-28 source version; annex v1.0.2; maintainer-directed ahead of the Canada AI Alliance expert review). | The count-currency risk the maintainer flagged (an undated "current release") was fixed by anchoring the 65/41 counts to the now-held AIA source (`Date modified: 2026-05-28`), verified this turn, so the counts kept a verifiable date instead of being dropped; the pre-push skeptical verifier and `/validate-pr` both returned 0 findings. | No friction on the edit itself; the `preflight-changelog` aid correctly blocked the first commit on a cross-repo path-shaped reference in the detailed entry (fixed by naming the ref repo instead of pathing into it). | No new pattern (the preflight cross-repo-path block is the guard working as designed). | None. **Disposition scan:** closes no register candidate; the fact-correction-scope WATCH candidate is separately reinforced by Sweep 106's A-1 (see the sweep record). |
| 2026-07-15 | #957 | none (four public-site text/typography fixes: landing hero two-line, §01 tagline one-line, about "thirty years", For-AI hero colour-per-line; website templates only). | The four maintainer-requested website text fixes applied cleanly with the established `&nbsp;`+`<br>` and `nobalance` techniques from the 2026-07-15 website session; `/validate-pr` returned 0 findings and PR #956's QA batched correctly. | No friction observed; routine single-concern website batch with a clean diff. | No new pattern; consistent with the 2026-07-15 website session's typography-control approach (#943-#953). | None. **Disposition scan:** closes no register candidate. |
| 2026-07-15 | #956 | none (sibling-doc fix: `ai/procedure-integrated-ai-and-privacy-assessment.md` "third review"->"fourth review", surfaced by #955's `/validate-pr` cross-reference check). | The #955 fact-correction-scope lesson was applied immediately: a corpus-wide bare-token "third review" grep confirmed the fix is now complete (no other TBS-Directive carrier remains; the sole residual is an unrelated "third reviewer" in the code-review procedure). `/validate-pr` returned 0 findings. | No friction; a clean one-line factual fix plus the batched #955 QA records. | No new pattern; this PR is the fix-side of #955's fact-correction-scope observation. | None. **Disposition scan:** closes no register candidate; reinforces (does not graduate) the #955 fact-correction-scope WATCH candidate. |
| 2026-07-15 | #955 | none (Canada AI annex accuracy corrections + new AI Strategy for the Federal Public Service 2025-2027 section, v1.0.1; maintainer-requested ahead of a Canada AI Alliance expert review). | The layered verification worked as designed on an expert-facing, deadline-driven change: three parallel adversarial fact-checkers against the held `grc_library_ref` primary sources, then a pre-push skeptical verifier, then the `/validate-pr` cross-reference check. Maintainer-provided canada.ca primary pages resolved a web-search conflict definitively, and the FIRST-EVER skeptical-verifier override was exercised cleanly (verifier flagged HIGH "trusted", orchestrator refuted it against the primary source, maintainer confirmed "responsible", logged in `verifier-overrides.md`). | The fact-check and fix were scoped to the touched annex only, so the identical "third review" error in a sibling doc (`ai/procedure-integrated-ai-and-privacy-assessment.md:76`) was not caught until the `/validate-pr` cross-reference check; a corrected FACT should be corpus-wide-greped at fix time, not just file-scoped. Separately, the CHANGELOG initially over-claimed "verified against held primary sources" for the newly-added FPS-strategy section (NOT in the ref base; it was verified against maintainer-provided canada.ca pages); the pre-push verifier caught the over-claim. | Fact-correction-scope: a factual correction propagated in the touched file but not corpus-wide (the same stale fact recurred in a sibling doc). Adjacent to the standing "full-file-grep / parallel-case" and "corpus-wide completion grep" close-out disciplines, but those are file-scoped or completion-claim-scoped; this is the fix-TIME obligation to bare-token-grep a corrected fact across the whole corpus. First occurrence in the recent window (historical analogue: the FR-44 shall->must corpus-scope miss, #455/#458, well outside 5 PRs), so observation. | Behavioural candidate: when correcting a factual error (date, ordinal, name, count) in a corpus doc, corpus-wide bare-token-grep the stale claim BEFORE finalizing the fix PR and fold sibling carriers into the same PR (or route them); extends the prose-fact completeness close-out line to fact corrections at fix time. WATCH (fires on a 2nd occurrence in-window); the `/validate-pr` cross-ref check already backstops it. Sub-lesson: the CHANGELOG over-claim reinforces the meta-prose measured-not-inferred discipline (scope a verification claim to what was actually verified, and where). **Disposition scan:** closes no register candidate. |
| 2026-07-15 | #953 | none (website batch item 4: per-type listing pages + clickable "By document type" chips, Option A). | The data-driven generation made 17 new public pages correct-by-construction (one `type.html` template + a `TYPE_SCOPE` map + a render loop, with a build-time completeness check and a per-type count assertion), so the skeptical verifier found NO defect across all 9 targets (domain non-regression after the CSS DRY move, chip<->page integrity, counts summing to 312, unique canonicals). `/validate-pr` clean but for one bookkeeping-staleness warning. | W-1 (`/validate-pr`): #953 re-stamped the lease heartbeat + `Active-session` shape fields but left the session-state Current-task / Worker-dispatches PROSE a PR stale (still describing #952). Non-gated (gate 63 checks only shape fields); `/resume`-narrative accuracy only. | THIRD bookkeeping-refresh-lag slip this session, all caught by the QA layer: (1) #950-retro-row table corruption caught by the pre-push guard/gate 50; (2) #951 P3 `Next item number` counter not advanced, caught by `/validate-pr`; (3) this #953 session-state prose one PR behind, caught by `/validate-pr`. Common class: a paired/secondary bookkeeping surface refresh lagging the substantive edit. All three DIFFERENT surfaces, all caught pre-escape. | AUTO-GRADUATION (3+ occurrences): the class is real but each instance is a distinct free-prose surface the QA layer already catches (guard/gate 50 + `/validate-pr`), so no single false-positive-free gate fits. Behavioural reinforcement (restate at close-out): when a PR touches ANY bookkeeping surface (a `.working/` table, a section counter, the session-state lease), refresh EVERY stale part of that surface in the same edit, not just the mechanical/shape field. WATCH candidate for a targeted mechanical backstop only where a surface has a checkable invariant (e.g. a session-state Current-task freshness check keyed on the latest-merged PR number, analogous to the D7 version-token check); fires on a 4th occurrence. **Disposition scan:** closes no register candidate. |
| 2026-07-15 | #952 | none (website batch item 3: landing eyebrows + green-check line -> boxed CTA). | Clean landing-polish PR: the six count-free eyebrow taglines + the CTA conversion shipped with the orphaned `verified-note` CSS removed (dead-code hygiene), the skeptical verifier found no high/medium finding, and `/validate-pr` returned 0 findings. The W-1 counter fix from #951 landed here correctly. | No material friction. `/validate-pr` surfaced a cosmetic session-wide note (below). | The `/validate-pr` N-1 note: every merge-commit subject this session carries a doubled `(#N) (#N)` suffix, because the PR title already contains `(#N)` and `gh pr merge --squash` appends the PR number again. Cosmetic (git-message-only, no tracked artefact), but session-wide. | Behavioural (adopted now): OMIT the trailing `(#N)` from PR titles, since `gh` squash-append adds it; the commit subject then reads cleanly with a single `(#N)`. Applied from #953 onward. No gate/register candidate (a git-message cosmetic, not corpus-affecting). **Disposition scan:** closes no register candidate. |
| 2026-07-15 | #951 | none (website batch item 2: sidebars on For-AI/domain/Contributors + DRY sidebar CSS + scrollspy trio-highlight). | The DRY refactor (shared sidebar CSS moved to `head-style.html`; pack reduced to a non-sticky override) shipped correct, with the critical pack-clipping non-regression holding by source-order specificity; a refute-briefed skeptical verifier found no defect pre-push, and the pre-push guard itself caught a real self-inflicted defect (my #950-retro-row insert had corrupted the improvement-log table, dropping the #949 row's `\| date \| #PR \|` prefix; gate 50 flagged it, fixed + re-verified before push, so it never shipped). | The #951 `/validate-pr` caught W-1: I added `### 3.77` to TODO but did not advance the Priority-3 `**Next item number:**` counter (should be 3.78). A paired-surface miss under the permanent-numbering counter convention adopted 2026-07-15. | Counter-not-advanced-when-adding-item: the permanent-numbering scheme made `**Next item number:**` a NEW paired surface that every item-add must bump; #951 missed it (1st occurrence of this specific shape). Recurrence-adjacent note: two `.working/`-table-editing slips this session (the #950-row corruption caught by the guard; this counter miss), both around bookkeeping-table edits. | Behavioural: when adding a numbered TODO item, advance that section's `**Next item number:**` counter in the SAME edit (the counter is a paired surface, like TODO/DONE rotation). If it recurs, add a mechanical backstop (a check that the max `### N.M` in a section is strictly below the section's `Next item number`). Fixed in #952 (counter -> 3.78). **Disposition scan:** closes no register candidate. |
| 2026-07-15 | #950 | none (maintainer-directed website "License" (s) spelling fix under §2.4). | The fix was complete in visible text with the internal (c)-spelled identifiers correctly left untouched (no half-rename: `id="licence"`, `.licence-*` classes, `#licence` anchors all still resolve), and the single shared-footer edit propagated the correction to all 15 rendered pages from one place. `/validate-pr` returned only one low cosmetic note; `run_all_audits.sh` 69/69. | The detailed-mirror #950 entry left three `.web/templates/*.html` paths as BARE code-spans (unlinked); `/validate-pr` Subagent A caught it. Root cause traced: BOTH `tools/preflight-changelog.py` and the CI gate `tools/lint-changelog-link-coverage.py` share a `FILE_EXTENSIONS` tuple lacking `.html`/`.css`/`.js`, so web-template path references are not recognized as path-shaped and escape both the pre-commit aid and the link-coverage gate (the mirror is additionally `.working/`-exempt, but a bare `.html` in the gated ROOT `CHANGELOG.md` would also slip). | The detailed-mirror bare-path class, now traced to a mechanical gap: the CHANGELOG link gates' extension list predates the website era and does not know `.html`/`.css`/`.js`, so every website PR that cites a template path must link it by hand or it slips. This is the file-type-width axis applied to the link gate's own detector. Recurs whenever a website PR cites a template path. | ROUTED to TODO (new item): add `.html`, `.css`, `.js` to `FILE_EXTENSIONS` in BOTH `tools/preflight-changelog.py` and `tools/lint-changelog-link-coverage.py` (kept in sync per the GR-11 two-parser seam), with a regression fixture, and re-scan the root `CHANGELOG.md` for any newly-flagged bare web-template path before landing. Own follow-up PR (tooling priority, after the website batch); not bundled here (gate-detection change needs its own fixture + multi-surface pass). This PR (#951) fixes #950's three bare paths by hand and links all `.html` paths in its own entry. **Disposition scan:** closes no register candidate. |
| 2026-07-15 | #949 | §3.76 (TYPE_ORDER + sort-key parity guard). | The guard-validity discipline caught my own interim regex bug in the new test before push (I ran it, it failed on the correct generators because a non-greedy match truncated the sort-key tuple at the inner paren, and I fixed it to capture-to-EOL), and the #948 §2.16 rotation-miss was caught and corrected here. `/validate-pr` clean in-window (69/69, test 2/2, rotations complete). | Minor: the §N-orphan cross-FILE close-out grep for §2.16 (run at #948/#949) did not surface a gate-blind `(TODO 2.16)` provenance reference in `.web/build.py:389` (a `.py` docstring, outside the corpus gates); `/validate-pr` caught it as out-of-window. | The gate-blind non-`.md` cross-FILE carrier class again (a closed-§ reference surviving in a `.py`/generator docstring). The CLAUDE.md close-out already codifies the file-type-width axis (grep non-`.md` on renames/closes); the miss was applying it, not the rule. | Behavioural: on a §-close, run the orphan grep over `.py`/generator docstrings too, not just `.md` siblings (file-type-width axis, already in CLAUDE.md). The `.web/build.py:389` provenance ref is a reword-on-next-`.web/`-touch candidate (fold into the next site-adjustment PR); it reads as design-origin provenance so keeping it is defensible. Also a non-actionable future-brittleness note: the new test's secondary-key `assertIn` is quote-style-coupled (double-quote literal); harden to quote-agnostic if `.web/build.py` ever switches quote style. **Disposition scan:** closes no register candidate. | 
| 2026-07-15 | #948 | §2.16 (residual: scrollspy + Get-started nesting). | The scrollspy shipped correct with graceful degradation and a drift-proof DOM-derived parent map; the pre-merge verifier's two SHIP-WITH-NOTES were both fixed in-window (comment scope; lowest-in-view selection so nested sub-items are not shadowed). `/validate-pr` returned zero findings for #948 (the one gate-36 regression FAIL it saw was correctly diagnosed as a concurrency artefact from the orchestrator's in-flight §3.76 test edit, out-of-window). | Real self-caught process slip: #948 COMPLETED §2.16 (built its full residual) but MISSED the TODO->DONE rotation, §2.16 was left in TODO. Caught in the very next PR (#949) while building §3.76 and corrected there (deleted §2.16 from TODO, added the DONE entry, reworded the stale TODO:64 "adoption follow-up §2.16" forward-ref). | Rotation-timing slip: a PR that completes the LAST residual of a MULTI-PR item can forget the rotation because the item was already partially-shipped and "feels" done. Distinct from the #495 prose-named-item miss; this is the residual-completion shape. Observation/early-signal. | Behavioural close-out reinforcement (the D5 PR-time check + gate 57 catch explicit closure markers, but a "completes a residual" close may carry no closure phrase): when a PR ships the final piece of a multi-PR item whose earlier pieces are already in DONE, treat it as a closing PR and rotate (delete from TODO + add the closing DONE entry keyed by this PR). Watch for a third occurrence to consider a mechanical backstop. **Disposition scan:** closes no register candidate. | 
| 2026-07-15 | #947 | none (maintainer-directed website attribution link under §2.4). | The advise-then-build flow worked cleanly: recommending the existing AUTHORS.md (already carrying attribution posture + APA/BibTeX how-to-cite) over a new CITATION.md avoided content duplication and drift; the maintainer confirmed Option A and the change was a one-line link + a trailing-slash consistency fix. `/validate-pr`: 0 findings. | No friction observed. | None (single clean PR). | none. **Disposition scan:** closes no register candidate. | 
| 2026-07-15 | #946 | none (maintainer-directed + plan-approved website feature under §2.4). | The full research-assistant + high-assurance discipline stack worked on a genuinely new, sensitive artefact: a research worker produced a coverage-honest resource map ([annex]/[cited]/[ref-only] flags), the orchestrator verified and authored it (catching 3 apply-time corrections: dropped the ~45 AI-doc count for a qualitative description since taxonomy shows 47; omitted an unverifiable AICM version string; fixed a bad register path ai/ -> governance/), no unverified external URL was shipped, and a refute-briefed skeptical verifier confirmed **SHIP**. The maintainer's explicit prompt-injection concern was met by a strict descriptive/true-false register on every sentence, independently verifier-confirmed clean; the maintainer then approved the live copy. `/validate-pr`: 0 findings. | The post-commit audit caught one gate failure: the new sitemap.xml embeds the standard `www.sitemaps.org` XML namespace URI, which `lint-external-link-domains` (it scans `.py`) flagged. Fixed by an allow-list addition following the existing grclibrary.ai site-infrastructure precedent (a protocol namespace, not a citation publisher, so no citation-spec §7 update). | 1st occurrence of a NEW shape: the `.web/` generator now emits files carrying standard external URLs/namespaces (the sitemap namespace), so a new external-URL-bearing output can trip the external-link-domain gate. Distinct from the prior-PR shapes. | Behavioural (no gate change; the gate is correct and the allow-list is the right lever): when a `.web/` generator change introduces a new external URL or namespace into an emitted file or the generator source, add the domain to `lint-external-link-domains.py`'s ALLOW_LIST in the same PR (site-infrastructure domains, not citation publishers, follow the grclibrary.ai precedent and need no spec §7 update). Watch for recurrence. **Disposition scan:** closes no register candidate. | 
| 2026-07-15 | #945 | none (maintainer-directed website polish under §2.4). | Clean small website consistency fix; the §05 counts (312 / 11 / 310 / 2) are computed by construction in the generator (`domain_docs = total - root_count`), so the reworded note's arithmetic cannot drift, and Subagent A verified it live. `/validate-pr` returned ZERO findings. | No friction observed. | None (single clean PR; no recurrence). | none. **Disposition scan:** closes no register candidate. | 
| 2026-07-15 | #944 | none (maintainer-directed website polish under §2.4; no numbered TODO item closed). | The landing-card kicker-link pattern gave a clean consistency fix with NO CSS change (global `a` is brass + hover-underline and `.mode .m-k` is already brass, so wrapping the kicker label in `<a>` matches the landing `.feat` kicker links). Render-verified 3/3 links pre-push; Subagent A independently confirmed all three targets exist in the repo and the HTML is balanced. | The #944 `/validate-pr` returned one note: `.working/next-prs.txt` line 1 was 150 chars, over the documented ~120-char console-statusline budget, so the 3rd queue item truncated out of the `next:` glance. Fixed in #945 (this PR) by trimming line 1. | 1st occurrence of the next-prs.txt line-1 overlength shape (the format directive is recent, 2026-07-14). Root: packing three descriptive items into one line easily exceeds 120 chars. Observation, not yet a pattern. | Behavioural (no gate; the statusline reads only the truncated first line, and a gate on a gate-exempt `.working/` file's cosmetic length would be low-value): when rewriting `next-prs.txt` line 1, keep each of the 3 items to a few words and eyeball the line at <=120 chars before commit. Watch for recurrence; if it recurs, a tiny length-check aid (not a corpus gate) could warn. **Disposition scan:** closes no register candidate. | 
| 2026-07-15 | #943 | none (Sweep 105 /validate close-out; no TODO item closed). | The loop-break `/validate` did its job at a session boundary: the asserted-expectations cross-check flagged the A-1 website ordering defect as a CONTRADICTS-ASSERTED-CLEAN, catching that the #942 handoff's "#940 consistent" self-assessment was wrong (the prior session verified TYPE_ORDER identical but never checked the secondary sort key). Subagent A then independently re-derived the A-1 fix's correctness (0/12 domains diverging post-fix vs 6/12 pre-fix), a stronger confirmation than the sweep's render spot-check. | The A-1 defect existed because the #940 session's "consistent" claim rested on the shared constant (TYPE_ORDER) without checking the full sort key. Separately, my own Sweep 105 detail file conflated the pre-flight candidate COUNT (11) with the candidate-FILE count (6) ("All 6 pre-flight FP candidates"), caught by the #943 `/validate-pr` and fixed in #944. | (a) **Paired-artefact parity beyond the shared constant** (1st explicit occurrence): asserting two generators are "consistent" from an identical shared constant, without checking the rest of the derivation (here the secondary sort key). (b) **Meta-prose count-and-label granularity** recurs (the project-tracked #630/#631/#633 class; a count stated next to an enumeration at a different granularity), this time files-vs-candidates. | (a) CODIFIED this session: TODO §3.76 widened so the planned DRY guard asserts the WHOLE per-domain sort key (rank + secondary tiebreaker), not only TYPE_ORDER; the guard is the false-positive-free mechanical answer. (b) No new gate (free `.working/` prose the CLAUDE.md "Meta-prose state-claim measurement" guard already covers; recurrence confirms it is hard to fully prevent on prose, caught reliably by the QA layer). **Env note (not friction):** gh-CLI merge of an own-authored PR needs `gh pr merge --admin` to clear the REVIEW_REQUIRED branch-protection block (the self-review gate the maintainer waives; the MCP path resolves it automatically). **Disposition scan:** (a) graduates the paired-generator-parity concern to the §3.76 widening; closes no other register candidate. | 
| 2026-07-15 | #941 | §2.16 partial (landing-nav two-level nesting shipped; scrollspy + Get-started nesting remain). | The nav rebuild delivered the maintainer's observed-problem fix (nav no longer "ends with domains"), and the adopted full-field lease rewrite (the #940 [3rd-occurrence] control) worked, Subagent A confirmed the append-not-reconcile class is genuinely closed (single green-at, no stale tokens). The dedicated nav verifier confirmed all 6 std-* anchors resolve + page-scoping before push. | The pre-push guard caught a gate-50 todo-rotation failure: my §2.16 residual annotation led with a bold uppercase "SHIPPED in #941" marker, which the gate reads as an in-place self-completion marker on a still-present TODO item. Fixed pre-push (reworded to lowercase "landed in #941, recorded in DONE"). | Minor (1st occurrence of this specific shape): a PARTIALLY-done TODO item annotated in-place with an uppercase completion marker (SHIPPED/DONE/CLOSED) trips gate 50's todo-rotation check, which cannot distinguish "partial, residual remains" from "closed but not rotated". | Behavioural: when annotating a partially-completed TODO item (done part rotated to DONE, residual kept), describe the done part in lowercase prose ("landed in #N, recorded in the closed-work ledger"), never a leading bold SHIPPED/DONE/CLOSED marker. No gate change (gate 50's conservatism is correct; the fix is the phrasing). **Disposition scan:** closes no register candidate; observation logged. | 
| 2026-07-15 | #940 | Domain-page document ordering (type-priority) shipped in both generators; TODO §3.76 opened. | The mid-build correction worked: on finding the taxonomy source-reorder alone would not change the website pages (build.py re-sorts), I verified build.py's actual sort, fixed it too, and corrected the already-written CHANGELOG to match reality (measured-not-inferred) rather than shipping the wrong claim. Both HA/skeptical verifiers + Subagent A caught nothing substantive. | #940's `/validate-pr` found the append-not-reconcile warning AGAIN (this session's #937 W1, then #940 W1): I updated the lease tail + Active-session + heartbeat but left the Current-task LEAD carrying the prior PR's snapshot (branch, merged-through, green-at). Fixed in #941 by a full-field rewrite. | **THIRD occurrence of the session-state append-not-reconcile class this session** (#937 lead-updated/tail-stale; #940 tail-updated/lead-stale). Root cause: the Current-task field is a long multi-sentence paragraph, and PATCHING individual sentences reliably leaves a stale one somewhere else in it. The D7 gate covers version tokens only; branch/green-at/merged-through prose stays gate-blind. | `[3rd-occurrence]` Behavioural control adopted + applied in #941: on EVERY lease update, REWRITE the entire Current-task field wholesale (never patch individual sentences), so no carried sentence can go stale. Structural follow-on to consider: shrink/structure the Current-task field (it is too large to patch safely) so a resume reads a short reconciled state. No new gate (free working-state prose; D7 already covers the version-token subset). **Disposition scan:** graduates the append-not-reconcile class to this adopted full-rewrite control. | 
| 2026-07-15 | #939 | §3.43 AICM matrix-fit applied (7 fabricated codes -> real AICM v1.1.0); §3.47 rescoped + §3.8 closed (maintainer decisions). | The full finding-to-fix loop closed cleanly across sessions: the §3.43 FP-census found the live fabricated-AICM-code defect, it was routed to the maintainer (not guessed), the maintainer approved the matrix-fit mapping, and the fix applied against the held catalogue with gate 48 + a dedicated verifier + the post-merge Subagent A all ZERO/clean. Surfacing the §3.47-vs-#929 scope-conflict and the §3.8 fidelity trade-off as maintainer questions (rather than executing them) got clean decisions. | None. The post-merge sweep returned 0 findings; the matrix-fit was maintainer-approved and title-grounded. | No new pattern. This is the intended behaviour of the /matrix-fit + clarify-before-acting disciplines: a gate-blind semantic defect surfaced by a census, decided by the maintainer, fixed against the held source. | None (working as designed). **Disposition scan:** closes no register candidate. | 
| 2026-07-15 | #938 | none (bookkeeping: batch #937 QA + route the §3.47-vs-#929 scope-conflict + reconcile session-state). | The append-not-reconcile fix from #937 held: #938's `/validate-pr` re-checked the session-state Current-task field in full and confirmed the HOLDING/EXHAUSTED contradiction and the stale §3.10-deferred entry were genuinely gone (not just papered over). The §3.47-vs-#929 scope-conflict was surfaced (surface-counterproductive-instructions) rather than executed, avoiding a sweep that would have stripped breadcrumbs #929 protects. | #938's `/validate-pr` found 2 cosmetic notes: F1 the next-prs statusline first line was 145 chars (over the ~120 cut), F2 a future-tense residual in the session-state tail. Both fixed in-batch. | No new pattern. F1 (statusline line length) and F2 (tense residual on a reconciled field) are both the general "long working-state prose drifts past a soft limit / carries a stale tense after a partial edit" class, already covered by the reconcile-the-whole-field discipline reinforced in the #937 retro; no separate codification. | Behavioural: after editing the next-prs first line, measure it (`wc -m`, counting each `§` once) against the ~120 cut; after reconciling a Current-task field, re-read the WHOLE field for tense consistency, not only for factual contradictions. Both are low-severity adherence reminders, no gate (free working-state prose). **Disposition scan:** closes no register candidate. | 
| 2026-07-15 | #937 | TODO §3.10 (fence-predicate consolidation; GR-4 tilde-blindness closed via shared `lint_common.is_fence_line()`). | Two things worked. (1) **Corrected an over-conservative "hold" call**: the prior turn concluded the overnight-safe queue was exhausted and held; on re-reading no-idle-stop + the maintainer's "proceed through any TODO items that you can in HA mode", §3.10 was recognized as genuinely buildable and shipped, rather than idling. (2) The §3.43 **FP-census (HA stage 2) did its job twice**: it refuted my initial "multi-segment tokens are FP-safe" design assumption AND surfaced a live fabricated-AICM-code defect, both caught BEFORE any bad gate shipped. Two independent HA verifiers on the refactor returned ZERO findings. | The post-merge `/validate-pr` found W1 `session-state-append-not-reconcile`: I reconciled the Current-task LEAD to "continued, not holding" but left the carried-forward TAIL asserting "HOLDING / queue EXHAUSTED" and listing the now-done §3.10 as deferred. Fixed in-batch. | **Append-not-reconcile on the session-state Current-task field recurred** (the class the CLAUDE.md session-migration checklist names; prior instances #619/#622/#628 were on the handoff Current-truth line, D7-backstopped for version tokens; this one was on the lease PROSE tail, which no gate checks). Prepending a corrected lead while leaving a stale multi-sentence tail is the specific shape. | Behavioural (no new gate; the tail is free prose no gate inspects): when reconciling the session-state/handoff Current-task, re-read the ENTIRE field to EOF and reconcile every carried assertion (holding-vs-continuing, done-vs-deferred item lists, green-at sha, branch tokens), not just prepend a lead. The checklist already says "re-read the retained tail"; this recurrence is an adherence miss, reinforced here. **Disposition scan:** closes no register candidate; reinforces the reconcile-the-whole-field discipline (the D7 gate covers version tokens only; the prose tail stays convention-guarded). | 
| 2026-07-15 | #936 | none (overnight resting-point close-out: refreshed next-prs to queue-exhausted state, batched #935 QA, held in overnight mode). | The bookkeeping close-out shipped clean; `/validate-pr` Subagent A SHIP 0/0/0 with the overnight-summary's #929-#936 §-attributions all verified against their commits. The pre-push guard's D5 check caught a self-inflicted phrasing defect before it reached CI (see Friction), the gate working as designed. | D5 (backlog-rotation-on-PR) FAILED at first guard run: the #936 CHANGELOG overnight-summary REFERENCED past PRs' closures using D5's own closure idioms ("rotated to DONE", "§3.50 CLOSED"), which D5 read as a CURRENT closure assertion needing a TODO/DONE rotation this bookkeeping PR does not have. Reworded to "moved to the DONE ledger" / "§3.50" (dropping the closure verbs) and D5 passed. | **New observation (1st occurrence): a CHANGELOG summary that narrates PAST closures trips D5's closure-form detection.** D5 cannot distinguish a historical reference ("#933 closed §3.50") from a current closure claim; a narrative/roll-up entry that recaps prior PRs' closures will false-fire D5 unless it also rotates TODO/DONE (which a roll-up PR does not). | Behavioural: when a CHANGELOG entry narrates or recaps PRIOR PRs' closures (an overnight-summary, a session roll-up), phrase them WITHOUT D5's closure idioms, use "moved to the DONE ledger" not "rotated to DONE", and "§N" or "the §N work" not "§N CLOSED" / "clos... §N". First occurrence = observation; no gate change (D5's conservatism here is correct, the fix is the authoring). **Disposition scan:** closes no register candidate; logs the D5-historical-closure-phrasing observation (fire on recurrence). | 
| 2026-07-15 | #935 | none (overnight prep: staged §3.22/§3.12 protected machinery for daytime, assessed §3.38, folded #934 preflight-message note). | Cleanly staged the two protected items for a quick daytime apply (per the overnight protocol), recorded an accurate §3.38 assessment, and folded in #934's preflight-message note; both HA verifiers SHIP-WITH-NOTES (an 11->10 count + a name-framing note, resolved pre-push). | The post-merge `/validate-pr` found W1: #935 did not touch `next-prs.txt`, leaving it stale (still listing §3.38/§3.22 as next while #935 deferred both). The step-6 "every PR touches next-prs.txt" discipline was missed on a PR that was itself re-scoping the queue. | **Recurrence of the next-prs-refresh-miss class** (the #847-era drift the step-6 discipline targets): a PR that CHANGES the queue's shape must refresh next-prs.txt in the SAME PR; #935 updated session-state + CHANGELOG but not next-prs.txt. | Behavioural: touch next-prs.txt in EVERY PR, especially one that re-scopes the queue; the /validate-pr caught it (the gate-less status control). No new gate (next-prs is convention-guarded). **Disposition scan:** closes no register candidate; reinforces the every-PR-next-prs-touch discipline. | 
| 2026-07-15 | #934 | none (§3.34 go-forward preflight link-resolution check; §3.34 stays OPEN for the historical-dangler cleanup). | The go-forward check shipped FP-safe (10-case behaviour test + a full-mirror census of 1948 links confirming no legitimate class newly-flagged) and dogfooded clean on its own CHANGELOG lines; both HA verifiers SHIP; the census surfaced ~23 historical danglers, recorded under §3.34 as attended cleanup rather than lost. | The post-merge `/validate-pr` found F1 (cosmetic): the new check was named in the docstring block + per-finding line but not in four other preflight user-facing message surfaces (exit-code docstring, argparse, OK, FAIL summary), the SAME within-file parallelism class as #933's docstring note that THIS PR had just folded in. Fixed in #935. | **The within-file multi-carrier parallelism class recurred a THIRD time** (cf. #933 gate-69 docstring; the #933 note folded into #934; now #934's preflight messages): adding a check to a multi-check tool requires updating EVERY user-facing enumeration of its checks (docstring, exit-code list, argparse, success + failure messages), not just the docstring + finding line. | Behavioural: when adding a check to a multi-check tool, grep the tool for every user-facing enumeration of its checks and extend each, the intra-file full-file-grep guard on tool-message surfaces. Now 3 occurrences; **WATCH candidate**, codify a close-out/worker-brief line if it recurs a 4th time. No new gate (cosmetic tool prose). **Disposition scan:** closes no register candidate; logs the intra-file-parallelism WATCH (fire on 4th). | 
| 2026-07-15 | #933 | §3.50 (widen gate 69 to the "TODO item N.M" phrasing, FP-safe). | The gate widening shipped correct: the FP analysis (corpus census, 0 live carriers) cleanly justified the widen-not-document choice; the widened regex passed behaviour tests across both pre-push HA verifiers + the post-merge sweep (up to 20 cases); both fixtures pin the detect + clean cases; all gate-change surfaces (regex, docstring, §6 narrative, fixture, regen) updated. | The post-merge `/validate-pr` found F1 (cosmetic): a THIRD in-file gate-69 docstring carrier (the scope-summary at line 18) still named two qualifier forms while the lead example + regex comment + §6 narrative named three; the #933 pre-push HA verifier's spec:178 fix did not extend to the docstring summary line. Fixed in #934. | **Multi-carrier-parallelism within a single file**: a widening whose description lives in several spots of one file (lead example + regex comment + scope-summary + §6 narrative) needs ALL spots updated in step; #933 updated 3 of 4, leaving the scope-summary line. | Behavioural: when widening a gate whose pattern is described in several places within its own file, grep the WHOLE file for every description of the old form (here "TODO or backlog item") and update each, the full-file-grep guard applied intra-file to a tool docstring. No new gate (cosmetic docstring prose). **Disposition scan:** closes no register candidate; reinforces the full-file-grep guard. | 
| 2026-07-15 | #932 | none (§2.5 AI-delta umbrella split; the "split into components" case, completes the massive-item split wave). | The intricate split (retire an umbrella + create 5 new items + orphan cleanup) shipped clean: the guard caught 2 issues pre-push, both fixed; the two HA verifiers both SHIP (all 11 §2.5 sub-parts accounted for, no dropped workstream, no live dangling §2.5); the post-merge `/validate-pr` returned SHIP 0/0/0. | Two pre-push guard catches, both mechanical, both fixed pre-push: (a) the re-homed items' "Re-homed from the retired §2.5" provenance tripped gate 18 (intra-doc §2.5 no longer a heading), reworded to drop the section token; (b) the HA-register edit for the orphan cleanup needed a Version bump (D2). Neither escaped. | **The retire-a-numbered-TODO-section change has a distinct two-part footprint**: (a) provenance prose that references the retired §N trips the intra-doc-ref gate (gate 18), and (b) any versioned `.working` file touched for the §N-orphan cleanup needs its own Version bump (D2). | Behavioural: when retiring a numbered TODO section, (a) phrase re-homed-item provenance WITHOUT the bare `§N` token (name the umbrella, not the retired number), and (b) version-bump any versioned `.working` file edited for the orphan cleanup. The guard (gate 18 + D2) is the working control that caught both. No new gate. **Disposition scan:** closes no register candidate; codifies the retire-a-section footprint. | 
| 2026-07-15 | #931 | none (§2.4 website-umbrella split: build effort rotated to DONE, §2.4 kept OPEN for remaining maintainer-gated items). | Clean at both stages: the two pre-push HA verifiers (correctness + completeness) returned SHIP / SHIP-WITH-NOTES and the post-merge `/validate-pr` returned SHIP 0/0/0. The completeness verifier caught a stale lease branch-token pre-push. | The lease Current-task paragraph's opening branch token was left at the previous PR's branch (`claude/todo-split-massive-items`) while the Active-session header was correctly updated. Caught pre-push by the HA completeness verifier, not shipped. | **Recurrence of the append-not-reconcile / lease-Current-task-lag class** (cf. #619/#622/#628 handoff snapshots; #929 lease-lag): a lease refresh reconciled the header but not the Current-task paragraph's branch token. | Behavioural: when refreshing the lease at close-out, reconcile EVERY branch/PR token in the Current-task paragraph, not only the Active-session header (gate 63 checks only the header, so the prose token is convention-guarded). The HA completeness verifier is the working control. No new gate. **Disposition scan:** closes no register candidate; reinforces the reconcile-not-append lease discipline. | 
| 2026-07-15 | #930 | none (first TODO split: §3.57/§3.62/§3.63/§3.68 completed components rotated to DONE, residuals kept). | The split shipped clean at all three stages: the two pre-push HA verifiers (correctness + completeness) both SHIP (all 18 cited PRs verified genuinely complete; no dropped residual, no adjacent item disturbed) and the post-merge `/validate-pr` returned SHIP 0/0/0. First split under the overnight HA-for-all regime. | None at the verification stages. One out-of-window observation: the `next-prs.txt` first line exceeded the ~120-char statusline guidance (a long-standing pattern, not regressed by #930); tightened in #931. | No new failure class; the dual-HA-verifier + post-merge sweep layering had nothing to catch on a clean trim-to-residual rotation. The next-prs first-line-length is a recurring low-grade drift worth keeping short each write. | Behavioural: keep the `next-prs.txt` first line to roughly 120 chars (the statusline truncation width) each write, so items 2-3 surface. No new gate. **Disposition scan:** closes no register candidate; confirms the split rotation pattern is mechanically sound under HA. | 
| 2026-07-15 | #929 | none (TODO permanent-numbering framework + 4 recorded items + a gate-69 rationale multi-surface fix). | The framework shipped correct after adversarial review: the two pre-push high-assurance verifiers (correctness + completeness) caught four issues, all fixed pre-push and re-verified SHIP, and the post-merge `/validate-pr` confirmed SHIP 0/0/0. | The P6 counter was mis-seeded to 6.7 because the seeding grep matched a document-section token `§6.6` (not a TODO item); and the P1 count fix went stale inside its own PR (corrected "one"->"two" but missed the +§1.14 the same PR added, so it should have been "three"). Both caught pre-push by the HA verifiers, not shipped. | **The count-next-to-enumeration / meta-prose-measurement class recurred** (P1 count; cf. #919 F1, #928), plus a NEW seeding-grep-scope class (P6): a bare `§N.M` ref-grep catches non-item document-section tokens and inflates the per-section max. Both are the "measure from the authoritative structure, not a broad grep" lesson. | Behavioural: seed per-section counters from the `### N.M` HEADINGS (the authoritative item structure) rather than a `§N.M` reference grep; and recount an enumeration in the same edit as the count beside it. The dual HA verifiers were the working control that caught both pre-push. No new gate. **Disposition scan:** closes no register candidate; reinforces the measured-not-inferred guard and adds the counter-seeding-from-headings lesson. | 
| 2026-07-15 | #928 | none (§2.4 website nav consistency: five-CTA hero, pack sidebar lists all skills, Get-started cards match the page; §2.4 stays OPEN until publish). | All three maintainer-flagged fixes verified correct against the rendered output at both stages: the pre-push skeptical verifier returned SHIP (0 material) and the post-merge `/validate-pr` confirmed the 5 CTAs, the 6 `.k`-link Get-started cards, and all 13 rule + 23 skill pack-sidebar links with the clip CSS fully removed. | The post-merge `/validate-pr` found F1 (note): the #928 detailed-CHANGELOG entry said "all 18 feature cards consistent" when only 15 of the 18 use the `.k`-link pattern (the 3 pack-teaser cards deliberately carry none). Internal, gate-exempt record; fixed in #929. | **Third in-session recurrence of the meta-prose-count-imprecision class** (cf. #919 F1, #923 F1): a count written from a mental model ("all 18") rather than the measured set (15 of 18). Caught in-window by the sweep. | Behavioural, reinforcing the close-out "Meta-prose state-claim measurement" guard (measure the enumeration in the same edit; state the precise granularity). No new gate (free prose in a gate-exempt record); the post-merge sweep is the working control that caught it. **Disposition scan:** closes no register candidate; reinforces the measured-not-inferred guard. | 
| 2026-07-15 | #927 | none (working-state close-out: §2.6 TODO watch-paths item + the fourth build.py page-set carrier fix + batched #926 QA; §2.4 stays OPEN until publish). | The class flagged at #926 was closed properly: the fourth page-set carrier (the argparse `--help` description) was fixed with a full-file grep confirming all four carriers include the pack page, and the post-merge `/validate-pr` returned SHIP (0/0/0), confirming the closure held and the terse root+detailed entries are paired. | None (0 findings at the post-merge sweep). The recurring build.py page-set class from #921/#923/#925/#926 did not recur this PR (it was the thing being closed). | No new failure class; this PR is the corrective close of the four-occurrence page-set-enumeration class, verified clean at both stages. | No change; the full-file-grep close (all 4 carriers) is the durable behavioural fix, and the tiered verification (pre-push verifier at #928 + this post-merge sweep) confirmed it. The mechanical docstring-vs-PAGES check remains a logged out-of-website-scope watch-item. **Disposition scan:** closes no register candidate; confirms the four-occurrence class is behaviourally closed. | 
| 2026-07-15 | #926 | none (§2.4 landing restructure, closes the website effort; §2.4 stays OPEN until publish). | The restructure verified clean at both stages: the pre-push skeptical verifier returned SHIP (0 defects; content-preservation confirmed, the ShareAlike block byte-identical) and the post-merge `/validate-pr` confirmed section integrity, nav resolution, and no content loss. | The post-merge sweep found F1: the `build.py` argparse `--help` description was a FOURTH page-set carrier the #925 N1 fix missed, so the #925 "3 carriers fixed" completion claim was one short. Internal help-text only; fixed in #927 with a full-file sweep. | **Fourth in-session recurrence of the build.py page-set-enumeration completeness class** (#921, #923, #925, #926): the enumeration lives in FOUR carriers (module-docstring line 1, WHAT-THIS-IS, CONTENT-BOUNDARY output, argparse description); each prior fix swept a subset, leaving one. Root miss: fixing the cited carriers rather than grepping the whole file for the fact. | Fixed the class properly in #927 (full-file grep, all 4 carriers now include the pack page). Strongly reinforces the standing full-file-grep guard; the durable structural fix, a mechanical docstring-vs-PAGES check, stays a logged out-of-website-scope watch-item, now with four occurrences of evidence. **Disposition scan:** closes no register candidate; 4-occurrence reinforcement of the full-file-grep guard. | 
| 2026-07-15 | #925 | none (§2.4 adoption round, the `/pack` page; §2.4 stays OPEN until publish). | The pack page shipped with verified accuracy: the pre-push skeptical verifier caught + fixed one Medium rule-description misstatement, and the post-merge `/validate-pr` confirmed all 13 rule + 23 skill targets exist, the 3 adoption modes match the README, and the content boundary was unchanged. | Two in-window internal-only notes at the post-merge sweep: N1 the `build.py` docstring page-set omitted the newly-added pack page (3 carriers); N2 the lease Current-task was one PR behind. Both fixed in #926. | **Third in-session recurrence of the `build.py` stale-docstring class** (N1; cf. #921 N1, #923 F2): a page-set fact changed in the code and a docstring carrier was not swept in the same edit. N2 is the lease-narrative-lag class (Current-task deferred a refresh). | Behavioural: continue the full-file-grep sweep on build.py docstrings when PAGES or a row shape changes; and refresh the lease Current-task every PR rather than deferring (the deferral is what produced N2). The only structural fix for N1 would be a mechanical docstring-vs-PAGES check, out of scope for the website work; logged as a watch-item. **Disposition scan:** closes no register candidate; reinforces the build.py-docstring watch-item and the every-PR lease-refresh habit. | 
| 2026-07-15 | #924 | none (§2.4 landing contents sidebar; §2.4 stays OPEN until publish). | The sidebar shipped clean: the pre-push skeptical verifier returned SHIP (0 defects across 7 axes) and the post-merge `/validate-pr` returned SHIP (0/0/0), both confirming the shell restructure left about/domain pages untouched, the content boundary unchanged, and the 6 section + 11 domain links resolving. First zero/zero PR of the website round. | None (0 findings at both verification stages). The only standing note is the deliberate landing/pack sidebar-CSS duplication, flagged in-file and slated to DRY into the shared partial. | No new failure class; the layered verification (pre-push verifier + post-merge sweep) had nothing to catch. | No change; reinforces that the tiered verification is calibrated (a low-risk restructure passed both stages clean). **Disposition scan:** closes no register candidate; the sidebar-CSS DRY is a tracked cleanup, not a finding. | 
| 2026-07-14 | #923 | none (§2.4 title-as-link + domain spacing; §2.4 stays OPEN until publish). | Title-linking shipped correctly: the post-merge `/validate-pr` verified all 15 landing card links resolve to their claimed corpus targets (all 14 distinct targets exist), the 6 pack-rule cards link to their rule files, the domain doc rows collapsed cleanly to a single title-link with no leftover "GitHub" link, and the spacing change stayed scoped to the domain pages. | Two in-window internal-only notes: F1, the #923 detailed-CHANGELOG said "16 target files" when it is 15 links to 14 distinct files (STRUCTURED+STYLE share the master spec) - the "16" was the earlier candidate-verification count carried into the ship claim; F2, the `render_domain_doc_rows` docstring still described the old two-element (title + separate GitHub link) row after the code merged them into one anchor. Both fixed in #924. | **Recurrence of the measured-not-inferred (F1) and stale-docstring / full-file-completeness (F2) classes** (cf. #919/#920/#921/#922, this session's dominant recurring class): a ship-claim count written from an earlier check rather than the final measured set, and a docstring carrier not swept when the code just below it changed. | Behavioural, reinforcing the close-out guards: (F1) write a ship-claim count from the FINAL measured set, not an earlier candidate count; (F2) when changing a function's behaviour, sweep its own docstring in the same edit. Both free-form / internal; no new gate. **Disposition scan:** closes no earlier register candidate; reinforces the measured-not-inferred + full-file-grep guards. | 
| 2026-07-14 | #922 | none (§2.4 per-domain website pages; §2.4 stays OPEN until the Cloudflare publish go). | The per-domain-pages generator shipped with a verified content boundary and correct partitioning: the pre-push skeptical verifier (0 critical/high, 0 leaks) and the post-merge `/validate-pr` both confirmed the 310 documents partition correctly across the 11 pages with no cross-page bleed and no leak, and the 13-page / 310 / 2-root figures match across every surface. | The post-merge `/validate-pr` found two in-window warnings: F1, the runbook Overview's two-page content-boundary description was not swept when #922 widened the generator's boundary (the runbook was touched and version-bumped, but only its watch-paths section updated); F2, the new `read_domain_purpose` passed `re.split` maxsplit positionally (a Python 3.13+ DeprecationWarning). Both fixed in #923. | **Recurrence of the multi-surface / full-file completeness class** (F1; same class as #920/#921): a file was touched and version-bumped for a change, but a SECOND description of the same fact elsewhere in that file was left stale. F2 is a Python-version-currency nit (positional-arg deprecation) not previously tracked for the `.web/` generator. | Behavioural for F1 (reinforce the full-file-grep guard: when a change's fact is described in more than one section of a touched file, sweep ALL sections, not just the headline one). For F2, a watch-item: the `.web/` generator runs on the CI build image's Python, so prefer keyword args for stdlib calls whose positional forms are deprecation-tracked. No new gate (both free-form / warning-level). **Disposition scan:** closes no earlier register candidate; F1 reinforces the multi-surface guard, F2 logged as a Python-currency watch-item. | 
| 2026-07-14 | #921 | none (§2.4 website review polish; §2.4 stays OPEN until the Cloudflare publish go). | The layered QA held: the pre-push work was clean-on-mechanicals and the post-merge `/validate-pr` (refute-briefed, read-only-git) confirmed all 11 off-site links carry `target="_blank"`, versions agree across surfaces, and per-document bumps are present. Net zero adopter escape at #921. | The post-merge `/validate-pr` found two `build.py` docstring carriers of the multi-page fact ("figure on the page", "seeded the template") that PR1's verification grep missed because it used the narrow phrase `"landing page\|the template are"` rather than a full-carrier sweep. Internal-help text only, fixed in #922. | **Recurrence of the full-file-grep / file-type-width completeness class** (cf. the #920 retro): a prose-fact correction verified with a phrasing-specific grep rather than a full-file sweep leaves sibling carriers of the same fact stale. Here the residual was in the SAME `.py` file, one narrow grep away. | Behavioural, reinforcing the existing close-out "Full-file-grep for prose corrections" guard: after a docstring/prose plurality correction, grep the WHOLE file for every form of the fact (singular "the page"/"the template", "N pages", "landing + about"), not the one phrase just fixed. No new gate (free-form docstring prose). **Disposition scan:** closes no earlier register candidate; reinforces the full-file-grep guard (same class as #920). | 
| 2026-07-14 | #920 | none (§2.4 website two-page restructure; §2.4 stays OPEN until the Cloudflare deploy). | The restructure into shared single-source partials means the landing and about pages cannot drift apart; the post-merge `/validate-pr` (run fresh by the resumed VM session after the cloud session crashed, refute-briefed, read-only-git) confirmed the content boundary intact and the contributor list matches `AUTHORS.md` 7/7. Subagent A's three citation-edition doubts were all REFUTED against the canonical register (current, recently-verified editions), the register cross-check catching a stale-general-knowledge false positive. | The #920 restructure updated the `build.py` content-boundary docstring paragraph to plural ("templates and partials", "landing + about pages") but left three sibling singular carriers ("landing page"/"the template") at `build.py:2,29,330`; caught by this post-merge `/validate-pr` and fixed in #921. Internal-help text only, no adopter surface. | **Partial-surface docstring update**: when a change pluralizes one docstring passage, the file's OTHER docstring/help carriers of the same fact were not swept (a full-file grep for the old singular phrasing would have caught all three at once). Same completeness class as the close-out full-file-grep guard, applied to a Python file's module docstring + argparse help. | Behavioural, reinforcing the existing "full-file-grep for prose corrections" close-out guard: extend the reflex to a touched `.py` file's module docstring AND argparse/help strings, not only `.md` prose (the file-type-width axis). No new gate (free-form docstring prose a gate cannot police). **Disposition scan:** closes no earlier register candidate; reinforces the full-file-grep / file-type-width guard. | 
| 2026-07-14 | #919 | none (§2.4 public-website build; §2.4 stays OPEN until the Cloudflare deploy). | The public-site build shipped with a verified content boundary: the pre-push website skeptical verifier returned SHIP, and the post-merge `/validate-pr` (Subagent A, refute-briefed) independently confirmed the generator reads a three-file allow-list (taxonomy, README, template) and walks nothing, the rendered template leaks no email / internal path / sibling-repo / absolute path, and all 40-plus landing-page citations match the register. The batched #918 phantom-catch correction was correctly withdrawn across all six carriers with the gate-50 Findings cell (`c[4]`) provably untouched. | IN THE ACT of correcting #918's phantom "5th fix", the #919 root CHANGELOG entry introduced a NEW count-imprecision: it said "the six records carrying it are reworded" when only four were reworded (one cell reverted, one merge-commit-message body immutable). Caught by the post-merge `/validate-pr` (F1, note, adopter-facing), so it reached `main` in #919 and was fixed in #920. The landing page also carried an ASVS version-form nicety (F2, "OWASP ASVS 5.0" vs register 5.0.0, within tolerance; deferred to the maintainer-directed TODO §3.74). | **Recurrence of the meta-prose count-imprecision class at bookkeeping-authoring time** (cf. #662/#663/#664 and the #918 phantom): a count in a correction record ("six ... reworded") that does not match the measured breakdown (four reworded + one reverted + one immutable). Notable that it recurred WHILE fixing the prior instance of the same class. | Reinforces the existing close-out "Meta-prose state-claim measurement" guard (measure the breakdown from the records before writing the count); no new gate, since it is free prose a gate cannot police and the post-merge `/validate-pr` is the working control that caught it. Separately, the maintainer-directed standards-reference-format standardization (TODO §3.74) will settle the F2 citation-form class repo-wide. **Disposition scan:** this #919 retro closes no earlier register candidate; it reinforces the measured-not-inferred guard and routes the citation-form standardization to §3.74. |
| 2026-07-14 | #918 | none (Sweep 104 `/validate` close-out; no backlog item closed). | The layered QA caught the defect in-window: the post-merge `/validate-pr` Subagent A (refute-briefed) refuted a fabricated "5th fix" #918 had recorded, and the four genuine fixes (C-1 + A-1/A-2/B-1) verified clean. The loop-break Sweep 104 itself was sound (all #917 asserted expectations corroborated, 0 contradicted). | #918 shipped a phantom "catch" to merged `main` (including the adopter-facing root CHANGELOG): I claimed a latent gate-50 Check-1 failure in #917's exemption row and "fixed" it, but #917's Findings cell (the cell gate 50 reads, `c[4]`) already carried the marker; I had read the adjacent Touched-files cell (`c[3]`) and edited that instead. An inferred-as-verified state assertion: I characterized a table cell without reading the actual cell the gate consumes. | Third-plus occurrence of the measured-not-inferred / read-before-characterizing class at bookkeeping-authoring time (cf. the #662/#663/#664 meta-prose-measurement instances already codified as the close-out "Meta-prose state-claim measurement" guard). New sub-shape: a "does the next PR break a latent gate" claim asserted from prose inspection of a cell rather than from running the gate against the real file. | Behavioral, reinforcing the existing guard: a latent-gate claim must be VALIDATED by running the gate/tool against the actual committed file (here, gate 50 / `parse_validate_pr_status` against #917's row), never inferred from reading a cell, per `validate-inference-before-action`. The close-out "measured-not-inferred" checklist line already covers the class; no new gate proposed (the claim is free prose a gate cannot easily police). **Disposition scan:** this #918 retro closes no earlier register candidate; it reinforces the measured-not-inferred guard and routes the "validate-the-gate-not-the-cell" sub-shape as a watch-item. |
| 2026-07-14 | #916 | TODO §1.2 (root CHANGELOG #902-#914 compact reformat + readability-guard strengthening). | The adopter-facing readability regression is fixed (13 dense run-on entries compressed to plain two-sentence form, faithfulness worker-drafted + orchestrator-verified + skeptical-verifier-checked against the mirror) and the guard now catches dense sub-130-word run-ons via a longest-sentence signal. The layered QA held: a pre-push verifier caught D1/D2 and the post-merge `/validate-pr` caught F1/F2, so net 0 adopter escape across the reformat. | The insert-above / table-row-clobber class recurred REPEATEDLY on this PR's own bookkeeping: the #915 detailed-mirror header and the #914 validate-pr row (both caught by #916's post-commit audit, gates 59/50/36), then the #916 validate-pr row (caught by an explicit read-back) - the SAME row-insert operation failing a 4th time this session, once (F1@#915) having escaped to `main`. Plus four meta-prose measurement slips (D1/D2 word ranges + #912 over-generalization; F1/F2@#916 docstring + off-by-one range). | DECISIVE RECURRENCE (mid-run degradation, beyond the #891 pattern-graduation): the table-row-join/insert-above class failed the SAME mechanical operation 4x this session, one escape to main, one more under explicit read-back attention. This is the mid-run degradation signal the wind-down framework acts on (a specific operation repeatedly wrong), not mere caught-slip density; it drove the maintainer-confirmed session-closing handoff at #917. | (a) The §3.73 ledger-table-row-integrity gate is ROUTED, but this PR's design analysis found the naive column-count check is NOT FP-free (~50 legit unescaped-prose-pipe rows across the ledgers) and the escaped-defect signature needs a smarter detector (mid-row row-start pattern + per-ledger sequence-gap); build deferred to a fresh session with the analysis recorded in §3.73. (b) INTERIM row-insert technique (codify): insert a ledger row by anchoring the `Edit` on the HEADER line alone and appending the new row AFTER it, NEVER matching-and-re-appending the next row's leading cells (the technique that failed 4x); this structurally cannot clobber the adjacent row. **Disposition scan:** #917 (this handoff) closes no earlier register candidate; it routes §3.73 and this interim technique. |
| 2026-07-14 | #915 | None (Sweep 103 `/validate` close-out, first PR of the resumed session; no backlog item closed). | The loop-break Sweep 103 confirmed the closing session's #901-#914 window clean at the corpus level (A: 0 / B: 0, 0 asserted-expectation contradictions), and the one in-window note (gate-50 §6 detailed prose omitted the #913 Check-5) was a documentation-completeness slip fixed in-window and refute-verifier-confirmed. The mandatory post-merge `/validate-pr` then caught a bookkeeping defect that had ESCAPED the pre-push layer, the QA cadence working exactly as designed (a slip past one layer caught by the next; 0 adopter escapes). | I introduced a table-row-join in `.working/validate-sweeps/history.md:15` (the Sweep 103 row Edit lacked the trailing newline before the retained Sweep 102 row, so the 102 row lost its `\| Date \| Sweep \|` identifier) AND skipped the post-edit pipe-count/join-residual self-check the #891 retro reinforced. Unlike every prior occurrence of this class, it ESCAPED to `main` (caught post-merge, not pre-push), because `validate-sweeps/history.md` is NOT read by gate 50 (which reads only `validate-pr/history.md` + `improvement-log.md`) and I omitted the self-check. Fixed in this PR (#916). | PATTERN + NEW SEVERITY (the table-row-join / ledger-row-fusion class): the class the #891 retro already graduated to PATTERN (occurrences #347/#462/#498/#656/#887/#888/#891), but #915 is the FIRST to ESCAPE the pre-push layer. Every prior occurrence was self-caught pre-commit (gate 50/36 or the pipe-count check), which is why #891 declined a gate ("the self-check catches it"). The #915 escape refutes that rationale: the class recurs AND now escapes when the fused rows land in a ledger gate 50 does not read AND the self-check is skipped. A convention alone has not extinguished a class that now escapes. | AUTO-GRADUATE to a gate (the escape is the trigger the #891 no-gate disposition rested on not happening): a `.working/` ledger-table-row-integrity check over the reverse-chronological bookkeeping tables (`validate-pr/history.md`, `improvement-log.md`, `validate-sweeps/history.md`, `deep-assessment/register.md`) asserting every non-separator table line begins with `\|` and carries the expected column count; FP-free, register/ledger-less forks yield no findings. It would have caught #915 pre-push. gate 50 already reads two of these ledgers despite the `.working/` exemption, so extending a STRUCTURAL check to the sweep-history + register ledgers is consistent. **Routed to TODO §3.73 (P3 machinery)** and surfaced to the maintainer. Disposition scan: this REFUTES the #891 row's "no new gate" rationale; #916 closed no earlier register candidate. |
| 2026-07-14 | #913 | r3 machinery G3: deep-assessment register row-order guard as gate 50 Check 5 (maintainer-authorized). | Built G3 as a check WITHIN gate 50 (the no-count-ripple pattern, like Check 4) rather than a standalone new gate, the right lightweight implementation for a low-churn 3-row register (avoids gate-count ripple + new-gate maintenance). The skeptical verifier EARNED ITS KEEP: it caught a genuine CORRECTNESS defect (the fork-safe read used `read_text_safe(...) or ""`, but that helper re-raises FileNotFoundError and the read was outside main()'s guard, so a register-less fork would crash, falsifying the fork-safe claim I asserted in 3 surfaces); fixed in-window with an `.is_file()` guard, verified. | I misread `read_text_safe`'s contract (assumed it swallows FileNotFoundError; it catches only decode errors) AND asserted a false fork-safe property across the docstring + CHANGELOG + TODO. This is the session's FIRST non-bookkeeping CORRECTNESS defect (prior catches were carrier/count/co-bump bookkeeping); caught pre-push by the verifier, 0 escape. | **PATTERN [precision-strain, escalating]: the verifier moved from catching bookkeeping misses (carrier-width, count-reflex, co-bump) to catching a genuine CODE-CORRECTNESS defect (a misread API contract + a false property claim) this deep (#913, ~13 PRs in).** All still caught in-window (the layered verification holding, 0 adopter escapes), but the shift from bookkeeping-slips to a correctness-slip is a clearer first-pass-precision-degradation signal than the earlier catches. | **This signal drove the wind-down: surfaced the continue-vs-handoff decision to the maintainer (status check), who chose the clean session-closing handoff (#914).** The remaining machinery (G1 hook, handoff-D-check) + the routed forks are recommended for a fresh/attended session. No new gate/convention (the skeptical-verifier tier IS the working control; the lesson is the honest degradation read + the maintainer-surfaced wind-down). **Disposition scan:** #913 codified/routed no earlier register candidate; G3 was itself the build. | 
| 2026-07-14 | #912 | FR-201 vuln-remediation-SLA single-source completion (§3.68 partial): 4 clear conversions + 4 routed. | The §3.68 research worker found the FR-201 (#904) "no other document diverges" claim was over-stated well beyond the one carrier the #904 `/validate-pr` caught: a full-corpus enumeration surfaced ~8 actionable carriers (incl. a dev-security policy FR-201 had left, and a significant pentest divergence under a FALSE "aligns with" claim) vs the §3.68 note's named 3. Applied the 4 CLEAR stricter-safe conversions (restatements/looser values -> SoT citations) and ROUTED the 4 authorial divergences (pentest intent, supplier-tier scale, exception-duration authority, KPI drift) rather than guessing. `/validate-pr` returned 0 findings; the pre-push verifier found no defects. | No friction of note (0-finding validate-pr, verifier-clean). | **PATTERN [meta, find-every-carrier]: a PRIOR pass's own "find-every-carrier confirmed no remaining divergence" claim was itself carrier-incomplete** (the FR-201 #904 verifier + the §3.68 note both under-scoped; the #912 whole-corpus worker found more). Same shape as the #911 register/change-mgmt scope-misses. The durable lesson (already applied here): for a "complete the rollout" item, dispatch a FRESH whole-corpus carrier-enumeration worker rather than trusting a prior pass's named subset; and route value-divergences (authorial) rather than converting them. | **No new codification** (the lesson, dispatch-fresh-whole-corpus-enumeration + route-authorial, is now applied practice and recorded across the #909/#910/#911/#912 retros). **Disposition scan:** #912 codified/routed no earlier register candidate; the §3.68 authorial divergences routed to §3.68 (kept open). | 
| 2026-07-14 | #911 | ISO/IEC 20000-1 clause-attribution accuracy pass (closes §3.72); 11 clause citations corrected across 7 docs. | High-value corpus accuracy fix: the pass corrected 11 genuine adopter-facing ISO/IEC 20000-1 clause mis-attributions (2 fabricated "§8.6 Service management" labels, 2 wrong-clause citations incl. Configuration management under §8.5), all verified verbatim against the held source. The skeptical-verifier LOOP was the hero: it caught 2 carriers my first-pass scoping missed (the change-mgmt doc via a `grep 20000-1` pattern-width miss on its bare "ISO/IEC 20000" column; the governance doc-index register via 9-doc scope-width), plus the count-reflex survivors, all fixed in-window; 0 adopter escapes. | The initial pass under-scoped twice (pattern-width: the file-discovery `grep 20000-1` missed the bare "ISO/IEC 20000" column; scope-width: the "9 operations docs" frame missed the governance register), and the 10/6->11/7 count expansion then left 2 stale-count survivors (F1 detailed-Fixed lead, F2 next-prs) + 1 paired-surface register bare-form (F3), all caught by the verifier/post-merge. | **PATTERN [completeness-family, strongly recurring; two sharp sub-shapes]:** (a) FILE-DISCOVERY pattern-width, a clause/citation pass's discovery grep must use the BARE standard token ("ISO/IEC 20000"), not the part-qualified form ("20000-1"), or a carrier under a part-omitting label is silently dropped; (b) MID-PR count-change reflex, when a count changes mid-PR (here 6/5->10/6->11/7 across two verifier rounds), a stale survivor of the OLD count keeps surviving in a non-lead sentence or a sibling `.working` file. Both are the find-every-carrier family; the verifier loop caught every instance (0 escapes). | **Discipline sharpened (convention, no gate; the loop is the backstop):** (a) file-discovery for a standard-citation pass greps the bare standard name AND enumerates carriers whole-corpus, not just a worker's named subset; (b) when a count changes mid-PR, bare-token grep the OLD count in BOTH digit and word form ("6"/"six", "5"/"five", "10"/"ten") across EVERY surface (the CHANGELOG entry's non-lead sentences, next-prs.txt, session-state) before push. The skeptical-verifier + re-verify loop remains the mechanism that caught all of this in-window. **Disposition scan:** #911 codified/routed no earlier register candidate. | 
| 2026-07-14 | #910 | ISO/IEC 20000 family reference-breadth review (closes TODO §3.67); TS 20000-11 see-also added; clause-mis-attribution routed to §3.72. | The research-worker `/reference-audit` new-ingest discipline produced an honest few-finding outcome (corpus already engages 20000-1 well) rather than citation-stuffing; the one applied addition (TS 20000-11) was verifier-clean (verbatim citation + currency confirmed). Crucially, the incidental clause-mis-attribution finding was ROUTED to a full-cluster §3.72 `/claim-fit` pass rather than fixed as the worker's 2-of-9 spot-check, correctly avoiding the carrier-incomplete trap the session keeps guarding against. `/validate-pr` returned 0 findings. | The guard's D2 then D4 caught a version-bearing `.working/` file (`reference-audit/history.md`) that I bumped neither Version nor Date when adding its row, because I co-bumped the frequently-touched versioned working files (validate-pr/history, improvement-log) but skipped the less-frequently-touched reference-audit/history. Two amend cycles pre-push. | **PATTERN [completeness-family, recurring sub-shape: version-bearing `.working/` file co-bump]:** when a PR touches a versioned `.working/` ledger, ALL such ledgers touched need Version+Date co-bumps, and the recurring miss is the less-frequently-touched ones (reference-audit/history, validate-sweeps/history) forgotten while the routine ones (validate-pr/history, improvement-log) are remembered. D2/D4 are the mechanical backstop and caught it (the gate working). | **No new codification (the gate already handles it):** D2 (per-PR version-bump) + D4 (Version-Date co-bump) already scan every versioned `.working/` file and caught this, so the durable control exists; the residual is authoring-side. Habit reinforcement: when adding a row to ANY `.working/*/history.md` or versioned ledger, bump its Version+Date in the same edit as writing the row. **Disposition scan:** #910 codified/routed no earlier register candidate. | 
| 2026-07-14 | #909 | r3 machinery low-precision: gate-25 scaffold retire + gate-41 docstring nit + G5 #376 expire (closes TODO §3.64, rotated to DONE). | The layered verification worked as designed on a gate-logic change: the guard caught a missed spec Version+Date co-bump (gate 40/36) pre-push, the pre-push skeptical verifier caught a stale `design-decisions.md:399` term-count carrier, and the post-merge `/validate-pr` caught a stale `session-handoff.md:52` §3.64 reference. All three fixed in-window (two pre-push, one in #910); 0 adopter escapes. The measured-not-inferred guard also corrected a stale "Check 1" to Check 4 by reading the gate-50 source rather than transcribing the pending-decisions note. | THREE same-family catches on ONE PR (#909): (1) spec co-bump omission [guard], (2) design-decisions.md:399 carrier [verifier], (3) session-handoff.md:52 carrier [post-merge]. The two carrier misses share a root cause: my §N-orphan / retirement grep covered the corpus + TODO + frozen dated `.working/` registers, but SKIPPED the LIVING `.working/` ledgers (design-decisions.md, session-handoff.md) by mentally filing them with the frozen records. | **PATTERN [completeness-family, recurring; specific sub-shape: living-`.working`-ledger carriers skipped by the orphan grep].** The find-every-carrier family is heavily codified, but the recurring miss this session is specifically that a rename/retirement/section-close carrier survives in a LIVING `.working/` ledger (`design-decisions.md`, `session-handoff.md`, `pending-decisions.md`) because those are gate-blind (`.working/` exempt) AND get lumped with the genuinely-frozen dated registers when scoping the orphan grep. **Convention refinement (WATCH -> propose at next occurrence; no gate, `.working/` is gate-exempt by design):** when a PR retires/renames/closes a token or section, the whole-repo orphan grep must EXPLICITLY include the living `.working/` ledgers (design-decisions.md, session-handoff.md, pending-decisions.md) as in-scope, distinct from the frozen dated registers (`deep-assessment/register.md`, `guardrail-reviews/`, `validate-pr/YYYY-*`, `deep-assessment/2026-*`) which stay moment-in-time. The layered catch (guard + verifier + post-merge) is holding, so this is authoring-side precision, not a detection gap. **Disposition scan:** #909 codified/routed no earlier register candidate; the G5 #376 candidate was itself EXPIRED here (its disposition stamped on the #376 row above). | 
| 2026-07-14 | #908 | Root CHANGELOG compact-form reformat #887-#901 + advisory length guard (closes TODO §3.65). | The research-assistant discipline applied cleanly to a BULK EDITORIAL task (not just content research): one worker drafted all 15 compressions from the detailed mirror, the orchestrator verified each against the mirror before applying and caught one worker embellishment at apply-time (#890's unsupported "regenerating the derived artefacts" clause, dropped). Subagent A's independent re-check of all 15 compressions returned 0 findings, corroborating the apply-time verification. The advisory tool's recall was independently verified (858/858 header-shaped lines matched, 0 skipped) before shipping the correctness claim - the measured-not-inferred discipline applied to a tool's own recall. | No friction of note (0-finding `/validate-pr`). The one apply-time catch (preflight flagged 4 unlinked path-shaped refs in the #908 CHANGELOG entry, incl. the config-file names in the tool bullet) was the aid working as designed pre-commit; reworded to prose/links and re-ran clean. | No new pattern. The preflight path-span catch is a further occurrence of the already-covered new-prose-introduces-path-spans class (the close-out pre-flight line + the aid are the standing guard); the reformat-faithfulness 0-finding result is the research-assistant discipline working as designed on an editorial task. | None new. **Disposition scan:** #908 codified/routed no earlier register candidate (the G5 #376 disposition rides #909, not this PR). | 
| 2026-07-14 | #907 | DA-ISO20000 (r3 §3.64) RESOLVED + RB-ETSI-104128 (r3 §3.63) primary see-also; FR-210 `:432` residual completed. | The missing-reference-document SOP worked end-to-end: DA-ISO20000 was unjudgeable at r3 (only ISO 20000-10 held), the maintainer ingested the 20000-1 family, the resume resynced the ref, and #907 verified + tightened the citation against the now-held source. The verifier (Subagent A) confirmed the ETSI TR 104 128 number+title verbatim against the held catalogue, and I VALIDATED its F1 recommendation against local convention rather than auto-applying it (see Pattern) - the verifier-finding-handling loop working as designed. | The post-merge `/validate-pr` found 2 in-window TODO-bookkeeping findings: F1 = the §3.64 DA-ISO20000 bullet kept its now-false "could not be judged against ground truth" text after #907 resolved it; F2 = the §3.63 RB-ETSI bullet's "add see-also" prose went stale once the primary shipped. I marked the resolutions in the CHANGELOG but left the TODO cluster bullets' state claims stale. Both fixed in #908. | **OBSERVATION [new sub-shape, single occurrence = watch]:** "stale-state-text left in an in-place-resolved TODO-cluster bullet" - when a routed-findings cluster (§3.64/§3.63) marks a bullet resolved in prose, the bullet's ORIGINAL state claim (what was unheld / unjudgeable / undone) can be left standing and become false. Distinct from the carrier-completeness family; it is the meta-prose measured-not-inferred rule applied to TODO resolution-marking. Also a verifier-context note: Subagent A's F1 recommendation (delete+DONE per step-7) over-applied the rotation rule to a SUB-bullet in an in-place-resolution cluster; the orchestrator validated against the cluster convention (siblings are `RESOLVED #NNN` in place) and adjusted the fix, confirming why verifier recommendations are validated, not auto-applied. | **Discipline (convention-level, no gate; single occurrence so WATCH not propose):** when marking a TODO/cluster bullet RESOLVED in place, re-read and correct any now-false state claim the bullet carries in the SAME edit (the meta-prose measured-not-inferred guard at TODO-resolution time). Fires as a pattern on a 2nd occurrence. **Disposition scan:** #907 codified/routed no earlier register candidate; F1/F2 fixed in #908. |
| 2026-07-14 | #906 | none (r3 fitness-tail batch). | The in-batch fixing of the verifier's two find-every-carrier findings (F-2 privacy-policy DSR carrier, F-1 internal-audit title residuals) applied the concept-wide discipline the prior retros firmed; the 7 fitness fixes + 3 routed forks landed clean with all values verified against the held annexes/register. | The post-merge `/validate-pr` caught a 4th carrier-completeness miss: a bare "GRC Manager" survived at `standard-internal-audit.md:432` because my own residual COUNT used a whole-line `grep -vE "GRC Programme Manager"` that suppressed the line (it contains BOTH the bare form and the fixed "CAE/GRC Programme Manager"), so I reported "0 bare" when there was 1. Fixed in #907. | **PATTERN [carrier-completeness, recurring; new sub-shape = GREP-FORM]:** the running find-every-carrier / completion-claim-width class (DA-DORA-A12 #899, F-1 #904, F-1 #905, now the grep-form miss #906) gained a concrete mechanism this time: a WHOLE-LINE `grep -vE <newform>` count HIDES a residual on any line carrying BOTH the old and new form, producing a false "0 residual". | **Discipline refined (concrete, habit-level):** count old-vs-new carriers with TOKEN-level `grep -oE '<oldtoken>'` (which counts occurrences, not lines), NEVER a whole-line `grep -vE '<newtoken>'` (which suppresses mixed-form lines), especially for a within-line rename where old and new forms co-occur. This is the grep-fidelity companion to the concept-wide (#904) and value-vs-metric (#905) refinements; the per-PR `/validate-pr` remains the backstop (caught all four; 0 escaped to the adopter-facing corpus). No gate (free-form grep discipline). **Disposition scan:** #906 codified/routed no earlier register candidate; the :432 residual fixed in #907. | 
| 2026-07-14 | #905 | none (FR-202/203/204 r3 value cluster). | The reconcile-to-governing was clean and the skeptical verifier's per-value find-every-carrier surfaced the CA citation section error (4->6) and the two P-256 dev-guidance carriers before push; both genuine forks (FR-205, P-256 tables) were routed rather than guessed, honouring the GRC-not-solutioning + route-forks directives. | The post-merge `/validate-pr` again caught a completion-claim-width phrasing miss (F-1): the #905 `.working` detailed-mirror said "last permissive TLS-1.2 carrier in the corpus" while two deliberately-retained OWASP ASVS-baseline pack lines still show "TLS 1.2+" (with a TLS-1.3-canonical caveat); adopter-facing root CHANGELOG was correctly scoped. Softened in-window. | **PATTERN [3rd occurrence, AUTO-GRADUATES]: over-broad completion/scope claims ("last carrier in the corpus", "no other document diverges") in the `.working` detailed mirror.** #899 DA-DORA-A12 (carrier-incomplete), #904 F-1 (missed a 3rd value), #905 F-1 (over-broad "in the corpus" excluding deliberate pack carriers). Each is Low/note, self-mitigating, confined to the maintainer-grade mirror (the adopter-facing root CHANGELOG has been precisely scoped every time), and the per-PR `/validate-pr` catches each. | **[3rd occurrence] Convention firmed (not gateable, free prose):** a corpus-wide completion/scope claim in any record MUST be bounded to the enumerated/verified set ("among the GRC governance documents", "among the enumerated carriers"), never unbounded ("in the corpus", "no other", "the last"), unless a concept-wide grep over ALL file types (incl. the pack) backs the unbounded form. The `/validate-pr` corpus-wide grep is the durable backstop (0 escapes; all three caught + corrected in the mirror, none reached the adopter-facing root CHANGELOG). No gate proposed (the claim is free prose; a gate would be FP-heavy). **Disposition scan:** #905 codified/routed no earlier register candidate; F-1 softened in-window. | 
| 2026-07-14 | #904 | none (FR-201 r3 fitness High; vuln-SLA single source of truth). | The cite-don't-restate single-source approach cleanly resolved the divergence, and the skeptical verifier's find-every-carrier pass + the MTTR-metric catch (my first fix tied SCA's mean-time metric to the SLA deadline 7/14 rather than the SoT's own tighter metric 5/12) both improved the result before push. | The post-merge `/validate-pr` caught **F-1**: a THIRD divergent value (`standard-software-evaluation-acceptance-and-lifecycle.md:103` "Critical patches within 15 days", looser than the SoT's 7-day Critical) that BOTH the pre-push find-every-carrier verifier AND I missed, so the #904 "no other document still diverges" completion claim was over-stated by one carrier. Routed to TODO 3.68; over-claim recorded honestly in the #904 `/validate-pr` row. | **PATTERN [2nd occurrence -> signal]: corpus-wide value-completion claims over-state because the find-every-carrier grep is VALUE-SPECIFIC, not CONCEPT-wide.** #899 DA-DORA-A12 was carrier-incomplete (fixed the cited line, missed 2 siblings); #904 FR-201's find-every-carrier grepped the KNOWN divergent values (30/90/180, 14/30/90) and missed a carrier holding a DIFFERENT value (15-day Critical) for the same concept. Both are completion-claim-width misses; the per-PR `/validate-pr` caught each post-merge. | **Reinforced discipline (no new gate; semantic):** a find-every-carrier for a value-SoT reconciliation must enumerate ALL carriers of the CONCEPT (every Critical/High/Med patch-SLA statement anywhere), not only the docs holding the KNOWN divergent values; and a "no other document diverges" completion claim must rest on that concept-wide grep, else soften the claim to "no divergent carrier among the enumerated set". The `/validate-pr` corpus-wide grep is the durable backstop and fired both times. **Disposition scan:** #904 codified/routed no earlier register candidate; F-1 folded into TODO 3.68. | 
| 2026-07-14 | #903 | none (FR-200 r3 fitness High; internal-audit Board Audit Committee reconciliation). | The reconcile-to-canonical approach worked cleanly: the corpus already carried the canonical Board-Audit-Committee structure in the compliance policy (§4.2.1/4.3.1/4.6.2), so the fix aligned the internal-audit standard TO it rather than inventing a structure, and the post-merge `/validate-pr` confirmed 0 in-window findings + canonical match verbatim. | The skeptical verifier SHIP'd the minimal §2.1/§2.3 change but flagged that §4.1 (plan approval) and §7.1 (escalation) still routed to the ERC-only, a partial reconciliation; I completed §4.1+§7.1 in-window rather than routing a follow-up TODO. | Not a new pattern, but a good instance of the find-every-carrier discipline applied at the VERIFIER-FINDING boundary: when the verifier flags a partial reconciliation of a value/structure across a doc's sections, complete the sibling carriers in-window (the DA-DORA-A12 lesson: a finding fixed at the cited surface while sibling carriers keep the old value is carrier-incomplete). | No new candidate. The completion was reconcile-to-canonical (matching the verifier-confirmed policy), so safe to do unattended. **Disposition scan:** #903 codified/routed no earlier register candidate. | 
| 2026-07-14 | #902 | none (DA-ASVS r3 High; high-assurance harness). | The high-assurance harness worked exactly as designed on a genuinely sensitive gate-blind change: 2 research workers + 2 independent adversarial verifiers CONVERGED (0 missed carriers corpus-wide, 0 wrong targets), the deterministic scripted apply (69 mappings, dry-run each exactly-1-match, re-parse 0 residual) made the 69-cell multi-file apply's correctness independent of orchestrator in-context precision, and the post-merge `/validate-pr` found 0 findings (the harness held through merge). The research also caught that the r3 finding named only 4 files while a 5th carrier (baseline-and-standards-reference) and the whole pack + sub-req level carried the same drift. | The scope ballooned mid-harness (maintainer expanded to pack + sub-req, then the sub-req research revealed ~40 governance citations with no clean 5.0.0 home); managed by surfacing the re-scope rather than fabricating precision. Guard caught 2 co-bump misses (D2 register Version, D4 pack README Date), both pre-push, routine. | Not a new pattern: the scope-discovery-during-harness is the harness working (the corpus-wide re-enumeration is a designed stage). The maintainer-scope-expansion-then-re-scope is `surface-counterproductive-instructions` functioning in both directions (expand on request, then surface the accuracy risk and re-scope). | No new candidate. The concurrency-race lesson (never run multiple `run_all_audits.sh` at once) from #901 recurred once more this session (my baseline step) and is self-corrected; it stays a convention note, not worth a gate (the tests/tmp fixture race is a test-harness property, not a corpus defect). **Disposition scan:** #902 codified/routed no earlier register candidate; it applied the DA-ASVS finding and deferred Class 2 to §3.66. | 
| 2026-07-14 | #901 | none (Sweep 102 `/validate` close-out; 2026-07-14 resume PR). | The loop-break compensating control worked exactly as designed: Sweep 102's three subagents were clean (0/0/0) and corroborated every #900 asserted-clean claim, and the close-out's mechanical re-baseline then caught the #900 handoff-exemption-row omission (gate 50 + the gate-36 `test_runs_clean_on_corpus_at_head` smoke test) the moment #901's CHANGELOG demoted #900 from highest-numbered. The resume also batched ALL maintainer decisions up front (maintainer up ~1h, then overnight) across two AskUserQuestion rounds, so the overnight run needs no mid-run authorial pauses. | Three items, all caught, none escaped: (1) the #900 handoff-no-row miss (see Pattern); (2) SELF-INFLICTED, I launched four concurrent `run_all_audits.sh` runs during the baseline step, which raced on `tests/tmp/*` fixtures and crashed gates 24/25 with FileNotFoundError, resolved by a single isolated re-run (all 69 green); (3) the #901 `/validate-pr` F1 (the #900 State-snapshot block was only partially reconciled, green-at annotated but the version token + two `<#900 merge sha>` placeholders left stale), fixed in #902. | **PATTERN [the #822 WATCH FIRES, 3rd+ notable occurrence]: the session-closing handoff PR omits its own `SKIPPED (handoff-PR exception)` row in `validate-pr/history.md` at authoring time.** History: #342/#345/#356 (early), #445/#450 (marker-in-wrong-cell, root-caused to a Summary-vs-Findings-cell contradiction in #451), #821/#822 (handoff-no-row miss, WATCH armed: codify on 3rd recurrence), now #900/#901 (WATCH fires). Gate 50 reliably catches it one PR later (at demotion) and the resume close-out fixes it, so the detect-and-fix loop is robust; the miss persists at the AUTHORING end because it relies on the handoff author remembering at the highest-context end-of-session moment. Separately, a NEW 1st-occurrence class: concurrent `run_all_audits.sh` runs race on `tests/tmp` fixtures (self-inflicted, self-corrected). | **[auto-graduated, 3rd occurrence] Proposed: an FP-free PR-time D-check "handoff-self-row"** - if a PR's diff flips `.working/session-state.md` to `Status: released` (the unambiguous session-closing-handoff signal), require a `validate-pr/history.md` row for this PR's own number carrying the handoff marker (`SKIPPED`+`handoff` or `handoff-PR exception`). FP-free (only a session-closing handoff releases the lease), and it extinguishes the class at the AUTHORING end (the handoff PR's own guard fails if it forgot its row), complementing gate 50's one-PR-later catch. Alternative (the #357 framing): codify that the NEXT PR is the designated row-adder (convention-only, accepts the current working flow). Recommend the D-check. Joins the machinery queue alongside G1/G3; maintainer decision (new gate/protected). Convention note (no gate warranted): never run multiple `run_all_audits.sh` concurrently (the `tests/tmp` fixture race); self-corrected. **Disposition scan:** #901 FIRES the #822 handoff-no-row WATCH (graduated to the candidate above); no other earlier candidate codified/routed; #901 touched working records + README + CHANGELOG (no protected-pack). | 
| 2026-07-13 | #899 | FR-207, FR-209, FR-213, RB-FFIEC-CAT, DA-DORA-A12, DA-AIACT-A26 (r3 clear-mechanical remediation batch). | Six maintainer-signed-off findings remediated in one coherent batch, each verified at source; the dedicated refute-verifier confirmed all six against primary/held sources (DORA Art 11/12, AI Act Art 26 + Annex XI, ffiec.gov CAT sunset, the INAI premise, the Board/CEO AUDIENCES index) and caught a real over-statement nit ("directs" vs the FFIEC's non-binding "may consider"), fixed across all 3 carriers. Apply-time restraint held: a tempting AI Act energy-locator precision addition was pared to the grep-verifiable "Annex XI technical documentation" rather than shipped on the subagent's characterization. | Two in-window catches, both by the QA layer post-merge: (1) the guard's D2 caught a missed fitness-history Version bump (body changed, version not); (2) #899's `/validate-pr` caught the DA-DORA-A12 fix as CARRIER-INCOMPLETE - I relabelled only the cited runbook line, but the identical "Article 12 ICT business continuity policy" mislabel survived in two sibling continuity-plan docs. Both fixed (D2 pre-push; the DORA carriers in #900). | PATTERN (the find-every-carrier / bare-token-width scope-width miss, a NAMED close-out-checklist class): when a finding cites ONE line of a token/mislabel, the fix must grep the corpus for the bare token and fix ALL carriers, not just the cited line. This recurred here on DA-DORA-A12 despite being a documented guard; the mechanical backstop (the per-PR `/validate-pr`'s corpus-wide grep) caught it. | Reinforced habit (memory + the close-out checklist already names it): a mislabel/token/value correction is not "resolved" until a corpus-wide bare-token grep of the OLD form returns zero. For DA-DORA-A12 specifically I fixed the cited line and declared it resolved WITHOUT the corpus grep - the exact miss the checklist's find-every-carrier + §N-orphan lines forbid. No new gate (the class is semantic-label mislabels no gate can enumerate; the per-PR `/validate-pr` grep is the durable backstop and it fired). Disposition scan: #899 touched corpus bodies + working records + README (no protected-pack); standing RM-ledger scan: no open RM entry's signal. | 
| 2026-07-13 | #898 | None (deep-assessment r3 Phase 7 routing consolidation + Phase 8 present-for-sign-off). | The r3 assessment reached its terminal gate correctly: Phases 1-7 consolidated into a sign-off-ready finding set, PRESENTED to the maintainer, and HELD (no self-close) until the maintainer signed off via AskUserQuestion. The whole 8-phase assessment ran end-to-end this session (#887-#898) and surfaced a genuine gate-blind high-value defect set (2 fitness Highs + the systematic DA-ASVS) on a corpus that passes all 69 gates, while confirming the quality machinery healthy (0 dead gates, honest QA ledgers, clean secret scan). #898's own /validate-pr returned 0 findings. | None in-window. The one prior-PR note (#897 register lag) was fixed in #898 as intended. | No new pattern. The Phase-8 discipline (present-and-hold, terminate only on explicit maintainer sign-off, no self-close even on a substantial finding set) worked as designed: the maintainer signed off and directed the remediation ordering (mechanical batch now, Highs to a fresh session), which is exactly the deep-assessment/trust-recovery sign-off-is-the-only-terminal-state convention. | No new candidate. The deep-assessment cadence executed cleanly across 12 PRs (#887-#898) with per-PR QA throughout; the sign-off gate held. Disposition scan: #898 touched .working + CHANGELOG + README (no protected-pack, no corpus body); standing RM-ledger scan: no open RM entry's signal. |
| 2026-07-13 | #897 | None (deep-assessment r3 Phase 6: pipeline integrity + QA-ledger meta-audit + adoptability; 1 Low F-1 fixed in-window). | Phase 6 was the honest-backstop phase and came back strong: the QA-ledger meta-audit confirmed the session's QA RECORD is honest (16 formal validate-pr rows, 15 retro rows, 0 sham markers, 0 uncaught escape), not just that the corpus is clean; the full-history secret scan was clean across 1123 commits; branch protection verified active via gh api; the one Low finding (F-1 nightly-sweep SHA-pin) was fixed in-window and aligns with the repo's own strict SHA-pin rule. The far-out-of-window Sweep-22 abbreviation incident being transparently retained (not scrubbed) is an integrity-positive the meta-audit surfaced. | #897's own post-merge validate-pr found 1 in-window NOTE: I advanced the deep-assessment register's TABLE ROW to Phase-6-complete but left its narrative UPDATE-clause paragraph saying "Phase 6 is next", a within-file contradiction (paired-surface status lag). Caught by the mandatory sweep, fixed in this (the next) PR. | PATTERN (paired-surface status-lag, the named #650-#653 / #893-retro class, now recurring on the deep-assessment register specifically: the row and the narrative paragraph are a pair, and I updated the row without the paragraph). This is the 2nd deep-assessment-register instance this session after the #893-noted class; the register carries BOTH a machine-readable phase-cell row AND a prose UPDATE clause, and its own established convention (recorded in the #896 validate-pr row) is to reconcile the UPDATE clause each PR. | No new gate (the lag is on free prose in a frozen `.working` record no gate inspects; the per-PR validate-pr caught it, as designed). Reinforced habit (memory): when advancing the deep-assessment register's phase-cell ROW, reconcile its narrative UPDATE clause in the SAME edit (the register is a row+paragraph pair, like the CHANGELOG root+detailed pair). This is the register-specific instance of the close-out checklist's paired-surface-completeness line. Disposition scan: #897 touched .working + CHANGELOG + README + one workflow file (F-1; no protected-pack, no corpus body); standing RM-ledger scan: no open RM entry's signal. |
| 2026-07-13 | #896 | None (deep-assessment r3 Phase 4 dead-gate deep pass + Phase 5 ground-truth citation sampling; 5 findings routed §3.64). | Phase 5 demonstrated the deep-assessment's core value: on a corpus that passes all 69 gates and that three prior citation passes THIS session (Sweep 101, /claim-fit, /full-qa) found clean, a fresh stratified ground-truth sample still surfaced a systematic gate-blind version-mismatch (DA-ASVS: dev-security declares ASVS 5.0.0 but uses 4.0.3 chapter numbers across >=4 docs). Each discrepancy was orchestrator-verified at source before routing, and #896's own /validate-pr INDEPENDENTLY re-confirmed DA-ASVS (against held ASVS 5.0.0) and DA-DORA-A12 (against held DORA) a second time. Phase 4 confirmed the gate suite healthy (0 dead gates). DA-ASVS correctly routed to the high-assurance register (delicate multi-doc remap) rather than a quick edit. | None in-window. #896's post-merge /validate-pr returned 0 findings across 8 checks. | No new negative pattern. POSITIVE: the detailed-mirror-header-clobber lesson held for a 2nd consecutive PR (#895 header intact at mirror :33; the #896 prepend anchored on the intro boundary and re-appended the consumed header). The finding-class insight worth noting: three same-session citation passes (Sweep 101 / claim-fit / full-qa) all found the citations clean, yet a FRESH-SAMPLE ground-truth lens caught DA-ASVS, i.e. version-currency-of-a-declared-baseline is a distinct axis from citation-verbatim-accuracy that the earlier passes did not stress. | No new gate (version-vs-chapter-number consistency for a declared framework baseline is semantic, not mechanically gateable without encoding every framework's per-version chapter map; the high-assurance harness + the /claim-fit and /reference-audit cadences are the durable instruments). Candidate consideration for a future /claim-fit or /reference-audit brief refinement: when a doc DECLARES a framework version, sample whether its chapter/control cross-references match THAT version's structure (the DA-ASVS axis), surfaced here as the seed. Disposition scan: #896 touched .working + CHANGELOG + README + TODO (no protected-pack, no corpus body); standing RM-ledger scan: no open RM entry's signal. |
| 2026-07-13 | #895 | None (deep-assessment r3 Phase-3 /reference-audit FULL; 2 findings routed §3.63; Phase 3 COMPLETE). | The 4-judge FULL reference-audit confirmed the corpus reference-breadth is strong (2 genuine findings on a 588-item base) AND cleanly closed RB-6(e): the SOC judge enumerated every corpus AICPA-TSC citation and found all at the edition-stable criteria/CC-group level, resolving a held-edition question three prior passes (Sweep 101, /claim-fit, /full-qa) had flagged but not closed. The FFIEC CAT currency finding was primary-source-verified (ffiec.gov, CAT sunset 31 Aug 2025) before routing, not asserted from memory. The header-clobber lesson from #894 WORKED: the #895 detailed-mirror entry was inserted with the #894 header re-appended, and #895's own /validate-pr confirmed the #894 header intact, no clobber. | None in-window. #895's own post-merge /validate-pr returned 0 findings across 8 checks. | No new negative pattern. POSITIVE: the detailed-mirror-header-clobber sub-class (2nd occurrence at #894) did NOT recur at #895 after the #894-retro habit reinforcement (anchor the Edit on the intro boundary / re-append any consumed header + grep both headers present pre-commit). The mechanical guard (gate 59 + gate 36) remains the backstop either way. | No new candidate. The #894-retro reinforced habit held on its first subsequent application; the mechanical guard is the durable backstop. Disposition scan: #895 touched .working + CHANGELOG + README + TODO (no protected-pack, no corpus body); standing RM-ledger scan: no open RM entry's signal. | 
| 2026-07-13 | #894 | None (deep-assessment r3 Phase-3 /fitness 10-persona pass; 20 findings routed FR-200 to FR-219). | High-value semantic result: the 10-persona fresh-reader pass surfaced 2 High + 9 Medium GENUINE gate-blind cross-document contradictions on a corpus that passes all 69 gates (internal-audit no-board-line, vuln-SLA divergence across 3 docs, TLS/crypto/password/MFA value drift, DSR clock gap), all Pass-1 orchestrator-verified at source before routing. The load-bearing dedup (FR-202 as a fresh carrier of closed FR-127, confirmed by git blame to predate the #193 fix) turned a would-be-duplicate into evidence that the earlier corpus-wide TLS reconciliation was carrier-incomplete. #894's own post-merge /validate-pr returned 0 findings across 8 checks. | One self-caught pre-push defect: inserting the #894 detailed-mirror entry via Edit clobbered the immediately-following "## ... PR #893" header (the new_string omitted the header the old_string consumed), orphaning the #893 body. Caught by the pre-push guard (gate 59 mirror-parity + the gate-36 ChangelogMirrorHeaderParityTests on committed HEAD), restored, amended; nothing escaped. | PATTERN (2nd occurrence of the detailed-mirror-header-clobber sub-class this session: #887 clobbered the #886 header the same way, now #894 clobbered #893). Both were the same mechanism (an Edit whose old_string consumes the newest entry's `## ...` header while inserting a newer entry above it, with new_string forgetting to re-append the consumed header) and both were caught by gate 59 + the gate-36 mirror-parity test. This is the structural-bookkeeping-edit class the #891 retro named, now specifically recurring on the CHANGELOG-detailed header. | The mechanical guard is already false-positive-free and catching every occurrence (gate 59 + gate 36 both fired on committed HEAD before push), so no NEW gate is warranted. Reinforced authoring habit (memory): when prepending a new CHANGELOG-detailed entry, anchor the Edit old_string on the intro-paragraph boundary line ONLY (or, if it must consume the next `## ...` header, re-append that exact header verbatim in new_string), then grep both `## ... PR #<prev>` headers present before commit. This sub-class is a NAMED, quotable mechanical-layer signal (2nd occurrence) surfaced to the maintainer. Disposition scan: #894 touched .working + CHANGELOG + README + the fitness report (no protected-pack, no corpus body); standing RM-ledger scan: no open RM entry's signal. |
| 2026-07-13 | #893 | None (deep-assessment r3 Phase-3 /full-qa 6-subagent forensic pass + EDPB 05/2020 v1.1-date fix). | The /full-qa pass caught a genuine citation-precision defect in the session's demonstrated-weakest dimension: the EDPB 05/2020 register row contradicted itself on the v1.1 date (current cell "4 May 2020" vs its superseded cell + the held version-history "13 May 2020"), a #867 apply that copied the source cover date over the version-history date. Apply-time verification against the held version-history table grounded the fix before it landed; register + carrier both version+date co-bumped, taxonomy/scorecard regenerated. All six subagents dispatched (C and F re-dispatched cleanly after API-connection deaths). | #893's own post-merge /validate-pr found 1 in-window warning: the r3 detail marked /full-qa COMPLETE but left /full-qa (and /fitness) in the adjacent PENDING enumeration line, a status-surface lag within one file. Self-caught by the mandatory sweep, fixed one PR later (this PR). | The paired-surface status-lag class (a summary/status surface marked resolved while its paired enumeration still lists the item open) recurs; the CLAUDE.md close-out checklist already names it as a codified class (the #650-#653 cluster). This is a fresh occurrence of that named class, in the deep-assessment r3 detail file's own status bookkeeping. | No new gate (the class is on free prose no gate inspects; the close-out checklist's paired-surface-lag line is the guard, and the per-PR /validate-pr caught this one). Reinforced habit: when marking a Phase/instrument COMPLETE in a multi-phase record, grep the same file's PENDING/remaining enumeration for the item in the SAME edit. Disposition scan: #893 touched .working + CHANGELOG + README + 2 corpus citation surfaces + regen (no protected-pack); standing RM-ledger scan: no open RM entry's signal. |
| 2026-07-13 | #892 | None (deep-assessment r3 Phase-3 /screen-publications no-op + whole-matrix /matrix-fit). | Both Phase-3 semantic passes came back clean (/screen-publications 0 pending; /matrix-fit 0 mismatches across 84 rows via 2 judges), and #892's own /validate-pr independently re-derived the matrix-fit worklist count as exact (64 matrix + 20 per-doc = 84), corroborating the record rather than taking it on trust. | None in-window. (The subsequent /full-qa pass, batched into the next PR, surfaced one genuine warning attributable to the EARLIER #867 EDPB citation cluster, not to #892; see the next PR's records.) | No new pattern. | No new gate. Disposition scan: #892 touched .working + CHANGELOG + README (no protected-pack, no corpus body, no version-bump-driven regen); standing RM-ledger scan: no open RM entry's signal. |
| 2026-07-13 | #891 | None (deep-assessment r3 Phase-3 `/claim-fit` + drift-cluster completion). | Clean substantive result: `/claim-fit` confirmed the one in-session Tier-A value claim (SOR/2018-64 24-month retention) `prescribed` against held s.6(1) verbatim, and correctly classed the AICPA held-edition question as a `source-not-held` verdict already tracked (RB-6(e)) rather than a new item. The drift-cluster completion applied the find-every-carrier lesson from #890's `/validate-pr`: a bare-token `tools/`-wide grep found the 2nd stale carrier the finding did not name, so the cluster is now complete across five surfaces. #891's own post-merge `/validate-pr` was 0-defect. | The improvement-log row-append JOIN ERROR recurred again while authoring the #890 retro row on this branch (a pipe-space-separator where a newline belonged, self-caught by the pipe-count/join-residual check before commit). This is the 3rd occurrence of this exact mechanical class this session (#887 improvement-log join, #888 register mis-order, this). All self-caught, none escaped. | PATTERN (3rd occurrence, the structural-bookkeeping-row-append class): appending a row to a reverse-chronological table via Edit recurrently emits a pipe-space separator instead of a literal newline before the preserved next-row-start, joining two rows on one line. All three were self-caught (pipe-count / join-residual check), so the QA layer holds, but the recurrence is real and concentrated in the mechanical-edit layer this deep in the session. | Standing mitigation (reinforced): when appending a table row via Edit, the `new_string` MUST place a literal newline before the preserved next-row-start, and the post-edit pipe-count/join-residual check is mandatory (it has caught all three). No new gate (the check is a cheap orchestrator-side habit; a `.working/`-table-row-integrity gate would be possible but the self-check already catches it pre-commit). This recurrence is a NAMED, quotable mechanical-layer signal worth surfacing to the maintainer at the checkpoint. Disposition scan: #891 touched .working + CHANGELOG + README + 2 tool docstrings (no protected-pack, no corpus body); standing RM-ledger scan: no open RM entry's signal. |
| 2026-07-13 | #890 | None (deep-assessment r3 Phase-3 `/guardrails` r10 + the drift-cluster fix). | The guardrail review did real structural work: three adversarial lens subagents, 0 genuine overlap findings (the machinery self-delineates its layered defences), and the drift lens caught a genuine single-root-cause cluster (the compact-CHANGELOG reformat left descriptive surfaces stale) that a future maintainer could act on to reintroduce the #388 orphaned-header defect. The clear-mechanical drift fixes were applied in-window per the deep-assessment convention; the machinery-change proposals were correctly ROUTED (not applied autonomously) per the guardrail-review skill. | The drift remediation was INCOMPLETE: I fixed exactly the three surfaces the r10 drift lens NAMED (gate-50 docstring, gate-59 docstring, spec §6 narrative) without grepping ALL sibling surfaces, so two more same-class carriers survived to `main` (gate-29 docstring `lint-version-date-consistency.py:9`; `check-changelog-on-pr.py:11` "lead-paragraph summaries"). #890's `/validate-pr` Subagent A caught the gate-29 one; the orchestrator's follow-up bare-token find-every-carrier grep of `tools/` caught the second. Both fixed in the follow-on PR; a brief escape-to-main of a documentation-only staleness. | RECURRENCE of the codified find-every-carrier / completeness-grep class (the standing bare-token-width + file-type-width discipline in CLAUDE.md): a drift/rename fix keyed to a FINDING'S NAMED surfaces must still grep ALL sibling carriers at bare-token width, because a lens that samples-and-names is not an exhaustive carrier census. The lens named 3; the corpus held 5. Not a new pattern; a recurrence of the standing completeness discipline, applied here one level up (the fix inherited the lens's incomplete enumeration instead of re-widening it). | No new gate (the completeness discipline is codified convention; a "stale legacy-header-form in a tool docstring" gate would be narrow and low-value). Standing reinforcement: when fixing a drift/rename cluster a lens or finding NAMES, treat the named surfaces as a floor and re-run the find-every-carrier grep (bare-token, all file types) before claiming the cluster complete. Applied in this PR (the `tools/`-wide grep found the 2nd carrier). Disposition scan: #890 touched tools + governance corpus doc + generated artefacts + .working + CHANGELOG + README; standing RM-ledger scan: no open RM entry's signal. |
| 2026-07-13 | #889 | None (deep-assessment r3 Phase-4 gate-efficacy: blind-spot CLEAN + mutation 15/0/5/0; batched #888 QA). | Clean PR (post-merge `/validate-pr` 0 findings). Applied the gate-36-fixture-race mitigation deliberately: ran the pre-push guard WITHOUT concurrent repo-tree polling (only scratchpad-log reads), and gate 36 passed first try with no recurrence of the earlier concurrent-suite race. The gate-mutation probe's `DISPOSABLE-COPY-OK` safety marker worked as intended (refused to run until the throwaway clone was explicitly marked), confirming the tool cannot mutate a live checkout. | None notable. The one earlier friction (the disposable-clone `rm -rf` being permission-denied) was avoided by cloning into a fresh non-existent dir (no destructive op), a clean workaround. | No new pattern. The gate-36 concurrent-suite fixture race is a KNOWN behaviour (documented); the mitigation (no concurrent repo-tree access during the guard's gate-36 window) worked, reinforcing that it is an artefact of concurrent tree access, not a gate defect. | None. Clean PR; the mitigation is already the documented practice. Disposition scan: #889 touched .working + CHANGELOG + README (no protected-pack, no corpus body, no version-bump-driven regen); standing RM-ledger scan: no open RM entry's signal. |
| 2026-07-13 | #888 | None (deep-assessment r3 opening: Phases 1-2 + Phase-3 `/validate` of record; batched #887 QA). | Opened the multi-session `/deep-assessment` r3 as a clean re-entrant checkpoint: derived the live instrument inventory from the repo (not remembered), confirmed the mechanical baseline across all three repos, and landed the batched #887 QA promptly rather than letting it ride an indefinitely-later PR. The register + dated detail make the run resumable, and Phase 8 correctly HOLDS for maintainer sign-off (no self-close). | The r3 register table row was inserted BETWEEN r1 and r2 instead of after r2 (the register is chronological-ascending, unlike history.md / improvement-log / validate-pr which are reverse-chronological), so the merged #888 table briefly read r1/r3/r2. Caught post-merge by #888's `/validate-pr` Subagent A (a brief escape-to-main, NOT a pre-push catch), fixed in the follow-on r3 Phase-3/4 PR. Cosmetic, self-labelled rows, no gate or fact affected. | SIGNAL (2nd occurrence, matching the #887 WATCH): a structural edit of a `.working/` bookkeeping file went wrong again, this time a mis-placed insertion driven by not checking the file's ordering convention (the #887 pair was a dropped required element). Two occurrences in consecutive PRs = signal, not yet a 3-PR pattern; the class is "careless structural edit of a bookkeeping file (dropped element or wrong-order insertion)." Unlike #887's gate-caught clobbers, this one was caught POST-merge by the sweep, so it briefly reached `main`. | Reinforce the #887 technique note with the ORDERING axis: before inserting a row into a bookkeeping table/list, confirm the file's ordering convention first (reverse-chronological + new-on-top for `history.md` / `improvement-log.md` / `validate-pr/history.md`; chronological-ascending + new-at-bottom for `deep-assessment/register.md`), and anchor the insert accordingly. Still WATCH, not yet a gate (a register-row-ordering gate would be low-value for a self-labelled, low-churn table); fires as a 3-PR pattern -> propose a convention line or a light ordering check. Disposition scan: #888 touched .working + CHANGELOG + README (no protected-pack, no corpus body); standing RM-ledger scan: no open RM entry's signal. |
| 2026-07-13 | #887 | None (Sweep 101 `/validate` close-out, first PR of the resumed session; routed the AICPA held-edition note to TODO RB-6(e); no TODO item closed). | Applied the #884 lesson cleanly (created the feature branch as the FIRST action after resume, no branch-to-main repeat). Sweep 101 was fully clean: three independent subagents corroborated every asserted-expectation with 0 contradictions, and the one in-window note (AICPA TSP 100 held copy is the March-2020 edition while the register cites the upstream-verified 2022-revised-POF edition) was correctly triaged as a reference-base-fidelity ROUTE (not a hot-fix: the citation is real-world-accurate and register-only) and handed to the coming `/deep-assessment` `/reference-audit`/`/claim-fit` passes rather than rushed. | TWO self-caught pre-merge block-edit clobbers on structured bookkeeping files: (1) gate 59: the Edit inserting the new #887 detailed-mirror entry clobbered the immediately-following `## ... PR #886` header because the `new_string` did not re-append the anchor it replaced, orphaning the #886 body under #887; (2) gate 63: a Python anchored-cut removing the stale `Current-task` paragraph over-deleted the required `**Worker-dispatches:**` field that sat inside the cut span. Both caught by the pre-push guard, fixed, amended; nothing escaped. Also a first-guard-run gate-36 FAIL that was the concurrent-suite `tests/tmp` fixture race (I ran polling greps during the guard), confirmed transient by an isolated re-run (exit 0); and a ~10-minute CI queue delay that looked like a stall but was genuinely queued (resolved by `gh run view`, not a hang). | The two clobbers share one underlying class: a multi-line replace/cut on a STRUCTURED bookkeeping file silently drops a required structural element (a `##` entry header; a required lease field). This is the FIRST occurrence of that class in the last 5 PRs (observation, not a recurrence), so no auto-graduation. The gate-36 concurrent-suite race and the queued-CI-looks-like-a-stall are both already-documented known behaviours, not new signal. | No new gate: gates 59 and 63 ALREADY caught both defects pre-push, so the mechanical layer worked exactly as designed; a checklist line, not a gate, is the fit. Standing orchestrator-side technique note (WATCH, fires as a pattern on a 2nd block-edit-dropped-required-element within 5 PRs): (a) when inserting a new top entry via Edit into an append-on-top file, the `new_string` MUST re-append the anchor header it replaced (or match on a non-structural anchor), so the following entry's header survives; (b) when cutting a span by start/end anchors, first grep the span for any required field and preserve it in the replacement. Disposition scan: #887 touched .working + TODO + CHANGELOG + README (no protected-pack, no corpus body, no version-bump-driven regen); no pending register-row carrier phrase applied; standing RM-ledger scan: the gate-36 race is the documented shared-tree/concurrent-suite family (gates caught it), so no open RM entry's signal and no new RM entry needed. |
| 2026-07-13 | #885 | Scratch-branch cleanup record (§3.58 follow-up) + batched #884 QA. | Applied the #884 lesson immediately: created the feature branch as the FIRST action (checkout -b before any edit), no repeat of the branch-to-main slip. The scratch cleanup was done verified-safe, not bulk: each of 38 deleted branches confirmed to have a merged PR (content on scratch main), 3 correctly LEFT (an unmerged test branch + two pending-research worker branches), a destructive op treated with per-branch verification + a definitive grep check. | The first batch-delete push failed on stale local remote-tracking refs ("remote ref does not exist" for branches already deleted upstream); fixed with `git fetch --prune` before recomputing the live delete set. | Local remote-tracking refs (`refs/remotes/origin/*`) go stale when branches are deleted upstream; a set-difference against them includes phantom branches. | Standing: `git fetch --prune` before any remote-branch-set computation/deletion, so the live remote state, not stale local tracking refs, drives the operation. Disposition scan: #885 touched .working + CHANGELOG + README (no protected-pack, no corpus); the scratch cleanup was a grc_library_scratch git op (no grc_library content). Standing RM-ledger scan: no open RM entry's signal. |
| 2026-07-13 | #884 | Bookkeeping: recorded the read-only deep-assessment parts (maintainer fallback directive) + batched #883 QA + fixed the #883 stale-TODO-sentence finding. | Ran the maintainer's deep-assessment fallback CONSERVATIVELY: only the read-only, low-risk parts unattended (gate-blindspots CLEAN, delivery-status expected), routing the heavier parts (gate-mutation, /claim-fit, /reference-audit FULL, adoptability persona) to a maintainer-invoked fresh-session formal /deep-assessment rather than forcing them deep in a long unattended session, respecting the skill's fresh-context, sign-off-terminated design. | BRANCH-TO-MAIN SLIP: I committed #884 directly on local `main` (skipped `git checkout -b` after the #883 merge+sync before editing). Self-caught fail-loud at push ("src refspec ... does not match any"), repaired by re-homing the commit to the feature branch + `git reset --hard origin/main`; remote `main` was NEVER touched (the push failed before reaching it). | The #866-class error recurred: after merge+sync I flowed straight into editing (the deep-assessment probes, then bookkeeping) without the explicit branch-creation step, because this PR's sequence did not START with `checkout -b` the way the citation PRs did. | HARD sequencing rule: the FIRST action after every merge+sync is `git checkout -b <branch>`, before ANY file edit or read-heavy probe; never edit on `main`. Applied immediately for #885 (branched as step 1). Consider a stop-hook that warns on an uncommitted edit while HEAD==main (queued as a tooling idea, not built). Disposition scan: #884 touched .working + TODO + CHANGELOG + README (no protected-pack, no corpus body); standing RM-ledger scan: the branch-to-main slip is the #866 read-only-git/shared-tree family but a DIFFERENT root (missing checkout-b, not a subagent checkout), so no existing RM entry's signal, logged here as the recurrence. |
| 2026-07-13 | #883 | §3.57 Medium follow-up: ANPD Resolution 4/2023 (sanctions dosimetry) cited in the Brazil annex + a register row; last citation-apply residual of §3.57 cleared (only the deferred matrix TSC-column /matrix-fit mapping remains). | Clean apply; the verifier confirmed the subject, dates, and amends-1/2021 relationship against multiple independent Brazilian sources with no findings. The load-bearing precision (Article 52 percentages are statutory maximums; 4/2023 supplies the base-value calculation methodology) landed clearly. | None; the one verifier observation (register Publication-date at month precision while siblings use full YYYY-MM-DD) was a sibling-consistency nit, applied. | Sibling-consistency nit: within a register cluster, match the neighbours' precision (the ANPD rows use full-precision dates), so a new row should too. | Standing: for a new register row, match the adjacent cluster rows' date precision and cell conventions, not just the column spec's minimum. Applied per the err-toward-quality directive. Disposition scan: #883 touched a corpus annex + register + TODO + .working + README (both taxonomy-tracked; no protected-pack); standing RM-ledger scan: no open RM entry's signal appeared. |
| 2026-07-13 | #882 | §3.57 [V]: EDPB Guidelines 8/2022 (lead supervisory authority) cited in the EU annex + a register row (last of the maintainer's named [V] set). | The substantive-tier verifier did real work: it caught that I had OVER-corrected the delivery candidate's correct "v2.1" down to "v2.0" based on an incomplete WebSearch summary ("final version, Version 2"). A direct Read of the primary-source PDF (cover + version-history table, fetched to disk since WebFetch couldn't parse it) settled it at v2.1 (28 Sept 2023 minor footnote correction); the register row was then mirrored on the sibling 01/2022 minor-correction treatment. | TWO frictions: (1) the version over-correction (my error, caught pre-push); (2) D5 (backlog-rotation) false-fired TWICE on my honest "narrowed rather than rotated to DONE" non-closure narration, because D5's form-6 not-guard only excludes "NOT rotated to", not "rather than rotated to" (nor did the opening "completes ... §3.57 set" help; the actual match was the tail "rotated to DONE"). Reworded to "narrowed rather than closed". | (1) A WebSearch SUMMARY can elide a minor `.1` correction; for a specific version token the authoritative source is the publisher's own version-history page/PDF, and a candidate value must not be "corrected" downward without primary-source evidence at least as strong. (2) D5's closure-form guards are narrow (only literal "NOT rotated to"); a NON-closure narration must avoid the literal closure forms ("rotated to DONE", "clos... TODO §") entirely, not rely on a negation word D5 does not recognize. | (1) For version/edition tokens, read the publisher's version-history (page or PDF), never settle a version from a search-result summary; treat a downgrade of a candidate value with the same skepticism as an upgrade. (2) When a CHANGELOG line narrates that an item is NOT being closed/rotated, phrase it without the literal D5 closure forms (use "narrowed", "kept open", "not closed"), since D5 negation-guards only catch "NOT rotated to". Disposition scan: #882 touched a corpus annex + register + TODO + .working + README (both taxonomy-tracked; no protected-pack); standing RM-ledger scan: no open RM entry's signal appeared. |
| 2026-07-13 | #881 | §3.57 [V]: NIS2 Commission Implementing Regulation (EU) 2024/2690 cited in the NIS2 annex (3 places) + a register row, with a factual locator correction and an entity-scope caveat. | The most error-prone [V] row got the proportionate response: because it both CORRECTED a fact (the framework-table locator "Article 21(5) and 27" -> "21(5) and 23(11)"; Article 27 is the ENISA registry, not the implementing-act basis) AND asserted a scope boundary (binds only ~11 digital entity types, not all NIS2 entities), I escalated to TWO independent verifier lenses (accuracy + scope). Both confirmed the substance and each caught one precision fix, both applied per the perfection directive: "DNS and TLD providers" -> "DNS providers and TLD name registries" (registries not registrars; matches the annex's own term), and the register Standard-ID "EU NIS2" -> "EU NIS 2" (sibling-row consistency). | The two precision nits; both cheap, both applied and propagated across all carriers by a whole-changeset grep. | Two-lens escalation is the right proportionality for a citation change that ALSO corrects an existing error or fences a scope boundary (as opposed to a plain additive citation, which one lens covers): one lens probes the correction's own accuracy, the other probes the caveat's completeness and internal consistency. A single confirm-oriented pass could rubber-stamp both. | Standing: reserve the two-lens escalation for [V] rows that correct-a-fact or assert-a-scope-boundary; plain additive citations stay single-lens. This is the substantive-tier analogue of the high-assurance harness's dual-lens stage, without the full deterministic-apply machinery (unwarranted for a 3-edit prose change). Disposition scan: #881 touched a corpus annex + register + TODO + .working + README (both taxonomy-tracked; no protected-pack); standing RM-ledger scan: no open RM entry's signal appeared. |
| 2026-07-13 | #880 | §3.57 [V]: EDPB Opinion 28/2024 (AI models) cited in privacy policy §4.7. | Exercised the register's OWN load-bearing-[V]-row re-check cadence rather than the citation gates alone: the opinion was already registered, so the work was to re-confirm currency (WebSearch + PDF cover page: adopted 17 Dec 2024, not superseded) before leaning a new citation on it, then refresh only the Last-verified cell, no duplicate row. The verifier raised two non-blocking observations and I applied BOTH per the "we seek perfection" directive (bare-date cell to match adjacent EDPB rows; softened "made against" -> "made by reference to" to avoid over-claiming the opinion's non-binding force). | None blocking; the two applied nits were quality polish, not defects. | (1) Held-and-registered vs new-source: for a source already in the register, the correct change is re-confirm-currency + cite + refresh Last-verified, NOT a new row (keeps the change minimal). (2) Under the perfection directive, apply a verifier's non-blocking quality observations even when the original is defensible-as-written. | Standing: distinguish held-and-registered from new-source citations (one-cell refresh vs new row); and treat verifier quality nits as apply-by-default under the err-toward-quality directive, not optional. Disposition scan: #880 touched a corpus policy + register + TODO + .working + README (both taxonomy-tracked; no protected-pack); standing RM-ledger scan: no open RM entry's signal appeared. |
| 2026-07-13 | #879 | §3.57 [V]: Canada Breach of Security Safeguards Regulations (SOR/2018-64) cited in the Canada annex + a register row. | The substantive-tier verifier did real work: it caught a stale "current to 2026-03-31" consolidation date in my draft note (I had taken it from a WebSearch result snippet; a direct WebFetch of the live Justice Laws page showed "current to 2026-05-26"). The core legal facts (in force 2018-11-01, no amendment, 24-month record of EVERY breach, regulation-not-Act attribution via s.6(1) "For the purposes of subsection 10.3(1)") were all independently confirmed correct. | The stale date had propagated to FOUR carriers (annex note, TODO §3.57 annotation, two CHANGELOG-detailed lines); a whole-changeset bare-token grep caught all four, and I fixed by dropping the drift-prone "current to" claim entirely rather than swapping in the newer (also-self-staling) date. | TWO lessons: (1) a WebSearch RESULT SNIPPET can carry a stale value; for a specific date/version value, confirm against a direct WebFetch of the live source, not the snippet; (2) a site-consolidation "current to" date drifts independently of the instrument and does not belong in a citation verification note (the register row's "verified <date>" already captures the check date). | Standing: (1) for a load-bearing date/version value, WebFetch the primary source rather than trusting a WebSearch snippet; (2) do not embed "current to <date>" consolidation markers in citation notes; state only the durable legal facts (in force, amended/not). The verifier + whole-changeset grep are the backstop that caught this. Disposition scan: #879 touched a corpus annex + register + TODO + .working + README (both taxonomy-tracked; no protected-pack); standing RM-ledger scan: no open RM entry's signal appeared. |
| 2026-07-13 | #878 | §3.57 [V]: Brazil ANPD Resolution CD/ANPD No. 18/2024 (encarregado/DPO) cited in the Brazil annex + a register row. | Currency confirmed by WebSearch (gov.br/ANPD) per the VM directive; the refute-verifier found nothing and confirmed 18/2024 is the encarregado regulation (Vigente). Applied attribution-precision proactively: tied the resolution to LGPD Article 41 (the encarregado provision) specifically, NOT the annex bullet's broader "Articles 41 to 43" header (42-43 cover agent liability), so the citation is not over-scoped, mirroring the annex's established 15/2024 / 19/2024 citation style. | None. | Attribution-precision (the running lesson): match the citation to the exact article/instrument it governs, not the surrounding bullet's broader framing. The verifier explicitly confirmed the narrower Article 41 attribution is more precise, not a defect. | Standing: keep tying each new citation to the narrowest correct legal basis and let the verifier confirm the precision. Disposition scan: #878 touched a corpus annex + register + TODO + .working + README (both taxonomy-tracked, regen done; no protected-pack); standing RM-ledger scan: no open RM entry's signal appeared. |
| 2026-07-13 | #877 | §3.57 [V]: Commission Implementing Decision (EU) 2021/915 (controller-processor SCCs) cited in the Article 28 DPA template + a register row. | Currency confirmed by WebSearch (EUR-Lex) per the VM directive; the refute-verifier found nothing and independently re-confirmed the critical 914-vs-915 non-swap. The accuracy guard (2021/915 is the Article 28 processing SCCs, NOT the Chapter V transfer SCCs, which are 2021/914) was carried PROACTIVELY into BOTH surfaces (doc text + register topic cell), so a future reader cannot conflate them. Applied the #876 preflight lesson: linked the taxonomy/scorecard paths in the CHANGELOG on first write, no preflight catch this time. | None; the #876 path-span lesson prevented a repeat. | Value-attribution-precision applied proactively (the lesson from the EDPB bundles #867/#868): when a cited instrument has a confusable sibling (2021/915 vs 2021/914), name the exact one AND fence off the adjacent-wrong one in the same edit. | Standing: for any citation with a confusable sibling, add the "distinct from X" fence in-line at authoring time; the verifier confirmed this is what prevents the conflation-class defect. Disposition scan: #877 touched a corpus template + register + TODO + .working + README (both taxonomy-tracked, regen done; no protected-pack); standing RM-ledger scan: no open RM entry's signal appeared. |
| 2026-07-13 | #876 | §3.57 [V]: AICPA TSP 100 (Trust Services Criteria) applied to the citation register (the first [V] row under the maintainer's assistant-does-the-WebSearch-currency direction). | The VM directive worked exactly as intended: instead of routing the [V] row to the maintainer, I confirmed the current version myself (WebSearch + WebFetch of the AICPA page + Deloitte DART), applied it, and the refute-briefed verifier could not break it across nine targets and independently re-confirmed the currency. A genuine internal inconsistency (TSC cited corpus-wide, absent from the register) is now closed. | The pre-push preflight caught two unlinked path-shaped code spans (`taxonomy.yml`, `docs/maturity-scorecard.md`) in the detailed-CHANGELOG Verification bullet; de-pathed to markdown links before commit. Minor, caught pre-commit. | Recurring-minor: naming a generated artefact by its path in a detailed-CHANGELOG bullet reintroduces an unlinked path span that preflight / gate 51 reject. | Standing habit: when a CHANGELOG bullet mentions a regenerated artefact by path, wrap it as a markdown link on first write (or avoid the path span). No new gate; preflight already backstops it. Disposition scan: #876 touched a corpus register + TODO + .working + README (register is taxonomy-tracked, regen done; no protected-pack); standing RM-ledger scan: no open RM entry's signal appeared. |
| 2026-07-13 | #875 | §3.58 delivery-pipeline reconciliation CLOSED (rotated to DONE) under the maintainer's age-plus-pure-research disposition rule; #874's owed QA batched. | Executed the maintainer's first-focus item end-to-end (scratch #164 removed the 5 consumed / stale drops, grc_library #875 closed §3.58, spun off §3.61 for the recycled-number tool weakness so it is tracked not dropped), and correctly fixed #874's two /validate-pr findings in-window (the 13-vs-17 count in the very field the finding lived in, and the (EU) 2021/915 naming). | TWO of my OWN new lines tripped the pre-push guard: gate 18 on bare `§3.58` (just-closed) and `§1.2` (not a heading) inside the new §3.61 item, and the working-tree prose-hygiene gate on 3 em-dashes I pasted verbatim from the #874 Subagent-A report into the new /validate-pr record file. Both caught by the guard, reworded, re-confirmed green before push; nothing escaped. | RECURRING: (1) §N-orphan-on-own-closure again (as in #871): closing §3.58 while a new sibling item (§3.61) referenced it recreated the bare-§N intra-doc-ref defect; (2) NEW: pasting a subagent's prose (which uses em-dashes) into a gate-scanned `.working` record reintroduces dashes the working-prose-hygiene gate rejects. | Standing reinforcement: (1) when a new TODO item must mention just-closed or recycled section numbers, write them WITHOUT the `§` (they are historical token values, not live section pointers) so gate 18 does not treat them as intra-doc refs; (2) when transcribing subagent report text into a `.working` record file, de-dash (em/en to comma/colon) as part of the paste, since those files are prose-hygiene-scanned. No new gate; both are pre-commit habits the guard already backstops. Disposition scan: #875 touched TODO + .working + CHANGELOG + README (no protected-pack, no corpus body); standing RM-ledger scan: no open RM entry's signal appeared. |
| 2026-07-13 | #874 | §3.57 reference-breadth close-out (bookkeeping): the `lc` held-and-currency-confirmed rows recorded APPLIED (#866-#869, #873), the remaining rows recorded as `[V]` and routed to the maintainer. | A clean close that correctly split the applied `lc` cluster from the version-sensitive `[V]` rows, resisting the pull to auto-apply the AICPA TSP 100 register fix (a genuine internal inconsistency, but `[V]`, so upstream-currency-gated and not an unattended-safe apply). The disciplined `[V]`-routing kept an attractive-but-unsafe apply out of the overnight window. | No friction; pure bookkeeping over `.working/` + a 2-line TODO annotation + version bumps. | The `[V]` flag is doing real work as an apply-gate: it correctly held back the one substantive-looking remaining candidate (AICPA) on currency grounds, so the close-out was honest about what is done vs routed rather than opportunistically applying. | Standing: continue treating `[V]` as a hard unattended-apply gate (confirm upstream currency this turn, per the VM-WebSearch directive, before any `[V]` row is applied). Disposition scan: #874 touched TODO + .working + CHANGELOG + README (no protected-pack, no corpus body); standing RM-ledger scan: no open RM entry's signal appeared. |
| 2026-07-13 | #873 | §3.57 Medium citation (cite EDPB 07/2022 in transfer-impact-assessment). | The scope-level, self-grounded citation landed CLEAN (verifier: no finding), confirming that self-grounding (reading the held source's subject + version-history directly before authoring) plus a scope-level cite (the guideline for its own titled subject, not a specific attributed value) is the low-mis-attribution-risk shape, in contrast to the earlier value-attribution bundles (#867/#868) that drew MEDIUMs. | No friction. | The citation-attribution risk is a function of the ATTRIBUTION TYPE: value-attributions (a specific clause/doctrine coupled to a document claim) drew the MEDIUMs; scope-level attributions (the guideline cited for its titled subject) are low-risk. | Standing: prefer scope-level citations and self-ground each; the citation verifier remains the backstop. Marks the end of the overnight-SAFE §3.57 work, the `lc` rows are applied (#866-#869, #873) and the remaining rows are all `[V]` (maintainer-routed for upstream currency). Disposition scan: #873 touched a corpus doc + register (no protected-pack); standing RM-ledger scan: no open RM entry's signal appeared. |
| 2026-07-13 | #872 | Backlog reconciliation (§3.60 scope assessment + §3.56(c) deferral, bookkeeping tier). | An honest-assessment PR: rather than force the §3.60 refactor or the stale §3.56 cosmetics under overnight time pressure, both were assessed and recorded so a fresh session tackles them right; the verifier corroborated both assessments as accurate (neither over- nor under-claimed). | No friction. The concurrency artefacts (gates 29/36/50) the /validate-pr saw are the now-well-understood shared-tree in-flight-#873 pattern, correctly read as artefacts, the read-only-git + concurrency-artefact codification from #870/#871 is paying off (the subagent read them correctly, no false alarm). | The value-per-PR is dropping as the high-value overnight scope completes; #872 is bookkeeping and the remaining safe queue is thin (a few Medium citations + assessed-deferred items + maintainer-routed deliveries). This is the natural saturation of the authorized overnight scope, not a degradation (zero escaped defects across the segment). | Continue per the overnight directive (wind down only on material quality risk; overnight mode cannot end unattended) on the highest-value remaining safe item, keeping the working state impeccable for the maintainer's return. Disposition scan: #872 touched TODO + .working (no protected-pack, no corpus); standing RM-ledger scan: no open RM entry's signal appeared. |
| 2026-07-13 | #871 | §3.59 pack half (read-only-git rule into the ai-assistant-workflow-disciplines pack rule, both trees), CLOSES §3.59. | The pack-distribution half completed the collision-fix codification cleanly on content (gate-37 parity byte-identical, pack stayed project-agnostic); §3.59 closed + rotated to DONE, §3.60 spun off. | The verifier caught TWO §N-orphan defects from my OWN §3.59 closure: a gate-18-blocking bare `§3.59` reference in the new §3.60 item, plus a stale CLAUDE.md forward-pointer, both from closing a §-section without proactively running the §N-orphan whole-repo grep FIRST. | RECURRING §N-orphan-on-own-closure: I know the CLAUDE.md §N-orphan-cleanup guard well but did not execute it proactively when rotating §3.59; the verifier and gate 18 caught it. | Standing reinforcement: on any §-section close, run the §N-orphan whole-repo grep (including the PR's own new items and gate-exempt forward-pointers) as a PRE-COMMIT step, not relying on the verifier to catch it. No new gate (gate 18 catches the intra-doc half; the cross-file half is the CLAUDE.md convention-guard). Disposition scan: #871 touched the pack (authorized overnight protected edits) + .claude/ + TODO; standing RM-ledger scan: §N-orphan-on-closure is a known convention-guard, not a new register candidate. |
| 2026-07-13 | #870 | §3.59 project surfaces (codify the read-only-git rule in the /validate-pr + /validate stubs + a CLAUDE.md note). | The #866 collision-fix codification landed clean on the project surfaces; the read-only-git briefing (in-flight since #866) is now durable in the command stubs, so a future session or a fresh orchestrator inherits it without re-remembering. The verifier and the post-merge /validate-pr both confirmed accuracy + a faithful #866 lineage. | No friction. The concurrency-artefact note (added in the same change) pre-emptively documents the gate-36/50 races the /validate-pr subagents kept observing this session, so future sweeps read them correctly. | The read-only-git + concurrency-artefact codification closes the loop on the #866 branch-collision class (subagent shared-tree git) and the shared-fixture-race class, both of which recurred as observations across this session's citation-bundle sweeps. | Project surfaces done #870; pack-distribution half #871 (closes §3.59); the deeper per-process-fixture-tempdir fix spun off as §3.60. No new gate. Disposition scan: #870 touched `.claude/` (authorized overnight protected edits); standing RM-ledger scan: the subagent-shared-tree-checkout candidate is now codified (§3.59 closed), graduating from the watch list. |
| 2026-07-13 | #869 | §3.57 reference-breadth bundle 4 (cite EDPB 9/2022 in breach-response), COMPLETING the held-verifiable EDPB lc cluster. | The last EDPB bundle landed CLEAN on content (verifier: verbatim attributions, distinct controller/processor clocks, no mis-coupling); the two prior-bundle lessons held, the v1.0 date was fresh-read from the held version-history (10 October 2022) and the becoming-aware / Art-33(2) attributions were coupled exactly as the source couples them. Only a LOW rationale-clause imprecision (caught + fixed; the verified date was then added to the register cell). | No content friction; the friction was authoring-precision on the CHANGELOG rationale prose (a clause implied the held history lacked a v1.0 date it actually records). | The EDPB cluster arc: bundle 1 clean; bundles 2-3 each 1 MEDIUM (register over-claim; §6.3 term mis-coupling) plus a description-lag; bundle 4 clean-on-content + 1 LOW prose. ALL caught pre-escape by the citation-accuracy verifier + post-merge /validate-pr; ZERO escaped. The verifier layer is load-bearing for citation-attribution accuracy. | Standing: citation work gets a citation-accuracy-focused refute-briefed verifier (held-source re-read) every bundle; fresh-read each version-history; verify each attribution's semantic-fit coupling against the held text. No new gate (semantic-fit is the /claim-fit + /matrix-fit + verifier domain). Disposition scan: #869 touched a corpus doc + register (no protected-pack); standing RM-ledger scan: no open RM entry's signal appeared. |
| 2026-07-13 | #868 | §3.57 reference-breadth bundle 3 (cite EDPB 01/2022 in data-subject-rights + register row). | The pre-push citation verifier caught a MEDIUM (§6.3 mis-coupled "layered approach" to the Art 15(4) reconciliation); fixed in the body pre-push, and the held-version-history fresh-read caught a catalogue-vs-doc version discrepancy (v2.1 = 30 May 2024, not the 28 March 2023 adoption) before it could ship. | The BODY MEDIUM was fixed pre-push, but the paired detailed-CHANGELOG Changed-bullet DESCRIPTION still listed "and the layered approach" for §6.3, a description-lag the post-merge /validate-pr caught (F1 Low): the fix touched the body surface but not the paired audit-trail description. | RECURRING summary/description-lag (the CLAUDE.md paired-surface-lag guard): when a mid-PR verifier reword changes a body surface, the paired CHANGELOG description must be grep-confirmed / updated in the same commit; here the §6.3 body dropped "layered approach" but the Changed-bullet parenthetical did not. | Fixed in #869 (Changed-bullet parenthetical corrected to "applied by redaction"). Reinforced discipline: on a mid-PR body reword, re-grep the drafted CHANGELOG entry for the dropped term before push. No new gate (the paired-surface-lag guard covers it; the miss is authoring-reflex). Disposition scan: #868 touched a corpus doc + register (no protected-pack); standing RM-ledger scan: no open RM entry's signal appeared. |
| 2026-07-13 | #867 | §3.57 reference-breadth bundle 2 (cite EDPB 05/2020 in consent-management + register row) + TODO §3.59. | The citation-accuracy verifier EARNED ITS KEEP: it caught a MEDIUM superseded-cell over-claim (v1.0 "for public consultation") before push, a pattern-match error from #866's 4/2019 phrasing that the held version-history refuted; fixed pre-push, and the post-merge /validate-pr confirmed the fix + no residual. Core citation accuracy was clean. | The MEDIUM defect: I pattern-matched #866's register superseded-cell phrasing onto 05/2020 without re-verifying that guideline's OWN version-history (05/2020 v1.0 was the adoption, not a consultation draft). A real accuracy error, caught pre-push. | RECURRING-RISK register-phrasing pattern-match: reusing a prior citation's superseded-cell phrasing on a different source without re-reading that source's version-history. On version-bearing citations the held document's version-history is authoritative, and this recurred as a NEAR-miss in #868 (the catalogue's "v2.1 adopted 28 March 2023" conflated v2.0's date, caught by fresh-reading the held history). | Standing discipline reinforced: fresh-derive each citation's version / superseded framing from the held document's own version-history, never pattern-match the previous bundle's phrasing or trust a catalogue summary. No new gate (the citation-accuracy verifier + `/claim-fit` cadence cover it). Disposition scan: #867 touched a corpus doc + register (no protected-pack) + TODO; standing RM-ledger scan: the subagent-shared-tree-checkout candidate (from #866) is now tracked as TODO §3.59. |
| 2026-07-13 | #866 | §3.57 reference-breadth bundle 1 (cite EDPB 4/2019 in privacy-by-design + register row). | The first citation apply landed clean: the EDPB 4/2019 citation was verified verbatim against the held source BEFORE authoring, the source was confirmed held + currency-checked, and both the pre-push citation-accuracy verifier and the read-only post-merge /validate-pr independently confirmed no fabrication. | A **branch-collision defect**: the #865 /validate-pr subagent ran `git checkout` on the SHARED working tree to judge at the merge commit, which switched the orchestrator OFF its feature branch back to main, so the bundle-1 commit landed on LOCAL main instead of the branch. Surfaced fail-loud at PR-create ("No commits between main and the branch"); repaired (moved the commit to the branch, reset local main to origin/main); remote main was never polluted. | RECURRING-RISK concurrency class: a subagent doing `git checkout` / `switch` / `reset` in the shared working tree collides with the orchestrator's concurrent branch work. | Codified in-flight: ALL /validate-pr + verifier subagent briefs now carry the read-only-git rule (no checkout / switch / reset / stash on the shared tree; judge via `git show` / `git diff`); the #866 read-only-briefed subagent confirmed it stayed read-only. Candidate for a CLAUDE.md standing note + the validate-pr / verifier skill briefs (durable codification). No new gate (subagent-brief discipline). Disposition scan: #866 touched a corpus doc + the register (no protected-pack); standing RM-ledger scan: NEW recurring-risk candidate (subagent-shared-tree-checkout), logged for codification. |
| 2026-07-13 | #865 | Two-file-split how-to harmonization (#864 follow-on) + delivery-pipeline reconciliation record. | Closed the paired-surface lag #864's own /validate-pr flagged (the how-to still said "write the lead paragraph only" after #864 flipped the model section); the verifier independently corroborated every recorded delivery-pipeline claim. The overnight run correctly DECLINED to auto-apply the misnumbered / drifted scratch deliveries (Integrity over Speed), routing them to maintainer disposition. | The delivery-pipeline forensics (reading manifests; discovering recycled-number token-mapping unreliability + a consumed delivery) took real investigation; the audit-delivery-status tool's PENDING count was misleading due to recycled TODO numbers. | RECURRING recycled-TODO-number class: numbers freed by closed items (1.2, 3.15, 3.19) get reassigned, and stale artefacts (delivery manifests, old handoff lines) still reference the OLD meaning of the number, so the token-based delivery-status tool silently mis-maps. | Recorded as TODO §3.58 (maintainer-disposition) + a pending-decisions entry; a durable fix would be a manifest read-basis-vs-current-backlog reconciliation (or keying manifests by a stable coded id, not a recyclable §-number). No new gate this PR. Disposition scan: #865 touched protected files (`.claude/` + pack) under overnight authorization; standing RM-ledger scan: no open RM entry's signal appeared. |
| 2026-07-13 | #864 | Authoring-convention flip (change-tracking rule + CLAUDE.md: compact root form documented as go-forward). | Clean protected-file convention edit under a substantive-tier verifier: gate-37 parity held byte-identical, the pack stayed project-agnostic (no leak in the rule body or the new history row), and the "ADOPTED" claim was independently confirmed true. The verifier's two procedural findings (placeholder token; commit-before-history-aware-checks) were both resolved before push. | The verifier's coherence check surfaced a paired-surface lag: line 37 (the model-section) was flipped to compact-go-forward but the line-162 authoring how-to still said "write the lead paragraph only". Judged coherent-not-contradictory (informational), but a real lag I introduced. | The paired-surface-lag class (flip one surface of a convention, its paired how-to lags) recurred here, exactly the guard CLAUDE.md carries; the model-description and the authoring how-to are a pair, and flipping one should scan the other in the same PR. | Fixed in #865 (the how-to harmonized to reference the compact go-forward form). No new gate (the paired-surface-lag guard already covers it; the miss was not scanning the how-to when flipping the model section). Disposition scan: #864 touched protected files (`.claude/` + pack) under the maintainer's overnight authorization; standing RM-ledger scan: no open RM entry's signal appeared. |
| 2026-07-13 | #863 | §1.2 (SP 800-154 "held source" wording accuracy). | The first post-3b protected-cleanup landed clean: a semantic-fit correction (SP 800-154 was never held) reframed as relevant-but-unavailable across 4 carriers, verified not-held in `grc_library_ref` and cleared by a substantive-tier refute-briefed verifier (NO defect, 7 axes) plus a clean post-merge /validate-pr. | The preflight aid caught 10 unlinked path-spans in the detailed-mirror entry (cross-repo `grc_library_ref` paths plus narratively-named skills) before the first commit; de-pathed and re-ran clean. No shipped defect. | Recurring-but-benign: detailed-mirror Verification bullets accrete backticked paths that are not repo-linkable (sibling-repo files) or should not be links (narrative skill names); the preflight aid is the right catch point (stricter than the `.working`-exempt gates). | None (authoring discipline: prefer descriptive names over backticked paths for cross-repo / narrative references in detailed-mirror prose). Disposition scan: #863 touched protected files (`.claude/` + pack) under the maintainer's overnight authorization; standing RM-ledger scan: no open RM entry's signal appeared. |
| 2026-07-13 | #862 | none (CHANGELOG plain-language rework stage 3b batch 7, FINAL) | The plain-language wave COMPLETED cleanly across 7 batches: batch 7 collapsed the last 119 full-body entries (incl. the 34 PR-less early releases and the initial CC BY-SA 4.0 release); the faithfulness verifier returned NO defect with complete coverage, and #862's post-merge /validate-pr independently confirmed the wave-completion invariant (846 one-liners, 0 full-body, collapse exact). | No friction. The final batch had the highest PR-less-entry density (34 keyed on date+version rather than PR number); the apply script's date-plus-version keying handled it swap-free, and the verifier cleared the backfill cluster row by row. | The wave is a template for large mechanical-but-faithfulness-sensitive corpus rewrites: a deterministic keyed apply (no dropped or reordered entries) plus a per-batch refute-briefed faithfulness verifier catching the one class the mechanical apply cannot (summary transposition; 1 HIGH caught in batch 5). | None; wave closed. TODO §3.16 narrowed to its deferred maintainer-gated git-history-collapse residual (not rotated to DONE). Disposition scan: #862 touched no protected files; standing RM-ledger scan: no open RM entry's signal appeared. |
| 2026-07-13 | #861 | none (CHANGELOG plain-language rework stage 3b batch 6) | Clean batch under the transposition and swap-risk guards added after batch 5: the faithfulness verifier explicitly cleared the #169-#175 duplicate-version cluster (the highest swap-risk spot) and found no misrepresentation, and #861's own post-merge /validate-pr confirmed lossless collapse + swap-free cluster. The wave is nearly done (batch 7 is the last). | No friction. The two accepted plain-English notes ("section" in batch 6, "normative" in #159/#160) are the only observations, both real English words explained in-context, neither the banned internal-token form. | The accepted-plain-English-word pattern (section, normative) confirms the token ban should target the internal `§N/TODO section` FORM, not the English words; the drafter and verifier briefs already carry this distinction. No new mechanism. | None; the wave cycle is stable through six batches. Batch 7 (final, incl. the PR-less early entries) then closes the wave, after which the intro's transitional clause is removed. |
| 2026-07-13 | #860 | none (CHANGELOG plain-language rework stage 3b batch 5) | The per-batch faithfulness verifier proved LOAD-BEARING: it caught a HIGH summary transposition (#259/#260, correct PR keys but swapped content+kind) that NO mechanical check could catch, because the deterministic apply keys by PR number and the swapped summaries were each internally well-formed. Caught before apply, so a substantive corpus change never shipped mislabeled "no published document changed". #860's own post-merge /validate-pr then confirmed the fix landed clean. | The transposition itself is the friction: a drafting subagent, summarizing 120 adjacent entries, attached two neighbours' content to each other's records. It surfaced in the duplicate-version backfill neighbourhood where headers look alike. | This is why the verify-BEFORE-apply gate (moved earlier from the #856 retro) and the per-batch faithfulness verifier are non-negotiable for the 3b wave: a summary swap is invisible to the PR-keyed mechanical apply and to the gates, detectable only by a body-vs-summary read. First HIGH of the wave; all prior were LOW. | Reinforced two standing guards for the remaining batches: (1) the drafter brief now carries an explicit transposition-guard (write each summary against the body under its own exact header, re-checked); (2) the faithfulness verifier brief explicitly targets duplicate-version/adjacent-header clusters as the swap-risk spot. Both are in the batch-6+ briefs. |
| 2026-07-13 | #859 | none (CHANGELOG plain-language rework stage 3b batch 4) | The measured-not-inferred lesson from #858 was applied cleanly: the #858 cumulative was corrected to the measured "363 of 842", the #859 detailed entry used batch-count-only phrasing, and #859's own /validate-pr independently reconciled the 484-collapsed count (363 + 120 + 1 born-collapsed), confirming the fix as measured-correct not merely present. Batch 4's faithfulness verifier found no misrepresentation across 65 sampled entries. | One caught trip: the #484 M&A summary used "decision gates" (legitimate domain term but the banned literal "gate" token); reworded to "decision points" before apply. | No new pattern. The batch cycle (draft, verify-before-apply, deterministic apply, per-batch QA) is stable and the count-discipline correction is holding. | No new mechanism; continue the stable cycle. The "gate" literal-token watch is now part of the drafter brief (the subagents self-scan for it). |
| 2026-07-13 | #858 | none (CHANGELOG plain-language rework stage 3b batch 3 + §4.5 spec + overnight setup) | The batch cycle held under the maintainer's overnight-mode switch: batch 3's faithfulness verifier caught 4 LOW (fixed pre-apply), the §4.5 adopter-reference-base-spec broadening landed to the maintainer's approval, and the overnight authorizations + wind-down bar were recorded durably in pending-decisions before the maintainer slept. | Two caught trips: (1) the session-state lease `Active-session` field carried a parenthetical annotation, which gate 63's strict field-line parser rejected (rc 2); fixed to a bare branch value. (2) the #858 detailed-entry running-total "360 of ~838" was batch-arithmetic, not measured; the measured reality is 363 of 842 (caught by #858's /validate-pr, fixed in #859). | Recurrence of the "meta-prose state-claim measured-not-inferred" class: a cumulative count stated from batch arithmetic drifts from the measured reality (born-collapsed recent entries + a stale denominator). It recurs because the running total changes every batch. | Two fixes: (1) the lease `Active-session` field takes a bare branch name only (no annotation), per gate 63's parser. (2) Going forward, changelog-batch detailed entries state the exact BATCH count + range and, if a cumulative is given, MEASURE it at authoring time (grep the collapsed vs total) rather than adding 120 per batch. |
| 2026-07-13 | #857 | none (CHANGELOG plain-language rework, stage 3b batch 2 + intro updates) | The verify-before-apply refinement (from the #856 retro) paid off: the faithfulness verifier ran against the drafted summaries BEFORE the apply and returned NO DEFECT, so batch 2 applied once with no re-apply. The maintainer-flagged intro updates (root + detailed mirror) landed in the same PR. | One caught trip: the collapse orphaned RESUME.md (its only markdown-linked referrer was a collapsed CHANGELOG body); caught by gate 26 in the post-apply audit and fixed by exempting RESUME.md as the convention-reached entry point it is. Also a preflight catch: a bare `RESUME.md` path-span in the detailed entry, wrapped as a link before commit. | The orphan is a genuine consequence-class of the 3b collapse worth noting: collapsing CHANGELOG bodies removes their markdown links, which can orphan a doc whose ONLY inbound link lived in a CHANGELOG body. RESUME.md was the only such case (it is a convention-reached entry point); a corpus document with real inbound links elsewhere is unaffected. | No new mechanism; RESUME.md is now exempt (correct classification). Future batches: watch gate 26 for any further doc whose sole referrer was a collapsed CHANGELOG body (none expected, but the audit catches it). |
| 2026-07-13 | #856 | none (CHANGELOG plain-language rework, stage 3b batch 1) | The research-assistant batch cycle worked end-to-end: a subagent drafted 120 plain-language summaries from the existing bodies, and a skeptical faithfulness verifier body-compared a 36-entry weighted sample and caught the one real defect (a 42001-vs-27001 standard misattribution in the #831 summary) BEFORE the apply, so it was fixed in the source JSON and applied once. The deterministic self-verifying apply script guaranteed no dropped/reordered entry. | Two caught trips, no escape: (1) the first apply produced adjacent (no-blank-line) entries, deviating from the change-tracking compact-format spec's "blank line between entries"; caught on inspection and fixed by re-applying with the spacing corrected. (2) the UTC day rolled over mid-work (2026-07-12 to -13), so the metadata Dates lagged the commit's UTC date; caught by the pre-push guard's D4 co-bump check and corrected. | Process refinement (applied from batch 2 onward): run the faithfulness verifier against the drafted summaries BEFORE the apply, not after, so a misattribution is fixed in the source and the apply happens once (batch 1 needed a re-apply for #831). The UTC-rollover D4 lag is a known recurring class the guard reliably catches. | No new mechanism. The batch cycle (draft to verify-before-apply to deterministic-apply to QA) is the stable pattern for the remaining batches; the apply helper's blank-line spacing is now baked in. |
|---|---|---|---|---|---|---|
|---|---|---|---|---|---|---|
