# Matrix Semantic-Fit Audit History

**Version:** 1.0.9\
**Date:** 2026-07-10\
**License:** CC BY-SA 4.0

Reverse-chronological table of every `/matrix-fit` invocation against this library. New rows on top. Each row summarises one cadenced semantic-fit audit of the compliance matrix (and per-document framework-alignment tables); detail for findings-producing runs lives in a per-run file `YYYY-MM-DD-<scope>.md` linked from the **Detail** column. A zero-mismatch run gets a history row with `Detail` = `none`.

`/matrix-fit` judges the gate-blind "valid code, wrong control" class (a control identifier that exists in its catalogue, so gates 48/49/54/58/61 pass it, but is the wrong control for the row's document). It is the semantic layer on top of the existence gates, not a substitute for them. See the [`matrix-fit` SKILL](../../dev-security/claude-rules/skills/matrix-fit/SKILL.md) for the six-step process and the [`/matrix-fit`](../../.claude/commands/matrix-fit.md) slash command. Cadence: after each FR-167 matrix-expansion batch, once at matrix completion, and ad-hoc.

This file is maintainer working state, exempt from corpus audit gates.

## Run entries

| Date | Scope | Worklist | Judge | Mismatches | Resulting PR | Detail | Summary |
|---|---|---|---|---|---|---|---|
| 2026-07-10 | Source-doc framework tables (deep-assessment r1 R11, the follow-on the r1 pass deferred): the 27 per-document COBIT/CCM/CSF/AICM table rows on the worklist | `audit-matrix-semantic-fit.py --source-docs-only`: 27 rows / 74 code-instances (overlap-0 candidates) | 2 independent judge subagents partitioned by document (11 + 16 docs), each refute-briefed, quoting held source title + `path:line`; 65 instances judged (33 fits, 20 loose-supporting, 12 mismatch) | 12 mismatch, all fixed in-window; PLUS 8 invalid `ISM-xx` codes (not a CCM domain) remapped to real CCM controls (maintainer-directed fold-in) | #769 | [`2026-07-10-source-docs.md`](2026-07-10-source-docs.md) | The r1 source-doc follow-on. 12 valid-but-wrong-control mismatches fixed (QA-testing SEF-06->CCC-02; logging I&S-09/SEF-01->LOG-01/02/03/12; threat-modelling CCC-06/CCC-04->TVM-04 + DSP-02->DSP-08; media/endpoint DSP-07->DCS-05/DSP-02), matrix-corroborated where the r1-judged matrix already carried the correct control. Discovered an invalid `ISM-01..12` family cited as CSA CCM across 2 docs (gate-blind: existence gate skips unrecognized-prefix tokens); 8 instances remapped to real CCM (GRC/DCS/TVM/SEF/LOG) + 2 honest `N/A` where CCM v4.1 has no control. Paired carriers fixed (logging purpose prose, QA-testing register row). Routed to TODO §3.40 (gate hardening) + §3.41 (20 loose-supporting + media matrix reconciliation). 67/67 green before and after. Closes TODO §3.25. |
| 2026-07-03 | Ad-hoc (the routed #588 S-1/N-1 candidates + the #589 N-2 ride-along) | 5 items: the due-diligence BAI05 pair, the compliance-policy MEA01.01 and MEA01.02 rows, the digital-trust MEA01/MEA02 gloss, the quality-cadence retirement pointer | Held COBIT extract (titles and purpose statements quoted per verdict) | 5: 3 RECODE (BAI05 to APO10 Managed Vendors, objective level on a four-practice span; MEA01.01 to EDM01; MEA01.02 to MEA04 Managed Assurance), 2 REWORD (the gloss to the canonical titles; the pointer to the document index register); the STA-01 domain-name-as-title fixed in the same pass | #592 | none | NEW finding routed to TODO section 3.14: five carriers mis-title MEA02 as Managed Assurance (a title error with a latent fit question, judged per carrier before any recode) |
| 2026-06-30 | FR-167(b) OT + AI-register batch: the 7 new rows (6 `operations/ot/*` + `governance/register-ai-security-tooling-landscape.md`) | The batch's 7 rows | 2 independent adversarial verifiers (FN completeness + FP fit), serving as the per-batch `/matrix-fit` per the AICM-column precedent | 6 FP mis-fits fixed + 3 FN adds applied (all in-window) | #494 | none | The dual-verifier harness IS this batch's semantic-fit check. FP judged every code vs source title: fixed row1 `A.7.2`->`A.7.4`, row3 `STA-08`->`STA-01` + `DSP-04`->`DSP-03`, row6 removed `A.5.10`/`A.8.1` (acceptable-use / endpoint-protection mis-fits, leaving `A.5.9`), row7 removed `GV.OC`. FN confirmed the customs-column `N/A` correct for all 7 (OT = plant-process security, not customs/cargo) and added row1 `LOG-03`/`BCR-08`/`RC.RP`, row6 `TVM-03`/`STA-08`, row7 AICM `TVM-13`. All fixes orchestrator-re-verified against the reference module + ISO full-text. 0 residual mismatches. Out-of-window: the general `operations/register-asset-inventory.md` row carries the same `A.5.10`/`A.8.1` flagged here, noted for a future run. |
| 2026-06-30 | **FR-167(b) gap-fill batch** (7 net-new session docs added as matrix rows: LIA, Privacy-by-Design, TIA, breach-notification register, M&A due-diligence, sanctions/export-control, AI Ethics Review Panel) | The 7 new rows are this batch; semantic fit judged per the FR-167 per-batch `/matrix-fit` cadence. Done as a 2-verifier adversarial harness pass (the equivalent-or-stronger substantive control, per the 2026-06-28 AICM-column precedent) rather than the worklist tool, since the high-assurance harness was already running on the batch. | 3 research workers proposed candidate mappings; then 2 independent adversarial verifiers (FN: re-read each doc, challenged every `N/A`; FP: judged every assigned code vs source title, confirmed all 13 ISO clause titles vs the scratch 27001:2022 full-text, attempted upstream confirmation of the sanctions customs columns via WebSearch). | **0 mismatches.** FN found 1 completeness miss (AICM GRC-13 Explainability omitted on the AI panel row, added). FP found 0 hard mismatches + 7 loose-supporting flags; sanctions customs columns confirmed `N/A` (no authoritative source maps sanctions/denied-party screening to CTPAT/AEO/PIP business-partner criteria). | this PR (#491) | none | The per-batch semantic-fit check for the FR-167(b) gap-fill rows. Every cited code judged against its source control title by two independent adversarial verifiers; GRC-13 added (FN completeness fix); loose stretches trimmed (A.5.26+RS.MA on the breach register, ISO §4.2 on sanctions, ISO A.5.36 on the AI panel); defensible supporting codes kept. Existence gates 48/49/54 green on the new rows. OT block + library-meta/glossary docs are deferred (not rowed); FR-167 stays open pending that scope decision. |
| 2026-06-30 | **Closing whole-matrix** (FR-167 closure item (a), the "matrix completion" close-check; full `compliance/matrix-grc-compliance-alignment.md` + the per-document framework-alignment tables) | The formal tool-driven worklist from [`audit-matrix-semantic-fit.py`](../../tools/audit-matrix-semantic-fit.py) (AICM-scoped since #449): **62 compliance-matrix rows + 16 per-document framework-table rows = 78** overlap-0 candidate rows. Mechanical baseline 57/57 at `fef6a58`; reference base confirmed ([`ccm_aicm_reference.py`](../../tools/ccm_aicm_reference.py) CCM v4.1 + AICM v1.1, [`nist_csf_reference.py`](../../tools/nist_csf_reference.py) CSF 2.0, scratch `ref/standards/`). | 5 independent judge subagents (matrix slices A 16 rows, B 14, C 18, D 14; source-docs slice E 16) judging every cited CCM/AICM/CSF code against its source control TITLE, flagging `mismatch` / `loose-supporting` with quoted title + `path:line`. | **1 mismatch** (fixed in-window): [`operations/register-asset-inventory.md`](../../operations/register-asset-inventory.md):125 cited `AIS-01 / HRS-01` against "Cloud asset inventory and management". The compliance matrix proper (all 62 worklisted rows) is **clean, 0 mismatches**. | this PR (#490) | `2026-06-30-closing-whole-matrix.md` | The FR-167 closing semantic-fit audit (closure item (a)). The matrix proper is clean across all 62 worklisted rows; the 16 per-document tables surfaced one confirmed mismatch: `register-asset-inventory.md`'s CCM cell cited `AIS-01` ("Application and Interface Security Policy and Procedures") and `HRS-01` ("Background Screening Policy and Procedures"), both unrelated to an asset inventory. Apply-time-verified against the reference-base titles AND the matrix's own row for the same document (line 196, `DCS-06, DCS-07, UEM-04`), which already carries the correct controls; corrected the per-document cell to `DCS-06` ("Assets Classification") / `DCS-07` ("Assets Cataloguing and Tracking"), co-bumped the register Version+Date, regenerated taxonomy + scorecard. The judges' `loose-supporting` flags (A&A-01 on ESG disclosure, STA-01/GV.SC on Sustainability, MDS-03 on the ADR standard, AIS-05 on the AI-algorithmic-compliance checklist, BCR-01/STA-11 on AI-inference-cost, DSP-07 as a blanket code on media-handling/endpoint rows, I&S-09 on the logging standard, DSP-02 on the LINDDUN privacy-threat row, others) are all defensible supporting mappings, not mismatches; recorded for the maintainer's awareness, not changed (overnight: ambiguous re-mappings are not auto-applied). **FR-167 closure item (b)** (matrix gap-fill, add rows for substantive documents not yet in the matrix) remains; FR-167 stays open. |
| 2026-06-28 | CSA AICM v1.1 column (FR-167 structural addition, `compliance/matrix-grc-compliance-alignment.md`; 68 assigned rows / 261, AICM-only codes) | The `audit-matrix-semantic-fit.py` worklist tool does NOT yet scope the AICM column (its `KNOWN_TITLES` is CCM v4.1 + CSF 2.0; it indexes `cells.index("CSA CCM v4.1")`), so no tool-driven worklist was produced. Instead the semantic-fit judgment was performed by a dedicated TWO-verifier adversarial pass over every AICM cell (the equivalent-or-stronger substantive control). | **Verifier A** (independent, N/A re-read): opened every title-based-`N/A` candidate and caught 9 genuine misses (each with a quoted dedicated AI-control section). **Verifier B** (independent, adversarial): judged every proposed AICM code against its AICM v1.1 control TITLE, returning per-code CONFIRM / TIGHTEN verdicts (3 tightenings: dropped a loose MDS-10, AIS-11, MDS-13 respectively). | **0 mismatches** (every applied AICM code is `is_aicm_only` AND an evidence-grounded fit; 3 over-assignments tightened pre-apply, 9 misses added) | this PR (PR-B, the AICM-column apply) | none | The FR-167 AICM-column expansion's semantic-fit assurance. AICM v1.1 is CSA's AI-focused extension of CCM v4.1; the column carries only the AICM-only AI-specific delta. The fit judgment (the `/matrix-fit` substance, "valid code, wrong control") was done adversarially by two independent verifiers reading each source document against the AICM control titles, then a deterministic re-parse confirmed every rendered cell matches the verified map (68 assigned / 193 N/A, column exactly after CCM in all 11 tables, 0 mismatches), and gate 49 (extended in #447) validates the column mechanically (AICM-only, no CCM-base). **Queued (recorded as the remaining FR-167 closure work, NOT done here):** extend `audit-matrix-semantic-fit.py` + `KNOWN_TITLES` to scope the AICM column, then run the formal tool-driven closing whole-matrix `/matrix-fit` (the "matrix completion" close-check); plus any matrix gap-fill of substantive docs not yet rowed. FR-167 is NOT yet closed by this PR. |
| 2026-06-27 | Privacy domain section (FR-167 batch 11, `compliance/matrix-grc-compliance-alignment.md` lines 254-301, 42 rows = 40 net-new + 2 corrected) | 7 of 42 rows flagged overlap-0 by [`audit-matrix-semantic-fit.py`](../../tools/audit-matrix-semantic-fit.py) `--matrix-only` (lines 262/263/264/265/267/272/274); the 25 jurisdiction annexes were NOT worklisted (their "... Privacy Regulatory Requirements" titles lexically anchor to the DSP/privacy control titles) | 1 semantic-fit judge subagent over the 7 worklisted rows, reading each source doc and judging every cited CCM/ISO/CSF code against its source control TITLE | **0 mismatches (clean)** | next PR (batched) | none | Per-batch `/matrix-fit` cadence firing for FR-167 batch 11 (privacy). Mechanical baseline 54/54 at `9d0507b`/#404; reference base confirmed (in-repo modules + scratch `ref/standards/`). The recall-oriented pre-filter worklisted 7 rows for overlap-0 (the 25 jurisdiction annexes and several root rows anchored lexically and were deprioritized, not certified). Judge verdicts over the 7: **0 mismatches**; every cited code is a genuine fit or defensible loose-supporting against its source title. **One closest-call (advisory, NOT a finding, NOT changed)**: row 272 (`template-joint-controller-arrangement.md`) carries `DSP-13` "Personal Data Sub-processing", but the document is a GDPR Article 26 joint-controller arrangement (joint determination of purposes and means), which it explicitly distinguishes from Article 28 controller-processor sub-processing; the judge rated DSP-13 loose-supporting (the nearest available CCM code for a multi-party personal-data arrangement, since CCM has no dedicated joint-controller control) and noted the row's strong fit `DSP-06` "Data Ownership and Stewardship" is already present. Not routed as a mismatch (no clearly-better CCM code exists; substituting would trade one loose code for another), surfaced for the maintainer's optional eye. Recurring-and-routine: `GV.PO` "Policy" and `PR.DS` "Data Security" are loose-supporting across several register/template rows (broad categories these doc types touch), a pattern not a defect. Single advisory pass; zero-mismatch run, history row only, no detail file. | 12 of 34 rows flagged overlap-0 by [`audit-matrix-semantic-fit.py`](../../tools/audit-matrix-semantic-fit.py) `--matrix-only` (lines 307/309/315/319/326/327/330/334/336/337/338/339) | 1 semantic-fit judge subagent over the 12 worklisted rows, reading each source doc in full and judging every cited code against its source control TITLE | **0 mismatches (clean)** | next PR (session-closing handoff #403, batched) | none | **First real `/matrix-fit` firing** (the per-batch cadence's primary case, FR-167 batch 10 ai). Mechanical baseline 54/54 at `f9220eb`/#402; reference base confirmed (in-repo modules + scratch `ref/standards/`). The recall-oriented pre-filter worklisted 12 rows for overlap-0 (no lexical anchor between doc subject and cited control titles, expected: GRC control titles routinely share no vocabulary with AI doc subjects). Judge verdicts: **4 rows all-fit** (307 checklist, 315 ethical-AI guideline, 319 evaluation procedure, 326 MCP-server register); **8 rows loose-supporting only, no mismatch** (309, 327, 330, 334, 336, 337, 338, 339); **0 rows with any mismatch**. Every cited code has a material or defensible anchor in its source document (judge quoted source titles + doc subject evidence per code). The gate-blind "valid code, wrong control" class did not surface in this batch. One optional refinement noted (not a finding, not changed): row 337 (`template-dataset-datasheet.md`) carries `DSP-13` "Personal Data Sub-processing" with a thin-but-real anchor (supplier-acquired datasets, cross-border transfer, rights cascade); `DSP-04` "Data Classification" would fit the datasheet's heavy classification content (identifiability tier, special-category, children's data) more squarely. Loose-supporting per the skill is noted, not routed; surfaced for the maintainer's optional second look. Also recurring-and-routine: `GV.OC` "Organizational Context" is loose-supporting across 6 documentation/inventory rows (a broad governance category these doc types touch lightly), a pattern not a defect. Single advisory pass; zero-mismatch run, history row only, no detail file. |
