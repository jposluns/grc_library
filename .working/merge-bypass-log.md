# Merge-bypass log

**License:** CC BY-SA 4.0

Every merge that used the maintainer's always-bypass path on a protected branch, one row each.

## Why this file exists

The codex deep assessment of 2026-07-25 (finding **M-04**) reported that `Main Protection` is active
and correctly configured (PR required, one approval, stale-review dismissal, thread resolution, strict
`Lint markdown corpus`, signatures, deletion prevention, non-fast-forward prevention) AND that the same
API response reports `current_user_can_bypass: "always"` for the maintainer identity. Its
recommendation, where the path is retained, was to require a written justification and post-bypass
validation, because **an always-on bypass is invisible when used**.

The maintainer's decision (2026-07-25) was to RETAIN the emergency path and make its use auditable.
This log is that audit trail. An unlogged bypass merge is a discipline failure.

The bypass is currently not optional for the assistant: a plain merge attempt fails with
`REVIEW_REQUIRED`, because the assistant cannot supply the required approval on its own PR. The
project CLAUDE.md previously claimed "the merge attempt resolves it", which was false; that claim is
corrected in the same change that creates this log. **If a protection change ever makes a plain merge
succeed, the plain merge is preferred and this log should stop growing.**

## What each row records

The PR, the CI state at merge time, the mechanism, the justification, and what the PR did. The CI
state matters most: a bypass on a GREEN PR skips only the human-approval requirement, whereas a bypass
on a RED PR would skip the mechanical gates as well, which nothing in this project authorizes.

## Rows (newest first within a date)

| Date (UTC) | PR | CI state at merge | Mechanism | Justification | Change |
| --- | --- | --- | --- | --- | --- |
| 2026-07-29 | #1233 | all three checks green, observed via `gh pr checks 1233` on the merged head `54e24250` (Lint markdown corpus pass 1m49s, Web generator health pass 13s, Cloudflare Pages pass) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored resume /validate close-out under the standing no-self-gatekeeping convention; attended-autonomous green-CI merge authority; pre-push guard green both runners. Row recorded in PR2b-2b (the following change). | resume /validate close-out (Sweep 129 loop-break + 3 clear-mechanical fixes + #1232 bypass row + handoff prune) |
| 2026-07-28 | #1232 | all three checks green, observed via `gh pr view 1232` statusCheckRollup on the merged head `50f7e4b1` (Lint markdown corpus SUCCESS, Web generator health SUCCESS, Cloudflare Pages SUCCESS; merged 2026-07-28T23:16:44Z) | REST `gh api --method PUT .../pulls/1232/merge` (INFERRED, not directly observable: the GraphQL pool was exhausted until 23:25Z per the #1231 env note and #1232 merged 23:16Z, so the prior session used the same REST path it used for #1231) | Plain merge refused under the base-branch approval policy; maintainer-authored session-closing handoff PR under the standing no-self-gatekeeping convention. Row BACK-FILLED at the 2026-07-28c `/resume` from the OBSERVED post-merge `gh pr view 1232` statusCheckRollup, not in anticipation: a session-closing handoff has no following PR of its own session (the same retrospective-row case as #1226 / #1184 / #1168), surfaced by gate 50 once this #1233 close-out demoted #1232 from highest-PR. | session-closing handoff (2026-07-28b attended wind-down; #1226..#1231 session) |
| 2026-07-28 | #1231 | UNKNOWN (derive by hand) | gh api --method PUT repos/.../pulls/1231/merge (REST; GraphQL exhausted) | Plain merge refused under base-branch approval policy; maintainer-directed migration PR2b-2a; attended-autonomous green-CI merge authority; pre-push guard green. Merged via REST endpoint (gh api PUT .../merge) because the GraphQL rate limit was exhausted this session. Row recorded in the closing handoff PR. | Post-merge /validate-pr for #1231 (PR2b-2a). The verifier built REAL pre-move and post-move git fixtures for all 3 re-scoped delta gates: post-move each accepts the public half only (and check-todo-rotation still FAILs when TODO is not rotated); pre-move each is byte-identical to before. audit-cross-repo --self-test 5/5, public walk byte-identical. CHANGELOG corrections + residue note verified. No other on-pr gate breaks post-move. One LOW: #1231's own detailed entry reintroduced the duplicated 'Pre-push dual-family skeptical verify:' lead phrase (the same bug #1231 fixed in #1230); FIXED in the closing handoff. SHIP. |
| 2026-07-28 | #1230 | all checks green (Cloudflare Pages 0, Lint markdown corpus 1m56s, Web generator health (.web/ --check) 14s), via `gh pr checks 1230` | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under base-branch approval policy; maintainer-directed migration PR2b-1; attended-autonomous green-CI merge authority; pre-push guard green. Row recorded in PR2b-2a. | Post-merge dual-family /validate-pr for #1230 (PR2b-1). Both families verified no un-rewired filesystem consumer was missed and the rewiring is correct pre/post-move. Findings: 4 LOW CHANGELOG inaccuracies (5-vs-8 tools, a nested link, a dup phrase, 2-vs-3 tests); 2 tooling bugs (audit-cross-repo private-walk link-resolution; resolve_working_for_write both-dirs split); 3 post-move delta-gate blockers (check-changelog/todo-rotation require .working in the public diff; D7 silently untriggers). ALL fixed or accepted-with-residue in PR2b-2a (this PR). NOTE: the pre-push codex findings were under-consumed at push (read the .output tail, not the full log); the post-merge re-verify caught them. |
| 2026-07-28 | #1229 | all checks green (Cloudflare Pages 0, Lint markdown corpus 1m46s, Web generator health (.web/ --check) 24s), via `gh pr checks 1229` | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-directed migration PR2a; attended-autonomous green-CI merge authority; pre-push guard green. Row recorded in the following PR. | Post-merge dual-family /validate-pr for #1229 (PR2a). Both families verified all 23 corpus->.working links converted to logical references (zero remaining in gate-3 scope), grammar/versions/citers clean, and the full 78-gate audit passing in an isolated checkout. SHIP; no defect survived. NOTE: the order brief carried the wrong parent SHA (8406fba7 vs true ee4b4b1d); the claude verifier caught it and used the correct parent. |
| 2026-07-28 | #1225 | all three checks green, confirmed via `gh pr checks 1225` on merged head `417e94c4` (Cloudflare Pages, Lint markdown corpus, Web generator health) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-directed migration foundation; attended-autonomous green-CI merge authority; pre-push guard green. Row recorded in the closing handoff PR (#1226). | migration foundation |
| 2026-07-28 | #1224 | all three checks green, confirmed via `gh pr checks 1224` on merged head `13bf0a28` | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-flagged TODO cleanup; attended-autonomous green-CI merge authority; pre-push guard green. Row recorded in #1225. | TODO cleanup part 1 |
| 2026-07-28 | #1223 | all three checks green, confirmed via `gh pr checks 1223` on merged head `1d5933c9` (Cloudflare Pages, Lint markdown corpus, Web generator health) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-directed rule codification; attended-autonomous green-CI merge authority; pre-push guard green both runners. Row recorded in the following PR. | dual-family codification |
| 2026-07-28 | #1222 | all three checks green, confirmed via `gh pr checks 1222` on merged head `6e4a0fcc` (Cloudflare Pages, Lint markdown corpus, Web generator health) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-directed worker-pool hotfix (tooling only); attended-autonomous green-CI merge authority; pre-push guard green both runners. Row recorded in #1223. | exec-dispatch is_orchestrator guard |
| 2026-07-28 | #1220 | all three checks green, confirmed via `gh pr checks 1220` on merged head `4d3f84be` (Cloudflare Pages pass, Lint markdown corpus pass, Web generator health pass) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored deep-assessment routing (working-state only); attended-autonomous green-CI merge authority; pre-push guard green both runners. Row written in the following change (#1221) from the OBSERVED `gh pr checks 1220` output. | route the 33 deep-assessment findings to TODO + #1219 QA close-out |
| 2026-07-28 | #1215 | all three checks green, `gh pr checks 1215 --watch` (rc=0) then confirming `gh pr checks 1215` (Cloudflare Pages SUCCESS, Lint markdown corpus SUCCESS, Web generator health SUCCESS) on merged head `e1e1107b` | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-directed roadmap-B tooling (tools/pr-closeout.py); daytime green-CI merge authority; pre-push guard green both runners; dual-family reviewed pre-merge (claude SHIP-WITH-FIXES caught + fixed the improvement-log #N drift; self-test 47/47). Row written in the following change (#1216) from the OBSERVED `gh pr checks 1215` output. | tools/pr-closeout.py, the PR close-out scaffolder (TODO 3.133) |
| 2026-07-28 | #1216 | all three checks green, `gh pr checks 1216 --watch` (rc=0) then confirming `gh pr checks 1216` (Cloudflare Pages pass, Lint markdown corpus pass, Web generator health pass) on merged head `9fd2dd2d` | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-directed daily CHANGELOG roll-up (D9) + #1215 QA batch; daytime green-CI merge authority; pre-push guard green both runners. Row written in the following change (#1217 handoff) from the OBSERVED `gh pr checks 1216` output. | 2026-07-27 daily CHANGELOG roll-up (D9): 20 per-PR entries collapsed + detailed-mirror swept to private archive |
| 2026-07-28 | #1217 | all three checks green, confirmed via `gh pr checks 1217` on merged head `6d10e464` (Cloudflare Pages pass, Lint markdown corpus pass 1m50s, Web generator health pass) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored session-closing handoff PR (working-state only) under the standing no-self-gatekeeping convention. Row BACK-FILLED at the #1218 resume close-out from the OBSERVED post-merge `gh pr checks 1217`, not in anticipation: the session-closing handoff PR merged itself and no following PR of that session existed to carry the row, the same retrospective-row case as #1212/#1168/#1184, surfaced by gate 50 once #1218 demoted #1217 from highest-PR. | session-closing handoff (2026-07-27b overnight plan) |
| 2026-07-28 | #1218 | all three checks green, `gh pr checks 1218 --watch` (rc=0) then confirming `gh pr checks 1218` (Cloudflare Pages pass, Lint markdown corpus pass 1m49s, Web generator health pass) on merged head `82298762` | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored overnight resume close-out (working-state only); overnight-unattended green-CI merge authority; pre-push guard green both runners. Row written in the following change (#1219) from the OBSERVED `gh pr checks 1218` output. | overnight resume close-out: Sweep 127, handoff prune, lease acquire |
| 2026-07-28 | #1219 | all three checks green, `gh pr checks 1219 --watch` (rc=0) then confirming `gh pr checks 1219` (Cloudflare Pages pass, Lint markdown corpus pass 1m48s, Web generator health pass) on merged head `1e8b9955` | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored component-1 /validate fixes + #1218 QA close-out; attended-autonomous green-CI merge authority; pre-push guard green both runners; the pre-merge skeptical verifier ran two rounds (DO-NOT-SHIP caught A.5.17->A.8.24 + an incomplete D1-D9 sweep; both fixed and UPHELD on re-verify). Row written in the following change (the routing PR) from the OBSERVED `gh pr checks 1219` output. | apply component-1 /validate fixes (ISO 27001:2022 citations, D1-D9) + #1218 dual-family validate-pr close-out |
| 2026-07-27 | #1214 | all three checks green, `gh pr checks 1214 --watch` (rc=0) then confirming `gh pr checks 1214` (Lint markdown corpus SUCCESS, Cloudflare Pages SUCCESS, Web generator health SUCCESS) on merged head `fc821107` | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-directed .claude/ + working-state (no-diff-walls hardening) change; daytime/overnight-unattended green-CI merge authority; pre-push guard green both runners; convention-tier so no standing verifier per the tiered standard. Row written in the following change (#1215) from the OBSERVED `gh pr checks 1214` output. | harden the no-diff-walls rule to cover the Edit/Write tools |
| 2026-07-27 | #1213 | all three checks green, `gh pr checks 1213 --watch` (rc=0) then confirming `gh pr checks 1213` (Lint markdown corpus SUCCESS, Web generator health SUCCESS, Cloudflare Pages SUCCESS) on merged head `f814d602` | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-directed deep-assessment sign-off-removal change; daytime-unattended green-CI merge authority; pre-push guard green both runners; the offloaded adversarial pre-push verifier returned SHIP after a DO-NOT-SHIP round (paired-command miss fixed). Row written in the following change (#1214) from the OBSERVED `gh pr checks 1213` output, since a PR cannot log its own merge. | deep-assessment drops the maintainer-sign-off terminal state |
| 2026-07-27 | #1212 | all three checks green, confirmed via `gh pr checks 1212` on the merged head `56db7fad` (Lint markdown corpus SUCCESS 1m56s, Web generator health SUCCESS 11s, Cloudflare Pages SUCCESS) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored session-closing handoff PR (working-state only) under the standing no-self-gatekeeping convention. Row BACK-FILLED at the #1213 close-out from the OBSERVED post-merge `gh pr checks 1212`, not in anticipation: the session-closing handoff PR merged itself and no following PR of that session existed to carry the row, the same retrospective-row case as #1168 and #1184, surfaced by gate 50 once #1213 demoted #1212 from highest-PR. | session-closing handoff (working-state only) |
| 2026-07-27 | #1211 | all three checks green, verified via `gh pr checks 1211 --watch` (exit 0) then confirming `gh pr checks 1211` (Cloudflare Pages SUCCESS, Lint markdown corpus SUCCESS 1m53s, Web generator health SUCCESS 10s) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored working-state QA-catch-up; attended-autonomous green-CI merge authority; pre-push guard green both runners; the six-PR catch-up subagent plus at-source re-verification were the QA. Row written in the following change (#1212) from the OBSERVED `gh pr checks 1211` output (merge commit `bc5eb00b`), since a PR cannot log its own merge. | QA-catch-up consuming six stranded/pending validate-pr sweeps |
| 2026-07-27 | #1210 | all three checks green, verified via `gh pr checks 1210 --watch` (exit 0) then confirming `gh pr checks 1210` (Lint markdown corpus SUCCESS 1m53s, Web generator health SUCCESS, Cloudflare Pages SUCCESS) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored active-QA-gate change (gate 50 detection logic); attended-autonomous green-CI merge authority; pre-push guard green both runners; pre-push skeptical refute-verifier UPHELD the classifier and its findings dispositioned. Row written in the following change (#1211) from the OBSERVED `gh pr checks 1210` output (merge commit `bd14601e`), since a PR cannot log its own merge. | close TODO 3.120 gate-50 Check 1 third `pending` state |
| 2026-07-27 | #1209 | all three checks green, confirmed via `gh pr checks 1209` (Lint markdown corpus SUCCESS 1m57s, Web generator health SUCCESS, Cloudflare Pages SUCCESS) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored open-findings-guard structural fix (a PR-blocking guard); attended-autonomous green-CI merge authority; pre-push guard green both runners; dual-family adversarial verify of the guard (false-pass and false-block directions both briefed and fixed). Row written in the following change (#1210) from the OBSERVED `gh pr checks 1209` output (merge commit `b252b378`), since a PR cannot log its own merge. | close TODO 3.126 open-findings disposition-token structural fix |
| 2026-07-27 | #1208 | all three checks green, verified via `gh pr checks 1208 --watch` (exit 0) then confirming `gh pr checks 1208` (Lint markdown corpus SUCCESS 1m56s, Web generator health SUCCESS, Cloudflare Pages SUCCESS) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored gate-44 fail-open fix (sensitive active-QA-gate); attended-autonomous green-CI merge authority; pre-push guard green both runners; dual-family adversarial verify + reissued fixed-state re-verify (both over-strips + a shadowed test fixed pre-merge). Row written in the following change (#1209) from the OBSERVED `gh pr checks 1208` output (merge commit `dceb4a1d`), since a PR cannot log its own merge. | close TODO 3.115 four gate-44 fail-open routes + successor 3.148 |
| 2026-07-27 | #1207 | all three checks green, verified via `gh pr checks 1207 --watch` (exit 0) then confirming `gh pr checks 1207` (Lint markdown corpus SUCCESS 1m55s, Web generator health SUCCESS, Cloudflare Pages SUCCESS) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored /sitrep-planning-refinement (bookkeeping) PR; attended-autonomous green-CI merge authority; pre-push guard green both runners. Row written in the following change (#1208) from the OBSERVED `gh pr checks 1207` output (merge commit `453f6d60`), since a PR cannot log its own merge. | the /sitrep planning refinement (TODO 3.142) + #1206 QA rows |
| 2026-07-27 | #1206 | all three checks green, verified via `gh pr checks 1206 --watch` (exit 0) then confirming `gh pr checks 1206` (Lint markdown corpus SUCCESS 1m56s, Web generator health SUCCESS, Cloudflare Pages SUCCESS) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored QA-cadence catch-up (bookkeeping) PR; attended-autonomous green-CI merge authority; pre-push guard green both runners. Row written in the following change (#1207) from the OBSERVED `gh pr checks 1206` output (merge commit `dfe20e89`), since a PR cannot log its own merge. | the QA-cadence catch-up (#1205 QA rows + the two stranded validate-prs 1173/1180) |
| 2026-07-27 | #1205 | all three checks green, verified via `gh pr checks 1205 --watch` (exit 0) then `gh` statusCheckRollup on the merged head `df0e8d82` (Lint markdown corpus SUCCESS 1m40s, Web generator health SUCCESS, Cloudflare Pages SUCCESS) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored impl-3111 verifier-independence PR; attended-autonomous green-CI merge authority; pre-push guard green both runners; dual-family (codex + claude) adversarial verify + clean re-verify. Row written in the following change (#1206) from the OBSERVED `gh` output (merge commit `df0e8d82`, merged 2026-07-27T15:35:24Z), since a PR cannot log its own merge. | exec-dispatch verifier-independence dispatch (TODO 3.111; `--not-worker` / `--not-account` / `--require-worker`, fail-closed) |
| 2026-07-27 | #1204 | all three checks green, verified via `gh` statusCheckRollup on the merged head `2264a93d` (Lint markdown corpus SUCCESS 14:51-14:53, Web generator health SUCCESS, Cloudflare Pages SUCCESS) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored concurrency-decision-recording PR; attended-autonomous green-CI merge authority; pre-push guard green both runners. Row written in the following change (#1205) from the OBSERVED `gh` statusCheckRollup (merge commit `2264a93d`, merged 2026-07-27T14:53:45Z), since a PR cannot log its own merge. | records the 3 concurrency-validation decisions (3.147 design, 3.146 default-ACL correction, fleet fallback config) + batch #1203 QA rows + version bump 2026.07.694 |
| 2026-07-27 | #1203 | all three checks green, verified via `gh pr checks 1203 --watch` (exit 0) then confirming `gh pr checks 1203` before merge (Lint markdown corpus pass 1m57s, Web generator health pass 15s, Cloudflare Pages pass) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored root-CHANGELOG history-loss repair; attended-autonomous green-CI merge authority; pre-push guard green both runners. Row written in the following change (#1204) from the observed `gh pr checks 1203` output (merge commit `e4a9e58a`). | the Week-of-2026-07-13 CHANGELOG history-loss repair (#866-#1055) + TODO 3.147 |
| 2026-07-27 | #1202 | all three checks green, verified via `gh pr checks 1202 --watch` (exit 0) then confirming `gh pr checks 1202` before merge (Lint markdown corpus pass 1m46s, Web generator health pass 14s, Cloudflare Pages pass) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored TODO 3.145 fail-closed-registry fix; attended-autonomous green-CI merge authority; pre-push guard green both runners; dual claude+codex adversarial verify (codex caught+fixed a scalar-list crash). Row written in the following change (#1203) from the observed `gh pr checks 1202` output (merge commit `26dfbcfd`). | the exec-dispatch fail-closed registry (TODO 3.145; gates concurrency>1) |
| 2026-07-27 | #1201 | all three checks green, verified via `gh pr checks 1201 --watch` (exit 0) then confirming `gh pr checks 1201` before merge (Lint markdown corpus pass 1m50s, Web generator health pass 11s, Cloudflare Pages pass) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored morning-resume close-out; attended-autonomous green-CI merge authority; pre-push guard green both runners. Row written in the following change (#1202) from the observed `gh pr checks 1201` output (merge commit `41834d8c`). | the 2026-07-27 morning-resume close-out (Sweep 125 loop-break /validate; drop + alert clearing; codex-framework route; handoff prune) |
| 2026-07-27 | #1200 | all three checks green, verified via `gh pr checks 1200` on the merged head (Lint markdown corpus pass 1m25s, Web generator health pass 13s, Cloudflare Pages pass) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored session-closing handoff PR; overnight-unattended green-CI merge authority; pre-push guard green both runners. Row written in the following change (#1201) from the observed `gh pr checks 1200` output (merge commit `8094b06d`, merged 2026-07-27T03:36:22Z), since a handoff PR cannot log its own merge. | the session-closing handoff at #1199 (handoff refresh, lease release, morning worker pre-queue) |
| 2026-07-27 | #1199 | all three checks green, verified via `gh pr checks 1199 --watch` (exit 0) then confirming `gh pr checks 1199` before merge (Lint markdown corpus 2m1s, Web generator health 11s, Cloudflare Pages) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored close-out; overnight-unattended green-CI merge authority; pre-push guard green both runners. Written after the merge from the observed `gh pr checks 1199` output. | the exec-dispatch per-account concurrency registry + --worker-id (TODO 3.141 code half; cap 1, not enabled) |
| 2026-07-27 | #1198 | all three checks green, verified via `gh pr checks 1198 --watch` (exit 0) then confirming `gh pr checks 1198` before merge (Lint markdown corpus 2m2s, Web generator health 12s, Cloudflare Pages) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored close-out; overnight-unattended green-CI merge authority; pre-push guard green both runners. Written after the merge from the observed `gh pr checks 1198` output. | the weekly CHANGELOG roll-up (Week of 2026-07-20) + the #1197 over-collapse correction + #1197 QA rows |
| 2026-07-27 | #1197 | all three checks green, verified via `gh pr checks 1197` on the head before merge (Lint markdown corpus 1m57s, Web generator health 12s, Cloudflare Pages) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored close-out; overnight-unattended green-CI merge authority; pre-push guard green both runners. Written after the merge from the observed `gh pr checks 1197` output. | the 2026-07-26 daily CHANGELOG roll-up + TODO 3.144 + #1196 QA rows |
| 2026-07-27 | #1196 | all three checks green, verified via `gh pr checks 1196` on the head before merge (Lint markdown corpus 1m56s, Web generator health 11s, Cloudflare Pages) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored bookkeeping bundle; overnight-unattended green-CI merge authority; pre-push guard green on both runners. Written after the merge from the observed `gh pr checks 1196` output. | the origin paragraph + the resume/compaction re-read rule + the /sitrep & worker-onboarding TODO items + the #1195 QA rows |
| 2026-07-27 | #1195 | all three checks green, verified via `gh pr checks 1195` on the head before merge (Lint markdown corpus 1m45s, Web generator health 12s, Cloudflare Pages) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored close-out PR; overnight-unattended green-CI merge authority; pre-push guard green on both runners (incl. the UTC-rollover Date co-bump). Written after the merge from the observed `gh pr checks 1195` output. | the post-wind-down resume close-out (Sweep 124 + #1194 QA) + the 1.26 P1 series + the diff-ban and worker-first codifications |
| 2026-07-26 | #1194 | all three checks green, verified via `gh pr checks 1194 --watch` (exit 0) then a confirming `gh pr checks 1194` on the head `c1468c1c` before merge (Lint markdown corpus 1m44s, Web generator health 15s, Cloudflare Pages) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored close-out PR under the standing no-self-gatekeeping convention; pre-push guard green (both runners). Written after the merge from the observed `gh pr checks 1194` output. | complete the #1193 close-out (spec D7 narrative sync + TODO 3.135 rotation) + the #1193 QA rows |
| 2026-07-26 | #1193 | all three checks green, verified via `gh pr checks 1193` on the head `bb7ff1f9` before merge (Lint markdown corpus 1m56s, Web generator health 15s, Cloudflare Pages) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green (78 gates) + an independent in-session skeptical verifier returned CLEAN (it stripped D4's exemption to prove the load-bearing claim and hunted every reader of the 5 de-versioned files). Written after the merge from the observed `gh pr checks 1193` output. | drop Version+Date from 5 append-only .working logs (TODO 3.135) + the #1192 close-out batch |
| 2026-07-26 | #1192 | all three checks green, verified via `gh pr checks 1192 --watch` on the merged head `f84e8822` (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green. Written after the merge from the observed `gh pr checks 1192` output. | the D9 daily-rollup reminder + weekly-summary restoration + the #1191 close-out |
| 2026-07-26 | #1191 | all three checks green, verified via `gh pr checks 1191 --watch` on the merged head `e09cde89` (Lint markdown corpus 1m57s, Web generator health 21s, Cloudflare Pages) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green. Written after the merge from the observed `gh pr checks 1191` output. | the daily CHANGELOG roll-up + mirror sweep to _private + the #1190 close-out |
| 2026-07-26 | #1190 | all three checks green, verified via `gh pr checks 1190 --watch` on the merged head `c5746f32` (Lint markdown corpus 2m37s, Web generator health 13s, Cloudflare Pages) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green. Written after the merge from the observed `gh pr checks 1190` output. | internal tooling + working-record maintenance + the #1189 close-out batch |
| 2026-07-26 | #1189 | all three checks green, verified via `gh pr checks 1189 --watch` on the merged head `c2a1db90` (Lint markdown corpus 1m43s, Web generator health 12s, Cloudflare Pages) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green + verifier SHIP. Written after the merge from the observed `gh pr checks 1189` output. | the token-spend parser fix (TODO 3.128) + the #1188 close-out batch |
| 2026-07-26 | #1188 | all three checks green, verified via `gh pr checks 1188 --watch` on the merged head `2824db7b` (Lint markdown corpus 1m46s, Web generator health 11s, Cloudflare Pages) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored corrective PR under the standing no-self-gatekeeping convention; pre-push guard green (both runners; D7 handoff-snapshot trap caught + fixed pre-push) + independent verifier SHIP. Written after the merge from the observed `gh pr checks 1188` output, never in anticipation. | the corrective PR: two stale-ledger rows + handoff sweep + TODO 3.140/3.141 + the #1187 close-out batch |
| 2026-07-26 | #1187 | all three checks green, verified via `gh pr checks 1187 --watch` on the merged head `1ac623a9` (Lint markdown corpus 1m46s, Web generator health 9s, Cloudflare Pages) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green (both runners) + independent verifier SHIP. Written after the merge from the observed `gh pr checks 1187` output, never in anticipation. | the exec-dispatch `--account` override + the #1186 close-out batch |
| 2026-07-26 | #1186 | all three checks green, verified via `gh pr checks 1186` on the merged head `dea21c95` (Lint markdown corpus 1m55s, Web generator health 11s, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused under the base-branch approval policy; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green. Written after the merge from the observed `gh pr checks 1186` output, never in anticipation. | the `--account` exec-dispatch fix + the #1186 close-out batch |
| 2026-07-26 | #1185 | all three checks green, verified via `gh pr checks 1185` on the merged head `ce3ed5cd` (Lint markdown corpus 1m52s, Web generator health 13s, Cloudflare Pages) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green on both runners (78 gates + PR-time checks) after it and an independent verifier caught + fixed 3 tool findings and 3 gate failures. Written after the merge from the observed `gh pr checks 1185` output, never in anticipation. | the exec-worker dispatch tool (`exec-dispatch.py`) + the loop-break `/validate` (Sweep 123) |
| 2026-07-26 | #1183 | all checks green (Lint markdown corpus 1m48s, Web generator health 13s, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR, green CI, daytime attended-autonomous merge authority during the orchestrator takeover. Written after the merge from the observed `gh pr checks` output. | the `/restore-broken` recovery command and guardrail review r16 |
| 2026-07-26 | #1182 | all checks green (Lint markdown corpus 1m52s, Web generator health 15s, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR, green CI, daytime attended-autonomous merge authority during the orchestrator takeover. Written after the merge from the observed `gh pr checks` output. | the takeover reconciliation: the lease, #1181's QA and bypass rows, the restored nudge-log, and the recorded sweep fail-open |
| 2026-07-26 | #1181 | all checks green (Lint markdown corpus 1m50s, Web generator health 11s, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR, green CI, daytime attended-autonomous merge authority during the orchestrator takeover. Written after the merge from the observed `gh pr checks` output. | the seven lost QA-history rows restored (none invented) and the commit-time version-bump hook |
| 2026-07-26 | #1180 | all checks green (Lint markdown corpus 2m6s, Web generator health 11s, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR, green CI, overnight-unattended merge authority. Written after the merge from the observed `gh pr checks` output. | the E1 false-SUBMITTED fix, the re-issue and AIQT-cadence directives, and both positives re-derived at source |
| 2026-07-26 | #1179 | all checks green (Lint markdown corpus 1m44s, Web generator health 10s, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR, green CI, overnight-unattended merge authority set by the maintainer this session. Written after the merge from the observed `gh pr checks` output. | the inbox-drop triage, one finding refuted by re-measurement, and TODO 2.20 egress-gated |
| 2026-07-25 | #1178 | all checks green (Lint markdown corpus 2m1s, Web generator health 11s, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR, green CI, attended merge authority. Written after the merge from the observed `gh pr checks` output, never in anticipation. | Sweep 122 close-out, the must-not-close-unvalidated directive, and two guard-input fixes |
| 2026-07-25 | #1177 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored session-closing handoff PR, green CI, attended-autonomous merge authority. Written at merge time from the observed CI state. | the week condensation, wind-down pre-queueing codified, the session-closing handoff |
| 2026-07-25 | #1176 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR, green CI, attended-autonomous merge authority. Written at merge time from the observed CI state. | the deferred-item review, the eleven spec citations, and the #1175 sweep's three findings |
| 2026-07-25 | #1175 | all checks green (Lint markdown corpus 2m14s, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR, green CI, attended-autonomous merge authority. Written immediately after the merge from the observed CI state, which is the practice the five backfilled rows in this same PR exist to restore. | four guard-input fixes, gate 50 Check 6, the token-spend instrument |
| 2026-07-25 | #1174 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR, green CI, attended-autonomous merge authority. **Row BACKFILLED in #1175, not written at merge time: this bypass went unlogged on the day, which is the discipline failure the log exists to prevent.** | the discriminability probe, the open-findings ledger and hook, and TODO 3.116's stall signal |
| 2026-07-25 | #1173 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR, green CI, attended-autonomous merge authority. **Row BACKFILLED in #1175, not written at merge time: this bypass went unlogged on the day, which is the discipline failure the log exists to prevent.** | gate 78, the backlog-item-number permanence gate |
| 2026-07-25 | #1172 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR, green CI, attended-autonomous merge authority. **Row BACKFILLED in #1175, not written at merge time: this bypass went unlogged on the day, which is the discipline failure the log exists to prevent.** | naming the guard-input defect class and actioning the QA tray |
| 2026-07-25 | #1171 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR, green CI, attended-autonomous merge authority. **Row BACKFILLED in #1175, not written at merge time: this bypass went unlogged on the day, which is the discipline failure the log exists to prevent.** | the delivery tray with two completeness layers |
| 2026-07-25 | #1170 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR, green CI, attended-autonomous merge authority. **Row BACKFILLED in #1175, not written at merge time: this bypass went unlogged on the day, which is the discipline failure the log exists to prevent.** | three worker-prompt-tool defects found by using the tool |
| 2026-07-25 | #1165 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green (both runners, 77 gates plus D1-D8, unpiped) | recycled backlog number mis-routing held research |
| 2026-07-25 | #1164 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green (both runners, 77 gates plus D1-D8, unpiped) | CLAUDE.md em-dash cleanup, TODO 1.23 item (4) |
| 2026-07-25 | #1163 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green (both runners, 77 gates plus D1-D8, unpiped) | TODO 3.110 not false-positive-free by construction (measured) |
| 2026-07-25 | #1162 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green (both runners, 77 gates plus D1-D8, unpiped) | session-state consolidation + TODO 3.116 |
| 2026-07-25 | #1161 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green (both runners, 77 gates plus D1-D8, unpiped) | MCP Beta register row + claim-side bare-token discipline |
| 2026-07-25 | #1160 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green (both runners, 77 gates plus D1-D8, unpiped) | the thirteenth gate-44 route (reference-form image alt text) |
| 2026-07-25 | #1159 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green (both runners, 77 gates plus D1-D8, unpiped) | gate-44 false-completeness correction + TODO 3.115 |
| 2026-07-25 | #1158 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green (both runners, 77 gates plus D1-D8, unpiped) | gate 59 cross-file version parity + the twelfth gate-44 route |
| 2026-07-25 | #1157 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green (both runners, 77 gates plus D1-D8, unpiped) | saturation observable reads both exchange planes |
| 2026-07-25 | #1156 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green (both runners, 77 gates plus D1-D8, unpiped) | TODO 1.25 citation precision (fabricated NIST title in two documents) |
| 2026-07-25 | #1155 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green (both runners, 77 gates plus D1-D8, unpiped) | five more gate-44 fail-open routes |
| 2026-07-25 | #1154 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green (both runners, 77 gates plus D1-D8, unpiped) | gate 44 subsection-representation parity + co-landed content fix |
| 2026-07-25 | #1153 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green (both runners, 77 gates plus D1-D8, unpiped) | H-01 EU AI Act Article 18 retention restated as floors |
| 2026-07-25 | #1152 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green (both runners, 77 gates plus D1-D8, unpiped) | AI-jurisdiction coverage surfaces (Sweep 120 F2 family) |
| 2026-07-25 | #1151 | all checks green (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green (both runners, 77 gates plus D1-D8, unpiped) | /home/grc wiring fix (Sweep 120 F1 family) |
| 2026-07-25 | #1166 | all three checks green (Lint markdown corpus, Web generator health, Cloudflare Pages), verified on the actual head SHA | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green both runners, unpiped | act on the unread codex deep assessment (M-04 bypass logging, M-06 dispatcher) |
| 2026-07-25 | #1168 | all three checks green at merge time (Lint markdown corpus, Web generator health, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused with the base-branch policy; maintainer-authored session-closing handoff PR under the standing no-self-gatekeeping convention | session-closing handoff; row added retrospectively at the Sweep 121 close-out, since #1168 was the session's last act and no following PR of that session existed to carry it |
| 2026-07-25 | #1169 | all three checks green, read from `gh pr checks 1169` on the actual head SHA before merging (Lint markdown corpus 1m46s, Web generator health 13s, Cloudflare Pages) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused with "the base branch policy prohibits the merge"; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green on both runners, unpiped, after it caught and blocked two real defects (a stale D7 version snapshot and a 117-word root entry over the 100 ceiling) | Sweep 121 close-out: the retrospective asserted-expectations block and the mutation-proved `manage-workers.py` self-test fix |
| 2026-07-25 | #1167 | all three checks green (Lint markdown corpus, Web generator health, Cloudflare Pages), verified on the actual head SHA | `gh pr merge --admin --squash` | Plain merge refused with `REVIEW_REQUIRED`; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green both runners, unpiped | two worker-fleet tools: drop reconciliation and the closed-verb prompt injector |
| 2026-07-26 | #1184 | all three checks green, verified via `gh pr checks 1184` on the merged head `dd50cf4a` (Lint markdown corpus 1m46s, Web generator health 13s, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused under the base-branch approval policy; maintainer-authored session-closing handoff PR under the standing no-self-gatekeeping convention. Row BACK-FILLED at the #1185 close-out from the OBSERVED post-merge `gh pr checks 1184`, not in anticipation: the prior takeover session merged the handoff and missed logging its bypass, the same retrospective-row case as #1168, surfaced by gate 50 once #1185 demoted #1184 from highest-PR | session-closing handoff for the orchestrator-takeover session (#1181..#1184) |
| 2026-07-28 | #1228 | all three checks green, verified via `gh pr checks 1228` on the merged head `ee4b4b1d` (Lint markdown corpus 1m56s, Web generator health 14s, Cloudflare Pages) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green on both runners, unpiped | resume close-out (Sweep 128 loop-break /validate + #1227 validate-pr fixes + lease acquire) |
| 2026-07-28 | #1227 | all three checks green, verified via `gh pr checks 1227` on the merged head `ed0b59d6` (Lint markdown corpus 1m46s, Web generator health 10s, Cloudflare Pages) | `gh pr merge --admin --squash --delete-branch` | Plain merge refused under the base-branch approval policy; maintainer-authored PR under the standing no-self-gatekeeping convention; pre-push guard green on both runners, unpiped | migration PR1 continuation (rewire the six remaining `.working`-reading gates via resolve_working) |
| 2026-07-28 | #1226 | all three checks green, verified via `gh pr checks 1226` on the merged head `8406fba7` (Lint markdown corpus 1m52s, Web generator health 18s, Cloudflare Pages) | `gh pr merge --admin --squash` | Plain merge refused under the base-branch approval policy; maintainer-authored session-closing handoff PR under the standing no-self-gatekeeping convention. Row BACK-FILLED at the #1227 close-out from the OBSERVED post-merge `gh pr checks 1226`, not in anticipation: a session-closing handoff has no following PR of its own session, the same retrospective-row case as #1168 and #1184, surfaced by gate 50 once #1227 demoted #1226 from highest-PR | session-closing handoff (attended C1 wind-down; #1218..#1225 session) |

## Backfill note, stated plainly

The 15 rows above were entered RETROSPECTIVELY on 2026-07-25, after the codex finding surfaced and the
maintainer decided the policy. They were not logged at merge time, because no log existed and the
assistant had not recognized `--admin` as a governed action; it read the merge failure as a mechanical
obstacle rather than as a control it was circumventing. That is the gap this file closes. Every row's
CI state is recoverable from the PR's own check history, so the backfill is verifiable rather than
asserted, and every one of the 15 was green on all three checks before merge.

**#1166, the change that creates this log, is not yet a row here.** It is expected to need the same
bypass, but a row asserting a completed merge cannot honestly be written before the merge completes, so
its row is added in the following change. That is the go-forward practice: the row is written AFTER the
bypass, from the observed result, never in anticipation of it. A log that pre-records its own entries
would be exactly the kind of artefact this project treats as fabricated rather than evidential.
