next: 1) session-closing handoff (wind down); 2) apply transport (next session); 3) P3 quick-wins; 4) §1.16 COBIT; 5) §1.22.3/4/7
# then: 2026-07-19b resumed session. #1052 = §3.70 crypto tables to P-384/RSA-4096. #1053 (this PR) = §3.69 MFA harmonized to type-anchored across the 3 governing docs + §4.30 adopter-experience umbrella (full assessment + .adopt kit + move DONE/improvement-log to _private with gate resolvers + /adopt recreate) + #1052 QA rows. WIND-DOWN NEXT (maintainer-directed; orchestrator credits exhausted until Wednesday): the session-closing handoff PR = daily-CHANGELOG-condensation (condense today 2026-07-19's entries, since tomorrow's day-boundary trigger won't fire) + #1053 QA rows + handoff refresh + lease RELEASE; skips its own trailing QA (loop-break, compensated by next /resume /validate). DEFERRED to next session: verify+apply the transport-finalize (worker-a DELIVERED it, durable in scratch inbox/results) then maintainer /clear + /credit-offload to switch workers onto /home/grc/grc_working file-drop; the /home/grc repo migration (full design in _private design-decisions); the §4.30 adopter-experience assessment build. Workers = separate Unix users (worker1/worker/codex), still LIVE (separate credit pool). MANDATORY-OFFLOAD RULE ACTIVE: verification is worker-level, offload it. Throughput >=10/day, quality>speed.
