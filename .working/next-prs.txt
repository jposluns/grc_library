next: 1) CCPA §2.23 apply (test US currency); 2) ref-acq packages->ref PR; 3) ETSI §3.14 apply; 4) GR-GAP-1 §3.15 gate; 5) deferred-protected 8/9/6
# then: SESSION IN PROGRESS 2026-07-16 (resumed from #968, on the VM, gh-CLI; maintainer switched to UNATTENDED mode ~18:00Z for the next few hours - "continue with everything you can do as long as quality holds and no degradation; two workers ready"). Merged #969-#974 (credit-offload phase 3 + elevated-QA policy + Sweep 108 close-out + backlog §3.82/§3.83 + design-of-record worker-allocation/ref-read models + §3.85). THIS PR (the egress-request + §2.22 defer PR): created .working/maintainer-egress-requests.md (the standing channel for egress-blocked sources the maintainer downloads to grc_library_ref/ingest/) listing the ~16 Canada.ca instruments for the §2.22 apply, and DEFERRED §2.22 (canada.ca blocks automated re-fetch, DD-10; maintainer will download fresh copies for the expert review) - tracked in pending-decisions + the TODO §2.22 status line. Also batches #974's /retro row + the §3.83 validate-before-deliver nit. NEXT (currency-independent, unattended): CCPA §2.23 apply (first test whether California/CCPA currency is fetchable, unlike canada.ca); the ref-acquisition packages (EDPB/Brazil/LatAm inbox) -> grc_library_ref PR + rsync /tmp; ETSI §3.14 + GR-GAP-1 §3.15 apply-drafts; then deferred-protected items 8/9/6. Delivered-but-unapplied on scratch: ccpa-regs-2026-alignment, etsi-en304223-option3-apply-draft, gr-gap-1-gate-draft, canada-ca-reference-breadth (§2.22, egress-deferred), canada-matrix-fit (§2.22, egress-deferred). Workers: worker-20260716-a stale, worker-20260716-b LIVE (elevated-QA window 2-of-2-to-3 clean after validate-pr-973+974). Keep the fleet fed. Repo-safety: confirm target repo before every write; keep /tmp/grc_library_ref in sync (rsync -av --delete) on any ref update.
