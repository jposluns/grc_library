1) §2.4 website build: a stdlib-Python `.web/` generator (reuse build-taxonomy.py parsing; ALLOW-LIST-driven from taxonomy.yml into a dedicated dist/; every corpus figure recomputed live, the gate suite kept qualitative) + page template from the staged inbox/grclibrary-landing-page/ design + a generator-health CI check + a Cloudflare Pages build-config + a Cloudflare setup runbook; preview-only, NO deploy until maintainer go. 2) Interactive Cloudflare console walkthrough (maintainer drives the dashboard; Pages project, build command/output dir, production branch, watch-paths, custom-domain grclibrary.ai). 3) THEN back to a VM next session for the deferred backlog.
# then: This session is maintainer-scoped to Cloudflare/the public web site ONLY (then back to a VM). CRITICAL build risk = the content-leak boundary: publish ONLY the corpus allow-list, never .working/ .claude/ tools/ tests/ .github, and never the private grc_library_ref / grc_library_scratch. Deferred to next VM session: §3.73 ledger-integrity gate (design-analysis done, build deferred), coverage-refresh sync (~188 PRs behind), protected r3 machinery (G1 hook + handoff-D-check), routed forks (FR-205=A + §3.68/§3.66/§3.63/§3.70/§3.71 + FR-214/215/217/219), the 3 AI-delta annexes A.6/B.1/B.2, §2.1 (FR-59) / §2.3 (FR-70 source-gated), §3.16 git-history collapse.
