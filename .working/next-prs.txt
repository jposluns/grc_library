next: 1) Sweep 108 close-out (sweep row + prune + 2 findings); 2) worker-lifecycle hooks (scratch); 3) deferred-protected 8/9/6; 4) Canada §2.22; 5) P1
# then: SESSION IN PROGRESS 2026-07-16 (resumed from #968, on the VM, gh-CLI, attended-autonomous). Merged #969 (credit-offload phase 3 wiring). The credit-offload machine was EXERCISED and the worker (worker-20260716-a, Opus 4.8) VALIDATED: Sweep 108 /validate + #969 /validate-pr both OFFLOADED and delivered in ~3 min; orchestrator validated delivery 1 under full elevated QA (independent re-derivation, all exact-match + a WORKER-CLEAN-CONFIRMED adversarial auditor) and delivery 2 under graduated elevated QA. Loop-break for #968 PASSES. PR #970 (this): codifies the elevated-QA-window policy (reset per session, QA-kind only, keyed worker+model, floor-not-cap, graduated auditor) in .claude/CLAUDE.md + credit-offload-design.md, and batches #969's offloaded-validated /validate-pr + /retro rows. NEXT: Sweep 108 close-out PR (record the sweep row + advance cursor + prune handoff + apply the 2 worker findings in TODO §3.80: phase-1 date 2026-07-15->2026-07-16, add `verify` to the offloadable list). Then scratch PR B (the 2 worker-lifecycle hooks + serve-loop self-refresh in queue.py/credit-offload.md/queue/README.md + worker-`model` field + capability-routing scaffold [Fable-5, TODO §3.82] + consume the worker-lifecycle-winddown inbox delivery). Then deferred-protected item 8 (D7 handoff-snapshot marker fix), item 9 (See-Also parity gate 70 + count ripple 69->70), item 6 (GR-P pack-design track). Repo-safety: confirm target repo before every write; keep /tmp/grc_library_ref in sync (rsync -av --delete, no -n) on any ref update. green-at 110fde6/#969 = 69/69.
