1) streaming worker output SOP; 2) 3.142 /sitrep tool; 3) 3.134 auto-bump-on-commit
# then: 2026-07-27/28 overnight session (from #1212). SHIPPED so far: #1213 deep-assessment sign-off removal (maintainer directive); #1214 no-diff-walls Edit/Write-tool hardening (maintainer directive); #1215 (this PR) tools/pr-closeout.py the mechanical per-PR close-out scaffolder (TODO 3.133, roadmap B) + 2026-07-27 daily CHANGELOG roll-up + #1214 QA batch. FLEET ROOT-CAUSED + FIXED THIS SESSION (the big win): worker OAuth de-auth was TODO 3.147 (the --worker-id snapshot deleted the refreshed token); write-back fix deployed for BOTH claude + codex; codex stdin-hang fixed (added </dev/null); codex kill utility added; 3.146 default-ACL fixed (applied). All fleet root fixes deployed + verified. STANDING RULES ADOPTED: all QA goes to BOTH a claude AND a codex worker (dual-family); Edit/Write tools forbidden for large existing-file edits (use sed/python); maintainer-run scripts go in /home/grc/maintainer/, moved to done/ after use, run as root (no sudo); one-dispatch-per-account relaxed to sequential-OK now that write-back persists tokens (concurrent same-account still off). NEXT QUEUE (roadmap B/C + the maintainer streaming directive): streaming worker output (add --output-format stream-json to the wrappers + exec-dispatch parsing, so worker progress is tail-able live; needs a root wrapper deploy, I prep the script); 3.142 /sitrep; 3.134 auto-bump-on-commit; 3.136 handoff/snapshot generator; roadmap C CLAUDE.md right-sizing (3.139 series) + activity-scoped rule loading. MORNING ROOT ITEMS (maintainer, paired): streaming wrapper deploy; 3.147 Step-3 concurrent-same-account serialized-refresh (OPTIONAL, only for concurrency>1). Roadmap A-H in _private/roadmap.md.
