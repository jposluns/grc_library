1) 3.124 _ref catalogue D4/D5 (cross-repo); 2) 3.125 codex heartbeat/liveness; 3) 3.150 gate-50 pending recall hardening
# then: 2026-07-27 attended-autonomous. SHIPPED this run: #1205 impl-3111 (exec-dispatch verifier-independence), #1206 QA-cadence catch-up, #1207 /sitrep planning refinement, #1208 TODO 3.115 (four gate-44 fail-open routes), #1209 TODO 3.126 (open-findings disposition-token structural grammar + pipe-robust fail-closed parser), #1210 (this PR) TODO 3.120 (gate-50 Check 1 third `pending` state: an undelivered `DISPATCHED, RESULT PENDING` validate-pr row now FAILS the parity gate once a later PR exists, closing the silence-reads-as-health hole that let validate-pr-1173/1180 sit unconsumed; highest-PR pending stays in-flight-exempt; a RETURNED row that merely mentions "dispatched" is not pending; reality-fixture regression tests + spec section 6 and docstring). 3.118 order-premise CLI-flag widening found NON-VIABLE-FOR-NOW: the base order-premise check (path/TODO existence at pinned SHA) lives in the SCRATCH dispatch tooling, not in grc_library's exec-dispatch, so there is nothing to extend to CLI flags here; a corpus-wide dry-run of the candidate flag-existence check was ~97% false-positive. Prereq: port the order-premise check into grc_library exec-dispatch first, THEN add flag validation (re-scoped, deferred). #1210's pre-push verifier UPHELD the pending-state logic (all 3 intents, boundaries, arm-ordering; rc 0 live ledger, 38 tests) and surfaced 4 latent safe-direction findings: 3 recall gaps ROUTED to new TODO 3.150 (recall brittleness, NOT-RUN subsumption collision, multi-row stale-overwrite), and finding #4 = PRs 1169-1172+1179 carry genuinely-unconsumed validate-pr rows aged below gate-50's window (real QA debt; substance largely covered by Sweep 121 / guard-input fixes / #1181 but formal per-PR QA never returned). NEXT #1211 = QA-CATCH-UP: consume those 5 stranded validate-pr (self-run inline, fleet out, the #1206 pattern) BEFORE new content since QA debt outranks content. THEN #1212 = 3.124 _ref catalogue D4/D5 (re-verify at ref SHA 4a0f2978, cross-repo; the codex refutation at db08742 was a wrong-line artefact). THEN 3.125 codex work-loop heartbeat + derived liveness (today's 50-min stall); 3.127 submit_state below-composer route (validate-pr-1180 F-1); fresh-reader adopter-prose 3 fixes (reconciled, open-findings.md); 3.148 gate-44 residual routes (indented code block; semantic-metadata word-cell); 3.129 _check_body testability refinement; 2.21 TRAIGA content (extraction held); 3.56a residuals; m05 register-reconcile. 3.146 default-ACL + 3.147 token-refresh need root. impl-3131 scratch-side. 3.113 worker-id ownership open. Roadmap A-H in _private/roadmap.md. OFFLOAD LESSON this turn: the fleet was NOT out (all accounts available); an offload attempt used full model IDs (`claude-opus-4-8`, `gpt-5-codex`) against the config's SHORT names (`opus`, `gpt-5.6-terra`), which read as 'no eligible account' and was misread as capped, so QA was wrongly self-run. Correct model names: claude `opus`/`sonnet`/`haiku`/`fable`; codex `gpt-5.6-terra`/`gpt-5.6-sol`/`gpt-5.6-luna`/`gpt-5.5`/`gpt-5.4`/`gpt-5.4-mini`. OFFLOAD going forward. Delivery tray = 47 files = FILING backlog not QA backlog (worker reconciliation classified all: A processed / B candidate-apply-to-open-TODO / C route-to-maintainer; every QA finding consumed or routed). Same-account multiples validated clean at concurrency 3-4 (~2.6x).
