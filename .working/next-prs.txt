WINDING DOWN. #940 taxonomy type-priority order (this PR); #941 landing-nav §2.16 (this session per maintainer); #942 handoff
# then: The maintainer returned attended (2026-07-15) and directed: AICM matrix-fit + corrections (DONE #939), then the domain-page document ordering, then, changed-mind, the landing-nav update also THIS session (so the maintainer reviews the deployed site during wind-down/resume). Merged this session through #939. Current wind-down PR plan: #940 = domain-page document ordering (type-priority, TWO generators): .web/build.py's per-domain sort changed from (type-string,title) to a TYPE_RANK reading-progression (govern->define->do->reference) = the ACTUAL website-page reorder (build.py re-sorts, so the source reorder alone would not have changed the pages), PLUS build-taxonomy.py TYPE_ORDER/_order_key so taxonomy.yml reads in the same order (lossless 312-doc re-sort). TYPE_ORDER replicated in both generators (DRY guard queued TODO §3.76); portal/scorecard sort independently (unchanged). build.py --check + drift gates + suite + 69/69 + one skeptical verifier ZERO findings. #941 = landing-page left-nav rebuild (TODO §2.16, maintainer-refined): nest the 11 domains indented under "By domain" so the nav no longer ends with a flat Domains group, nest the 6 Standards sub-groups under "Standards" (Information security & management systems / AI governance & assurance / NIST cybersecurity / Global data-protection law / EU digital & sector regulation / Security frameworks & threat models), keep Licence, ADD a Contributors link (to /about) at the end, + scrollspy; page-scoped to the landing sidebar (about/domain pages untouched). First verify whether the live site actually lacks Standards/Licence (committed template + dist HAVE them, in a Contents group above the flat Domains group) or it was a cache/overlook. #942 = session-closing handoff (full refresh, RELEASE the lease, exempt from trailing /validate-pr; next /resume corpus-wide /validate is the compensating control). MORNING SESSION: review the deployed site (ordering + nav); then the attended items (§3.47 editorial sweep now scope-confirmed, §3.34 historical-dangler cleanup, §3.38 residual), §3.43 Check-6 gate build (unblocked now the AICM codes are fixed), deferred-protected 6/8/9, routed forks.
