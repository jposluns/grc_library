next: 1) S120 citations 1.25; 2) saturation patch; 3) never-recycle gate; 4) exclude-worker flag; 5) codex env pkg
# then: overnight run 2026-07-25, after #1154 (gate44 co-land). Drafts DELIVERED, awaiting apply: (1) TODO 1.25 citation precision, held-source verified, includes a FABRICATED NIST SP 800-208 title in TWO alignment tables and the OWASP Agentic 2026-vs-Dec-2025 date; (2) the audit-worker-saturation file-drop-awareness patch (the tool still reports NO-WORKERS while workers are live); (3) the TODO 3.110 never-recycle gate draft, which also recommends a disposition for the pre-existing 3.100 collision; (4) TODO 3.111 --exclude-worker flag, which would make verifier independence enforceable (FOUR failures tonight, and it is the mechanism behind open alert 2026-07-24-a); (5) the codex env package, which the orchestrator CANNOT apply (/home/mailz not writable) so it needs the maintainer or a root placement. MAINTAINER QUESTIONS BANKED: (a) require verifier independence and build the flag, or accept orchestrator authorship plus positive re-verification as sufficient (also answers alert 2026-07-24-a); (b) H-01's Union-established-GPAI-provider boundary, disclosed as no-stated-period, or assert 10 years unconditionally; (c) H-01 has NO independent post-merge read (both live workers were conflicted and the sweep correctly returned NOT-RUN), so decide whether a third worker or a maintainer review is wanted; (d) codex DOWN since 04:14Z (minted an id, wrote one heartbeat, never resumed its serve loop). Alerts -b RESOLVED, -c partially fixed, all three left UNCLEARED.
