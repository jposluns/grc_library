next: 1) Sweep 108 worker result consume + close-out; 2) worker-lifecycle hooks (scratch); 3) deferred-protected 8/9/6; 4) Canada §2.22; 5) P1
# then: SESSION IN PROGRESS 2026-07-16 (resumed from #968, on the VM, gh-CLI, attended-autonomous; lease ACQUIRED on claude/credit-offload-phase3). Maintainer started a LIVE credit-offload worker (worker-20260716-a) and directed: OFFLOAD the mandatory Sweep 108 /validate to it as the test (ENQUEUED as blocking prio-0 order sweep-108-validate on scratch; awaiting delivery to results/), apply deferred-protected item 10 (credit-offload phase 3) as designed + develop the worker's worker-lifecycle-winddown design brief, then work the whole remaining deferred-protected backlog (items 6/8/9). PR #969 (this): credit-offload phase 3 wiring applied (/resume step 6 blocking-resume-/validate enqueue/consume + step 3 queue check; new ## Credit-offload mode CLAUDE.md section; design-of-record to phase-3-applied + the 2 worker hooks; TODO §3.80 phase-3 rotated; deferred item 10 rotated out). Per-PR /validate-pr is OFFLOADED to the worker this session (credit conservation); pre-push skeptical verifiers stay inline (§3.81). NEXT: consume the Sweep 108 worker result when delivered -> Sweep 108 close-out PR (sweep row + cursor advance + handoff prune, the loop-break control for #968). Then scratch PR B (the 2 worker-lifecycle hooks + serve-loop self-refresh in queue.py/credit-offload.md/queue/README.md + consume the worker-lifecycle-winddown inbox delivery). Then deferred-protected item 8 (D7 handoff-snapshot marker fix), item 9 (See-Also parity gate 70 + count ripple 69->70), item 6 (GR-P pack-design track). Repo-safety: confirm target repo before every write; keep /tmp/grc_library_ref in sync (rsync -av --delete, no -n) on any ref update. green-at 37abc69/#968 = 69/69.
