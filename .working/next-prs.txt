1) impl-3111/3113 independence dispatch + impl-3131 logging; 2) 3.147 token-refresh (measure+build) & 3.146 ACL fix; 3) P3 tooling 3.133+
# then: 2026-07-27 attended-autonomous. Same-account concurrency VALIDATED (4 workers concurrent on one account, zero corruption). Three decisions ENACTED: 3.147 token-refresh = write-back-under-lock + serialized-pre-batch-refresh for rotation (measure claude's token behaviour first); 3.146 = fix the default ACL source + make check_perms ACL-aware (NOT umask, corrected); fleet config = jposluns-work-claude + jeff-posluns-codex at concurrency 1 (fallbacks), other 4 accounts at max 4 for the testing window. NEXT: consume the Y impl-3111 independence-dispatch design (validated deliverable) + build impl-3111/3113 (exclude/require a worker, keyed on account+family, exclude by drop/TODO-source authorship); then impl-3131 per-worker logging; the 3.147 measure-then-build and the 3.146 ACL fix; ECC reconcile; P3 tooling 3.133/3.134/3.136/3.137. Roadmap A-H in _private/roadmap.md.
