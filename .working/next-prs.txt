1) §1.12 CHANGELOG entry-length remediation + guard strengthening (after the website review); 2) Cloudflare watch-paths §2.6 + publish go (maintainer console); 3) standing backlog + session-closing handoff
# then: §2.4 website effort live on grclibrary.ai and in a maintainer-review loop (PR #921-#928): #928 (this PR) makes the top-nav hero CTAs comprehensive (incl. a /pack link), fixes the pack-page sidebar so all 23 skills list, and reworks the Get-started cards to match the page's card pattern. Deferred by the maintainer to AFTER the website work: TODO §1.12, compress the over-long recent root CHANGELOG entries (#921 onward) + strengthen the length/readability guard. Maintainer-console-only, TODO §2.6: set the Cloudflare build-watch-paths to include the 11 domain directories; then the publish go; §2.4 stays OPEN until publish. Small cleanup: DRY the landing/pack contents-sidebar CSS into the shared partial. Then the standing VM backlog: §3.73 ledger-integrity gate, §3.74 standards-reference standardization, coverage-refresh sync, protected-r3 machinery, routed forks, the 3 AI-delta annexes, §2.1/§2.3 (source-gated), §3.16 git-history collapse.
