next: 1) root .directories cleanup (delete stubs + /adopt + gate-70); 2) credit-offload bundle; 3) apply 5 research seeds; 4) §1.16 COBIT; 5) §1.22.3/4
# then: 2026-07-19b resumed session (/resume from #1043). #1044 = §1.19.12 CLAUDE.md sensitivity-trim (merged). #1045 = self-guard bundle (this PR): repeat-block PreToolUse hook + diagnosis circuit-breaker + evidence-validated degradation-watch log (threshold = a few hours AND >=1 compaction) + read-back/intent-is-not-action CLAUDE.md disciplines; carries the #1044 QA batch (validate-pr-1044 found 3 stale cross-file pointers, fixed). NEXT (maintainer-locked): (1) ROOT .DIRECTORIES CLEANUP (maintainer-directed 2026-07-19, expanded §1.22.2): DELETE the .ref/.private/.scratch stub dirs from grc_library root (they make the maintainer nervous); add an OPTIONAL /adopt step to recreate them; reframe all docs so it is clear the three are SEPARATE REPOS at origin and adopters decide how their clone runs; adjust gate 70 (lint-sibling-placeholders, which guards their shape - remove/repurpose + four-surface + spec + fixture), DEFAULT_EXEMPT_DIRS refs, resolve_sibling placeholder logic, detect-env, the §1.19 LOCKED notes. Part of the public-facing cleanup, "do soon". (2) CREDIT-OFFLOAD BUNDLE: alert 2026-07-19-a idle-heartbeat fix + clear on merge; elevated-QA window ->1; worker session-start-time at registration; orchestrator restart-advice; STALL THRESHOLD = 20 min; MID-PROCESS 5-min heartbeat carrying PROGRESS that resets the stall window; touches scratch /credit-offload + queue tool + _private/credit-offload-design.md. (3) apply the 5 delivered research seeds; (4) §1.16 COBIT guard-first; then §1.22.3/4/5/7, §3.102, §1.1/§1.18. MAINTAINER-GATED/LAST: §1.19.13 history scrub. DISCUSS post-merge: worker read-only FS-access (/home/grc shared-parent + group-read; _private group-read off/toggleable). Workers both live Opus 4.8; worker-a UNVALIDATED (2 misses: #1041 F2, #1044 F3). Throughput: >=10 items/day, quality > speed.
