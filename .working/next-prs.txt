next: 1) §1.19.8 minimization (CLAUDE.md directive + strip pointers); 2) §1.19.9 archive sweep; 3) §1.19.10 tiered CHANGELOG
# then: 2026-07-18 attended-autonomous session (/resume from #1020; VM, gh-CLI). Merged #1021-#1028; #1029 IN FLIGHT = the layered _private-required fail-loud ASSURANCE (detect-env private_availability HALT + a PreToolUse Edit/Write hook + a pre-push-guard line + /resume wiring) AND _private brought under its own validate gate + CI (closes §1.19.11). _private-side (Part 0) pushed: INDEX dispatcher + validate.py + CI + CLAUDE.md (verifier-fixed a dead -ise-stem defect). NEXT = PR 2, the HYBRID _private-pointer minimization (maintainer-approved): ONE CLAUDE.md delegation directive pointing at grc_library_private/INDEX.md (maintainer: read it / clone-if-absent-on-fresh-VM / FAIL LOUD; adopter: skip or redirect to .private or create own) + strip the ~20 inline CLAUDE.md pointers + the 4 rules-overlay pointers + the spec gate-46 line; the 3 command functional reads (resume/retro/high-assurance) get a one-line _private opener (maintainer fail-loud / adopter choice) + stay index-routed; add the read-evidence discipline (a claim "per _private" must quote). Then §1.19.8 CLOSES. Then §1.19.9 (dated-archive sweep to _private + dynamic gate 50/60/D5 cutoffs) -> §1.19.10 (tiered public CHANGELOG, folds §1.12) -> Phase-5 CLAUDE.md trim (§1.19.12 apply) -> §1.19.13 (history scrub, LAST, maintainer-gated). Also queued: task #4 (worker-brief-template -> scratch); the deferred tension-scan/residual-scan resolve_sibling rewiring; the moved-doc cross-repo link cleanup in _private. Repo-safety: sync scratch every PR (§3.93); explicit `cd /home/jposluns/<repo> &&` on cross-repo git. AFTER EVERY MERGED PR: present the per-priority TODO count table.
