next: 1) §3.23/§3.11 convention rails; 2) §3.103 decision-log keywords (cross-repo); 3) deferred cleanup-PRs / Tier-2 sweeps
# then: #1078 added worker-brief guard rail 15 (§3.35: a gate whose config enumerates live repo paths ships a fixture asserting each configured path exists + parse-target matches, the F6/#634 class). Tooling queue COMPLETE; survey Tier-1 quick-clears done: §3.77, §3.89/101, §3.96, §3.35. REMAINING survey Tier-1 convention adds: §3.23 (new-annex discoverability checklist), §3.11 (gate-66 default-population shape, checklist-line), §3.103 (widen decision-log deferral keywords: .claude/ hook + mirrored _private validate, cross-repo, orchestrator-only). DEFERRED dedicated cleanup-PRs: §1.22.3 initial destructive sweep (emit-to-_private + verify + prune); OWASP §36 ASI-column matrix-fit (RB-7 residual). Tier-2 corpus-wide serial (single-session): §3.71 GRC-Manager->GRC Programme Manager rename (~60 carriers), §3.86 CCPA CPI caveat. PROCESS NOTE (adopt + morning-review): pin offloaded orders to the SQUASH-MERGE / main SHA, not the pre-merge PR branch-head SHA (unreachable after squash+branch-delete; worker-a hit this on §3.35, ran against content-equivalent main, logged a LOW MAINTAINER_ALERT). Optional hygiene (not TODO'd): the _origin_url/classify detect-env tests are convention-nonconforming but safe. MORNING-REVIEW: (1) a worker read _private this session (read-only); (2) §2.22 currency may be stale (dischargeable per _ref #87); (3) the LOW SHA-pin watchdog alert. Session so far: 13 PRs (#1066-#1078) once #1078 merges. Workers: both live Opus 4.8. MANDATORY-OFFLOAD active.
