next: 1) §1.19.9 PR B sweep tool + initial sweep; 2) §1.19.10 tiered CHANGELOG; 3) §1.19.12 CLAUDE.md-trim apply
# then: 2026-07-18 attended-autonomous session (/resume from #1020; VM, gh-CLI). Merged #1021-#1031 (#1031 = the §1.19.8 deep-assessment findings-fix: all 7 findings + 6 same-class stragglers fixed, deep-assessment MAINTAINER-SIGNED-OFF, 0 High, §1.19.8 fully closed). #1032 IN FLIGHT = §1.19.9 PR A (guard-first): gate 50's per-register dynamic floor for the dated-archive sweep (verifier CONFIRMED-CORRECT; behaviour unchanged until a sweep runs). Gate-grouping CORRECTED at build (source-verified): only gate 50 needs the floor; gate 60 reads the newest guardrail-review row only (sweep-safety, not a cutoff); D5 reads only the PR diff (no change). #1031's validate-pr (OFFLOADED to worker-b, elevated) + retro batch into #1032. NEXT: §1.19.9 PR B (generalize sweep tool -> _private + add aged-roll-up-row sweep for validate-pr/history + improvement-log + guardrail-reviews newest-row safety + run the initial sweep) -> §1.19.10 (tiered public CHANGELOG, folds §1.12) -> Phase-5 CLAUDE.md trim (§1.19.12 apply, 32 sections 2 MOVE/17 KEEP/13 SPLIT) -> §1.19.13 (history scrub, LAST, maintainer-gated). Also queued: task #4 (worker-brief-template -> scratch); the deferred tension-scan/residual-scan resolve_sibling rewiring; the moved-doc cross-repo link cleanup in _private; TODO/.working Category-D _private pointers (left per the minimization assessment; strip on maintainer request). Repo-safety: sync scratch every PR (§3.93); explicit `cd /home/jposluns/<repo> &&` on cross-repo git. AFTER EVERY MERGED PR: present the per-priority TODO count table.
