next: 1) §1.22.4 cross-repo ref-existence tool; 2) §1.22.7 egress-gated TODO section; 3) §3.102 pack-distribute auto-handoff
# then: #1070 shipped the §1.22.3 .working cycle-out TOOL BUILD (one-off-dir sweep via an explicit allow-list + a read-only --staleness-report + the self-tested one-off verify-before-prune helper; offloaded draft + independent adversarial verify SHIP-WITH-FIXES, the one required fix applied; 7 self-tests). The destructive DONE/pending-decisions entry sweeps are deliberately NOT enabled (await a maintainer DONE-cutoff + effective_floor + the conservative pending-decisions predicate). REMAINING for §1.22.3: (a) the INITIAL destructive sweep as a dedicated cleanup-PR (a large aged backlog exists: ~1 detailed entry + ~46 dated record files + ~383 roll-up rows + 2 one-off dirs; emit-to-_private + verify + prune); (b) enable the DONE/pending destructive sweeps (maintainer-gated); (c) codify the session-boundary cleanup-PR policy. Overnight tooling queue continues: §1.22.4 (cross-repo ref-existence tool, shares 1.22.3's engine) → §1.22.7 (egress-gated TODO section, research delivered) → §3.102 (pack-distribute the degradation-auto-handoff) → §1.16 COBIT gate (guard-first); THEN the P1/P3 quick-clear wave (worker survey delivered). Also open follow-up: the OWASP §36 ASI-column matrix-fit in ai/standard-ai-and-agentic-development-security.md (tracked under the RB-7 residual). Workers: both live Opus 4.8. MANDATORY-OFFLOAD active.
