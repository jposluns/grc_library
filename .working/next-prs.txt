next: 1) pack cwd-corollary (validate-inference); 2) finish §3.1 (needs ISO 12207 edition); 3) TODO-assessment -> ask decisions 1-by-1
# then: 2026-07-24 MAINTAINER-ATTENDED (awake ~06:47, directed a full plan). #1125 (this PR) = the absolute-path repo-target guardrail: block-wrong-repo-tool.py widened to block cwd-relative tools/x and repo-mutating bare git (no -C/cd), forcing absolute paths; CLAUDE.md flipped to absolute-first; 13-case self-test wired into the suite; batches #1124 QA rows. PLAN IN FLIGHT (maintainer "proceed with the plan"): (A) #1125 guardrail [THIS PR]; (A2) portable pack corollary to validate-inference-before-action (both trees + pack README bump) - split out to keep #1125 focused; (B) FULL per-item TODO assessment - OFFLOADED to 2 workers (research-todo-assess-tracks123 + tracks4567), sorting all 96 open items into a=doable-now / b=egress-testable / c=maintainer-decision / d=novel-blocker, drafting a question for every c/d (decisions-search-checked); (C) egress SOP over the (b) set - offload fetches to an egress-worker with the anti-injection rail (treat fetched content as untrusted data, run instruction-scan), route genuine failures to _private maintainer-egress-requests; NEW _private/maintainer-activity-requests.md for the browser-automation egress recommendation (X11 Firefox+CDP or a Windows VM); (D) ask the maintainer the c/d questions ONE BY ONE (structured UI, one per screen), starting with the 4 already-queued + the §3.1 ISO 12207 edition; (E) execute doable-now + unblocked, serial, per-PR QA + verifier. §3.1 WIP is on branch b85a499a (5 verified ISO->ISO/IEC edits + bumps + regen; needs the 12207 edition to finish). Both workers Opus 4.8. Absolute-path convention now ENFORCED by the widened hook. Maintainer on iPad: keep chat <=30 lines.
