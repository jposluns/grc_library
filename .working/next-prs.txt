next: 1) §3.86 CCPA CPI-adjustment caveat (Tier-2 corpus serial); 2) §1.22.3 destructive sweep (cleanup-PR); 3) OWASP §36 ASI-column matrix-fit
# then: #1082 canonicalized the "GRC Manager" role title corpus-wide to "GRC Programme Manager" (§3.71: 60 occurrences across 5 docs, deterministic count-verified rename, skeptical-verifier SHIP). ALL 7 survey Tier-1 quick-clears + the first Tier-2 rename now done. REMAINING survey Tier-2 corpus-wide SERIAL (single-session): §3.86 CCPA CPI-adjustment caveat (grep the $25M / 250,000 / 50,000 CCPA threshold carriers, add a one-line "as periodically adjusted for inflation" caveat; base figures correct, low-risk, expert-review-facing; offloadable-as-draft like §3.71). DEFERRED dedicated cleanup-PRs: §1.22.3 initial destructive sweep (emit-to-_private + verify + prune of the aged backlog: ~46 dated record files + ~383 roll-up rows + 2 one-off dirs; orchestrator-only data op, reversible via archive+git); OWASP §36 ASI-column matrix-fit (RB-7 residual follow-up). MORNING-REVIEW: (1) a worker read _private this session (read-only); (2) §2.22 currency may be stale (dischargeable per _ref #87); (3) a LOW SHA-pin watchdog alert (pin-to-main-tip adopted). Session so far: 17 PRs (#1066-#1082) once #1082 merges, plus a _private direct-push. Workers: both live Opus 4.8. MANDATORY-OFFLOAD active.
