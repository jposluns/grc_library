next: 1) P2 2.17 California AI annex (verified, ready); 2) P2 2.27 OWASP-Agentic build; 3) attended P3 items
# then: 2026-07-23b resumed session, OVERNIGHT-UNATTENDED (maintainer authorized all P3 + the two P2 AI annexes + the OWASP ASI08/09/10 build; content mode = build-with-verifier-and-merge-on-green; do not stop until the maintainer wakes; if the queue drains, re-assess all not-hard-blocked TODO items). Shipped this session: #1106-#1109 (setup/rules/§1.18-close), #1110 (§2.19 Singapore GenAI annex), #1111 (§3.94 sidebar overflow-x:clip robustness half; §3.94 UX root cause stays open for the attended pass). READY: 2.17 California AI annex draft + the delivered upstream+held VERIFICATION fact-sheet (`qa-217-california-verify`) - author next (binding CPPA ADMT, version-sensitive, high-assurance). DELIVERED: 2.27 OWASP-Agentic build draft (`research-227-owasp-agentic-build-draft`). SOURCE-VERIFIED P3-BATCH FINDING (`research-p3-batch-diffs`): of the 4 P3 quick-clears, ALL are non-clean-unattended - 3.38 (gate-39 idioms) is FP-UNSAFE per its own body (the naive `N audits` idiom the tool deliberately excludes; would redden CI), 3.47 (TODO provenance strip) is an ~85-item attended editorial pass, 3.60 (test tempdir) has a real Repository-Path metadata coupling the worker's per-pid diff missed (lint-metadata checks Repository-Path == on-disk path, so per-pid needs dynamic fixture content paths, ~10 sites), 3.13 (mutation variants) is partial (2 variants ready, full set is per-gate work). So P3 quick-clears are NOT clean overnight wins; focus is the P2 content. Both workers live Opus 4.8; MANDATORY-OFFLOAD active; pre-push + high-assurance verifiers stay orchestrator-side.
