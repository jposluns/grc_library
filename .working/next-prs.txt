1) At /resume: run the mandatory loop-break corpus-wide /validate (Sweep 104) over the #915..#917 delta window; cross-check the handoff's Asserted-expectations block (green-at 7596f18/#916 = 69/69). 2) THEN the deferred low-risk + machinery queue (the maintainer's continue-low-risk items, now fresh): §3.73 ledger-table-row-integrity gate BUILT per the design analysis in TODO §3.73 (NOT the naive column-count check, which flags ~50 legit rows; use the mid-row-row-start + per-ledger-sequence-gap detector, or normalize-then-simple); grc_library_scratch coverage-refresh sync (index 185 PRs behind); P3 cleanup. 3) THEN the deferred PROTECTED r3 machinery (daytime/authorized): G1 branch-to-main `.claude/hooks/` edit guard + the handoff-self-row D-check. 4) THEN the routed forks (FR-205 MFA = option A, banked in pending-decisions; the rest stricter-safe-where-defensible): §3.68 vuln-SLA carriers, §3.66 DA-ASVS Class 2, §3.63 RB-ETSI secondary, §3.70 P-256 dev tables, §3.71 corpus-wide GRC-Manager sweep, FR-214/215/217/219. 5) THEN attended backlog: 3 AI-delta annexes A.6/B.1/B.2, §2.4 website + §4.9 packaging, §2.1 (FR-59) / §2.3 (FR-70 source-gated), §3.16 git-history collapse.
# then: SESSION CLOSED at #917 (session-closing handoff, lease RELEASED). Shipped #915 (Sweep 103 /validate close-out + gate-50 §6 Check-5 fix) + #916 (P1 §1.2 CHANGELOG reformat + guard), net 0 adopter escapes. Maintainer chose wind-down on a recurring row-insert precision signal (same operation clobbered an adjacent ledger row 4x, all caught, one escaped to .working on main). INTERIM row-insert technique: anchor the Edit on the HEADER line and append the new row after it, never re-append the next row's prefix. Resync grc_library_ref after each PR. FR-70 source-gated; planalto.gov.br unreachable.
