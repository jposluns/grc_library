next: 1) S120 citations (1.25); 2) H-01 retention; 3) deep-assess+gate44 co-land; 4) saturation patch; 5) scratch prune
# then: overnight run 2026-07-25, after #1152 (TODO 1.24 coverage surfaces). Queue: (1) TODO 1.25 citation precision, draft in flight, includes a FABRICATED NIST SP 800-208 title in two alignment tables and the OWASP Agentic 2026-vs-Dec-2025 date, all held-source verified before apply; (2) H-01 EU AI Act Article 18 retention, maintainer-authorized to MERGE on green, run the FULL high-assurance harness (two adversarial verifiers) as the compensating control for the forgone human review, TWO carriers (model cards AND training-data provenance); (3) the fnaudit's 2 error-severity deep-assessment command findings CO-LANDING with the gate44 subsection-parity widening, which is SHIPPABLE and false-positive-free across all 13 pairs but reddens the corpus by design so it MUST be in the same commit as the content fix; (4) apply the delivered audit-worker-saturation file-drop-awareness patch (the tool still reports NO-WORKERS while workers are live); (5) execute the scratch prune from the two delivered triage passes. Also open: 3 MAINTAINER_ALERTs (fix, leave UNCLEARED, clearing is the maintainer's call); TODO 3.110 never-recycle mechanical check; codex worker DOWN since 04:14 (minted, one heartbeat, never resumed its serve loop, needs a maintainer look); codex env package cannot be applied by the orchestrator (/home/mailz not writable); _private writes batch into one PR since _private is PR-required.
