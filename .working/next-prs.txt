next: 1) maintainer: batch-answer the 3 surfaced decisions (in pending-decisions.md); 2) then unblock OWASP §36 / §3.19 / §3.32; 3) heavy machinery held
# then: #1086 surfaced the decision-blocked cluster into .working/pending-decisions.md (a new 2026-07-23 block) with named options, so decisions-search indexes them and /resume surfaces them for a batch answer: (A) OWASP §36 ASI08/09/10 threat-class treatment [rec: cite-only]; (B) §3.19 worker-provenance link convention [rec: plain backticked text]; (C) §3.32 count-vs-enumeration advisory CLOSE-confirm [rec: close]. Session so far: 21 PRs (#1066-#1086) once #1086 merges, plus 2 _private direct-pushes. Both workers live Opus 4.8; MANDATORY-OFFLOAD active. STATE: the clean quick-clears the maintainer prioritized are DRAINED. Remaining work is all heavier/blocked (full analysis unchanged from the prior next-prs): DECISION-BLOCKED = the 3 above (need maintainer). FRESH-SESSION-FLAGGED (do NOT do deep unattended; each carries the flag in its own TODO block) = §3.73 ledger-row-integrity (BUILD DEFERRED to fresh session; needs an FP-free detector + a risky ~50-row normalization), §3.60 test-fixture-tempdir (14 entangled sites, fresh-session), §3.38 gate-39 idiom (reddens CI if mis-scoped), §3.13-remainder (needs a larger in-table-insertion probe enhancement). ACT-ABLE-BUT-M-EFFORT (held per "prioritize quick clears / conserve credits") = §3.99 parity-gate additive hardening (safe blast radius, offloadable draft), §3.83/§3.88 credit-offload scratch tooling (modifies the LIVE fleet tooling; low value/hygiene). BIG DEFERRED = §1.22.3 destructive .working/ sweep (DONE/pending path maintainer-decision-blocked on cutoff/floor/predicate). If continuing unattended after the decisions land, §3.99 (offloaded draft + verifier) is the cleanest next substantive PR. MORNING-REVIEW: (1) a worker read _private this session (read-only); (2) §2.22 currency may be stale (dischargeable per _ref #87); (3) LOW SHA-pin watchdog alert (pin-to-main-tip adopted); (4) GitHub Actions incident 07:59-09:11Z blocked #1084 CI ~70 min (handled, logged in _private third-party-issues).
