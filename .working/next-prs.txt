next: 1) §1.22.3 initial destructive sweep (dedicated cleanup-PR, orchestrator data op); 2) OWASP §36 ASI-column matrix-fit (RB-7 residual); 3) further TODO drain
# then: #1083 added the §3.86 CCPA $25M CPI-adjustment caveat (single carrier, source-grounded at the held statute, monetary-only scope). ALL 7 survey Tier-1 quick-clears + BOTH Tier-2 corpus-wide serial items (§3.71 rename, §3.86 caveat) are now DONE, and the independent tooling queue is complete. REMAINING deferred dedicated cleanup-PRs: (a) §1.22.3 INITIAL DESTRUCTIVE SWEEP of the aged .working/ backlog (emit-to-_private archive + verify + prune: ~46 dated record files + ~383 roll-up rows + 2 one-off dirs; the tool is data-safe emit-verify-prune; orchestrator-only, reversible via the _private archive + git history; run tools/sweep-working-records-to-private.py --emit-archive <_private>/archive then --prune --verify-archived); (b) OWASP §36 ASI-column matrix-fit (the RB-7 residual follow-up: add the OWASP Agentic ASI01-ASI10 column/mapping to the agentic-security standard's matrix, /matrix-fit-checked). Beyond the survey: continue draining TODO P3 (the survey's Tier-2 doc-edit items §3.19/§3.33/§3.38, and remaining machinery). MORNING-REVIEW: (1) a worker read _private this session (read-only); (2) §2.22 currency may be stale (dischargeable per _ref #87); (3) a LOW SHA-pin watchdog alert (pin-to-main-tip adopted). Session so far: 18 PRs (#1066-#1083) once #1083 merges, plus a _private direct-push. Workers: both live Opus 4.8. MANDATORY-OFFLOAD active.
