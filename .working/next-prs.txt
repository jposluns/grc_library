next: 1) /home/grc migration + file-drop transport; 2) §4.30 adopter assessment; 3) Codex worker; 4) decision-preload protocol; 5) §1.16 COBIT
# then: SESSION CLOSED at #1055 (2026-07-19b/c; re-opened after #1054 for the CHANGELOG condensation + an aidefensematrix.com gap analysis; orchestrator credits exhausted until ~Wednesday). Sweep 115 corpus-wide /validate PASS (0 error/0 warning) over #1044..#1053, run PRE-CLOSE. Lease RELEASED at #1055. RB-7: the 4 not-held AI-security frameworks the matrix references (OWASP AI Exchange, SANS CAISG v1.4, Google SAIF, Cyber Defense Matrix) are on _private/maintainer-egress-requests.md v1.0.3 for maintainer-acquisition; Wednesday ingest whatever is provided + assess corpus use/cite (the matrix ITSELF is not acquired). NEXT SESSION: (1) verify+apply the DELIVERED /home/grc transport-finalize (durable in scratch inbox/results/research-homegrc-transport-finalize.md) = the /home/grc/grc_working/{opus,fable,codex} per-family file-drop serve-loop + orchestrator helper + substructure; (2) MAINTAINER does the sudo repo-move to /home/grc + the grc group + worker#/codex Unix users, orchestrator does the in-repo path-reference edits; (3) maintainer /clear + /credit-offload to switch workers onto the file-drop. Then §4.30 adopter-experience assessment (.adopt kit + move DONE/improvement-log to _private with gate resolvers + /adopt recreate; coordinate §4.1/4.5/4.6/4.9/4.29). Then Codex-worker integration (per-model subtree + routing), the decision-preload protocol codification (AI-only-works-when-watched; drafted in scratchpad), pending-decisions (§3.70 pack-vs-corpus crypto confirm-or-redirect, :333 SEF-06/07 apply), then §1.16 COBIT, §1.22.3/4/7, §3.102, §2.24 content, then the P3 wave. Full design in _private design-decisions "/home/grc multi-user migration + same-VM file-drop worker model". Workers = separate Unix users (worker1/worker/codex), Opus 4.8, live (separate credit pool). MANDATORY-OFFLOAD rule ACTIVE: verification + worker-level work is offloaded; no workers on a VM = alert or get authorization. Throughput today ~15 PRs.
