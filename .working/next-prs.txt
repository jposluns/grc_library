next: 1) deep-assessment of §1.19.8 change-set (worker); 2) §1.19.9 archive sweep; 3) §1.19.10 tiered CHANGELOG
# then: 2026-07-18 attended-autonomous session (/resume from #1020; VM, gh-CLI). Merged #1021-#1029; #1030 IN FLIGHT = the §1.19.8 _private-pointer MINIMIZATION (the approved hybrid): ONE CLAUDE.md delegation directive -> grc_library_private/INDEX.md (maintainer read/clone/fail-loud; adopter skip/redirect/create) + ~20 CLAUDE.md pointers + 4 overlays + spec pointer collapsed to bare concern-names + a one-line _private opener on the 3 commands (they keep functional reads) + the read-evidence discipline. THIS CLOSES §1.19.8. §1.19.8 shipped across #1028 (relocation) + #1029 (layered fail-loud assurance + _private gate/CI, closed §1.19.11) + #1030 (minimization). NEXT (maintainer-directed 2026-07-18): after §1.19.8 closes, OFFLOAD a DEEP-ASSESSMENT to a worker, pinned to #1030's merge SHA, scoped to the WHOLE §1.19.8 change-set (relocation + assurance + minimization + _private Part 0); consume under ELEVATED QA; WORK ANY FINDINGS AS TOP PRIORITY (ahead of §1.19.9/10/etc.); surface to the maintainer (terminates on their sign-off). Then §1.19.9 (dated-archive sweep to _private + dynamic gate 50/60/D5 cutoffs) -> §1.19.10 (tiered public CHANGELOG, folds §1.12) -> Phase-5 CLAUDE.md trim (§1.19.12 apply) -> §1.19.13 (history scrub, LAST, maintainer-gated). Also queued: task #4 (worker-brief-template -> scratch); the deferred tension-scan/residual-scan resolve_sibling rewiring; the moved-doc cross-repo link cleanup in _private; TODO/.working Category-D _private pointers (left per the minimization assessment; strip on maintainer request). Repo-safety: sync scratch every PR (§3.93); explicit `cd /home/jposluns/<repo> &&` on cross-repo git. AFTER EVERY MERGED PR: present the per-priority TODO count table.
