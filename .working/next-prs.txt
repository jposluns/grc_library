1) Interactive Cloudflare console walkthrough (maintainer drives the dashboard; the `.web/` generator + template + generator-health CI check + Cloudflare runbook shipped in #919): create the Pages project connected to `jposluns/grc_library`, set the production branch, build command `python3 .web/build.py`, output dir `.web/dist`, watch-paths (include content dirs + `.web/`; exclude tools/tests/.working/.claude/.github), then the custom domain `grclibrary.ai`; every vendor value confirmed against the live console. 2) Maintainer publish go-decision (the site stays preview-only until then; confirm the §07 bio + CGEIT/CISSP strip and the production branch first). 3) THEN back to a VM next session for the deferred backlog.
# then: The §2.4 website BUILD landed in #919 (the `.web/` generator renders the landing page live from the corpus; rendered output git-ignored; SHIP-verified content boundary, no `.working`/`.claude`/sibling-repo/email leak); §2.4 stays OPEN until the Cloudflare deploy. CRITICAL at deploy: publish ONLY the corpus allow-list, never the private grc_library_ref / grc_library_scratch. Deferred to next VM session: §3.73 ledger-integrity gate (design done, build deferred), coverage-refresh sync (~188 PRs behind), protected r3 machinery (G1 hook + handoff-D-check), routed forks (FR-205=A + §3.68/§3.66/§3.63/§3.70/§3.71 + FR-214/215/217/219), the 3 AI-delta annexes A.6/B.1/B.2, §2.1 (FR-59) / §2.3 (FR-70 source-gated), §3.16 git-history collapse.
