1) per-domain pages + register links (PR2); 2) Cloudflare watch-paths for domain dirs; 3) back to standing backlog
# then: §2.4 website: PR1 (this PR) shipped layout fixes (.lede full width; stat word-cells no longer overflow), all off-site links open in a new tab, Group-4 housekeeping, and #920's batched /validate-pr + /retro. PR2 builds 11 per-domain on-site pages (each domain README + the taxonomy doc list, each document linking to its GitHub blob) + register-table domain links + per-page SEO meta. After PR2, update the Cloudflare Pages build-watch-paths to include the 11 domain dirs. §2.4 stays OPEN until the maintainer confirms publish. Standing backlog then resumes: §3.73 ledger-integrity gate, §3.74 standards-reference standardization, coverage-refresh sync (191 PRs behind), protected-r3 machinery, routed forks, the 3 AI-delta annexes, §2.1/§2.3 (source-gated), §3.16 git-history collapse.
