WINDING DOWN. #941 landing-nav two-level nesting (this PR); #942 session-closing handoff. Morning: review deployed site + §2.16 residual
# then: The maintainer returned attended (2026-07-15), ran the task sequence, and moved the website work into this session so the deployed site is reviewable during wind-down/resume. Merged through #940. #941 (this) = landing-page left-nav rebuilt into a two-level quick-nav (TODO §2.16): 11 domains nested as indented sub-links under "By domain" (no longer a flat Domains group ending the nav), 6 Standards sub-groups nested under "Standards" (short labels, new std-* heading ids), Licence kept, new Contributors link to /about added, indent CSS; also fully rewrote the concurrency-lease Current-task field to end the 3rd-occurrence append-not-reconcile pattern (adopted control: rewrite the whole field, never patch), and batches #940 QA. §2.16 residual (scrollspy active-highlighting + Get-started-step nesting) stays open. #942 = session-closing handoff (full handoff/asserted-expectations/session-metrics refresh, RELEASE the lease, exempt from trailing /validate-pr; next /resume corpus-wide /validate is the compensating control). MORNING SESSION FIRST: review the deployed grclibrary.ai (domain-page type-priority ordering from #940 + the landing-nav nesting from #941), confirm/redirect; then the §2.16 residual (scrollspy + Get-started nesting), §3.47 editorial TODO-provenance sweep (scope-confirmed keep-(was X.Y)), §3.34 historical-dangler cleanup, §3.38 residual, §3.43 gate-48 Check-6 (unblocked now the AICM codes are fixed), §3.76 TYPE_ORDER drift guard, deferred-protected-changes.md items 6/8/9, routed forks.
