# Open Findings Ledger

**License:** CC BY-SA 4.0

Every CONFIRMED defect, from the moment it is confirmed until it is fixed or routed. One row each.

## Why this file exists

A finding that has been read but not acted on is the most expensive state a defect can be in, because
the record shows it was found, so the surface reads as examined. The project already had a rule that a
delivered QA result blocks progress, and that rule has a scope hole this ledger closes: it covered
findings arriving FROM WORKERS and said nothing about findings the orchestrator generates ITSELF while
building. On 2026-07-25 that hole cost two live defects in a file-moving tool, found by the
orchestrator's own probe, rendered as a table row, and walked past in favour of writing a summary
statistic about them. The maintainer caught it.

**So the rule is now source-independent: ANY confirmed defect, from any source, blocks progress.** A
worker delivery, a gate run, an instrument the orchestrator just wrote, a maintainer observation, a
self-caught slip mid-edit. The severity of a defect does not depend on who noticed it.

## The one act this forbids specifically

**Do not write a count, a table, or a comparative statistic about findings before every row has a
recorded disposition.** That is the precise thing that went wrong: three live defects became "3 blind
cases against 27 coverage gaps", and the summary felt like progress while the defects stayed open.
Summarising is not dispositioning.

## Severity and what each blocks

| Severity | Meaning | Blocks |
| --- | --- | --- |
| `error` | wrong behaviour, data loss, a fail-open on a destructive path, a false clean | opening or merging ANY PR, and starting new work |
| `warning` | real defect, bounded blast radius | starting NEW work; an in-flight PR may finish |
| `note` | cosmetic, or correct-but-unclear | nothing; carried to the next PR |

## Disposition values

`FIXED <pr>` (the fix landed), `ROUTED <item>` (a tracked backlog item now owns it, with its tier),
`REFUTED <evidence>` (verified not a defect, with what showed that), `ACCEPTED <rationale>` (a
deliberate, recorded decision to live with it). A row leaves this file only via one of those four; an
empty disposition is what blocks.

## Open

| Found | Severity | Finding | Source | Disposition |
| --- | --- | --- | --- | --- |
| 2026-07-27 | note | `tests/test_linters.py` `PairedSkillStepParityTests` defined `test_runs_clean_on_corpus_at_head` TWICE (the parity smoke test at ~:2301 running `lint-paired-skill-step-parity.py`, and a mis-placed collection-enumeration smoke test at ~:2809 running `lint-collection-enumeration-consistency.py`). Python keeps the LATER definition, so the parity gate's own HEAD-smoke test never ran (dead). Pre-existing (present at base `8b65cacd`, NOT introduced by the 3.115 fix); latent (a future parity-gate regression on the real corpus would not be caught by this class's smoke test, though the direct gate run still would). | claude reverify-3115-fix (security-work), the reissue after the codex re-verify stalled 50 min | FIXED in #1208: the collection-enumeration method renamed to `test_collection_enumeration_runs_clean_on_corpus_at_head`, so BOTH now run (class count 21 -> 22, the parity smoke test un-shadowed and passing). |
| 2026-07-27 | warning | `tools/lint-paired-skill-step-parity.py` `SLUG_ONLY_CELL_RE`'s three-plus-`-_.`-segment branch (TODO 3.115 route (b)) is a SHAPE heuristic that cannot distinguish a machine-id slug from a multi-segment English compound, so a compound like `state-of-the-art` / `up-to-date` / `well-thought-out` in a metadata table cell is wrongly stripped: a LATENT gate-44 over-strip (false positive). The docstring and spec over-claimed "machine-identifier atom" precision. | verify-3115 **claude** (jeff-mailz, adversarial): corroborated the codex slash over-strip AND extended it to multi-hyphen compounds | **FIXED in #1208 by honest documentation + tracking** (the multi-hyphen case has no reliable syntactic signal, unlike the slash case): the docstring now states the shape-heuristic residual, the spec §6 enumeration was softened from "machine-identifier atom" to "path or multi-segment identifier-shaped atom (a shape heuristic ... known over-strip residual)", and TODO 3.148 (3) tracks it with the `command-exempt` escape hatch. Latent (zero current PAIRS files), so the zero-verdict-change headline holds. |
| 2026-07-27 | error | `tools/lint-paired-skill-step-parity.py` `SLUG_ONLY_CELL_RE` (impl of TODO 3.115 route (b)) OVER-STRIPS: its `\S*/\S*` branch treats ANY single-slash no-space expression as a machine-identifier path, so an ordinary English slash-compound in a table cell (`read/write`, `input/output`, `true/false`) is blanked. A gate-44 FALSE POSITIVE: a command representing a `### Read Write` subsection via a `read/write` metadata cell loses `read`/`write` from `content_tokens`, so `unrepresented_subsections` wrongly flags the pair. Reproduced at source (`read/write`, `input/output`, `true/false`, `yes/no` all lose their tokens). | verify-3115 **codex** (jeff-mailz, adversarial, the over-strip lens the claude re-verify missed) | **FIXED in #1208** (pre-merge): the slash branch now requires a PATH indicator (a leading `/`,`./`,`../`; a second `/`; or a `.` on either side of the slash), so a two-word slash-compound stays prose while real paths still strip. Negative-control fixture added (`read/write` kept). Re-verified at source: all compounds kept, all real paths/slugs stripped, gate 44 still green + zero-verdict-change. |
| 2026-07-27 | note | `TODO.md` 3.146's HEADING still asserted the refuted "umask 0002 re-introduces group-writable files" cause after #1204 rewrote the BODY to the inherited-default-ACL cause (heading/body contradiction on a LIVE doc; a reader scanning headings, or a grep keying on the heading, sees the refuted cause as the item title). | `validate-pr-1204` N1 (offloaded, jeff-mailz/claude) | FIXED in #1205: heading retitled to "Group-writable files re-appear in the READ-ONLY repos via the inherited default ACL, not umask (check_perms recurrence...)", matching the corrected body. |
| 2026-07-27 | error | `tools/exec-dispatch.py` `resolve_exclusions` (impl-3111) validated only that a `--not-worker`/`--not-account` token PARSED, not that the resolved/named account EXISTS in the config for the family, so a well-formed exclusion matching ZERO configured accounts (a mistyped `worka` for `work-a`, or a renamed/removed account, or a `--not-worker` id whose account left config) silently no-op'd at rc=0 while reporting the exclusion as applied. This silently defeats the verifier-independence control: the operator believes they excluded the authoring account, but the real account (correct name) stays eligible and the verifier lands back on it. Reproduced at source (`--not-worker claude-unknown-account-...` -> "independence exclusions: unknown-account", rc=0, author's account top pick). | verify-impl-3111 dual-family: **codex** (jeff-mailz, graded error) AND **claude** (security-work, graded warn) INDEPENDENTLY converged on the same root defect | **FIXED in #1205** (this PR): `resolve_exclusions` now takes `known_accounts` (config accounts for the family, availability-independent) and returns a third `unknown` list; the CLI FAILS CLOSED (`ap.error`, rc=2) on any zero-match exclusion, for both `--not-worker` and `--not-account`. Reality-fixture self-tests added (unregistered `--not-worker`, `--not-account` typo, known-in-family control); self-test 53 -> 56. Re-verified at source: both scenarios now rc=2, control (real account) still rc=0. |
| 2026-07-27 | warning | `tools/exec-dispatch.py:296-310` `_release_slot` has a SECOND, distinct fail-open beyond the RESERVE one: it reads via `_read_inflight` (fail-open `[]` on corruption) then `_write_inflight`s the filtered list, so a corrupt `inflight.json` during a release persists `[]` and WIPES every live co-tenant entry across all keys. Unlike a leaked OWN entry (reaping restores by dropping it), wiped co-tenants are never re-added (append-only at reserve), so the under-count is PERMANENT -> over-admission at `max_concurrent > 1`. Re-verified at source against `main` (8094b06d). | Sweep 125 (loop-break /validate) W1, re-verified at source | ROUTED TODO 3.145 -> **FIXED in #1202** (the fail-closed candidate applied; `_release_slot` no-ops on corruption, so no co-tenant wipe). |
| 2026-07-27 | warning | `tools/exec-dispatch.py` `_read_inflight` returns `[]` on a CORRUPT/unreadable `inflight.json` (not just a missing one), a bounded fail-open on the now-sole per-account concurrency cap (a dispatch could under-count in-flight jobs and over-shoot the cap). Bounded + self-healing (atomic write) but violates refuse-on-doubt. | #1199 skeptical verifier (check 7) | ROUTED TODO 3.145 -> **FIXED in #1202** (`_read_inflight` now distinguishes missing (`[]`) from corrupt (unparseable/OSError/non-list/list-of-non-objects -> `InflightCorruptError` -> RESERVE refuses)). |
| 2026-07-27 | warning | `tools/exec-dispatch.py` `_read_inflight` accepted a JSON list whose ELEMENTS are not objects (e.g. `[1,2,3]`): it passed the `isinstance(data, list)` check, then `_reap` crashed with an uncaught `AttributeError` (`(1).get(...)`) instead of failing closed cleanly. A crash is safe-direction (no over-admission) but not the clean operator-facing refuse 3.145 intends. | Codex adversarial verifier (verify-3145), re-verified at source (reproduced the AttributeError) | **FIXED in #1202**: `_read_inflight` now requires a list OF OBJECTS, so a scalar-list is corrupt -> clean fail-closed refuse; reality-fixture self-tests added (self-test 37 -> 39). |
| 2026-07-27 | warning | #1197's daily-rollup root collapse used end-boundary 'Week of 2026-07-13', so it replaced not only the sixteen 2026-07-26 per-PR entries (#1179-#1194) but ALSO the 07-22/23/24/25 daily-summary lines between them and the week block, removing four daily summaries instead of summarizing them in place (transient never-remove violation; git history retains them). | orchestrator (found building #1198), also in validate-pr-1197's scope | FIXED in #1198: the Week of 2026-07-20 block summarizes the whole week (#1056-#1194), restoring the 07-22..07-25 content in aggregate. |
| 2026-07-27 | note | `.claude/CLAUDE.md` new re-read/persistence section used US 'behavioral' where the file uses Canadian 'behaviour' elsewhere (internal-orthography inconsistency; ungated, `lint-language.py` does not scan `.claude/`). | `validate-pr-1196` N1 | FIXED in #1197: 'behavioral' -> 'behavioural'. |
| 2026-07-27 | warning | `.claude/CLAUDE.md` worker-offload rule lead-in still read 'The two-part hard rule' after the body was expanded to SIX numbered points (stale-narrative-after-a-change, which #1195 fixed elsewhere in TODO 3.128). | `validate-pr-1195` W1 | FIXED in #1196: lead-in changed to 'The hard rule ... expanded to six points 2026-07-26'. |
| 2026-07-26 | warning | TODO section 3.128's main prose describes W1 and W2 present-tense as OPEN defects in `tools/audit-token-spend.py` ("12 of 31 reading UNKNOWN"; "withheld/declined/unavailable all still yield a fabricated 8000"), but #1189 FIXED both (`re.S`/DOTALL for W1, the `gap_is_connector` connector-allowlist for W2, self-test 43/43), so the item prose contradicted the shipped code (stale-narrative-after-logic-change, the same class #1193 shipped). The item legitimately stays open only for the per-worker-display measured-versus-estimated residual. | Sweep 124 W1 | FIXED in #1195: section 3.128 re-scoped to the per-worker-display residual with a STATUS line marking W1/W2 fixed in #1189; the original W1/W2 prose retained below it as the historical record. |
| 2026-07-26 | note | The handoff `session-handoff.md` Current-truth snapshot lagged 4 PRs (as-of-#1190: library `2026.07.680` / README `1.10.41`) versus live at #1194. Expected forward-drift (self-declared as-of-last-refresh; `/resume` verifies live), not a gate violation, but worth refreshing after the emergency-wind-down session. | Sweep 124 N1 | FIXED in #1195: snapshot version tokens refreshed to the #1195 head and the Resume cursor advanced to Sweep 124; the keep-current-plus-one-prior block prune is DEFERRED to the session-closing handoff to keep this error-recovery PR focused. |
| 2026-07-26 | warning | The D7 section-6 narrative in `governance/specification-audit-programme.md:235` still enumerated the three D7 surfaces #1193 REMOVED (the guardrail-review, validate-pr, and improvement-log history registers) plus a "runbook" surface D7's code never had, so the spec prose contradicted the code after #1193 changed D7's `SURFACES`. Ungated free-prose drift (change-impact surface-map type-A). | `validate-pr-1193` W1 | FIXED in #1194: the sentence now names only D7's live surfaces (library CalVer, README Version, pack README Version, this spec's Version, the claim-fit history register); the pre-existing "runbook" mis-name (N1) folded into the same fix. |
| 2026-07-26 | warning | #1193 fully closed TODO section 3.135 but dropped the TODO-to-DONE rotation: section 3.135 stayed in TODO.md with no DONE.md entry, so a closed item read as open and would mislead the next-five queue. | `validate-pr-1193` W2 | FIXED in #1194: section 3.135 deleted from TODO.md and a `3.135 (#1193)` entry added to DONE.md, in the same commit. |
| 2026-07-26 | error | `tools/sweep-working-records-to-private.py` has a FAIL-OPEN on its one destructive path: `oneoff_missing_from_archive()` (the verify-before-prune guard) tests archive-copy EXISTENCE (`if not p.is_file()`) rather than a byte/content comparison, so `--prune` deletes a source whose archive copy is present-but-WRONG (stale, truncated, or divergent). Its own docstring calls this "the whole data-safety guarantee of the only new destructive path," which an existence-only check does not deliver. Re-verified at source at `4f79cb94` (the guard still reads `if not p.is_file()`) | `hunt-unsound-guard-inputs` (codex `...f8b8`), re-verified at source by the orchestrator during the takeover tray reconciliation | **FIXED in #1186** (TODO 3.129 closed). The existence check became a byte/content comparison on ALL FOUR destructive paths, not only the named one-off helper: the whole-file copies (records, one-off dirs) byte-compare source against archive via `_files_identical`, and the generated-body archives (weekly changelog-details, roll-up rows) recompute the body from a SHARED source-of-truth helper (`weekly_archive_body` / `rollup_archive_body`, used by both `--emit-archive` and the verify) and compare, so a present-but-divergent copy REFUSES the prune. Reality-fixture self-tests added (a present-but-divergent archive MUST refuse; `_files_identical` identical/divergent/absent), suite 8/8. Prior disposition: ROUTED TODO 3.129, imminence LOW (manual step). |
| 2026-07-26 | warning | My #1179 refutation is TRUE as stated and TOO BROADLY SCOPED. `git grep -c "TODO section"` on `governance/specification-audit-programme.md` genuinely returns 0, but two BARE positional references survive there, `(section 1.19.1)` and `section 3.56a`, and both cite RETIRED numbers (live TODO has 1.19.13 and 3.56, not those). Gate 69 cannot see them because its pattern requires the qualifier ADJACENT to the number, so a bare parenthetical escapes; that is the known gate-69 blind spot surviving in the very file my refutation declared clean | `validate-pr-1179` F-1 | FIXED in #1181 (13861709): both bare references are gone from `governance/specification-audit-programme.md`. Re-verified at source at HEAD (#1187), where `grep -F "1.19.1"` and `grep -F "3.56a"` on the file each return ZERO. Attribution corrected: git shows the occurrence count is 1 and 1 at #1176 (462352b1) and 0 and 0 at #1181 (13861709), so #1176 CONVERTED the eleven qualified `TODO section N.M` citations to closing-PR anchors but LEFT these two bare parentheticals, which is exactly why this F-1 (run post-#1176) could still see them; #1181 removed them. The claim's scope stays corrected in the record from "no positional references remain" to "no QUALIFIED positional references remain". This is the project's own claim-scoping rule turned on me: the grep must run at bare-token width but the CLAIM must name the slot, and I scoped the claim to the grep instead of to the defect |
| 2026-07-26 | error | `submit_state` still has a FALSE-SUBMITTED path. The #1175 fix normalized whitespace so a wrapped probe matches, but did not fix the REGION: `composer_region` takes `rules[-2]` when two rules are present and `rules[-1]` otherwise, and cannot distinguish "one rule because this is Codex" from "one rule because the payload is tall enough that the TOP border scrolled out of the 12-line tail". In the second case it reads the status line BELOW the composer, never finds the probe, and returns `submitted`. Reachable rather than theoretical: the bottom border is essentially always in the tail, the top leaves it at roughly 900+ characters, and the probe is the payload's FIRST 40 characters, the part that scrolls out first, so the check fails most readily for exactly the long payloads the original fix targeted | `validate-pr-1176` E1, proven against the real module | FIXED in #1180 (e7eea68e). `composer_region(pane_text, runtime=None)` now takes the runtime EXPLICITLY (`tools/manage-workers.py:219`) instead of inferring it from the rule count: a one-rule claude pane returns None, which the caller renders INDETERMINATE, rather than reading `rules[-1] + 1` (the status line BELOW the composer) and answering `submitted` for a payload still in the box (`tools/manage-workers.py:270-281`). The E1 reality fixture is at `tools/manage-workers.py:796-820`: a claude pane with only the BOTTOM rule is INDETERMINATE both with and without a runtime, a codex one-rule pane still resolves to submitted, a both-rule claude pane is unaffected by the runtime argument, and `composer_region` itself refuses a one-rule claude pane while still locating a one-rule CODEX composer. Residual `submit_state` hardening beyond this path stays tracked as TODO 3.127. Same severity precedent as V1175-1, graded error for this identical failure direction |
| 2026-07-26 | warning | The token-spend parser reads UNKNOWN for 12 of 31 real deliveries in the tray: `SPEND_PATTERNS` uses `(.{0,20}?)` between phrase and number and `.` does not match a newline without `re.DOTALL`, so any delivery putting its figure in a `## Token spend` SECTION (heading, blank line, paragraph) never matches. Measured against the real tray, not constructed inputs | `validate-pr-1176` W1 | FIXED in #1189 (TODO 3.128, connector-allowlist + DOTALL), and RE-DERIVED at source by the orchestrator rather than accepted: 14 of 36 in the live tray, consistent with the worker's earlier 12 of 31. **The re-derivation contradicts the "safe direction" reading in this row's own finding text.** The per-worker column shows `78ff` at about 1,175 across 12 deliveries while ONE of them states about 360,000 in a `## Token spend` section, so the tool prints a confident figure understating by roughly 300 times rather than falling back to UNKNOWN. A wrong number with no caveat beside an honest unknown-count that has one |
| 2026-07-26 | warning | The token-spend NEGATION guard is a closed enumeration and still invents figures: it closes the exact string the #1175 sweep fed it (`not reported`) while `withheld`, `declined` and `unavailable` all still yield a fabricated 8000 from the adjacent budget figure | `validate-pr-1176` W2 | FIXED in #1189 (TODO 3.128, connector-allowlist + DOTALL) with W1, same tool and same edit. The structural point is that enumerating negations cannot be completed; the parser should require the number to be attributable to a SPEND phrase rather than merely nearby, which is the same input-authority shape as the other findings this week |
| 2026-07-26 | error | **A SEVENTH lost QA-history row, by a mechanism neither worker's detector can see.** `.working/validate-sweeps/history.md`'s Sweep 91 row was not fused: in `8d198b2c` (the Sweep 92 close-out, PR #748) an Edit anchored on the Sweep 91 line and REPLACED it with Sweep 92's, deleting it outright. The row was added in `e94b6923` (#721) and is fully recoverable from that revision. An overwrite leaves a well-formed, width-normal table, so the over-width signal cannot fire and the fusion detector TODO 3.73 specifies would never catch it; the only signal is the sequence gap | orchestrator, running `git log -S"91 iter 1"` against the re-scan's own unresolved gap caveat | FIXED in #1181: Sweep 91's row is restored, recovered from `e94b6923` and verified byte-identical at 1502 characters against the orphaned Summary cell that was its only surviving trace. Worth recording that a verbatim application of the fusion reconstruction would have DELETED that orphan while appearing to repair the line. TODO 3.73 stays open for the gate, which now needs the sequence-gap signal as a first-class check because an overwrite leaves a width-normal table no fusion detector can see |
| 2026-07-26 | note | The independent re-scan's verdict that "six is the true total" is CORRECT as to fusions and INCOMPLETE as to lost rows; it measured the Sweep 91 sequence gap, declined to reconstruct from inference (rightly, that would have been fabrication), but did not run the one-command history search that resolves a gap into a real row | orchestrator, following up the re-scan's own caveat | ACCEPTED as a sound judgement by the worker rather than a defect: refusing to fabricate was correct, and its caveat is precisely what pointed here. The lesson is for the ORDER, not the worker: a completeness order over a ledger must ask explicitly for `git log -S` on each unresolved sequence gap, since a gap is either a number never used or a row deleted, and only history distinguishes them |
| 2026-07-26 | warning | My #1178 fix to `block-on-open-findings.py` reproduced the class it was closing, one layer in. `startswith` on a free-prose cell fails BOTH directions: it FALSE-BLOCKS six real dispositions (`now fixed`, `resolved in #1178`, `closed as duplicate`, `WONTFIX`) because it demands the vocabulary word first and omits synonyms, and it FALSE-PASSES five narrations that open with a terminal word and then negate it, the sharpest being `**routed** but nobody took it`, which has correct markup, correct vocabulary, and says in plain words that nothing was decided | `validate-pr-1178` F-1, worker `opus-20260725T122030Z-6443`, 19-case adversarial probe | ROUTED TODO 3.126 as a STRUCTURAL fix, not a third round of matcher hardening: a free-prose cell cannot answer "is this dispositioned", so the input needs changing rather than the predicate. Hardening the check instead of fixing the input is verbatim the anti-pattern `validate-inference-before-action` names, and I did it while citing that rule |
| 2026-07-26 | note | A codex worker registered under the id `codex-root`, which reads as running with root privilege and prompted the orchestrator to flag a privilege concern to the maintainer | orchestrator, misreading a worker-chosen id | REFUTED by the maintainer, 2026-07-26: the worker is NOT root, it simply chose that id. Recorded rather than dropped because the next reader of the registry would draw the same wrong conclusion. It is a minor instance of the session's theme, that a worker's self-description is not evidence of its state; a worker id is free text the worker picks and establishes nothing about the account it runs under |
| 2026-07-26 | error | Re-dispatching an order to a DIFFERENT worker family does not remove the original: after re-routing `validate-pr-1176` from codex to opus, `opus-20260725T121943Z-78ff` claimed it within seconds while the codex copy remained in `codex/available-work` and still read as available, so the same order id was claimable twice and two workers could have delivered it | orchestrator, observed doing the re-route | FIXED operationally by moving both codex copies to `done/superseded/` with a name recording why, and re-verified (`list-available` now reports no available orders). ROUTED TODO 3.125 for the mechanical half, since the dispatch path should either refuse a re-dispatch of a live order id or supersede the original itself rather than leaving the operator to remember |
| 2026-07-26 | error | A LIVE codex worker reads as DEAD for most of its life: `codex-20260725T210500Z-81f5` went **163 minutes** between its first heartbeat and its second, roughly eight times the 20-minute stale window, while holding an order. During that gap it was a textbook reclaim candidate, so the stale-scan could have reassigned its order and two workers would then deliver the same order id | worker `opus-20260725T121943Z-78ff`, self-CORRECTING its own earlier drop | ROUTED TODO 3.125 change (c): until the cadence is fixed, the stale-scan must not reclaim from the codex family, by a per-family window or an explicit refusal. Orchestrator re-measured the live registry and saw `81f5` LIVE at hb 13.7m while earlier in the same session it read 54.7m stale, which is consistent with the gap |
| 2026-07-26 | note | My own drop `issue-codex-sessions-die-after-one-heartbeat.md` asserted two consecutive codex sessions died at startup and inferred a startup loop; one of the two refuted it by heartbeating again after 163 minutes | worker self-correction, before this orchestrator committed anything on it | REFUTED and corrected in place: TODO 3.125 and the #1179 CHANGELOG entry both carried the wrong claim in draft and were rewritten before commit. Recorded because the correction arrived inside the window where acting on the wrong diagnosis was still possible, which is the value of an out-of-band issue channel |
| 2026-07-26 | warning | The pre-queued order `selftest-gaps-workers-deliveries` instructs `python3 tools/audit-selftest-discriminability.py --tool <path>`, but the tool rejects `--tool` (exit 2, `unrecognized arguments`) and accepts positional paths; a third order whose text was not executable as written, after `validate-pr-1170` naming a function that never existed and `mutation-audit-todays-three-tools` naming gate 78 before it merged | worker `codex-20260725T210500Z-81f5` | ROUTED TODO 3.118 (order-premise validation at dispatch), whose scope this widens from paths and item ids to CLI FLAGS: three instances in one day is past the codification threshold, and the existing dispatch check verifies referenced paths and `TODO N.M` items but not that a named flag exists in the named tool |
| 2026-07-25 | warning | `tools/decisions-search.py` does not scan `.claude/CLAUDE.md`, where maintainer directives are recorded, so it reported NO recorded decision for the gate-69-`docs/` question the maintainer had answered in writing on 2026-07-25 (CLAUDE.md:1756, quoted verbatim in the defence-in-depth section). The guardrail built to prevent re-asking a settled question gave a false negative on one | orchestrator, while triaging inbox drop D1 | ROUTED TODO 3.123: add CLAUDE.md (and the pack rules) to `SEARCH_STORES`, with a self-test case keyed on this exact query. Same guard-input-authority class as the other four this week: the check asks "has this been decided?" and its three-store input cannot answer it. D1 itself is NOT re-asked; the recorded answer is widen |
| 2026-07-25 | error | `grc_library_ref` `catalogue.yml:4922`: the `notes` field on the EU Implementing Regulation (EU) 2025/454 entry describes an AUSTRALIAN statute (royal assent, amends the Privacy Act 1988), and the Australian entry at `:4838` has no `notes` of its own, so the text was moved onto the wrong entry rather than duplicated; both carry `last_checked: 2026-07-11` | worker inbox drop `opus-worker-maintainer-decisions-2026-07-25.md` D4, worker-verified three ways | ROUTED TODO 3.124: a cross-repo `_ref` fix needing no egress (move the line to the Australian entry, give the EU entry notes describing the EU instrument or none). NOT yet re-verified at source by the orchestrator, because `_ref` is a separate repository and the fix is a separate PR; re-verify before applying |
| 2026-07-25 | note | `grc_library_ref` `catalogue.yml:5531`: the Canada TB Directive title presents a 24 June 2025 to 24 June 2026 compliance transition as running, but the held text's sections 1.2.1 and 1.2.2 put that window's end at 2026-06-24, which has passed | worker inbox drop D5 | ROUTED TODO 3.124 alongside D4, since both are `_ref` catalogue corrections in the same file needing no egress. Held-evidence-plus-calendar, not a currency claim |
| 2026-07-25 | note | Inbox drop D2 claimed eleven positional references in `governance/specification-audit-programme.md`, five already broken, including a `TODO section 3.9` resolving to a DIFFERENT item through pre-rule recycling | worker inbox drop D2 | REFUTED as to its headline: re-measured on live `main`, `grep -c 'TODO section'` on that file returns **0**, because #1176 converted all eleven citations to closing-PR anchors, and Sweep 122 independently confirmed the same zero. The drop was written against a pre-#1176 tree. The residual half (whether adding `section(s)` to gate 69's vocabulary is FP-free today) is re-measured when TODO 3.110(c) is built |
| 2026-07-25 | warning | Gate 50 Check 1 is satisfied by row PRESENCE, so #1176's honest `DISPATCHED, RESULT PENDING` row satisfies it and the parity gate reads GREEN while that PR's `/validate-pr` has never run; an undelivered order can hold a PR's QA open indefinitely with the gate green throughout | Sweep 122 Part 3, and the maintainer's directive at the same resume | ROUTED: convention half FIXED in #1178 (CLAUDE.md close-out item 3 and pack `session-lifecycle.md` section 5); the mechanical half is TODO 3.120, a third `pending` state in Check 1, which cannot land green until #1176's own QA returns, so it is sequenced behind that |
| 2026-07-25 | warning | #1176 merged UNVALIDATED: 22 files, 813 insertions, six backlog-item closures, its `/validate-pr` order dispatched but never claimed, and the session then closed | maintainer, 2026-07-25 | ROUTED TODO 3.122 (H severity), which closes only when the result has RETURNED and every finding is dispositioned, never when the order is merely re-dispatched. Still queued and unclaimed: both codex workers declined on a documented independence conflict and both opus workers are chain-adjacent, so the fleet cannot serve it; a fresh worker is requested, and per the rule #1178 adds the orchestrator runs it directly rather than closing over it |
| 2026-07-25 | warning | `audit-selftest-discriminability.py` divided a MEASURED numerator by an ATTEMPTED denominator: its summary said `73 NOT-DETECTED across 21 tool(s)` when 10 of those 21 are VOID and contribute no figures, so the line read as full coverage of 21 tools when only 11 were measured and the other 10 carry an UNKNOWN exposure | Sweep 122 note, re-measured at source by the orchestrator | FIXED in #1178: the summary now reports measured-of-probed, names the 10 void tools, and states the total understates the truth by an unmeasured amount; self-test 17/17 and the re-run reproduced the worker's void list exactly |
| 2026-07-25 | note | The handoff's per-tool discriminability figures `9/4/14/6 (total 33)` attribute a 6 to `audit-selftest-discriminability.py`, which appears ZERO times in its own report; the three real figures sum to 27, and the two tools reporting 6 are `audit-delivery-status.py` and `audit-inbox-drops.py` | Sweep 122 note, re-measured at source | FIXED in #1178 in the live carrier (the handoff soft-spot list). The frozen `.working` records that carry the old figure (the #1175 per-PR file, its history row, the detailed CHANGELOG entry) are left as written, because a frozen record states what was believed at write time and retro-editing it is the anti-pattern; the correction is recorded here and in the Sweep 122 row |
| 2026-07-25 | note | A sweep order that directs the worker to remove `origin` from its clone makes the pre-push guard's D-series unrunnable (all eight fail rc=2 on merge-base resolution), so a handoff's pre-push-green claim is not re-derivable and comes back VOID rather than confirmed or contradicted | Sweep 122 Part 5 | ROUTED to TODO 3.121: future sweep orders either keep `origin` and give the version-monotonicity gate its own exclusion, or create a local `origin/main` ref at the base SHA. Not fixed here because changing what the guard measures is a decision, not a workaround |
| 2026-07-25 | warning | `manage-workers.py` reported NOT SUBMITTED for the `mailz` pane (payload still in the composer after 8s) while the pane in fact showed an empty composer and a completed reply, so the verdict was WRONG rather than merely the known UNVERIFIED indeterminacy | orchestrator, observed this session while nudging codex | ROUTED: folds into queue item 5 (`submit_state` indeterminacy), widening it from "returns UNVERIFIED when the composer is not in the captured tail" to "can also return a positively WRONG NOT-SUBMITTED". The pane capture is retained above as the reality fixture |
| 2026-07-25 | error | `.working/validate-sweeps/history.md` carries FIVE fused ledger rows and `matrix-fit/history.md` one, all live on `main`; four of the fusions DROPPED a sweep's row entirely (Sweeps 88, 86, 27, and one unidentified), so QA audit trail has been silently lost, and TODO 3.73 records only #915 as the class's first escape | worker `opus-20260725T121943Z-78ff` inbox issue drop, `design-3.73-ledger-row-gate` | FIXED in #1181: all six fusions repaired, reconstructed from the intact prior revision of their own ledger with nothing synthesized, applied deterministically with a re-parse rather than hand-edited, and the de-link rule applied so five swept detail files did not return as dead links. Post-repair: zero fusions and zero unexplained sequence gaps remain. TODO 3.73 stays open for the GATE, whose scope widened |
| 2026-07-25 | error | `credit-offload-queue.py list-workers` returned an EMPTY registry while the file-drop plane held up to eight live workers, AND `/resume` step 3 plus the CLAUDE.md mandatory-offload gate both directed the orchestrator to that tool, so a resume would read zero workers and license self-running every offloadable pass | orchestrator, 2026-07-25 | FIXED both halves: the tool now fails loud naming the other plane, its count and the authoritative command, distinguishing an absent root from a present-but-empty one (self-test 66/66, pushed); and both instruction surfaces are repointed, each edit asserted to have landed |
| 2026-07-25 | error | The worker-id exclusion control FAILED OPEN: a transposed id named a worker present on neither plane and excluded NOBODY, silently, which is how a change merged with both independent adversarial lenses served by the same worker | maintainer alert 2026-07-25-a NOTE 2 | FIXED: dispatch now refuses when `not_worker` names an id the exchange has never seen, with an override for a genuinely not-yet-started worker; verified by replaying the alert's ACTUAL transposed id (refused) against a real id (dispatched) |
| 2026-07-25 | warning | No release path existed, so a worker that must not serve an order it holds had to deliver a NOT-RUN notice, leaving the order consumed-but-unserved; it cost two cycles the same day | maintainer alert 2026-07-24-c | FIXED: `release` added, refusing a non-holder, a missing reason and a name collision; both worker contracts updated to prefer RELEASE over NOT-RUN when the order is fine but the worker is the wrong party |
| 2026-07-25 | note | I called a helper `_clear_held_order` that does not exist anywhere in the tool, while writing the release command | orchestrator, self-caught | FIXED before any run: verified zero definitions, found that this plane derives holding from file LOCATION with no marker at all, removed the call and documented the property instead of inventing the function |
| 2026-07-25 | warning | The git-scratch queue tool had NEITHER exclusion key in its write-field list, so a rewrite SILENTLY DROPPED `not_worker` / `not_author_of` and independence was unenforced while appearing enforced | `triage-tray-34` rank 2 | FIXED: both keys added to the preserved-field list, with the revert mutation-proved DETECTED under bracketing controls; verified not to have bitten beforehand, since `reconcile-queue` uses a different write path |
| 2026-07-25 | warning | Gate 69's two blind spots remain live (part (c) of the never-recycle work, and the surviving F3 of the #1152 pre-push verify) | `triage-tray-34` ranks 4 and 20, `draft-3110c-gate69-blindspots` | ROUTED: this is TODO 3.110 part (c), already carried as a separate item with its candidate delivered and unapplied |
| 2026-07-25 | warning | Three fresh-reader findings against the adopter documentation verified still live at HEAD | `codex-fresh-reader-corpus-review` via `triage-tray-34` rank 29 | ROUTED: adopter-facing prose, queued behind the safety and correctness work; the delivery is retained in the tray until applied |
| 2026-07-25 | note | `manage-workers.py` and `collect-deliveries.py` each have one `if failures:` self-test reporting branch that the self-test cannot assert from inside itself | `audit-selftest-discriminability.py` | ACCEPTED: structurally untestable in place, and covered EXTERNALLY by the probe's positive control; now classified `INHERENT-EXTERNALLY-COVERED` rather than reported as a fixable defect |
| 2026-07-25 | note | 27 coverage-gap guards across four tools (`manage-workers` 9, `collect-deliveries` 4, `audit-worker-saturation` 8, `audit-inbox-drops` 6): guards no self-test case reaches, chiefly CLI dispatch and error paths needing a live external process | `audit-selftest-discriminability.py` | ROUTED: these are coverage gaps rather than blind cases, so the fix is a case or a recorded out-of-scope decision per guard; queued behind the error and warning work |

## Closed today

| Found | Severity | Finding | Disposition |
| --- | --- | --- | --- |
| 2026-07-25 | warning | `collect-deliveries.py`'s `report()` was ENTIRELY untested, all four branches genuine coverage gaps: the function that says what was held back and why, so an untested report is how a held-back file becomes the silent skip the tool's design forbids | FIXED; 8 output-asserting cases added, all four branches mutation-proved DETECTED with corrected controls |
| 2026-07-25 | note | I claimed `collect-deliveries.py` had 9 guards; an independent audit measured 21 control-flow decision sites. I had counted my own hand-picked mutation list and called it the population | ACCEPTED and recorded: the probe now discovers sites rather than taking a hand-listed set, which is why it is a tool |


| Found | Severity | Finding | Disposition |
| --- | --- | --- | --- |
| 2026-07-25 | error | `collect-deliveries.py:81` empty-part guard in `split_name` was BLIND: the cases covered a non-tray name and a wrong extension but never an empty part, so `split_name("__x.md")` returned `("", "x")` and a caller could use an empty worker id, which the saturation tool's phantom-pending retirement depends on being well-formed | FIXED this PR; two cases added and the guard's removal now FAILS the self-test, verified |
| 2026-07-25 | error | `audit-worker-saturation.py` reported SATURATED while a worker sat idle, because the delivery tray added a third location its phantom-pending retirement did not scan, and the tray's composed filename also needed splitting | FIXED this PR; two fixture cases added, both mutation-proved load-bearing with controls bracketing the run |
| 2026-07-25 | error | The probe voided itself on any tool importing a sibling module, because it wrote the mutant into a temp SUBDIRECTORY | FIXED this PR; mutants are sibling files now, caught by the probe's own negative control |
| 2026-07-25 | error | The probe reported a FALSE CLEAN (22 detected, 0 findings) after the verdict strings gained a suffix while the filter still tested exact equality | FIXED this PR; filter matches by prefix, caught only because the number contradicted an earlier run |
