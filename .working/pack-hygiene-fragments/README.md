# Pack-hygiene scrub fragments (transitional staging directory)

One file per scrubbed pack surface, each preserving VERBATIM (inside fenced code
blocks) every passage the pack-hygiene generalization removed or rewrote, with a
one-line note per entry naming the generic replacement. The move-not-delete
invariant's audit surface for the skills and README tranches.

Transitional by design: at apply time these entries are appended to the removal
ledger [`claude-rules-considerations.md`](../claude-rules-considerations.md) (or
retained here at the maintainer's choice), and the two README archives
(`readme-version-history-original.md`, `readme-body-removals.md`) stay as the
pre-thinning record. Nested code fences inside originals are wrapped in
four-backtick outer fences.
