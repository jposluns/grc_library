# Pending Decisions

**Status:** 5 pending items (the EU AI Act jurisdiction annex, BUILT this session but merge HELD pending maintainer, TODO line-48 egress-deferral for currency plus a newly-found pending "Digital Omnibus on AI" amendment; the FR-59 privacy-jurisdiction annex deepenings, DEFERRED-BLOCKED as egress-gated; the GR-P2 condense framework-table design decision, a stricter-safe default applied with tranches 2-12 HELD pending maintainer confirmation; the FR-154 operational-vagueness cluster applied in #738, 5 stricter-safe values PROCEEDED for confirm-or-redirect plus sub-item 5 DEFERRED-BLOCKED as unheld-source; and the eIDAS2 annex naming/title stricter-safe default applied in #739 for confirm-or-redirect; see the Pending section). Prior: 0 pending items (the changelog-restructure PR 2 sweep-to-scratch blocker was RESOLVED to Option A by the maintainer 2026-07-08 and EXECUTED in scratch PR #113 + grc_library #708; see the resolved entry in the Pending section). **The TODO 1.9 PreToolUse-hook-firing disposition was RESOLVED 2026-07-06** (maintainer: close 1.9 as a documented harness limitation, disposition A; root cause pinned, no portable corpus-side fix, compensating controls active and recorded in [`third-party-issues.md`](third-party-issues.md); TODO 1.9 CLOSED and rotated to DONE in #677; see the resolved-inline marker on the entry in the Pending section). **The #670 lowercase-tolerant acronym-pattern rejection was CONFIRMED by the maintainer 2026-07-06** (no reopen; the corpus-wide-evidence rejection recorded in the #670 `/retro` row stands, so the "confirm or reopen" surfaced at #670 merge is closed). **All four formerly-pending items were RESOLVED at the maintainer's 2026-07-05 daytime-unattended round** (the session switched overnight -> daytime-unattended; execution is queued per the resolutions): (1) Sweep-84 Brazil citations -> ANNOTATE the four 2026-statutory designations as pending primary-source verification in the annex and the breach-response matrix [EXECUTED in #662; TODO 1.11 tracks the verification]; (2) TODO 3.1 FIT-8 -> coherence-preserving (fix row 274's internal mix only, leave row 225) [SUPERSEDED 2026-07-05 by the maintainer's accuracy-principle redirect and EXECUTED corpus-wide in #663 to the /IEC accuracy direction, not option B]; (3) TODO 3.1 matrix ISO header -> coupled lockstep PR (rename the 11 headers + legend AND update gate 49's ISO_HEADER constant + fixtures + docstring + spec together) [EXECUTED in #664, gate-49 proven still-firing post-rename, two adversarial verifiers]; (4) scratch `originals/README.md` -> refresh to a LIVE manifest (rebuild the table against current disk, correct the present-tense status; leave `ref/README.md`'s dated seeding section as a timestamped log with a since-superseded pointer). The same round also resolved: **TODO 3.21** four residuals -> captured 2026-07-05 as (normalize the matrix C-TPAT cell; carve-out the five `ensure`-in-tool-string cases; apply the approved FQ-B1 monotonicity reword; normalize the three `PCI DSS v4` labels to v4.0.1), then SUPERSEDED and EXECUTED in #667 (2026-07-06 overnight run): the maintainer re-answered the four in plain chat (the `AskUserQuestion` tap-option failed to render again) as (a) normalize the matrix C-TPAT cell to bare `C-TPAT` [matches], (b) REWORD the five tool strings to `ensure that` (NOT the 2026-07-05 carve-out), (c) apply the FQ-B1 monotonicity reword [matches], (d) LEAVE the three `PCI DSS v4` family labels as-is. The maintainer then CLARIFIED (d) the same night: the standing preference is the full latest version `PCI DSS v4.0.1`, with the family label `v4` acceptable ONLY when quoting another document that uses it; so (d) is re-resolved to NORMALIZE, EXECUTED in #668 (the three cloud-baseline framework-alignment cells `standard-cloud-hardening-baseline-{aws,azure,gcp}.md` to `PCI DSS v4.0.1`; the citation-form template's discouraged-example `PCI DSS v4` at `register-compliance-obligations-template.md:49` is LEFT, since it illustrates the bad form rather than citing it). The (b) reword stands. Both (b) and (d) are reversible and non-harmful. TODO 3.21 CLOSED and rotated to DONE in #667; the (d) follow-through landed in #668. The standing PCI-DSS version-label preference is recorded in [`design-decisions.md`](design-decisions.md). Root of the divergence: the 2026-07-05 resolution was recorded here but never propagated into TODO 3.21's text (which still read "await maintainer decisions"), so the resumed session re-surfaced it from the stale TODO (logged as the #667 `/retro` observation); **TODO 1.9** hook-firing -> PURSUE the harness fix (investigate why PreToolUse hooks do not fire in resumed sessions before closing; the maintainer overrode the accept-and-close recommendation); **TODO 3.15 r5a** self-contradiction -> reword the rule-3 fallback (drop the longer-check-in option); **P2 start** -> 2.1 privacy jurisdiction annex operational deepening (FR-59). The four formerly-pending entries below are retained with their resolutions until each is executed and rotated. See the Pending section below. Prior to that, every open item was resolved at the maintainer's 2026-07-04 morning rounds (two chat batches, thirteen answers; the `AskUserQuestion` window failed to render for the maintainer, so batch 2 ran as plain chat text). Round 1: WCO SAFE CONFIRMED as shipped; the GR-8(b) disposition vocabulary (with WATCH) CONFIRMED as shipped; the expiry tail resolved to a ONE-PASS BATCH REVIEW (a research-drafted, orchestrator-verified triage proposal over the 236 pending pre-July rows, maintainer dispositions in one round; IN PROGRESS); the option-B register-ageing advisory tool DECIDED BUILD (TODO GR-8 follow-on A). Round 2 (nine answers, all recorded in TODO/DONE/design-decisions): (1) the authorized CLAUDE.md touch bundle AUTHORIZED as the next PR; (2) GR-3 wave 3 CLOSED keep-as-is; (3) GR-4 = standalone unbalanced-fence gate; (4) gate-54 rename APPROVED; (5) GR-GAP-1 = BUILD after the P2 applies; (6) r4 G-3 = extend gate 64; (7) r4 G-4 = hoist the binding-sentinel constants into lint_common; (8) r4 G-7 = runbook subsection now with a standing revisit-the-skill-option note; (9) the INAI-staleness sweep folds into the Mexico P2 apply's window. Prior state: every open item was resolved at the maintainer's 2026-07-03 morning round (the three 2026-07-03 overnight entries below: the DPO sweep's four proceeded defaults CONFIRMED as shipped; the CLAUDE.md D5 form-description refresh and the RM-10 unpiped-guard codification both AUTHORIZED, shipping together in the D5 PR with the seventh-form widening). The same round issued seven standing directives for the day's queue, recorded in the `## Resolved at the maintainer's 2026-07-03 morning round` section below: P2 operational values set by the assistant with logged defaults; scratch SR-wave self-merge on green; protected-tree factual-staleness one-liners authorized as a class (each logged); TODO's Standing-conventions / Backlog-totals / Priority-7 sections KEEP (section 3.10 closes); section 2.5 ships as a methodology doc with scoring unchanged; section 3.8 CLOSED accepting `not instrumented` as the permanent state; FR-70, the 51 needs-reconfirm rows, and the ISO-31000 refresh STAY DEFERRED pending the maintainer's egress instance. Prior state: every open item was resolved at the maintainer's 2026-07-02 return round. The two formerly-held pending items (the advisory-directive codification and the CLAUDE.md D5-description refresh) are AUTHORIZED to ship together with the proceeded-defaults rotation ("Yes, ship all three"), so they are now queue work, not open decisions. All 6 proceeded entries are maintainer-resolved: the four round-confirmed entries (the three overnight entries below, the C3 taxonomy-reconcile canonical-wins pair, the C4 retention composed value, and the C5 DSAR choices, plus the 2026-07-02 section-3.14 retention pair whose two flat-valued AI-row siblings were since composed per the round-1 "Compose both" answer) and, at the return round, the gate-60 drift threshold (REDIRECTED to fail-at-3; the change ships in the follow-up gate-60 PR) and the GR-P3 aid-not-gate shape (CONFIRMED). The return round also decided: lifecycle vocabulary aligns to "Superseded" across the four carriers; the second coverage-gaps batch executes re-grade-with-evidence; the coverage vocabulary widens Partial's definition. The move-to-resolved rotation executed in the authorized CLAUDE.md batch PR: eight entries moved to the Resolved section below (the six maintainer-resolved proceeded entries plus the two shipped held items). The FR-70 source gate (MiCA / NYDFS BitLicense not held) remains a standing constraint tracked in the handoff Open-decisions section. Historical: both 2026-07-01 entries were **RESOLVED at the 2026-07-01 `/resume`** (maintainer answers, recorded in the Resolved section below): (1) **§3.2 detailed-CHANGELOG-mirror header-parity** = **cutoff-scoped gate** (accept the 3 pre-split-era missing headers as an exemption; scope the parity gate to compare only from a cutoff PR forward; FP-free, no retroactive audit-trail edit, still prevents future drift); (2) **§3.3 citation-verification-pass recording model** = **consistency cross-check, no register row** (§3.3 cross-checks register version+date vs the held `ref/` extract, surfacing/fixing any MISMATCH but recording NO §9 register row when consistent; the register's human-primary-capture trust model is unchanged, so the "record in the register" deliverable drops). Both are now **P3 tooling** work, scheduled after the P1 FR-48 rename per the maintainer's post-P1 = P3-tooling direction. A reference-base currency item was RESOLVED 2026-07-01 (reference-base work tracked separately in the reference repo; the corpus register was already current from #512). The **51 `needs-reconfirm` register rows** remain informational (not a decision; a future browser-egress cycle fills their upstream-check URLs). See the resolved entry in the Pending section below. **FR-24 control-testing result-classification reconcile-vs-keep was RESOLVED 2026-06-30** (maintainer confirmed at the `/resume`: keep the SOX-framed scheme + the §4.2 CAPA crosswalk as shipped in #492; no corpus change; the keep-and-crosswalk stricter-safe state stands). The former entry is moved to resolved below. **FR-58 inheritance-vocabulary was RESOLVED 2026-06-30 (shipped #497): the maintainer chose the index-register-column locus and FR-58 CLOSED; the former mechanism-fork entry below is moved to resolved.** **FR-167(b) matrix gap-fill IN/OUT scope RESOLVED 2026-06-30 (maintainer chose Option B: row the 6 OT docs + the AI Security Tooling Landscape register, exclude the deprecated `annex-regional-privacy-requirements.md` stub; shipped #494, FR-167 CLOSED).** §4.6 (QA-cadence mechanical enforcement) was RESOLVED 2026-06-29 by the maintainer ("close 4.6 as satisfied", in chat after `AskUserQuestion` flaked a 3rd time): close §4.6 as substantially-satisfied by gate 50 Check 1 (which already enforces the missing-row half corpus-wide); the abbreviated-marker residual stays the CLAUDE.md "Throughput pressure does not authorize QA abbreviation" convention. See the §4.6 entry below. The S3 design fork (TODO §4.5; surfaced 2026-06-29) was RESOLVED 2026-06-29 by the maintainer (defer/park S3 pending a clearer design, since gates 48/49/54 already cover control-code existence corpus-wide; move to §4.6 / §4.8 next). The earlier 2026-06-29 decisions are RESOLVED by the maintainer: the §4.10/§4.6 gate-family (build Option B marked-done PLUS the complementary CHANGELOG-asserts-closure check; the marked-done half shipped as gate 57 in #468, the complementary check shipped as the D5 PR-time check in #469) and the `docs/` house-style (Option A then refined to "full house-style now": extend the gate-2 language audit to `docs/` and fix all 117 findings; shipped in #470). See the resolved entries below. The S2 role-drift decision (2026-06-29) was RESOLVED by the maintainer (the "migrate allow-list to register" reframe), shipped as PR #463 (since merged). The four overnight-clarification questions were answered by the maintainer 2026-06-28 via screenshot; recorded below as RESOLVED standing directives. **Two resume-session decisions resolved 2026-06-29 (maintainer screenshot):** operating model = **full project PR workflow** (per-PR feature branch, open + self-merge green PRs via GitHub MCP, attended-autonomous), overriding the wrong harness-config "single-branch / no-PR" text (the maintainer confirmed the harness config was wrong; no cleanup needed since no work was committed under it); next focus = **guard-rail gates + codifications**. Recorded here for durability across compaction.

This file is the durable queue for the **attended-autonomous operating mode** (see the
`## Attended-autonomous operating mode` section in [`.claude/CLAUDE.md`](../.claude/CLAUDE.md)):
when the assistant surfaces a decision that is genuinely the maintainer's and no answer
arrives (for example because the `AskUserQuestion` primitive errors, a transient
permission-stream failure), it records the decision here and continues, rather than
stalling.

On `/resume`, the assistant reads this file first, surfaces the still-`pending` entries to
the maintainer, resolves those tasks, and only then continues to the next queued items.

## Overnight-run authorizations (2026-07-09, maintainer-batched for a no-pause overnight backlog run)

The maintainer batched these (2026-07-09) to enable an autonomous overnight BACKLOG-clearing run (the swap session). All four are RESOLVED; the overnight `/resume` executes them without re-asking.

1. **Overnight scope = BACKLOG ONLY.** The first `/deep-assessment` run is DEFERRED (NOT run overnight); the overnight session clears the scratch-delivery backlog and authors the research deliveries. This supersedes the earlier "deep-assessment overnight" plan; `/deep-assessment` stays maintainer-invoke-only (Opus 4.8 when run, per the earlier decision), for a later session.

2. **Overnight autonomy = fix + route + APPLY protected edits.** Fix clear findings in in-window PRs (full per-PR `/validate-pr` + `/retro` + a refute-briefed skeptical verifier on substantive changes; green CI = merge authority); route authorial/ambiguous findings to the register; apply stricter-safe defaults where reversible and evidence-backed; defer-and-skip irreversible/outward-facing; **APPLY (not stage) protected `.claude/` + `dev-security/claude-rules/` pack edits overnight.** CONSTRAINT (maintainer): protected-apply is **LOCAL-INSTANCE-ONLY.** If the overnight session runs on the local NUC, apply protected edits; if it runs on a CLOUD instance, STAGE them in [`deferred-protected-changes.md`](deferred-protected-changes.md) instead (the standing overnight-defer discipline). No idle-stop; work the priority-ordered queue.

3. **TODO 3.21 = fix cell + pack-wide.** F1: migrate the audit-trail-trust attribution from CCM `LOG-08` to the fitting held `LOG-04` / `LOG-10` BOTH in the AIQT principle document §4 AND across the pack-wide `LOG-02, LOG-08` convention (fold the pack half into the GR-P2 condense). F2: swap ISO `A.8.34` to `A.5.33` ("Protection of records", the stronger anchor) in the AIQT doc §4. Attribution-accuracy corrections; spot-check each against the held CCM v4.1 and ISO/IEC 27001:2022 texts (the ISO Annex A.1 extract is conversion-scrambled, so use context reads, not line adjacency). Closes TODO 3.21.

4. **Big / cross-repo deliveries = APPLY overnight.** Apply 2.11 publications-screening (the two-repo new-skill + new-command build: `grc_library_ref` PR A first, then the grc_library PR B) and the cross-repo `grc_library_ref` PRs (SR-3 ref binary-scan; 2.11's ref half) overnight under full autonomy, each with per-PR QA + a skeptical verifier. The `grc_library_ref` writes go via PR (the local git proxy 403s direct pushes).

The detailed overnight backlog queue (the delivery list, per-delivery handling apply-ready-vs-research-authoring, and the priority order) is in the [`session-handoff.md`](session-handoff.md) `## Next actions` overnight block.

## Pending (open; surface at next attended boundary / `/resume`)

### EU AI Act jurisdiction annex: BUILT this session, merge HELD pending maintainer (2026-07-09 unattended run)

The FR-62 EU AI Act annex (TODO §5.1 / §5.9) was authored this session against the held enacted text (`grc_library_ref/legislation/EU/EU-AI-Act-Regulation-2024-1689--full-text.md`): a new `ai/jurisdictions/annex-ai-european-union.md` founding the `ai/jurisdictions/` subdirectory, an 11-section per-regime view (operator roles Art 3; risk tiers Art 5/6 + Annex III; provider chain Art 16/49/50; deployer chain Art 26/27; GPAI Art 51/53/55; timeline Art 113; penalties Art 99/101; adopter-role framing). Every article anchor was verified verbatim against the held text. Owner/Category stricter-safe default = Chief Information Security Officer / AI Governance (matches `ai/policy-ai-compliance.md`).

**Disposition: merge HELD, work PRESERVED (not discarded), surfaced for maintainer.** TODO line 48 (maintainer Option B, 2026-07-06) explicitly egress-deferred the EU AI Act annex (5.1/5.9) for currency, unlike eIDAS §5.7 (not on that list, so #739 shipped). The deferral's condition is "currency cannot be confirmed upstream this session." I confirmed currency upstream THIS turn via WebSearch (which works here, though EUR-Lex full-text is not fetchable): **Regulation (EU) 2024/1689 remains the in-force authoritative text** (exactly what the annex cites), BUT a material amendment is in the legislative pipeline: the **"Digital Omnibus on AI"** (Council doc 9247/26), **provisionally agreed 13 May 2026 but NOT yet adopted** (still needs Council + Parliament endorsement, then legal-linguistic revision), inserting Articles 4a/60a/75a-75e and a new Annex XIV. Because (a) the maintainer explicitly deferred this exact item, (b) "pending the maintainer's egress instance" suggests they intended to own the currency check, and (c) a known pending supersession is a reference-currency-SOP flag, I did NOT merge on my own reading (merging an adopter-facing annex against an explicit deferral, unattended, is the outward-facing action the reversibility gate protects). The built annex is preserved at `scratchpad/held-fr62/annex-ai-european-union.md` with a `WIRING-NOTES.md` for quick re-apply.

**Maintainer action:** (A) authorize merge now, in which case I add a Digital-Omnibus currency caveat to the annex's Limitations (the in-force text is cited; the Omnibus is flagged as a pending-not-adopted amendment to track) and wire it into ai/README + doc-index + decision-tree §5.1 + glossary; or (B) keep it deferred for your egress-instance currency review / personal review of the jurisdiction annexes; or (C) redirect (e.g. hold until the Digital Omnibus is adopted, then author against the consolidated text). The sibling Colorado AI Act annex (also §5.1/5.9, egress-deferred) and Mexico LFPDPPP (§5.8) share this constraint and were NOT built this run. The eIDAS discoverability-wiring PR (immediately following #739) carries only the non-blocked eIDAS decision-tree/glossary fixes and the #738/#739 QA batch, not the held annex.

### GR-P2 governance-rule condense: framework-table direction + HOLD on tranches 2-12 (2026-07-09 overnight run)

GR-P2 (the operative-core condense of the 13 governance rules) shipped **tranche 1** (the condense method, the removal ledger [`.working/claude-rules-considerations.md`](claude-rules-considerations.md), and one exemplar rule condensed, `gate-discipline.md` 1559->1173 words, moving only the why-section to the ledger and keeping the operative core + framework table). The Fable delivery surfaced a genuine **design decision** the maintainer should settle before the pattern is replicated across the other 12 rules: GR-P2's brief lists "framework tables" among the removable rationale, but the pack README (line 34) names the canonical RULE as the source of truth for framework alignment and carries no per-rule alignment matrix, so moving the tables to the `.working/` ledger would strip distributed traceability adopters rely on (adopters do not receive `.working/`).

**Disposition: stricter-safe default APPLIED (KEEP the compact framework table in each rule; move only the why-section and extended worked examples), tranches 2-12 HELD.** The default is reversible and safe (it preserves distributed traceability). Tranche 1 is applied on it. But tranche 1 SETS THE PATTERN the other 12 rules replicate, so the remaining tranches are held rather than mass-applied unattended on an unconfirmed design default. **Maintainer action:** confirm the framework-tables-KEPT direction (then tranches 2-12 proceed on it), OR redirect (if framework tables should be cut too, they must land somewhere the pack DISTRIBUTES, e.g. a new pack-README alignment matrix, in the same change, not only the ledger, which reshapes the whole GR-P2 series). The tranche-2/3/4 worker deliveries exist in `grc_library_scratch` (`worker/gr-p2-tranche{2,3,4}`) and apply once the direction is confirmed. Also folded into GR-P2 (per overnight authorization #3): the pack-wide `LOG-02, LOG-08` -> `LOG-02, LOG-04, LOG-10` audit-trail-row migration and the apex-rule `A.8.34` -> `A.5.33` swap (deferred from the 3.21 PR).

### eIDAS2 annex naming + title: stricter-safe default APPLIED (2026-07-09 unattended run)

The eIDAS2 §5.7 apply (#739) created a NEW public-sector annex, which carries a durable naming choice the worker surfaced (file name; and "eIDAS" vs "eIDAS2" in the Document Title). **Disposition: stricter-safe default APPLIED (unattended), recorded for confirm-or-redirect.** File name `compliance/public-sector/annex-eidas-requirements.md` follows the FedRAMP sibling convention (`annex-<regime>-requirements.md`) exactly, the strongest available precedent for a public-sector regime annex. Document Title "eIDAS Sector Requirements Annex" names the framework family (mirroring how the FedRAMP annex titles the programme not a revision), with the amending regulation (2024/1183, eIDAS2) stated in the body and the Framework-alignment row. Low-stakes and reversible on-branch pre-merge; once merged a rename is a follow-up. **Maintainer action:** confirm the name/title, or redirect (e.g. to `annex-eidas2-requirements.md` / "eIDAS2 ..." if the version-precise form is preferred).

### FR-154 operational-vagueness cluster: 5 stricter-safe values PROCEEDED, sub-item 5 DEFERRED (2026-07-09 unattended run)

The FR-154 operational-vagueness cluster (TODO §2.9) was applied in #738 under the maintainer's orchestrator-sets-stricter-safe decision model. **Proceeded (stricter-safe, reversible, on-branch; confirm or redirect on resume):** sub-item 1 (DSR forward clock harmonized to "immediately on receipt, and in any event no later than the same business day"; internal-consistency + GDPR Art 12(3)); sub-item 3 (Critical-risk interim containment authority named: risk owner + CISO or domain executive, pending ERC; formal acceptance stays with the Executive Committee or Board; per the role-authority register and the exception policy); sub-item 4 (supplier remediation gate made tier/severity-conditioned: a Tier 1 supplier or any critical-severity deficiency requires remediation completed-and-verified before engagement, lower tiers proceed on a plan-with-deadline; per the tiering rubric); sub-item 6 (dept-continuity template derivation guidance + one illustrative row: MTD/RTO/RPO derived from the BIA, MTD >= RTO, per ISO 22301); sub-item 8 (model-lifecycle recorded-threshold obligation: metric, floor, owner recorded in the AI System Inventory at deployment, no threshold no production; per ISO/IEC 42001 Section 9). Each is anchored to an internal canonical source (not an authorial invention), so the confirm-or-redirect is low-stakes. **Deferred-blocked (needs maintainer / a held source):** sub-item 5 (whistleblower investigation feedback ceiling), the recommended 3-month value rests on the EU Whistleblower Directive 2019/1937 Article 9, which is NOT held in `grc_library_ref` and not upstream-confirmable this turn (EUR-Lex unfetchable); per the accuracy rule the unverifiable value was NOT asserted. **Maintainer action:** confirm or redirect the 5 proceeded values; and for sub-item 5, confirm the 3-month feedback ceiling and supply a held anchor (the EU Whistleblower Directive text, or ISO 37002 whistleblowing-management guidance), or redirect. TODO §2.9 stays OPEN for sub-item 5.

### FR-59 privacy-jurisdiction annex deepenings: DEFERRED-BLOCKED, egress-gated (2026-07-09 overnight run)

The 2026-07-09 overnight backlog run reached TODO 2.1 (FR-59, the privacy-jurisdiction annex operational deepenings; the first P2 research-authoring item). The Fable `worker-20260703-a` `fr-59-privacy-jurisdiction` delivery is a RESEARCH delivery (gap analysis + cited edit shapes for Japan / US / Canada / Brazil / the Latin-America members), and its own MANIFEST currency section states: **"All cited provisions are currency-UNCONFIRMED as of this delivery. The orchestrator must confirm each relied-upon source is current upstream before the citation lands."** Two concrete signals: the held Japan APPI text is the June-2020 amended-Act translation whose article numbers were renumbered by the 2021 consolidation (cite the obligation, confirm the number upstream); and the Mexico entry is STALE (the corpus describes the 2010 LFPDPPP/INAI, but the held text is the new LFPDPPP DOF 2025-03-20). Landing version-specific statutory citations without upstream confirmation violates the accuracy rule (version-specific / time-sensitive claims need a primary source verified this turn).

**Disposition: DEFERRED-BLOCKED (egress-gated), routed around per the overnight graceful-degradation mechanism.** This is the same egress-gated class the maintainer has standing-deferred (1.5's needs-reconfirm rows, 1.11 Brazil, FR-70 "STAY DEFERRED pending the maintainer's egress instance"), so no safe overnight default exists for landing the statutory citations; the run routed to the next non-egress-gated item (TODO 3.21, shipped). **The Mexico staleness is the highest-value piece** (the corpus currently asserts a repealed regime) and is noted here so it is not lost; its correct fix to the held 2025 text also wants upstream currency confirmation. **Maintainer action:** on an egress-capable instance, confirm the upstream statutory currency (Japan e-Gov consolidated APPI article numbering; Mexico DOF LFPDPPP 2025 in-force text) so the FR-59 deepenings (and the sibling FR-60 / FR-74 / FR-154 / FR-41 jurisdiction items, which likely share the constraint) can be authored with confirmed citations. The FR-59 research delivery stays staged in scratch; the held-source-deepenings and source-gated-gaps analyses are ready to apply once currency is confirmed.


### Changelog-restructure PR 2 (sweep-to-scratch): how to reconcile the archive's maintainer-watermark tokens with scratch's privacy gate (2026-07-08 overnight; RESOLVED 2026-07-08 to Option A, EXECUTED in scratch PR #113 + grc_library #708)

**RESOLUTION (2026-07-08).** The maintainer chose **Option A** ("A per your recommendation"): exempt the frozen `archive/` from scratch's Check 5 (watermark PII) and Check 6 (house style), on the frozen-archive principle. EXECUTED: scratch PR #113 added the `archive/` exemption to `grc_library_scratch/tools/validate.py` and landed the archive (647 detailed entries across 5 weekly files + 204 records, 209 files, lossless 647 == 647; the exemption confirmed load-bearing, the archive carrying incidental `/home/jposluns/` paths and 4 historical-em-dash files); grc_library #708 then ran `--prune --verify-archived` (mirror pruned to the current week, now 42 entries with this PR's #708, #667 through #708; 204 records removed, re-parse assertion passed, 66/66 post-prune). The original blocker analysis is retained below for the record.


**Context.** PR 1 (the machinery: gate-59 dynamic cutoff + [`tools/sweep-working-records-to-scratch.py`](../../tools/sweep-working-records-to-scratch.py) + `.gitattributes export-ignore` + model docs) merged as #695. PR 2 is the initial sweep: move the completed-week detailed-CHANGELOG entries (647) plus the pre-current-week dated per-run records (204) into a `grc_library_scratch/archive/` of weekly Monday-dated files, then prune them from grc_library (the in-repo mirror drops to the current week only). The sweep tool is built and verified data-safe end to end (676 == 676 lossless round-trip; a `--prune` re-parse assertion; an archive-verify guard).

**The blocker (found 2026-07-08).** Staging the archive in scratch trips scratch's own `tools/validate.py`:
- **Check 5 (watermark PII):** the archive carries the maintainer-watermark token `jposluns` (in `archive/changelog-details/2026-06-01_detailed.md`, and likely across the 204 records as `/home/jposluns/...` local paths and `github.com/jposluns/...` URLs quoted in historical entries). `jposluns` is fine IN grc_library (it is the public repo owner) but scratch's privacy gate blocks it by design, to keep maintainer identity out of the worker-exchange repo (the same identity-minimization intent as the #693 cleanup).
- **Check 6 (house style):** 11 dash + 115 `-ise` / bare-`ensure` / heading-case findings in the frozen historical prose (predates various gates).

**Why the assistant deferred rather than decided.** Resolving this means one of: (a) EXEMPT scratch's Check 5 + Check 6 for the frozen `archive/` (weakens a privacy gate the maintainer built, on identity content they personally scrubbed in #693; a gate-discipline call that is the maintainer's, not the assistant's); (b) SCRUB `jposluns` and the style across the roughly 207 historical files (mutates the historical record; a new genericization pass in the sensitive identity area without explicit direction); or (c) RELOCATE the archive somewhere other than scratch (changes the maintainer's chosen design). All three are authorial decisions in the identity-cleanup area the maintainer directed in #693, so per gate-discipline and the overnight err-on-caution directive the assistant did not force it.

**Options (recommendation first):**
- **A (recommended): exempt the frozen `archive/` from scratch's Check 5 + Check 6** (add `archive/` to scratch `validate.py`'s exempt set), on the frozen-archive principle (an imported record, not authored scratch prose, exactly as grc_library exempts `.working/` from its corpus gates); `jposluns` is already public in grc_library, so scratch-side exemption adds no new exposure. Lowest effort, no history mutation.
- **B: scrub `jposluns` plus the 11 dash / 115 style in the archive before committing to scratch**, aligning with the #693 identity-minimization intent, at the cost of mutating the historical text across roughly 207 files.
- **C: keep the detailed history and records in grc_library** (chunked in-repo, not swept to scratch), relying on `export-ignore` plus git history for fork-cleanliness; this reconsiders the sweep-to-scratch design itself.

**State (at the time of the block; SUPERSEDED by the RESOLUTION above).** grc_library `main` was UNCHANGED (no prune had run); scratch was clean (the trial archive removed); nothing committed. The sweep tool and the dynamic gate were live (PR 1). PR 2 waited on this decision, now resolved to Option A and executed in scratch #113 + grc_library #708.

### Changelog-restructure PR 3 (root reformat + 1-2-sentence compression): deferred as a wide sensitive change, recommend attended / fresh-context (2026-07-08 overnight; NOT blocked, prudentially deferred)

The maintainer-directed root reformat to the compact `**date | version | PR** - summary` form plus compressing the ~679 historical root entries to 1-2 sentences is a WIDE change: it de-headings every root entry (dropping the `## ` headers), which requires updating gate 59's root header parser (and any other root-`## `-parser) in lockstep, plus a scripted reformat and then a 679-entry judgment compression of the historical record. Per the 2026-07-08 "err on the side of caution and integrity" directive, the assistant did not force a 679-entry historical-record rewrite plus a multi-gate structural change unattended this deep in a long session. Recommended: do it as a fresh, focused effort (or with explicit go-ahead for the batched, fidelity-verified approach). Not blocked by the PR 2 decision (root format is independent of the detailed-mirror location). Tracked as TODO 3.19 PR 3+.

### TODO 1.9 PreToolUse-hook-firing: close as harness limitation, or track a harness-feedback filing (2026-07-06 overnight investigation; root cause pinned; maintainer disposition) [RESOLVED 2026-07-06: disposition A, close as documented harness limitation; TODO 1.9 CLOSED and rotated to DONE in #677, the limitation recorded in `third-party-issues.md`; retained here with its resolution until rotated]

The maintainer overrode accept-and-close on 2026-07-05 and directed pursuing "why the PreToolUse hook does not fire in resumed sessions." The 2026-07-06 overnight read-only investigation PINNED the root cause: in a resumed/child session (`CLAUDE_CODE_CHILD_SESSION` set) `CLAUDE_PROJECT_DIR` is UNSET, so the `.claude/settings.json` hook command `python3 "$CLAUDE_PROJECT_DIR"/.claude/hooks/block-verification-pipes.py` resolves to the nonexistent `/.claude/hooks/block-verification-pipes.py`, `python3` cannot open it, and the Bash tool proceeds unblocked. There is NO portable corpus-side fallback (in this environment `$PWD` defaults to `/home/user` not the project root; no other `CLAUDE_*` var carries the project path; `git rev-parse --show-toplevel` fails from `/home/user`), so the fix is harness-level: set `CLAUDE_PROJECT_DIR` (or run hooks with cwd = project root) for child sessions. The hook script itself is correct (`--self-test` passes: 14 blocked, 17 allowed). **Disposition (maintainer):** (A) close 1.9 now that the root cause is documented, accepting it as a harness limitation, since the compensating controls (the RM-10 unpiped-verification habit and the guard's `PRE_PUSH_GUARD_ALLOW_PIPE`-gated pipe self-defence) fully cover the underlying risk; or (B) keep 1.9 open tracking a harness-feedback filing ("`CLAUDE_PROJECT_DIR` should be set in child/resumed/SDK sessions"). Recommended: (A) with the finding recorded; a corpus settings.json change is NOT staged because no robust portable fallback exists in this environment. 1.9 is NOT closed pending this decision (the maintainer overrode accept-and-close once already).

### Sweep 84 A-F1: verify the Brazil ANPD-restructuring citations against a primary source (2026-07-05 `/resume`; note-level; RESOLVED 2026-07-05 daytime round: annotate-as-unverified-now, and TODO 1.11 created in #652 to track the verification; ANNOTATION EXECUTED in #662, verification tracked by TODO 1.11)

Sweep 84's Subagent A flagged [`privacy/jurisdictions/annex-privacy-brazil.md`](../privacy/jurisdictions/annex-privacy-brazil.md):29 (added by #631): it asserts the Brazilian DPA was "Restructured as the Agência Nacional de Proteção de Dados, a regulatory agency (agência reguladora), by Lei No. 15.352/2026 (converting Medida Provisória No. 1.317/2025, which amended LGPD Articles 5 and 55-A)", and the paired breach-response matrix [`privacy/procedure-data-protection-and-privacy-breach-response.md`](../privacy/procedure-data-protection-and-privacy-breach-response.md):177 cites "3 business days ... per Resolution CD/ANPD No. 15/2024". These are internally CONSISTENT corpus-wide and NOT refuted, but they are 2026-dated statutory designations past the reference base's LGPD (2018); the reference base does not hold the 2026 amendment, so neither the orchestrator nor any gate can verify them, and the accuracy rules forbid asserting version-specific / time-sensitive claims without a primary source. **NOT edited** (no evidence it is wrong; changing it would be guessing). **Maintainer action:** confirm the four designations (Lei 15.352/2026; MP 1.317/2025; LGPD Art. 55-A; Resolution CD/ANPD No. 15/2024's 3-business-day figure) against a primary source (planalto.gov.br / the ANPD site); the #631 held-source-batch delivery record should carry the provenance. If a discrepancy is found, correct the annex and the breach-response matrix row together; if confirmed, no edit. **Provenance concern to check:** the reference base's Brazil source is LGPD 2018, so the 2026-restructuring claim was NOT authored from that text (it came from the worker's training knowledge or an unrecorded source), which is exactly the currency class the corpus guards against.

### TODO 3.1 FIT-8: register-cell ISO-form harmonization direction (2026-07-05, #650 scoping; RESOLVED 2026-07-05 daytime round: option B coherence-preserving; SUPERSEDED 2026-07-05 by the maintainer's accuracy-principle redirect and EXECUTED in #663)

B3 directed "harmonize all short-form ISO 27001 headers/cells to `ISO/IEC 27001:2022` incl. the 11 FR-167 matrix rows + FIT-8 register cells." The **individual-document framework-alignment-table headers are directed and unambiguous** (harmonized to the full `ISO/IEC` form, shipped in #650); the **matrix headers**, initially in scope, turned out to be gate-49-coupled and are separately deferred (the companion entry below). The **FIT-8 register cells are a genuine conflict** surfaced during scoping: the register [`register-document-index-and-classification.md`](register-document-index-and-classification.md) Frameworks column is about 90 percent BARE short forms (`ISO 27001`, `ISO 42001 §8`, `ISO 27005:2022`), with a `/IEC` minority (mostly the OT rows 267-271, plus scattered rows 140/274). FIT-8 flagged row 274 (`ISO 42001 §8, ISO/IEC 42005:2025`, internally MIXED) and row 225 (`ISO 42001 §8, ISO 27001, ISO 23247`, all bare and already matching the dominant convention). **Harmonizing just these 2 cells to full `/IEC` form (literal B3) would make them inconsistent with 90-plus bare sibling cells** in the same column, a larger inconsistency than it fixes, and the silent scope expansion onto the register that TODO 3.1 explicitly warns against. **Options (recommended first): (B)** coherence-preserving: fix row 274's internal MIX by matching the register's dominant bare convention (`ISO/IEC 42005:2025` to `ISO 42005:2025`), leave row 225 (already bare-consistent); minimal, resolves the flagged internal inconsistency, keeps the column coherent. **(A)** literal B3: both cells to full `/IEC` form (row 274 to both `/IEC`; row 225 to `ISO/IEC 27001:2022`, `ISO/IEC 42001`, `ISO/IEC 23247`); resolves row 274 but leaves the 2 cells inconsistent with 90-plus bare siblings, and needs the correct `/IEC` plus edition verified for 42001/23247. **(C)** register-wide: harmonize the WHOLE Frameworks column to one form (large scope, TODO 3.1 warns against). **(D)** leave FIT-8 (accept the register's mixed convention as standing style). Resolved 2026-07-05 to option B (coherence-preserving: fix row 274's internal MIX by matching the register's dominant bare convention, leave row 225); the individual-document header harmonization (#650) is independent of this and shipped. **Note:** B3 was given against the TODO 3.1 census framing; the register's dominant-bare-convention detail (which makes literal B3 counterproductive on these 2 cells) is the specific evidence surfaced here.

**SUPERSEDED and EXECUTED (2026-07-05, #663).** The maintainer's later accuracy-principle redirect ("if a standard is joint ISO/IEC it should be listed as joint; if it is only from one, list only the one; accuracy is critical to the integrity of the project") reframed this from a form-consistency question into an accuracy requirement, and directed option A for a corpus-wide sweep of the base-verified joint standards. #663 executed that (232 `ISO NNNNN` to `ISO/IEC NNNNN` conversions across 31 files, the 13 joint standards: 27001, 27002, 27004, 27005, 27017, 27018, 27019, 27035, 27036, 27701, 42001, 23894, 20000). This overrides the option-B "keep the dominant bare convention" resolution: option B's own objection (harmonizing 2 cells to `/IEC` would make them inconsistent with about 90 bare siblings) is dissolved, because the sweep converted the bare joint-ISO siblings across the whole Frameworks column, so the column is now uniformly `/IEC` for the base-verified joint standards. Register row 274 is now internally consistent and accurate (`ISO/IEC 42001 §8, ISO/IEC 42005:2025`, both genuinely joint); row 225's 42001 and 27001 are now `/IEC`, with `ISO 23247` left bare because it is unverified (tracked in the TODO 3.1 gap item / task #9). This entry is closed.

### TODO 3.1 compliance-matrix ISO column headers: gate-49 machine-key coupling (2026-07-05, #650 scoping; RESOLVED 2026-07-05 daytime round: option A coupled lockstep PR; EXECUTED in #664)

B3 directed harmonizing "the 11 FR-167 matrix rows" ISO column header from `ISO 27001:2022` to `ISO/IEC 27001:2022`. Scoping (and an independent pre-push verifier probe) found this is NOT a display-only change: gate 49 ([`tools/lint-matrix-control-codes.py`](../tools/lint-matrix-control-codes.py)) identifies the matrix ISO column by the exact constant `ISO_HEADER = "ISO 27001:2022"` (line 268, `if ISO_HEADER in cells`), so renaming the header makes gate 49 **silently skip the entire matrix's control-code validation** (the single header-detection block at `lint-matrix-control-codes.py:268-273` sets all four column indices only when `ISO_HEADER in cells and NIST_HEADER in cells`, so a renamed ISO header leaves every index unset and the `if iso_idx is None: continue` guard then skips every matrix body row: gate 49 returns exit 0 having validated none of the four columns, so the pre-push guard would NOT catch the coverage loss), and the gate-49 regression fixtures in [`tests/test_linters.py`](../tests/test_linters.py) construct matrix samples whose ISO assertions depend on the bare header string. Harmonizing the matrix header therefore requires a **lockstep gate-49 update**: the `ISO_HEADER` constant, the regression fixtures (distinguishing gate-49 matrix fixtures from gate-58 per-document fixtures, whose `ISO(?:/IEC)?` regex already matches both forms and must NOT be conflated), the gate-49 docstring, and the audit-programme spec ([`governance/specification-audit-programme.md`](../governance/specification-audit-programme.md) line 68, "the ISO 27001:2022 column tokens"). This is a gate-detection-input change with a silent-coverage-loss failure mode, deserving its OWN focused, carefully-verified PR (build the gate-49 lockstep change alongside the matrix rename, re-run the regression suite, prove gate 49 still catches an injected invalid ISO code post-rename). **Options (recommended first): (A)** build the coupled change as a dedicated PR (rename the 11 matrix headers + legend cell AND update `ISO_HEADER` to `ISO/IEC 27001:2022` + the fixtures + docstring + spec line 68, in lockstep). **(B)** make gate 49's `ISO_HEADER` a regex matching both forms (like gate 58) instead of a rename, so the column label can be harmonized without a constant flip (larger gate-code change). **(C)** leave the matrix column header as the compact `ISO 27001:2022` machine-key label (it parallels the matrix's other compact column labels `NIST CSF 2.0` / `CSA CCM v4.1`, and the legend already maps it to the full `ISO/IEC 27001:2022`), i.e. do not harmonize the matrix header. NOT harmonized in #650 (the individual-document headers shipped; the matrix was reverted on discovering the coupling). B3 was given as a display-harmonization directive and likely did not anticipate the gate-49 machine-key coupling; that is the specific evidence surfaced here.

### Scratch `originals/README.md` stale delivery snapshot: refresh vs keep-as-dated-snapshot (2026-07-05, found during the SR-4 re-scoping; RESOLVED 2026-07-05 daytime round: option A refresh-to-live-manifest; execution pending in a scratch PR)

Closing SR-4 item 31 (the publications count corrections, scratch PR #104) surfaced a larger, separately-scoped staleness the 2026-07-02 audit did not capture: the reference-base publications manifest (`grc_library_scratch`) is a frozen **2026-06-27 delivery snapshot** making false present-tense claims (reference-base contents tracked separately in the reference repo). It states publications are pending that are in fact present, and its manifest table is stale against current disk. The reference-base README's dated seeding-status section carries the same stale snapshot in a milder, explicitly-dated form. **NOT fixed** (the fix direction is a judgment about the file's role). **Options (recommended first): (A)** refresh the reference-base publications manifest to a LIVE state: rebuild it against current disk and correct the present-tense status; leave `ref/README.md`'s dated seeding section as a timestamped log with a "since superseded" pointer. **(B)** keep `originals/README.md` as a DATED delivery snapshot: retitle/date its status so "22 ... not yet produced" reads as a historical 2026-06-27 record, not a present-tense claim; do not rebuild the table. **(C)** leave both as-is (accept the staleness). A rebuild is a roughly 22-row table reconstruction (the escape-prone class), so it wants a focused, verified scratch PR once the direction is set; ships via MCP PR, self-merge on green per the SR-wave authorization.

The four entries above are RESOLVED (2026-07-05 daytime round; see the Status line and each entry's inline marker), so no decision remains open. Three have now been EXECUTED: the Brazil annotation (#662), the FIT-8 register-cell direction (#663, via the maintainer's superseding accuracy-principle redirect), and the compliance-matrix gate-49-coupled lockstep (#664). One remains awaiting execution and is tracked as a forward item: the scratch `originals/README.md` live-manifest refresh. Each is retained here with its resolution until executed and rotated. The four 2026-07-04 morning-boundary items below were RESOLVED at the maintainer's same-day morning rounds (resolutions in the Status line above and inline per entry); retained with their resolutions for the audit trail. The three 2026-07-03 entries further down were resolved at the maintainer's 2026-07-03 morning round; retained with their resolutions for the audit trail.

### GR-8(b) disposition-token vocabulary and WATCH holding state (2026-07-04 overnight, PR #628; proceeded default; RESOLVED 2026-07-04 morning round 1: maintainer-CONFIRMED as shipped)

The GR-8(b) closure build shipped the register's `## Disposition convention` (tokens `CODIFIED in <carrier>` / `ROUTED to <destination>` / `WATCH (fires on <n>th occurrence)` / `REJECTED (<reason>)` / `EXPIRED (<date>)`; a non-empty cell with no token is pending) and `/retro` step 6 (the disposition scan plus the carried-candidates check) as a convention default rather than waiting on a morning design round: the vocabulary is additive (no row's original text is rewritten; tokens append), forward-looking (no retroactive bulk edits), and reversible (delete the section and the appended tokens). Rejection and expiry remain maintainer calls by the convention's own text. Redirect if you want different token names, a different holding-state shape, or retroactive tokenization of the historical rows.

### GR-8(b) expiry tail: aged pending candidates (2026-07-04; RESOLVED 2026-07-04 morning round 1: option A, the one-pass batch review; triage draft in progress)

The register survey found aged pending candidates predating the current gate/checklist machinery, headline cases the #233 and #235 rows and the habit-band rows (candidates proposing habits the checklist has since absorbed or superseded). Per the convention, `EXPIRED`/`REJECTED` are maintainer calls: I propose EXPIRED for candidates whose target machinery has since shipped in a different form, but none is tokenized until you disposition them. Options: (A, recommended) I produce a one-pass list of every pending pre-2026-07 candidate with a proposed disposition each for your batch review; (B) leave the tail pending and disposition opportunistically at each `/retro` step-6 scan.

### Option B: advisory register-ageing tool (2026-07-04; RESOLVED 2026-07-04 morning round 1: BUILD; TODO GR-8 follow-on A)

The GR-8(b) design round's option B is a small advisory tool (not a gate) reporting pending Proposed-improvement cells older than N PRs, the register-side analogue of `audit-brief-freshness.py`. WATCH-state candidates make ageing visible without it, but only at `/retro` scans. Scope it for the build queue, or drop it as redundant with the step-6 scan.

### Supply-chain matrix WCO SAFE cell + legend repair (2026-07-04 section-3.1 sweep; proceeded default; RESOLVED 2026-07-04 morning round 1: maintainer-CONFIRMED as shipped)

The section-3.1 stray-punctuation sweep found [`../supply-chain/matrix-supply-chain-security-programme-alignment.md`](../supply-chain/matrix-supply-chain-security-programme-alignment.md) carrying TWO instances of the mangled-glyph class in one table: the Financial Solvency row's WCO SAFE cell held a lone `:`, and the legend's third marker definition was itself mangled (`;: = not a primary requirement`), so the author's intended third-state glyph is unrecoverable from history (present since the initial public-release commit). Proceeded with: (1) the CELL set to `✓`, the source-supported value (the WCO SAFE Framework, section "C. Financial Viability": the AEO "is required to be in good financial standing which is sufficient to fulfil its commitments considering the specific characteristics of its business model and activity"), consistent with the AEO-S and AEO (EU) columns whose criteria derive from SAFE; and (2) the LEGEND's vestigial third state replaced with a definition of `N/A` ("not addressed by this programme's published criteria"), which the table's cells actually use and the legend previously left undefined; after (1), no cell carries a "not a primary requirement" state. Reversible on-branch history; redirect if the third state should instead be restored with a new marker (that would also mean re-adjudicating the cell to that marker rather than `✓`).

## Resolved at the maintainer's 2026-07-03 morning round

The maintainer returned after the #607 merge (the FR-48 series-completing PR), confirmed the mode switch to daytime-unattended, and answered two `AskUserQuestion` rounds covering the three held entries plus seven queue-de-blocking directives:

1. **DPO sweep four proceeded defaults: CONFIRMED as shipped** (all four, including the breach-notification authority extension; see the entry below).
2. **CLAUDE.md D5 form-description refresh: AUTHORIZED** (see the entry below); ships in the D5 PR together with item 3 and the seventh-form widening (the #607 retro's space-separated `section N.M ... closed` variant, which D5's six forms missed on #607 itself).
3. **RM-10 unpiped-guard codification: AUTHORIZED** (see the entry below); same D5 PR.
4. **P2 operational values (sections 2.9, 2.4, 2.5, 2.1, 2.2): the assistant sets stricter-safe, canonical-source-supported values with logged proceeded entries** for later confirm-or-redirect; no per-document decision round.
5. **Scratch SR wave (SR-1..SR-5): self-merge on green** (scratch validate CI), as with scratch PRs #47/#48; the 2026-07-02 defaults (SR-1 presence+backfill, SR-4 drop-failed-extract, SR-5 re-bucket) stand.
6. **Protected-tree factual-staleness one-liners: authorized as a class** (e.g. the GR-9 `.claude/commands/resume.md` scratch-bucket fix); each instance named in its PR's CHANGELOG entry; substantive convention changes still surface individually.
7. **TODO section-3.10(c): KEEP all three sections in TODO** (`## Standing conventions`, `## Backlog totals`, `## Priority 7`); section 3.10 closes as keep-by-design.
8. **Section 2.5 (FR-15 maturity ladder): methodology doc + scoring unchanged** (documents the aggregation and its outlier-masking limitation; no generator change).
9. **Section 3.8 (orchestrator-token instrumentation): CLOSED accepting `not instrumented` as the permanent state**; revisit only if the harness surfaces main-loop usage.
10. **FR-70, the 51 needs-reconfirm register rows, and the ISO-31000 refresh: STAY DEFERRED** pending the maintainer's egress instance.

### CLAUDE.md D5 form-description refresh (surfaced 2026-07-03, #595 sweep L-1, overnight-deferred, protected tree; RESOLVED 2026-07-03: AUTHORIZED at the morning round, ships in the D5 PR)

The PR-workflow step-7 D5 description in `.claude/CLAUDE.md` under-describes the widened forms 3 and 6 after #595: form 3 also matches `TODO item` and `bullet(s)` with a decimal-dot-tolerant clause run, and form 6 also matches the guarded short `rotated to DONE` (negations excluded). Factual staleness fix, one clause each; needs maintainer authorization for the protected file. The gate-scanned sibling carrier (the audit spec's section-6.1 D5 narrative) was fixed in #596.

### DPO sweep: four proceeded defaults (2026-07-03 overnight, PR #590; RESOLVED 2026-07-03: maintainer-CONFIRMED all four at the morning round, no redirect)

The maintainer-directed section-1.3 DPO role-separation sweep resolved four judgment points with canonical-source-supported or stricter-safe defaults, each reversible on-branch history: (1) **charter interim block reframed, not deleted** ([`../privacy/charter-privacy-management-programme.md`](../privacy/charter-privacy-management-programme.md)): the Interim-Accountability note and the CIO-as-DPO conflict subsection became generic adopter guidance ("where an adopter operates an interim arrangement..."), preserving the WP243-sourced mitigation table and the canonical-citations anchor while removing the composite as the library's own model. (2) **Breach-notification authority moved to the DPO** ([`../privacy/procedure-data-protection-and-privacy-breach-response.md`](../privacy/procedure-data-protection-and-privacy-breach-response.md)): the DPO decides and submits supervisory-authority notifications with Legal Counsel content review, the CIO retains programme-level executive accountability and P1/P2 notification; supported by the charter's own Article 38(3) row (management cannot direct the DPO's breach-notification decisions case-by-case). The maintainer's "DPO advises, EC accepts" answer covered PIA risk acceptance, not breach notification, so this is the one default that extends the answer's principle to an adjacent authority; redirect if the CIO-approval model was intended. (3) **PIA internal pathway rewired to Executive Committee acceptance** ([`../privacy/procedure-privacy-impact-and-cross-border-transfer.md`](../privacy/procedure-privacy-impact-and-cross-border-transfer.md)): per the answer and the canonical chain (exception policy: EC/BRC for High or Critical), with the ERC narrowed to its role-register oversight function; the two GRC-programme sibling carriers aligned in the same pass. (4) **Owner metadata reconciled to the document index** (five core privacy documents now Owner: Data Protection Officer, matching [`../governance/register-document-index-and-classification.md`](../governance/register-document-index-and-classification.md)).

### RM-10 codification: unpiped-guard convention line in CLAUDE.md (surfaced 2026-07-03, overnight-deferred, protected tree; RESOLVED 2026-07-03: AUTHORIZED at the morning round, ships in the D5 PR; the RM-10 ledger entry dispositioned when it lands)

The GR-8(a) disposition pass found RM-10's watch-signal appeared in variant form: two pushes went past a FAILING pre-push guard via pipe-masked exit codes (#569, #583; both self-caught before CI harm). The corrective convention (run the guard standalone and unpiped, read its exit before the chained push) lives only in those retro rows, not in CLAUDE.md's PR-workflow steps. Proposed morning decision: add one sentence to PR-workflow step 1 or 2 codifying it. Protected-tree edit, so deferred per the overnight rules; the RM-10 ledger entry stays open pending this decision.

### Cross-file section-ref gate: sequencing vs FR-48 (surfaced + RESOLVED 2026-07-02, maintainer chose option B: complete FR-48 first, then build/ship the gate)

**RESOLVED 2026-07-02 (maintainer, attended): pause the gate, complete FR-48 §1.1, then resume the gate so it validates against a fully-numbered corpus.** Rationale (assistant-assessed, maintainer-agreed): (1) the numbers-phase gate can only check refs to *numbered* targets, so shipping it mid-migration would silently skip refs into the not-yet-renumbered deferred docs (incomplete coverage that looks green); (2) the one blocking finding resolves for free in the FR-48 lockstep renumber (no throwaway standalone citer fix); (3) building against the finished corpus avoids locking the design to a transitional state. The gate is already BUILT and TESTED (low-FP after fixing two FP classes); its design, survey, and FP lessons are preserved in [`cross-file-section-ref-gate-design.md`](cross-file-section-ref-gate-design.md), and the unwired draft file was removed from `tools/` so it does not leak into FR-48 PRs. During FR-48 the preserved gate may be run ADVISORY-ly after each renumber to mechanically catch the #548-class cross-file remap miss; it is wired as CI-blocking only once FR-48 completes on a clean corpus.

The one finding it surfaced (retained for the record): [`risk/procedure-risk-acceptance.md`](../risk/procedure-risk-acceptance.md):41 cites `[policy-exception-and-risk-acceptance-management.md](...) §2.2`, but that policy has no `§2.2` (unnumbered H2s, flat H3s that reset per H2). The policy is on the FR-48 §1.1 deferred hard-class worklist; this inbound `§N` citer resolves in that policy's lockstep renumber. Not a standalone action.

### Overnight EPUB-untrack authorization SUPERSEDED before execution (2026-07-02, maintainer, attended: keep tracked)

The 2026-07-02 overnight authorization on the former TODO 7.5 (the then-direction to untrack certain reference sources and queue history-purge instructions) was SUPERSEDED the same morning, before any part of it executed: after a maintainer-requested walkthrough of the gitignore mechanics and the actual exposure model, the maintainer decided the reference-base sources REMAIN TRACKED, under the private-forever promise, with exposure controls unchanged. The full decision record with its three conditions is in [`design-decisions.md`](design-decisions.md); the former TODO 7.5 rotated to DONE. The history-purge instruction queue is dropped as moot. Nothing is pending here; recorded so the superseded overnight authorization is never re-executed from a stale read.

_The 2026-07-01 entries below were resolved at the 2026-07-01 `/resume`; retained for the audit trail with their resolutions. The MITRE ATLAS scratch-archival (also below) was resolved earlier the same day._

### §3.2 detailed-CHANGELOG-mirror header-parity gap (surfaced 2026-07-01; RESOLVED 2026-07-01 at `/resume`: maintainer chose the cutoff-scoped gate)

**RESOLVED 2026-07-01 (maintainer, at `/resume`): cutoff-scoped gate** (the recommended option). Accept the 3 missing pre-split-era headers (#268/#353/#462) as a known exemption and scope §3.2's parity gate to compare the detailed-mirror `##`-header set against root [`CHANGELOG.md`](../CHANGELOG.md)'s only from a cutoff PR forward. FP-free by construction, no retroactive audit-trail edit, and it still prevents future drift (the gate's whole purpose). The gate build is now **P3 tooling** work, scheduled after the P1 FR-48 rename. The original deferred-state analysis is retained below for the record.

While scoping TODO §3.2 (a gate asserting the detailed CHANGELOG mirror's per-PR `##`-header set equals root [`CHANGELOG.md`](../CHANGELOG.md)'s), a diff of the two header sets found the detailed mirror [`.working/changelog-details/CHANGELOG-detailed.md`](changelog-details/CHANGELOG-detailed.md) is **missing 3 per-PR headers that exist in root**: **#268** (2026.06.246; a session-closing handoff PR), **#353** (2026.06.332; the `.working/` em-dash conformance PR), and **#462** (2026.06.440; the gate-55 PR). Root carries 465 `## ...PR #N` headers; the mirror carries 462; the delta is exactly these three. Each PR's content is REFERENCED elsewhere in the mirror (e.g. #268 as a handoff, #353/#462 inside later entries' bodies), but none has its own `##` header/entry. This is the exact audit-trail-integrity class §3.2's proposed gate targets, so **the gate cannot ship green until the gap is remediated** (a naive equality gate would fail immediately).

**Why deferred (needs maintainer): the remediation retro-touches the CHANGELOG audit trail and is a judgment call.** Options (recommended first): **(C) accept the 3 as a known pre-`#125`-split-era exemption and scope §3.2's gate to compare only from a cutoff PR forward** (FP-free by construction, no retroactive audit-trail edit; the gate still prevents FUTURE drift, which is its whole purpose); **(A) reconstruct** the 3 missing detailed entries from each PR's root summary + diff (full parity, but #268 was a handoff PR whose detailed entry may have been intentionally terse/absent, and reconstructing dated historical entries edges toward the "retroactive entry" the change-tracking rule cautions about); **(B) stub-with-pointer** entry for each ("full detail in root `CHANGELOG.md`; mirror header restored 2026-07-01"). **Not an adopter-facing defect** (root `CHANGELOG.md`, the adopter surface, is complete); it is a maintainer-working-state mirror gap. Route-around applied 2026-07-01: §3.2 deferred, the session continued with other input-free P3 work. Resolve on `/resume`, then §3.2's gate can be built to match the chosen remediation.

### §3.3 citation-verification-pass recording model (surfaced 2026-07-01; RESOLVED 2026-07-01 at `/resume`: maintainer chose the consistency cross-check, no register row)

**RESOLVED 2026-07-01 (maintainer, at `/resume`): consistency cross-check, no register row** (the recommended option B). §3.3 performs a register-vs-held-extract **consistency cross-check** (register version+date vs the held `ref/` Tier-1 extract), surfacing and fixing any MISMATCH but recording **no** §9 register row when the two are consistent. The register's §9 human-primary-capture trust model is left unchanged (no new AI-local-extract row class), so the original "record in the Citation Verifications Register" deliverable is dropped. The cross-check is now **P3 tooling** work, scheduled after the P1 FR-48 rename. The original deferred-state analysis is retained below for the record.

TODO §3.3 asks to run a citation-verification pass confirming the register's version+date rows against the reference-base extracts and **record the results in the Citation Verifications Register** ([`.project-governance/register-citation-verifications.md`](../.project-governance/register-citation-verifications.md)), as "a local-ground-truth substitute for the previously egress-blocked verification". That collides with the register's own trust model: [`specification-citation-verification.md`](../governance/specification-citation-verification.md) §6.1 makes the publisher canonical domain "the only source that counts as primary verification", §8.2 assigns the fetch/capture step to a **human** verifier, and the §9 schema (mirrored in the register's Schema section) states a row whose `Captured by` field implies **AI primary capture is invalid and not a verification**. An AI (me) reading a local extract therefore **cannot** produce a valid §9 verification row; recording one would violate the register's integrity control. Meanwhile §6.6 (shipped #515) positions the local base as an AI-readable source but still calls it "believed-current STORAGE, not the version authority (confirm upstream per §12.3)". So the §9 register schema and the recent §3.3 / §6.6 direction are not yet reconciled on **whether, and how, an AI-local-extract confirmation is recorded**.

**Why deferred (needs maintainer): resolving this changes the register's trust model, an authorial call.** Options (recommended first): **(A)** add a distinct lower-confidence row-class to the methodology and register schema for "AI cross-checked the register value against a held Tier-1 local extract" (a `Captured by: AI (local extract)` class with its own `Confidence`, explicitly NOT a publisher-capture verification), then §3.3 records those; **(B)** keep the §9 register human-capture-only and have §3.3 instead perform a **consistency cross-check** (register version+date vs the held extract), surfacing and fixing any MISMATCH but recording NO register row when consistent (the "record in the register" deliverable then drops); **(C)** defer §3.3 entirely until a human-verification cycle (browser egress) can capture the publisher pages the §9 schema requires. The (B) consistency-cross-check half is NOT blocked and could run input-free today under any option; only the register-recording half is blocked. Route-around applied 2026-07-01: §3.3's register-recording deferred, the session continued with §3.5. Resolve on `/resume`.

### MITRE ATLAS scratch superseded-archival: RESOLVED 2026-07-01 (scratch PR #47 merged)

**RESOLVED 2026-07-01 (this resumed session, scratch PR #47 `018b9aa`):** the reference-base currency item is done (reference-base work tracked separately in the reference repo). Upstream re-verified v2026.06 (2026-06-30 release) this turn. The corpus register was already correct at v2026.06 from #512, so scratch and register are now coherent. ATT&CK v19.1 unchanged. **This closes the last open pending decision.** The original deferred-state analysis is retained below for the record.

**UPDATE 2026-07-01 (re-verified upstream this turn):** MITRE ATLAS upstream advanced to **v2026.06** (published 2026-06-30 23:20 UTC, commit `651dad9`; monthly content update: 3 new techniques, 3 updated mitigations, 6 new case studies), superseding the **v2026.05** that #505 recorded (2026-05-27). The corpus Canonical Citations Register ATLAS row was corrected to **v2026.06** (verified 2026-07-01) in **#512**, so the corpus side is current. Egress to the MITRE atlas-data repo was **confirmed available 2026-07-01** (a WebFetch of the release page and the dist file both succeeded), so the earlier "MCP / maintainer-download-gated" framing does NOT hold: the data is auto-fetchable. **The remaining open item is a reference-base currency refresh** (tracked separately in the reference repo). Per the `## Reference-version currency` SOP, do NOT write the superseded `v5.6.0` anywhere as current, and keep the register row and the scratch base coherent (the register is now at v2026.06; the scratch archival brings scratch to v2026.06 too). ATT&CK v19.1 is current and matches both register and scratch (no action).

The prior framing (kept for lineage): the §1.5 build (#505) recorded ATLAS current as v2026.05 and framed the scratch archival as MCP/maintainer-download-gated; the 2026-07-01 re-verification supersedes both the version (v2026.06) and the egress assumption (auto-fetchable).

**Related (informational, not a decision):** the §1.5 register population left **51 of 151 rows `needs-reconfirm`** because their upstream sources (`iso.org` and the IEC webstore for all ISO/IEC rows; `nerc.com`, `tsa.gov`, `hhs.gov`, `planalto.gov.br`, `wcoomd.org`, and similar government sources for several others) returned HTTP 403 / JS-gated responses to automated fetch this cycle. These are not defects and need no decision; a future cycle with browser access or different egress fills the `Upstream check location` URLs. The 100 confirmed rows carry fetch-verified URLs dated 2026-06-30.

_No open decisions remain from the 2026-07-01 window (the live open set is the top-of-file Status line, which stays authoritative as entries accrue; this sentence deliberately no longer restates its counts). Both 2026-07-01 entries (the §3.2 detailed-mirror header-parity gap and the §3.3 citation-verification-pass recording model) were RESOLVED at the 2026-07-01 `/resume`; the MITRE ATLAS scratch-archival was RESOLVED 2026-07-01 (scratch PR #47); FR-24 was resolved 2026-06-30. The 51 `needs-reconfirm` register rows are informational, not a decision. The FR-70 source gate (MiCA / NYDFS BitLicense not held) remains a standing constraint tracked in the handoff's Open-decisions section, surfaced if/when FR-70 is scheduled._

## Resolved at the maintainer's 2026-07-02 return round

All eight entries below were resolved on 2026-07-02: the four decision-round confirmations (C3, C4, C5, the section-3.14 retention pair), the two in-window proceeded defaults resolved at the return round (the gate-60 threshold, REDIRECTED to fail-at-3 and executed in #583; the GR-P3 aid shape, CONFIRMED), and the two held pending items authorized "ship all three" and shipped in the CLAUDE.md batch PR (the advisory-directive codification and the D5-description refresh). Rotated here in the same PR per the authorized rotation.

### CLAUDE.md D5-description refresh (#576 sweep I2; RESOLVED 2026-07-02: authorized at the return round, SHIPPED in the CLAUDE.md batch PR)

`.claude/CLAUDE.md` PR-workflow step 7 describes D5 as detecting the coded-id and prose closure forms "too, not only" the section form: still true after the #577-window six-form widening, but now under-inclusive (three of six forms named). A protected-file edit; the maintainer's 2026-07-02 instruction defers CLAUDE.md edits until their return, so this batches with the advisory-directive codification PR rather than shipping unattended (the #573 precedent fixed CLAUDE.md staleness under the pre-approved fix tier, but the fresh instruction is later in time and specific).

### Gate-60 drift threshold (proceeded 2026-07-02, daytime-unattended; RESOLVED 2026-07-02 at the maintainer's return: REDIRECTED to fail-at-3, the stricter option; the change ships in the follow-up gate-60 PR) (redirect EXECUTED in #583)

**Proceeded with FAIL_DRIFT_THRESHOLD = 5 (sum of absolute per-axis deltas across gates/rules/skills/commands) as the gate-60 guardrail-cadence fail bar, warn on any non-zero drift.** The maintainer answered "Mechanize" for GR-5 without threshold specifics; the build fell in the 3-hour unavailability window, so the value is a logged proceeded default (reversible, one constant in [`tools/lint-guardrail-cadence.py`](../tools/lint-guardrail-cadence.py)). Calibration: larger than one so a single gate-adding PR (including the gate-60 PR itself) warns rather than blocks; small enough that a machinery wave cannot run past the cadence unreviewed (the r1-to-r2 gap was drift 13 on the same metric). Revert path: change the constant (and its docstring paragraph) in one commit; at build time the fixtures pinned the threshold cases at 1 (warn) and 5 (fail). **Redirect EXECUTED in the follow-up gate-60 PR:** constant 3, docstring and spec narrative updated, the boundary re-pinned by fixtures at drift 2 (warn) and drift 3 (fail), suite 275 to 276.

### GR-P3 tension-marker check shipped as an advisory aid, not a gate (proceeded 2026-07-02, daytime-unattended; RESOLVED 2026-07-02 at the maintainer's return: aid shape CONFIRMED)

**Proceeded with the advisory-aid shape for the GR-P3 partial-rewrite tension-marker item: [`tools/tension-scan.py`](../tools/tension-scan.py) flags diff-rewritten TODO bullets and ledger blocks carrying a status-claim token alongside a hedge token, prints each for an end-to-end read, and always exits 0 on findings (2 only on usage or git error); it is not a numbered gate and not wired into CI.** The TODO bullet framed the item as a gate proposal, so this is a shape deviation, taken on census evidence inside the unavailability window: a build-time census over TODO.md and the six working ledgers found 91 block-level status+hedge pair coincidences, essentially all honest narrative (retro rows narrating "shipped X; pending Y", the handoff soft-spots block, and bullets that QUOTE the marker vocabulary, the GR-P3 bullet itself among them), so the bookkeeping-gate family's precision-first bar (a standing gate ships only with a zero-false-positive census, the D5 precedent) is unreachable for this class; the mechanizable half is the trigger (a partial rewrite) and the candidate list (the pairs), with the contradiction judgment staying human. This is the same census-driven aid-not-gate outcome as the GR-12 residual-scan aid. Close-out convention adopted alongside: run the aid before pushing any PR that edits TODO or a ledger block in place. Revert path if redirected to a gate: the detection core is already diff-scoped; add an exit-non-zero mode plus a curated allowlist and wire the four surfaces in one PR (the census predicts a heavy allowlist burden, which is the evidence against that shape).

### C5 DSAR-reconcile apply-time choices (proceeded 2026-07-02, overnight; RESOLVED: maintainer-CONFIRMED at the 2026-07-02 decision round, all defaults stand)

Three apply-time choices inside the maintainer-locked C5 directions (procedure ladder canonical; independent denial sign-off = Legal Counsel):

1. **The template's 3-business-day verification SLA kept as an initial-attempt target nested inside the procedure's 10-business-day suspend clock** (rather than deleted). The two clocks measure different things (an operational first-attempt target vs the outer suspension boundary), so the composition preserves the tighter internal discipline while adopting the canonical outer clock. Redirect path: drop the 3-day sentence from the template's SLA row.
2. **The CIO (acting DPO) denial sign-off retained, with the Legal Counsel written concurrence made mandatory and recorded as the independent review.** The privacy policy's 4.8 mandates CIO review of denials, so removing the CIO sign-off would contradict the policy; independence is instead supplied by upgrading Legal Counsel's step from "reviews and confirms" to a blocking written concurrence recorded in the DSR register. Redirect path: transfer the sign-off itself to Legal Counsel (requires a paired policy-4.8 edit).
3. **The 72-hour restriction acknowledgement retained and framed as a calendar-clock ceiling on the intake receipt** (rather than deleted as redundant beside the workflow's 1-business-day acknowledgement, or loosened to a business-day clock). The clause's real work is the long-weekend case, where 1 business day exceeds 72 calendar hours; both surfaces now name the ceiling. Redirect paths: drop the 72-hour clause from the rights table (the 1-business-day SLA then governs alone), or convert it to a business-day clock (weaker; not stricter-safe). (This third choice was counted on the Status line at #564 ship time but its numbered entry was missed; added at the #564 sweep's F1, the C6 PR.)

### C4 retention-reconcile composed value (proceeded 2026-07-02, overnight; RESOLVED: maintainer-CONFIRMED at the 2026-07-02 decision round, the composed value stands)

One apply-time choice inside the maintainer-locked C4 direction (stricter-safe 7-year floor for audit records): the PIA retention conflict (procedure said 7 years flat; register said 5 years after system decommission) was resolved by COMPOSING the two, "7 years, or 5 years after the associated system's decommission, whichever is longer", on both surfaces; the same composition was applied to the register's AI Impact Assessments row (the pre-push verifier caught that the procedure's Step 6 blanket line covers the PIA/AI-IA report, so the AI-IA row carried the identical conflict). Neither source value dominates the other for all system lifetimes, so the composition is the only strictly-not-weaker resolution; it is also a new value neither surface previously carried. Redirect paths: pick either original value verbatim on both surfaces. The other three fixes (AI audit 5 to 7, supplier audit 5 to 7, breach notifications 5 to 7) take the policy's existing seven-year floor and need no default: the canonical source governs.

### C3 taxonomy-reconcile canonical-wins defaults (proceeded 2026-07-02, overnight; RESOLVED: maintainer-CONFIRMED at the 2026-07-02 decision round, both defaults stand)

Two apply-time choices inside the maintainer-locked C3 direction (canonical risk-category set = the register template's 12), both taken under the overnight stricter-safe/canonical-wins wide-latitude grant and reversible on `main` history:

1. **The ERM standard's "Environmental and Sustainability" category folded, not carried.** The canonical 12 has no environmental category, so the standard's section-4 row was retired and its example subcategories redistributed with none lost: environmental damage to Strategic (beside its existing ESG non-compliance example), energy inefficiency to Operational, technology lifecycle waste to Technology. Redirect path: re-add a thirteenth category to the register template's canonical field and restore the row.
2. **The register procedure's Domain field "or other" escape value dropped.** The Domain enumeration now carries exactly the canonical 12 (closed set, matching the register template). Redirect path: append "or other" back to the procedure's field row.

### Advisory-directive codification (surfaced 2026-07-02; RESOLVED: approved at the decision round, authorized at the return round, SHIPPED in the CLAUDE.md batch PR)

**The behaviour is ALREADY documented and binding now**; this item ELEVATES it. The maintainer directed (2026-07-02) that the assistant proactively surface a better / more-efficient / higher-quality alternative, and disagree when a choice looks against the project's best interests, **any time it sees one, not only when the maintainer says "suggest"/"advise"**, surfacing the disagreement WITH its reasoning and giving the maintainer an opportunity to change their mind, with the maintainer retaining override, governed by INTEGRITY > SPEED > COST. This is exactly the `surface-counterproductive-instructions` pack rule + the CLAUDE.md "'Suggest'/'advise' invite assessment" convention; the two just live in separate places and the "suggest/advise" convention is scoped to when the maintainer uses those words.

**Why deferred (not a defect, a protected-file edit):** codifying it edits [`.claude/CLAUDE.md`](../.claude/CLAUDE.md) (and possibly the pack rule), which the maintainer's own overnight rule defers to morning + requires approval; and per always-split a convention change is its own reviewable PR, not bundled into the #549 bookkeeping handoff. Maintainer confirmed 2026-07-02 "this can be done in the morning."

**Draft for the morning PR (a one-approval action):** elevate the CLAUDE.md "'Suggest'/'advise' invite assessment" bullet into a standing top-level expectation reading roughly: *"Proactively surface a better, more-efficient, or higher-quality alternative, and disagree when a choice looks against the project's best interests, any time you see one, not only when the maintainer says 'suggest' or 'advise.' Surface the disagreement with its reasoning and give the maintainer an opportunity to change their mind; the maintainer retains override. This is the `surface-counterproductive-instructions` discipline as a standing default, governed by INTEGRITY > SPEED > COST."*, cross-linked to the pack rule. Resolve/approve on `/resume` (or the morning), then ship as its own dedicated PR.

### Two 3.14 retention values resolved stricter-safe (proceeded 2026-07-02, daytime-unattended; RESOLVED: maintainer-CONFIRMED at the 2026-07-02 decision round; the two flat AI-row siblings were composed per the round's answer in #580)

Two apply-time value choices in the section-3.14 batch-B PR, both taken under the attended-autonomous stricter-safe rule, reversible on `main` history:

1. **L-f: the register's Security-incident-records row raised 5 to 7 years** (with the [`security/standard-logging-and-monitoring.md`](../security/standard-logging-and-monitoring.md) example echo co-changed), aligning the record row to the 7-year forensic-evidence minimum in the incident-response procedure's Section 8 and the privacy-breach row's 7-year floor (a privacy breach is a security-incident subclass; records should not outlive their evidence in reverse). Redirect path: restore 5 years on both surfaces and document the records-vs-evidence rationale instead.
2. **The #563-routed AI-audit retention pair (mis-labelled "Item 14" in the first record; the audit's item 14 was the C8-closed encryption conflict): the register's AI-audit-reports row composed** to "7 years, or 5 years after the associated system's decommission, whichever is longer" (the same composition shape as the adjacent AI-Impact-Assessments row), so neither the compliance policy's 7-year audit floor nor the retention standard's AI-Systems domain minimum (5 years post-decommission) is weakened for any system lifetime. Redirect path: pick either original value verbatim and declare the domain minimum a floor rows may exceed.

**Observed siblings, NOT actioned (scope discipline):** the register's AI-incident-records row (5 years, flat) and its AI-decision-and-detection-logs row (7 years, flat; the #574 sweep's catch) both sit under the same AI-Systems domain minimum and may carry the same composition question for long-lived systems; neither was in the 3.14 item, so both are surfaced here for triage rather than silently expanded into. **ACTIONED 2026-07-02 (the round-1 maintainer answer "Compose both"): both siblings composed in the follow-up PR with the same whichever-is-longer shape (register 1.0.13), closing this triage note; the composition is itself maintainer-decided, not a proceeded default.**

## Resolved at the 2026-06-30 `/resume`

### FR-24 control-testing result-classification scheme: reconcile to canonical, or keep with crosswalk (surfaced 2026-06-30, overnight; authorial; PROCEEDED with the keep-and-crosswalk stricter-safe state; RESOLVED 2026-06-30 at `/resume`: maintainer confirmed keep-and-crosswalk, no change)

The FR-24 deepening (#492) surfaced a cross-document inconsistency: [`compliance/procedure-control-testing.md`](../compliance/procedure-control-testing.md) §4 classifies results as **Effective / Observation / Deficiency / Material Weakness** (a Sarbanes-Oxley framing; the doc frames "material weakness" via SOX §404 after the #493 correction), whereas the corpus-canonical finding-classification scheme used by [`compliance/standard-internal-audit.md`](../compliance/standard-internal-audit.md) §7 and [`compliance/procedure-capa.md`](../compliance/procedure-capa.md) §5 is **Critical / High / Moderate / Low / Observation** (and the CAPA register the control-testing procedure feeds is keyed by that canonical scheme). **PROCEEDED (stricter-safe, reversible/on-branch):** #492 did NOT change the scheme (re-classifying an existing procedure's normative content is beyond FR-24's depth scope and is authorial given the deliberate SOX framing); instead it added a §4.2 classification-scheme note that (a) flags the relationship and (b) specifies the CAPA-severity mapping applied on routing (Material Weakness to Critical; Deficiency to High or Moderate by impact), so the CAPA register stays internally consistent today. **Maintainer decision (confirm or redirect on resume):** (A) replace the procedure's SOX-framed scheme with the canonical Critical/High/Moderate/Low/Observation outright (cleanest corpus consistency; loses the explicit SOX "material weakness" term unless retained as a crosswalk); (B) keep the SOX scheme with the crosswalk note as shipped (#492's current state; preserves the deliberate SOX framing). Recommended: confirm (B) if the SOX framing is intentional, else (A). Not a defect either way; the crosswalk keeps the register consistent in the interim.

### FR-167(b) matrix gap-fill: IN/OUT scope for OT + meta/glossary/crosswalk docs (surfaced 2026-06-30; RESOLVED 2026-06-30, maintainer chose Option B)

**RESOLVED 2026-06-30 (maintainer, attended):** Option B, row the 6 OT docs (`operations/ot/*`) + the AI Security Tooling Landscape register, with honest-`N/A` customs columns; EXCLUDE the deprecated `annex-regional-privacy-requirements.md` stub (its live replacement is already rowed). Shipped via the high-assurance harness in **#494**, which **CLOSES FR-167**. The library-meta / glossary / crosswalk docs remain unrowed (the matrix's deliberate exclusion pattern; not revisited). The original deferred-state analysis is retained below for the record.

The FR-167 closing `/matrix-fit` (closure item (a)) shipped in #490; the only FR-167 residual is **closure item (b)**: add compliance-matrix rows for any substantive corpus document not yet rowed. A read-only discovery (all 347 non-README corpus docs minus the 280 the matrix references) found **84 absent docs, of which ~52 are the `dev-security/claude-rules/` governance pack** (out of corpus-matrix scope by construction). Of the 30 remaining non-pack absences, calibration against what doc-TYPES the matrix already rows is decisive: the matrix rows 26 templates, plus procedures/standards/frameworks/charters, but rows **0 OT docs, 0 specifications, 0 glossaries/terms** (a consistent exclusion pattern).

**Stricter-safe default being applied now (in-scope under every reading; reversible/on-branch; evidence-backed):** add rows for the **7 net-new adopter-facing documents shipped this session** whose doc-types the matrix already rows and which were simply never added when they shipped: `privacy/template-legitimate-interest-assessment.md` (#480), `privacy/framework-privacy-by-design.md` (#482), `privacy/template-transfer-impact-assessment.md` (#483), `compliance/procedure-mergers-acquisitions-due-diligence.md` (#484), `compliance/standard-sanctions-and-export-control-screening.md` (#485), `ai/charter-ai-ethics-review-panel.md` (#486), `privacy/template-breach-notification-regulator-register.md` (#487). Each is a SENSITIVE change (gate-blind control-mapping correctness) and goes through the high-assurance harness per the resolved overnight directive 1, mirroring the existing matrix row structure exactly. Recorded as "proceeded with X (stricter-safe default); confirm or redirect on resume".

**DEFERRED-BLOCKED (genuine maintainer scope judgment; `AskUserQuestion` errored):** whether the following also get rows: (a) the **6 OT docs** `operations/ot/*` (OT/ICS security, change-mgmt, incident-response, asset-inventory, overviews), 0 OT docs are currently rowed and OT has its own IEC 62443 basis (not one of the matrix's 8 columns) and the supply-chain domain has its OWN alignment matrix, so OT's absence reads as a deliberate separate-treatment choice rather than a gap; (b) the **2 pre-existing substantive docs** `governance/register-ai-security-tooling-landscape.md` and `privacy/annex-regional-privacy-requirements.md` (likely in-scope but held for a single coherent scope answer); (c) the **library-meta / glossary / crosswalk docs** (specs, registers about the library itself, glossaries, the matrix/crosswalk self-reference docs), 0 of these types are rowed, reading as a deliberate exclusion. **Options for the maintainer (recommended first): (A)** the net-new-docs + the 2 pre-existing substantive ones, EXCLUDE OT/meta/glossary/crosswalk (matches the matrix's existing exclusion pattern); **(B)** also include the 6 OT docs with honest-N/A in the customs columns; **(C)** only the 7 net-new docs this pass, OT and the rest a separate determination. Confirm or redirect on resume; the candidate list is in `/tmp/.../scratchpad/gap-nonpack.txt` and reproducible via `comm -23` of the corpus doc set vs the matrix-referenced set.

### FR-58 inheritance-vocabulary 3-label scheme: mechanism fork (deferred-blocked 2026-06-30, overnight; RESOLVED 2026-06-30, shipped #497)

**RESOLVED 2026-06-30 (maintainer, attended):** the maintainer answered the mechanism fork by choosing the **index-register-column locus** (a variant of option (B) without a metadata-block migration): add an `Adoption Disposition` column to [`governance/register-document-index-and-classification.md`](../governance/register-document-index-and-classification.md) classifying all 292 active documents (`library-internal` / `template` / `reference`), define the vocabulary in the Key Terms register, and add the disposition-model section + a maintenance rule, with no 13-field-metadata change. Shipped via the deterministic-apply + skeptical-verifier harness in **#497**, which **CLOSES FR-58**. The original deferred-state analysis is retained below for the record.

A research scope of FR-58 (P1) this session found its premise does not hold as a mechanical prose conflict: the three target words each appear in the corpus in a **settled, locally-consistent sense that is NOT the FR-58 content-disposition sense** (`library-internal` = a GRC-term class at [`governance/register-key-terms-and-definitions.md`](../governance/register-key-terms-and-definitions.md):21 / [`register-glossary.md`](../governance/register-glossary.md):28; "reference content" = adopter-facing register classification at [`governance/specification-project-governance-separation.md`](../governance/specification-project-governance-separation.md):119-124; "template content" = a `.working/`-only governance-vs-project framing). No document defines an inheritance/disposition vocabulary and the 13-field metadata block has no disposition field, so there are **zero clean mechanical edits**; rewording the overloaded lines to the FR-58 labels would introduce error. The maintainer's 2026-06-25 decision ("apply the 3-label scheme corpus-wide, per-document apply-time verification; surface any document that does not fit cleanly") is **silent on mechanism**, and the originating fitness Rec-9 proposed a 4-value `Adoption Disposition` metadata field the decision did not adopt. **Authorial fork; deferred per the overnight "defer anything needing maintainer input" directive.** Options for the maintainer (recommended first): **(A)** add a defining glossary / document-architecture entry for the 3 labels and disambiguate the 3 existing overloaded usages (smallest faithful reading, additive); **(B)** add a per-document `Adoption Disposition` metadata field + presence gate (the Rec-9 shape, a 13->14-field corpus-wide metadata-model migration, single-session, large); **(C)** confirm there is no live prose inconsistency and reframe/close FR-58. NON-partitionable single-session whichever is chosen; do NOT apply until the maintainer picks the mechanism.

_The 2026-06-29 and 2026-06-30 entries below are RESOLVED (retained for the audit trail)._

## Resolved overnight directives (maintainer-answered 2026-06-30, `/resume` into overnight mode; standing for this run)

The `/resume` put the session into **overnight mode** (maintainer asleep; a mid-night prompt does NOT exit overnight mode; anything needing maintainer input is deferred). Three authorial decisions were surfaced up front (via `AskUserQuestion`, then confirmed by maintainer screenshot, both agreeing) so items would not defer:

1. **Net-new authority = self-merge green + harness.** Author AND self-merge green P1/P2 net-new content PRs; run the high-assurance adversarial-verification harness (independent false-negative + false-positive verifiers, deterministic apply, re-parse) on EACH net-new standalone document BEFORE merge; record each in [`high-assurance/register.md`](high-assurance/register.md). (Same as the prior run's directive 1, re-confirmed for this run.)
2. **Missing sources = attempt download, then defer.** Deepen ref-covered jurisdictions now; for each primary source not yet available in the reference base, attempt to acquire it via the reference-base workflow (egress permitting per DD-10); defer only the truly-unavailable, recording it.
3. **ERC tier-table reconcile (TODO P3) = collapse dup; drop Tier-2 stray.** In [`governance/guideline-minimum-viable-governance-structure.md`](../governance/guideline-minimum-viable-governance-structure.md): Tier-1 delete the duplicate "Executive Risk Committee" row (line 58), keep "Enterprise Risk Committee" (line 59); Tier-2 remove "Executive Risk Committee" from the Executive Committee combined-forums list (line 80), since the risk committee is its own Tier-2 body (line 81). Treats both residuals as drift/duplication. Closes the ERC residual P3 item.

**Overall directive (maintainer `/resume` args):** work Priority 1, 2, and 3 items to reduce the list as much as possible overnight; **fix-issues-first, new-content-second**; correct any errors discovered; continue (do not wind down) absent evidence of hallucination/drift.

4. **Protected-file changes are DEFERRED in overnight mode (maintainer-directed 2026-06-30, mid-run).** Do NOT work items that require changing **protected files such as [`.claude/CLAUDE.md`](../.claude/CLAUDE.md) and the governance pack ([`dev-security/claude-rules/`](../dev-security/claude-rules/), [`.claude/rules/`](../.claude/rules/))** while in overnight mode: the maintainer must manually approve those and is asleep. (The #478 §4.8 CLAUDE.md codification was already in flight when this was raised; the maintainer approved finishing #478 specifically, then directed deferral of anything else needing explicit approval.) **Deferred until attended mode (status as of the 2026-06-30 resume, now attended):** the §1.3 residual (the renumbered former §4.8) had three parts. **compute-don't-ask into the `clarify-before-acting` pack rule shipped in #504** (the compute-first gate); the **new-skill-drafting checklist** and the **count-gate word-form remainder** remain open and are now workable. This overnight-deferral note is itself spent (overnight mode has ended); retained only for audit lineage. **In overnight scope (not protected):** corpus content documents (the FR items, deepenings, FR-58, FR-167 matrix), corpus corrections, and the routine version / CHANGELOG / TODO / DONE / `.working/` bookkeeping those require.

### §4.6 QA-cadence mechanical enforcement: close as satisfied (surfaced 2026-06-29; RESOLVED 2026-06-29, maintainer chose close-as-satisfied)

Scoping §4.6 (a candidate gate that "compares the post-merge history files against the merged-PR list and fails when a PR's row is missing or marked abbreviated") surfaced that its mechanically-valuable core, the **missing-row half**, is **already shipped as gate 50 Check 1** ([`tools/lint-bookkeeping-parity.py`](../tools/lint-bookkeeping-parity.py):27-48, "Check 1, QA-cadence parity (the §4.6 surface)"): it derives the merged-PR list from CHANGELOG headers, requires a [`validate-pr/history.md`](validate-pr/history.md) row + (for substantive PRs) an [`improvement-log.md`](improvement-log.md) row per in-window PR, and handles the handoff / subsumption exemptions and `KNOWN_HANDOFF_NO_ROW`. The §4.6 design questions (where the gate lives, the handoff exemption, mirroring the rotation family) are all already answered by gate 50. The only residual, the **abbreviated-marker half** (detecting a row that exists but is a sham, the literal Sweep 22 "abbreviated spot-check, 0 findings" failure), is **not FP-free-mechanizable** on the free-prose Findings/Summary cells (an honest abbreviation-admission has no unambiguous shape, unlike the strikethrough / uppercase-`SHIPPED` markers the family's precision-first discipline relies on); building it would violate the gate-48/gate-50 FP-free discipline, and an FP-prone gate gets worked around (the `gate-discipline` concern). This is the same shape as the S3 item (parked) and the S2/#463 resolution (the "new gate" was redundant with existing gate 8). Surfaced via `AskUserQuestion` (flaked, 3rd time this session). **RESOLVED 2026-06-29 (maintainer, in chat): "close 4.6 as satisfied"** (Option A, recommended): §4.6 closes as substantially-satisfied by gate 50 Check 1; the abbreviated-marker residual stays the existing CLAUDE.md "Throughput pressure does not authorize QA abbreviation" convention (no new gate, no schema change). §4.6 deleted from TODO and rotated to DONE in the closing PR.

### §4.5 S3 citation-precision-for-claim gate: design fork (surfaced 2026-06-29; RESOLVED 2026-06-29, maintainer chose defer/park)

Scoping the S3 "citation-precision-for-claim" gate surfaced that it is **not mechanizable as a corpus gate**: the corpus holds no external-standard source text to check a claim's precision against (the source text lives in the separate reference base, unreachable from CI). Three options were surfaced (the `AskUserQuestion` primitive flaked, so the fork was held): (A) a narrower mechanical gate (existence/format only, reusing the gate 48/49/54 validators); (B) a cadenced `/matrix-fit`-style skill using the scratch `ref/` source text (not a CI gate); (C) defer S3 and move to §4.6 / §4.8. **RESOLVED 2026-06-29 (maintainer, by screenshot): defer/park S3** ("Defer S3; move to §4.6 / §4.8 next. Park S3 pending a clearer design (gates 48/49/54 already cover control-code existence corpus-wide, so S3's marginal mechanical value is unclear). Proceed to the §4.6 QA-cadence gate or the §4.8 apply-time-discipline codifications, which have no feasibility fork."). S3 stays in TODO §4.5 marked parked-pending-design; the next work is §4.6 or §4.8.

### §4.10 / §4.6 bookkeeping-parity gate-family design + sequencing (surfaced 2026-06-29, post-#466/#467; RESOLVED 2026-06-29)

**RESOLVED 2026-06-29 (maintainer):** "build Option B, add the complementary CHANGELOG-asserts-closure check." Both checks are to be built. The **marked-done detector** shipped as **gate 57** in **#468** (scoped to the three FP-free structural markers, strikethrough / `[done]` / `Status: completed`; the bare-`SHIPPED` facet was dropped because it false-positives on open multi-part items like FR-167's "column SHIPPED in PR-B", per the maintainer's "long-term best solution" directive, and the complementary check covers that case instead). The **complementary CHANGELOG-asserts-closure PR-time check** (a PR whose added CHANGELOG asserts it closes a TODO item must touch both [`TODO.md`](../TODO.md) and [`.working/DONE.md`](DONE.md), with the handoff-PR exemption) **shipped as the D5 PR-time delta check** ([`tools/check-todo-rotation-on-pr.py`](../tools/check-todo-rotation-on-pr.py), wired into [`tools/run-pr-time-checks.sh`](../tools/run-pr-time-checks.sh) and the `quality.yml` pull_request section), which closes §4.10. With both halves shipped, §4.10 is fully closed. Sequencing thereafter: the `docs/` dash extension (Option A, next), then §4.5 S3, then §4.8 codifications (deliberately, not at a long-turn tail), then §4.4 / §4.3.

### §4.10 / §4.6 superseded pending note (the original open framing, retained)

The #466 `/retro` surfaced a coverage gap in the maintainer-decided §4.10 design. §4.10 was DECIDED 2026-06-28 as **Option B, the "marked-done detector"** (flag a TODO item that marks ITSELF done via `~~` / `[done]` / `SHIPPED`). But #466's actual failure (the omitted §4.5 S4 rotation, fixed in #467) was a **wholesale-forgotten** rotation: TODO.md was never edited, so there was nothing self-marked for Option B to detect. **Option B would not have caught #466.** A complementary check would: a PR whose CHANGELOG asserts it closes a TODO item (regex on "closing TODO §X" / "closes TODO §X") must touch BOTH [`TODO.md`](../TODO.md) and [`.working/DONE.md`](DONE.md) in the same diff. **Surfaced to the maintainer** (chat, post-#467) with the coverage-gap note plus the next-5 sequencing question. **Decision needed:** (1) build Option B as decided, (2) build the complementary CHANGELOG-closure-claim check instead/additionally, or (3) some other shape; AND confirm the sequencing among §4.10/§4.6 (gate family), §4.5 S3, §4.8 codifications, §4.4, §4.3. **Authorial / not auto-defaulted** (building the wrong gate wastes real effort and gives false confidence); the gate build is deferred-blocked pending steer. Route-around considered: §4.8 codification is independent of this decision, BUT §4.8's own TODO entry says "action deliberately, not at a long-turn tail", so it was NOT auto-started on the 2026-06-29 graceful-degradation timeout; the assistant idled for maintainer steer instead (no degradation signal, so not a wind-down handoff). On `/resume`: resolve this gate-family decision and the sequencing, then proceed.

### `docs/` dash house-style decision (Sweep-76 Finding 2, surfaced 2026-06-29; RESOLVED 2026-06-29)

**RESOLVED 2026-06-29 (maintainer): Option A, then refined to "full house-style now"** ("always go with the long term best solution for the project rather than a one time fix"): extend the language gate to cover `docs/` (close the gate-blind path-scope gap permanently) AND fix the violations. On implementation (PR #470), bringing `docs/` under the actual gate surfaced **117 findings, not the 71 dashes the original framing assumed** (71 dashes + 25 `-ise` + 14 heading-case + 5 sanitisation + 2 ensure); the scope fork was surfaced and the maintainer chose **"full house-style now"** (the full gate, not a dash-only check). **SHIPPED in PR #470**: gate 2 scan extended to `docs/` (generated artefacts excluded, the worked-example meta-tutorial exempted from the three checks it demonstrates), all 117 findings fixed. The original options are retained below for the audit trail.

Sweep 76 found `docs/` authored prose carries 71 em/en-dashes across 4 files ([`docs/decision-tree.md`](../docs/decision-tree.md) 58, [`docs/adopter-guide.md`](../docs/adopter-guide.md) 5, [`docs/template-quickstart.md`](../docs/template-quickstart.md) 4, [`docs/template-implementation-roadmap.md`](../docs/template-implementation-roadmap.md) 4), violating the CLAUDE.md house-style but outside [`tools/lint-language.py`](../tools/lint-language.py)'s path scope. Surfaced to the maintainer with named options; the `AskUserQuestion` primitive is flaky this session, so it is recorded here per the attended-autonomous defer path and tracked in TODO P3. **Options put to the maintainer** (recommended first): (A) extend the dash check to `docs/` and fix all 71 (closes the gate-blind gap permanently); (B) document `docs/` as an intentional house-style carve-out (no fix); (C) one-time fix the 71 without extending the gate. Out-of-window / pre-existing; reversible / on-branch when actioned. **Not auto-defaulted** (it is a gate-scope authorial decision); held for the maintainer. Does not block other queued work.

### S2 role-drift resolution (2026-06-29; RESOLVED 2026-06-29, maintainer chose the allow-list to register reframe)

Building the **S2 role-definition-consistency gate** (TODO §4.5) with the low-FP design (check the `Owner:` / `Approving Authority:` metadata fields and RACI-table roles against [`governance/register-role-authority.md`](../governance/register-role-authority.md)) surfaced **5 roles used as document Owners/Approvers but absent from the register's 35 canonical roles**:

- **GRC Programme Manager** (Owner on ~7 docs)
- **Compliance Maintainer**, **Information Security Maintainer**, **Security Architecture Maintainer** (Owners; siblings of register rows like *AI Security Maintainer*, *Supplier Risk Maintainer*, *Control Framework Maintainer*, which ARE registered)
- **Governance Library Maintainer** (the register's own Owner/Approving Authority; a library-meta role)

These are pre-existing FR-115-class role drift. The gate cannot ship green until they are resolved, and the resolution is **authorial** (defining governance accountabilities for the 4 substantive roles, and deciding whether the library-meta role belongs in an org-role register), which "Proceed" (build S2 with the low-FP design) did not delegate. The `AskUserQuestion` primitive errored (permission-stream flake) when surfacing this, so it is recorded here per the attended-autonomous defer-and-skip path.

**Options put to the maintainer** (recommended first): (A) I draft register rows for the 4 substantive roles by mirroring their sibling rows' accountability pattern, for maintainer review, and handle Governance Library Maintainer as a library-meta row or a documented scope-exclusion, then build the gate; (B) scope-exclude all 5 as library-internal/meta roles via a documented allow-list and build the gate now; (C) defer S2 and build S3/S4 next.

**RESOLVED 2026-06-29 (maintainer choice, superseding the earlier defer-and-skip).** On resuming the build, the orchestrator discovered that gate 8 (`lint-roles.py`, "Owner and Approving Authority role audit", wired across all four parity surfaces) ALREADY validates every `Owner` / `Approving Authority` value against the register, and the 5 "missing" roles above were exactly its `EXTRA_KNOWN_ROLES` allow-list entries. The originally-scoped "new S2 gate" was therefore redundant with gate 8. The orchestrator surfaced this with named options; the maintainer chose **"migrate allow-list to register"** (confirmed by screenshot of the `AskUserQuestion` selection plus "Add rows and draft"): add the 5 roles as register rows so the register is the single source of truth, empty the linter allow-list, no new gate, no gate-count change. Shipped as **PR #463**, which is **held for maintainer review of the drafted accountability prose** (the prose is semantically authorial; gate 8 stays green regardless of its wording, so the maintainer can revise it freely before merge). RACI-cell checking was deliberately excluded (descriptive composites, high-FP, against the low-FP directive). S2 is closed in TODO §4.5.

## Resolved overnight directives (maintainer-answered 2026-06-28; standing instructions for the overnight run)

The `AskUserQuestion` primitive errored twice (permission-stream closed); the maintainer
answered all four in plain text (screenshot). These are now standing directives the overnight
session follows directly. No longer pending.

1. **Overnight merge authority = FULL AUTONOMY, with mandatory adversarial verification on
   net-new.** The orchestrator authors and self-merges green PRs for ALL P1/P2 content,
   including net-new standalone documents, WITHOUT holding for morning review. **The one
   condition: run the new high-assurance adversarial-verification harness (the
   [`high-assurance-verification`](../dev-security/claude-rules/governance/high-assurance-verification.md)
   process, independent false-negative + false-positive verifier agents over the net-new
   content) on ANYTHING net-new BEFORE accepting/merging it.** Record each net-new item and
   its harness outcome in [`high-assurance/register.md`](high-assurance/register.md). (This
   directly operationalizes the rule shipped this session: net-new domain docs are exactly the
   gate-blind, high-escaped-cost class the harness is for.)

2. **Source-absent jurisdictions/regimes (FR-59 / FR-61 / FR-62) = cover sourced, ATTEMPT to
   download the missing legislation, defer only the truly-unavailable.** Deepen the
   ref-base-covered ones; for each missing source, ATTEMPT to acquire the primary-source
   legislation and add it to the reference base via the reference-base workflow (egress
   permitting per DD-10). Only where a source cannot be obtained, defer that jurisdiction and
   record it. (Redirect from the "defer all source-absent" default: the maintainer wants the
   download attempted first.)

3. **Out-of-overnight-scope = multi-session orchestration tracks (§4.7 / §4.11 / §4.16 /
   §4.18), FR-48 (H2 rename), and FR-167 gap-fill.** The procedural closing `/matrix-fit` may
   still run. **FR-70 (crypto-asset / blockchain domain) is IN overnight scope** (the
   maintainer's list omitted it): treat it as net-new and run the adversarial-verification
   harness on it before merging, per directive 1.

4. **ISO/IEC 27001:2022 Annex A control TITLES are usable (NOT proprietary).** The maintainer
   clarified: a control title is like a detailed table of contents and is commonly reproduced;
   the proprietary material is the section CONTENT the title heads, not the title. So the ISO
   title-map may carry the verbatim Annex A titles (unblocking the queued P3 ISO-validation
   gate-54 extension and the `/matrix-fit` ISO-column assessment); do NOT reproduce the ISO
   control body/implementation text. Build the ISO title-map from the reference-base ISO Annex A material.

The audit trail lives here, in the [`session-handoff.md`](session-handoff.md) next-actions
queue, and in the session-closing chat message.
