# Session Handoff

**Purpose.** This file is the single resume point for a new Claude Code session. It is refreshed at every PR close-out so that opening a fresh session and continuing requires only one instruction from the maintainer (see "How to resume"). It exists because long sessions degrade (context dilution, lossy compaction, state drift, error compounding); a fresh session that rebuilds state from this file plus the durable repo artefacts is more reliable than a long one running on accumulated memory. The mechanisms and mitigations are written up generically in `grc_library_private/session-length-considerations.md`. This file is maintainer working state, exempt from corpus audit gates per the `.working/` exemption.

This is an **as-of-last-refresh snapshot**, not a live-HEAD claim. Versions and counts drift forward as work advances; always verify against live files before relying on them.

## How to resume (the one command)

In a new session, the maintainer sends only: **`/resume`** (or "read `.working/session-handoff.md` and continue").

On `/resume`, the assistant:
0. **Runs the concurrency lease check FIRST** (`/resume` step 0, shipped with the section-3.7 interlock): read [`session-state.md`](session-state.md) from `main` and git-cross-check unmerged `origin/claude/*` sibling branches; 60-minute staleness window, advisory HOLD on a live signal (never proceed on a timeout); ACQUIRE the lease on proceeding. Gate 63 guards the lease file's shape.
1. Reads this file in full, including the **"Known environment behaviours"** section below.
2. Reads `.claude/CLAUDE.md` (the PRIMORDIAL RULE and project disciplines), the most recent few `CHANGELOG.md` entries, and **`grc_library_private/third-party-issues.md`**.
2a. **Loads the `grc_library_ref` reference index into memory** (`grc_library_ref/INDEX.md`, `grc_library_ref/catalogue.yml`, `grc_library_ref/SECTION-INDEX.md`, `grc_library_ref/COVERAGE-MAP.md`) so the reference set and its versions are known before any task; the index, not a guessed path, is authoritative for the reference set and its locations. See `/resume` command's Reference-knowledge-base step and the `## Reference-version currency` SOP in `.claude/CLAUDE.md`.
3. Runs `git rev-parse --is-shallow-repository` (unshallow with `git fetch --unshallow` if `true`, **before any history-aware audit**), then `tools/run_all_audits.sh` to confirm the corpus is green, and `git log --oneline -5` to confirm HEAD.
4. Verifies the version/count snapshot below against live files.
4a. **Batches the unattended-run clarifications up front (maintainer-directed 2026-06-25)**: before resuming the queue, asks the maintainer (via `AskUserQuestion`, batched into as few rounds as possible) every decision that would let the session run unattended longer, pre-populating decisions for **all Priority 1 and Priority 2 backlog items** and **the next 10-plus planned PRs** (including any outside P1/P2, e.g. the FR-167 batches). Compute-don't-ask first (ask only genuine authorial decisions, not what the assistant can verify); record the answers in the next-actions queue so they survive a mid-session compaction; mark deferrals and route around them. See `/resume` command step 5 for the authoritative form.
5. **Runs a full corpus-wide `/validate` as the first substantive task** (the loop-break compensating control for whatever session-closing handoff PR last closed, which skips its trailing `/validate-pr` + `/retro`): full A/B/C subagent dispatch over the closing session's delta window, recorded in [`validate-sweeps/history.md`](validate-sweeps/history.md) and the Resume-cursor section above. **Cross-check the findings against the "Asserted expectations" section below**: a contradiction of an asserted-clean touched surface is a genuine miss, escalated; an ordinary finding routes to the backlog. Standing invariants to confirm: the corpus is CSF-1.1-clean (gate 54, 0 carriers; a carrier in a per-document NIST table is a genuine finding, not residue); four-surface parity at the current gate count (gate 35); the gate/rule/skill/command counts (gate 39 + prose). The specific sweep number and delta window are in the current "Next actions" block below.
6. **Prunes this file as part of that first `/validate` close-out PR** (keep current + 1 prior, per `## Refresh and pruning discipline` below), so the handoff does not accumulate stale session history across resumes.
7. Continues from "Next actions" below.

## Resume cursor (machine-readable)

This is the relocated home of the former `## Session resume metadata` block (moved out of
[`TODO.md`](../TODO.md) in PR #413 so TODO stays purely forward-looking). The sweep cursor below is
read by [`tools/lint-todo-staleness.py`](../tools/lint-todo-staleness.py) (gate 45), which flags it
if it falls behind [`validate-sweeps/history.md`](validate-sweeps/history.md). The full sweep history
lives in that history file; the live per-session state (branch, versions, counts, green-at-`<sha>`) is
in the "Next actions" blocks below and is an as-of-last-refresh snapshot (drift forward is expected).

- **Last validation sweep**: Sweep 115 iter 1 (2026-07-19; the corpus-wide `/validate` over the **#1044..#1053** deltas (diff base #1043 `123291ed`, head #1053 `dedb17a1`) of the 2026-07-19b resumed session, run **PRE-CLOSE this session (maintainer-directed, because the gap to the next session is several days)** rather than deferred to the next `/resume`. **OFFLOADED to worker-20260716-b** (Opus 4.8) as blocking prio-0 order `sweep-115-validate` pinned `dedb17a1`/#1053: proof-of-run genuine (A/B/C returns ~264K tokens); mechanical baseline **72/72**, 443-test regression rc 0, `build-taxonomy`/`build-portal --check` in sync; counts 72/13/24/15/18; four-surface parity 72. **0 error / 0 warning / 4 note.** N1 (`validate-pr/history.md:22` count nit `1261`->`1262`) + N2 (mandatory-offload hook message repointed from the moved `## Credit-offload mode` to the live public `## Mandatory worker offload`) FIXED this close-out; N3 (§3.70 pack-vs-corpus crypto divergence) tracked in `pending-decisions.md` for the maintainer's confirm-or-redirect; N4 (`_private` worker-scope coverage) covered by the orchestrator's own full-access `_private` verification this session. All #1044-#1052 asserted-clean surfaces CORROBORATED, 0 contradicted. **This session's loop-break control ran HERE (pre-close), so the next `/resume` need not re-run it over #1044..#1053.** No detail file (worker source `grc_library_scratch:results/sweep-115-validate.md`). Prior: Sweep 114 iter 1 (2026-07-19; the loop-break corpus-wide `/validate` over the **#1040..#1043** deltas (diff base #1039 `130540cc`, head #1043 `123291ed`) of the 2026-07-19 resumed session, run at the `/resume` from the session-closing handoff #1043. **OFFLOADED to worker-20260716-b** (Opus 4.8) as the pre-positioned blocking prio-0 order `sweep-114-validate` pinned `123291ed`/#1043, consumed under ELEVATED QA (worker-b restarted this session, so the window re-establishes): proof-of-run genuine (A/B/C returns, per-finding `path:line`); independent re-derivation HEAD `123291ed`=#1043=pinned, baseline **72/72** non-shallow, counts 13/24/15/18, four-surface parity 72, versions library `2026.07.531`/pack `1.62.2`/spec `1.17.13` EXACT-MATCH. **0 error / 1 warning / 3 note. PASS.** F1 (TODO §1.22.6 rotation debris at `TODO.md:80`, gate-57-blind "CLOSED this PR" phrasing) FIXED this close-out (the §1.22.6 line deleted); 3 informational notes (guardrail cadence drift 2/3, `decisions-search.py` correctly non-gated, advisory-tool count). All #1040-#1042 asserted-clean surfaces CORROBORATED, 0 contradicted. **Loop-break control for #1043 PASSES.** Worker-b 1st clean ELEVATED pass this session. No detail file (worker source `grc_library_scratch:results/sweep-114-validate.md`). Prior: Sweep 113 iter 1 (2026-07-19; the loop-break corpus-wide `/validate` over the **#1021..#1039** deltas (diff base #1020 `56889588`, head #1039 `130540cc`) of the 2026-07-18b resumed §1.19 Phase-2/3 privatization session, run at the `/resume` from the session-closing handoff #1039 on branch `claude/resume-sweep113-closeout`. **OFFLOADED to worker-20260716-b** (Opus 4.8) as the pre-positioned blocking prio-0 order `sweep-113-validate` pinned `130540cc`/#1039, consumed under full ELEVATED QA (worker-b delivery 1 this fresh session, so the session-scoped trust window re-establishes): proof-of-run genuine (~360K tokens, B~174K/C~63K); independent orchestrator re-derivation of HEAD `130540cc`=#1039=pinned SHA, baseline **72/72** non-shallow, counts 13/24/15/18, four-surface parity 72 (312/310/269/39), versions library `2026.07.527`/pack `1.62.2`/spec `1.17.13` EXACT-MATCH; the two changed citation families independently re-greped (ISO `8.15.[357]` zero live carriers, both `SEF-02` residuals genuine ITSM controls, `SEF-07` in both touched files). **0 error / 0 warning / 0 note / 0 new.** A dedicated delivery-1 false-negative auditor independently re-derived all five §1.19.x hunt classes and returned **CLEAN in-delta**; its one out-of-window note (`operations/procedure-security-monitoring-and-alert-management.md:333` SEF-02, same class as #1022) is the **already-maintainer-decided** `:333`->`SEF-06, SEF-07` item in `pending-decisions.md` (apply pending), NOT a miss. All #1021-#1038 asserted-clean surfaces CORROBORATED, 0 contradicted; re-confirm (c) shows the #1039 F1 fix took (resume.md and CLAUDE.md agree alert-clearing is the maintainer's decision). **Loop-break control for #1039 PASSES.** Worker-b at 1 of 2-to-3 clean elevated passes. No detail file (worker SARIF-lite source `grc_library_scratch:results/sweep-113-validate.md`). Prior: Sweep 112 iter 1 (2026-07-18; the loop-break corpus-wide `/validate` over the **#1000..#1019** deltas (diff base #999 `65c5075b`, head #1020 `56889588`) of the 2026-07-17b resumed session, run at the `/resume` from the session-closing handoff #1020 on branch `claude/resume-sweep112-closeout`. **OFFLOADED to worker-20260716-a** (Opus 4.8) as the pre-positioned blocking prio-0 order `sweep-112-validate` pinned `56889588`/#1020, and consumed under full ELEVATED QA (worker-a delivery 1 this fresh 2026-07-18 session, so the session-scoped trust window re-establishes): proof-of-run genuine (~240K tokens, A/B/C returns); independent orchestrator re-derivation of HEAD `56889588`=#1020=pinned SHA, baseline **72/72** on the maintainer clone (the worker's 71/72 is the known-open §1.20 origin-dependent adopt-preflight test, which classifies the worker's nested clone `adopter` so the guard proceeds rc 0, EXPECTED and honestly disclosed, not a discrepancy), counts 13/24/15/18, four-surface parity 72, versions library `2026.07.508`/pack `1.62.1`/spec `1.17.11` all EXACT-MATCH; the 3 superseded-citation residual greps (`23.3`/`ID.AM-3`/`8.15.3`) confirmed zero live carriers; a dedicated delivery-1 false-negative auditor independently re-verified all 4 changed citations at held source (Quebec P-39.1 s. 3.3, ChaCha20-absent-from-SP-800-52r2, CSF 2.0 ID.AM-02, CSF 2.0 DE/RS IDs + ISO 27002 8.17) and returned **CLEAN-CONFIRMED (delta)**. **0 error / 0 warning / 0 new findings in-window.** All #1000-#1019 asserted-clean surfaces (the §1.19.7 machinery, gates 71/72, the Quebec s. 3.3 + §1.17 gate-blind citation fixes, counts/parity) CORROBORATED, 0 contradicted. **Loop-break control for #1020 PASSES.** Worker-a stays ELEVATED (1 of 2-to-3 clean elevated passes this session). The auditor ALSO surfaced one OUT-OF-WINDOW lead (does NOT refute the delta-clean verdict, the file was not in the #1000-#1019 delta): **S112-1**, `security/standard-logging-and-monitoring.md:140-142` cites three non-existent ISO/IEC 27002:2022 sub-controls (`§8.15.3` Time-sync, `§8.15.5` Central-collection, `§8.15.7` Access-and-protection) in a doc that claims 27002:2022, where the held standard's controls are FLAT (no `8.15.x`; `8.15.3` = 0 hits): time-sync is **§8.17** "Clock synchronization" (unambiguous), the other two fold into **§8.15** "Logging"; orchestrator-re-verified at held ISO 27002:2022. **ROUTED to TODO §1.21** (same citation-accuracy class as the closed §1.17 r4 items; fixed as a dedicated near-term PR since two rows are a semantic-fit call). No detail file (worker SARIF-lite source `grc_library_scratch:results/sweep-112-validate.md`). Prior: Sweep 111 iter 1 (2026-07-17; the loop-break corpus-wide `/validate` over the **#992..#998** deltas (diff base #991 `97f4ecb3`) of the 2026-07-17 §1.19 Phase-1 execution sprint, run at the `/resume` from the session-closing handoff #999 on branch `claude/resume-sweep111-closeout`. **OFFLOADED to worker-20260716-a** (Opus 4.8) as blocking prio-0 order `sweep-111-validate` pinned `65c5075b`/#999, and consumed under full ELEVATED QA (worker-a delivery 1 this fresh 2026-07-17b session, so the session-scoped trust window re-established): proof-of-run genuine (~296K tokens, A/B/C returns); independent re-derivation of HEAD `65c5075b`=#999=pinned SHA, baseline **70/70**, pre-flight 422/32/11, four-surface parity 70, counts 13/24/15/18, versions library `2026.07.487`/pack `1.62.0`/spec `1.17.9` all EXACT-MATCH; every finding re-verified at source; a dedicated delivery-1 false-negative auditor ran 10 adversarial checks and returned **CLEAN VERDICT HOLDS**. **0 error / 0 warning / 0 new findings.** The only 2 items are known-tracked notes, both on surfaces #999 did NOT assert clean, so EXPECTED not misses: S111-1 (r4 §1.17 N2+W2, the CSF-1.1 subcategory IDs + ISO 27002 `8.15.3` in `operations/procedure-security-monitoring-and-alert-management.md`) and S111-2 (S110-1, `ID.AM-3`->`ID.AM-02` in `dev-security/standard-software-composition-analysis.md:253`); both tracked in TODO §1.17, now UNBLOCKED by the r4 Phase-8 sign-off. All #992-#998 asserted-clean surfaces CORROBORATED, 0 contradicted. **Loop-break control for #999 PASSES.** Worker-a stays ELEVATED (1 of 2-to-3 clean elevated passes this session). No detail file (worker SARIF-lite source `grc_library_scratch:results/sweep-111-validate.md`). NOTE (self-caught, §3.93): at this resume I first mis-read the scratch coordination plane from the un-synced LOCAL scratch checkout (16 commits behind origin) and wrongly reported both workers stale + the order unclaimed, when worker-a had already delivered on `origin/main`; a scratch `git fetch` corrected it. Fix queued as TODO §3.93 (sync scratch every PR). Prior: Sweep 110 iter 1 (2026-07-17; the loop-break corpus-wide `/validate` over the **#985..#991** deltas of the 2026-07-16c resumed session, run at the `/resume` from #991 on branch `claude/resume-sweep110-validate`. **OFFLOADED to worker-20260716-a** (Opus 4.8) as order `sweep-110-validate` at pinned `97f4ecb3`/#991 (the pre-positioned order was stuck at `status: queued`, the footgun; the orchestrator flipped it to `pending` this resume so a live worker could claim it) and consumed under ELEVATED QA (worker-a delivery 3 post-reset): proof-of-run genuine (~359K tokens, A/B/C subagent returns); independent re-derivation of base #984 `28d4146b`, baseline 69/69, pre-flight 421/33/11, parity 69, counts 13/23/14/18 all EXACT-MATCH; EVERY finding re-verified at source. **0 error / 1 warning / 2 note.** S110-1 (warning, out-of-window, NEW: CSF 2.0 `ID.AM-3`->`ID.AM-02` in `dev-security/standard-software-composition-analysis.md:253`, verified at held CSWP-29) ROUTED to §1.17(d); S110-2 (note, in-window: the #986 CHANGELOG mis-quoted the charter's deleted COBIT title "Managed Data", actual cell was "Manage Data") FIXED this close-out; S110-3 (note, known-open N2) no action. **Worker-a stays ELEVATED this session (1 of 2-to-3 clean elevated passes)** (no red flag, no separate auditor; the session-scoped window resets each session, so the earlier #992 "re-graduated" wording was a cross-session miscount, corrected in #993). All #985-#991 asserted-clean surfaces CORROBORATED, 0 contradicted. **Loop-break control for #991 PASSES.** No detail file (worker SARIF-lite source `grc_library_scratch:results/sweep-110-validate.md`). Prior: Sweep 109 iter 1 (2026-07-16; the loop-break corpus-wide `/validate` over the **#969..#984** deltas of the 2026-07-16b resumed session, run at the `/resume` from #984 on branch `claude/resume-sweep109-validate`. OFFLOADED to credit-offload worker `worker-20260716-b` (Opus 4.8) as order `sweep-109-validate-2` (the pre-positioned `sweep-109-validate` came back BLOCKED on an unreachable pinned SHA before the worker clones were resynced, so it was re-enqueued after the maintainer had the workers resync) and orchestrator-VALIDATED under full ELEVATED QA (worker B's first QA-kind delivery this session): independent re-derivation of HEAD `28d4146b`/#984, baseline 69/69, pre-flight 421/33/11, four-surface parity 69, and counts 13/23/14 all EXACT-MATCH, plus a dedicated adversarial false-negative auditor that re-verified every load-bearing CCPA citation and Quebec Law 25 figure at held source and returned NO MISSED FINDING. **A: 0; B: 0; C: 0** from the sweep; **1 note** surfaced by the elevated-QA auditor (`S109-N1`: `privacy/jurisdictions/annex-privacy-united-states.md:58` rendered the section 7120 cybersecurity-audit threshold as only the (b)(2) 250k/50k prong, omitting the section 7120(b)(1) revenue trigger and the (b)(2) section 1798.140(d)(1)(A) conjunct; verified at the held CCPA Regulations and Civil Code section 1798.140(d)(1)(A)/(C), **FIXED this close-out**, annex `1.2.3`->`1.2.4`). All #969-#983 asserted-clean surfaces CORROBORATED, 0 contradicted. **Loop-break control for #984 PASSES.** Detail [`2026-07-16-sweep109-iter1.md`](validate-sweeps/2026-07-16-sweep109-iter1.md). Prior: Sweep 108 iter 1 (2026-07-16; the loop-break corpus-wide `/validate` over the **#964..#968** deltas of the 2026-07-16 resumed session, run at the `/resume` from #968 on branch `claude/sweep108-closeout`. **The FIRST OFFLOADED sweep**: dispatched to credit-offload worker `worker-20260716-a` (Opus 4.8) as the blocking prio-0 order `sweep-108-validate` and orchestrator-VALIDATED under full ELEVATED QA (new-worker window delivery 1): independent re-derivation of the 5 in-window SHAs, counts, gate 54 (336 docs clean), and pre-flight (421/11/33) all EXACT-MATCH, plus a dedicated adversarial false-negative auditor returning WORKER-CLEAN-CONFIRMED. Mechanical baseline **69/69** at `37abc69`/#968 (verified this resume; a descendant of the asserted green-at `6c28f57`/#967, no close-vs-start drift); clone non-shallow. Pre-flight: 421 files, 33 suppressed, 11 candidates, ALL false positives (the collection-count-word class), dismissed by all three subagents. **A: 0; B: 0; C: 0 blocking; 1 note** (0 error / 0 warning / 1 note). The one note (`credit-offload-phase1-date-drift`: `.working/credit-offload-design.md` phase-1 `2026-07-16` vs TODO §3.80 `DONE 2026-07-15`, in-window, NOT a contradiction of an asserted-clean surface) **FIXED this close-out** (TODO §3.80 aligned to `2026-07-16`, the UTC date per scratch #168's merge), together with the parallel `validate-pr-969` `verify`-enum drift (added `verify` to the §3.80 list). All #964-#967 asserted-clean surfaces CORROBORATED, 0 contradicted. **Loop-break control for #968 PASSES.** No detail file (worker SARIF-lite source `grc_library_scratch:results/sweep-108-validate.md`). Prior: Sweep 107 iter 1 (2026-07-15; the loop-break corpus-wide `/validate` over the **#955..#963** deltas of the 2026-07-15 closing session (Canada AI annex #955/#958, fourth-review procedure/register #956/#959, website #957/#961/#962, TODO cleanup #960), run at the `/resume` from #963 on branch `claude/resume-sweep107-validate`; full three-subagent A/B/C dispatch. Mechanical baseline **69/69** at `23087a3`/#963 (a descendant of the closing session's asserted green-at `674914b`/#962; no close-vs-start drift); clone non-shallow. Pre-flight: 421 files, 33 suppressed, 11 candidates, ALL false positives (the collection-count-word class), dismissed by all three subagents. **A: 0; B: 1 note; C: 0** (0 error / 0 warning / 1 note). The one finding **B-1** (three live carriers of the prose form `(TODO section 2.4)` - `.web/build.py:2`, `.github/workflows/web-generator-health.yml:4`, `.working/cloudflare-pages-setup.md:1` - survive #960's §2.4 closure, since #960's residual grep was `§`-glyph-only and TODO-scoped) **CONTRADICTS** the #955-#962 handoff's asserted-clean "§2.4/§2.6 residual 0 across the whole repo" claim (a genuine low-severity miss on key-form + file-type width, the §N-orphan-guard class), so escalated + **FIXED this close-out #964** (stale pointer dropped from all three; the two frozen `CHANGELOG-detailed.md:727,752` historical carriers correctly left). All other asserted-clean surfaces CORROBORATED, 0 contradicted. **Loop-break control for #963 PASSES.** Detail [`2026-07-15-sweep107-iter1.md`](validate-sweeps/2026-07-15-sweep107-iter1.md). Prior: Sweep 106 iter 1 (2026-07-15; the loop-break corpus-wide `/validate` over the **#943..#958** deltas of the 2026-07-15 resumed session (website #943-#954 and #957, Canada AI annex #955 and #958, procedure "third review" fix #956) plus the cross-repo `grc_library_ref` HIGH-AI ingest (ref PR #80), run at the deferred `/validate` on branch `claude/sweep106-register-ordinal-fix`; full three-subagent A/B/C dispatch. Mechanical baseline **69/69** at `b1828c6`/#958; clone non-shallow. Pre-flight: 421 files, 33 suppressed, 11 candidates, ALL false positives (the collection-count-word class), dismissed by all three subagents. **A: 1 warning; B: 0; C: 0 blocking + 1 note** (0 error / 1 warning / 1 note). The one finding **A-1** (`governance/register-canonical-citations.md:148` labelled the 24 June 2025 TBS Directive on ADM transition "third-review" via the hyphenated spelling the earlier space-form grep missed; it is the fourth review) **FIXED this close-out #959** (`third-review`->`fourth-review`, register `1.5.36`->`1.5.37`); C's note (the `.working/` handoff version-snapshot lag) routed to the session-closing handoff refresh. NOT a contradiction of the #954 handoff's website-only asserted-clean set, so an ordinary in-window finding. **Loop-break control for #954 PASSES**. Detail [`2026-07-15-sweep106-iter1.md`](validate-sweeps/2026-07-15-sweep106-iter1.md). Prior: Sweep 105 iter 1 (2026-07-15; the loop-break corpus-wide `/validate` over the **#918..#942** deltas of the 2026-07-15 long resumed session's closing PRs, run at the `/resume` from #942 on branch `claude/resume-sweep105-validate`; full three-subagent A/B/C dispatch. Mechanical baseline **69/69** at `7651a1b`/#942 (a descendant of the closing session's asserted green-at `3fc2a0c`/#941; no close-vs-start drift); clone non-shallow. Pre-flight: 421 files, 33 suppressed, 11 candidates, ALL false positives (the collection-count-word class), dismissed by all three subagents. **A: 1 warning; B: 0; C: 0** (0 error / 1 warning / 0 note; 1 asserted-expectation contradiction). The one finding **A-1** (`.web/build.py:306` sorted domain-page docs within a type case-SENSITIVELY while `tools/build-taxonomy.py:96` sorts case-INSENSITIVELY with a path tiebreaker, so the on-site domain pages diverged from the canonical taxonomy/portal order in ~6 domains, e.g. compliance listed "eIDAS ... Annex" last vs among the E-annexes) **CONTRADICTS** the #942 handoff's asserted-clean "#940 ... consistent" claim (TYPE_ORDER itself IS byte-identical; the "consistent" header was the miss), so a genuine miss escalated + **FIXED this close-out** (`.web/build.py` sort key aligned to `(rank, title.lower(), path)`; `--check` EXIT=0). Follow-up routed: extend §3.76 TYPE_ORDER DRY guard to the secondary key. All other asserted-clean surfaces CORROBORATED, 0 contradicted. **Loop-break control for #942 PASSES**; one in-window user-visible defect found + fixed, no other escaped adopter-facing defect. Detail [`2026-07-15-sweep105-iter1.md`](2026-07-15-sweep105-iter1.md). Prior: Sweep 104 iter 1 (2026-07-14; the loop-break corpus-wide `/validate` over the **#915..#917** deltas of the 2026-07-14 resumed session's closing PRs (#915 Sweep 103 close-out + gate-50 Check-5 §6 fix; #916 P1 §1.2 root-CHANGELOG reformat + readability-guard strengthening; #917 session-closing handoff), run at the `/resume` from #917 on branch `claude/cloudflare-public-website-2d7sis`; full three-subagent A/B/C dispatch. Mechanical baseline **69/69** at `9cb42fe`/#917 (a descendant of the closing session's asserted green-at `7596f18`/#916; no close-vs-start drift); clone non-shallow. Pre-flight: 421 files, 33 suppressed, 11 candidates, ALL false positives (the collection-count-word class), dismissed by all three subagents. **A: 2 note; B: 2 note; C: 1 warning** (0 error / 1 warning / 4 note). **Asserted-expectations cross-check: all CORROBORATED, 0 contradicted.** The 4 in-window findings, all docstring/rationale accuracy in two `tools/` files (C-1 gate-50 module docstring "four checks"->"five"; A-1 total-vs-sentence word conflation; A-2 sentence-splitter regex omitting `?`/`!`; B-1 recycled `§1.2` provenance ref), FIXED this close-out (tool self-test 6/6, gate 50 re-run OK); B-2 (`compliance/procedure-capa.md` Source-field, out-of-window, low-confidence) routed to the maintainer. Loop-break control for #917 **PASSES**; no in-window escaped adopter-facing defect. Detail [`2026-07-14-sweep104-iter1.md`](validate-sweeps/2026-07-14-sweep104-iter1.md). Prior: Sweep 103 iter 1 (2026-07-14; the loop-break corpus-wide `/validate` over the **#901..#914** deltas of the 2026-07-14 r3-remediation session, run at the `/resume` from #914 on branch `claude/resume-sweep103-validate`; full three-subagent A/B/C dispatch. Mechanical baseline **69/69** at `f40a042`/#914 (verified this resume); clone non-shallow. Pre-flight: 421 files, 33 suppressed, 11 candidates, ALL false positives (the collection-count-word class), dismissed by all three subagents. **A: 0; B: 0; C: 1 note** (0 error / 0 warning / 1 note). **Asserted-expectations cross-check: all CORROBORATED, 0 contradicted.** The 1 note (the gate-50 §6 detailed-prose paragraph omitted the #913 Check-5 register row-order guard, breaking the checks-3/4 precedent, the same free-prose class as Sweep 77's gate-57) FIXED this close-out (fifth-check sentence added, matching the checks-3/4 precedent; spec `1.17.3`->`1.17.4`, taxonomy+scorecard regenerated). Loop-break control for #914 **PASSES**; no in-window escaped defect. Detail [`2026-07-14-sweep103-iter1.md`](2026-07-14-sweep103-iter1.md). Prior: Sweep 102 iter 1 (2026-07-14; the loop-break corpus-wide `/validate` over the **#887..#900** deltas of the 2026-07-13 deep-assessment r3 session, run at the `/resume` from #900 on branch `claude/resume-sweep102-validate`; full three-subagent A/B/C dispatch. Mechanical baseline **69/69** at `95a2772`/#900 (verified this resume); clone non-shallow. Pre-flight: 421 files, 33 suppressed, 11 candidates, ALL false positives (the collection-count-word class), dismissed by all three subagents. **A: 0; B: 0; C: 0** (0 error / 0 warning / 0 escalate-class; C added 6 confirming notes). **Asserted-expectations cross-check: all CORROBORATED, 0 contradicted.** A verified all 9 #899/#900 corpus edits at source (DORA Art 11/12 split corroborated against held text, EU AI Act Art 26/53/55/Annex XI, EDPB 05/2020 date precision, FFIEC CAT retirement) with Version+Date co-bumps and generated artefacts in sync; B confirmed DA-DORA-A12/A26 corpus residual 0 and no count drift (13/23/14/69); C confirmed four-surface parity at gate 69, regression suite 373 OK, and the #890 dual-header + #897 nightly-sweep SHA-pin intact. The r3 HIGH findings (DA-ASVS, FR-200/201, FR-202..205, remaining mediums/lows) are KNOWN-OPEN, expected, not misses. One scope note: the AICPA TSP 100 register row A re-checked was added out-of-window (#876), not a defect. **Close-out mechanical re-baseline caught 1 in-window bookkeeping finding: PR #900 omitted its handoff-exemption row (gate 50 + the gate-36 smoke test failed once #901's CHANGELOG demoted #900 from highest-numbered), FIXED this PR (added the #900 `SKIPPED (handoff-PR exception)` row); re-baseline then 69/69.** Loop-break control for #900 **PASSES**; no in-window escaped defect. Subagent-clean sweep, no detail file. Prior: Sweep 101 iter 1 (2026-07-13; the loop-break corpus-wide `/validate` over the **#852..#886** deltas of the 2026-07-12/13 resumed sweep100 session, run at the `/resume` from #886 on branch `claude/resume-sweep101-validate`; full three-subagent A/B/C dispatch. Mechanical baseline **69/69** at `fbd0162`/#886 (a descendant of the closing session's asserted green-at `503fda1`/#885; no close-vs-start drift); clone non-shallow. Pre-flight: 421 files, 33 suppressed, 11 candidates, ALL false positives (the collection-count-word class), dismissed by all three subagents. **A: 2 note; B: 0; C: 0** (0 error / 0 warning / 2 note). **Asserted-expectations cross-check: all CORROBORATED, 0 contradicted.** A held-verified every newly-cited §3.57 instrument's version/date/anchor against the held source text and confirmed the 3 self-caught slips (#879/#882/#884) left zero residual; B refuted the closed-§/count-drift/instrument-version/cross-tree classes corpus-wide; C confirmed four-surface parity at gate 69, gate-37 17/0 overlay, gate 60 clean, the compact-CHANGELOG-header parity-linter widening EXPANDED-not-weakened, counts 69/13/23/14/18. The 1 in-window note (`register-canonical-citations.md:196` AICPA TSP 100 held copy is the March-2020 edition while the register cites the upstream-verified 2022-revised-POF edition) ROUTED to TODO RB-6(e) (register-only, citation real-world-accurate, low risk; flagged for `/deep-assessment` `/reference-audit`+`/claim-fit`); the other note (handoff count-attribution scoping) is a frozen-record no-action. Loop-break control for #886 **PASSES**; no in-window escaped defect. Detail [`2026-07-13-sweep101-iter1.md`](2026-07-13-sweep101-iter1.md). Prior: Sweep 100 iter 1 (2026-07-12; the loop-break corpus-wide `/validate` over the **#834..#851** deltas of the 2026-07-12 resumed sweep99 session (§4.8 pack-hygiene phases 1-4 #835-#846, AI-delta EU cluster + currency #843-#850, matrix-fit #840, next-prs codification #848, session-closing handoff #851), run at the `/resume` from #851 on branch `claude/resume-sweep100-validate`; full three-subagent A/B/C dispatch. Mechanical baseline **69/69** at `6fd59c7`/#851 (a descendant of the closing session's asserted green-at `ebceadc`/#850; no close-vs-start drift); clone non-shallow. Pre-flight: 421 files, 33 suppressed, 11 candidates, ALL false positives (the collection-count-word class), dismissed by all three subagents. **A: 1 note; B: 3 note; C: 0** (0 error / 0 warning / 4 note, plus 1 refuted ETSI flag). **Asserted-expectations cross-check: all CORROBORATED, 0 contradicted.** The 4 notes: A's 1 in-window (EU annex frames GPAI at statute-level, does not name the Article 56 Codes-of-Practice route) ADJUDICATED by-design no-fix (the Article 56 framing is carried in `framework-ai-model-documentation-and-transparency.md §4.4` + `policy-ai-compliance.md §6`, where the statute-level annex routes operational detail; optional one-line cross-reference enhancement offered, recommendation leave-as-is); B's SP 800-154 "held source" note already-tracked §1.2; B's 2 pack-project-flavour notes (change-tracking.md example paths, project-integrity.md "consuming GRC library" naming) routed to §3.56; A's ETSI `EN 304 223 V2.1.1` un-verifiable flag REFUTED (ref catalogue confirms the designation, verified 2026-07-04; TS 104 223 is the superseded predecessor). A held-verified every in-window EU AI Act citation verbatim against the held Regulation 2024/1689 + the GPAI chapters + DORA RTS 2025/301; C confirmed four-surface parity + §5/§6 detailed-prose + docstring + fixture at gate 69, counts 69/13/23/14/18, gate-37 13/0 overlay, gate 60 clean (drift < 3). Loop-break control for #851 **PASSES**; no in-window escaped defect. Detail [`2026-07-12-sweep100-iter1.md`](2026-07-12-sweep100-iter1.md). Prior: Sweep 99 iter 1 (2026-07-12; the loop-break corpus-wide `/validate` over the **#828..#833** deltas of the 2026-07-12 sweep98 OVERNIGHT session (Sweep 98 close-out #828, §3.48 DORA window fix #829, §3.51 reference-breadth #830, §3.52 42001/27001 clause-8.4 remaps #831, §3.53 SOP pack-distribution #832, session-closing handoff #833), run at the `/resume` from #833 on branch `claude/resume-sweep99-validate`; full three-subagent A/B/C dispatch. Mechanical baseline **69/69** at `60ea553`/#833 (a descendant of the closing session's asserted green-at `7cb5752`/#832; no close-vs-start drift); clone non-shallow. Pre-flight: 418 files, 35 suppressed, 11 candidates, ALL false positives (the collection-count-word class), dismissed by all three subagents. **A: 1 warning; B: 1 warning (SAME finding, independent convergence); C: 0** (1 in-window warning, 0 error, 0 note). **Asserted-expectations cross-check: all CORROBORATED, 0 contradicted.** The one finding: the DORA ICT-incident-reporting cell at `annex-financial-services-sector-requirements.md:118` under-stated the major-incident initial-notification window (only the 4h-from-classification bound), the parallel carrier the §3.48 fix (#829) corrected in the dedicated DORA annex but did not scan; **FIXED this close-out** (line 118 reworded to both statutory bounds, 4h-from-classification AND 24h-from-awareness, plus intermediate/final anchors and the RTS 2025/301 citation, mirroring the verifier-SHIP'd #829 text; annex `1.0.8`→`1.0.9`, artefacts regenerated). NOT a contradiction of an asserted-clean surface (the #833 handoff asserted the DORA *annex* clean, which it is; the *sector* annex was not in the assertion set), so an ordinary in-window finding. C confirmed four-surface parity + §5 grouped-list + §6 detailed-prose + docstring + fixture at gate 69, counts 69/13/21/14/18, gate-37 SOP mirror byte-identical, gate 60 clean (drift 1 < 3). Loop-break control for #833 **PASSES**. Detail [`2026-07-12-sweep99-iter1.md`](2026-07-12-sweep99-iter1.md). Prior: Sweep 98 iter 1 (2026-07-12; the loop-break corpus-wide `/validate` over the **#822..#827** deltas of the 2026-07-11/12 sweep97 session (Sweep-97 close-out #822, deep-assessment r2 record + gate-60 reset #823, gate 69 #824, F1 42001 §8.4 fix #825, r2 follow-ups + missing-ref SOP #826, session-closing handoff #827), run at the `/resume` from #827 on branch `claude/resume-sweep98-validate`; full three-subagent A/B/C dispatch. Mechanical baseline **69/69** at `d3dd480`/#827 (a descendant of the closing session's asserted green-at `711a108`/#826; no close-vs-start drift); clone non-shallow. Pre-flight: 418 files, 35 suppressed, 11 candidates, ALL false positives (the collection-count-word class), dismissed by all three subagents. **A: 2 note; B: 2 note; C: 0** (0 error / 0 warning / 4 note, all out-of-window). A verified the F1 42001 §8.4 relabel semantically correct and complete across all 9 carriers + gate 69 + the SOP/F2/F3/F4 clean; B refuted the §3.4-orphan / gate-count-drift / 42001-§8.4-consistency / closed-section-orphan / AIDA-present-tense / OECD-2023 classes corpus-wide (discriminating the ISO/IEC 5259-4:2024 §8.4 refs as a legitimate different-standard citation); C confirmed four-surface parity + §5 grouped-list + §6 detailed-prose + docstring + fixture at gate 69, counts 69/13/21/14/18, the gate-60 reset, and gate 68 not regressed. **Asserted-expectations cross-check: all CORROBORATED, 0 contradicted.** A's 2 notes = already-tracked TODO §3.52 (a) transparency→42001-§8.4 across 5 annexes + (b) non-existent 27001-§8.4 across 3 matrix rows, NO action; B's 2 notes = closed-§ docstring breadcrumbs FIXED this PR (`audit-register-currency.py`→#817, `block-verification-pipes.py`→#620), plus 2 more §1.9 carriers of the same class caught by the orchestrator's file-type-width grep (`pre-push-guard.sh`, `tail-safe.sh`) FIXED (#620), zero residual. Loop-break control for #827 **PASSES**. Detail [`2026-07-12-sweep98-iter1.md`](2026-07-12-sweep98-iter1.md). Prior: Sweep 97 iter 1 (2026-07-11; the loop-break corpus-wide `/validate` over the **#790..#821** deltas of the 2026-07-11 sweep96 session (AI-workstream PR 1-10 incl. gate 68, all P1 §1.7-§1.11 closes, §2.2/§3.17/§3.46, the §4.8 pack-hygiene consolidation + new §1.1), run at the `/resume` from session-closing handoff #821 on branch `claude/resume-sweep97-validate`; full three-subagent dispatch A/B/C. Mechanical baseline **68/68** at `10cff59`/#821 (a descendant of the closing session's asserted green-at `68f1aa3`/#820; no close-vs-start drift); clone non-shallow. Pre-flight: 418 files, 35 suppressed, 11 candidates, ALL false positives (the collection-count-word class), dismissed by all three subagents. **A: 0; B: 0; C: 0 findings** (0 error / 0 warning / 0 note). A held-verified the new-AI-doc citations (ISO/IEC 42005 Annex C/D, 5338 6.4.14, 23894 Annex B, OSFI E-23, EU AI Act Art 27/113); B refuted the closed-section-orphan / term-drift / count-drift classes corpus-wide; C confirmed four-surface parity + detailed-prose + docstring + fixture at gate 68 and counts 13/21/14/18. **Asserted-expectations cross-check: all CORROBORATED, 0 contradicted.** Loop-break control for #821 **PASSES**. Zero-finding sweep, no detail file. Prior: Sweep 96 iter 1 (2026-07-10; the loop-break corpus-wide `/validate` over the **#780..#788** deltas, run at the `/resume` from session-closing handoff #789 on branch `claude/resume-sweep96-validate`, UNATTENDED on the VM; full three-subagent dispatch. Mechanical baseline **67/67** at `e13835a`/#789 (= `origin/main`), a descendant of the closing session's asserted green-at `e3b155c`/#788 (no close-vs-start drift); clone non-shallow. Pre-flight: 410 files, 35 suppressed, 11 candidates, all FALSE POSITIVES (the standing comparative/historical phrasing class), dismissed by all three subagents. **A: 1 warning + 1 note; B: 2 warning + 1 note; C: 0 (1 by-design note).** **Asserted-expectations cross-check: 3 of 4 CORROBORATED, 1 CONTRADICTED (soft, escalated).** Corroborated: `AI-XX-NN`=0, `ISO/PAS 8800`=0 in corpus bodies, "15 business day"(Art-73)=0, Art 73 deadlines 15/2/10 correct, §36 CSA AICM codes all valid AICM v1.1.0, ledger integrity intact, gate-48 Check-5 four-surface COMPLETE, gate count 67 CONSISTENT, counts 13/21/14 MATCH. CONTRADICTED: the #789 handoff's "zero present-tense-AIDA carriers **corpus-wide**" over-claim, refuted by the live carrier at `dev-security/standard-security-baseline-and-standards-reference.md:115` (a soft miss: the same handoff's §1.10 already tracked this `:115` carrier as remaining, so the wording over-reached, not an unnoticed defect). Routed: TODO §1.10 broadened to a corpus-wide AIDA-framing reconcile (6 carriers) + a NEW EO-14110-present-tense-despite-rescission bullet (accepted-unverified, Federal-Register-confirm). Fixed in-window: `TODO.md:218` closed-§ provenance refs reworded to stable PR refs (#781/#782/#769). Loop-break control for #789 **PASSES** (over-claim caught, defect already tracked; one novel untracked catch). Detail [`2026-07-10-sweep96-iter1.md`](2026-07-10-sweep96-iter1.md). Prior: Sweep 95 iter 1 (#761..#778 deltas, 8 findings all fixed/routed in the #780 close-out, loop-break control for #779 PASSED; [`2026-07-10-sweep95-iter1.md`](2026-07-10-sweep95-iter1.md)). Prior: Sweep 94 iter 1 (#753..#759 deltas, 4 findings all fixed in the #761 close-out, loop-break control for #760 PASSED; [`2026-07-10-sweep94-iter1.md`](2026-07-10-sweep94-iter1.md)).)

## Next actions (the queue for the next session)

**CLOSING (2026-07-19b resumed session; `/resume` from #1043; merged #1044-#1053; closes at this session-closing handoff PR #1054; on the VM, gh-CLI no GitHub MCP; ATTENDED throughout, a major maintainer-directed AI-orchestration self-governance + adopter-portability design session).** Shipped #1044 (§1.19.12 CLAUDE.md sensitivity-trim), #1045 (self-guard bundle), #1046 (QA + captures), #1047 (§1.22.2 `.directories` cleanup), #1048 (§1.22.5 CHANGELOG daily-tier), #1049 (decision-guardrail: act/ask/name-a-blocker + write-before-enact log), #1050 (credit-offload public tail: §3.87 socket revision + programmatic-restart/asymmetric-trust), #1051 (**MANDATORY-OFFLOAD guardrail**: the primordial "you are the orchestrator, orchestrate" rule + `block-mandatory-offload.py` hook; its first offloaded verify caught a real MEDIUM the orchestrator missed), #1052 (§3.70 crypto tables to P-384/RSA-4096), #1053 (§3.69 MFA type-anchored + §4.30 adopter umbrella). Plus scratch PR #173 (credit-offload liveness) + several `_private` pushes (orchestrator-claude, credit-offload-design, design-decisions incl. the `/home/grc` migration design, autonomous-decisions-log, degradation-watch). **Sweep 115 loop-break control ran PRE-CLOSE this session (maintainer-directed, several-day gap): PASS, 0 error / 0 warning; N1/N2 fixed, N3 (§3.70 pack-vs-corpus) tracked, N4 (`_private` coverage) orchestrator-verified.** Lease RELEASED by this handoff.

**NEXT SESSION (2026-07-19b close; likely several days out, orchestrator credits exhausted until ~Wednesday).** (0) fixing any surfaced issue outranks the queue. (1) At `/resume`: the corpus-wide `/validate` over **#1044..#1053 ALREADY RAN this session (Sweep 115 PASS)**, so do NOT re-run it over that window; run a fresh `/validate` only over any NEW deltas since #1053. (2) **THE BIG NEXT-SESSION BUILD, the `/home/grc` migration + file-drop transport + worker switch** (maintainer-directed; full design in `grc_library_private/design-decisions.md` "`/home/grc` multi-user migration + same-VM file-drop worker model"): (a) VERIFY+APPLY the **transport-finalize the worker DELIVERED this session** (durable in `grc_library_scratch` `inbox/`/`results/research-homegrc-transport-finalize.md`), the `/home/grc/grc_working/{opus,fable,codex}` per-family file-drop serve-loop + orchestrator dispatch helper + substructure; (b) the MAINTAINER runs the sudo repo-move to `/home/grc` + the `grc` group + worker#/codex users, the orchestrator does the in-repo path-reference edits; (c) then the maintainer `/clear` + `/credit-offload` to switch the workers onto the file-drop. The `/home/grc/grc_working/{opus,fable,codex}` dirs are ALREADY CREATED (group-permissioned). (3) **§4.30 adopter-experience assessment build** (full clean-clone assessment + `.adopt` kit + move DONE/improvement-log to `_private` with gate resolvers + `/adopt` recreate; coordinate §4.1/4.5/4.6/4.9/4.29). (4) Codex-worker integration (the `/home/grc/grc_working/codex` subtree; model-routing intelligence). (5) The **decision-preload protocol** codification (the "AI-only-works-when-watched" per-merge decision-scan + presence-flush; drafted in `scratchpad/decision-preload-draft.md`, memory saved). (6) Resolve pending-decisions: the **§3.70 pack-vs-corpus crypto divergence** (confirm-or-redirect), the `:333` SEF-06/07 apply, the worker-a elevated-QA recalibration (RESOLVED this session: only-substantive-misses-reset the 1-pass window). (7) Then the deferred queue: §1.16 COBIT, §1.22.3/4/7, §3.102, §2.24 content, then the P3 wave. **Workers:** separate Unix users (worker1/worker/codex), Opus 4.8, live (separate credit pool, still had budget at close). **MANDATORY-OFFLOAD rule ACTIVE:** verification and all worker-level work is offloaded; if no workers on a VM, ALERT the maintainer or get explicit authorization. **Throughput today:** ~15 PRs merged (#1039-#1053 across both 2026-07-19 sessions).

**CURRENT (2026-07-19b resumed session; `/resume` from #1043; on the VM, gh-CLI no GitHub MCP; ATTENDED, maintainer actively directing).** First PR **#1044 = §1.19.12 CLAUDE.md sensitivity-trim** (1699->1262 lines; operational mechanics + war-stories moved to `grc_library_private/orchestrator-claude.md` loaded every `/resume` + the removal ledger; the portable governance spine stays public). Worker apply-draft (worker-a deterministic script) + orchestrator hunk-by-hunk audit + 2 independent verifiers (in-session full-access CONFIRMED-CLEAN + worker-b public-side). Carries the Sweep 114 loop-break close-out (PASS for #1043; F1 = TODO §1.22.6 rotation-debris deleted) + this handoff prune + lease acquire.

**NEXT (maintainer-LOCKED order; updated through #1048).** DONE this session: **#1045** self-guard bundle (repeat-block PreToolUse hook + diagnosis circuit-breaker + evidence-validated `grc_library_private/degradation-watch-log.md` + read-back / intent-vs-artefact CLAUDE.md disciplines); **#1046** QA batch + TODO **§4.29** (`/adopt` adjustment/tooling-update mode) and **§2.24** (Governance Relationship and Flow Modelling Framework, scoped turnkey) captures + a stale P2-counter fix; **#1047** root `.directories` cleanup (**§1.22.2** expanded from a README reword to DELETE the `.ref`/`.private`/`.scratch` stub dirs; gate 70 -> guard-if-present-as-stub; `/adopt` creates-on-request; pack `1.62.3` + spec `1.17.14`); **#1048** §1.22.5 CHANGELOG daily-tier (public root current-week condensed to a daily model, current day per-PR, six previous days to daily summaries, mirror pruned to match gate 59). NEXT: (1) **CREDIT-OFFLOAD BUNDLE:** fix alert 2026-07-19-a (idle-worker heartbeat) + CLEAR it on merge (maintainer-authorized); elevated-QA window -> **1 delivery**; worker session-**START-TIME** at registration/check-in; orchestrator **RESTART-ADVICE** duty; **STALL THRESHOLD = 20 min** (not the 5-min re-stamp window); a **MID-PROCESS heartbeat every 5 min carrying PROGRESS** that resets the stall window. Touches the scratch `/credit-offload` + queue tool + `_private/credit-offload-design.md`. (2) apply the 4 remaining research seeds; §1.16 COBIT guard-first; then §1.22.3/4/7, §1.22.8 (chat-pacing, DISCUSS), §1.22.9, §3.102, §1.1/§1.18. **CONTENT TIER (author later):** §2.24 Governance Relationship and Flow Modelling Framework (new Framework-type doc + reframing edit; ONE coherent PR). **MAINTAINER-GATED/LAST:** §1.19.13 history scrub. **DISCUSS post-merge:** the worker read-only FS-access model (`/home/grc` shared-parent owned by jposluns + group-read for workers; `_private` moved there but group-read OFF, toggleable; SHA-pinned reads via `git show`; details in `scratchpad/post-merge-advice-worker-fs-access.md`). Workers both live Opus 4.8, restarted this session. Throughput: >=10 items/day, quality > speed; per-priority counts + daily tally after every merged PR. PR-splitting: one coherent on-topic task = one PR (do not over-split; maintainer-directed 2026-07-19).

**CLOSING (2026-07-19 resumed session; `/resume` from #1039; merged #1040-#1042; closes at this session-closing handoff PR #1043; on the VM, gh-CLI no GitHub MCP; started attended-autonomous -> overnight-unattended -> EARLY WIND-DOWN on a named-degradation signal, converted to a proper session-closing handoff).** Ran the Sweep 113 loop-break close-out (#1040, offloaded to worker-b + elevated-QA CLEAN, PASS for #1039) with the handoff prune + lease acquire. Shipped **both guardrails the maintainer directed**: #1041 the **answered-question guardrail** (§1.22.6: `block-answered-question.py` PreToolUse hook + `decisions-search.py` + a CLAUDE.md "search decisions before asking" discipline, after a same-session re-ask-already-decided failure) and #1042 the **wrong-repo tool guardrail** (§1.22.1: `block-wrong-repo-tool.py` blocks a cwd-relative sibling-tool invocation from the wrong cwd; it re-caught the exact slip in production). #1043 codifies the **degradation-auto-handoff discipline** (a degradation signal in unattended mode = an automatic session-closing handoff, NOT a bare pause) + batches #1042 QA + this refresh + lease RELEASE. **Maintainer collaboration this session was extensive** (project-governance KEEP, `.working` cycle-out design, the TODO Maintainer-or-Egress-Gated restructure §1.22.7, worker-efficiency fan-out, the CHANGELOG daily-tier §1.22.5, the §3.87 unix-socket architecture assessment, the chat-pacing issue §1.22.8). **Worker fan-out was highly productive: all 5 research seeds delivered** (see NEXT SESSION). **Wind-down honesty (for the record):** the trigger was a density of SELF-CAUGHT slips in one enormous un-reset marathon turn (a `git reset --hard` on a feature branch; 5 command-composition slips where the new wrong-repo hook fought the persisted-cwd habit) plus this conversation's extreme length, NOT diffuse degradation, the slips were specific + fixable (lessons in the #1042 retro). The maintainer flagged that hitting issues should trigger a proper wind-down (not a pause) AND that 3 PRs is atypically early; the honest read is the raw un-reset context length was the real residual, so a fresh `/resume` is the clean insurance, and #1043 codifies doing the handoff (not a pause) so it recurs cleanly.

**NEXT SESSION (2026-07-19 close):** (0) **fixing any surfaced issue outranks the queue** (standing). (1) At `/resume`, run the compensating-control corpus-wide **`/validate` over the #1040..#1043 deltas** (cross-check the Asserted-expectations block below). (2) **Resolve pending-decisions FIRST**: the **worker-a elevated-QA escalation** (worker-a's validate-pr-1041 delivery missed F2, caught by the delivery-1 auditor; worker-a stays UNVALIDATED, surface for the maintainer, recorded in `pending-decisions.md` with a possible protocol-refinement question) + `:333` SEF-06/SEF-07 apply. (3) **APPLY THE 5 DELIVERED RESEARCH SEEDS** (high-value, ready in `grc_library_scratch` `results/` + `inbox/`; each = orchestrator re-authors + verifies at source + applies as its own QA'd PR): **`research-changelog-daily-summaries`** -> §1.22.5 (the CHANGELOG daily-tier, maintainer wanted it SOON); **`research-cobit-title-normalization`** -> §1.16 (guard-first: build the title-text validation gate, THEN apply the past-participle normalization from the carrier-list; recount carriers at build); **`research-content-forks`** -> §3.68/§3.69/§3.70/§3.74 (each its own PR + skeptical verifier + Version/Date co-bump); **`research-canada-ca-urls`** -> add the CONFIRMED URLs to the `_private` maintainer-egress list + §1.22.9 (mark UNCONFIRMED ones for the maintainer). (4) Then the rest of the §1.22 block: §1.22.2 (`.ref`/`.scratch`/`.private` README fallback-wording fix), §1.22.3 (`.working` cycle-out tool + reporter + session-boundary policy + initial sweep), §1.22.4 (cross-repo reference-existence advisory tool, reuse gate 3), §1.22.7 (build the single Maintainer-or-Egress-Gated TODO section), §3.102 (pack-distribute the degradation-auto-handoff discipline). (5) Then §1.1 (discussion-vs-execution gate DESIGN), §1.18 (change-impact map + D8 pilot), then §1.19.12/§1.19.13 (maintainer-gated), then P1/P3. **DISCUSS with the maintainer (from §1.22.8 + the socket thread):** the chat text-pacing convention, and the §3.87 unix-socket transport design. **Workers:** both live Opus 4.8; **worker-a UNVALIDATED** (full ELEVATED QA + a delivery-1 auditor on its next QA delivery; escalation in pending-decisions); worker-b elevated (1 clean pass this session, re-establishes next session). **Keep the worker research pipeline FULL** (the maintainer's efficiency directive: dispatch research/draft seeds for the upcoming items so workers never idle). **Stale artefact to clean:** a `validate-pr-1042` order sits in the scratch `queue/` (never pushed cleanly; #1042 QA was self-run instead) plus a local untracked copy, delete it. **The prior-locked track still pending:** §1.19.12 (CLAUDE.md sensitivity-trim apply, SENSITIVE, classification locked in `_private/claudemd-trim-review-draft.md`) -> §1.19.13 (history scrub, MAINTAINER-GATED, LAST). Maintainer-alert channel ARMED, no open alerts. **Throughput (maintainer-directed):** >=10 TODO items/day, quality > speed; present per-priority counts + daily tally after every merged PR.

_Session history older than the current and immediately-prior session is pruned at each resume (keep current + 1 prior, per `## Refresh and pruning discipline` below). The durable record of pruned sessions lives in [`CHANGELOG.md`](../CHANGELOG.md) and its detailed mirror, [`DONE.md`](DONE.md), [`validate-sweeps/history.md`](validate-sweeps/history.md), [`session-metrics.md`](session-metrics.md), and git history._

## State snapshot (2026-07-19b SESSION-CLOSING at #1054; on the VM)

- **Current truth (verify against live files at `/resume`)**:
  - **Session / mode:** resumed from #1043; ATTENDED throughout; SESSION-CLOSING at #1054. Lease RELEASED (`Status: released`, `Active-session: none`); the next `/resume` sets its own mode.
  - **Version snapshot (D7 validates these tokens):** library `2026.07.542`, README `1.9.903` (this #1054 handoff bumps them); pack `1.62.3`, spec `1.17.14` (unchanged this session, no pack/spec/gate/rule/skill/command/doc-type count change); gate **72**, rules **13**, skills **24**, commands **15**, Document-types **18** (all confirmed by Sweep 115's hand-count).
  - **Green-at:** the #1053 merge `dedb17a1` (72/72; the corpus-wide **Sweep 115** `/validate` PASSED 0 error / 0 warning over the #1044..#1053 delta, so no error sits over the multi-day gap). This #1054 handoff is working-state + CHANGELOG + version only; `main` stays 72/72 at its descendant merge.
  - **Shipped this session (grc_library):** #1044-#1053 (see the CLOSING block). Corpus content: #1052 §3.70 crypto (P-384/RSA-4096), #1053 §3.69 MFA (type-anchored). Everything else is `.claude/` guardrails + `.working` + CHANGELOG + TODO.
  - **Workers / research:** worker-a + worker-b live Opus 4.8 (separate credit pool, still had budget at close). worker-a delivered the §3.69/§3.70 seeds + the pre-merge verifies + the transport-finalize; worker-b delivered Sweep 115. **The `/home/grc` transport-finalize is DELIVERED and durable** in scratch (verify+apply next session). No open `MAINTAINER_ALERT`.
  - **Queue:** `pending-decisions.md`: the §3.70 pack-vs-corpus crypto divergence (confirm-or-redirect) + `:333` SEF-06/07 apply. The big next-session build (`/home/grc` migration + file-drop transport + §4.30 adopter-experience assessment + Codex-worker integration + decision-preload codification) is in the NEXT SESSION block above.

## State snapshot (2026-07-19b resumed session, #1044+; on the VM)

- **Current truth (verify against live files at `/resume`)**:
  - **Session / mode:** resumed from #1043; ATTENDED (maintainer actively directing). On the VM (gh-CLI, no GitHub MCP). Lease ACQUIRED (`Status: active`, `Active-session: claude/119-12-claudemd-trim`, `Operating-mode: attended-autonomous`).
  - **Version snapshot (D7 validates these tokens):** library `2026.07.537`, README `1.9.898`, pack `1.62.3`, spec `1.17.14`; gate **72**, rules **13**, skills **24**, commands **15**, Document-types **18**. (Session 2026-07-19b through #1047: #1045 self-guard bundle, #1046 QA plus captures, #1047 root `.directories` cleanup, deleted the 3 sibling stubs and made gate 70 guard-if-present, bumping pack to `1.62.3` and spec to `1.17.14`; gate/rule/skill/command/doc-type counts unchanged.)
  - **Green-at:** #1045 (72/72; self-guard bundle applied clean; `grc_library_private` `validate.py` green; verify post-merge).
  - **Shipped this session:** #1044 (§1.19.12 CLAUDE.md sensitivity-trim, 1699->1262 lines, + the Sweep 114 loop-break close-out). The `grc_library_private` companion commit `f328118` adds `orchestrator-claude.md` (the loaded operational extension) + the removal-ledger archive + the INDEX entry.
  - **Workers:** both live Opus 4.8, RESTARTED by the maintainer this session (~07:16 Toronto). worker-a drafted the deterministic apply; worker-b QA'd the public trim. Bundle-3 codifies: elevated-QA window ->1, session-start-time reporting, restart-advice, 20-min stall threshold, 5-min mid-process progress heartbeat.
  - **Queue:** [`pending-decisions.md`](pending-decisions.md): the worker-a elevated-QA escalation (maintainer changed the window to 1 this session) + `:333` SEF-06/SEF-07 apply pending. Alert 2026-07-19-a OPEN (idle-heartbeat; fix + clear-on-merge in bundle 3, maintainer-authorized clear).

## State snapshot (2026-07-19 resumed session, #1040-#1043; SESSION-CLOSING at #1043; on the VM)

- **Current truth (verify against live files at `/resume`; each sub-line reconciled to the state THIS PR produces)**:
  - **Session / mode:** resumed from #1039; started attended-autonomous -> overnight-unattended ~01:12Z -> EARLY WIND-DOWN this morning on a named-degradation signal (converted to this session-closing handoff, per the discipline #1043 codifies). On the VM (gh-CLI, no GitHub MCP). Lease RELEASED by this handoff (`Status: released`, `Active-session: none`); the NEXT session sets its own mode at `/resume` (maintainer present = attended).
  - **Version snapshot (D7 validates these tokens):** library `2026.07.531`, README `1.9.892`, pack `1.62.2`, spec `1.17.13`; gate **72**, rules **13**, skills **24**, commands **15**, Document-types **18**. (Library/README bumped by this #1043 handoff; substantive session bumps ran through #1042 at `2026.07.530`/`1.9.891`; NO pack/spec/gate/rule/skill/command/doc-type change this session, the two guardrails are hooks + `tools/` scripts + `.claude/` guidance, none count-bearing.)
  - **Green-at:** the #1043 merge (this handoff is working-state + `.claude/` + version + CHANGELOG only; `main` stays 72/72 at its descendant merge; verify post-merge). Every PR this session ran green 72/72.
  - **Shipped this session (grc_library):** #1040 (Sweep-113 close-out, PASS), #1041 (answered-question guardrail §1.22.6), #1042 (wrong-repo guardrail §1.22.1), #1043 (this closing handoff + degradation-auto-handoff codification). Both maintainer-directed guardrails LIVE.
  - **Workers / research:** both live Opus 4.8; **worker-a UNVALIDATED** (validate-pr-1041 miss F2 caught by the delivery-1 auditor; full elevated QA + auditor on its next QA delivery); worker-b elevated (1 clean pass, re-establishes next session). **5 research seeds DELIVERED and ready to apply** (changelog-daily-summaries, cobit-title-normalization, content-forks, canada-ca-urls, + an older weekly one) in scratch `results/` + `inbox/`, the fresh session applies them per the NEXT SESSION block.
  - **Queue:** [`pending-decisions.md`](pending-decisions.md): the **worker-a elevated-QA escalation** (for maintainer review) + `:333` SEF-06/SEF-07 apply pending. Maintainer-alert channel ARMED, no open alerts. Not-yet-done §1.22 items + the 5-seed applies + §1.16/§1.1/§1.18 + the maintainer-gated §1.19.12/13 are all queued in NEXT SESSION.

## Asserted expectations (session close-out convention)

**Purpose.** At session close, the closing session records here its *asserted-clean* claims, scoped to the surfaces it actually touched or verified this session (not a blanket corpus-wide clean bill). The next session's `/resume` runs its corpus-wide `/validate` (the loop-break compensating control) and cross-checks the findings against these claims: a finding that *contradicts* an asserted-clean touched surface is a genuine miss signal (the closing session's self-assessment was wrong), to be escalated rather than triaged as an ordinary out-of-window finding. A finding on a surface NOT in this list is an ordinary finding, handled by the normal in-window / out-of-window triage.

**Why this shape (and not a two-run diff).** A rejected alternative was: the closing session also runs a full `/validate`, passes the result forward, and the receiving session diffs the two. That is wasteful and noisy: the corpus is byte-identical across the boundary (same `main` commit), so there is no corpus delta for two runs to legitimately differ on, and `/validate`'s expensive layer is non-deterministic subagent fan-out, so two runs differ by LLM sampling variance, not corpus facts (a "discrepancy = major issue" rule would fire on noise). It also collides with the loop-break (a closing-session finding can only ride the prompt forward, the lossy state the handoff exists to avoid). The asserted-expectations shape gets the real signal (a contradiction of a claimed-clean touched surface) at the cost of a few lines, no second sweep. The only deterministic close-vs-start diff worth keeping is the mechanical one, captured by the green-at-`<sha>` line in the snapshot above.

**Scope discipline.** Assert clean ONLY what this session mechanically verified (a green gate) or covered with a formal `/validate-pr` or `/validate` subagent pass. Do not assert clean a domain the session did not touch; an unfounded clean claim turns an ordinary pre-existing finding into a spurious "major miss" at the next resume. This functions as a lightweight proxy for the `/validate-pr` the session-closing handoff PR skips (the loop-break): it records what the session's own QA actually covered, for the next `/validate` to test.

**This session's asserted expectations (2026-07-19 resumed session #1040-#1042; closes at this session-closing handoff PR #1043):**
- **Every merged PR #1040 through #1042 received full formal per-PR QA** (a validate-pr row + a retro row; #1040 self-run validate-pr [1 finding F1 fixed #1041], #1041 offloaded to worker-a under ELEVATED QA [F1 worker-flagged + F2 auditor-caught, both fixed #1042], #1042 self-run validate-pr [CLEAN 0/0/0]). Every post-merge finding was in-window and fixed. **Net zero adopter escapes** (all slips, including the self-inflicted `git reset`/command-composition ones, were self-caught pre-merge or gate-caught).
- **The Sweep 113 loop-break control for #1039 PASSED** (offloaded to worker-b, elevated-QA CLEAN 0/0/0/0; all #1021-#1038 asserted-clean surfaces corroborated; recorded in #1040).
- **Both maintainer-directed guardrails shipped and verified**: §1.22.6 answered-question hook (`block-answered-question.py`, 12/12 self-tests, over-block fix applied; blocks a recorded-decision question, allows a novel one) and §1.22.1 wrong-repo hook (`block-wrong-repo-tool.py`, 8/8 self-tests; blocks a cwd-relative sibling-tool invocation from the wrong cwd, allows explicit-`cd`/local/unknown/argument). Both wired 2-hook per matcher; settings.json valid JSON. ASSERTED CLEAN on both hook surfaces.
- **The degradation-auto-handoff discipline is codified** in CLAUDE.md (`## No idle-stop in unattended mode` area): a degradation trigger in unattended mode = an automatic session-closing handoff, not a bare pause. ASSERTED CLEAN on the CLAUDE.md addition (no dash, resolves).
- **Counts / parity verified**: gate **72**, rules **13**, skills **24**, commands **15**, Document-types **18**; four-surface parity (gate 35), gate 39, gate 50 (incl. all #1040-#1043 QA rows), and gate 60 green at #1042.
- **KNOWN-OPEN, NOT asserted clean (a resume-`/validate` hit here is EXPECTED, not a miss):** the 4 remaining research-seed applies (§1.16/§3.68-70-74/§1.22.9) UNAPPLIED; §1.22.3/4/7 UNBUILT; §3.102 (pack-distribute the degradation discipline) UNBUILT; §1.1/§1.18 UNSTARTED; §1.19.12/13 maintainer-gated; the worker-a elevated-QA escalation OPEN (pending-decisions); a stale `validate-pr-1042` scratch order to delete. These are KNOWN, tracked, not misses.
- **green-at the #1043 merge = 72/72**; four-surface parity at gate **72**; rules **13** / skills **24** / commands **15**; Document-types **18**; spec `1.17.13`; pack `1.62.2`.

**This session's asserted expectations (2026-07-18b resumed session #1021-#1038; closes at this session-closing handoff PR #1039):**
- **Every merged PR #1021 through #1038 received full formal per-PR QA** (an offloaded or self-run `/validate-pr` row + a `/retro` row; a refute-briefed skeptical verifier on every substantive PR). Every post-merge finding was in-window and fixed. **Net zero adopter escapes.**
- **The Sweep 112 loop-break control for #1020 PASSED** (offloaded to worker-a, elevated-QA CLEAN; all #1000-#1019 asserted-clean surfaces corroborated, 0 contradicted; the S112-1 logging-standard 27002 sub-control lead routed to §1.21, fixed #1024).
- **§1.19.8 COMPLETE and verified**: 19 living operational docs relocated to `grc_library_private`, 51 pointers repointed, the layered fail-loud `_private`-required assurance (detect-env HALT + `block-operational-without-private.py` PreToolUse hook + pre-push guard) shipped and gate/CI-covered (#1028-#1030, §1.19.11 closed #1029); a maintainer-directed change-set deep-assessment signed off, 7 low findings fixed (#1031). ASSERTED CLEAN on the relocation + assurance surfaces.
- **§1.19.9 COMPLETE and verified data-safe**: gate-50 per-register dynamic floor (#1032); the sweep tool generalized + renamed (#1033); the dated-archive MIGRATION ran (1217 roll-up rows + 189 completed-week CHANGELOG entries + 53 dated QA records to the `_private` archive), row-by-row conservation confirmed by a dedicated data-loss verifier, gate-clean 72/72 (#1034). ASSERTED CLEAN on the sweep-tool + migration + gate-floor surfaces.
- **§1.19.10 COMPLETE and verified data-safe**: the tiered public-CHANGELOG migration (#1035-#1037). The current-week block is BYTE-IDENTICAL to the pre-migration root; the full 1020-entry per-PR source is preserved verbatim in `grc_library_private`; the six weekly headers partition the older entries with zero gap/overlap; every weekly-paragraph claim was re-verified against the source per-PR bullets by the orchestrator AND independently by validate-pr-1037. ASSERTED CLEAN on the tiered root + the `_private` full-source surfaces.
- **The maintainer-alert watchdog SOP is consistent across four surfaces AS OF #1039** (CLAUDE.md `## Credit-offload mode`, resume.md step 3, the `_private` design-of-record, the scratch convention). NOTE: validate-pr-1038's extra-care review caught ONE inconsistency (F1: resume.md step 3 still said autonomous-clear vs the maintainer-decides bullet), FIXED in #1039 (resume.md reworded); a Sweep 113 hit on resume.md/CLAUDE.md clearing-authority consistency would mean the #1039 fix did not take (escalate), otherwise the four surfaces agree. The three 2026-07-18 alerts (A/B/C) were assessed, advised, and cleared on maintainer authorization (#1038); F1 is surfaced for the maintainer's clear-decision.
- **Counts / parity verified**: gate **72**, rules **13**, skills **24**, commands **15**, Document-types **18**; four-surface parity (gate 35), collection-enumeration (gate 41), gate-count (gate 39), gate 50 (incl. the dynamic floor), gate 59 (incl. the tiered root), and gate 60 all green at #1038.
- **KNOWN-OPEN, NOT asserted clean (a resume-`/validate` hit here is EXPECTED, not a miss):** §1.19.12 (CLAUDE.md sensitivity-trim apply) UNSTARTED; §1.19.13 (history scrub) UNSTARTED; §1.19.10a (weekly-rollup wiring) UNBUILT; task #4 (worker-brief-template to scratch) UNDONE; Priority 1 (8) + the rest of the backlog OPEN. These are KNOWN, tracked, not misses.
- **green-at the #1039 merge = 72/72** (this handoff is working-state + version + CHANGELOG + `.working` only; verify post-merge); four-surface parity at gate **72**; rules **13** / skills **24** / commands **15**; Document-types **18**; spec `1.17.13`; pack `1.62.2`.

## Open decisions awaiting maintainer

- **QUEUE STATUS 2026-07-18b (wind-down): EMPTY (no blocking pending decision).** [`pending-decisions.md`](pending-decisions.md) is empty. This session's maintainer directions are DECIDED, not pending, and captured in the "Next actions" block above: **§1.19.12 DEFERRED** to a fully-awake session (unhurried surgical time; locked classification ready), **§1.19.13 GATED** (LAST), **both workers stay Opus 4.8** (Fable-5 experiment deferred), the **§1.19.x-moves worker-QA extra-care** directive, the **maintainer-alert SOP** now live, and the **operating-mode transition** (resume starts attended-autonomous, switches to overnight-unattended in a few hours). The maintainer-alert channel had its 3 morning alerts cleared, then validate-pr-1038 logged one new alert **F1** (the resume.md/CLAUDE.md clearing-authority inconsistency, FIXED in #1039); F1 is surfaced for the maintainer's clear-decision (may carry into next session if undecided at close). No decision is carried forward as blocking. The entries below are the durable historical decision record (mostly long-RESOLVED), retained per keep-current-plus-prior and pruned at resume; none is live.
- **RESOLVED (2026-07-01 at `/resume`; recorded in [`pending-decisions.md`](pending-decisions.md)):** the two 2026-07-01 pending decisions were answered by the maintainer. (1) **§3.2 detailed-CHANGELOG-mirror header-parity** = **cutoff-scoped gate** (accept the 3 pre-split-era missing headers #268/#353/#462 as an exemption; scope the parity gate from a cutoff PR forward; FP-free, no retroactive edit). (2) **§3.3 citation-verification recording-model** = **consistency cross-check, no register row** (register-vs-held-extract check surfacing/fixing mismatches only; the §9 human-primary-capture trust model unchanged). Both are now **P3 tooling** work, scheduled after the P1 FR-48 rename per the maintainer's post-P1 = P3-tooling direction.
- **OPEN (surfaced 2026-07-01): FR-70 source gate.** FR-70 (crypto-asset / blockchain governance, XL, H[critical], authorized this run via the harness) is source-gated: the high-assurance harness for a H[critical] regulatory domain needs ground truth that is not yet available (reference-base work tracked separately in the reference repo). Needs the maintainer to supply the missing primary sources, OR authorize authoring the crypto-regulatory core from training knowledge (advised against), OR redirect.
- **§6.3-vs-§7.3 audit-scope spec gap**: **CLOSED in #338.** Sweep 45 found that #336's "added to all 9 explicit-list content linters" enumeration was incomplete: gate 31 (date-staleness) and gate 10 (review-cadence), two more explicit-allow-list content gates, silently skipped the dir (gate 31 a clear §6.3 violation since §6.3 names "date currency"). #338 added `.project-governance` to both (with a scope-coverage regression test each) and amended the spec with **§7.4**, which enumerates the explicit-allow-list content-linter completeness obligation (the maintainer chose, 2026-06-25, to include gate 10 and to amend the spec). The maintainer-decided spec amendment closes the decision; the **directory-scan-scope parity meta-check** (TODO P2) is the queued mechanical backstop that would have caught this class.
- **R2 relocation**: CLOSED by principle in #314 (the project-governance separation spec classifies the 6-file citation-verification cluster as project governance, so it migrates in Phase 1). The Phase-1 migration, the queued directional-dependency gate (§7.3), and the 2 deferred §5.3 classifications are now forward-looking TODO items, not open decisions awaiting the maintainer.
- **FR-167 mapping gate (asked 2026-06-24)**: RESOLVED, built as gate 49 (#325 ISO + NIST well-formedness; #326 NIST category membership + 17-cell remap closing §4.6a). The control-code-validity gate catches hallucinated/mistyped codes; semantic mapping correctness remains the orchestrator's apply-time job. No longer awaiting.
- **DD-12 scope**: RESOLVED + APPLIED (#329, maintainer-approved "broaden + extend gate 49"). DD-12 in TODO is broadened from `PR.IP`-only to all CSF-1.1 carriers (`PR.IP`/`ID.SC`/`ID.BE`/`RS.RP`/`DE.DP`/`PR.AC`/`PR.PT`/...; `RS.RP`→mainly `RS.MA`), and a **gate-49-extension** TODO item is queued (validate framework codes in per-document reference tables, not only the central matrix; build the scanner first, then gate the migration). Both are now forward-looking TODO items, not open decisions. The migration itself (corpus-wide, non-partitionable) is queued.
- **Publications-assessment process (§4.12, surfaced + queued 2026-06-25)**: a maintainer-directed process to assess reference-base publications for useful info AND detect poisoning/false info before it informs corpus work, queued as TODO §4.12 (not yet built). Forward-looking, not an open decision.
- **Maintainer actions still pending**: (a) **seed the reference binaries** durably (git-proxy blocks writes), belt-and-suspenders ONLY: the in-repo reference modules gates 48/49 use are the authoritative validators, so FR-167 batch 5+ code validity is gate-protected without them; re-attach only if a worker wants the source text; (b) **provision the least-privilege external-worker account** (read `grc_library` / write `grc_library_scratch` only), this is the gating action for both the separate-session worker primitive (§4.11) and the queued **§4.4** worker-ready brief staging / `/subagent` capability. Neither blocks the next session's queue (in-session `Agent` fan-out works for FR-167 today).
- The larger-track decisions (FR-167 batches 4..N, the High[critical] net-new docs, the L/XL items) remain queued and scheduled deliberately.

## Trust-recovery state

The trust-recovery suite (`/full-qa` + `/fitness` r2) ran and the maintainer **signed off** (2026-06-22). Codification COMPLETE. Not re-triggered since; quality held throughout the 2026-06-24 overnight run, morning continuation, and the resume session (Sweep 38 + close-out #312, both warnings fixed first try, post-commit + pre-push green).

## Known environment behaviours (read before assuming working-tree state)

- **The stop-hook auto-commits AND pushes uncommitted changes on turn-end.** The working tree is auto-persisted to the feature branch; verify `git log` rather than assuming edits are held locally.
- **The squash-merge commit on `main` shows locally as `E noreply@github.com`** (GitHub's own merge commit; Verified in the GitHub UI, not locally verifiable). After syncing the branch to `main` the tip equals `origin/main`; **do NOT `git commit --amend`** it (that rewrites merged `main` history). Set `git config user.email noreply@anthropic.com && git config user.name Claude` so your own feature-branch commits are attributed correctly.
- **A squash-merge makes the local feature branch diverge from `main`** (the squash commit is new; the local pre-squash commit is its content but a different SHA), so `git merge --ff-only origin/main` fails after a merge; use `git reset --hard origin/main` to re-sync the feature branch (the local commit's content is in the squash, preserved in reflog).
- **The GitHub MCP server can disconnect mid-session** (it did 2026-06-24 after #304, blocking #305's open/merge). `git push` over HTTPS keeps working; only the MCP-routed GitHub-API operations are affected. If `mcp__github__*` tools vanish: keep committing and pushing via `git`, record the pending-merge state in the handoff, and resume the PR lifecycle when MCP returns. Do NOT merge by any non-MCP path. Full writeup: [`third-party-issues.md`](third-party-issues.md).
- **The commit-signing server can 503**, distinguish from a real defect (gates 31/40 pass on the real corpus while gate 36's failures are `git commit` subprocess errors); re-run after a pause. Full writeup: [`third-party-issues.md`](third-party-issues.md).
- **The clone may start shallow**; `git fetch --unshallow` before any history-aware audit (gates 31/40).
- **The `Bash` tool's working directory can reset to a non-repo directory between calls** (observed twice this session after long async-subagent gaps: a bare `git ...` failed with "not a git repository"). Prefix git-command sequences with `cd /home/user/grc_library &&` rather than assuming the cwd persisted.
- **After a post-merge sync you are ON `main`; `git checkout -B <feature-branch>` BEFORE building the next PR.** The post-merge sync (`git checkout main && git pull origin main`) leaves you on `main`. If you then build and `git commit` the next PR without switching branches, you commit to local `main` (which the stop-hook flags as an unverified commit ahead of `origin/main`), and a subsequent `git push -u origin <feature-branch>` pushes the STALE feature-branch ref (still at the prior PR's pre-squash commit), so the PR opens with an empty/wrong diff. This happened on the #361 handoff (the commit landed on local `main` as `2b589cd`; recovery was `git checkout -B <feature> <commit>` to repoint the feature branch, `git branch -f main origin/main` to undo the local-main commit, then a force-push of the feature branch). Recovery is clean and on-branch (no `main` push, no shared-history rewrite), but the fix is cheaper than the catch: switch to the feature branch immediately after the sync.
- **An execution-environment probe now exists; the `/resume` step 3 runs it.** `python3 tools/detect-env.py` (TODO 3.18, shipped) profiles the transport/tooling assumptions that diverge between the managed cloud sandbox and the local VM (gh presence/auth/rate, sibling-repo access with launch-bound fix lines, per-family egress classes, the stop-hook and pipe-guard as ASSISTANT-PROBE lines the script cannot self-observe); the `/resume` step-3 wiring reads its profile to pick the PR mechanism, the CI/merge transport, the commit-push mode, and the pipe-guard expectation. It never self-grants access (`--add-dir` / `settings.local.json` fixes are surfaced to the maintainer; they bind at launch). Observed VM profile: gh authenticated, GraphQL pool healthy, pipe-guard fires, `grc_library_ref`/`grc_library_scratch` readable, iso.org 403 and planalto.gov.br unreachable.
- **After a squash-merge, the stop-hook flags the GitHub merge commit as Unverified; do NOT amend it.** When the designated branch is restarted from the merged `main` (`git checkout -B <feature> origin/main`), its tip becomes GitHub's squash-merge commit (committer `noreply@github.com`), which the stop-hook reports as Unverified and suggests amending. DECLINE the amend: amending rewrites already-merged `main` history (forbidden), and GitHub signs its own merge commits (they show Verified on the platform; the local `%G?` check cannot see that). The flag clears once the next authored commit lands on the branch. (Observed at #681; predictably recurs each merge on the reused branch.)
- **Operating-mode mechanics** (set 2026-06-26, codified in [`.claude/CLAUDE.md`](../.claude/CLAUDE.md) #349): green CI = standing merge authority (merge green PRs and proceed without per-merge approval), stricter-is-safer always, and the pending-decisions / 2-minute-timeout graceful-degradation mechanism ([`pending-decisions.md`](pending-decisions.md)). The `/resume` command has a step 7 that checks `pending-decisions.md` before the queue. **Current mode: ATTENDED-AUTONOMOUS (maintainer-set 2026-07-05, the `claude/resume-tl5rez` resumed session, Q1=A)**: green CI = merge authority (merge own green PRs and proceed, decisions surfaced by exception); full per-PR `/validate-pr` + `/retro` always; stricter-is-safer always. This is NOT overnight/daytime-unattended, so there is no skip-to-morning or timeout-to-default path; genuine maintainer decisions are asked, not deferred. This session's queue (Q2=A): clear the four resolved-but-unexecuted pending items + the coverage-refresh scratch sync + the small P3 items, then P2 2.1. Egress re-tested at resume (Q3=try-then-defer): egress is up (control host loaded) but planalto.gov.br 503'd and iso.org 403'd, so the upstream-verification items (1.11 Brazil verification, 1.5's 51 needs-reconfirm rows, FR-70 sources) STAY DEFERRED; the Brazil annotation proceeds. The mode-exit priority ordering (overnight cleanup, then fixes, then tooling and protections, then new work) is codified in `.claude/CLAUDE.md`.

## Standing disciplines (do not drift from these)

- **PRIMORDIAL RULE**: the AIQT Principle, (Accuracy = Integrity = Quality = Trust) > Speed > Cost; the AIQT tier non-negotiable. Emit the AIQT-check line at task start, before commit, before completion claims, and at tension points.
- **Post-commit audit**: after every commit, run `tools/run_all_audits.sh` standalone before pushing; never chain commit and push. Before push, also `tools/run-pr-time-checks.sh`.
- **`lint-language` pre-flight on new pack prose** before the first commit (catches em-dashes / British `-ise`). (Relevant for DD-4/5's `go.md` rewrite.)
- **Pre-commit dash-grep on new root `CHANGELOG.md` lines** (`grep -nP '[\x{2013}\x{2014}]'`, the codepoint form; the older byte-sequence form `\xe2\x80\x93|\xe2\x80\x94` matches nothing in a UTF-8 locale and returns a false-clean, confirmed 2026-07-10, so use the codepoint form or `LC_ALL=C` with the byte form): the D3 dash gate is PR-time only, so a late-assembled CHANGELOG entry can slip an em-dash that surfaces at pre-push; grep the added lines before the first commit. The same false-clean bit the sweep94 detail file (em-dashes pasted verbatim from subagent returns), caught by gate 51.
- **Grep-after-wiring / convention change**: after editing a restated-across-surfaces value, `grep` the OLD form across the full changed file AND every sibling surface; zero hits before commit.
- **Corpus-wide-grep / special-case-the-edge / find-every-carrier**: a mechanical bulk edit (a rename, a relabel, a title alignment) must verify EVERY adjacent field, special-case the known edge cases (e.g. preserve rating "Critical" while fixing impact-5; the DD-2/3/11 grep found a 4th carrier beyond the 3 named), and search corpus-wide for the OLD form, not apply one convenient rule uniformly.
- **Sweep close-out bookkeeping triplet**: a `/validate` close-out PR regenerates the generated artefacts (after per-doc version bumps), advances the TODO sweep cursor (gate 45 + the gate-36 TodoStaleness regression both enforce it), and bumps `validate-sweeps/history.md`'s own Version. The post-commit audit catches all three; doing them pre-commit avoids the fail-then-fix loop.
- **60-second** paired fallback timer after every `subscribe_pr_activity`.
- **Per-PR QA**: formal `/validate-pr` (Subagent A) + `/retro` after every merge; no skip, no abbreviation. **One standing exception**: the session-closing handoff PR skips both (loop-break); the compensating control is the corpus-wide `/validate` that `/resume` runs first. (This exception is now generalized into the pack layer, #305.)
- **PR close-out checklist** (the degradation guard): prior PR's `/validate-pr` row AND `/retro` row both batched in; closed TODO items rotated to DONE (every path-shaped code span in a CHANGELOG entry linked, changed or merely mentioned); stale-count check if an enumerated collection changed; session-handoff.md refreshed.
- **Self-assess for degradation / session-continuation SOP**: the default is to CONTINUE. Wind down ONLY on actual drift/hallucination/mistakes the QA layer did not catch; work size/shape, session length, and "a large / substantial / fresh-context-best series remains" are NOT triggers (corrected #460, maintainer 2026-06-29: about 13 of the last 15 proposed handoffs were the wrong call). A large series ahead is worked PR-by-PR with skeptical verifier subagents sustaining quality, not handed off. Steady-state misses the per-PR `/validate-pr` catches are NOT drift. The assistant may OFFER a handoff for the maintainer to consider before a substantial/critical/long piece of work begins, as a non-default suggestion (the maintainer chooses; absent a choice, continue). **When a handoff IS warranted (genuine degradation evidence), do NOT take it silently: run the `## Wind-down decision framework` in [`.claude/CLAUDE.md`](../.claude/CLAUDE.md)** (surface the justification + a per-PR likelihood assessment of the next-five PRs + named options A handoff / B recommended additional PRs / C higher-risk order / D the Ulysses-pact impulse-check; roughly-2-minute timer defaulting to handoff on no answer; codified #375, corrected #460).
- **Compute-don't-ask**: before surfacing a question, apply a "can I compute/verify this myself?" gate; surface only the result or a decision that genuinely needs maintainer judgement. (Counterpart: surface a genuine design tradeoff before building, as S5's FP surface was surfaced with named options rather than guessed.)

## Refresh and pruning discipline

**Refresh (at session close).** This file is refreshed at every PR close-out (post-merge), as part of the recursion-avoidance batch that carries the validate-pr/retro rows into the next PR. The session-closing handoff PR prepends this session's new blocks (the "Next actions" narrative block, the "State snapshot", and the "Asserted expectations" block) as the new *current* set.

**Prune (at resume, in the first PR).** Because the closing session only ever *adds*, the per-session blocks would otherwise accumulate without bound and bury the load-bearing state (the file's one job is a fast, reliable single resume point). So the receiving session prunes, as part of its first PR (the `/validate` close-out, per `## How to resume` step 6):

- **Retention: keep current + 1 prior.** Keep the most-recent session's blocks and the single immediately-prior session's blocks in each per-session stack (the "Next actions" narrative blocks, the "State snapshot" blocks, the "Asserted expectations" blocks). Delete everything older. Also delete superseded one-off "Next-session queue" / dated "This session's work" sections.
- **Keep the standing sections in full** (not per-session): the header / `## How to resume` / `## Resume cursor`, the `## Open decisions awaiting maintainer`, `## Trust-recovery state`, `## Known environment behaviours`, `## Standing disciplines`, and this section. These are durable reference, not session narrative.
- **Neutralize the dangling "supersedes ... below" pointer.** After deleting the oldest per-session blocks, the newly-oldest-retained block's own `**Superseding note:** ... supersedes the #<pruned> block below` clause now points at a block that no longer exists. Reword that one clause (drop "below", mark it "(pruned at the Sweep N resume)") so the prune does not leave a dangling internal pointer (the #852 `/validate-pr` note-2 finding).
- **Migrate-before-delete (the safety rule).** Before deleting a block, confirm any load-bearing item it carries that is NOT recorded elsewhere (an open decision, a pending maintainer action) has been migrated to `## Open decisions awaiting maintainer` / [`TODO.md`](../TODO.md) / [`pending-decisions.md`](pending-decisions.md). The durable record of pruned session *narrative* already lives in [`CHANGELOG.md`](../CHANGELOG.md) + its detailed mirror, [`DONE.md`](DONE.md), [`validate-sweeps/history.md`](validate-sweeps/history.md), [`session-metrics.md`](session-metrics.md), and git history, so narrative is safe to drop; only un-recorded forward-looking items need migrating.

The companion close-out-checklist line and the `/resume` command step encode this so it is a standing convention, not a one-off. This convention was added 2026-06-28 by maintainer direction after the file reached 426 lines / 47 stacked session blocks; the first prune cut it to ~120.
