# Session Handoff

**Purpose.** This file is the single resume point for a new Claude Code session. It is refreshed at every PR close-out so that opening a fresh session and continuing requires only one instruction from the maintainer (see "How to resume"). It exists because long sessions degrade (context dilution, lossy compaction, state drift, error compounding); a fresh session that rebuilds state from this file plus the durable repo artefacts is more reliable than a long one running on accumulated memory. The mechanisms and mitigations are written up generically in [`.working/session-length-considerations.md`](session-length-considerations.md). This file is maintainer working state, exempt from corpus audit gates per the `.working/` exemption.

This is an **as-of-last-refresh snapshot**, not a live-HEAD claim. Versions and counts drift forward as work advances; always verify against live files before relying on them.

## How to resume (the one command)

In a new session, the maintainer sends only: **`/resume`** (or "read `.working/session-handoff.md` and continue").

On `/resume`, the assistant:
1. Reads this file in full, including the **"Known environment behaviours"** section below.
2. Reads `.claude/CLAUDE.md` (the PRIMORDIAL RULE and project disciplines), the most recent few `CHANGELOG.md` entries, and **[`.working/third-party-issues.md`](third-party-issues.md)**.
2a. **Loads the scratch reference index into memory** (`grc_library_scratch/ref/INDEX.md`, `ref/catalogue.yml`, `ref/SECTION-INDEX.md`, `ref/COVERAGE-MAP.md`) so the held reference set and its versions are known before any task; the index, not a guessed path, is authoritative for what is held and where. See `/resume` command's Reference-knowledge-base step and the `## Reference-version currency` SOP in `.claude/CLAUDE.md`.
3. Runs `git rev-parse --is-shallow-repository` (unshallow with `git fetch --unshallow` if `true`, **before any history-aware audit**), then `tools/run_all_audits.sh` to confirm the corpus is green, and `git log --oneline -5` to confirm HEAD.
4. Verifies the version/count snapshot below against live files.
4a. **Batches the unattended-run clarifications up front (maintainer-directed 2026-06-25)**: before resuming the queue, asks the maintainer (via `AskUserQuestion`, batched into as few rounds as possible) every decision that would let the session run unattended longer, pre-populating decisions for **all Priority 1 and Priority 2 backlog items** and **the next 10-plus planned PRs** (including any outside P1/P2, e.g. the FR-167 batches). Compute-don't-ask first (ask only genuine authorial decisions, not what the assistant can verify); record the answers in the next-actions queue so they survive a mid-session compaction; mark deferrals and route around them. See `/resume` command step 5 for the authoritative form.
5. **Runs a full corpus-wide `/validate` as the first substantive task** (the loop-break compensating control for whatever session-closing handoff PR last closed, which skips its trailing `/validate-pr` + `/retro`): full A/B/C subagent dispatch over the closing session's delta window, recorded in [`validate-sweeps/history.md`](validate-sweeps/history.md) and the Resume-cursor section above. **Cross-check the findings against the "Asserted expectations" section below**: a contradiction of an asserted-clean touched surface is a genuine miss, escalated; an ordinary finding routes to the backlog. Standing invariants to confirm: the corpus is CSF-1.1-clean (gate 54, 0 carriers; a carrier in a per-document NIST table is a genuine finding, not residue); four-surface parity at the current gate count (gate 35); the gate/rule/skill/command counts (gate 39 + prose). The specific sweep number and delta window are in the current "Next actions" block below.
6. **Prunes this file as part of that first `/validate` close-out PR** (keep current + 1 prior, per `## Refresh and pruning discipline` below), so the handoff does not accumulate stale session history across resumes.
7. Continues from "Next actions" below.

## Resume cursor (machine-readable)

This is the relocated home of the former `## Session resume metadata` block (moved out of
[`TODO.md`](../TODO.md) in PR #413 so TODO stays purely forward-looking). The sweep cursor below is
read by [`tools/lint-todo-staleness.py`](../tools/lint-todo-staleness.py) (gate 45), which flags it
if it falls behind [`validate-sweeps/history.md`](validate-sweeps/history.md). The full sweep history
lives in that history file; the live per-session state (branch, versions, counts, green-at-`<sha>`) is
in the "Next actions" blocks below and is an as-of-last-refresh snapshot (drift forward is expected).

- **Last validation sweep**: Sweep 77 iter 1 (2026-06-30; the loop-break corpus-wide `/validate` over the #466..#475 deltas, run at the `/resume` continuing from session-closing handoff #475, entering overnight mode; full three-subagent dispatch. Mechanical baseline 57/57 at `1f1f219`/#475, matching the #475 green-at-`cfa6e0c` baseline. Subagent C clean (four-surface parity 57; gate 56/57/D5/50 docstrings + §6/§5 narrative accurate, no stale "queued §4.6/§4.10"). **Finding A-1 (in-window, fixed this PR)**: [`governance/specification-audit-programme.md`](../governance/specification-audit-programme.md):140 the §6 detailed-prose enumeration covered gates 38-56 but not gate 57 (#468); gate-blind (gate 35 checks the table, gate 39 counts rows). Added the gate-57 two-sentence pair (gate-50 convention: "open backlog bullet"/"backlog file", link DONE.md not TODO.md, to avoid gate 9's line-window); spec 1.16.21->1.16.22, artefacts regenerated. NOT a #475 asserted-clean contradiction (the §6 prose convention was not asserted clean), an ordinary in-window finding. **Findings B-1/2/3 (out-of-window, routed + fixed in the ERC-canonicalization PR)**: lowercase "executive risk committee" #456's title-case sweep missed, at [`security/standard-data-loss-prevention.md`](../security/standard-data-loss-prevention.md):111 (self-tags "(ERC)"), [`risk/template-board-risk-report.md`](../risk/template-board-risk-report.md):212/:224; canonical body per [`governance/register-role-authority.md`](../governance/register-role-authority.md):32 is "Enterprise Risk Committee (ERC)", no distinct "Executive Risk Committee" body, so resolved by the canonical-internal-source rule. Asserted-expectations cross-check: no contradiction of any #475 claimed-clean surface. Corpus CSF-1.1-clean 0/311, four-surface parity 57. Detail: [`validate-sweeps/2026-06-30-sweep77-iter1.md`](validate-sweeps/2026-06-30-sweep77-iter1.md)).

## Next actions (the queue for the next session)

**OVERNIGHT SESSION PROGRESS (2026-06-30, in flight; this block batch-refreshes into the next PR).** Resumed from session-closing handoff #475, ran the loop-break Sweep 77 corpus-wide `/validate` (clean except the in-window gate-57 §6-prose fix + the out-of-window ERC lowercase fixes), then entered overnight mode under the maintainer's directive: work P1/P2/P3 to reduce the backlog, **fix-issues-first then new content**, defer anything needing maintainer input, do not wind down absent drift evidence, do not exit overnight mode on a mid-night prompt. Merged this session: #476-#479 (Sweep 77 close-out, ERC canonicalization, §4.8 codification, #478 hot-fix + FR-58 deferral), #480 (FR-32 LIA template), #481 (FR-145 AI-standards crosswalk), #482 (FR-31 privacy-by-design framework; harness caught a `29134:2023` hallucination, fixed pre-merge), #483 (FR-34 Transfer Impact Assessment template; FR-74 instrument-half delivered), #484 (FR-71 M&A due-diligence procedure in `compliance/`, net-new via the harness; FP verifier 0 defects, 2 FN completeness improvements applied; post-merge `/validate-pr` 0 findings), #485 (FR-72 sanctions and export-control screening Standard in `compliance/`, net-new via the harness; both adversarial verifiers clean, 4 FN operability refinements folded in; re-wired the M&A procedure's financial-crime/sanctions dimension to it; post-merge `/validate-pr` 0 findings), #486 (FR-73 AI Ethics Review Panel charter; split independent AI ethics review out of the AI Governance Council into a new `ai/charter-ai-ethics-review-panel.md` with a five-step independent challenge mechanism + a surgical AIGC-charter edit; both harness verifiers clean; post-merge `/validate-pr` surfaced 1 cross-FILE stale-attribution finding (a KPI crediting the AIGC with completing ethics reviews), fixed in #487), #487 (FR-144 breach-notification regulator register template; new `privacy/template-breach-notification-regulator-register.md` adopter-fillable register with the missing internal-target clock + strictest-applicable-requirement rule, wired into the breach procedure §6.2; both harness verifiers clean, 1 FN metadata field-order warning fixed; carried the #486 /validate-pr fix; post-merge `/validate-pr` 0 findings).#488 (FR-61 financial-services prudential regulators; extended the financial-services sector annex's regulatory-landscape overview with Asia-Pacific MAS/APRA/HKMA/JFSA, Switzerland FINMA, and explicit US OCC/FRB/FDIC, structurally named (no-held-source case); both harness verifiers clean, FP confirmed all 6 instruments upstream via WebSearch, 1 FN heading-case warning fixed; post-merge `/validate-pr` 0 findings). **In flight: #489 (TIA cross-reference wiring; linked the new TIA template from the EU privacy annex, the DPIA template, the cross-border data flow register, the glossary, and the cross-border procedure's Related Documents; bookkeeping-tier, 5 co-bumps, no harness; closes the #483 TIA-wiring follow-up).** **DEFERRED, need maintainer (overnight): FR-58 mechanism fork (in [`pending-decisions.md`](pending-decisions.md)); the §4.8 residual and any `.claude/` / pack-rule change (protected-file directive: need manual approval, deferred until not overnight); the #486 /retro lesson (a function-reassignment needs a corpus-wide stale-attribution grep, a close-out-checklist codification = a `.claude/` change, logged in the improvement log, deferred); the #487 /retro tooling option (extend lint-metadata to check field ORDER not just presence, a gate-logic change, deferred).** **Remaining net-new P1 (maintainer pre-authorized self-merge-with-harness): FR-72 SHIPPED #485; FR-73 SHIPPED #486; FR-144 SHIPPED #487; FR-61 SHIPPED #488.** Operative net-new queue: FR-59 (privacy jurisdiction annexes deepening; held sources for Japan/Virginia/Colorado/LATAM, APAC-beyond-Japan maintainer-drop per resume-decision (2) attempt-download-then-defer), FR-60 (HIPAA deepening), FR-154 thin-baseline cluster, FR-74 Schrems II residual; FR-58 (3-label inheritance vocab, maintainer-decided, NON-partitionable corpus-wide sweep); FR-70 (crypto-asset/blockchain domain, XL). FR-167 closing whole-matrix `/matrix-fit` + gap-fill remains. Tooling follow-ups queued (P3): lint-language `-ises` pattern gap (#480, a delicate corpus-wide sweep with a possible protected-pack-file entanglement, deferred); TIA cross-reference wiring SHIPPED #489. The remediation-priority narrative below is from the prior (pre-overnight) resume session and is retained for the issue-type taxonomy; the operative order now is the net-new P1 content queue just listed (the High correctness/contradiction backlog it describes was closed by the prior session at #417).

**STANDING DIRECTIVE (maintainer-set 2026-06-27): fix issues before new content.** Until the routed-issue backlog is cleared, correcting defects takes priority over adding new content; greenfield work (the privacy templates FR-31/32/34, FR-73 AI ethics panel, the AI crosswalk FR-145, and the matrix *gap-fill* rows) waits behind it. The five issue **types** are: (1) **correctness** (a value simply wrong against ground truth: wrong/non-existent control code, wrong legal in-force date, miscitation); (2) **cross-document contradiction** (the same fact stated two ways across or within documents); (3) **semantic-fit / weak-mapping** (a code that exists and is in the right catalogue but is the wrong/loose control, the gate-blind class); (4) **format / structural** (table-shape, column-format, metadata inconsistency); (5) **editorial / prose** (acronym drift, `shall`->`must`, stale narrative counts, wording). **Remediation priority order** (severity-tiered first, then harm-if-copied times fix-certainty):
1. **High correctness defects** (type 1, High) FIRST: high harm (an adopter copies the falsehood), cheap, verifiable against the reference base. Example: FR-168 (`GRC-12`->`GRC-04`).
2. **High cross-document contradictions** (type 2, High): decision-gated. Pick the canonical value via stricter-is-safer plus external-standard / canonical-internal-source grounding, then propagate corpus-wide in ONE pass per fact (grep the chosen value everywhere before closing; one-surface-at-a-time re-introduces the drift). Intra-document contradictions jump the queue within this tier. The High set here is acceptance authority (CRO vs Executive Management vs Board), incident P2 CISO timing (4h vs 1h vs 2h), ROPA retention (3y vs Active+5y), and the India DPDPA in-force date.
3. **Medium semantic-fit + format** (types 3-4): batch by file/domain; route loose-fit codes through `/matrix-fit`. Examples FR-198 (DSP-10 weak fit), FR-199 (register column format).
4. **Low editorial** (type 5): batch; the corpus-wide ones (ERC ~26 docs, FR-44 `shall`->`must`, the FR-58 label scheme) are NON-partitionable single-session sweeps, not incremental edits.
Cross-cutting rules: within a tier do cheap-and-certain before decision-gated; land a correction BEFORE any content that cites it; the matrix *dedup* is an issue (fix-first) while the matrix *gap-fill* rows are new content (wait). **UPDATE (this resume session, closes at #417): the entire High set FR-168..FR-175 is now CLOSED** (#411 FR-168/170/173; #412 FR-169/171/177/185; #414 FR-172/186/187; #415 FR-174/179/180; #416 FR-175). So the concrete order is now **FR-176..199 (the 24 P2 Medium/Low/FYI) by type, then the deferred new content (matrix gap-fill / FR-31/32/34 / FR-73 / FR-145 / FR-58).** The maintainer's standing next-session plan (agreed this session): **get most/all of the P3 batch in one fan-out as a test**, alongside the P2 type-batches; the P2 set (FR-176..199) is largely partitionable by file/domain (research-fan-out-eligible), while the corpus-wide editorial sweeps (ERC ~26 docs, FR-44 `shall`->`must`, the FR-58 label scheme) stay NON-partitionable single-session.

**THIS RESUMED SESSION (2026-06-30, `/resume` into OVERNIGHT mode).** Resumed from session-closing handoff #475 (HEAD `1f1f219` = `origin/main`, 57/57 green, matching the #475 green-at-`cfa6e0c` baseline). Operating mode: **OVERNIGHT** (maintainer asleep; a mid-night prompt does NOT exit overnight mode; items needing maintainer input defer). Three resume clarifications maintainer-answered (`AskUserQuestion` + confirming screenshot, both agreeing), recorded in [`pending-decisions.md`](pending-decisions.md): (1) net-new self-merge + high-assurance harness; (2) attempt-download-then-defer for missing sources; (3) ERC tier-table reconcile = collapse Tier-1 dup + drop Tier-2 stray. Standing directive: work P1/P2/P3, **fix-issues-first, new-content-second**; correct errors as found; continue absent drift evidence. Ran the mandatory loop-break **Sweep 77** FIRST (corpus-wide `/validate` over #466..#475: A-1 in-window gate-57 §6 detailed-prose gap FIXED this PR; B-1/2/3 out-of-window lowercase-ERC drift routed to the immediately-following ERC-canonicalization PR; C clean; no asserted-clean contradiction). **Shipped this session (#476-#490):** #476 (Sweep 77 close-out), #477 (ERC-canonicalization), #478 (§4.8 close-out-checklist codifications), #479 (#478 hot-fix + FR-58 deferral), #480 (FR-32 LIA template), #481 (FR-145 AI-security crosswalk), #482 (FR-31 Privacy-by-Design framework), #483 (FR-34 TIA template), #484 (FR-71 M&A due-diligence procedure), #485 (FR-72 sanctions/export-control standard), #486 (FR-73 AI Ethics Review Panel charter), #487 (FR-144 breach-notification register template), #488 (FR-61 financial-services prudential regulators), #489 (TIA cross-reference wiring), #490 (FR-167 closing whole-matrix `/matrix-fit` item (a) + the 1 mismatch it found, `register-asset-inventory.md` `AIS-01/HRS-01` -> `DCS-06/DCS-07`; batches #489 QA rows), **#491 (this PR: FR-167(b) matrix gap-fill, 7 net-new-session-doc rows added via the high-assurance harness, AICM GRC-13 added by the FN verifier, sanctions customs columns confirmed N/A; batches #490 QA rows)**. The net-new content PRs (#480-#488) and the #491 matrix-row batch each went through the high-assurance dual-verifier harness; #489-#490 are bookkeeping/fix tier. **Queue (fix-issues-first):** remaining deepen-baselines (FR-15/23/24/63/99/154) via the harness -> FR-70 crypto (XL) -> Sweep-3 consistency follow-up. **DEFERRED (need maintainer / not overnight):** **FR-167(b) residual scope** (whether the 6 OT docs + the library-meta/glossary/crosswalk docs get matrix rows; the net-new-doc gap-fill is done, FR-167 stays OPEN pending this scope answer, [`pending-decisions.md`](pending-decisions.md)), FR-59 (25-annex deepening, scope+source gaps), FR-60 (new-artefacts scope decision), FR-58 (mechanism fork), the lint-language `-ises` gate-gap sweep (delicate / possible protected-pack entanglement), and the #486/#487 `/retro` codification lessons (corpus-wide stale-attribution-grep close-out line; lint-metadata field-order option) which are `.claude/`/pack changes barred overnight. **Sweep 78** over this session's deltas (#476..) is the next loop-break control at the next session-closing handoff.

**SESSION-CLOSING UPDATE (2026-06-29, this session's wind-down; closes at PR #475, the session-closing handoff PR).** Maintainer directed wind-down "after the next few PRs, no rush" mid-session; the session continued to a clean stopping point and closes here. Full arc beyond the #466-#471 narrative below: **#472** (fixed the 2 cross-FILE stale §4.6/§4.10 "queued" carriers #471's `/validate-pr` surfaced, in [`.claude/CLAUDE.md`](../.claude/CLAUDE.md) + the [`tools/lint-bookkeeping-parity.py`](../tools/lint-bookkeeping-parity.py) docstring); **#473** (codified the §4.8 batch's focused slice: 2 close-out-checklist bullets, the cross-FILE section-close cleanup guard + the gate-39 count-phrasing guard); **#474** (hot-fix for #473's own `/validate-pr` finding: the gate-39 bullet had mis-stated gate 39's scope, claiming `CHANGELOG.md` is scanned when it is in `EXEMPT_FILES`; corrected to name the actually-scanned surfaces, verified accurate against source by #474's `/validate-pr`). **#475 (this session-closing handoff PR)** batches the #474 `/validate-pr` (0 confirmed, 1 cosmetic FYI fixed in #475) + `/retro` rows, the cosmetic P7-string fix, and this refresh; it skips its own trailing `/validate-pr` + `/retro` per the handoff-PR exception (marker in its `validate-pr/history.md` Findings cell). Gate count **57** throughout (all this session's work was scope/bookkeeping/codification, no new gate). Library `.451` on #471, `.452` on #474 (`.449`->`.450` #472, `.450`->`.451` #473, `.451`->`.452` #474); README `1.9.323`; spec `1.16.21`; pack live `1.53.5` (unchanged). **The §4.6/§4.10/gate-39 cluster is CLOSED**: §4.6 closed-as-satisfied (#471), the cross-FILE refs fixed (#472), the close-out lessons codified (#473), and the gate-39-scope mis-description corrected and verified (#474); the `/retro` cluster lesson ("verify a mechanism against its source artefact, not a derivation") is logged in the #473/#474 rows. **Next `/resume` runs a full corpus-wide `/validate` first (Sweep 77 over the #466..#475 deltas)**, cross-checked against this session's `## Asserted expectations` block below. **Still OPEN for the next session (attended-autonomous; fix-issues-first holds):** the remaining **§4.8 apply-time-discipline codifications** (the gate-prose-collision close-out line now 4x firm; "adding a PR-only delta gate Dn needs its step name in `WORKFLOW_DELTA_GATE_STEPS`"; the #462 table-edit-safety + #463 `replace_all`-scope notes) -> Sweep-3 follow-up + the ERC guideline-doc authorial reconcile -> FR-58 3-label scheme -> P1/P2 net-new content each via the high-assurance harness -> closing FR-167 whole-matrix `/matrix-fit`. §4.5 **S3** stays PARKED (maintainer-deferred). The prior-session blocks below are retained until the next `/resume` prune.

**THIS SESSION (2026-06-29, `/resume` continuing; attended-autonomous, green CI = merge authority; CLOSED at #475 per the update above).** Resumed from the #464 session-closing handoff. Ran the loop-break **Sweep 76** first (corpus-wide `/validate` over the #458..#464 deltas, the #464 compensating control), then worked the guard-rail-gates queue: **#466** (gate 56, the §4.5 S4 bare-normative-`shall` audit), **#467** (rotated TODO §4.5 S4 to DONE, the #466 in-window rotation finding, plus batched #466 QA), **#468** (gate 57, the §4.10 marked-done static detector, the content-side half of the rotation gate family), **#469** (the **D5 backlog-rotation-on-PR check**, the PR-time complementary half, **closing the §4.10 gate family**; §4.10 rotated out of TODO into DONE), **#470** (brought the authored `docs/` adopter guides under the gate-2 language/style audit and fixed all **117** accumulated house-style findings: 71 dashes, 25 `-ise`, 4 heading-case, 1 vendor name; the worked-example meta-tutorial exempted from heading-case/sanitisation/ensure; maintainer-decided "full house-style now", refining the earlier dash-only Option A; closed the P3 `docs/` enforcement-gap item), and **#471** (closed TODO §4.6 QA-cadence mechanical enforcement as substantially-satisfied by the existing gate 50 Check 1, which already enforces the missing-row half corpus-wide; the residual abbreviated-marker half is not FP-free-mechanizable on free prose, so it stays the CLAUDE.md no-QA-abbreviation convention; maintainer-decided "close 4.6 as satisfied"; rotated §4.6 to DONE, reworded its 4 TODO cross-refs, decremented P4 `8`->`7`). Gate count still **57** (the docs/ and §4.6-close work are scope/bookkeeping, not new gates). The #469 `/validate-pr` (0 findings) + `/retro` rows and the handoff refresh batched into #470; #470's own `/validate-pr` + `/retro` batch into the next PR. **Next queue (attended-autonomous; fix-issues-first holds):** §4.5 **S3** is PARKED (maintainer-deferred 2026-06-29 pending a clearer design, since gates 48/49/54 already cover control-code existence) and **§4.6 closed as satisfied by gate 50 Check 1** (#471), so next is the **§4.8 apply-time-discipline codifications** (now including the #469 `/retro` candidates: the gate-prose-collision close-out line, now 4x firm; "adding a PR-only delta gate Dn needs its step name in `WORKFLOW_DELTA_GATE_STEPS`"; "closing a numbered TODO §-section needs an intra-doc `§N` cross-ref cleanup pass"; plus the #462 table-edit-safety + #463 replace_all-scope notes) -> Sweep-3 follow-up + the ERC guideline-doc authorial reconcile -> FR-58 3-label scheme -> P1/P2 net-new content each via the high-assurance harness -> closing FR-167 whole-matrix `/matrix-fit`. Library `2026.06.447`->`.448` on #470, `.448`->`.449` on #471; spec `1.16.21`; pack live `1.53.5` (unchanged; not a pack change). **Sweep 77** over this session's deltas (#466 onward) is the loop-break control at the next session-closing handoff. The blocks below are the prior session's, retained until the next `/resume` prune.

**SESSION-CLOSING UPDATE (2026-06-29, this session's wind-down; closes at PR #464, the session-closing handoff PR).** Operating mode shifted mid-session from OVERNIGHT FULL AUTONOMY to **unattended (attended-autonomous)**: the maintainer returned, stated "we should not be in overnight mode, just unattended mode", and directed wind-down after PR #463. [`overnight-pr.md`](overnight-pr.md) was transitioned `in-flight` -> `stub` per the overnight protocol's awake-early exception (content already captured durably; the four former-overnight directives' substance remains a standing decision in [`pending-decisions.md`](pending-decisions.md), only the "no-answer maintains overnight" framing lapses). Session arc this run (resumed from #457): **#458** (the `/resume` Sweep-75 close-out + the FR-44 completion fix it escalated: #455 had missed [`dev-security/policy-secure-development-and-engineering.md`](../dev-security/policy-secure-development-and-engineering.md), 12 `shall`->`must`); **#459** (acted on the #458 `/retro`: verification-scope=claim-scope close-out-checklist line + §4.5 S4 gate candidate); **#460** (corrected the wind-down decision framework: default is continue, not hand off; degradation-evidence-only triggers); **#461** (codified the skeptical pre-push verification standard, 3-tier, + the [`verifier-overrides.md`](verifier-overrides.md) register); **#462** (gate 55, the S1 cross-document retention-consistency audit); **#463** (S2, this session's last substantive PR: promoted 5 maintainer roles into [`register-role-authority.md`](../governance/register-role-authority.md) and emptied the gate-8 allow-list, a register consolidation NOT a duplicate gate, after discovering gate 8 already did the role-consistency check). **#464** (this session-closing handoff PR) batches the #463 `/validate-pr` (0 findings, 1 frozen-record note) + `/retro` rows, transitions overnight-pr to `stub`, and refreshes this handoff + the asserted-expectations + green-at-`<sha>` + the [`session-metrics.md`](session-metrics.md) row; it skips its own trailing `/validate-pr` + `/retro` per the handoff-PR exception. **Next `/resume` runs a full corpus-wide `/validate` first** (the loop-break compensating control, Sweep 76, over the #458..#464 deltas), cross-checked against the Asserted expectations block below. **Still OPEN for the next session (unattended mode; green CI = merge authority):** the remaining guard-rail gates (§4.5 **S3** citation-precision, **S4** no-bare-normative-`shall`; §4.6 QA-cadence mechanical gate; §4.10 Option-B marked-done detector) -> §4.8 apply-time-discipline codifications (incl. the #462 table-edit-safety + the #463 replace_all-scope notes) -> Sweep-3 follow-up + the ERC guideline-doc authorial reconcile (the one genuinely-authorial deferral) -> FR-58 3-label scheme -> P1/P2 net-new content each via the high-assurance harness (FR-70 crypto, FR-73, FR-31/32/34/71/72, FR-59/61/62 per the download-then-defer directive) -> closing FR-167 whole-matrix `/matrix-fit`. The STANDING fix-issues-first directive holds. Library `2026.06.441`->`.442` on #464; pack live `1.53.2`. The prior-session blocks below are retained as the +1-prior set.

_Session history older than the current and immediately-prior session is pruned at each resume (keep current + 1 prior, per `## Refresh and pruning discipline` below). The durable record of pruned sessions lives in [`CHANGELOG.md`](../CHANGELOG.md) and its detailed mirror, [`DONE.md`](DONE.md), [`validate-sweeps/history.md`](validate-sweeps/history.md), [`session-metrics.md`](session-metrics.md), and git history._

## State snapshot (as of 2026-06-29, session-closing handoff PR #475; the attended-autonomous session that ran #466 through #474)

- **Current truth (verify against live files at `/resume`)**: work merged through **#474**; HEAD `main` = #474 (`cfa6e0c`); **PR #475 (this session-closing handoff PR)** in flight on branch `claude/resume-tw5ukk`. **Mechanical baseline green-at-`cfa6e0c` = 57/57** (the #474 merged-main state), re-confirmed at the #475 branch tip via the pre-push guard (#475 adds only `.working/` records + README version surfaces, none gate-affecting beyond what the guard checks). Library `2026.06.452` (to **`.453`** on #475), README `1.9.323` (to **`.324`**), pack live `1.53.5`, audit-spec `1.16.21`, register-role-authority `1.5.2`, canonical-citations register `1.5.5`, compliance matrix `1.11.3`. **Gate count 57**, **12 governance rules**, **17 skills**, **10 commands**, corpus doc count 311, corpus CSF-1.1-clean 0/311. **Standing register [`.working/verifier-overrides.md`](verifier-overrides.md)** carries NO `pending` override from this session (no verifier was overruled; the substantive PRs were bookkeeping/codification tier with no standing verifier, and the two `/validate-pr` findings were validated-and-fixed, not overruled). **[`pending-decisions.md`](pending-decisions.md): 0 pending** (the §4.5 S3 park and the §4.6 close-as-satisfied are both RESOLVED entries). **Operating mode: attended-autonomous** (green CI = merge authority; [`overnight-pr.md`](overnight-pr.md) `stub`). **This session owns both repos and may write to scratch** (MCP-PR transport; the git proxy 403s direct scratch pushes; no scratch write was needed this session). This session shipped #466-#471 (guard-rail-gate / §4.6-close work, per the block above) plus #472 (cross-FILE ref fix), #473 (close-out codification slice), #474 (gate-39-scope correction). **No net-new standalone corpus content shipped** (the harness-gated FR-70/73/etc. remain queued), so the high-assurance register has no new active rows. **Immediate next-`/resume` queue**: corpus-wide `/validate` (Sweep 77, #466..#475 deltas) FIRST, then the remaining §4.8 codifications -> Sweep-3 + ERC-guideline authorial reconcile -> FR-58 -> P1/P2 net-new under the harness -> closing FR-167 `/matrix-fit`; see the SESSION-CLOSING UPDATE block above. **Sweep 77** is the loop-break compensating control for this handoff.

## State snapshot (as of 2026-06-29, session-closing handoff PR #464; the unattended session that ran #458 through #463)

- **Current truth (verify against live files at `/resume`)**: work merged through **#463**; HEAD `main` = #463 (`9dc073c`); **PR #464 (this session-closing handoff PR)** in flight on branch `claude/session-handoff-2026-06-29`. **Mechanical baseline green-at-`9dc073c` = 55/55** (the #463 merged-main state), re-confirmed at the #464 branch tip via the pre-push guard (#464 adds only `.working/` records, the overnight-pr `stub` transition, and the README version surfaces, none gate-affecting beyond what the guard checks). Library `2026.06.441` (to **`.442`** on #464), README `1.9.312` (to **`.313`**), register-role-authority `1.5.2`, pack `1.53.2`, audit-spec `1.16.17`, canonical-citations register `1.5.5`, compliance matrix `1.11.3`. **Gate count 55**, **12 governance rules**, **17 skills**, **10 commands**, corpus doc count 311, corpus CSF-1.1-clean 0/311. **Standing register [`.working/verifier-overrides.md`](verifier-overrides.md)** carries NO `pending` override from this session (the #463 pre-push verifier could-not-refute; no override was logged). **Operating mode: unattended (attended-autonomous)** ([`overnight-pr.md`](overnight-pr.md) now `stub`; overnight mode ended this session by maintainer direction). **This session owns both repos and may write to scratch** (MCP-PR transport; the git proxy 403s direct scratch pushes). This session shipped #458 (Sweep-75 close-out + FR-44 completion), #459 (close-out-checklist + S4 candidate), #460 (wind-down framework correction), #461 (skeptical-verifier standard), #462 (gate 55, S1), #463 (S2 register consolidation). **No net-new standalone content shipped** (the harness-gated net-new items FR-70/73/etc. remain queued), so the high-assurance register has no new active rows. **Immediate next-`/resume` queue**: corpus-wide `/validate` (Sweep 76) FIRST, then guard-rail gates §4.5 S3/S4 -> §4.6 QA-cadence -> §4.10 Option-B -> §4.8 codifications -> Sweep-3 + ERC-guideline authorial reconcile -> FR-58 -> P1/P2 net-new under the harness -> closing FR-167 `/matrix-fit`; see the SESSION-CLOSING UPDATE block above. **Sweep 76** over the #458..#464 deltas is the loop-break control.

## Asserted expectations (session close-out convention)

**Purpose.** At session close, the closing session records here its *asserted-clean* claims, scoped to the surfaces it actually touched or verified this session (not a blanket corpus-wide clean bill). The next session's `/resume` runs its corpus-wide `/validate` (the loop-break compensating control) and cross-checks the findings against these claims: a finding that *contradicts* an asserted-clean touched surface is a genuine miss signal (the closing session's self-assessment was wrong), to be escalated rather than triaged as an ordinary out-of-window finding. A finding on a surface NOT in this list is an ordinary finding, handled by the normal in-window / out-of-window triage.

**Why this shape (and not a two-run diff).** A rejected alternative was: the closing session also runs a full `/validate`, passes the result forward, and the receiving session diffs the two. That is wasteful and noisy: the corpus is byte-identical across the boundary (same `main` commit), so there is no corpus delta for two runs to legitimately differ on, and `/validate`'s expensive layer is non-deterministic subagent fan-out, so two runs differ by LLM sampling variance, not corpus facts (a "discrepancy = major issue" rule would fire on noise). It also collides with the loop-break (a closing-session finding can only ride the prompt forward, the lossy state the handoff exists to avoid). The asserted-expectations shape gets the real signal (a contradiction of a claimed-clean touched surface) at the cost of a few lines, no second sweep. The only deterministic close-vs-start diff worth keeping is the mechanical one, captured by the green-at-`<sha>` line in the snapshot above.

**Scope discipline.** Assert clean ONLY what this session mechanically verified (a green gate) or covered with a formal `/validate-pr` or `/validate` subagent pass. Do not assert clean a domain the session did not touch; an unfounded clean claim turns an ordinary pre-existing finding into a spurious "major miss" at the next resume. This functions as a lightweight proxy for the `/validate-pr` the session-closing handoff PR skips (the loop-break): it records what the session's own QA actually covered, for the next `/validate` to test.

**This session's asserted expectations (2026-06-29 attended-autonomous session: #466-#471 guard-rail-gate / §4.6-close work, #472 cross-FILE ref fix, #473 close-out codification slice, #474 gate-39-scope correction; resumed from #464, closes at #475):**
- **The §4.6 close is mechanically clean** (#471): gate 50 ([`tools/lint-bookkeeping-parity.py`](../tools/lint-bookkeeping-parity.py)) Check 1 enforces the QA-cadence missing-row parity corpus-wide; [`TODO.md`](../TODO.md) carries no surviving `§4.6` heading or section-ref (the intra-doc-ref gate is green) and its P4 count reads `7`. A Sweep-77 finding of a surviving §4.6 section / live ref in TODO, or a gate-50 mis-classification of an in-window PR, would be a genuine miss. **(Carve-out: the abbreviated-marker residual is the CLAUDE.md no-QA-abbreviation convention, NOT gate-asserted; a finding about sham-QA-row detection is ORDINARY.)**
- **The cross-FILE §4.6/§4.10 "queued" carriers are removed** (#472, `/validate-pr` confirmed): [`.claude/CLAUDE.md`](../.claude/CLAUDE.md) and the [`tools/lint-bookkeeping-parity.py`](../tools/lint-bookkeeping-parity.py) docstring no longer describe §4.6 or §4.10 as queued / unbuilt future gates (both closed: §4.6 = gate 50 Check 1; §4.10 = gate 57 + the D5 PR-time check). Only `.working/` frozen archives retain historical "queued" wording. A Sweep-77 finding of a LIVE (non-frozen-`.working`) "queued §4.6 / §4.10" reference would be a genuine miss.
- **The two new close-out-checklist bullets are accurate against source** (#473 added, #474 corrected): the "Gate-39 count-phrasing" bullet's scope claims were independently re-verified by #474's `/validate-pr` against the linter source (`CHANGELOG.md` in gate 39's `EXEMPT_FILES`; `.claude` / `.working` in `DEFAULT_EXEMPT_DIRS` at [`tools/lint_common.py`](../tools/lint_common.py):54-56; `SCAN_SUFFIXES` = `.md`/`.py`/`.sh`); the "Section-close cross-FILE cleanup" bullet matches the intra-doc-ref gate's same-file-only behaviour. Both bullets live in [`.claude/CLAUDE.md`](../.claude/CLAUDE.md) (gate-exempt), so they are `/validate-pr`-asserted, not gate-asserted. A Sweep-77 prose finding of a residual mis-description in either bullet would be a genuine miss (correcting these was the cluster's whole purpose).
- **Both runners green** `tools/run_all_audits.sh` **57/57 at `cfa6e0c`/#474**, re-confirmed via the pre-push guard on the #475 branch tip (a `.working/` + version-surface change, none gate-affecting beyond the wiring the guard checks). `.working/` prose em/en-dash-free under gate 51. **green-at-`cfa6e0c` 57/57** (#475 is a descendant adding only bookkeeping + version surfaces). Four-surface parity at gate count **57** (gate 35 + gate 39 green).
- **Known soft spots (NOT asserted clean)**: the remaining **§4.8 apply-time codifications** are UNSTARTED (the gate-prose-collision close-out line, the `WORKFLOW_DELTA_GATE_STEPS` note, the #462 table-edit-safety + #463 `replace_all`-scope notes); **Sweep-3 follow-up**, the **ERC guideline-doc reconcile** (authorial, deferred), **FR-58**, and all **net-new content** (FR-70/73/31/32/34/71/72/59/61/62) UNSTARTED; **FR-167** OPEN. The [`tools/lint_common.py`](../tools/lint_common.py):21 docstring describes `DEFAULT_EXEMPT_DIRS` as only `.git`/`node_modules`/`__pycache__` while the actual frozenset (line 55) also has `.claude`/`.working`: a pre-existing incomplete-docstring nit, NOT touched this session, so a Sweep-77 finding there is ORDINARY, not a miss. §4.5 **S3** stays PARKED. **The routine next is Sweep 77** at the next `/resume` over the #466..#475 deltas, the loop-break compensating control for this handoff.

**Prior session's asserted expectations (2026-06-29 unattended session: #458 Sweep-75 close-out + FR-44 completion, #459-#462 guard-rail / standard work, #463 S2 register consolidation; resumed from #457, closed at #464):**
- **The S2 role consolidation is mechanically clean** (#463; pre-push verifier could-not-refute across 6 axes + post-merge `/validate-pr` 0 findings): [`governance/register-role-authority.md`](../governance/register-role-authority.md) carries the 5 new rows + `Version 1.5.2` / `Date 2026-06-29`; gate 8 ([`tools/lint-roles.py`](../tools/lint-roles.py)) resolves every corpus `Owner` / `Approving Authority` value with `known: 40` and `EXTRA_KNOWN_ROLES` empty; [`taxonomy.yml`](../taxonomy.yml) + [`docs/maturity-scorecard.md`](../docs/maturity-scorecard.md) are `--check`-in-sync; `lint-language.py` clean on the register. A Sweep-76 finding of an unresolved Owner/Approver role, a stale `EXTRA_KNOWN_ROLES` entry, or a register-vs-generated-artefact version mismatch would be a genuine miss. **(Carve-out: the 5 rows' accountability PROSE was maintainer-reviewed and approved; its semantic fit beyond gate-8 name-resolution is NOT gate-asserted, so a `/validate` prose finding there is ORDINARY, not a miss.)**
- **Gate 55 (cross-document retention-consistency, S1) is wired and green** (#462): four-surface parity at gate count **55** (gate 35 + gate 39 green); the 4-case regression fixture passes. A Sweep-76 finding of a four-surface parity break or a stale gate count on a live surface would be a genuine miss.
- **The FR-44 `shall`->`must` sweep is now complete corpus-wide** (#458 closed the #455 residual in [`dev-security/policy-secure-development-and-engineering.md`](../dev-security/policy-secure-development-and-engineering.md), 12 conversions; post-fix corpus-wide grep 0 residual bare normative `shall` outside the 3 preserved classes). A Sweep-76 finding of a residual bare normative `shall` in authored corpus prose would be a genuine miss.
- **The pack / `.claude/` changes (#459-#461) are mirror-parity clean** (gate 37 byte-identical across the mirrors; the skeptical-verifier-standard section is byte-identical between the pack [`ai-assistant-workflow-disciplines.md`](../dev-security/claude-rules/governance/ai-assistant-workflow-disciplines.md) and its `.claude/` copy). Pack `1.53.2`. A Sweep-76 mirror-drift finding would be a genuine miss.
- **Both runners green** `tools/run_all_audits.sh` **55/55 at `9dc073c`/#463**, re-confirmed via the pre-push guard on the #464 branch tip (a `.working/` + overnight-pr-`stub` + version-surface change, none gate-affecting beyond the wiring the guard checks). `.working/` prose em/en-dash-free under gate 51. **green-at-`9dc073c` 55/55** (#464 is a descendant adding only bookkeeping + version surfaces).
- **Known soft spots (NOT asserted clean)**: the guard-rail gates **§4.5 S3 / S4, §4.6 QA-cadence, §4.10 Option-B** are UNSTARTED; **§4.8 apply-time codifications** UNSTARTED (including the #462 table-edit-safety and #463 `replace_all`-scope notes); the **ERC guideline-doc reconcile is DEFERRED** (authorial); **FR-58** and all **net-new content** (FR-70/73/31/32/34/71/72/59/61/62) UNSTARTED; **FR-167** OPEN (closing `/matrix-fit` + gap-fill remain). The four former-overnight directives in [`pending-decisions.md`](pending-decisions.md) are maintainer-RESOLVED standing decisions, not open. **The routine next is Sweep 76** at the next `/resume` over the #458..#464 deltas, the loop-break compensating control for this handoff.

## Open decisions awaiting maintainer

- **§6.3-vs-§7.3 audit-scope spec gap**: **CLOSED in #338.** Sweep 45 found that #336's "added to all 9 explicit-list content linters" enumeration was incomplete: gate 31 (date-staleness) and gate 10 (review-cadence), two more explicit-allow-list content gates, silently skipped the dir (gate 31 a clear §6.3 violation since §6.3 names "date currency"). #338 added `.project-governance` to both (with a scope-coverage regression test each) and amended the spec with **§7.4**, which enumerates the explicit-allow-list content-linter completeness obligation (the maintainer chose, 2026-06-25, to include gate 10 and to amend the spec). The maintainer-decided spec amendment closes the decision; the **directory-scan-scope parity meta-check** (TODO P2) is the queued mechanical backstop that would have caught this class.
- **R2 relocation**: CLOSED by principle in #314 (the project-governance separation spec classifies the 6-file citation-verification cluster as project governance, so it migrates in Phase 1). The Phase-1 migration, the queued directional-dependency gate (§7.3), and the 2 deferred §5.3 classifications are now forward-looking TODO items, not open decisions awaiting the maintainer.
- **FR-167 mapping gate (asked 2026-06-24)**: RESOLVED, built as gate 49 (#325 ISO + NIST well-formedness; #326 NIST category membership + 17-cell remap closing §4.6a). The control-code-validity gate catches hallucinated/mistyped codes; semantic mapping correctness remains the orchestrator's apply-time job. No longer awaiting.
- **DD-12 scope**: RESOLVED + APPLIED (#329, maintainer-approved "broaden + extend gate 49"). DD-12 in TODO is broadened from `PR.IP`-only to all CSF-1.1 carriers (`PR.IP`/`ID.SC`/`ID.BE`/`RS.RP`/`DE.DP`/`PR.AC`/`PR.PT`/...; `RS.RP`→mainly `RS.MA`), and a **gate-49-extension** TODO item is queued (validate framework codes in per-document reference tables, not only the central matrix; build the scanner first, then gate the migration). Both are now forward-looking TODO items, not open decisions. The migration itself (corpus-wide, non-partitionable) is queued.
- **Publications-assessment process (§4.12, surfaced + queued 2026-06-25)**: a maintainer-directed process to assess `ref/publications/` for useful info AND detect poisoning/false info before it informs corpus work, queued as TODO §4.12 (not yet built). Forward-looking, not an open decision.
- **Maintainer actions still pending**: (a) **seed the scratch reference binaries** durably (git-proxy blocks scratch writes), belt-and-suspenders ONLY: the in-repo reference modules gates 48/49 use are the authoritative validators, so FR-167 batch 5+ code validity is gate-protected without them; re-attach only if a worker wants the source text; (b) **provision the least-privilege external-worker account** (read `grc_library` / write `grc_library_scratch` only), this is the gating action for both the separate-session worker primitive (§4.11) and the queued **§4.16** multi-session research-brief / `/subagent` capability. Neither blocks the next session's queue (in-session `Agent` fan-out works for FR-167 today).
- The larger-track decisions (FR-167 batches 4..N, the High[critical] net-new docs, the L/XL items) remain queued and scheduled deliberately.

## Trust-recovery state

The trust-recovery suite (`/full-qa` + `/fitness` r2) ran and the maintainer **signed off** (2026-06-22). Codification COMPLETE. Not re-triggered since; quality held throughout the 2026-06-24 overnight run, morning continuation, and the resume session (Sweep 38 + close-out #312, both warnings fixed first try, post-commit + pre-push green).

## Known environment behaviours (read before assuming working-tree state)

- **The stop-hook auto-commits AND pushes uncommitted changes on turn-end.** The working tree is auto-persisted to the feature branch; verify `git log` rather than assuming edits are held locally.
- **The squash-merge commit on `main` shows locally as `E noreply@github.com`** (GitHub's own merge commit; Verified in the GitHub UI, not locally verifiable). After syncing the branch to `main` the tip equals `origin/main`; **do NOT `git commit --amend`** it (that rewrites merged `main` history). Set `git config user.email noreply@anthropic.com && git config user.name Claude` so your own feature-branch commits are attributed correctly.
- **A squash-merge makes the local feature branch diverge from `main`** (the squash commit is new; the local pre-squash commit is its content but a different SHA), so `git merge --ff-only origin/main` fails after a merge; use `git reset --hard origin/main` to re-sync the feature branch (the local commit's content is in the squash, preserved in reflog).
- **The GitHub MCP server can disconnect mid-session** (it did 2026-06-24 after #304, blocking #305's open/merge). `git push` over HTTPS keeps working; only the MCP-routed GitHub-API operations are affected. If `mcp__github__*` tools vanish: keep committing and pushing via `git`, record the pending-merge state in the handoff, and resume the PR lifecycle when MCP returns. Do NOT merge by any non-MCP path. Full writeup: [`third-party-issues.md`](third-party-issues.md).
- **The commit-signing server can 503**, distinguish from a real defect (gates 31/40 pass on the real corpus while gate 36's failures are `git commit` subprocess errors); re-run after a pause. Full writeup: [`third-party-issues.md`](third-party-issues.md).
- **The clone may start shallow**; `git fetch --unshallow` before any history-aware audit (gates 31/40).
- **The `Bash` tool's working directory can reset to a non-repo directory between calls** (observed twice this session after long async-subagent gaps: a bare `git ...` failed with "not a git repository"). Prefix git-command sequences with `cd /home/user/grc_library &&` rather than assuming the cwd persisted.
- **After a post-merge sync you are ON `main`; `git checkout -B <feature-branch>` BEFORE building the next PR.** The post-merge sync (`git checkout main && git pull origin main`) leaves you on `main`. If you then build and `git commit` the next PR without switching branches, you commit to local `main` (which the stop-hook flags as an unverified commit ahead of `origin/main`), and a subsequent `git push -u origin <feature-branch>` pushes the STALE feature-branch ref (still at the prior PR's pre-squash commit), so the PR opens with an empty/wrong diff. This happened on the #361 handoff (the commit landed on local `main` as `2b589cd`; recovery was `git checkout -B <feature> <commit>` to repoint the feature branch, `git branch -f main origin/main` to undo the local-main commit, then a force-push of the feature branch). Recovery is clean and on-branch (no `main` push, no shared-history rewrite), but the fix is cheaper than the catch: switch to the feature branch immediately after the sync.
- **Operating mode is now attended-autonomous, not overnight** (set 2026-06-26, codified in [`.claude/CLAUDE.md`](../.claude/CLAUDE.md) #349): green CI = standing merge authority (merge green PRs and proceed without per-merge approval), stricter-is-safer always, and the pending-decisions / 2-minute-timeout graceful-degradation mechanism ([`pending-decisions.md`](pending-decisions.md), currently empty). `overnight-pr.md` is `stub` (overnight mode is OFF); do not treat it as in-flight. The `/resume` command now has a step 7 that checks `pending-decisions.md` before the queue.

## Standing disciplines (do not drift from these)

- **PRIMORDIAL RULE**: Quality > Speed > Cost; integrity absolute. Emit the integrity-check line at task start, before commit, before completion claims, and at tension points.
- **Post-commit audit**: after every commit, run `tools/run_all_audits.sh` standalone before pushing; never chain commit and push. Before push, also `tools/run-pr-time-checks.sh`.
- **`lint-language` pre-flight on new pack prose** before the first commit (catches em-dashes / British `-ise`). (Relevant for DD-4/5's `go.md` rewrite.)
- **Pre-commit dash-grep on new root `CHANGELOG.md` lines** (`grep -nP "\xe2\x80\x93|\xe2\x80\x94"`): the D3 dash gate is PR-time only, so a late-assembled CHANGELOG entry can slip an em-dash that surfaces at pre-push; grep the added lines before the first commit.
- **Grep-after-wiring / convention change**: after editing a restated-across-surfaces value, `grep` the OLD form across the full changed file AND every sibling surface; zero hits before commit.
- **Corpus-wide-grep / special-case-the-edge / find-every-carrier**: a mechanical bulk edit (a rename, a relabel, a title alignment) must verify EVERY adjacent field, special-case the known edge cases (e.g. preserve rating "Critical" while fixing impact-5; the DD-2/3/11 grep found a 4th carrier beyond the 3 named), and search corpus-wide for the OLD form, not apply one convenient rule uniformly.
- **Sweep close-out bookkeeping triplet**: a `/validate` close-out PR regenerates the generated artefacts (after per-doc version bumps), advances the TODO sweep cursor (gate 45 + the gate-36 TodoStaleness regression both enforce it), and bumps `validate-sweeps/history.md`'s own Version. The post-commit audit catches all three; doing them pre-commit avoids the fail-then-fix loop.
- **60-second** paired fallback timer after every `subscribe_pr_activity`.
- **Per-PR QA**: formal `/validate-pr` (Subagent A) + `/retro` after every merge; no skip, no abbreviation. **One standing exception**: the session-closing handoff PR skips both (loop-break); the compensating control is the corpus-wide `/validate` that `/resume` runs first. (This exception is now generalized into the pack layer, #305.)
- **PR close-out checklist** (the degradation guard): prior PR's `/validate-pr` row AND `/retro` row both batched in; closed TODO items rotated to DONE (every path-shaped code span in a CHANGELOG entry linked, changed or merely mentioned); stale-count check if an enumerated collection changed; session-handoff.md refreshed.
- **Self-assess for degradation / session-continuation SOP**: the default is to CONTINUE. Wind down ONLY on actual drift/hallucination/mistakes the QA layer did not catch; work size/shape, session length, and "a large / substantial / fresh-context-best series remains" are NOT triggers (corrected #460, maintainer 2026-06-29: about 13 of the last 15 proposed handoffs were the wrong call). A large series ahead is worked PR-by-PR with skeptical verifier subagents sustaining quality, not handed off. Steady-state misses the per-PR `/validate-pr` catches are NOT drift. The assistant may OFFER a handoff for the maintainer to consider before a substantial/critical/long piece of work begins, as a non-default suggestion (the maintainer chooses; absent a choice, continue). **When a handoff IS warranted (genuine degradation evidence), do NOT take it silently: run the `## Wind-down decision framework` in [`.claude/CLAUDE.md`](../.claude/CLAUDE.md)** (surface the justification + a per-PR likelihood assessment of the next-five PRs + named options A handoff / B recommended additional PRs / C higher-risk order / D the Ulysses-pact impulse-check; roughly-2-minute timer defaulting to handoff on no answer; codified #375, corrected #460).
- **Compute-don't-ask**: before surfacing a question, apply a "can I compute/verify this myself?" gate; surface only the result or a decision that genuinely needs maintainer judgement. (Counterpart: surface a genuine design tradeoff before building, as S5's FP surface was surfaced with named options rather than guessed.)

## Refresh and pruning discipline

**Refresh (at session close).** This file is refreshed at every PR close-out (post-merge), as part of the recursion-avoidance batch that carries the validate-pr/retro rows into the next PR. The session-closing handoff PR prepends this session's new blocks (the "Next actions" narrative block, the "State snapshot", and the "Asserted expectations" block) as the new *current* set.

**Prune (at resume, in the first PR).** Because the closing session only ever *adds*, the per-session blocks would otherwise accumulate without bound and bury the load-bearing state (the file's one job is a fast, reliable single resume point). So the receiving session prunes, as part of its first PR (the `/validate` close-out, per `## How to resume` step 6):

- **Retention: keep current + 1 prior.** Keep the most-recent session's blocks and the single immediately-prior session's blocks in each per-session stack (the "Next actions" narrative blocks, the "State snapshot" blocks, the "Asserted expectations" blocks). Delete everything older. Also delete superseded one-off "Next-session queue" / dated "This session's work" sections.
- **Keep the standing sections in full** (not per-session): the header / `## How to resume` / `## Resume cursor`, the `## Open decisions awaiting maintainer`, `## Trust-recovery state`, `## Known environment behaviours`, `## Standing disciplines`, and this section. These are durable reference, not session narrative.
- **Migrate-before-delete (the safety rule).** Before deleting a block, confirm any load-bearing item it carries that is NOT recorded elsewhere (an open decision, a pending maintainer action) has been migrated to `## Open decisions awaiting maintainer` / [`TODO.md`](../TODO.md) / [`pending-decisions.md`](pending-decisions.md). The durable record of pruned session *narrative* already lives in [`CHANGELOG.md`](../CHANGELOG.md) + its detailed mirror, [`DONE.md`](DONE.md), [`validate-sweeps/history.md`](validate-sweeps/history.md), [`session-metrics.md`](session-metrics.md), and git history, so narrative is safe to drop; only un-recorded forward-looking items need migrating.

The companion close-out-checklist line and the `/resume` command step encode this so it is a standing convention, not a one-off. This convention was added 2026-06-28 by maintainer direction after the file reached 426 lines / 47 stacked session blocks; the first prune cut it to ~120.
