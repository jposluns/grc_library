# Session Handoff

**Purpose.** This file is the single resume point for a new Claude Code session. It is refreshed at every PR close-out so that opening a fresh session and continuing requires only one instruction from the maintainer (see "How to resume"). It exists because long sessions degrade (context dilution, lossy compaction, state drift, error compounding); a fresh session that rebuilds state from this file plus the durable repo artefacts is more reliable than a long one running on accumulated memory. The mechanisms and mitigations are written up generically in [`.working/session-length-considerations.md`](session-length-considerations.md). This file is maintainer working state, exempt from corpus audit gates per the `.working/` exemption.

This is an **as-of-last-refresh snapshot**, not a live-HEAD claim. Versions and counts drift forward as work advances; always verify against live files before relying on them.

## How to resume (the one command)

In a new session, the maintainer sends only: **`/resume`** (or "read `.working/session-handoff.md` and continue").

On `/resume`, the assistant:
0. **Runs the concurrency lease check FIRST** (`/resume` step 0, shipped with the section-3.7 interlock): read [`session-state.md`](session-state.md) from `main` and git-cross-check unmerged `origin/claude/*` sibling branches; 60-minute staleness window, advisory HOLD on a live signal (never proceed on a timeout); ACQUIRE the lease on proceeding. Gate 63 guards the lease file's shape.
1. Reads this file in full, including the **"Known environment behaviours"** section below.
2. Reads `.claude/CLAUDE.md` (the PRIMORDIAL RULE and project disciplines), the most recent few `CHANGELOG.md` entries, and **[`.working/third-party-issues.md`](third-party-issues.md)**.
2a. **Loads the scratch reference index into memory** (`grc_library_scratch/ref/INDEX.md`, `ref/catalogue.yml`, `ref/SECTION-INDEX.md`, `ref/COVERAGE-MAP.md`) so the held reference set and its versions are known before any task; the index, not a guessed path, is authoritative for what is held and where. See `/resume` command's Reference-knowledge-base step and the `## Reference-version currency` SOP in `.claude/CLAUDE.md`.
3. Runs `git rev-parse --is-shallow-repository` (unshallow with `git fetch --unshallow` if `true`, **before any history-aware audit**), then `tools/run_all_audits.sh` to confirm the corpus is green, and `git log --oneline -5` to confirm HEAD.
4. Verifies the version/count snapshot below against live files.
4a. **Batches the unattended-run clarifications up front (maintainer-directed 2026-06-25)**: before resuming the queue, asks the maintainer (via `AskUserQuestion`, batched into as few rounds as possible) every decision that would let the session run unattended longer, pre-populating decisions for **all Priority 1 and Priority 2 backlog items** and **the next 10-plus planned PRs** (including any outside P1/P2, e.g. the FR-167 batches). Compute-don't-ask first (ask only genuine authorial decisions, not what the assistant can verify); record the answers in the next-actions queue so they survive a mid-session compaction; mark deferrals and route around them. See `/resume` command step 5 for the authoritative form.
5. **Runs a full corpus-wide `/validate` as the first substantive task** (the loop-break compensating control for whatever session-closing handoff PR last closed, which skips its trailing `/validate-pr` + `/retro`): full A/B/C subagent dispatch over the closing session's delta window, recorded in [`validate-sweeps/history.md`](validate-sweeps/history.md) and the Resume-cursor section above. **Cross-check the findings against the "Asserted expectations" section below**: a contradiction of an asserted-clean touched surface is a genuine miss, escalated; an ordinary finding routes to the backlog. Standing invariants to confirm: the corpus is CSF-1.1-clean (gate 54, 0 carriers; a carrier in a per-document NIST table is a genuine finding, not residue); four-surface parity at the current gate count (gate 35); the gate/rule/skill/command counts (gate 39 + prose). The specific sweep number and delta window are in the current "Next actions" block below.
6. **Prunes this file as part of that first `/validate` close-out PR** (keep current + 1 prior, per `## Refresh and pruning discipline` below), so the handoff does not accumulate stale session history across resumes.
7. Continues from "Next actions" below.

## Resume cursor (machine-readable)

This is the relocated home of the former `## Session resume metadata` block (moved out of
[`TODO.md`](../TODO.md) in PR #413 so TODO stays purely forward-looking). The sweep cursor below is
read by [`tools/lint-todo-staleness.py`](../tools/lint-todo-staleness.py) (gate 45), which flags it
if it falls behind [`validate-sweeps/history.md`](validate-sweeps/history.md). The full sweep history
lives in that history file; the live per-session state (branch, versions, counts, green-at-`<sha>`) is
in the "Next actions" blocks below and is an as-of-last-refresh snapshot (drift forward is expected).

- **Last validation sweep**: Sweep 82 iter 1 (2026-07-02; the loop-break corpus-wide `/validate` over the #550..#551 deltas plus the #552 handoff, run at the `/resume` from session-closing handoff #552; full three-subagent dispatch. Mechanical baseline **59/59** at `cd017e1`/#552 (= `origin/main`), matching the #552 green-at-`7afb1f6` baseline (`7afb1f6` is `cd017e1`'s parent; no close-vs-start drift). Pre-flight: 393 files, 28 suppressed, 11 candidates, all dismissed FP by Subagent B (comparative/growth pack prose, the Sweep-81 classes). **A: 3 findings (2 M, 1 L), all in-window on live bookkeeping surfaces; B and C: 0 findings.** The three: a markdown link in the #552 [`session-metrics.md`](session-metrics.md) row to the removed cross-file section-ref draft linter (broken at write-time); the #552 handoff/metrics/CHANGELOG-detailed surfaces re-listing item 32/SR-5 as NEEDS-UPSTREAM although #551 upgraded it to CONFIRMED via upstream verification; and SR-5's stale "line ~222" pointer to the [`TODO.md`](../TODO.md) §5.1 carrier. All three fixed in this close-out PR (Sweep-19-precedent termination: fixes applied iter 1, verified by direct contradiction greps; re-dispatching the fan-out would be corroboration-only). **Asserted-expectations cross-check: NO contradiction** (the two warnings are on `.working/` surfaces #552 did not assert clean; B confirmed counts 59/12/17 and versions library 2026.07.40 / README 1.9.401 / pack 1.53.10 plus 63/63 Related-Documents targets; C confirmed four-surface parity at 59, D1-D5 wiring, 12/12 pack-mirror sync, generated artefacts in sync). Detail file: [`validate-sweeps/2026-07-02-sweep82-iter1.md`](validate-sweeps/2026-07-02-sweep82-iter1.md).)

## Next actions (the queue for the next session)

**PROGRESS (2026-07-02, the session resumed from the #552 handoff; overnight run then daytime-unattended, then attended, then OVERNIGHT again from the end of #589 (maintainer-confirmed 2026-07-03Z); the 2026-07-03 overnight run closed at #607 with FR-48 COMPLETE, eighteen first-run-green merges #590 to #607; mode switched to DAYTIME-UNATTENDED at the maintainer's 2026-07-03 morning return; current through #610; the day queue after the 2026-07-03 morning decision rounds: #608 morning processing MERGED, #609 D5/CLAUDE.md codifications MERGED, #610 gate 62 + guardrail r3 MERGED, #611 the section-3.7 concurrency lease + gate 63 MERGED, #612 the section-3.6 close-out (gate 50 check-3 worker-provenance activation) MERGED, both injected priorities landed and the plain second-session-safe report DELIVERED to the maintainer, #613 the section-3.15 GR-3 wave-2 parser migration (gate 40 / D2 / D4 onto the shared `head_version()`) MERGED first-run-green, #614 the section-3.15 guardrail hygiene batch MERGED first-run-green (GR-9 on every live carrier plus the functionally-broken `verify-reference-modules.py` repaired, S-4, GR-11, r3 O-F2; its sweep: 1 Medium + 3 Low + 2 Informational, all four actionable fixed in the next PR), #615 the sweep-fix rider PR in flight (runbook 1.0.9 obligation-1 currency, matrix-fit command module list, `GRC_SCRATCH_REF` typo hardening, boundary-token wording, the #614 QA trio), then GR-4, GR-6 behind its /guardrails prerequisite, the remaining r3 bullets, section-3.1 sweeps, P2 gap-fills, and the scratch SR wave).** The `/resume` ran **Sweep 82** first (3 in-window bookkeeping findings, fixed in its close-out #553; no asserted-expectations contradiction), then the maintainer-authorized **overnight run shipped eighteen green merges #553 to #570** (the guardrail-enabler wave #554 to #559, the C-wave C1 to C10 #560 to #569, and M1 #570, the ~2050-flip spelling harmonization under the full HA harness). The maintainer awoke mid-M1 and switched to **daytime-unattended**; the morning directives (scratch ISO/EPUB keep-tracked with watermark scrub, the mode-exit priority ordering, cleanup-first) executed in **#571** (overnight cleanup). Then the fix-tier and QA/guardrails steps of the accepted queue: **#572** (section-2.13 item 11, the DSR statutory clocks, source-verified), **#573** (section-3.14 mechanical batch A), **#574** (section-3.14 judgment tier, two logged stricter-safe retention resolutions), **#575** (the GR-12 residual-scan aid), **#576** (the section-3.13 gates-widening: gate 55 to eight retention pairs, D5 to six closure forms, gate 50 dual-spelling marker, plus the #575 sweep fixes), **#577** (GR-5: the guardrail-review cadence mechanized as gate 60, keyed on the newest history row's inventory token, with the 2026-07-02 review's missing r2 record backfilled from the intake artefacts and the drift threshold logged as a proceeded default), **#578** (L-k: the pre-commit regen hook moved after the check hooks, plus the #577 sweep dispositions), **#579** (L-j: the Classification lifecycle overload migrated to a Status: Superseded marker across every live carrier, three linters re-keyed with fixtures, plus the #578 sweep dispositions), **#580** (the two flat AI retention rows composed with both example echoes co-changed per the verifier's bare-token catch, plus the #579 sweep dispositions; the L-2 vocabulary fork routed to TODO 3.14), **#581** (the coverage-gaps register's eleven stale Coverage cells re-graded with corpus evidence across twelve re-grade events, closing the section-3.14 triage bullet, plus the #580 sweep dispositions; its records initially shipped pre-verifier counts and the superseded Kubernetes grade, reconciled in #582 per the #581 sweep), **#582** (GR-P3 shipped as the `tools/tension-scan.py` advisory aid on census evidence, the aid shape maintainer-CONFIRMED at the return round; the #581 sweep's record-correction cluster fixed and its second coverage-gaps triage batch routed to TODO 3.14; plus the #581 QA rows), **#583** (the gate-60 fail bar tightened to drift 3 per the maintainer's return-round redirect, fixtures re-pinning the boundary, suite 275 to 276; the #582 sweep's six findings dispositioned, four fixed in-window; plus the #582 QA rows; the #582 sweep agent stalled silently mid-run and was recovered by a status probe, the incident behind the maintainer's new 60-second task-check SOP, codifying in the CLAUDE.md batch PR), **#584** (the authorized CLAUDE.md batch: the advisory-directive codification, the D5 six-form refresh, the 60-second background-task SOP, the eight resolved pending-decisions entries rotated; plus the #583 QA rows, the #583 sweep having returned zero findings above Informational; its pre-push verifier's two staleness SHOULD-FIXes and three accuracy NITs fixed pre-push), **#585** (the lifecycle-vocabulary alignment: the four supersession-vocabulary carriers aligned to the Status: Superseded marker per the return-round answer, Retired kept as the distinct removal stage, the routed section-3.14 fork rotated to DONE; plus the #584 QA rows; its sweep returned 1 Low, fixed in-window by #586's rotation, and 2 Informational), **#586** (the second coverage-gaps batch: sixteen register rows re-graded with evidence verified at file, the Partial definition widened per the return-round answer; plus the #585 QA rows; its sweep routed the out-of-enumeration coverage residue as the third triage batch), and **#587** (gate 61, the COBIT/ISO 31000 citation-existence audit, both halves of the section-3.13 build per the maintainer's answer: the reference module derived deterministically from the held extracts under the HA harness, the /matrix-fit extension, three day-one catches fixed, the seven fit-pass recode verdicts routed to their own PR; suite 276 to 284, gate count 60 to 61, pack 1.53.14; plus the #586 QA rows; MERGED as 55b911a, its sweep returning 1 Medium fixed in-window plus 4 Low and 5 Informational). **#588 (MERGED as 78cb1d6; its sweep: 3 Low + 4 Informational, 4 of 7 fixed in-window in #589, 3 log-only)** executed the recode worklist: the seven routed fit-pass verdicts, the sweep-M-1 fold-in (all four mis-titled MEA01 cells in the compliance-management table), the ISO 31000 6.6 clause-number correction on its three carriers, the gate-2 verbatim-external-title exemption its own verbatim MEA01.05 title surfaced (linter span-mask, two fixtures, suite 284 to 286, audit-spec 1.16.39), the #587 QA trio, and the sweep's five in-window fixes (HA register 1.0.16, the section-2.13 intro re-anchor, the two 48/49/54 standing-surface widenings, the #587 mirror addendum). Concurrently, a maintainer-supplied ISO bundle (ISO 31000:2018, IEC 31010:2019, the screened practical guide) was ingested to scratch by a background worker and MERGED as scratch PR #48 (validate CI green, watermark scrub verified twice), un-gating the ISO-31000 half of the section-3.13 citation gate. **A 12-question maintainer decision round (three AskUserQuestion rounds, all answered 2026-07-02) de-blocked the next 20+ TODO items**; the answers are recorded against their queue items: gate-55 author-both-sentences; GR-5 mechanize; L-k reorder; L-j lifecycle-marker migration (add `**Status:** Superseded`, re-key the three linters, restore Classification: Public); compose both flat AI retention rows; coverage-gaps re-grade with evidence; advisory-directive codification approved to ship; all six proceeded defaults CONFIRMED (move to resolved); section-3.7 lease all three defaults (60-min staleness, advisory HOLD, well-formedness gate same PR); scratch SR wave all three defaults (SR-1 presence+backfill, SR-4 drop failed extract, SR-5 re-bucket); the stale June branches already maintainer-deleted; FR-70 and the ISO-31000 gate half deferred 1-2 weeks pending the maintainer's local egress instance. **Queue after #582** (re-ordered by the maintainer's 2026-07-02 return round, which resolved every held item: CLAUDE.md batch authorized "ship all three"; gate-60 threshold REDIRECTED to fail-at-3; GR-P3 aid shape CONFIRMED; lifecycle vocabulary ALIGN-to-Superseded; coverage batch 2 EXECUTE re-grade-with-evidence; coverage vocabulary WIDEN Partial's definition): (1) the gate-60 fail-at-3 redirect PR (MERGED as #583); (2) the CLAUDE.md batch PR (MERGED as #584); (3) the lifecycle-vocabulary alignment PR (MERGED as #585); (4) the coverage-gaps batch-2 PR (MERGED as #586); (5) the section-3.13 build (MERGED as #587); (6) the COBIT fit-pass recode PR (MERGED as #588); (6a) the small-fixes PR (MERGED as #589); (6b) the section-1.3 DPO role-separation sweep (MERGED as #590; its sweep: 1 Medium + 1 Low + 4 Informational, the Medium and Low fixed in-window in #591) (was IN FLIGHT as #590: the composite separated across ten documents, the PIA acceptance authority applied per the answer, sections 1.3 and 2.13 closed, four proceeded defaults logged in pending-decisions, the #589 QA trio; the overnight-pr file transitioned to in-flight); (7) the coverage-gaps third triage batch (routed); then FR-48 section-1.1 to completion (the accepted P1 direction) interleaved with section-3.15 remaining machinery (GR-3 waves 2-3, GR-4, GR-6 behind its /guardrails sequencing prerequisite, GR-8 to GR-11; the routed gate-60/gate-39/D5 candidates closed in #595); scratch SR-1..SR-5 via MCP PR; section-3.7 concurrency lease; then 3.6, 4.4, 2.11 and the supervised multi-session pilot (3.8 closed 2026-07-03, not-instrumented accepted as permanent). Versions at #586: library `2026.07.74`, README `1.9.435`, pack `1.53.13`, audit-spec `1.16.37`, gate count **60**, suite **276** (at #587: library `2026.07.75`, README `1.9.436`, pack `1.53.14`, audit-spec `1.16.38`, gate count **61**, suite **284**; at #588: library `2026.07.76`, README `1.9.437`, audit-spec `1.16.39`, suite **286**; at #589: library `2026.07.77`, README `1.9.438`; at #590: library `2026.07.78`, README `1.9.439`; at #591: library `2026.07.79`, README `1.9.440`; at #592: library `2026.07.80`, README `1.9.441`; at #593: library `2026.07.81`, README `1.9.442`; at #594: library `2026.07.82`, README `1.9.443`; at #595: library `2026.07.83`, README `1.9.444`, suite **287**; at #596: library `2026.07.84`, README `1.9.445`; at #597: library `2026.07.85`, README `1.9.446`; at #598: library `2026.07.86`, README `1.9.447`; at #599: library `2026.07.87`, README `1.9.448`, pack `1.53.15`; at #600: library `2026.07.88`, README `1.9.449`; at #601: library `2026.07.89`, README `1.9.450`; at #602: library `2026.07.90`, README `1.9.451`; at #603: library `2026.07.91`, README `1.9.452`; at #604: library `2026.07.92`, README `1.9.453`; at #605: library `2026.07.93`, README `1.9.454`; at #606: library `2026.07.94`, README `1.9.455`; at #607: library `2026.07.95`, README `1.9.456`; at #608: library `2026.07.96`, README `1.9.457`; at #609: library `2026.07.97`, README `1.9.458`, audit-spec `1.16.44`, the section-1.6 D5 seventh form + CLAUDE.md D5/RM-10 codifications shipped; at #610: library `2026.07.98`, README `1.9.459`, audit-spec `1.16.45`, pack `1.53.16`, gate count **62** (the cross-file section-reference audit), suite **289**, guardrail review r3 run in-PR per the gate-60 threshold firing, template `1.4.0`; at #611: library `2026.07.99`, README `1.9.460`, audit-spec `1.16.46`, gate count **63** (the session-state lease audit), suite **298**; at #612: library `2026.07.100`, README `1.9.461`, audit-spec `1.16.47`, suite **304**, gate 50 check 3 ACTIVE). **Maintainer priority injection (2026-07-03): sections 3.7 (concurrency lease) then 3.6 residue (worker-provenance gate) are the next two substantive PRs after #610 and any High findings**, so the maintainer's second session can start safely; the maintainer holds the second session until both land. Scratch `WORKER-ONBOARDING.md` shipped (scratch PR #49) for the second-account Model-B launch. **The 2026-07-03 morning rounds (two AskUserQuestion rounds, all answered)** confirmed the four DPO defaults, authorized the D5/RM-10 CLAUDE.md codifications (TODO section 1.6), granted the P2 stricter-safe-values-with-logged-defaults model, confirmed scratch SR-wave self-merge-on-green, authorized the protected-tree factual-staleness one-liner class, decided KEEP for the TODO audit-trail sections (sections 3.10 and former-7.1 closed), decided section 2.5 as methodology-doc-only, closed section 3.8 (not-instrumented permanent), and reaffirmed the FR-70 / needs-reconfirm / ISO-31000 egress deferrals. **Day queue from here (mode-exit ordering):** #608 (morning processing, this PR); the section-1.6 D5 PR; section-3.15 wave 2 (the cross-file section-ref gate FIRST per the #602 retro, then GR-3 wave 2, GR-4, GR-6 with its /guardrails prerequisite, GR-8(b), GR-9, S-4, GR-10, GR-11); the section-3.1 sweep pair (27001 headers, stray-punctuation cells); the P2 gap-fills (2.9, 2.5, 2.4, 2.1, 2.2 under the HA harness); the scratch SR wave via MCP PR; section-3.7 lease. **Still pending for the maintainer:** the GitHub MCP token needed re-authorization at the #575 sweep (retried at the next PR call); provisioning least-privilege worker accounts (the multi-session dependency).

**SESSION-CLOSING UPDATE (2026-07-02, this session's wind-down; closes at PR #552, the session-closing handoff PR; MAINTAINER-DIRECTED wind-down, not degradation).** This session resumed from #549, ran the loop-break **Sweep 81** first (0 findings across all three subagents; #550), then: **#550** (Sweep-81 close-out + handoff prune); built + tested the **cross-file section-reference gate (numbers phase)** and **PAUSED** it pending FR-48 completion (maintainer decision B: build/ship it against a fully-numbered corpus so its coverage is complete and its one blocking finding resolves in the FR-48 lockstep renumber; design + FP-lessons preserved in [`cross-file-section-ref-gate-design.md`](cross-file-section-ref-gate-design.md), the unwired draft removed from `tools/`); and **#551** (TODO intake of the 2026-07-02 read-only audit: 35 validated findings across P1 §1.6-1.8, P2 §2.12-2.14, P3 §3.13-3.14, P4 §4.6, P7 §7.4-7.5, and a new `## Scratch reference-base work` SR-1..SR-5 section; intake ONLY, no fixes). **#552 (this handoff PR)** batches the #551 `/validate-pr` (0) + `/retro` rows, refreshes this handoff + the `## Asserted expectations` block + the green-at-`7afb1f6` snapshot + the [`session-metrics.md`](session-metrics.md) row; it skips its own trailing `/validate-pr` + `/retro` per the handoff-PR exception (marker in its `validate-pr/history.md` Findings cell). **Wind-down rationale: maintainer directed it** ("Once done with the TODO updates, wind down and hand off"); no degradation signal (every PR shipped green; the QA layer caught and fixed all authoring slips pre-push). **Next `/resume` runs a full corpus-wide `/validate` first (Sweep 82 over the #550..#551 deltas)**, cross-checked against this session's `## Asserted expectations` block below, and PRUNES this handoff (keep current + 1 prior: the #552-session + the #549-session; the #518-session blocks removed). **Open queue for the next session (maintainer directs the order):** (1) the **FR-48 §1.1 inline-clause remainder** (~13 hard-class docs + `internal-audit`, one doc per PR via the high-assurance harness, Option A deep-hierarchical); **completing it UNBLOCKS the paused cross-file section-ref gate**, which then gets built + four-surface-wired + shipped against the clean corpus per its preserved design (run it advisory-only after each renumber to backstop the #548-class miss meanwhile); (2) **remediation of the #551 audit-intake findings** (fix-first: P1 §1.6-1.8 first), where **items 7/9/10 (BASC year, COBIT APO12 titles, ANPD regulation) are NEEDS-UPSTREAM** (confirm the external value before the fix lands; item 32/SR-5's ETSI TS 104 223 designation was upgraded to CONFIRMED via upstream verification at #551, so its fix is not upstream-gated; this cell originally re-listed 32/SR-5 as NEEDS-UPSTREAM, corrected by Sweep 82) and several §2.13/§1.8 items need a which-value-governs decision; (3) the earlier-planned diagnostic sequence **`/full-qa` + `/fitness`** (not yet run this session). **Pending decisions to surface at `/resume`:** the advisory-directive codification (protected-file morning item), §7.4 organisation-spelling (~1026 vs 3), §7.5 the 22 tracked O'Reilly EPUBs, and the FR-70 source gate (MiCA / NYDFS not held). Library `2026.07.39` (to **`2026.07.40`** on #552); README `1.9.400` (to **`1.9.401`**).

**SESSION-CLOSING UPDATE (2026-07-02, this session's wind-down; closes at PR #549, the session-closing handoff PR).** NOTE: this file's older narrative blocks below are STALE (their latest reflects ~#523); the FR-48 §1.1 single-document renumber run continued through **#548** without an intervening session-close, so this block is the authoritative current state (the per-PR record is [`validate-pr/history.md`](validate-pr/history.md) + [`fr48-deferred-worklist.md`](fr48-deferred-worklist.md)). **Current truth (verify at `/resume`):** work merged through **#548**; HEAD `main` = #548 (`50acd0b`), **59/59 green**; **PR #549 (this session-closing handoff)** in flight on branch `claude/session-handoff-post-548`. **FR-48 §1.1: 25 of 38 deferred docs done.** #548's post-merge `/validate-pr` (Subagent A, 16-file diff) returned **0 in-window findings + 1 Low advisory** (the `TODO.md`:28 "4 pack files" undercount, FIXED in this handoff); #548's `/retro` logged the inbound-discovery-grep-too-narrow pattern (3rd occurrence) whose durable fix is the cross-file section-ref gate below. Both #548 QA rows batch into this #549 per recursion-avoidance. **Wind-down rationale (clean batch boundary, NOT degradation):** 25 renumber PRs done, main green; the maintainer chose a fresh session before the next, fresh-context-class workstream. No degradation signal drove this close (the #548 pre-push verifier caught a real 2-file miss + 2 stale counts, all fixed pre-push, the QA layer working).

**CONFIRMED NEXT-SESSION PLAN (maintainer-accepted 2026-07-02; OVERNIGHT-authorized, extreme QA):** run in order, each phase triage->fix-or-update-TODO: (1) **`/resume` corpus-wide `/validate` FIRST** (the loop-break control; Sweep 81 over the deltas since Sweep 80, i.e. #518 through #548, the FR-48 renumber run); NOTE the current gate set is BLIND to the cross-file section-ref class, so that class surfaces at phase 2, not here; (2) **build the cross-file section-reference gate** `lint-cross-file-section-refs.py` and run it corpus-wide; (3) **`/full-qa`**; (4) **`/fitness`**. This reaches a fully-understood/fully-trusted base (ACHIEVED at the maintainer's morning sign-off, not declared overnight). (5) **Then fix-first**: fix the identified issues (especially TODO-added ones) BEFORE resuming file work. (6) **Then the FR-48 inline-clause remainder** (Option A deep-hierarchical `## N.`/`### N.M`/`#### N.M.K` + remap every inline-clause citation, via the high-assurance harness with independent sub-agent validation before accepting each; one doc per PR) plus the `internal-audit` doc.

**OVERNIGHT + APPROVAL-DEFERRAL (governs the whole next session):** overnight authorized for the FR-48 remainder + the diagnostic sequence, extreme QA (spawn workers as needed, verify everything, fix on find; if unfixable, defer to morning). **Reversibility gate on every fix:** fixable + reversible + on-branch + non-protected -> fix (substantive fix gets a refute-briefed verifier; sensitive gets the full high-assurance harness). **DEFER to morning-decisions** ([`pending-decisions.md`](pending-decisions.md)), never guess: protected-file edits (`.claude/CLAUDE.md`, `.claude/`, `dev-security/claude-rules/` pack rules), authorial decisions, generated-artefact policy, anything outward-facing. TODO updates themselves are fine overnight (working-state). **Trust-recovery suite (`/full-qa` + `/fitness`) sign-off lands in the MORNING:** overnight, FIX only mechanical/reversible/unambiguous findings; ROUTE every judgment-call/semantic/authorial finding to TODO + `pending-decisions.md` for the maintainer's morning review-and-sign-off (do NOT silently close). `overnight-pr.md` is `stub` at this handoff; the next session transitions it to `in-flight` when overnight work begins and the morning-processing PR resets it.

**CROSS-FILE SECTION-REF GATE, design (phase 2 of the plan):** `lint-cross-file-section-refs.py` fills the one uncovered quadrant (verified read-only prep this session): `lint-intra-doc-refs.py` deliberately FILTERS OUT cross-doc refs; `lint-section-anchors.py` covers only the `[text](path.md#anchor)` link form; NO gate validates cross-file "Section N"/§N cited in prose (the swift/kotlin near-miss class). **Numbers phase** (strict, low-FP): every cross-file "Section N"/§N resolves to a real heading in the target doc. **Names phase** (FP-prone, scope to structured/declared refs only): a ref pairing a number with a quoted heading title matches the target's actual heading. Must handle the **document-level-declaration binding** ("section numbers below refer to that standard" binding subsequent bare "Section N" to a named target), the exact #548 miss. **Reuse:** `lint-intra-doc-refs.py`'s `extract_sections()` + INVERT its `is_cross_doc_context()`; `lint-section-anchors.py` target resolution. **Deliver as TWO PRs** (numbers-phase gate first, wired + green; names-phase gate second, FP-tuned) per always-split. It is a new gate -> full 4-surface wiring + §6 spec inventory + regression fixture + a skeptical (or high-assurance) verifier.

**MORNING-DECISION ITEM (maintainer-directed 2026-07-02, deferred to morning per its protected-file scope):** codify a standing advisory-directive. The behaviour is ALREADY documented (the `surface-counterproductive-instructions` pack rule + the CLAUDE.md "'Suggest'/'advise' invite assessment" convention) and is binding now; the codification ELEVATES it into one prominent standing expectation in `.claude/CLAUDE.md`: *proactively surface a better / more-efficient / higher-quality alternative, and disagree when a choice looks against the project's best interests, ANY time you see one, not only when the maintainer says "suggest"/"advise"; surface the disagreement WITH its reasoning and give the maintainer an opportunity to change their mind; the maintainer retains override; governed by INTEGRITY > SPEED > COST*, cross-linked to the pack rule. It is a **protected-file edit -> its own dedicated morning PR** (draft in [`pending-decisions.md`](pending-decisions.md)); NOT bundled into this bookkeeping handoff (always-split + the maintainer's own protected-file-defer rule).

**Standing constraints unchanged:** **FR-70 source gate** (MiCA / NYDFS BitLicense not held; DORA is), the **51 needs-reconfirm register rows**. Library `2026.07.36` (to **`2026.07.37`** on #549); README `1.9.397` (to **`1.9.398`**).

**THIS RESUMED SESSION (2026-07-01, `/resume` from session-closing handoff #518; ATTENDED-AUTONOMOUS).** Resumed at HEAD `d53c145` = `origin/main` (the merged #518), **58/58 green**, matching the #518 green-at-`ec1a822` baseline (no close-vs-start drift). Unshallowed the clone; verified versions (library `2026.07.6`, pack `1.53.8`, audit-spec `1.16.27`); read pending-decisions (2 open: §3.2, §3.3) + verifier-overrides (0 pending); loaded the scratch `ref/` index. **Maintainer `/resume` args: "Ending overnight mode. Priority 1 items first please."** Operating mode returned to **attended-autonomous** (self-merge green + high-assurance harness on net-new). Resume clarification batch answered: **P1 = FR-48 H2 numbering-pattern rename now** (the dedicated session it was deferred for); **§3.2 = cutoff-scoped detailed-mirror header-parity gate**; **§3.3 = register-vs-held-extract consistency cross-check, no register row**; **post-P1 = P3 tooling cleanup** (the §3.2/§3.3 gates + other P3 items) before P2 content. Ran the mandatory loop-break **Sweep 80** FIRST (corpus-wide `/validate` over the #513..#517 deltas): **0 findings** across all three subagents (A recent-PR deep review, B corpus-wide stale-reference, C audit-programme integrity), NO contradiction of the #518 asserted expectations; recorded in [`validate-sweeps/history.md`](validate-sweeps/history.md). **This first PR (the Sweep-80 close-out):** the Sweep 80 row + this handoff prune (keep current + 1 prior: #518-session + #513-session; #502-session + per-PR #504..#512 detail removed) + the §3.2/§3.3 pending-decision resolutions + version bumps. **Immediate next:** (1) **FR-48** (the P1 dedicated-session H2 rename; corpus-wide, three current patterns [fully-numbered / fully-unnumbered / hybrid], canonical-pattern choice put to the maintainer before applying); then (2) **P3 tooling** (§3.2 gate, §3.3 cross-check, other P3 items). **FR-70 source gate** (MiCA / NYDFS BitLicense not held) and the **51 needs-reconfirm register rows** remain standing constraints. Library `2026.07.6` (to **`2026.07.7`** on this PR); README `1.9.367` (to **`1.9.368`**).

**PROGRESS (2026-07-01, continuing this session; now UNATTENDED per maintainer directive "change to unattended mode, push-notify a question if I need to turn back").** Shipped since the Sweep-80 close-out: **#519** (Sweep-80 close-out + handoff prune + §3.2/§3.3 resolutions), **#520** (FR-48 safe subset: 28 policy/standard docs to fully-numbered; the 38-doc entangled residual deferred to [`fr48-deferred-worklist.md`](fr48-deferred-worklist.md), TODO §1.1), **#521** (gate 59, CHANGELOG mirror header-parity audit; **closes §3.2**). **#522** (TODO §3.3 register-vs-scratch citation consistency cross-check under model B, no register row, **0 mismatches** across 33 ISO/NIST/CSA/COBIT/MITRE citations, **closes §3.3**), **#523 (this PR)** (maintainer-authorized protected `.claude/CLAUDE.md` cleanup: §3.11 wind-down SOP refinements + §3.14 section-close checklist reinforcement + §3.13 closed-as-decided; **closes §3.11, §3.13, §3.14**, keeps standing §3.12; carries the batched #522 `/validate-pr` (0) + `/retro` rows and this refresh). **Overnight mode confirmed OFF (maintainer, 2026-07-01): unattended DAY mode**; `overnight-pr.md` is `stub` (nothing to process). **Remaining P3 (all now maintainer-gated or non-trivial):** §3.1 (term consistency; under-specified, real fix is a mechanical gate), §3.4 (effort-sizing labels convention; touches pack SKILLs, scheduled after the FR backlog), §3.6/§3.7/§3.8 (maintainer-scheduled design, open sub-decisions), §3.10-(c) (a maintainer call on whether Standing-conventions/Backlog-totals/Priority-7 sections belong in TODO). The **directed autonomous P3-tooling queue (§3.2, §3.3) plus the authorized protected cleanup (§3.11/§3.13/§3.14) are now complete.** Standing constraints unchanged: **FR-70 source gate** (MiCA / NYDFS BitLicense not held) and the **51 needs-reconfirm register rows**. The `/retro` for #521 surfaced a **3rd-occurrence** CHANGELOG-narrative-vs-actual-edit pattern (after #512, #519), which #522 then did NOT recur (the #521 self-reminder held); its proposed-improvement candidate now landed as part of §3.14 / §3.11 in #523. Library `2026.07.10` (to **`2026.07.11`** on #523); README `1.9.371` (to **`1.9.372`**).

_Session history older than the current and immediately-prior session is pruned at each resume (keep current + 1 prior, per `## Refresh and pruning discipline` below). The durable record of pruned sessions lives in [`CHANGELOG.md`](../CHANGELOG.md) and its detailed mirror, [`DONE.md`](DONE.md), [`validate-sweeps/history.md`](validate-sweeps/history.md), [`session-metrics.md`](session-metrics.md), and git history._

## State snapshot (as of 2026-07-02, session-closing handoff PR #552; the resumed session that ran Sweep 81 + #550 + the paused cross-file gate + #551 audit-intake)

- **Current truth (verify against live files at `/resume`)**: work merged through **#551**; HEAD `main` = #551 (`7afb1f6`); **PR #552 (this session-closing handoff PR)** in flight on branch `claude/resume-9r1drh`. **Mechanical baseline green-at-`7afb1f6` = 59/59** (the #551 merged-main state), re-confirmed at the #552 branch tip via the pre-push guard (#552 adds only `.working/` records + README version surfaces, none gate-affecting beyond what the guard checks). Library `2026.07.39` (to **`2026.07.40`** on #552), README `1.9.400` (to **`1.9.401`**), pack `1.53.10`, audit-spec `1.16.28`. **Gate count 59** (no gate added this session; the cross-file section-ref gate is BUILT but PAUSED/unwired pending FR-48, design in [`cross-file-section-ref-gate-design.md`](cross-file-section-ref-gate-design.md)). **[`pending-decisions.md`](pending-decisions.md): 1 open** (the advisory-directive codification, protected-file morning item) + standing FR-70 source gate; the gate-vs-FR-48 sequencing decision was RESOLVED (complete FR-48 first). **verifier-overrides: 0 pending.** **Operating mode this session: attended (maintainer reachable, directed the gate pause and the wind-down).** No net-new corpus document shipped this session (Sweep + close-out + TODO intake only), so the high-assurance register has no new active rows. **Immediate next-`/resume` queue**: corpus-wide `/validate` (Sweep 82, #550..#551 deltas) FIRST + prune this handoff; then the maintainer-directed order among FR-48 remainder (unblocks the paused gate), #551 audit-intake remediation (fix-first, P1 first, items 7/9/10 NEEDS-UPSTREAM; SR-5 upstream-CONFIRMED at #551, a third stale carrier corrected by the #553 `/validate-pr`), and the `/full-qa` + `/fitness` diagnostic sequence. **Sweep 82** is the loop-break compensating control for this handoff.

## State snapshot (as of 2026-07-02, session-closing handoff PR #549; the FR-48-renumber-tail session that ran the single-document renumbers through #548)

- **Current truth (verify against live files at `/resume`)**: work merged through **#548**; HEAD `main` = #548 (`50acd0b`); **PR #549 (this session-closing handoff PR)** in flight on branch `claude/session-handoff-post-548`. **Mechanical baseline green-at-`50acd0b` = 59/59** (the #548 merged-main state), re-confirmed at the #549 branch tip via the pre-push guard (#549 adds only `.working/` records + the gate-exempt `TODO.md`:28 fix + README version surfaces, none gate-affecting beyond what the guard checks). Library `2026.07.36` (to **`2026.07.37`** on #549), README `1.9.397` (to **`1.9.398`**), pack live `1.53.10` (bumped #548). **Gate count 59** (gate 59 CHANGELOG-mirror header-parity added earlier this run). **[`pending-decisions.md`](pending-decisions.md): FR-70 source gate open + the morning codification item (this session)**; verifier-overrides carries NO `pending` override from this session (the #548 pre-push verifier findings were all validated-and-fixed, none overruled). **Operating mode: attended-autonomous this session; the next `/resume` is OVERNIGHT-authorized** (extreme QA; protected-file edits + authorial decisions + trust-recovery-suite judgment findings all deferred to morning). **FR-48 §1.1: 25 of 38 deferred docs done; the ~13 remaining are the inline-clause hard class (Option A deep-hierarchical) + `internal-audit`, sequenced AFTER the diagnose-and-fix phase.** No net-new corpus document shipped in #548 (a renumber + citation remap), so the high-assurance register has no new active rows from it. **Immediate next-`/resume` queue** (confirmed plan): corpus-wide `/validate` (Sweep 81, #518..#548 deltas) FIRST + prune this handoff; then build the cross-file section-ref gate (2 PRs) + run it; then `/full-qa`; then `/fitness`; each triage->fix-or-TODO; then fix-first; then the FR-48 inline-clause remainder under the high-assurance harness. **Sweep 81** is the loop-break compensating control for this handoff.

## Asserted expectations (session close-out convention)

**Purpose.** At session close, the closing session records here its *asserted-clean* claims, scoped to the surfaces it actually touched or verified this session (not a blanket corpus-wide clean bill). The next session's `/resume` runs its corpus-wide `/validate` (the loop-break compensating control) and cross-checks the findings against these claims: a finding that *contradicts* an asserted-clean touched surface is a genuine miss signal (the closing session's self-assessment was wrong), to be escalated rather than triaged as an ordinary out-of-window finding. A finding on a surface NOT in this list is an ordinary finding, handled by the normal in-window / out-of-window triage.

**Why this shape (and not a two-run diff).** A rejected alternative was: the closing session also runs a full `/validate`, passes the result forward, and the receiving session diffs the two. That is wasteful and noisy: the corpus is byte-identical across the boundary (same `main` commit), so there is no corpus delta for two runs to legitimately differ on, and `/validate`'s expensive layer is non-deterministic subagent fan-out, so two runs differ by LLM sampling variance, not corpus facts (a "discrepancy = major issue" rule would fire on noise). It also collides with the loop-break (a closing-session finding can only ride the prompt forward, the lossy state the handoff exists to avoid). The asserted-expectations shape gets the real signal (a contradiction of a claimed-clean touched surface) at the cost of a few lines, no second sweep. The only deterministic close-vs-start diff worth keeping is the mechanical one, captured by the green-at-`<sha>` line in the snapshot above.

**Scope discipline.** Assert clean ONLY what this session mechanically verified (a green gate) or covered with a formal `/validate-pr` or `/validate` subagent pass. Do not assert clean a domain the session did not touch; an unfounded clean claim turns an ordinary pre-existing finding into a spurious "major miss" at the next resume. This functions as a lightweight proxy for the `/validate-pr` the session-closing handoff PR skips (the loop-break): it records what the session's own QA actually covered, for the next `/validate` to test.

**This session's asserted expectations (2026-07-02 resumed session: Sweep 81 + #550 + paused cross-file gate + #551 audit-intake; closes at #552):**
- **Both runners green** `tools/run_all_audits.sh` **59/59 at `7afb1f6`/#551**, re-confirmed via the pre-push guard on the #552 branch tip (a `.working/` + version-surface change, none gate-affecting). `.working/` prose em/en-dash-free under gate 51. **green-at-`7afb1f6` 59/59** (#552 is a descendant adding only bookkeeping + version surfaces). Four-surface parity at gate count **59** (gate 35 + gate 39 green).
- **Sweep 81 (this session's loop-break control for #549) returned 0 findings** across all three subagents over the #518..#548 deltas, with NO contradiction of #549's asserted expectations (Subagent A re-confirmed #548's renumber + 129-ref remap complete/correct). So the #518..#548 delta window is clean per this session's verification; a Sweep-82 re-surfacing of a genuine miss there would contradict this.
- **The #551 TODO audit-intake is gate-clean and internally consistent** (verified by the post-merge `/validate-pr` Subagent A, 8-file diff, 0 findings): intra-doc-ref + gate 39 + gate 45 green; the Backlog-totals counts match the actual new subsections (P1=6/P2=14/P3=9/P4=6/P7=3-pending/Scratch=5); the new intra-TODO `§`-refs resolve; and the three load-bearing corrective claims are accurate at source (item 5's 72h+denial-self-review are in `procedure-data-subject-rights-management.md` not the template; item 8's fabricated A.5.36 title is x2 not x3; L-j `Classification: Deprecated` at :10). A Sweep-82 finding of a dangling `§`-ref, a stale Backlog-totals count, or an inaccurate corrective claim in the new TODO subsections would be a genuine miss. **(Carve-out: the audit-intake FINDINGS themselves are RECORDED, not fixed; see soft spots.)**
- **The cross-file section-ref gate is PAUSED and inert**: no `tools/lint-cross-file-section-refs.py` exists on `main`, the gate is unwired, and `run_all_audits.sh` reports **59** gates (no gate-60). A Sweep-82 finding that the gate leaked into a wired/shipped state would be a genuine miss; its design lives only in [`cross-file-section-ref-gate-design.md`](cross-file-section-ref-gate-design.md).
- **Known soft spots (NOT asserted clean)**: the **#551 audit-intake findings are recorded defects awaiting remediation, NOT verified-clean surfaces** (a Sweep-82 or `/full-qa` re-surfacing of any P1 §1.6-1.8 / P2 §2.12-2.14 / etc. finding is EXPECTED and ORDINARY, not a miss). **Items 7/9/10 (BASC year, COBIT APO12 titles, ANPD regulation)** are NEEDS-UPSTREAM (item 32/SR-5's ETSI designation was upstream-CONFIRMED at #551; this line originally re-listed SR-5 as NEEDS-UPSTREAM, corrected by Sweep 82). The **cross-file `Section N`/§N class remains gate-blind corpus-wide** (the gate is paused pending FR-48). The **~13 FR-48 inline-clause hard-class docs + `internal-audit`** are UNSTARTED. **FR-70** source-gated (MiCA / NYDFS not held). The **51 needs-reconfirm register rows** and the **advisory-directive codification** remain standing. **The routine next is Sweep 82** at the next `/resume` over the #550..#551 deltas.

**Prior session's asserted expectations (2026-07-02 FR-48-renumber-tail session: single-document renumbers through #548; closes at #549):**
- **Both runners green** `tools/run_all_audits.sh` **59/59 at `50acd0b`/#548**, re-confirmed via the pre-push guard on the #549 branch tip (a `.working/` + gate-exempt `TODO.md`:28 + version-surface change, none gate-affecting beyond what the guard checks). `.working/` prose em/en-dash-free under gate 51. **green-at-`50acd0b` 59/59** (#549 is a descendant adding only bookkeeping + the `TODO.md`:28 fix + version surfaces). Four-surface parity at gate count **59** (gate 35 + gate 39 green).
- **#548's mobile-application-security renumber + 129-ref pack-file remap is complete and correct** (verified by the post-merge `/validate-pr` Subagent A, 16-file diff, 0 in-window findings across all 7 classes): the standard's H2s are gapless 1-19, the 3 intra self-refs remap correctly to `§4`/`§10`/`§4-§12`, all 129 "Section N" refs across the 7 pack files map old-N->new-N+2 with no stale "Section 1"/"Section 2" survivor, the pack README is 1.53.10 with a version-history row, and taxonomy/portal/scorecard are `--check` in sync. A Sweep-81 finding of a stale/off-by-one "Section N" ref in any of the 7 pack files, or a broken heading target in the standard, would be a genuine miss. **(Carve-out: the cross-file section-ref class corpus-WIDE beyond #548's own 7 citers was NOT gate-verified this session; see soft spots.)**
- **`TODO.md`:28 undercount fixed** (#549): the (b)-class example's "4 pack files" corrected to "6 languages files + README (7 total), renumbered in #548, done". A Sweep-81 finding of a surviving "4 pack files" undercount there would be a genuine miss.
- **Known soft spots (NOT asserted clean)**: **the cross-file "Section N"/§N reference class is GATE-BLIND corpus-wide**: only #548's own 7 citers were manually verified (by Subagent A's `grep -rln`); every OTHER cross-file section reference in the corpus is unverified, which is exactly what phase-2's `lint-cross-file-section-refs.py` will check (expect findings there, ORDINARY not misses). **FR-70** source-gated (MiCA / NYDFS not held; DORA is). The **~13 remaining FR-48 inline-clause hard-class docs + `internal-audit`** are UNSTARTED (deferred to after the diagnose-and-fix phase). The **51 needs-reconfirm register rows** remain the standing §1.5 residual. **The routine next is Sweep 81** at the next `/resume` over the #518..#548 deltas, the loop-break compensating control for this handoff.

## Open decisions awaiting maintainer

- **RESOLVED (2026-07-01 at `/resume`; recorded in [`pending-decisions.md`](pending-decisions.md)):** the two 2026-07-01 pending decisions were answered by the maintainer. (1) **§3.2 detailed-CHANGELOG-mirror header-parity** = **cutoff-scoped gate** (accept the 3 pre-split-era missing headers #268/#353/#462 as an exemption; scope the parity gate from a cutoff PR forward; FP-free, no retroactive edit). (2) **§3.3 citation-verification recording-model** = **consistency cross-check, no register row** (register-vs-held-extract check surfacing/fixing mismatches only; the §9 human-primary-capture trust model unchanged). Both are now **P3 tooling** work, scheduled after the P1 FR-48 rename per the maintainer's post-P1 = P3-tooling direction.
- **OPEN (surfaced 2026-07-01): FR-70 source gate.** FR-70 (crypto-asset / blockchain governance, XL, H[critical], authorized this run via the harness) is source-gated: DORA is held in scratch but **MiCA and NYDFS BitLicense are NOT**, and the high-assurance harness for a H[critical] regulatory domain needs held ground truth. Needs the maintainer to supply MiCA / NYDFS sources into scratch `ref/`, OR authorize authoring the crypto-regulatory core from training knowledge (advised against), OR redirect. The DORA-anchored operational-resilience slice is sourceable now.
- **§6.3-vs-§7.3 audit-scope spec gap**: **CLOSED in #338.** Sweep 45 found that #336's "added to all 9 explicit-list content linters" enumeration was incomplete: gate 31 (date-staleness) and gate 10 (review-cadence), two more explicit-allow-list content gates, silently skipped the dir (gate 31 a clear §6.3 violation since §6.3 names "date currency"). #338 added `.project-governance` to both (with a scope-coverage regression test each) and amended the spec with **§7.4**, which enumerates the explicit-allow-list content-linter completeness obligation (the maintainer chose, 2026-06-25, to include gate 10 and to amend the spec). The maintainer-decided spec amendment closes the decision; the **directory-scan-scope parity meta-check** (TODO P2) is the queued mechanical backstop that would have caught this class.
- **R2 relocation**: CLOSED by principle in #314 (the project-governance separation spec classifies the 6-file citation-verification cluster as project governance, so it migrates in Phase 1). The Phase-1 migration, the queued directional-dependency gate (§7.3), and the 2 deferred §5.3 classifications are now forward-looking TODO items, not open decisions awaiting the maintainer.
- **FR-167 mapping gate (asked 2026-06-24)**: RESOLVED, built as gate 49 (#325 ISO + NIST well-formedness; #326 NIST category membership + 17-cell remap closing §4.6a). The control-code-validity gate catches hallucinated/mistyped codes; semantic mapping correctness remains the orchestrator's apply-time job. No longer awaiting.
- **DD-12 scope**: RESOLVED + APPLIED (#329, maintainer-approved "broaden + extend gate 49"). DD-12 in TODO is broadened from `PR.IP`-only to all CSF-1.1 carriers (`PR.IP`/`ID.SC`/`ID.BE`/`RS.RP`/`DE.DP`/`PR.AC`/`PR.PT`/...; `RS.RP`→mainly `RS.MA`), and a **gate-49-extension** TODO item is queued (validate framework codes in per-document reference tables, not only the central matrix; build the scanner first, then gate the migration). Both are now forward-looking TODO items, not open decisions. The migration itself (corpus-wide, non-partitionable) is queued.
- **Publications-assessment process (§4.12, surfaced + queued 2026-06-25)**: a maintainer-directed process to assess `ref/publications/` for useful info AND detect poisoning/false info before it informs corpus work, queued as TODO §4.12 (not yet built). Forward-looking, not an open decision.
- **Maintainer actions still pending**: (a) **seed the scratch reference binaries** durably (git-proxy blocks scratch writes), belt-and-suspenders ONLY: the in-repo reference modules gates 48/49 use are the authoritative validators, so FR-167 batch 5+ code validity is gate-protected without them; re-attach only if a worker wants the source text; (b) **provision the least-privilege external-worker account** (read `grc_library` / write `grc_library_scratch` only), this is the gating action for both the separate-session worker primitive (§4.11) and the queued **§4.16** multi-session research-brief / `/subagent` capability. Neither blocks the next session's queue (in-session `Agent` fan-out works for FR-167 today).
- The larger-track decisions (FR-167 batches 4..N, the High[critical] net-new docs, the L/XL items) remain queued and scheduled deliberately.

## Trust-recovery state

The trust-recovery suite (`/full-qa` + `/fitness` r2) ran and the maintainer **signed off** (2026-06-22). Codification COMPLETE. Not re-triggered since; quality held throughout the 2026-06-24 overnight run, morning continuation, and the resume session (Sweep 38 + close-out #312, both warnings fixed first try, post-commit + pre-push green).

## Known environment behaviours (read before assuming working-tree state)

- **The stop-hook auto-commits AND pushes uncommitted changes on turn-end.** The working tree is auto-persisted to the feature branch; verify `git log` rather than assuming edits are held locally.
- **The squash-merge commit on `main` shows locally as `E noreply@github.com`** (GitHub's own merge commit; Verified in the GitHub UI, not locally verifiable). After syncing the branch to `main` the tip equals `origin/main`; **do NOT `git commit --amend`** it (that rewrites merged `main` history). Set `git config user.email noreply@anthropic.com && git config user.name Claude` so your own feature-branch commits are attributed correctly.
- **A squash-merge makes the local feature branch diverge from `main`** (the squash commit is new; the local pre-squash commit is its content but a different SHA), so `git merge --ff-only origin/main` fails after a merge; use `git reset --hard origin/main` to re-sync the feature branch (the local commit's content is in the squash, preserved in reflog).
- **The GitHub MCP server can disconnect mid-session** (it did 2026-06-24 after #304, blocking #305's open/merge). `git push` over HTTPS keeps working; only the MCP-routed GitHub-API operations are affected. If `mcp__github__*` tools vanish: keep committing and pushing via `git`, record the pending-merge state in the handoff, and resume the PR lifecycle when MCP returns. Do NOT merge by any non-MCP path. Full writeup: [`third-party-issues.md`](third-party-issues.md).
- **The commit-signing server can 503**, distinguish from a real defect (gates 31/40 pass on the real corpus while gate 36's failures are `git commit` subprocess errors); re-run after a pause. Full writeup: [`third-party-issues.md`](third-party-issues.md).
- **The clone may start shallow**; `git fetch --unshallow` before any history-aware audit (gates 31/40).
- **The `Bash` tool's working directory can reset to a non-repo directory between calls** (observed twice this session after long async-subagent gaps: a bare `git ...` failed with "not a git repository"). Prefix git-command sequences with `cd /home/user/grc_library &&` rather than assuming the cwd persisted.
- **After a post-merge sync you are ON `main`; `git checkout -B <feature-branch>` BEFORE building the next PR.** The post-merge sync (`git checkout main && git pull origin main`) leaves you on `main`. If you then build and `git commit` the next PR without switching branches, you commit to local `main` (which the stop-hook flags as an unverified commit ahead of `origin/main`), and a subsequent `git push -u origin <feature-branch>` pushes the STALE feature-branch ref (still at the prior PR's pre-squash commit), so the PR opens with an empty/wrong diff. This happened on the #361 handoff (the commit landed on local `main` as `2b589cd`; recovery was `git checkout -B <feature> <commit>` to repoint the feature branch, `git branch -f main origin/main` to undo the local-main commit, then a force-push of the feature branch). Recovery is clean and on-branch (no `main` push, no shared-history rewrite), but the fix is cheaper than the catch: switch to the feature branch immediately after the sync.
- **Operating-mode mechanics** (set 2026-06-26, codified in [`.claude/CLAUDE.md`](../.claude/CLAUDE.md) #349): green CI = standing merge authority (merge green PRs and proceed without per-merge approval), stricter-is-safer always, and the pending-decisions / 2-minute-timeout graceful-degradation mechanism ([`pending-decisions.md`](pending-decisions.md)). The `/resume` command has a step 7 that checks `pending-decisions.md` before the queue. **Current mode: DAYTIME-UNATTENDED (maintainer-set 2026-07-02, mid-M1)**: green CI merges; maintainer decisions are surfaced by push notification plus a choice list with the short timer, proceeding on logged stricter-safe defaults if unanswered; irreversible or outward-facing items still hold. The 2026-07-02 overnight run (#553 to #570) ended when the maintainer awoke; `overnight-pr.md` was routed and reset to `stub` in #571 under the awake-early exception. The mode-exit priority ordering (overnight cleanup, then fixes, then tooling and protections, then new work) is codified in `.claude/CLAUDE.md`.

## Standing disciplines (do not drift from these)

- **PRIMORDIAL RULE**: Quality > Speed > Cost; integrity absolute. Emit the integrity-check line at task start, before commit, before completion claims, and at tension points.
- **Post-commit audit**: after every commit, run `tools/run_all_audits.sh` standalone before pushing; never chain commit and push. Before push, also `tools/run-pr-time-checks.sh`.
- **`lint-language` pre-flight on new pack prose** before the first commit (catches em-dashes / British `-ise`). (Relevant for DD-4/5's `go.md` rewrite.)
- **Pre-commit dash-grep on new root `CHANGELOG.md` lines** (`grep -nP "\xe2\x80\x93|\xe2\x80\x94"`): the D3 dash gate is PR-time only, so a late-assembled CHANGELOG entry can slip an em-dash that surfaces at pre-push; grep the added lines before the first commit.
- **Grep-after-wiring / convention change**: after editing a restated-across-surfaces value, `grep` the OLD form across the full changed file AND every sibling surface; zero hits before commit.
- **Corpus-wide-grep / special-case-the-edge / find-every-carrier**: a mechanical bulk edit (a rename, a relabel, a title alignment) must verify EVERY adjacent field, special-case the known edge cases (e.g. preserve rating "Critical" while fixing impact-5; the DD-2/3/11 grep found a 4th carrier beyond the 3 named), and search corpus-wide for the OLD form, not apply one convenient rule uniformly.
- **Sweep close-out bookkeeping triplet**: a `/validate` close-out PR regenerates the generated artefacts (after per-doc version bumps), advances the TODO sweep cursor (gate 45 + the gate-36 TodoStaleness regression both enforce it), and bumps `validate-sweeps/history.md`'s own Version. The post-commit audit catches all three; doing them pre-commit avoids the fail-then-fix loop.
- **60-second** paired fallback timer after every `subscribe_pr_activity`.
- **Per-PR QA**: formal `/validate-pr` (Subagent A) + `/retro` after every merge; no skip, no abbreviation. **One standing exception**: the session-closing handoff PR skips both (loop-break); the compensating control is the corpus-wide `/validate` that `/resume` runs first. (This exception is now generalized into the pack layer, #305.)
- **PR close-out checklist** (the degradation guard): prior PR's `/validate-pr` row AND `/retro` row both batched in; closed TODO items rotated to DONE (every path-shaped code span in a CHANGELOG entry linked, changed or merely mentioned); stale-count check if an enumerated collection changed; session-handoff.md refreshed.
- **Self-assess for degradation / session-continuation SOP**: the default is to CONTINUE. Wind down ONLY on actual drift/hallucination/mistakes the QA layer did not catch; work size/shape, session length, and "a large / substantial / fresh-context-best series remains" are NOT triggers (corrected #460, maintainer 2026-06-29: about 13 of the last 15 proposed handoffs were the wrong call). A large series ahead is worked PR-by-PR with skeptical verifier subagents sustaining quality, not handed off. Steady-state misses the per-PR `/validate-pr` catches are NOT drift. The assistant may OFFER a handoff for the maintainer to consider before a substantial/critical/long piece of work begins, as a non-default suggestion (the maintainer chooses; absent a choice, continue). **When a handoff IS warranted (genuine degradation evidence), do NOT take it silently: run the `## Wind-down decision framework` in [`.claude/CLAUDE.md`](../.claude/CLAUDE.md)** (surface the justification + a per-PR likelihood assessment of the next-five PRs + named options A handoff / B recommended additional PRs / C higher-risk order / D the Ulysses-pact impulse-check; roughly-2-minute timer defaulting to handoff on no answer; codified #375, corrected #460).
- **Compute-don't-ask**: before surfacing a question, apply a "can I compute/verify this myself?" gate; surface only the result or a decision that genuinely needs maintainer judgement. (Counterpart: surface a genuine design tradeoff before building, as S5's FP surface was surfaced with named options rather than guessed.)

## Refresh and pruning discipline

**Refresh (at session close).** This file is refreshed at every PR close-out (post-merge), as part of the recursion-avoidance batch that carries the validate-pr/retro rows into the next PR. The session-closing handoff PR prepends this session's new blocks (the "Next actions" narrative block, the "State snapshot", and the "Asserted expectations" block) as the new *current* set.

**Prune (at resume, in the first PR).** Because the closing session only ever *adds*, the per-session blocks would otherwise accumulate without bound and bury the load-bearing state (the file's one job is a fast, reliable single resume point). So the receiving session prunes, as part of its first PR (the `/validate` close-out, per `## How to resume` step 6):

- **Retention: keep current + 1 prior.** Keep the most-recent session's blocks and the single immediately-prior session's blocks in each per-session stack (the "Next actions" narrative blocks, the "State snapshot" blocks, the "Asserted expectations" blocks). Delete everything older. Also delete superseded one-off "Next-session queue" / dated "This session's work" sections.
- **Keep the standing sections in full** (not per-session): the header / `## How to resume` / `## Resume cursor`, the `## Open decisions awaiting maintainer`, `## Trust-recovery state`, `## Known environment behaviours`, `## Standing disciplines`, and this section. These are durable reference, not session narrative.
- **Migrate-before-delete (the safety rule).** Before deleting a block, confirm any load-bearing item it carries that is NOT recorded elsewhere (an open decision, a pending maintainer action) has been migrated to `## Open decisions awaiting maintainer` / [`TODO.md`](../TODO.md) / [`pending-decisions.md`](pending-decisions.md). The durable record of pruned session *narrative* already lives in [`CHANGELOG.md`](../CHANGELOG.md) + its detailed mirror, [`DONE.md`](DONE.md), [`validate-sweeps/history.md`](validate-sweeps/history.md), [`session-metrics.md`](session-metrics.md), and git history, so narrative is safe to drop; only un-recorded forward-looking items need migrating.

The companion close-out-checklist line and the `/resume` command step encode this so it is a standing convention, not a one-off. This convention was added 2026-06-28 by maintainer direction after the file reached 426 lines / 47 stacked session blocks; the first prune cut it to ~120.
