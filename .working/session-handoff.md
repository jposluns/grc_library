# Session Handoff

**Purpose.** This file is the single resume point for a new Claude Code session. It is refreshed at every PR close-out so that opening a fresh session and continuing requires only one instruction from the maintainer (see "How to resume"). It exists because long sessions degrade (context dilution, lossy compaction, state drift, error compounding); a fresh session that rebuilds state from this file plus the durable repo artefacts is more reliable than a long one running on accumulated memory. The mechanisms and mitigations are written up generically in [`.working/session-length-considerations.md`](session-length-considerations.md). This file is maintainer working state, exempt from corpus audit gates per the `.working/` exemption.

This is an **as-of-last-refresh snapshot**, not a live-HEAD claim. Versions and counts drift forward as work advances; always verify against live files before relying on them.

## How to resume (the one command)

In a new session, the maintainer sends only: **`/resume`** (or "read `.working/session-handoff.md` and continue").

On `/resume`, the assistant:
1. Reads this file in full, including the **"Known environment behaviours"** section below.
2. Reads `.claude/CLAUDE.md` (the PRIMORDIAL RULE and project disciplines), the most recent few `CHANGELOG.md` entries, and **[`.working/third-party-issues.md`](third-party-issues.md)**.
2a. **Loads the scratch reference index into memory** (`grc_library_scratch/ref/INDEX.md`, `ref/catalogue.yml`, `ref/SECTION-INDEX.md`, `ref/COVERAGE-MAP.md`) so the held reference set and its versions are known before any task; the index, not a guessed path, is authoritative for what is held and where. See `/resume` command's Reference-knowledge-base step and the `## Reference-version currency` SOP in `.claude/CLAUDE.md`.
3. Runs `git rev-parse --is-shallow-repository` (unshallow with `git fetch --unshallow` if `true`, **before any history-aware audit**), then `tools/run_all_audits.sh` to confirm the corpus is green, and `git log --oneline -5` to confirm HEAD.
4. Verifies the version/count snapshot below against live files.
4a. **Batches the unattended-run clarifications up front (maintainer-directed 2026-06-25)**: before resuming the queue, asks the maintainer (via `AskUserQuestion`, batched into as few rounds as possible) every decision that would let the session run unattended longer, pre-populating decisions for **all Priority 1 and Priority 2 backlog items** and **the next 10-plus planned PRs** (including any outside P1/P2, e.g. the FR-167 batches). Compute-don't-ask first (ask only genuine authorial decisions, not what the assistant can verify); record the answers in the next-actions queue so they survive a mid-session compaction; mark deferrals and route around them. See `/resume` command step 5 for the authoritative form.
5. **Runs a full corpus-wide `/validate` as the first substantive task** (the loop-break compensating control for whatever session-closing handoff PR last closed, which skips its trailing `/validate-pr` + `/retro`): full A/B/C subagent dispatch over the closing session's delta window, recorded in [`validate-sweeps/history.md`](validate-sweeps/history.md) and the Resume-cursor section above. **Cross-check the findings against the "Asserted expectations" section below**: a contradiction of an asserted-clean touched surface is a genuine miss, escalated; an ordinary finding routes to the backlog. Standing invariants to confirm: the corpus is CSF-1.1-clean (gate 54, 0 carriers; a carrier in a per-document NIST table is a genuine finding, not residue); four-surface parity at the current gate count (gate 35); the gate/rule/skill/command counts (gate 39 + prose). The specific sweep number and delta window are in the current "Next actions" block below.
6. **Prunes this file as part of that first `/validate` close-out PR** (keep current + 1 prior, per `## Refresh and pruning discipline` below), so the handoff does not accumulate stale session history across resumes.
7. Continues from "Next actions" below.

## Resume cursor (machine-readable)

This is the relocated home of the former `## Session resume metadata` block (moved out of
[`TODO.md`](../TODO.md) in PR #413 so TODO stays purely forward-looking). The sweep cursor below is
read by [`tools/lint-todo-staleness.py`](../tools/lint-todo-staleness.py) (gate 45), which flags it
if it falls behind [`validate-sweeps/history.md`](validate-sweeps/history.md). The full sweep history
lives in that history file; the live per-session state (branch, versions, counts, green-at-`<sha>`) is
in the "Next actions" blocks below and is an as-of-last-refresh snapshot (drift forward is expected).

- **Last validation sweep**: Sweep 78 iter 1 (2026-06-30; the loop-break corpus-wide `/validate` over the #476..#501 deltas, run at the `/resume` continuing from session-closing handoff #502; full three-subagent dispatch. Mechanical baseline 57/57 at `fe88442`/#502, matching the #502 green-at-`d86f90b` baseline (no close-vs-start drift). Subagent C clean (four-surface parity 57; gate 57's §6 detailed-prose pair present, the Sweep-77-A1 regression now fixed; D5 §6.1 prose matches the linter's three closure forms). **Subagent A finding A-1 REFUTED at apply-time**: A claimed the FR-58 disposition register holds 275 rows making the #497 CHANGELOG "292 ... template 282" stale; an orchestrator recount returned 292 (8 library-internal + 282 template + 2 reference), matching the CHANGELOG, so asserted-expectation #2 is correct and A-1 is the false positive (not routed). **Subagent B: 5 out-of-window findings, all fixed this PR** (B-1 `customised` in [`tools/build-portal.py`](../tools/build-portal.py):343 + rendered [`docs/portal.md`](../docs/portal.md):49, the gate-2-blind `-ise` class #499 targeted; B-2/B-3 stale [`.claude/CLAUDE.md`](../.claude/CLAUDE.md) TODO §4.26->§1.5 and §4.20-dangling refs; B-4 [`TODO.md`](../TODO.md):252 cited closed FR-58/FR-167 as live candidates; B-5 a present-tense FR-167 docstring illustration). Asserted-expectations cross-check: no contradiction. Corpus CSF-1.1-clean, four-surface parity 57. Detail: [`validate-sweeps/2026-06-30-sweep78-iter1.md`](validate-sweeps/2026-06-30-sweep78-iter1.md)).

## Next actions (the queue for the next session)

**THIS RESUMED SESSION (2026-06-30, `/resume` from session-closing handoff #502).** Resumed at HEAD `fe88442` = `origin/main` (the merged #502), 57/57 green, matching the #502 green-at-`d86f90b` baseline. Verified state, read pending-decisions (1 entry) + verifier-overrides (0 pending), loaded the scratch `ref/` index. Ran the mandatory loop-break **Sweep 78** FIRST (corpus-wide `/validate` over #476..#501): clean of genuine misses (A-1 the 292-count finding REFUTED at apply-time; 5 out-of-window B-findings fixed in this close-out PR). Maintainer answered the resume clarifications: **FR-24 = keep the SOX result-classification scheme + the CAPA crosswalk as shipped (#492), no change** (pending-decisions now 0 open); **§1.4 S3 stays parked**; **mode = attended-autonomous, self-merge green PRs, `.claude`/pack edits pre-approved this session**. **Next P1 queue** (S3 parked, so the lead falls to): **§1.5** reference version-currency register (maintainer-approved extend-the-existing-register design; also corrects the MITRE ATLAS row to v2026.05 + the egress/maintainer-download-gated scratch superseded-archival, per [`pending-decisions`](pending-decisions.md)/TODO §1.5) -> **§1.2** Per-document ISO Annex A gate-54 extension (precision-first design; ISO titles usable per the 2026-06-28 clarification) -> the **§1.3 residual** (now just the new-skill-drafting checklist + count-gate remainder; the compute-don't-ask codification SHIPPED in #504) -> **P2 gaps** FR-59 / FR-60 deepenings + the deepen-baselines cluster -> **FR-70** (XL crypto) last. **Sweep 79** over this session's deltas is the next loop-break control at the next session-closing handoff. This first PR (the Sweep-78 close-out) also pruned this handoff (keep current + 1 prior: #502 + #475; #464 removed) and resolved the FR-24 pending-decisions entry.

**PR #504 MERGED (2026-06-30, `1f544c6`):** codified the §1.3 compute-don't-ask residual as the **compute-first gate** in `clarify-before-acting` (pack 1.53.7; rule + byte-identical `.claude/` mirror + paired skill; named the action-side companion to `validate-inference-before-action`). Pre-push skeptical verifier and post-merge `/validate-pr` Subagent A both returned 0 findings. Its `/validate-pr` (0 findings) + `/retro` rows and this handoff / pending-decisions refresh are batched (committed on-branch) into the next PR, **§1.5**, per recursion-avoidance. **Immediate next: build §1.5** (reference version-currency register; the consolidated upstream-verification research is parked in the session scratchpad as `citation-currency-sweep-2026-06-30.md`).

**PR #505 MERGED (2026-06-30, `ae58d2b`):** the §1.5 reference version-currency register, built under the high-assurance harness. Added `Upstream check location` + `Last verified (UTC)` to all 16 register tables (100 verified 2026-06-30, 51 `needs-reconfirm` where iso.org/IEC/gov sources 403 automated fetch), wired the advisory cadence into `specification-citation-verification.md` §12.3 + §7, applied 7 upstream-confirmed corrections, adapted `lint-standards-currency.py`'s parser + extended `lint-external-link-domains.py`'s allow-list. Library `2026.06.483`, README `1.9.354`, register `1.5.7`, spec `1.2.3`. Dual adversarial verifiers (1 finding fixed pre-commit); post-merge `/validate-pr` returned **1 Low in-window finding** (a CHANGELOG-detailed portal-regen imprecision, the #404 class), **fixed** by reword. The #505 `/validate-pr` row + record + `/retro` row + the A-1 fix + this handoff refresh batch (committed on-branch) into the next PR per recursion-avoidance. **§1.5 CORE CLOSED.** **Next P1 queue:** **§1.2** Per-document ISO Annex A gate-54 extension (needs a precision-first design; ISO titles usable per the 2026-06-28 clarification) -> the **§1.5 version-upgrade follow-ups** (ISO/IEC 27033->27033-1:2015 with its 2 corpus citers, ISO/IEC 27036-2->2022, NIST SP 800-88->Rev.2, one tightly-scoped PR each) -> the **§1.3 residual** (new-skill-drafting checklist + count-gate remainder) -> **P2 gaps** FR-59 / FR-60 + deepen-baselines -> **FR-70** (XL crypto) last. The **MITRE ATLAS scratch superseded-archival** is the one open pending-decision (MCP/maintainer-gated; register row already correct at v2026.05).

**PR #506 MERGED (2026-06-30, `3fb22a0`):** first of the three §1.5 version-upgrade follow-ups (maintainer chose option A: take the three follow-ups next, one tightly-scoped PR each). Corrected the phantom-year `ISO/IEC 27033:2020` citation to the genuine **`ISO/IEC 27033-1:2015`** (Part 1, Overview and concepts; 2nd edition, confirmed unchanged 2021; prior edition 2009) in the canonical-citations register (1.5.7->1.5.8, with the upstream URL + `verified 2026-06-30` stamp) and the two corpus framework-alignment tables that cited it ([`operations/standard-network-security-and-segmentation.md`](../operations/standard-network-security-and-segmentation.md) 1.4.1->1.4.2, [`security/policy-network-communications-security.md`](../security/policy-network-communications-security.md) 1.1.2->1.1.3). A pre-push skeptical verifier (substantive tier) and the post-merge `/validate-pr` Subagent A both returned **0 findings**; upstream corroborated via WebSearch. Library `2026.06.484`, README `1.9.355`. The #506 `/validate-pr` (0 findings) + `/retro` rows + a prose-hygiene fix (3 em-dashes in the #505 QA-batch files, gate-51-caught pre-push) + this handoff refresh batch (committed on-branch) into the next PR per recursion-avoidance. **§1.5 follow-up #1 (27033) CLOSED.** **Next P1 queue:** the **§1.5 follow-up #2** ISO/IEC 27036-2:2014->2022 (1 corpus citer: [`supply-chain/procedure-supplier-due-diligence.md`](../supply-chain/procedure-supplier-due-diligence.md)) -> **§1.5 follow-up #3** NIST SP 800-88 Rev.1->Rev.2 (1 corpus citer: [`operations/procedure-media-handling-and-transport.md`](../operations/procedure-media-handling-and-transport.md), a substantive re-point) -> **§1.2** Per-document ISO Annex A gate-54 extension (needs precision-first design) -> the **§1.3 residual** (new-skill-drafting checklist + count-gate remainder) -> **P2 gaps** FR-59 / FR-60 + deepen-baselines -> **FR-70** (XL crypto) last. The **MITRE ATLAS scratch superseded-archival** remains the one open pending-decision (MCP/maintainer-gated).

**PR #507 MERGED (2026-06-30, `6eb1617`):** second of the three §1.5 version-upgrade follow-ups. Upgraded the superseded `ISO/IEC 27036-2:2014` to the current **`ISO/IEC 27036-2:2022`** (Cybersecurity - Supplier relationships - Part 2: Requirements; Edition 2, 2022-06, cancels and replaces the 2014 first edition; retitled from "Information security for supplier relationships", matching the sibling 27036-3 row) in the register (1.5.8->1.5.9, upstream URL 82060 + `verified 2026-06-30`) and the one version-bearing corpus citer ([`supply-chain/procedure-supplier-due-diligence.md`](../supply-chain/procedure-supplier-due-diligence.md) 1.1.3->1.1.4). Upstream-confirmed via WebSearch. Pre-push skeptical verifier + post-merge `/validate-pr` Subagent A both **0 findings**. The pre-push guard also caught a row-fusion defect from the #506 QA batch (the #506 /retro row had merged the #505 row's columns; reconstructed both). Library `2026.06.485`, README `1.9.356`. The #507 `/validate-pr` (0 findings) + `/retro` rows + this handoff refresh batch (committed on-branch) into the next PR per recursion-avoidance. **§1.5 follow-up #2 (27036-2) CLOSED.** **Next P1 queue:** the **§1.5 follow-up #3** NIST SP 800-88 Rev.1 -> Rev.2 (a substantive sanitization-guidance re-point; broader version-bearing citer set to sweep at the start: the register row plus [`supply-chain/template-supplier-offboarding-evidence.md`](../supply-chain/template-supplier-offboarding-evidence.md) which cites `Rev. 1` / `800-88r1`, beyond the lead doc [`operations/procedure-media-handling-and-transport.md`](../operations/procedure-media-handling-and-transport.md)) -> **§1.2** Per-document ISO Annex A gate-54 extension (needs precision-first design; flag for maintainer steer) -> the **§1.3 residual** (new-skill-drafting checklist + count-gate remainder) -> **P2 gaps** FR-59 / FR-60 + deepen-baselines -> **FR-70** (XL crypto) last. The **MITRE ATLAS scratch superseded-archival** remains the one open pending-decision (MCP/maintainer-gated).

**PR #508 MERGED (2026-06-30, `cddd296`):** third and last §1.5 version-upgrade follow-up, **closing the §1.5 version-upgrades**. NIST SP 800-88 Rev. 2 (final 2025-09-26; Rev. 1 withdrawn) reframed media sanitization around an enterprise programme and deferred technique detail to **IEEE 2883**; per the maintainer's decision (faithful + introduce IEEE 2883), re-pointed the corpus and introduced IEEE 2883:2022 as a newly-cited standard (new register `## IEEE standards` section + `standards.ieee.org` allow-list domain). Touched 6 corpus docs (register-canonical 1.5.10, register-document-index 1.27.50, standard-records-retention 1.4.7, procedure-endpoint-management 1.3.2, procedure-media-handling 1.3.2, template-supplier-offboarding 1.0.6) + the allow-list tool. The pre-push skeptical verifier caught 1 Medium cross-surface miss (a sibling HDD row pinning `Clear` to 800-88), fixed in-window + a 2nd via full-corpus re-sweep; post-merge `/validate-pr` Subagent A returned **0 findings** (1 pre-existing optional observation: doc-index pinned-vs-bare 800-88 asymmetry, latent, not PR-introduced). Citations verified against the maintainer-supplied authoritative IEEE 2883 PDF. Library `2026.06.486`, README `1.9.357`. The #508 `/validate-pr` (0 findings) + `/retro` rows + this handoff refresh batch (committed on-branch) into the **next corpus PR** per recursion-avoidance.

**IEEE 2883 scratch ingestion DONE (maintainer-directed 2026-06-30; scratch PR #46 merged `20065b6`).** The maintainer supplied the authoritative IEEE Std 2883-2022 PDF and directed its ingestion into scratch [`grc_library_scratch`](../../grc_library_scratch/) `ref/standards/IEEE/`, watermark-scrubbed. Completed: the per-licensee watermark (name + date) was redacted from all 67 pages of the binary (PyMuPDF, 536 redactions) and from the text extract, scratch `validate.py` passed (check 13 PDF-watermark scan clean, no PII tokens), and a new `ref/standards/IEEE/` source folder was created (clean binary in `originals/`, a watermark-free `--full-text.md` internal-reference extract, a README row, a `catalogue.yml` entry origin=IEEE/proprietary/authoritative/topics=cybersecurity, regenerated indexes). Note: the direct scratch push SUCCEEDED this session (the previously-recorded "git proxy 403s direct scratch pushes" constraint did not apply; attempting beat assuming, per `action-before-explanation-of-inaction`). NIST SP 800-88 Rev. 2 is already in scratch (`NIST-SP-800-88r2--...`, prior NIST batch). **The corpus P1 queue now resumes:** **§1.2** Per-document ISO Annex A gate-54 extension (needs a precision-first design; flag for maintainer steer before building) -> **§1.3 residual** (new-skill-drafting checklist + count-gate remainder) -> **P2 gaps** FR-59 / FR-60 + deepen-baselines -> **FR-70** (XL crypto) last. The **MITRE ATLAS scratch superseded-archival** and the **51 needs-reconfirm register rows** remain the other §1.5 residuals.

**PR #509 MERGED (2026-06-30, `866ef46`):** **gate 58**, the per-document ISO/IEC 27001:2022 Annex A validity audit (`tools/lint-document-iso-annex-a.py`), **closing TODO §1.2**. The ISO-column sibling of gate 54 and per-document counterpart of gate 49's ISO column: validates Annex A control codes, theme-only refs, same-theme ranges, and clauses in ISO-labelled per-document framework tables (matrix + pack excluded), edition-pinned and table-scoped so a bare prose `§N` is never mis-read. Extracted the shared `tools/iso_27001_reference.py` module (Annex A 37/8/14/34=93, clauses §4-§10, `check_iso_token`) and refactored gate 49 to import it so the two ISO gates cannot drift; wired all four surfaces + a 9-test regression fixture. Maintainer-approved design: separate gate (not folded into gate 54), theme-only refs valid. **Gate count is now 58** (was 57); 217 regression tests (was 208). A pre-push skeptical verifier caught 1 CRITICAL regen-order false-clean (the spec 1.16.25 bump left taxonomy/scorecard stale, gates 33/34) which was fixed (regen taxonomy then portal/scorecard) and re-verified before push; post-merge `/validate-pr` Subagent A returned **0 findings**. Library `2026.06.487`, README `1.9.358`, audit-spec `1.16.25`. The #509 `/validate-pr` (0 findings) + `/retro` rows + this handoff refresh batch (committed on-branch) into the next corpus PR per recursion-avoidance. **Next P1 queue:** the **§1.3 residual** (new-skill-drafting checklist + count-gate word-form remainder) -> **P2 gaps** FR-59 / FR-60 deepenings + the deepen-baselines cluster -> **FR-70** (XL crypto) last. The **MITRE ATLAS scratch superseded-archival** and the **51 needs-reconfirm register rows** remain the standing §1.5 residuals.

**PR #510 MERGED (2026-06-30, `8a3fffed`):** **TODO §1.3-A** (new-skill-drafting checklist), completed by folding the four parallel new-skill surfaces (repository-internal link-depth/gate3, slash-command sibling + PAIRS registry/gate44, lint-language pre-flight/gate2, README skills tree/gate41) into the existing [`skills/skill-authoring-discipline/SKILL.md`](../dev-security/claude-rules/skills/skill-authoring-discipline/SKILL.md) Process step 7 (+ Verification + a Common-Rationalizations row), its actual owner. **Maintainer-decided** placement (skill-authoring-discipline, not the worker-brief template the #213 retro had guessed, since research workers do not draft skills). Also fixed an in-window gate-39-blind word-form stale count from #509: [`skills/guardrail-review/SKILL.md`](../dev-security/claude-rules/skills/guardrail-review/SKILL.md):93 "fifty-seven" -> "fifty-eight". Pack `1.53.7` -> `1.53.8`, library `2026.06.488`, README `1.9.359`. A pre-push skeptical verifier returned 0 defects; two mechanical pre-push catches fixed before push (a gate-3 broken-link from copying a SKILL link depth into the shallower pack README row; a D2 ledger-Version-bump slip where the #509 QA-batch added ledger rows without bumping the two `.working` ledgers' Version fields). Post-merge `/validate-pr` Subagent A returned **0 findings**. The #510 `/validate-pr` (0 findings) + `/retro` rows + this handoff refresh batch (committed on-branch) into the next corpus PR per recursion-avoidance. **Next P1 queue:** **§1.3-B** (broaden the count gate to word-form counts via a word->number map + the free-prose rule-count case; a gate-39 change, not a checklist line) -> **P2 gaps** FR-59 / FR-60 deepenings + the deepen-baselines cluster (FR-15/23/63/74/99/154/41) -> **FR-70** (XL crypto) last. §1.4 S3 stays parked; §1.1 FR-48 deferred to a dedicated session. The **MITRE ATLAS scratch superseded-archival** and the **51 needs-reconfirm register rows** remain the standing §1.5 residuals.

**PR #511 MERGED (2026-07-01, `fdab89b`):** **TODO §1.3-B**, broadening gate 39 ([`tools/lint-gate-count-consistency.py`](../tools/lint-gate-count-consistency.py)) to detect WORD-FORM collection counts (a word->number map 1-99 + patterns P9-P12), **closing TODO §1.3 entirely** (§1.3-A in #510). Narrow precision-first (maintainer-chosen over full coverage / won't-fix): only the qualified and growth-narrative idioms match, never the pervasive bare small-numbers ("two rules", "one gate"); a `## Version history` section is skipped as a frozen log. Rule/skill canonical counts derived like gate 41; a latent out-of-repo output crash fixed in passing. Gate 39 name unchanged (gate 35 parity intact). Suite 217->222. Spec `1.16.26`, library `2026.06.489`, README `1.9.360`. A pre-push skeptical verifier returned 0 defects; the pre-push guard caught two mechanical issues fixed before push (gate 9: "TODO" as an uncertainty marker in the new spec prose near a mandatory verb; gate 18: two `§1.3` intra-doc refs orphaned by the section deletion, one reintroduced by a first reword). Post-merge `/validate-pr` Subagent A returned **0 findings**. The #511 `/validate-pr` (0 findings) + `/retro` rows + this handoff refresh batch (committed on-branch) into the next corpus PR per recursion-avoidance. **Next queue: P2 gaps** (the fix-issues-first P1 tooling is now clear except the parked §1.4 S3 and the deferred §1.1 FR-48): **FR-59** (privacy jurisdiction annex operational deepening, H) -> **FR-60** (HIPAA deepening) -> the **deepen-baselines cluster** (FR-15/23/63/74/99/154/41) -> **FR-70** (XL crypto-asset domain) last. P2 net-new/deepening content goes through the high-assurance harness where a change is sensitive. The **MITRE ATLAS scratch superseded-archival** and the **51 needs-reconfirm register rows** remain the standing §1.5 residuals (MCP/egress-gated).

**PR #512 MERGED (2026-07-01, `df15510`):** **TODO §1.5 residual**, correcting the MITRE ATLAS citation currency. The reference-version-currency SOP (verify externally-versioned refs against the upstream primary source THIS turn) found ATLAS had advanced to **v2026.06** (2026-06-30 release) past the register's v2026.05 (recorded 2026-06-28 in #505); egress confirmed via WebFetch (retiring the earlier "download-gated" framing). Corrected [`governance/register-canonical-citations.md`](../governance/register-canonical-citations.md) (1.5.10->1.5.11, ATLAS row v2026.05->v2026.06, Date 2026-07-01) and updated the [`worklist`](../.project-governance/worklist-citation-verification-batch-q4-canonical-citations.md) line-48 currency flag (1.0.5->1.0.6). Month-rollover CalVer **`2026.06.489`->`2026.07.0`** (patch resets to 0 at the month boundary per §4.5), README `1.9.361`, Date 2026-07-01; regenerated taxonomy/portal/scorecard. Both the pre-push skeptical verifier AND post-merge `/validate-pr` Subagent A independently re-confirmed v2026.06 via WebFetch. **Subagent A returned 1 Low in-window finding:** #512 updated the worklist's gate-6-live flag (line 48) to v2026.06 but left the sibling batch-capture row (line 128) at v2026.05, and the #512 CHANGELOG mischaracterized the worklist as "left as-is" while the diff showed it was edited; **fixed in #513** (line 128 annotated with the supersession, worklist 1.0.6->1.0.7, #513 CHANGELOG corrects the "left as-is" wording). The #512 `/validate-pr` (1 Low, fixed) + `/retro` rows and this handoff refresh batch (committed on-branch) into **#513, the session-closing handoff PR**, per recursion-avoidance. **All P1 tooling is now clear** except the parked §1.4 S3 and the deferred §1.1 FR-48; the **MITRE ATLAS scratch superseded-archival** (scratch still holds v5.6.0; archive to `ref/.superseded/`, fetch v2026.06) and the **51 needs-reconfirm register rows** remain the standing §1.5 residuals.

**STANDING DIRECTIVE (maintainer-set 2026-06-27): fix issues before new content.** Until the routed-issue backlog is cleared, correcting defects takes priority over adding new content; greenfield work (the privacy templates FR-31/32/34, FR-73 AI ethics panel, the AI crosswalk FR-145, and the matrix *gap-fill* rows) waits behind it. The five issue **types** are: (1) **correctness** (a value simply wrong against ground truth: wrong/non-existent control code, wrong legal in-force date, miscitation); (2) **cross-document contradiction** (the same fact stated two ways across or within documents); (3) **semantic-fit / weak-mapping** (a code that exists and is in the right catalogue but is the wrong/loose control, the gate-blind class); (4) **format / structural** (table-shape, column-format, metadata inconsistency); (5) **editorial / prose** (acronym drift, `shall`->`must`, stale narrative counts, wording). **Remediation priority order** (severity-tiered first, then harm-if-copied times fix-certainty):
1. **High correctness defects** (type 1, High) FIRST: high harm (an adopter copies the falsehood), cheap, verifiable against the reference base. Example: FR-168 (`GRC-12`->`GRC-04`).
2. **High cross-document contradictions** (type 2, High): decision-gated. Pick the canonical value via stricter-is-safer plus external-standard / canonical-internal-source grounding, then propagate corpus-wide in ONE pass per fact (grep the chosen value everywhere before closing; one-surface-at-a-time re-introduces the drift). Intra-document contradictions jump the queue within this tier. The High set here is acceptance authority (CRO vs Executive Management vs Board), incident P2 CISO timing (4h vs 1h vs 2h), ROPA retention (3y vs Active+5y), and the India DPDPA in-force date.
3. **Medium semantic-fit + format** (types 3-4): batch by file/domain; route loose-fit codes through `/matrix-fit`. Examples FR-198 (DSP-10 weak fit), FR-199 (register column format).
4. **Low editorial** (type 5): batch; the corpus-wide ones (ERC ~26 docs, FR-44 `shall`->`must`, the FR-58 label scheme) are NON-partitionable single-session sweeps, not incremental edits.
Cross-cutting rules: within a tier do cheap-and-certain before decision-gated; land a correction BEFORE any content that cites it; the matrix *dedup* is an issue (fix-first) while the matrix *gap-fill* rows are new content (wait). **UPDATE (this resume session, closes at #417): the entire High set FR-168..FR-175 is now CLOSED** (#411 FR-168/170/173; #412 FR-169/171/177/185; #414 FR-172/186/187; #415 FR-174/179/180; #416 FR-175). So the concrete order is now **FR-176..199 (the 24 P2 Medium/Low/FYI) by type, then the deferred new content (matrix gap-fill / FR-31/32/34 / FR-73 / FR-145 / FR-58).** The maintainer's standing next-session plan (agreed this session): **get most/all of the P3 batch in one fan-out as a test**, alongside the P2 type-batches; the P2 set (FR-176..199) is largely partitionable by file/domain (research-fan-out-eligible), while the corpus-wide editorial sweeps (ERC ~26 docs, FR-44 `shall`->`must`, the FR-58 label scheme) stay NON-partitionable single-session.

**SESSION-CLOSING UPDATE (2026-07-01, this session's wind-down; closes at PR #513, the session-closing handoff PR).** This session resumed 2026-06-30 from the #502 handoff, ran the loop-break **Sweep 78** first (0 genuine misses), then shipped the **P1 tooling backlog**: **#509** (gate 58, per-document ISO/IEC 27001:2022 Annex A validity audit, closing TODO §1.2, gate count **57->58**, suite 208->217), **#510** (TODO §1.3-A new-skill-drafting checklist folded into `skill-authoring-discipline`), **#511** (gate 39 broadened to word-form counts, closing TODO §1.3 entirely, suite 217->222), and **#512** (MITRE ATLAS citation currency v2026.05->v2026.06, TODO §1.5 residual). **#513 (this session-closing handoff PR)** batches the #512 `/validate-pr` (1 Low, fixed here) + `/retro` rows, the worklist line-128 supersession fix, this handoff refresh + the `## Asserted expectations` block + the green-at-`df15510` snapshot + the [`session-metrics.md`](session-metrics.md) row; it skips its own trailing `/validate-pr` + `/retro` per the handoff-PR exception (marker in its `validate-pr/history.md` Findings cell). **Wind-down rationale (maintainer-directed):** the maintainer directed, before #512, that after that PR the session wind down and the next `/resume` open in **OVERNIGHT mode**. No degradation signal drove this; it is a maintainer-set boundary. The P1 tooling is complete except the parked §1.4 S3 and deferred §1.1 FR-48. **Next `/resume` runs a full corpus-wide `/validate` first (Sweep 79 over the #504..#512 deltas)**, cross-checked against this session's `## Asserted expectations` block below, THEN opens in overnight mode. **Still OPEN for the next session:** **P2 gaps** FR-59 (privacy jurisdiction annex deepening, H) -> FR-60 (HIPAA deepening) -> the **deepen-baselines cluster** (FR-15/23/63/74/99/154/41) -> **FR-70** (XL crypto) last, each net-new/deepening change through the high-assurance harness where sensitive. **Standing §1.5 residuals:** the **MITRE ATLAS scratch superseded-archival** (a separate `grc_library_scratch` task: scratch holds v5.6.0, archive to `ref/.superseded/`, fetch v2026.06) and the **51 needs-reconfirm register rows**. **Pending decisions (surface at `/resume`):** the MITRE ATLAS scratch-archival entry in [`pending-decisions.md`](pending-decisions.md) (corpus register already correct at v2026.06; the scratch-side archival is the open half). Library `2026.07.0` (to **`2026.07.1`** on #513), README `1.9.361` (to **`1.9.362`**). The prior-session blocks below are retained until the next `/resume` prune.

**SESSION-CLOSING UPDATE (2026-06-30, this session's wind-down; closes at PR #502, the session-closing handoff PR).** **#501 merged** (HEAD `main` = `d86f90b`, 57/57 green; its `/validate-pr` Subagent A returned 0 findings). This session was the largest single run in the ledger: resumed from #475 into OVERNIGHT mode, ran the loop-break Sweep 77 first, then shipped **26 PRs (#476-#501)** under the fix-issues-first-then-net-new directive, **closing FR-167** (#490 `/matrix-fit` + #491 gap-fill + #494 OT/AI rows) and **FR-58** (#497, the 292-doc Adoption-Disposition column), nine net-new standalone documents each through the high-assurance dual-verifier harness, and a late tooling/bookkeeping cluster (#498 work-type re-tier, #499 lint-language `-ises` gate fix, #500 priority-prefixed renumber, #501 D5 broadening). **#502 (this session-closing handoff PR)** batches the #501 `/validate-pr` (0 findings) + `/retro` rows, resolves the stale FR-58 pending-decisions entry (FR-58 shipped #497), refreshes this handoff + the asserted-expectations + the green-at-`d86f90b` snapshot + the [`session-metrics.md`](session-metrics.md) row; it skips its own trailing `/validate-pr` + `/retro` per the handoff-PR exception (marker in its `validate-pr/history.md` Findings cell). **Wind-down rationale (maintainer-authorized):** the maintainer authorized a wind-down after #501 if the assistant judged a fresh-session resume a sound preventative measure; it is, on objective evidence: three edit-MECHANICS slips this session (the #498 pipe-masked write, the #497/#496 ledger-row fusion, the #500 substring-anchor collision) were each caught pre-merge by a deterministic post-edit check, a recurring class (logged as the #500 retro pattern) that a fresh context reduces. No content-correctness drift, no shipped defect; the slips were all in scripted-edit mechanics, all QA-caught. **Next `/resume` runs a full corpus-wide `/validate` first (Sweep 78 over the #476..#501 deltas)**, cross-checked against this session's `## Asserted expectations` block below. **Still OPEN for the next session (attended; fix-issues-first holds):** **§1.4 S3** (citation-precision gate build) -> **§1.5** (reference version-currency register, cross-repo) -> **1.2** Per-document ISO gate-54 extension (needs precision-first design) -> the **§1.3 residual** (compute-don't-ask codification, new-skill-drafting checklist, count-gate remainder, all `.claude/`/pack changes that were barred overnight) -> **P2 gaps** FR-59 / FR-60 + deepen-baselines -> **FR-70** (XL crypto) last. **Pending decisions (surface at `/resume`):** FR-24 result-classification reconcile-vs-keep (PROCEEDED with keep-and-crosswalk stricter-safe; confirm/redirect) is the one open entry in [`pending-decisions.md`](pending-decisions.md); FR-58 is now resolved there. Also still queued: the #486/#487 `/retro` codification lessons and the TODO-hygiene "15-second-readable / split mega-items" directive (all `.claude/`/pack changes). Library `2026.06.479` (to **`.480`** on #502), README `1.9.350` (to **`.351`**). The prior-session blocks below are retained until the next `/resume` prune.

**SESSION-CLOSING UPDATE (2026-06-29, this session's wind-down; closes at PR #475, the session-closing handoff PR).** Maintainer directed wind-down "after the next few PRs, no rush" mid-session; the session continued to a clean stopping point and closes here. Full arc beyond the #466-#471 narrative below: **#472** (fixed the 2 cross-FILE stale §4.6/§4.10 "queued" carriers #471's `/validate-pr` surfaced, in [`.claude/CLAUDE.md`](../.claude/CLAUDE.md) + the [`tools/lint-bookkeeping-parity.py`](../tools/lint-bookkeeping-parity.py) docstring); **#473** (codified the §4.8 batch's focused slice: 2 close-out-checklist bullets, the cross-FILE section-close cleanup guard + the gate-39 count-phrasing guard); **#474** (hot-fix for #473's own `/validate-pr` finding: the gate-39 bullet had mis-stated gate 39's scope, claiming `CHANGELOG.md` is scanned when it is in `EXEMPT_FILES`; corrected to name the actually-scanned surfaces, verified accurate against source by #474's `/validate-pr`). **#475 (this session-closing handoff PR)** batches the #474 `/validate-pr` (0 confirmed, 1 cosmetic FYI fixed in #475) + `/retro` rows, the cosmetic P7-string fix, and this refresh; it skips its own trailing `/validate-pr` + `/retro` per the handoff-PR exception (marker in its `validate-pr/history.md` Findings cell). Gate count **57** throughout (all this session's work was scope/bookkeeping/codification, no new gate). Library `.451` on #471, `.452` on #474 (`.449`->`.450` #472, `.450`->`.451` #473, `.451`->`.452` #474); README `1.9.323`; spec `1.16.21`; pack live `1.53.5` (unchanged). **The §4.6/§4.10/gate-39 cluster is CLOSED**: §4.6 closed-as-satisfied (#471), the cross-FILE refs fixed (#472), the close-out lessons codified (#473), and the gate-39-scope mis-description corrected and verified (#474); the `/retro` cluster lesson ("verify a mechanism against its source artefact, not a derivation") is logged in the #473/#474 rows. **Next `/resume` runs a full corpus-wide `/validate` first (Sweep 77 over the #466..#475 deltas)**, cross-checked against this session's `## Asserted expectations` block below. **Still OPEN for the next session (attended-autonomous; fix-issues-first holds):** the remaining **§4.8 apply-time-discipline codifications** (the gate-prose-collision close-out line now 4x firm; "adding a PR-only delta gate Dn needs its step name in `WORKFLOW_DELTA_GATE_STEPS`"; the #462 table-edit-safety + #463 `replace_all`-scope notes) -> Sweep-3 follow-up + the ERC guideline-doc authorial reconcile -> FR-58 3-label scheme -> P1/P2 net-new content each via the high-assurance harness -> closing FR-167 whole-matrix `/matrix-fit`. §4.5 **S3** stays PARKED (maintainer-deferred). The prior-session blocks below are retained until the next `/resume` prune.

**THIS SESSION (2026-06-29, `/resume` continuing; attended-autonomous, green CI = merge authority; CLOSED at #475 per the update above).** Resumed from the #464 session-closing handoff. Ran the loop-break **Sweep 76** first (corpus-wide `/validate` over the #458..#464 deltas, the #464 compensating control), then worked the guard-rail-gates queue: **#466** (gate 56, the §4.5 S4 bare-normative-`shall` audit), **#467** (rotated TODO §4.5 S4 to DONE, the #466 in-window rotation finding, plus batched #466 QA), **#468** (gate 57, the §4.10 marked-done static detector, the content-side half of the rotation gate family), **#469** (the **D5 backlog-rotation-on-PR check**, the PR-time complementary half, **closing the §4.10 gate family**; §4.10 rotated out of TODO into DONE), **#470** (brought the authored `docs/` adopter guides under the gate-2 language/style audit and fixed all **117** accumulated house-style findings: 71 dashes, 25 `-ise`, 4 heading-case, 1 vendor name; the worked-example meta-tutorial exempted from heading-case/sanitisation/ensure; maintainer-decided "full house-style now", refining the earlier dash-only Option A; closed the P3 `docs/` enforcement-gap item), and **#471** (closed TODO §4.6 QA-cadence mechanical enforcement as substantially-satisfied by the existing gate 50 Check 1, which already enforces the missing-row half corpus-wide; the residual abbreviated-marker half is not FP-free-mechanizable on free prose, so it stays the CLAUDE.md no-QA-abbreviation convention; maintainer-decided "close 4.6 as satisfied"; rotated §4.6 to DONE, reworded its 4 TODO cross-refs, decremented P4 `8`->`7`). Gate count still **57** (the docs/ and §4.6-close work are scope/bookkeeping, not new gates). The #469 `/validate-pr` (0 findings) + `/retro` rows and the handoff refresh batched into #470; #470's own `/validate-pr` + `/retro` batch into the next PR. **Next queue (attended-autonomous; fix-issues-first holds):** §4.5 **S3** is PARKED (maintainer-deferred 2026-06-29 pending a clearer design, since gates 48/49/54 already cover control-code existence) and **§4.6 closed as satisfied by gate 50 Check 1** (#471), so next is the **§4.8 apply-time-discipline codifications** (now including the #469 `/retro` candidates: the gate-prose-collision close-out line, now 4x firm; "adding a PR-only delta gate Dn needs its step name in `WORKFLOW_DELTA_GATE_STEPS`"; "closing a numbered TODO §-section needs an intra-doc `§N` cross-ref cleanup pass"; plus the #462 table-edit-safety + #463 replace_all-scope notes) -> Sweep-3 follow-up + the ERC guideline-doc authorial reconcile -> FR-58 3-label scheme -> P1/P2 net-new content each via the high-assurance harness -> closing FR-167 whole-matrix `/matrix-fit`. Library `2026.06.447`->`.448` on #470, `.448`->`.449` on #471; spec `1.16.21`; pack live `1.53.5` (unchanged; not a pack change). **Sweep 77** over this session's deltas (#466 onward) is the loop-break control at the next session-closing handoff. The blocks below are the prior session's, retained until the next `/resume` prune.

_Session history older than the current and immediately-prior session is pruned at each resume (keep current + 1 prior, per `## Refresh and pruning discipline` below). The durable record of pruned sessions lives in [`CHANGELOG.md`](../CHANGELOG.md) and its detailed mirror, [`DONE.md`](DONE.md), [`validate-sweeps/history.md`](validate-sweeps/history.md), [`session-metrics.md`](session-metrics.md), and git history._

## State snapshot (as of 2026-07-01, session-closing handoff PR #513; the resumed attended-autonomous session that ran the P1 tooling backlog #504 through #512)

- **Current truth (verify against live files at `/resume`)**: work merged through **#512**; HEAD `main` = #512 (`df15510`); **PR #513 (this session-closing handoff PR)** in flight on branch `claude/resume-0cvzeu`. **Mechanical baseline green-at-`df15510` = 58/58** (the #512 merged-main state), re-confirmed at the #513 branch tip via the pre-push guard (#513 adds only `.working/` records + the gate-exempt worklist line-128 fix + README version surfaces, none gate-affecting beyond what the guard checks). Library `2026.07.0` (to **`2026.07.1`** on #513), README `1.9.361` (to **`1.9.362`**), pack live `1.53.8` (bumped #510), audit-spec `1.16.26` (bumped #511), canonical-citations register `1.5.11` (bumped #512). **Gate count 58** (gate 58 added #509), **12 governance rules**, **17 skills**, **10 commands**, regression suite **222** (was 208; +9 #509, +5 #511), corpus CSF-1.1-clean. **Standing register [`.working/verifier-overrides.md`](verifier-overrides.md)** carries NO `pending` override from this session (each substantive-tier verifier finding was validated-and-fixed, none overruled; the #509 CRITICAL regen-order catch was fixed, not overruled). **[`pending-decisions.md`](pending-decisions.md): 1 pending** (the MITRE ATLAS scratch superseded-archival; the corpus register is already correct at v2026.06, the scratch-side archival is the open half). **Operating mode: attended-autonomous this session; next `/resume` opens in OVERNIGHT mode** (maintainer-directed wind-down after #512). **This session owns both repos and may write to scratch** (direct scratch push succeeded this session for IEEE ingestion; the git-proxy-403 constraint did not apply). **TODO §1.2, §1.3 (both sub-items), and the §1.5 ATLAS-currency residual all CLOSED** this session. No net-new standalone corpus documents shipped this session (all P1 tooling / bookkeeping), so the high-assurance register has no new active rows. **Immediate next-`/resume` queue**: corpus-wide `/validate` (Sweep 79, #504..#512 deltas) FIRST, then open overnight and work **P2 gaps** FR-59 / FR-60 + deepen-baselines -> FR-70 (XL) last; see the SESSION-CLOSING UPDATE block above. **Sweep 79** is the loop-break compensating control for this handoff.

## State snapshot (as of 2026-06-30, session-closing handoff PR #502; the OVERNIGHT-into-attended session that ran #476 through #501)

- **Current truth (verify against live files at `/resume`)**: work merged through **#501**; HEAD `main` = #501 (`d86f90b`); **PR #502 (this session-closing handoff PR)** in flight on branch `claude/session-handoff-2026-06-30`. **Mechanical baseline green-at-`d86f90b` = 57/57** (the #501 merged-main state), re-confirmed at the #502 branch tip via the pre-push guard (#502 adds only `.working/` records + README version surfaces, none gate-affecting beyond what the guard checks). Library `2026.06.479` (to **`.480`** on #502), README `1.9.350` (to **`.351`**), pack live `1.53.6` (bumped #499 for the `-ises` pack-file fix), audit-spec `1.16.24` (bumped #501), register-document-index-and-classification `bumped #497`, compliance matrix `1.11.4` (last bumped #494). **Gate count 57**, **12 governance rules**, **17 skills**, **10 commands**, corpus doc count 311 (+ the net-new docs #480-#487 raise the active total; verify the live count at `/resume`), corpus CSF-1.1-clean. **Standing register [`.working/verifier-overrides.md`](verifier-overrides.md)** carries NO `pending` override from this session (the high-assurance harness verifier findings were validated-and-fixed or were FN-completeness additions, none overruled). **[`pending-decisions.md`](pending-decisions.md): 1 pending** (FR-24 result-classification reconcile-vs-keep, PROCEEDED stricter-safe; FR-58 resolved this PR). **Operating mode: attended-autonomous** ([`overnight-pr.md`](overnight-pr.md) `stub`; overnight ended when the maintainer became reachable mid-session). **This session owns both repos and may write to scratch** (MCP-PR transport; the git proxy 403s direct scratch pushes). **FR-167 and FR-58 both CLOSED** this session. The net-new standalone documents (#480-#488) and the matrix-row batches (#491/#494) went through the high-assurance harness; the high-assurance register has their rows. **Immediate next-`/resume` queue**: corpus-wide `/validate` (Sweep 78, #476..#501 deltas) FIRST, then §1.4 S3 -> §1.5 -> 1.2 ISO gate-54 -> §1.3 residual -> P2 gaps FR-59/FR-60 + deepen-baselines -> FR-70 (XL); see the SESSION-CLOSING UPDATE block above. **Sweep 78** is the loop-break compensating control for this handoff.

## State snapshot (as of 2026-06-29, session-closing handoff PR #475; the attended-autonomous session that ran #466 through #474)

- **Current truth (verify against live files at `/resume`)**: work merged through **#474**; HEAD `main` = #474 (`cfa6e0c`); **PR #475 (this session-closing handoff PR)** in flight on branch `claude/resume-tw5ukk`. **Mechanical baseline green-at-`cfa6e0c` = 57/57** (the #474 merged-main state), re-confirmed at the #475 branch tip via the pre-push guard (#475 adds only `.working/` records + README version surfaces, none gate-affecting beyond what the guard checks). Library `2026.06.452` (to **`.453`** on #475), README `1.9.323` (to **`.324`**), pack live `1.53.5`, audit-spec `1.16.21`, register-role-authority `1.5.2`, canonical-citations register `1.5.5`, compliance matrix `1.11.3`. **Gate count 57**, **12 governance rules**, **17 skills**, **10 commands**, corpus doc count 311, corpus CSF-1.1-clean 0/311. **Standing register [`.working/verifier-overrides.md`](verifier-overrides.md)** carries NO `pending` override from this session (no verifier was overruled; the substantive PRs were bookkeeping/codification tier with no standing verifier, and the two `/validate-pr` findings were validated-and-fixed, not overruled). **[`pending-decisions.md`](pending-decisions.md): 0 pending** (the §4.5 S3 park and the §4.6 close-as-satisfied are both RESOLVED entries). **Operating mode: attended-autonomous** (green CI = merge authority; [`overnight-pr.md`](overnight-pr.md) `stub`). **This session owns both repos and may write to scratch** (MCP-PR transport; the git proxy 403s direct scratch pushes; no scratch write was needed this session). This session shipped #466-#471 (guard-rail-gate / §4.6-close work, per the block above) plus #472 (cross-FILE ref fix), #473 (close-out codification slice), #474 (gate-39-scope correction). **No net-new standalone corpus content shipped** (the harness-gated FR-70/73/etc. remain queued), so the high-assurance register has no new active rows. **Immediate next-`/resume` queue**: corpus-wide `/validate` (Sweep 77, #466..#475 deltas) FIRST, then the remaining §4.8 codifications -> Sweep-3 + ERC-guideline authorial reconcile -> FR-58 -> P1/P2 net-new under the harness -> closing FR-167 `/matrix-fit`; see the SESSION-CLOSING UPDATE block above. **Sweep 77** is the loop-break compensating control for this handoff.

## Asserted expectations (session close-out convention)

**Purpose.** At session close, the closing session records here its *asserted-clean* claims, scoped to the surfaces it actually touched or verified this session (not a blanket corpus-wide clean bill). The next session's `/resume` runs its corpus-wide `/validate` (the loop-break compensating control) and cross-checks the findings against these claims: a finding that *contradicts* an asserted-clean touched surface is a genuine miss signal (the closing session's self-assessment was wrong), to be escalated rather than triaged as an ordinary out-of-window finding. A finding on a surface NOT in this list is an ordinary finding, handled by the normal in-window / out-of-window triage.

**Why this shape (and not a two-run diff).** A rejected alternative was: the closing session also runs a full `/validate`, passes the result forward, and the receiving session diffs the two. That is wasteful and noisy: the corpus is byte-identical across the boundary (same `main` commit), so there is no corpus delta for two runs to legitimately differ on, and `/validate`'s expensive layer is non-deterministic subagent fan-out, so two runs differ by LLM sampling variance, not corpus facts (a "discrepancy = major issue" rule would fire on noise). It also collides with the loop-break (a closing-session finding can only ride the prompt forward, the lossy state the handoff exists to avoid). The asserted-expectations shape gets the real signal (a contradiction of a claimed-clean touched surface) at the cost of a few lines, no second sweep. The only deterministic close-vs-start diff worth keeping is the mechanical one, captured by the green-at-`<sha>` line in the snapshot above.

**Scope discipline.** Assert clean ONLY what this session mechanically verified (a green gate) or covered with a formal `/validate-pr` or `/validate` subagent pass. Do not assert clean a domain the session did not touch; an unfounded clean claim turns an ordinary pre-existing finding into a spurious "major miss" at the next resume. This functions as a lightweight proxy for the `/validate-pr` the session-closing handoff PR skips (the loop-break): it records what the session's own QA actually covered, for the next `/validate` to test.

**This session's asserted expectations (2026-07-01 resumed attended-autonomous session: the P1 tooling backlog #504-#512, closing TODO §1.2 / §1.3 / the §1.5 ATLAS-currency residual; resumed from #502, closes at #513):**
- **Both runners green** `tools/run_all_audits.sh` **58/58 at `df15510`/#512**, re-confirmed via the pre-push guard on the #513 branch tip (a `.working/` + gate-exempt worklist + version-surface change, none gate-affecting beyond the wiring the guard checks). `.working/` prose em/en-dash-free under gate 51. **green-at-`df15510` 58/58** (#513 is a descendant adding only bookkeeping + the worklist line-128 fix + version surfaces). Four-surface parity at gate count **58** (gate 35 + gate 39 green).
- **Gate 58 (per-document ISO/IEC 27001:2022 Annex A validity) is wired and green** (#509): all four surfaces carry gate 58 (gate 35 parity green at 58), the 9-case regression fixture passes, and the shared [`tools/iso_27001_reference.py`](../tools/iso_27001_reference.py) module is imported by BOTH gate 49 and gate 58 so the two ISO gates cannot drift. A Sweep-79 finding of a gate-35 parity gap on gate 58, a gate-58 false-fire on a legitimate ISO-labelled table, or an `iso_27001_reference` drift between the two gates would be a genuine miss. **(Carve-out: the SEMANTIC fit of a cited Annex A code to its document beyond code-existence is gate-blind by design; a fit finding is ORDINARY.)**
- **Gate 39's word-form broadening is FP-free** (#511): [`tools/lint-gate-count-consistency.py`](../tools/lint-gate-count-consistency.py) detects word-form collection counts (a word->number map 1-99 + patterns P9-P12) with zero false positives across the 469-file corpus; the 5 new plus prior regression cases pass; a `## Version history` section is skipped as a frozen log; the gate 39 NAME is unchanged (gate 35 parity intact). A Sweep-79 finding of a gate-39 false-positive on legitimate prose, or a gate-35 parity break, would be a genuine miss. **(Carve-out: the pervasive bare small-number idioms ("two rules", "one gate") are DELIBERATELY not matched (maintainer-chosen narrow precision-first); a finding asking for them is by-design, ORDINARY.)**
- **The skill-authoring-discipline new-skill surfaces are complete** (#510): [`skills/skill-authoring-discipline/SKILL.md`](../dev-security/claude-rules/skills/skill-authoring-discipline/SKILL.md) Process step 7 enumerates the four parallel new-skill surfaces (repository-internal link-depth/gate3, slash-command sibling + PAIRS/gate44, lint-language pre-flight/gate2, README skills tree/gate41); pack `1.53.8` with its paired version-history row. A Sweep-79 finding of a missing new-skill surface in the checklist, or a pack-README version-vs-history-row mismatch, would be a genuine miss.
- **The MITRE ATLAS citation currency is correct and internally consistent** (#512 + the #513 fix): [`governance/register-canonical-citations.md`](../governance/register-canonical-citations.md)'s ATLAS row is **v2026.06** (upstream-confirmed 2026-07-01), gate 6 (standards-currency) is green, and the [`worklist`](../.project-governance/worklist-citation-verification-batch-q4-canonical-citations.md)'s line-48 currency flag AND line-128 batch-capture row both reflect the v2026.06 supersession (line 128 annotated `superseded 2026-06-30 by v2026.06`). A Sweep-79 finding of a stale `v2026.05` presented as a CURRENT value in the register, or a gate-6 fail on the ATLAS row, would be a genuine miss. **(Carve-out: line 128's historically-accurate 2026-06-28 batch-capture of v2026.05, now annotated-as-superseded, is correct-as-history; a finding asking to delete the historical value is ORDINARY. The scratch-side v5.6.0->v2026.06 archival is UNSTARTED, a known soft spot below.)**
- **Known soft spots (NOT asserted clean)**: the **MITRE ATLAS scratch superseded-archival** (scratch [`grc_library_scratch`](../../grc_library_scratch/) still holds v5.6.0; the corpus register is correct at v2026.06 but the scratch `ref/.superseded/` archival + v2026.06 fetch is UNSTARTED) and the **51 needs-reconfirm register rows** are the standing §1.5 residuals; **P2 gaps** FR-59 / FR-60 deepenings, the **deepen-baselines** cluster (FR-15/23/63/74/99/154/41), and **FR-70** (XL crypto) are UNSTARTED; **§1.4 S3** stays PARKED; **§1.1 FR-48** is DEFERRED to a dedicated session. **The routine next is Sweep 79** at the next `/resume` over the #504..#512 deltas, the loop-break compensating control for this handoff, after which the session opens in OVERNIGHT mode.

**Prior session's asserted expectations (2026-06-30 OVERNIGHT-into-attended session: #476-#501, the 26-PR fix-issues-first-then-net-new run that CLOSED FR-167 and FR-58; resumed from #475, closes at #502):**
- **Both runners green** `tools/run_all_audits.sh` **57/57 at `d86f90b`/#501**, re-confirmed via the pre-push guard on the #502 branch tip (a `.working/` + version-surface change, none gate-affecting beyond the wiring the guard checks). `.working/` prose em/en-dash-free under gate 51. **green-at-`d86f90b` 57/57** (#502 is a descendant adding only bookkeeping + version surfaces). Four-surface parity at gate count **57** (gate 35 + gate 39 green).
- **FR-167 is CLOSED and the matrix existence gates are green** (#490 `/matrix-fit` + #491 gap-fill + #494 OT/AI rows): gates 48/49/54 pass on the compliance matrix; every new row's Path link resolves; the matrix is at `1.11.4`. A Sweep-78 finding of a matrix existence-gate failure, a ragged/short matrix row, or a broken new-row link would be a genuine miss. **(Carve-out: the SEMANTIC FIT of the new rows' control codes beyond what the dual-verifier harness checked is NOT gate-asserted; a `/matrix-fit`-class fit finding is ORDINARY, not a miss.)**
- **FR-58 is CLOSED** (#497): the `Adoption Disposition` column in [`governance/register-document-index-and-classification.md`](../governance/register-document-index-and-classification.md) classifies all 292 active documents, the table is uniform (no ragged rows), the vocabulary is defined in the Key Terms register, and the register's Version+Date co-bumped with taxonomy/scorecard in sync. A Sweep-78 finding of a missing/ragged disposition row, an undefined label, or a register-vs-generated-artefact version mismatch would be a genuine miss. **(Carve-out: the Type-rule CLASSIFICATION's semantic correctness for an individual document is verifier-asserted, not gate-asserted; a reclassification finding is ORDINARY.)**
- **The gate-2 `-ises` widening and its corpus cleanup are complete** (#499): the corpus and the 7 `dev-security/claude-rules/` pack files are `-ises`-clean (gate 2 green corpus-wide; the `-ises` regression fixture passes in the 208-plus-case suite). A Sweep-78 finding of a surviving Commonwealth `-ises` in authored corpus or pack prose would be a genuine miss (closing this gap was #499's whole purpose).
- **The D5 rotation-check broadening is accurate and FP-free** (#501): [`tools/check-todo-rotation-on-pr.py`](../tools/check-todo-rotation-on-pr.py) detects the three closure forms with zero false positives against the whole CHANGELOG history; the positive + negative regression fixtures pass; the §6.1 D5 prose in [`governance/specification-audit-programme.md`](../governance/specification-audit-programme.md) matches the linter's three forms. A Sweep-78 finding of a D5 false-positive on clean history, or a §6.1-vs-linter mismatch, would be a genuine miss. **(Carve-out: bare lowercase `Closes FR-N` is DELIBERATELY not detected (maintainer decision, narration FP risk); a finding asking for it is by-design, ORDINARY.)**
- **The TODO renumber is structurally clean** (#500): every backlog item is a `### N.M` heading whose leading digit is its priority, gate 18 (intra-doc-refs) + gate 45 (TODO staleness) are green, all internal `§`-cross-references resolve, and a `(was X.Y)` tag preserves each item's prior number for one cycle. A Sweep-78 finding of a broken intra-TODO `§`-ref or a dropped/duplicated item body would be a genuine miss (the body-preservation diff verified one-to-one block preservation).
- **The nine net-new standalone documents are present and surface-complete** (#480-#488, each through the high-assurance dual-verifier harness + a post-merge `/validate-pr`, all 0 confirmed except the #486 cross-FILE KPI fixed in #487): each is registered in its listing + generated surfaces (taxonomy / domain README / register / portal / scorecard, `--check` clean), each co-bumped Version+Date, and all in-doc links resolve. A Sweep-78 finding of a missing-surface, a broken link, or a generated-artefact lag for these would be a genuine miss. **(Carve-out: their cross-document citation / control-mapping SEMANTIC fit beyond what the harness + per-PR `/validate-pr` verified is the next sweep's job, ORDINARY.)**
- **Known soft spots (NOT asserted clean)**: **§1.4 S3** (citation-precision gate), **§1.5** (reference version-currency register), **1.2** (ISO gate-54 extension), and the **§1.3 residual** (compute-don't-ask, new-skill-drafting checklist, count-gate remainder) are all UNSTARTED; **FR-59 / FR-60** deepenings, the **deepen-baselines** cluster, and **FR-70** (XL crypto) UNSTARTED. The **FR-24 result-classification scheme** is PROCEEDED-stricter-safe (keep-the-SOX-scheme-with-a-CAPA-crosswalk), NOT resolved, in [`pending-decisions.md`](pending-decisions.md); a Sweep-78 finding on the SOX-vs-canonical classification scheme is the pending authorial decision, ORDINARY not a miss. The **#486/#487 `/retro` codification lessons** + the **TODO-hygiene "15-second-readable / split mega-items" directive** are deferred `.claude/`/pack changes. **The routine next is Sweep 78** at the next `/resume` over the #476..#501 deltas, the loop-break compensating control for this handoff.

**Two-sessions-prior asserted expectations (retained until the next `/resume` prune; 2026-06-29 attended-autonomous session: #466-#471 guard-rail-gate / §4.6-close work, #472 cross-FILE ref fix, #473 close-out codification slice, #474 gate-39-scope correction; resumed from #464, closes at #475):**
- **The §4.6 close is mechanically clean** (#471): gate 50 ([`tools/lint-bookkeeping-parity.py`](../tools/lint-bookkeeping-parity.py)) Check 1 enforces the QA-cadence missing-row parity corpus-wide; [`TODO.md`](../TODO.md) carries no surviving `§4.6` heading or section-ref (the intra-doc-ref gate is green) and its P4 count reads `7`. A Sweep-77 finding of a surviving §4.6 section / live ref in TODO, or a gate-50 mis-classification of an in-window PR, would be a genuine miss. **(Carve-out: the abbreviated-marker residual is the CLAUDE.md no-QA-abbreviation convention, NOT gate-asserted; a finding about sham-QA-row detection is ORDINARY.)**
- **The cross-FILE §4.6/§4.10 "queued" carriers are removed** (#472, `/validate-pr` confirmed): [`.claude/CLAUDE.md`](../.claude/CLAUDE.md) and the [`tools/lint-bookkeeping-parity.py`](../tools/lint-bookkeeping-parity.py) docstring no longer describe §4.6 or §4.10 as queued / unbuilt future gates (both closed: §4.6 = gate 50 Check 1; §4.10 = gate 57 + the D5 PR-time check). Only `.working/` frozen archives retain historical "queued" wording. A Sweep-77 finding of a LIVE (non-frozen-`.working`) "queued §4.6 / §4.10" reference would be a genuine miss.
- **The two new close-out-checklist bullets are accurate against source** (#473 added, #474 corrected): the "Gate-39 count-phrasing" bullet's scope claims were independently re-verified by #474's `/validate-pr` against the linter source (`CHANGELOG.md` in gate 39's `EXEMPT_FILES`; `.claude` / `.working` in `DEFAULT_EXEMPT_DIRS` at [`tools/lint_common.py`](../tools/lint_common.py):54-56; `SCAN_SUFFIXES` = `.md`/`.py`/`.sh`); the "Section-close cross-FILE cleanup" bullet matches the intra-doc-ref gate's same-file-only behaviour. Both bullets live in [`.claude/CLAUDE.md`](../.claude/CLAUDE.md) (gate-exempt), so they are `/validate-pr`-asserted, not gate-asserted. A Sweep-77 prose finding of a residual mis-description in either bullet would be a genuine miss (correcting these was the cluster's whole purpose).
- **Both runners green** `tools/run_all_audits.sh` **57/57 at `cfa6e0c`/#474**, re-confirmed via the pre-push guard on the #475 branch tip (a `.working/` + version-surface change, none gate-affecting beyond the wiring the guard checks). `.working/` prose em/en-dash-free under gate 51. **green-at-`cfa6e0c` 57/57** (#475 is a descendant adding only bookkeeping + version surfaces). Four-surface parity at gate count **57** (gate 35 + gate 39 green).
- **Known soft spots (NOT asserted clean)**: the remaining **§4.8 apply-time codifications** are UNSTARTED (the gate-prose-collision close-out line, the `WORKFLOW_DELTA_GATE_STEPS` note, the #462 table-edit-safety + #463 `replace_all`-scope notes); **Sweep-3 follow-up**, the **ERC guideline-doc reconcile** (authorial, deferred), **FR-58**, and all **net-new content** (FR-70/73/31/32/34/71/72/59/61/62) UNSTARTED; **FR-167** OPEN. The [`tools/lint_common.py`](../tools/lint_common.py):21 docstring describes `DEFAULT_EXEMPT_DIRS` as only `.git`/`node_modules`/`__pycache__` while the actual frozenset (line 55) also has `.claude`/`.working`: a pre-existing incomplete-docstring nit, NOT touched this session, so a Sweep-77 finding there is ORDINARY, not a miss. §4.5 **S3** stays PARKED. **The routine next is Sweep 77** at the next `/resume` over the #466..#475 deltas, the loop-break compensating control for this handoff.

## Open decisions awaiting maintainer

- **§6.3-vs-§7.3 audit-scope spec gap**: **CLOSED in #338.** Sweep 45 found that #336's "added to all 9 explicit-list content linters" enumeration was incomplete: gate 31 (date-staleness) and gate 10 (review-cadence), two more explicit-allow-list content gates, silently skipped the dir (gate 31 a clear §6.3 violation since §6.3 names "date currency"). #338 added `.project-governance` to both (with a scope-coverage regression test each) and amended the spec with **§7.4**, which enumerates the explicit-allow-list content-linter completeness obligation (the maintainer chose, 2026-06-25, to include gate 10 and to amend the spec). The maintainer-decided spec amendment closes the decision; the **directory-scan-scope parity meta-check** (TODO P2) is the queued mechanical backstop that would have caught this class.
- **R2 relocation**: CLOSED by principle in #314 (the project-governance separation spec classifies the 6-file citation-verification cluster as project governance, so it migrates in Phase 1). The Phase-1 migration, the queued directional-dependency gate (§7.3), and the 2 deferred §5.3 classifications are now forward-looking TODO items, not open decisions awaiting the maintainer.
- **FR-167 mapping gate (asked 2026-06-24)**: RESOLVED, built as gate 49 (#325 ISO + NIST well-formedness; #326 NIST category membership + 17-cell remap closing §4.6a). The control-code-validity gate catches hallucinated/mistyped codes; semantic mapping correctness remains the orchestrator's apply-time job. No longer awaiting.
- **DD-12 scope**: RESOLVED + APPLIED (#329, maintainer-approved "broaden + extend gate 49"). DD-12 in TODO is broadened from `PR.IP`-only to all CSF-1.1 carriers (`PR.IP`/`ID.SC`/`ID.BE`/`RS.RP`/`DE.DP`/`PR.AC`/`PR.PT`/...; `RS.RP`→mainly `RS.MA`), and a **gate-49-extension** TODO item is queued (validate framework codes in per-document reference tables, not only the central matrix; build the scanner first, then gate the migration). Both are now forward-looking TODO items, not open decisions. The migration itself (corpus-wide, non-partitionable) is queued.
- **Publications-assessment process (§4.12, surfaced + queued 2026-06-25)**: a maintainer-directed process to assess `ref/publications/` for useful info AND detect poisoning/false info before it informs corpus work, queued as TODO §4.12 (not yet built). Forward-looking, not an open decision.
- **Maintainer actions still pending**: (a) **seed the scratch reference binaries** durably (git-proxy blocks scratch writes), belt-and-suspenders ONLY: the in-repo reference modules gates 48/49 use are the authoritative validators, so FR-167 batch 5+ code validity is gate-protected without them; re-attach only if a worker wants the source text; (b) **provision the least-privilege external-worker account** (read `grc_library` / write `grc_library_scratch` only), this is the gating action for both the separate-session worker primitive (§4.11) and the queued **§4.16** multi-session research-brief / `/subagent` capability. Neither blocks the next session's queue (in-session `Agent` fan-out works for FR-167 today).
- The larger-track decisions (FR-167 batches 4..N, the High[critical] net-new docs, the L/XL items) remain queued and scheduled deliberately.

## Trust-recovery state

The trust-recovery suite (`/full-qa` + `/fitness` r2) ran and the maintainer **signed off** (2026-06-22). Codification COMPLETE. Not re-triggered since; quality held throughout the 2026-06-24 overnight run, morning continuation, and the resume session (Sweep 38 + close-out #312, both warnings fixed first try, post-commit + pre-push green).

## Known environment behaviours (read before assuming working-tree state)

- **The stop-hook auto-commits AND pushes uncommitted changes on turn-end.** The working tree is auto-persisted to the feature branch; verify `git log` rather than assuming edits are held locally.
- **The squash-merge commit on `main` shows locally as `E noreply@github.com`** (GitHub's own merge commit; Verified in the GitHub UI, not locally verifiable). After syncing the branch to `main` the tip equals `origin/main`; **do NOT `git commit --amend`** it (that rewrites merged `main` history). Set `git config user.email noreply@anthropic.com && git config user.name Claude` so your own feature-branch commits are attributed correctly.
- **A squash-merge makes the local feature branch diverge from `main`** (the squash commit is new; the local pre-squash commit is its content but a different SHA), so `git merge --ff-only origin/main` fails after a merge; use `git reset --hard origin/main` to re-sync the feature branch (the local commit's content is in the squash, preserved in reflog).
- **The GitHub MCP server can disconnect mid-session** (it did 2026-06-24 after #304, blocking #305's open/merge). `git push` over HTTPS keeps working; only the MCP-routed GitHub-API operations are affected. If `mcp__github__*` tools vanish: keep committing and pushing via `git`, record the pending-merge state in the handoff, and resume the PR lifecycle when MCP returns. Do NOT merge by any non-MCP path. Full writeup: [`third-party-issues.md`](third-party-issues.md).
- **The commit-signing server can 503**, distinguish from a real defect (gates 31/40 pass on the real corpus while gate 36's failures are `git commit` subprocess errors); re-run after a pause. Full writeup: [`third-party-issues.md`](third-party-issues.md).
- **The clone may start shallow**; `git fetch --unshallow` before any history-aware audit (gates 31/40).
- **The `Bash` tool's working directory can reset to a non-repo directory between calls** (observed twice this session after long async-subagent gaps: a bare `git ...` failed with "not a git repository"). Prefix git-command sequences with `cd /home/user/grc_library &&` rather than assuming the cwd persisted.
- **After a post-merge sync you are ON `main`; `git checkout -B <feature-branch>` BEFORE building the next PR.** The post-merge sync (`git checkout main && git pull origin main`) leaves you on `main`. If you then build and `git commit` the next PR without switching branches, you commit to local `main` (which the stop-hook flags as an unverified commit ahead of `origin/main`), and a subsequent `git push -u origin <feature-branch>` pushes the STALE feature-branch ref (still at the prior PR's pre-squash commit), so the PR opens with an empty/wrong diff. This happened on the #361 handoff (the commit landed on local `main` as `2b589cd`; recovery was `git checkout -B <feature> <commit>` to repoint the feature branch, `git branch -f main origin/main` to undo the local-main commit, then a force-push of the feature branch). Recovery is clean and on-branch (no `main` push, no shared-history rewrite), but the fix is cheaper than the catch: switch to the feature branch immediately after the sync.
- **Operating mode is now attended-autonomous, not overnight** (set 2026-06-26, codified in [`.claude/CLAUDE.md`](../.claude/CLAUDE.md) #349): green CI = standing merge authority (merge green PRs and proceed without per-merge approval), stricter-is-safer always, and the pending-decisions / 2-minute-timeout graceful-degradation mechanism ([`pending-decisions.md`](pending-decisions.md), currently empty). `overnight-pr.md` is `stub` (overnight mode is OFF); do not treat it as in-flight. The `/resume` command now has a step 7 that checks `pending-decisions.md` before the queue.

## Standing disciplines (do not drift from these)

- **PRIMORDIAL RULE**: Quality > Speed > Cost; integrity absolute. Emit the integrity-check line at task start, before commit, before completion claims, and at tension points.
- **Post-commit audit**: after every commit, run `tools/run_all_audits.sh` standalone before pushing; never chain commit and push. Before push, also `tools/run-pr-time-checks.sh`.
- **`lint-language` pre-flight on new pack prose** before the first commit (catches em-dashes / British `-ise`). (Relevant for DD-4/5's `go.md` rewrite.)
- **Pre-commit dash-grep on new root `CHANGELOG.md` lines** (`grep -nP "\xe2\x80\x93|\xe2\x80\x94"`): the D3 dash gate is PR-time only, so a late-assembled CHANGELOG entry can slip an em-dash that surfaces at pre-push; grep the added lines before the first commit.
- **Grep-after-wiring / convention change**: after editing a restated-across-surfaces value, `grep` the OLD form across the full changed file AND every sibling surface; zero hits before commit.
- **Corpus-wide-grep / special-case-the-edge / find-every-carrier**: a mechanical bulk edit (a rename, a relabel, a title alignment) must verify EVERY adjacent field, special-case the known edge cases (e.g. preserve rating "Critical" while fixing impact-5; the DD-2/3/11 grep found a 4th carrier beyond the 3 named), and search corpus-wide for the OLD form, not apply one convenient rule uniformly.
- **Sweep close-out bookkeeping triplet**: a `/validate` close-out PR regenerates the generated artefacts (after per-doc version bumps), advances the TODO sweep cursor (gate 45 + the gate-36 TodoStaleness regression both enforce it), and bumps `validate-sweeps/history.md`'s own Version. The post-commit audit catches all three; doing them pre-commit avoids the fail-then-fix loop.
- **60-second** paired fallback timer after every `subscribe_pr_activity`.
- **Per-PR QA**: formal `/validate-pr` (Subagent A) + `/retro` after every merge; no skip, no abbreviation. **One standing exception**: the session-closing handoff PR skips both (loop-break); the compensating control is the corpus-wide `/validate` that `/resume` runs first. (This exception is now generalized into the pack layer, #305.)
- **PR close-out checklist** (the degradation guard): prior PR's `/validate-pr` row AND `/retro` row both batched in; closed TODO items rotated to DONE (every path-shaped code span in a CHANGELOG entry linked, changed or merely mentioned); stale-count check if an enumerated collection changed; session-handoff.md refreshed.
- **Self-assess for degradation / session-continuation SOP**: the default is to CONTINUE. Wind down ONLY on actual drift/hallucination/mistakes the QA layer did not catch; work size/shape, session length, and "a large / substantial / fresh-context-best series remains" are NOT triggers (corrected #460, maintainer 2026-06-29: about 13 of the last 15 proposed handoffs were the wrong call). A large series ahead is worked PR-by-PR with skeptical verifier subagents sustaining quality, not handed off. Steady-state misses the per-PR `/validate-pr` catches are NOT drift. The assistant may OFFER a handoff for the maintainer to consider before a substantial/critical/long piece of work begins, as a non-default suggestion (the maintainer chooses; absent a choice, continue). **When a handoff IS warranted (genuine degradation evidence), do NOT take it silently: run the `## Wind-down decision framework` in [`.claude/CLAUDE.md`](../.claude/CLAUDE.md)** (surface the justification + a per-PR likelihood assessment of the next-five PRs + named options A handoff / B recommended additional PRs / C higher-risk order / D the Ulysses-pact impulse-check; roughly-2-minute timer defaulting to handoff on no answer; codified #375, corrected #460).
- **Compute-don't-ask**: before surfacing a question, apply a "can I compute/verify this myself?" gate; surface only the result or a decision that genuinely needs maintainer judgement. (Counterpart: surface a genuine design tradeoff before building, as S5's FP surface was surfaced with named options rather than guessed.)

## Refresh and pruning discipline

**Refresh (at session close).** This file is refreshed at every PR close-out (post-merge), as part of the recursion-avoidance batch that carries the validate-pr/retro rows into the next PR. The session-closing handoff PR prepends this session's new blocks (the "Next actions" narrative block, the "State snapshot", and the "Asserted expectations" block) as the new *current* set.

**Prune (at resume, in the first PR).** Because the closing session only ever *adds*, the per-session blocks would otherwise accumulate without bound and bury the load-bearing state (the file's one job is a fast, reliable single resume point). So the receiving session prunes, as part of its first PR (the `/validate` close-out, per `## How to resume` step 6):

- **Retention: keep current + 1 prior.** Keep the most-recent session's blocks and the single immediately-prior session's blocks in each per-session stack (the "Next actions" narrative blocks, the "State snapshot" blocks, the "Asserted expectations" blocks). Delete everything older. Also delete superseded one-off "Next-session queue" / dated "This session's work" sections.
- **Keep the standing sections in full** (not per-session): the header / `## How to resume` / `## Resume cursor`, the `## Open decisions awaiting maintainer`, `## Trust-recovery state`, `## Known environment behaviours`, `## Standing disciplines`, and this section. These are durable reference, not session narrative.
- **Migrate-before-delete (the safety rule).** Before deleting a block, confirm any load-bearing item it carries that is NOT recorded elsewhere (an open decision, a pending maintainer action) has been migrated to `## Open decisions awaiting maintainer` / [`TODO.md`](../TODO.md) / [`pending-decisions.md`](pending-decisions.md). The durable record of pruned session *narrative* already lives in [`CHANGELOG.md`](../CHANGELOG.md) + its detailed mirror, [`DONE.md`](DONE.md), [`validate-sweeps/history.md`](validate-sweeps/history.md), [`session-metrics.md`](session-metrics.md), and git history, so narrative is safe to drop; only un-recorded forward-looking items need migrating.

The companion close-out-checklist line and the `/resume` command step encode this so it is a standing convention, not a one-off. This convention was added 2026-06-28 by maintainer direction after the file reached 426 lines / 47 stacked session blocks; the first prune cut it to ~120.
