# Session Handoff

**Purpose.** This file is the single resume point for a new Claude Code session. It is refreshed at every PR close-out so that opening a fresh session and continuing requires only one instruction from the maintainer (see "How to resume"). It exists because long sessions degrade (context dilution, lossy compaction, state drift, error compounding); a fresh session that rebuilds state from this file plus the durable repo artefacts is more reliable than a long one running on accumulated memory. The mechanisms and mitigations are written up generically in [`.working/session-length-considerations.md`](session-length-considerations.md). This file is maintainer working state, exempt from corpus audit gates per the `.working/` exemption.

This is an **as-of-last-refresh snapshot**, not a live-HEAD claim. Versions and counts drift forward as work advances; always verify against live files before relying on them.

## How to resume (the one command)

In a new session, the maintainer sends only: **`/resume`** (or "read `.working/session-handoff.md` and continue").

On `/resume`, the assistant:
0. **Runs the concurrency lease check FIRST** (`/resume` step 0, shipped with the section-3.7 interlock): read [`session-state.md`](session-state.md) from `main` and git-cross-check unmerged `origin/claude/*` sibling branches; 60-minute staleness window, advisory HOLD on a live signal (never proceed on a timeout); ACQUIRE the lease on proceeding. Gate 63 guards the lease file's shape.
1. Reads this file in full, including the **"Known environment behaviours"** section below.
2. Reads `.claude/CLAUDE.md` (the PRIMORDIAL RULE and project disciplines), the most recent few `CHANGELOG.md` entries, and **[`.working/third-party-issues.md`](third-party-issues.md)**.
2a. **Loads the `grc_library_ref` reference index into memory** (`grc_library_ref/INDEX.md`, `grc_library_ref/catalogue.yml`, `grc_library_ref/SECTION-INDEX.md`, `grc_library_ref/COVERAGE-MAP.md`) so the reference set and its versions are known before any task; the index, not a guessed path, is authoritative for the reference set and its locations. See `/resume` command's Reference-knowledge-base step and the `## Reference-version currency` SOP in `.claude/CLAUDE.md`.
3. Runs `git rev-parse --is-shallow-repository` (unshallow with `git fetch --unshallow` if `true`, **before any history-aware audit**), then `tools/run_all_audits.sh` to confirm the corpus is green, and `git log --oneline -5` to confirm HEAD.
4. Verifies the version/count snapshot below against live files.
4a. **Batches the unattended-run clarifications up front (maintainer-directed 2026-06-25)**: before resuming the queue, asks the maintainer (via `AskUserQuestion`, batched into as few rounds as possible) every decision that would let the session run unattended longer, pre-populating decisions for **all Priority 1 and Priority 2 backlog items** and **the next 10-plus planned PRs** (including any outside P1/P2, e.g. the FR-167 batches). Compute-don't-ask first (ask only genuine authorial decisions, not what the assistant can verify); record the answers in the next-actions queue so they survive a mid-session compaction; mark deferrals and route around them. See `/resume` command step 5 for the authoritative form.
5. **Runs a full corpus-wide `/validate` as the first substantive task** (the loop-break compensating control for whatever session-closing handoff PR last closed, which skips its trailing `/validate-pr` + `/retro`): full A/B/C subagent dispatch over the closing session's delta window, recorded in [`validate-sweeps/history.md`](validate-sweeps/history.md) and the Resume-cursor section above. **Cross-check the findings against the "Asserted expectations" section below**: a contradiction of an asserted-clean touched surface is a genuine miss, escalated; an ordinary finding routes to the backlog. Standing invariants to confirm: the corpus is CSF-1.1-clean (gate 54, 0 carriers; a carrier in a per-document NIST table is a genuine finding, not residue); four-surface parity at the current gate count (gate 35); the gate/rule/skill/command counts (gate 39 + prose). The specific sweep number and delta window are in the current "Next actions" block below.
6. **Prunes this file as part of that first `/validate` close-out PR** (keep current + 1 prior, per `## Refresh and pruning discipline` below), so the handoff does not accumulate stale session history across resumes.
7. Continues from "Next actions" below.

## Resume cursor (machine-readable)

This is the relocated home of the former `## Session resume metadata` block (moved out of
[`TODO.md`](../TODO.md) in PR #413 so TODO stays purely forward-looking). The sweep cursor below is
read by [`tools/lint-todo-staleness.py`](../tools/lint-todo-staleness.py) (gate 45), which flags it
if it falls behind [`validate-sweeps/history.md`](validate-sweeps/history.md). The full sweep history
lives in that history file; the live per-session state (branch, versions, counts, green-at-`<sha>`) is
in the "Next actions" blocks below and is an as-of-last-refresh snapshot (drift forward is expected).

- **Last validation sweep**: Sweep 89 iter 1 (2026-07-08; the loop-break corpus-wide `/validate` over the #687..#699 deltas, run at the `/resume` from session-closing handoff #699 on branch `claude/resume-sweep89-deep-assessment`, attended-autonomous daytime; full three-subagent dispatch. Mechanical baseline **66/66** at `c5e2ed6`/#699 (= `origin/main`), a descendant of the closing session's asserted green-at `b516ad9` (no close-vs-start drift). Pre-flight: 396 files, 31 suppressed, 9 candidates, all FALSE POSITIVES (the standing comparative/historical phrasing class), dismissed by A and B. **A: 0 findings; B: 0 findings; C: 1 warning** (C-1, `specification-audit-programme.md:149` §6 + `:70` §5: the gate-59 spec narrative still described the static `CUTOFF_PR = 463` comparison boundary that #695 replaced with a dynamic floor; the tool docstring was updated, the spec was not; in-window, gate-blind, FIXED this PR: both passages rewritten to the dynamic-floor / current-week model, spec `1.16.59`->`1.16.60`, taxonomy/scorecard regenerated). **No asserted-expectation contradiction** of any #687-#698 claimed-clean surface (per-PR QA rows, FR-63 §2.7 wiring, #695 no-data-moved, #696 ANPD secondary-tier, #694 reframe complete, and the 66/12/18/11 four-surface parity all corroborated by A and B); the loop-break compensating control PASSES. Detail: [`2026-07-08-sweep89-iter1.md`](2026-07-08-sweep89-iter1.md). Prior: Sweep 88 iter 1 (#685..#686 deltas, 2 out-of-window low-severity findings both fixed in #687; [`2026-07-07-sweep88-iter1.md`](2026-07-07-sweep88-iter1.md)).)

## Next actions (the queue for the next session)

**CURRENT (2026-07-08 resumed session `claude/resume-sweep89-deep-assessment`; ATTENDED-AUTONOMOUS DAYTIME; on the NUC).** Resumed from the #699 session-closing handoff. Step-0 lease ACQUIRED (prior `released`, no live `origin/claude/*` siblings inside the 60-minute window, git cross-check clean). Environment: local NUC, `gh` CLI (no GitHub MCP), egress available (iso.org 403s), NO stop-hook auto-commit-push (commit/push manually), pipe-guard PreToolUse hook fires. Corpus green **66/66** at `c5e2ed6`/#699; versions confirmed (library `2026.07.187`, README `1.9.548`, pack `1.54.7`); counts 66/12/18/11; suite 347. **0 verifier overrides, 0 active high-assurance items; 1 pending decision** (changelog PR 2, surfaced this resume). **This close-out PR:** ran **Sweep 89** (loop-break control over #687..#699; 1 in-window warning C-1 = the gate-59 spec-narrative dynamic-floor drift from #695, FIXED this PR; no asserted-expectation contradiction, control PASSES), morning-processed [`overnight-pr.md`](overnight-pr.md) to `stub` (ENDING overnight mode per the #699 directive), pruned this handoff to current + 1 prior, and surfaced the deferred-question queue + the changelog-PR-2 pending decision to the maintainer. **Mode = ATTENDED-AUTONOMOUS DAYTIME** (maintainer reachable/glanceable; green CI = merge authority, decisions surfaced by exception; full per-PR `/validate-pr` + `/retro` always; stricter-is-safer; NOT overnight, so genuine authorial decisions are asked not deferred). **MAINTAINER-DIRECTED TASK AHEAD OF THE STANDING QUEUE: integrate the Fable `/deep-assessment` delivery** (merged scratch-side as PR #109, `inbox/worker-20260708-fable/deep-assessment-build/`), two PRs under validate-then-apply with per-PR QA: **PR A** = the two advisory gate-efficacy tools (`tools/audit-gate-blindspots.py`, `tools/audit-gate-mutation.py`, `tools/gate-mutation-variants.json`); **PR B** = the SKILL + `/deep-assessment` command + `.working/deep-assessment/register.md` stub + gate-44 PAIRS registration + `resume.md` step-7 hook + pack README minor bump (new skill, `1.54.7`->`1.55.0`) + the post-#695 deltas (per-run records as dated FILES `.working/deep-assessment/YYYY-MM-DD-rN.md` not directories; `deep-assessment` added to `RECORD_SUBDIRS` in `sweep-working-records-to-scratch.py`; one model-agnostic model-tiering sentence in SKILL step 3 + command step 3). Re-run both tools and recount every figure before citing; carry the `**Worker provenance:**` line naming the manifest path in both detailed CHANGELOG entries. Sequencing flag RESOLVED (the `.working/` relocation is TODO 3.19, PR-1 machinery merged in #695; does NOT block this integration). After both PRs, OFFER the first `/deep-assessment` run as a named option (do NOT auto-start). **Fable staged defaults CONFIRMED** (maintainer, this resume): command `/deep-assessment`; `derives_from` `trust-recovery-escalation.md`; no TODO item (register tracks runs); model-tiering = add the sentence in PR B. **Standing queue after Fable (deferred/blocked, maintainer triage):** changelog PR 2 (scratch privacy-gate) / PR 3 (root reformat); the 3.16/3.17 alignment maps; the register-ageing classifier (TODO 3.15); the egress-priority items (GR-GAP-1, 11 legislation deepenings); the git-history collapse; the scratch coverage-refresh sync (index 28 PRs behind). **Superseding note:** this block supersedes the CLOSING #699 block's "next `/resume` will..." directives (they are now DONE by this PR) and the mode is ATTENDED-AUTONOMOUS DAYTIME, not overnight.

**CLOSING (2026-07-08 SESSION-CLOSING HANDOFF #699; OVERNIGHT; on the NUC).** The closing block for the long NUC session that resumed 2026-07-07 (DAYTIME-UNATTENDED: Sweep 88 + TODO 1.12 + the reference-library split #687-#689, then the reference-base ingests) and continued into 2026-07-08 (switched to OVERNIGHT by the maintainer: #693/#694 the `/full-qa` reference-information cleanup + fixes, #695 the changelog-restructure PR 1 machinery, #696 the TODO 1.11 ANPD residual, #697 FR-63 the adoption worked example closing §2.7, #698 the README companion-catalogue row + the register-ageing-tool deferral). Session merged through **#698**; this **#699** handoff is its last act (the loop-break: NO trailing `/validate-pr` / `/retro`; the compensating control is the next `/resume`'s corpus-wide `/validate` over the #687..#699 deltas). **MAINTAINER DIRECTIVE FOR THE NEXT SESSION (2026-07-08): "wind down and we'll start a new session. that session will end overnight mode, ask me the deferred questions, clarifications, and begin attended autonomous daytime mode."** So the next `/resume`: (0) runs the standard `/resume` protocol (lease, reference index, the loop-break `/validate` over #687..#699 FIRST, prune this file to current + 1 prior); (1) ENDS overnight mode and does the [`overnight-pr.md`](overnight-pr.md) morning-processing (route the 2026-07-07 and 2026-07-08 run content to the ledgers and reset to `stub`; the file sits `in-flight` now, gate 46 passes on `in-flight`, so the next session closes it); (2) ASKS the maintainer the deferred questions/clarifications below; (3) begins ATTENDED-AUTONOMOUS DAYTIME mode. **Deferred questions/clarifications for the next session to ask (recommendation in each; detail in [`pending-decisions.md`](pending-decisions.md) / [`../TODO.md`](../TODO.md)):** (1) **changelog-restructure PR 2 (the sweep)** the one FORMALLY-PENDING item: how to reconcile the archive's `jposluns` watermark token + historical house-style with scratch's `validate.py` Check 5 (privacy) + Check 6 (style) (recommend A: exempt the frozen `archive/` from scratch Checks 5+6, the frozen-archive principle); (2) **changelog-restructure PR 3** (root reformat to the compact `**date | version | PR** - summary` form + 1-2-sentence compression of the ~679 historical root entries, a wide sensitive change touching gate 59's root parser): proceed as a fresh focused effort or keep deferred (recommend fresh / attended); (3) **alignment maps 3.16 ETSI-SAI / 3.17 ATLAS**: the surfaced-not-picked alignment-shape (Q3 default = smallest-defensible) + trust-tier decision; (4) **register-ageing advisory tool (TODO 3.15)**: the classifier-design / register-marker decision (the prototype over-flags adopted-habit notes, deferred #698); (5) **4.6 S-b / S-e** untriaged XS enhancements, **S-f** design-scoping, and **S-c** now OVERLAPS the FR-63 adoption doc shipped in #697 (reconsider its scope); (6) **egress-priority items**: GR-GAP-1 (the 17 unregistered ISO/IEC pairs + the 29134:2017-vs-:2023 conflict, iso.org-upstream-gated) and the 11 legislation deepenings (the maintainer's egress instance); (7) **git-history collapse** (Option A collapse-all-to-one-commit, a protected-branch force-push the maintainer runs awake, to purge pre-cleanup file versions from GitHub history). **State:** grc_library `main` clean at #698; scratch clean (no archive committed, PR 2 not run); `grc_library_ref` live post-split. 0 verifier overrides, 0 active high-assurance items; 1 formally-pending decision (PR 2). Environment: local NUC, `gh` CLI (no MCP; GraphQL pool exhausted this session so CI-poll / merge run via REST), egress available (iso.org 403s), NO stop-hook auto-commit-push, pipe-guard PreToolUse hook fires.

**PROGRESS (2026-07-07 resumed session `claude/sweep88-todo112-nuc-resume`; DAYTIME-UNATTENDED; on the maintainer's NUC).** Resumed from the #686 session-closing handoff. **Environment (re-verified at resume, per the #686 prediction that the NUC differs from the cloud env):** local Linux NUC, `gh` CLI is the PR mechanism (no GitHub MCP), egress available (iso.org bot-blocks 403 but general egress 200), NO stop-hook auto-commit-push (commit/push manually), the pipe-guard PreToolUse hook DOES fire here (a difference from the cloud resumed-session's TODO-1.9 limitation; feeds the env-detection TODO). `grc_library_ref` exists as an empty repo (split not yet started). Step-0 lease ACQUIRED (prior `released`, no live siblings, git cross-check clean). Corpus green **66/66** at `df04241`; versions confirmed (library `2026.07.174`->`.175` this PR, README `1.9.535`->`.536`, pack `1.54.6`). **0 pending decisions, 0 verifier overrides, 0 active high-assurance items** (all three registers re-read clean). **Sweep 88 ran** (loop-break control over the #685..#686 deltas): 2 out-of-window low-severity findings, both FIXED this PR (TODO 1.12 procedure architecture omission + a stale illustrative comment in `lint-guardrail-cadence.py`), no asserted-expectation contradiction, control PASSES. **TODO 1.12 CLOSED** (architecture added to the annual-review procedure Scope, all three completeness enumerations now at 11 domains). **Mode = DAYTIME-UNATTENDED** (maintainer reachable, glanceable periodically; green CI = merge authority; no idle-stop; stricter-is-safer; graceful-degradation defers/skips authorial/irreversible decisions; NOT overnight, so no skip-to-morning, no overnight-off protection, normal wind-down applies only on genuine degradation evidence). **Reference base is being SPLIT** scratch -> scratch + `grc_library_ref` (the #686 handoff's single-scratch-reference framing is superseded). **Queue (work order, maintainer-directed at this resume):** (1) this Sweep-88 + TODO-1.12 close-out PR; (2) the **reference-library split** EARLY (3a additive: copy scratch `ref/` into `grc_library_ref` at repo root dropping the `ref/` prefix, rewrite generator/index/catalogue paths, stand up ref's own validate gate green, least-privilege access, verify egress READ+WRITE via `gh` PR; then a **CHECKPOINT** pausing for maintainer go-ahead before the cutover 3b: re-point grc_library's reference SOP + `/resume` step + runbook + scratch CLAUDE.md, then subtractively remove `ref/` from scratch, coordinated PRs ref-green -> grc_library-re-points -> scratch-removes; on no answer HOLD the cutover, keep ref live-and-green alongside scratch's `ref/`, route to the non-legislation queue, never delete scratch `ref/` without explicit confirmation); (3) fast-follow ref schema (license + access_tier) + a citation-not-embed gate on grc_library + a forkability note; (4) post-cutover, with egress: the egress-gated 1.5 (51 rows) / 1.11 (Brazil ANPD) / GR-GAP-1 (register + ISO 29134 year) / SR-1 + the 11 legislation deepenings (FR-59 batch 2, FR-60, FR-74, FR-41, DORA, NIS2, jurisdiction annexes), confirming each externally-versioned source upstream this turn; (5) split-independent non-legislation queue as the checkpoint-hold fallback: FR-63 (2.7, apply-ready), 3.16/3.17 alignment builds (Q3=A smallest-defensible, record the choice), small P3/P4; (6) file the environment-detection-intelligence TODO (tooling tier, `.claude/` parts maintainer-authored); (7) a consolidated post-batch scratch coverage-refresh sync (TODO 1.12 + split churn + dead brief paths + 4 dead coverage anchors). The egress worker seed in `grc_library_scratch/requests/` is a SEPARATE scratch-only worker session, launched only on maintainer direction; do NOT start the deferred reference-base ingest item (re-attached later).

_Session history older than the current and immediately-prior session is pruned at each resume (keep current + 1 prior, per `## Refresh and pruning discipline` below). The durable record of pruned sessions lives in [`CHANGELOG.md`](../CHANGELOG.md) and its detailed mirror, [`DONE.md`](DONE.md), [`validate-sweeps/history.md`](validate-sweeps/history.md), [`session-metrics.md`](session-metrics.md), and git history._

## State snapshot (2026-07-08 resumed session `claude/resume-sweep89-deep-assessment`; ATTENDED-AUTONOMOUS DAYTIME; on the NUC)

- **Current truth (verify against live files at `/resume`)**: the resumed session has shipped #700 (the `/resume` close-out: Sweep 89 + the C-1 gate-59 spec fix + `overnight-pr.md` morning-processing + lease acquire), #701 and #702 (the Fable `/deep-assessment` integration: the two advisory tools, then the skill + command + register + hooks), #703 and #704 (correcting then completing a paired-skill parity gate-number mislabel, gate 42 to gate 44), #705 (the AIQT-Principle apex-rule codification: the pack apex rule and its pack-wide restatements reframed to (Accuracy = Integrity = Quality = Trust) > Speed > Cost), and #706/#707 (the reference-audit-build integration: the advisory tool in PR A, then the `/reference-audit` skill + command + wiring + TODO 2.14 rotation in PR B, with the gate-60 auto-fired guardrail review r6 riding in #707), and #708 (the changelog-restructure PR 2: the initial current-week sweep, the detailed mirror pruned to the current week (now 42 entries, #667 through #708), 647 detailed entries + 204 records archived to scratch #113). At this PR's head: library `2026.07.196`, README `1.9.557`, pack `1.57.0`; audit-programme spec `1.16.60`; gate count **66**, suite **347**, rules **12**, skills **20**, commands **13** (the reference-audit skill and command added in #707; deep-assessment's in #702; #708 is working-state + versions only). **Mechanical baseline: green-at `59daff2` (#707) = 66/66**; #708 is a working-state data-move (the detailed-mirror prune to current-week + the 204 dated-record removals) plus version bumps (no corpus document body), so `main` stays 66/66 at #708's descendant merge (verify post-merge). Gate 59 mirror parity holds under its dynamic floor, now anchored at #667 (the oldest entry in the pruned mirror). **[`pending-decisions.md`](pending-decisions.md): 0 pending (the changelog PR 2 sweep-to-scratch privacy-gate was resolved to Option A and EXECUTED in scratch #113 + grc_library #708). verifier-overrides: 0 pending. high-assurance register: 0 active.** **Operating mode: ATTENDED-AUTONOMOUS DAYTIME** (overnight mode ENDED this PR per the #699 directive; green CI = merge authority with decisions surfaced by exception; full per-PR `/validate-pr` + `/retro` always; stricter-is-safer). **Environment: local NUC, `gh` CLI (no GitHub MCP), egress available (iso.org 403s), NO stop-hook auto-commit-push (commit/push manually), pipe-guard PreToolUse hook fires.** **Next (remaining maintainer-directed work): the reference-audit-build integration is DONE (#706 + #707) and the changelog-restructure PR 2 sweep is DONE (#708 + scratch #113). NEXT is the AIQT follow-ups: PR 2 (the corpus AIQT principle document in `governance/`, claim-fit-verified framework mappings, review gate d before its first commit) and the scratch `CLAUDE.md` AIQT one-liner (payload section D). Then the deferred first runs (first `/deep-assessment`, first FULL-mode `/reference-audit`) as dedicated follow-up tasks; then the changelog-restructure PR 3 (the root-CHANGELOG compact reformat, a deferred wide change) plus the per-PR sweep-step wiring residual; then the standing deferred/blocked queue for maintainer triage. The two r6 `[guardrails]` proposals (the per-touch reference-breadth backstop, the worker-brief gate-number-verification rail) await the maintainer's decision in TODO 3.15.** `overnight-pr.md` morning-processed to `stub` this PR; the deferred-question queue and the changelog-PR-2 pending decision surfaced to the maintainer at this resume.

## State snapshot (2026-07-07 resumed session, continued 2026-07-08 OVERNIGHT; on the NUC; closes at the #699 session-closing handoff)

- **Current truth (verify against live files at `/resume`)**: work merged through **#698**; **this #699 session-closing handoff PR is in flight** and is the session's last act. The 2026-07-08 OVERNIGHT session merged **#694** (`/full-qa` follow-up fixes), **#695** (changelog-restructure PR 1 machinery), **#696** (TODO 1.11 ANPD residual, secondary-tier), **#697** (FR-63 adoption worked example, §2.7 closed), and **#698** (README companion-catalogue row folding #697's note + register-ageing-tool deferral). At the #699 handoff head: library `2026.07.187`, README `1.9.548`, pack `1.54.7`, runbook `1.1.7`, validate-pr history `1.2.474`, improvement-log `1.0.418`, session-metrics `1.0.41`; #699 bumps NO corpus document (working-state + README version surfaces + CHANGELOG only); gate count **66**, suite **347**, rules **12**, skills **18**, commands **11**. D7 validates the labelled version tokens against the live headers at this PR's head. **Mechanical baseline: green-at `b516ad9` (= #698 merge, current `main`) = 66/66**; #699 is working-state + version-surface + CHANGELOG only, so `main` stays 66/66 at #699's descendant merge (verify post-merge). **[`pending-decisions.md`](pending-decisions.md): 1 pending (changelog PR 2 sweep-to-scratch privacy-gate decision). verifier-overrides: 0 pending. high-assurance register: 0 active.** **Operating mode: OVERNIGHT, being handed off. MAINTAINER DIRECTIVE FOR THE NEXT SESSION (2026-07-08): the next session ENDS overnight mode, does the overnight-pr morning-processing, ASKS the maintainer the deferred questions/clarifications (enumerated in the CLOSING next-actions block above), and begins ATTENDED-AUTONOMOUS DAYTIME mode.** **Environment: local Linux NUC, `gh` CLI (no GitHub MCP; GraphQL pool exhausted this session, so CI-poll + merge run via REST endpoints), egress available (iso.org 403s), NO stop-hook auto-commit-push (commit/push manually), pipe-guard PreToolUse hook DOES fire.** **Reference-library split DONE** (the reference base lives in the private `grc_library_ref` repo; grc_library re-pointed to it, completed by #689). **The changelog-restructure is PARTLY landed:** PR 1 machinery (#695) is in; PR 2 (the sweep) is BLOCKED on the scratch privacy-gate decision (no data moved, `main`/scratch both clean); PR 3 (root reformat/compress) is prudentially deferred as a wide sensitive change; protected-file descriptive edits are staged in [`deferred-protected-changes.md`](deferred-protected-changes.md) item 7. **DEFERRED to a future attended session (maintainer 2026-07-08): the git-history collapse** (Option A, collapse-all-to-one-commit, recommended) to purge the pre-cleanup file versions from GitHub history, a protected-branch force-push the maintainer runs awake. **The readily-actionable UNBLOCKED substantive queue is thin** (honest assessment recorded in [`overnight-pr.md`](overnight-pr.md)): the remaining items are the deferred/blocked set the next-actions CLOSING block enumerates for the maintainer's triage (changelog PR 2/3, the 3.16/3.17 alignment maps, the register-ageing classifier, 4.6 S-b/S-e/S-f/S-c, the egress-priority items, the git-history collapse).

## Asserted expectations (session close-out convention)

**Purpose.** At session close, the closing session records here its *asserted-clean* claims, scoped to the surfaces it actually touched or verified this session (not a blanket corpus-wide clean bill). The next session's `/resume` runs its corpus-wide `/validate` (the loop-break compensating control) and cross-checks the findings against these claims: a finding that *contradicts* an asserted-clean touched surface is a genuine miss signal (the closing session's self-assessment was wrong), to be escalated rather than triaged as an ordinary out-of-window finding. A finding on a surface NOT in this list is an ordinary finding, handled by the normal in-window / out-of-window triage.

**Why this shape (and not a two-run diff).** A rejected alternative was: the closing session also runs a full `/validate`, passes the result forward, and the receiving session diffs the two. That is wasteful and noisy: the corpus is byte-identical across the boundary (same `main` commit), so there is no corpus delta for two runs to legitimately differ on, and `/validate`'s expensive layer is non-deterministic subagent fan-out, so two runs differ by LLM sampling variance, not corpus facts (a "discrepancy = major issue" rule would fire on noise). It also collides with the loop-break (a closing-session finding can only ride the prompt forward, the lossy state the handoff exists to avoid). The asserted-expectations shape gets the real signal (a contradiction of a claimed-clean touched surface) at the cost of a few lines, no second sweep. The only deterministic close-vs-start diff worth keeping is the mechanical one, captured by the green-at-`<sha>` line in the snapshot above.

**Scope discipline.** Assert clean ONLY what this session mechanically verified (a green gate) or covered with a formal `/validate-pr` or `/validate` subagent pass. Do not assert clean a domain the session did not touch; an unfounded clean claim turns an ordinary pre-existing finding into a spurious "major miss" at the next resume. This functions as a lightweight proxy for the `/validate-pr` the session-closing handoff PR skips (the loop-break): it records what the session's own QA actually covered, for the next `/validate` to test.

**This session's asserted expectations (2026-07-07 to 2026-07-08 NUC session, `claude/sweep88-todo112-nuc-resume`, #687-#698; closes at #699, the session-closing handoff PR):**
- **Every merged PR #687 through #698 received its full formal per-PR QA**: a `/validate-pr` Subagent A dispatch on each, with a refute-briefed skeptical verifier on the substantive-tier changes, recorded in [`validate-pr/history.md`](validate-pr/history.md) and the `/retro` rows in [`improvement-log.md`](improvement-log.md). Post-merge findings were in-window and fixed in the next PR's batch (e.g. #695's stale `TODO.md:7` pointer fixed in #696; #698's one immaterial CHANGELOG-prose NOTE reworded in this #699). The #687 Sweep-88 close-out's own `/validate-pr` returned 0 findings.
- **Sweep 88 (the loop-break corpus-wide `/validate` over the #685..#686 deltas) passed clean**: 2 out-of-window low-severity findings fixed in #687 (TODO 1.12 annual-review architecture omission + a stale illustrative comment in `lint-guardrail-cadence.py`), no contradiction of any #686 asserted-clean surface. The loop-break compensating control for #686 PASSED.
- **green-at `b516ad9` (= #698 merge, current `main`) = 66/66** (the #698 post-merge standalone, confirmed by the #698 `/validate-pr` Subagent A: `All 66 audit gates passed`, EXIT=0; #699 is working-state + version-surface + CHANGELOG only, so `main` stays 66/66 at #699's descendant merge); suite **347**; four-surface parity at gate count **66** (gates 35/39/60/64 green standalone); rules **12** / skills **18** / commands **11**.
- **The changelog-restructure PR 1 machinery (#695) was verified data-safe end-to-end** before any data move: the gate-59 dynamic cutoff (`effective_cutoff` = 463 on the unswept mirror) and the sweep tool's lossless round-trip (676 == 676) + `--prune` re-parse assertion + archive-verify guard, independently reproduced by the #695 `/validate-pr` Subagent A. No data moved (PR 2 is blocked); `main` and scratch are both clean.
- **FR-63 (§2.7) is complete and correctly wired** (#697, re-verified by the #697 and #698 `/validate-pr` passes): [`docs/worked-example-adoption.md`](../docs/worked-example-adoption.md) exists, is reachable, closes §2.7 (TODO heading deleted, DONE entry present, P2 totals 11->10), is correctly NOT in the five composition entry-point lists (parity with its ingestion peer), and the README "(adoption)"/"(ingestion)" catalogue annotations match both docs' intros.
- **Known soft spots (NOT asserted clean)**: this #699 handoff PR's own diff receives no trailing sweep (the standing handoff exception; the compensating control is the next `/resume`'s corpus-wide `/validate` over the #687..#699 deltas). **TODO 1.11's primary-tier re-confirmation is PENDING** (the ANPD Resolution 15/2024 small-agent doubling was confirmed only at SECONDARY tier in #696, 1.11 kept OPEN; egress-gated on the maintainer's instance; the `register-canonical-citations.md` surface deliberately not re-signposted until the primary confirmation lands). The **changelog-restructure PR 2 (sweep) is BLOCKED** on the scratch privacy-gate decision and **PR 3 (root reformat/compress) is deferred** as a wide sensitive change (both in `pending-decisions.md`). The **3.16/3.17 alignment maps are UNBUILT** (blocked on authorial alignment-shape + trust-tier decisions). The **register-ageing advisory tool (TODO 3.15) was NOT shipped** (its heuristic over-flags adopted-habit notes; classifier-design refinement needed). The **egress-priority items** (GR-GAP-1's 17 unregistered ISO/IEC pairs + the 29134 conflict; the 11 legislation deepenings) are UNADDRESSED (egress-gated). The **git-history collapse is DEFERRED** to a maintainer-run attended session. **4.6 S-b/S-e untriaged, S-f un-design-scoped, S-c now overlaps the FR-63 adoption doc.** The `grc_library_ref` reference-base ingests and the `grc_library_scratch` PRs this session are non-corpus (gated by their own repos' CI), outside this corpus's asserted expectations.

**This session's asserted expectations (2026-07-06 to 2026-07-07 resumed session, `claude/grc-acquisition-reconfirm-brief-k8fppo`, #685; closes at #686, the session-closing handoff PR):**
- **Sweep 87 (the loop-break corpus-wide `/validate` over the #680..#683 deltas) passed clean**: A-F1 (framework Scope line missing architecture) fixed in #685; B and C 0 findings; no contradiction of any #684 asserted-clean surface (the second-line-enumeration corpus-wide resolution and the 66/12/18/11 four-surface parity both corroborated). The loop-break compensating control for #684 PASSED.
- **PR #685 received its full formal per-PR QA**: a refute-briefed `/validate-pr` Subagent A (run 2026-07-07, batched into this #686 handoff), which returned 1 OUT-OF-WINDOW finding (below, NOT asserted clean) and 0 in-window findings; cross-reference check 0 breaks; recorded in [`validate-pr/history.md`](validate-pr/history.md) + [`2026-07-07-PR-685.md`](validate-pr/2026-07-07-PR-685.md) and the `/retro` row in [`improvement-log.md`](improvement-log.md).
- **green-at `dc297ea` = 66/66** (the #685 post-merge standalone; #686 is working-state and version-surface only, so `main` stays 66/66 at #686's descendant merge); suite **346**; four-surface parity at gate count **66** (gates 35/39/60/64 green standalone); rules **12** / skills **18** / commands **11**.
- **The three domain-model enumerations were verified as a bounded class** (orchestrator apply-time verification of the #685 `/validate-pr` finding, a corpus-wide family grep): exactly three full "spans all governance domains / all documents" enumerations exist (`governance/standard-maturity-assessment-methodology.md:34`, `governance/framework-governance-performance-and-improvement.md:27`, `governance/procedure-grc-programme-management-and-annual-review.md:29`); two carry 11 domains including architecture, the third omits it (the routed finding); the other domain-word prose lists (README, control-testing) are deliberately-scoped, not the completeness class.
- **Known soft spots (NOT asserted clean)**: this #686 handoff PR's own diff receives no trailing sweep (the standing handoff exception; the compensating control is the next `/resume`'s corpus-wide `/validate`, Sweep 88 over the #685..#686 deltas, on the NUC). **TODO 1.12** (`governance/procedure-grc-programme-management-and-annual-review.md:29` omits architecture) is UNFIXED, routed as the NUC's first corpus PR: it is a KNOWN pre-existing finding, NOT a miss, if Sweep 88 re-surfaces it. The overnight queue (FR-63, the 3.16/3.17 alignment builds, small P3/P4, the now-egress-workable 1.5/1.11/GR-GAP-1/SR-1 and the 11 legislation deepenings) is UNAPPLIED, handed to the NUC. The remaining scratch-inbox deliveries beyond the 3 applied (#681-#683) remain UNAPPLIED. The deferred reference-base ingest item is DEFERRED (source re-attached a later session). Scratch #106/#107 are scratch-side (non-corpus), gated by scratch `validate.py`, outside this corpus's asserted expectations.

## Open decisions awaiting maintainer

- **RESOLVED (2026-07-01 at `/resume`; recorded in [`pending-decisions.md`](pending-decisions.md)):** the two 2026-07-01 pending decisions were answered by the maintainer. (1) **§3.2 detailed-CHANGELOG-mirror header-parity** = **cutoff-scoped gate** (accept the 3 pre-split-era missing headers #268/#353/#462 as an exemption; scope the parity gate from a cutoff PR forward; FP-free, no retroactive edit). (2) **§3.3 citation-verification recording-model** = **consistency cross-check, no register row** (register-vs-held-extract check surfacing/fixing mismatches only; the §9 human-primary-capture trust model unchanged). Both are now **P3 tooling** work, scheduled after the P1 FR-48 rename per the maintainer's post-P1 = P3-tooling direction.
- **OPEN (surfaced 2026-07-01): FR-70 source gate.** FR-70 (crypto-asset / blockchain governance, XL, H[critical], authorized this run via the harness) is source-gated: the high-assurance harness for a H[critical] regulatory domain needs ground truth that is not yet available (reference-base work tracked separately in the reference repo). Needs the maintainer to supply the missing primary sources, OR authorize authoring the crypto-regulatory core from training knowledge (advised against), OR redirect.
- **§6.3-vs-§7.3 audit-scope spec gap**: **CLOSED in #338.** Sweep 45 found that #336's "added to all 9 explicit-list content linters" enumeration was incomplete: gate 31 (date-staleness) and gate 10 (review-cadence), two more explicit-allow-list content gates, silently skipped the dir (gate 31 a clear §6.3 violation since §6.3 names "date currency"). #338 added `.project-governance` to both (with a scope-coverage regression test each) and amended the spec with **§7.4**, which enumerates the explicit-allow-list content-linter completeness obligation (the maintainer chose, 2026-06-25, to include gate 10 and to amend the spec). The maintainer-decided spec amendment closes the decision; the **directory-scan-scope parity meta-check** (TODO P2) is the queued mechanical backstop that would have caught this class.
- **R2 relocation**: CLOSED by principle in #314 (the project-governance separation spec classifies the 6-file citation-verification cluster as project governance, so it migrates in Phase 1). The Phase-1 migration, the queued directional-dependency gate (§7.3), and the 2 deferred §5.3 classifications are now forward-looking TODO items, not open decisions awaiting the maintainer.
- **FR-167 mapping gate (asked 2026-06-24)**: RESOLVED, built as gate 49 (#325 ISO + NIST well-formedness; #326 NIST category membership + 17-cell remap closing §4.6a). The control-code-validity gate catches hallucinated/mistyped codes; semantic mapping correctness remains the orchestrator's apply-time job. No longer awaiting.
- **DD-12 scope**: RESOLVED + APPLIED (#329, maintainer-approved "broaden + extend gate 49"). DD-12 in TODO is broadened from `PR.IP`-only to all CSF-1.1 carriers (`PR.IP`/`ID.SC`/`ID.BE`/`RS.RP`/`DE.DP`/`PR.AC`/`PR.PT`/...; `RS.RP`→mainly `RS.MA`), and a **gate-49-extension** TODO item is queued (validate framework codes in per-document reference tables, not only the central matrix; build the scanner first, then gate the migration). Both are now forward-looking TODO items, not open decisions. The migration itself (corpus-wide, non-partitionable) is queued.
- **Publications-assessment process (§4.12, surfaced + queued 2026-06-25)**: a maintainer-directed process to assess reference-base publications for useful info AND detect poisoning/false info before it informs corpus work, queued as TODO §4.12 (not yet built). Forward-looking, not an open decision.
- **Maintainer actions still pending**: (a) **seed the reference binaries** durably (git-proxy blocks writes), belt-and-suspenders ONLY: the in-repo reference modules gates 48/49 use are the authoritative validators, so FR-167 batch 5+ code validity is gate-protected without them; re-attach only if a worker wants the source text; (b) **provision the least-privilege external-worker account** (read `grc_library` / write `grc_library_scratch` only), this is the gating action for both the separate-session worker primitive (§4.11) and the queued **§4.4** worker-ready brief staging / `/subagent` capability. Neither blocks the next session's queue (in-session `Agent` fan-out works for FR-167 today).
- The larger-track decisions (FR-167 batches 4..N, the High[critical] net-new docs, the L/XL items) remain queued and scheduled deliberately.

## Trust-recovery state

The trust-recovery suite (`/full-qa` + `/fitness` r2) ran and the maintainer **signed off** (2026-06-22). Codification COMPLETE. Not re-triggered since; quality held throughout the 2026-06-24 overnight run, morning continuation, and the resume session (Sweep 38 + close-out #312, both warnings fixed first try, post-commit + pre-push green).

## Known environment behaviours (read before assuming working-tree state)

- **The stop-hook auto-commits AND pushes uncommitted changes on turn-end.** The working tree is auto-persisted to the feature branch; verify `git log` rather than assuming edits are held locally.
- **The squash-merge commit on `main` shows locally as `E noreply@github.com`** (GitHub's own merge commit; Verified in the GitHub UI, not locally verifiable). After syncing the branch to `main` the tip equals `origin/main`; **do NOT `git commit --amend`** it (that rewrites merged `main` history). Set `git config user.email noreply@anthropic.com && git config user.name Claude` so your own feature-branch commits are attributed correctly.
- **A squash-merge makes the local feature branch diverge from `main`** (the squash commit is new; the local pre-squash commit is its content but a different SHA), so `git merge --ff-only origin/main` fails after a merge; use `git reset --hard origin/main` to re-sync the feature branch (the local commit's content is in the squash, preserved in reflog).
- **The GitHub MCP server can disconnect mid-session** (it did 2026-06-24 after #304, blocking #305's open/merge). `git push` over HTTPS keeps working; only the MCP-routed GitHub-API operations are affected. If `mcp__github__*` tools vanish: keep committing and pushing via `git`, record the pending-merge state in the handoff, and resume the PR lifecycle when MCP returns. Do NOT merge by any non-MCP path. Full writeup: [`third-party-issues.md`](third-party-issues.md).
- **The commit-signing server can 503**, distinguish from a real defect (gates 31/40 pass on the real corpus while gate 36's failures are `git commit` subprocess errors); re-run after a pause. Full writeup: [`third-party-issues.md`](third-party-issues.md).
- **The clone may start shallow**; `git fetch --unshallow` before any history-aware audit (gates 31/40).
- **The `Bash` tool's working directory can reset to a non-repo directory between calls** (observed twice this session after long async-subagent gaps: a bare `git ...` failed with "not a git repository"). Prefix git-command sequences with `cd /home/user/grc_library &&` rather than assuming the cwd persisted.
- **After a post-merge sync you are ON `main`; `git checkout -B <feature-branch>` BEFORE building the next PR.** The post-merge sync (`git checkout main && git pull origin main`) leaves you on `main`. If you then build and `git commit` the next PR without switching branches, you commit to local `main` (which the stop-hook flags as an unverified commit ahead of `origin/main`), and a subsequent `git push -u origin <feature-branch>` pushes the STALE feature-branch ref (still at the prior PR's pre-squash commit), so the PR opens with an empty/wrong diff. This happened on the #361 handoff (the commit landed on local `main` as `2b589cd`; recovery was `git checkout -B <feature> <commit>` to repoint the feature branch, `git branch -f main origin/main` to undo the local-main commit, then a force-push of the feature branch). Recovery is clean and on-branch (no `main` push, no shared-history rewrite), but the fix is cheaper than the catch: switch to the feature branch immediately after the sync.
- **After a squash-merge, the stop-hook flags the GitHub merge commit as Unverified; do NOT amend it.** When the designated branch is restarted from the merged `main` (`git checkout -B <feature> origin/main`), its tip becomes GitHub's squash-merge commit (committer `noreply@github.com`), which the stop-hook reports as Unverified and suggests amending. DECLINE the amend: amending rewrites already-merged `main` history (forbidden), and GitHub signs its own merge commits (they show Verified on the platform; the local `%G?` check cannot see that). The flag clears once the next authored commit lands on the branch. (Observed at #681; predictably recurs each merge on the reused branch.)
- **Operating-mode mechanics** (set 2026-06-26, codified in [`.claude/CLAUDE.md`](../.claude/CLAUDE.md) #349): green CI = standing merge authority (merge green PRs and proceed without per-merge approval), stricter-is-safer always, and the pending-decisions / 2-minute-timeout graceful-degradation mechanism ([`pending-decisions.md`](pending-decisions.md)). The `/resume` command has a step 7 that checks `pending-decisions.md` before the queue. **Current mode: ATTENDED-AUTONOMOUS (maintainer-set 2026-07-05, the `claude/resume-tl5rez` resumed session, Q1=A)**: green CI = merge authority (merge own green PRs and proceed, decisions surfaced by exception); full per-PR `/validate-pr` + `/retro` always; stricter-is-safer always. This is NOT overnight/daytime-unattended, so there is no skip-to-morning or timeout-to-default path; genuine maintainer decisions are asked, not deferred. This session's queue (Q2=A): clear the four resolved-but-unexecuted pending items + the coverage-refresh scratch sync + the small P3 items, then P2 2.1. Egress re-tested at resume (Q3=try-then-defer): egress is up (control host loaded) but planalto.gov.br 503'd and iso.org 403'd, so the upstream-verification items (1.11 Brazil verification, 1.5's 51 needs-reconfirm rows, FR-70 sources) STAY DEFERRED; the Brazil annotation proceeds. The mode-exit priority ordering (overnight cleanup, then fixes, then tooling and protections, then new work) is codified in `.claude/CLAUDE.md`.

## Standing disciplines (do not drift from these)

- **PRIMORDIAL RULE**: the AIQT Principle, (Accuracy = Integrity = Quality = Trust) > Speed > Cost; the AIQT tier non-negotiable. Emit the AIQT-check line at task start, before commit, before completion claims, and at tension points.
- **Post-commit audit**: after every commit, run `tools/run_all_audits.sh` standalone before pushing; never chain commit and push. Before push, also `tools/run-pr-time-checks.sh`.
- **`lint-language` pre-flight on new pack prose** before the first commit (catches em-dashes / British `-ise`). (Relevant for DD-4/5's `go.md` rewrite.)
- **Pre-commit dash-grep on new root `CHANGELOG.md` lines** (`grep -nP "\xe2\x80\x93|\xe2\x80\x94"`): the D3 dash gate is PR-time only, so a late-assembled CHANGELOG entry can slip an em-dash that surfaces at pre-push; grep the added lines before the first commit.
- **Grep-after-wiring / convention change**: after editing a restated-across-surfaces value, `grep` the OLD form across the full changed file AND every sibling surface; zero hits before commit.
- **Corpus-wide-grep / special-case-the-edge / find-every-carrier**: a mechanical bulk edit (a rename, a relabel, a title alignment) must verify EVERY adjacent field, special-case the known edge cases (e.g. preserve rating "Critical" while fixing impact-5; the DD-2/3/11 grep found a 4th carrier beyond the 3 named), and search corpus-wide for the OLD form, not apply one convenient rule uniformly.
- **Sweep close-out bookkeeping triplet**: a `/validate` close-out PR regenerates the generated artefacts (after per-doc version bumps), advances the TODO sweep cursor (gate 45 + the gate-36 TodoStaleness regression both enforce it), and bumps `validate-sweeps/history.md`'s own Version. The post-commit audit catches all three; doing them pre-commit avoids the fail-then-fix loop.
- **60-second** paired fallback timer after every `subscribe_pr_activity`.
- **Per-PR QA**: formal `/validate-pr` (Subagent A) + `/retro` after every merge; no skip, no abbreviation. **One standing exception**: the session-closing handoff PR skips both (loop-break); the compensating control is the corpus-wide `/validate` that `/resume` runs first. (This exception is now generalized into the pack layer, #305.)
- **PR close-out checklist** (the degradation guard): prior PR's `/validate-pr` row AND `/retro` row both batched in; closed TODO items rotated to DONE (every path-shaped code span in a CHANGELOG entry linked, changed or merely mentioned); stale-count check if an enumerated collection changed; session-handoff.md refreshed.
- **Self-assess for degradation / session-continuation SOP**: the default is to CONTINUE. Wind down ONLY on actual drift/hallucination/mistakes the QA layer did not catch; work size/shape, session length, and "a large / substantial / fresh-context-best series remains" are NOT triggers (corrected #460, maintainer 2026-06-29: about 13 of the last 15 proposed handoffs were the wrong call). A large series ahead is worked PR-by-PR with skeptical verifier subagents sustaining quality, not handed off. Steady-state misses the per-PR `/validate-pr` catches are NOT drift. The assistant may OFFER a handoff for the maintainer to consider before a substantial/critical/long piece of work begins, as a non-default suggestion (the maintainer chooses; absent a choice, continue). **When a handoff IS warranted (genuine degradation evidence), do NOT take it silently: run the `## Wind-down decision framework` in [`.claude/CLAUDE.md`](../.claude/CLAUDE.md)** (surface the justification + a per-PR likelihood assessment of the next-five PRs + named options A handoff / B recommended additional PRs / C higher-risk order / D the Ulysses-pact impulse-check; roughly-2-minute timer defaulting to handoff on no answer; codified #375, corrected #460).
- **Compute-don't-ask**: before surfacing a question, apply a "can I compute/verify this myself?" gate; surface only the result or a decision that genuinely needs maintainer judgement. (Counterpart: surface a genuine design tradeoff before building, as S5's FP surface was surfaced with named options rather than guessed.)

## Refresh and pruning discipline

**Refresh (at session close).** This file is refreshed at every PR close-out (post-merge), as part of the recursion-avoidance batch that carries the validate-pr/retro rows into the next PR. The session-closing handoff PR prepends this session's new blocks (the "Next actions" narrative block, the "State snapshot", and the "Asserted expectations" block) as the new *current* set.

**Prune (at resume, in the first PR).** Because the closing session only ever *adds*, the per-session blocks would otherwise accumulate without bound and bury the load-bearing state (the file's one job is a fast, reliable single resume point). So the receiving session prunes, as part of its first PR (the `/validate` close-out, per `## How to resume` step 6):

- **Retention: keep current + 1 prior.** Keep the most-recent session's blocks and the single immediately-prior session's blocks in each per-session stack (the "Next actions" narrative blocks, the "State snapshot" blocks, the "Asserted expectations" blocks). Delete everything older. Also delete superseded one-off "Next-session queue" / dated "This session's work" sections.
- **Keep the standing sections in full** (not per-session): the header / `## How to resume` / `## Resume cursor`, the `## Open decisions awaiting maintainer`, `## Trust-recovery state`, `## Known environment behaviours`, `## Standing disciplines`, and this section. These are durable reference, not session narrative.
- **Migrate-before-delete (the safety rule).** Before deleting a block, confirm any load-bearing item it carries that is NOT recorded elsewhere (an open decision, a pending maintainer action) has been migrated to `## Open decisions awaiting maintainer` / [`TODO.md`](../TODO.md) / [`pending-decisions.md`](pending-decisions.md). The durable record of pruned session *narrative* already lives in [`CHANGELOG.md`](../CHANGELOG.md) + its detailed mirror, [`DONE.md`](DONE.md), [`validate-sweeps/history.md`](validate-sweeps/history.md), [`session-metrics.md`](session-metrics.md), and git history, so narrative is safe to drop; only un-recorded forward-looking items need migrating.

The companion close-out-checklist line and the `/resume` command step encode this so it is a standing convention, not a one-off. This convention was added 2026-06-28 by maintainer direction after the file reached 426 lines / 47 stacked session blocks; the first prune cut it to ~120.
