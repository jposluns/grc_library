# Session Handoff

**Purpose.** This file is the single resume point for a new Claude Code session. It is refreshed at every PR close-out so that opening a fresh session and continuing requires only one instruction from the maintainer (see "How to resume"). It exists because long sessions degrade (context dilution, lossy compaction, state drift, error compounding); a fresh session that rebuilds state from this file plus the durable repo artefacts is more reliable than a long one running on accumulated memory. The mechanisms and mitigations are written up generically in [`.working/session-length-considerations.md`](session-length-considerations.md). This file is maintainer working state, exempt from corpus audit gates per the `.working/` exemption.

This is an **as-of-last-refresh snapshot**, not a live-HEAD claim. Versions and counts drift forward as work advances; always verify against live files before relying on them.

## How to resume (the one command)

In a new session, the maintainer sends only: **`/resume`** (or "read `.working/session-handoff.md` and continue").

On `/resume`, the assistant:
1. Reads this file in full, including the **"Known environment behaviours"** section below.
2. Reads `.claude/CLAUDE.md` (the PRIMORDIAL RULE and project disciplines), the most recent few `CHANGELOG.md` entries, and **[`.working/third-party-issues.md`](third-party-issues.md)**.
3. Runs `git rev-parse --is-shallow-repository` (unshallow with `git fetch --unshallow` if `true`, **before any history-aware audit**), then `tools/run_all_audits.sh` to confirm the corpus is green, and `git log --oneline -5` to confirm HEAD.
4. Verifies the version/count snapshot below against live files.
4a. **Batches the unattended-run clarifications up front (maintainer-directed 2026-06-25)**: before resuming the queue, asks the maintainer (via `AskUserQuestion`, batched into as few rounds as possible) every decision that would let the session run unattended longer, pre-populating decisions for **all Priority 1 and Priority 2 backlog items** and **the next 10-plus planned PRs** (including any outside P1/P2, e.g. the FR-167 batches). Compute-don't-ask first (ask only genuine authorial decisions, not what the assistant can verify); record the answers in the next-actions queue so they survive a mid-session compaction; mark deferrals and route around them. See `/resume` command step 5 for the authoritative form.
5. **Runs a full corpus-wide `/validate` as the first substantive task**, the loop-break compensating control for whatever session-closing handoff PR last closed (which skips its trailing `/validate-pr` + `/retro`). **Sweep 53** ran at the 2026-06-26 resume from handoff #365, the control over the #362/#363/#364/#365 deltas; full A/B/C dispatch, **1 out-of-window note (B-1)** apply-time-verified and cross-referenced into DD-12 (a new CSF-1.1 carrier `RC.IM` at `compliance/procedure-capa.md:475`), 0 in-window, 0 audit-programme, no asserted-expectations contradiction. The next such sweep is **Sweep 54**, the control for THIS session-closing handoff PR **#369**, covering the **#366/#367/#368/#369 deltas** (D4 Version-Date co-bump gate in #366, §5 functional-category currency in #367, the CHANGELOG-hygiene pre-flight aid in #368, plus this handoff's `.working/`/version housekeeping). Routes any findings to the backlog. **Cross-check against the "Asserted expectations" section below** (a contradiction of an asserted-clean touched surface is a genuine miss, escalated). **Note for Sweep 54**: #366's corpus-document change is the audit-programme spec §6.1 (D4 delta-gate row + narrative) + the new `tools/check-date-cobump-on-pr.py` + its `DateCobumpOnPrTests`; #367's is the spec §5 functional-category list (gates 49-53 added + append-order note) + regenerated taxonomy/scorecard; #368 added the `tools/preflight-changelog.py` aid (no corpus doc); #369 is `.working/` + version-surface housekeeping; gate 35 covers four-surface gate parity (still 53 numbered gates; D4 is a delta gate, not a §6 gate), gate 39 the gate-count prose. The known out-of-window CSF-1.1-residue carriers (now THREE: `risk/standard-third-party-and-supply-chain-risk.md`:230 `ID.SC`; `operations/procedure-security-monitoring-and-alert-management.md`:333/336/337 `RS.RP`/`DE.DP`; and the Sweep-53-surfaced `compliance/procedure-capa.md`:475 `RC.IM`→`ID.IM`) remain expected, not new misses (DD-12 scope, all in DD-12's known-carriers list).
6. Continues from "Next actions" below.

## Next actions (the queue for the next session)

**This session (2026-06-26 continued, integrity-tooling: Sweep 53 + the §4.17/§4.15/§4.14 integrity items; attended-autonomous).** Resumed from handoff #365 via `/resume`; verified live state (HEAD `4376ec1`/#365, 53/53 green at `41f0dce`, versions consistent, clone unshallowed, pending-decisions empty, no drift). The resume clarification batch delivered this time: the maintainer chose the **integrity track then FR-167**, **autonomous green-CI=merge chaining while quality holds**, and **§5.3 → move BOTH registers to `.project-governance/`** (recorded for when §5.3 runs). Ran the mandatory **Sweep 53** FIRST (the #365 loop-break control over the #362/#363/#364/#365 deltas): full A/B/C dispatch, **1 out-of-window note (B-1, a new CSF-1.1 carrier `RC.IM` at `compliance/procedure-capa.md:475`)** apply-time-verified and cross-referenced into DD-12's known-carriers list (not hot-fixed), 0 in-window, 0 audit-programme, no asserted-expectations contradiction. Then shipped three integrity items: **#366** (delta gate **D4** per-PR Version-Date co-bump, [`tools/check-date-cobump-on-pr.py`](../tools/check-date-cobump-on-pr.py), closing §4.17; built as a PR-time delta gate after prototyping showed a HEAD-state gate-40 extension has a 78-doc historical FP surface, a design fork surfaced to and chosen by the maintainer); **#367** (§4.15 audit-programme spec §5 functional-category currency: gates 49-53 placed in their categories + the numeric-append-order decision recorded, closing the recurring "gates N absent from §5" soft spot); **#368** (§4.14 CHANGELOG-hygiene commit-gating pre-flight aid [`tools/preflight-changelog.py`](../tools/preflight-changelog.py), a standalone `&&`-chain helper since no pre-commit git hook fires here; it dogfooded itself on its own PR). Each non-handoff PR got a formal `/validate-pr` (#366 0, #367 0, #368 1 in-window note: the aid's fenced-block fidelity gap, fails-safe, addressed by a docstring limitation note) + `/retro`. Quality held throughout: #366's post-commit audit caught 6 consequential gates (parity-exclusion-set, sweep-cursor, artefact-regen, the "TODO"-token / §-ref narrative traps), all fixed pre-push; every CI run green first try. **Wound down on Quality > Speed** at this clean boundary (the maintainer's "§4.15 then §4.14" chain complete; heavy context after a corpus-wide sweep + 3 PRs). This session-closing handoff PR **#369** lands the batched #368 `/validate-pr` + `/retro` rows + the #368 docstring fix + the session-metrics row + this refresh; skips its own trailing `/validate-pr`+`/retro` (loop-break) and records its exemption row inline. **Sweep 54** is the next `/resume`'s control over the #366/#367/#368/#369 deltas.

**Next-session queue (after the mandatory Sweep 54 loop-break `/validate`; pending-decisions empty).** The integrity-tooling remainder, then FR-167. Next candidate PRs (from TODO): (1) **project-gov §5.3 deferred classifications** (M; **DECIDED this session by the maintainer**: move BOTH [`register-coverage-gaps.md`](../governance/register-coverage-gaps.md) and [`register-document-review-schedule.md`](../governance/register-document-review-schedule.md) to `.project-governance/`; a Phase-1-style migration: `git mv` + sever/redirect any citations + add the moved files to the explicit-allow-list content linters + regen artefacts + record the decision in [`design-decisions.md`](design-decisions.md)); (2) **gate-49 extension** (M; validate CSF/ISO codes in per-document reference tables, TODO P3) with the **scope finding this session**: it is entangled with the deferred L-effort DD-12 corpus-wide CSF-1.1 migration (a corpus-wide CSF-code gate fails on the existing 3 known carriers until DD-12 is done), so sequence scanner → DD-12 migration → wire-the-gate, best as its own focused session; then the maintainer-directed **FR-167 batch 5 (resilience, 22 docs)**. Confirm running order at resume. The §4.16 multi-session capability + §4.18 session-concurrency safety remain queued (maintainer-timed). Lower-priority integrity items remain in TODO P4 (§4.2/§4.3/§4.4/§4.6/§4.10 and the §4.8 retro-consolidation set, which now includes the #366 new-gate-authoring reflex and the #367 root-vs-detailed-lead rule-vs-practice reconciliation candidate).

**Prior session (2026-06-26 continued, integrity-tooling: Sweep 52 + directional-dependency gate 53; attended-autonomous).** Resumed from handoff #361; ran **Sweep 52** (clean) and shipped **#362** (gate 53 corpus-to-project directional-dependency, the §4 one-way-dependency backstop) then, on maintainer direction after a productivity-vs-QA token-split question, **#364** (the durable session-metrics convention + the gate-exempt [`session-metrics.md`](session-metrics.md) ledger). #363 and #365 were session-closing handoff PRs (the session continued past #363 on maintainer direction). Each non-handoff PR got a formal `/validate-pr` (0 findings) + `/retro`; quality held (local audit caught the gate-39/shall-near-uncertainty and D2 ledger-Version slips pre-push). **Sweep 53** at this session's resume was the loop-break control over the #362-#365 deltas.

**Prior session (2026-06-26 continued, integrity-tooling: Sweep 51 + scan-scope parity gate 52; attended-autonomous).** Resumed from handoff #358 via `/resume`; verified live state (HEAD `fe76ea4`/#358, 51/51 green at `c482bc3`, versions consistent, clone unshallowed, pending-decisions empty). Maintainer re-attached the CCM/AICM/NIST FR-167 reference binaries (set aside this session, not the integrity track). Batched the resume clarifications: maintainer chose the **integrity-tooling guard-rails track**, **green-CI=merge (standing)**, **chain while quality holds**, and **build the directory-scan-scope parity meta-check now (groundwork + gate)**. Ran the mandatory **Sweep 51** FIRST (the #358 loop-break control over the #357/#358 deltas): full A/B/C dispatch, **0 findings (clean)**, corpus near-identical to post-#357, no asserted-expectations contradiction. Then shipped the deferred scan-scope-parity meta-check as a clean groundwork-then-gate split: **#359** (groundwork: `AUDITED_DOMAIN_DIRS` single source of truth in `lint_common.py` + 11 explicit-allow-list content linters refactored to splat it, behaviour-preserving, scanned-path sets unchanged) and **#360** (the gate: `tools/lint-scan-scope-parity.py` (gate 52), forbidding any content linter from hardcoding the run, robust to all three declaration styles since it matches literals, with a documented 3-file `EXEMPT` set for the corpus-only-set divergent files (`lint-structure`, `build-taxonomy`, `detect-collection-candidates`); four-surface wiring + `ScanScopeParityTests`; separation-spec §7.4 narrative rewritten to the single-source-of-truth model). The §7.4 `lint-structure` classification was resolved on evidence (adding `.project-governance` to it fails with 6 corpus-index-membership "index miss" findings, so it legitimately scans the corpus-only set and is EXEMPT). Each non-handoff PR got a formal `/validate-pr` (#359: 1 note, resolved-by-construction in #360; #360: 0 findings) + `/retro`; quality held throughout (the gate caught 2 defects in itself at apply-time, both fixed before commit; D2 caught 2 unbumped `.working/`-ledger Versions pre-push). **Wound down on maintainer direction** at this clean complete-deliverable boundary (the next item, the §7.3 directional-dependency gate, is a discrete new-gate build best started on fresh context). This handoff PR **#361** lands the batched #360 `/validate-pr` (0 findings) + `/retro` rows + this refresh; skips its own trailing `/validate-pr`+`/retro` (loop-break) and records its exemption row inline. **Sweep 52** is the next `/resume`'s control over the #359/#360/#361 deltas.

**Prior session (2026-06-26 continued, integrity-tooling: Sweep 50 + gate 51; attended-autonomous).** Resumed from handoff #356 via `/resume`; verified live state (HEAD `655ceaa`/#356, 50/50 green at `d5395fc`, versions consistent, clone unshallowed, pending-decisions empty). Batched the resume clarifications: maintainer chose the **integrity-tooling plan (gate 51 next)**, **per-item review before merge** for the decided-content items (Phase D held), and **accept the §4.18 defaults** if/when built. Ran the mandatory deferred **Sweep 50** FIRST (the #354/#356 loop-break control over the accumulated #352/#353/#354/#355/#356 deltas): full A/B/C dispatch, **0 findings (clean)**, corpus byte-identical to the Sweep-48/49 state, no asserted-expectations contradiction. Shipped **#357**: **gate 51 (working-tree prose-hygiene)**, forbidding em/en-dashes in `.working/` prose while allowing them inside inline code spans and fenced blocks (ratcheting the #353 conformance so it cannot silently regress), `tools/lint-working-prose-hygiene.py` wired into all four surfaces + a `WorkingProseHygieneTests` regression class; apply-time correction (the pinned naive single-backtick code-span strip false-positived on the self-referential `session-handoff.md`:28 line, upgraded to the standard CommonMark code-span regex, and that design block rephrased). #357 got a formal `/validate-pr` (**0 findings**) + `/retro` (surfaced the recurring handoff-exemption-row-omission pattern, #342 then #356, with a proposed convention alignment). Also added #356's missing handoff-exemption row (gate 50 surfaced it once #357 made #356 non-highest). **Wound down on Quality > Speed** at this clean boundary: the next queued item (the directory-scan-scope parity meta-check) is a fragile meta-gate (content linters declare scan roots three ways, module `DEFAULT_SCAN_PATHS`, module `DOMAINS`, and function-local default lists like `lint-language.py`'s, the last invisible to a constant-name grep; a naive meta-check would reproduce the very multi-surface-incompleteness it targets), best built on fresh context with a shared introspectable scan-root declaration as groundwork. This handoff PR **#358** lands #357's `/validate-pr` (0 findings) + `/retro` rows + this refresh; skips its own trailing `/validate-pr`+`/retro` (loop-break) and records its own exemption row inline (closing the loop on the retro pattern). **Sweep 51** is the next `/resume`'s control over the #357/#358 deltas. **Open for the maintainer at the TODO §4.18 build** (unchanged): the staleness-window value (proposed 60 min), advisory-HOLD vs hard-block (proposed advisory), and whether to add a well-formedness gate.

**Prior session (2026-06-26, integrity-tooling phase; attended, then maintainer-asleep).** The maintainer's `/resume` task: assess [`improvement-log.md`](improvement-log.md) + [`hallucination-metrics.md`](hallucination-metrics.md), build the resulting guard rails, plus a full `.working/` em-dash conformance pass (maintainer chose "full conformance incl. separator"). The assessment's finding: worker-side hallucination is controlled (no escape since #167); the active class is orchestrator-side, and the highest-value mechanisms move catches pre-commit. Shipped **#352** (pack em-dash convention conformance: DONE header separator to `### PR #N:` colon, history-table zero-finding `Detail` placeholder to `none`, across the change-tracking rule + mirror + 2 SKILLs + 2 commands) and **#353** (bulk `.working/` apply: 74 files, ~1299 prose lines, code-span-aware transform preserving 11 intentional code-span literals; batched the Sweep 49 loop-break `/validate` (A/B/C 0 findings) + #352 `/validate-pr`+`/retro` rows + pack README `Date` align + TODO §4.17 queued). Each non-handoff PR got formal `/validate-pr` (0 findings) + `/retro`; quality held throughout (D2 caught 4 missing `.working/` version bumps pre-push; Subagent A 0 findings on both). This **handoff PR #354** lands #353's QA rows + this refresh + resets `overnight-pr.md` to stub; skips its own `/validate-pr`+`/retro` (loop-break); **Sweep 50** is the next `/resume`'s control over the #352/#353 deltas.

**The 20-PR plan (chat-surfaced 2026-06-26; #352/#353 done; resume here).** Phase A-C = the integrity-tooling guard rails the assessment recommended: **Tier-1 prose-hygiene gate (gate 51, SHIPPED this PR)**; directory-scan-scope parity meta-check (the §7.4 mechanical backstop); #356 directional-dependency gate (no corpus doc links into `.project-governance/`); #357 verify gate-50 QA-cadence/rotation coverage then build any §4.6/§4.10 residual; #358 gate-49 extension to per-document reference tables (TODO §88, pairs with DD-12); #359 discipline codification (candidate E pre-`AskUserQuestion` turn-scan + prose-fact full-file-grep line + §4.15 functional-category-index currency); #360 the two maintainer-directed regular wind-down disciplines (`.working/` standardization; hallucination-metrics refresh at every wind-down) + TODO §4.17 Version-Date co-bump check. Phase D = content backlog (FR-167 batches 5-11; FR-73/58/145; deepen-all baselines; H[critical] net-new docs), **maintainer-confirmation-gated** (semantic mappings + new prose, not gate-caught).

**Tier-1 prose-hygiene gate (gate 51): SHIPPED this PR.** `tools/lint-working-prose-hygiene.py` forbids em/en-dashes in `.working/` prose while allowing them inside inline code spans (the principled boundary the #353 bulk-apply established). It scans the `.working/` tree only (overriding the default `.working` exemption); the root `CHANGELOG.md` stays covered by the D3 PR-time dash gate and is not full-scanned (its historical entries carry legitimate en-dashes). Wired into all four surfaces (runner after gate 50, workflow, pre-commit, audit-programme spec §6 as row 51) with a `WorkingProseHygieneTests` regression class, and the `guardrail-review/SKILL.md` growth-narrative word-form bumped to fifty-one (the gate-39-blind carrier). Implementation note: the inline-code-span stripper uses the standard CommonMark code-span regex (a run of N backticks, the shortest content, then a closing run of N backticks) rather than the naive single-backtick strip the original design sketched, because a single-backtick strip mis-pairs a span whose content itself contains backticks (the self-referential design line was exactly such a case).

Work is merged through **#350**. The 2026-06-25/26 resume session (continued #6) resumed from handoff #345 via `/resume`, verified live state (HEAD `cd02c39`/#345, 50/50 green, versions consistent, clone unshallowed), and ran the mandatory **Sweep 48** (the #345 loop-break control over the #343/#344 deltas), **substantively clean** (Subagents B/C 0; Subagent A surfaced 2 trivial gate-exempt `.working/` cosmetic issues, a four-vs-five TODO-case miscount in the #343 detailed CHANGELOG entry and two em-dashes in the #344 guardrail-review record, both apply-time-verified and fixed; no asserted-expectations contradiction). The maintainer then directed a session of cleanup + operating-mode codification: **#346** (Sweep 48 close-out, the 2 fixes), **#347** (morning-processed the overnight run and reset [`overnight-pr.md`](overnight-pr.md) to **stub**, **overnight mode ENDED** on maintainer direction; routed the FR-58 3-label and deepen-all decisions to [`design-decisions.md`](design-decisions.md) + TODO dispositions), **#348** (refreshed [`hallucination-metrics.md`](hallucination-metrics.md) from its frozen #294 state to #347), **#349** (codified the new **attended-autonomous operating mode**: green-CI=merge standing authority + stricter-is-safer-always + the pending-decisions / 2-minute-timeout graceful-degradation mechanism, with [`pending-decisions.md`](pending-decisions.md) + a new `/resume` step), and **#350** (queued the multi-session research-brief capability as TODO §4.16, sharpened §4.14 to commit-gating). Each non-handoff PR (#346-#350) got a formal `/validate-pr` (all **0 findings** after apply-time fixes) + `/retro`; quality held (the QA layer caught every slip, including two mashed-table-row editing errors and a 3rd CHANGELOG link-coverage slip, all repaired pre-push). **Wound down on the SOP's large-high-stakes-series clause** (maintainer-confirmed 2026-06-26): FR-167 batch 5 (resilience, 22 docs) has non-gate-caught semantic mappings best done on a fresh context. This session-closing handoff PR (**#351**) lands the batched #350 `/validate-pr` (0 findings) + `/retro` rows and this refresh; skips its own trailing `/validate-pr`+`/retro` (loop-break), compensated by **Sweep 49** at the next `/resume`. The prior-session narrative (through #345) follows below.

**Maintainer overnight redirect (2026-06-25, governs the running order):** (1) finish the **§4.11 multi-session capability** (top priority, "usable tomorrow"); (2) then **FR-167**; (3) then **highest-priority items completable without further questions / write-permission prompts**. Recorded decision set (durable in [`overnight-pr.md`](overnight-pr.md)): deepen-ALL the thin-baseline cluster; FR-73 = standing independent AI Ethics Panel; FR-58 = the 3-label scheme (`library-internal`/`template`/`reference`); project-gov-separation Phase 1 PULLED AHEAD of remaining FR-167 batches; all net-new + structural tracks authorized to run unattended (green-CI=merge, stricter-is-safer+evidence on conflicts); FR-70 deferred.

**§4.11 is COMPLETE.** Parts 1-3 (runbook+brief #330, light SOP #332, pre-push-runner gate #333) + the Phase-1 migration (#336) shipped earlier; the **worker-provenance / bookkeeping-parity gate (gate 50)** shipped this session in **#343** (built per the pinned design; the worker-provenance check is the documented dormant stub awaiting the worker-marking convention + external-collaborator primitive). The first **`/guardrails` r1** (#344) then verified the 50-gate machinery coherent. No §4.11 work remains in the queue.

**Operating mode (changed this session):** **overnight mode is ENDED** ([`overnight-pr.md`](overnight-pr.md) is `Status: stub`); the standing mode is now **attended-autonomous** (green-CI = merge authority, stricter-is-safer-always, and the pending-decisions / 2-minute-timeout graceful-degradation mechanism), codified in [`.claude/CLAUDE.md`](../.claude/CLAUDE.md)'s `## Attended-autonomous operating mode` section (#349). [`pending-decisions.md`](pending-decisions.md) is currently `Status: empty`. The 2026-06-25 overnight decision set is fully routed (FR-58 3-label scheme and deepen-all to [`design-decisions.md`](design-decisions.md) + TODO; the rest were already durable). **FR-167 is the maintainer's directed next big activity.**

**Next-session queue (after the mandatory Sweep 49 loop-break `/validate`, the #351 control over the #346-#350 deltas; and after the new `/resume` step-7 pending-decisions check, currently empty):**
1. **FR-167 batch 5 (resilience, 22 docs)**, batch 4 (supply-chain, 18 rows) shipped #341. gate 49 gates ISO+NIST membership on new rows, CCM via gate 48; the worker-brief FR-167 override correctly directs CSF 2.0. **Apply-time reference availability**: the in-repo reference modules (`tools/ccm_aicm_reference.py`, `tools/nist_csf_reference.py`) are the authoritative validators gates 48/49 use and are PERMANENT, so batch authoring is gate-protected without the ephemeral scratchpad extracts; the maintainer-attached CCM/AICM/NIST binaries are belt-and-suspenders only (re-attach if a worker wants the source text, but not required for code validity). Smallest-first remaining: resilience 22 → operations 27 → compliance 30 → governance 31 → security 34 → ai 34 → privacy 42, then partial sections. **Apply-time pattern (from #341, reuse for each batch)**: fan out ~5 research workers over the net-new docs (verified-disjoint), each citing `path:line` + validating codes against the in-repo reference modules; orchestrator authors every cell + validates all codes against the modules BEFORE commit (gates 48/49 then confirm); trade columns = MAP for supplier/business-partner-lifecycle docs, N/A for IT/data-internal docs (per the matrix's N/A convention); EXCLUDE any framework-matrix/crosswalk doc (out of scope per the matrix's coverage-summary). **Note**: when each remaining domain's batch runs, also de-duplicate any cross-listed docs to their home domain (per the #313 path-domain-home convention).
2. **Decided content items**: FR-73 (ethics panel), FR-58 (the 3-label scheme, **decision now routed to [`design-decisions.md`](design-decisions.md) + the TODO P1 FR-58 disposition, #347**), FR-145 (keep both + crosswalk), the deepen-all thin baselines (FR-15/23/24/63/74/99/154, **the deepen-all decision is now routed to [`design-decisions.md`](design-decisions.md) + the TODO dispositions, #347**), the H[critical] net-new docs (FR-30/31/32/34/71/72; one PR each per the design-decisions overnight authorizations). These are the cleanest fit for the §4.16 multi-session research-worker fan-out (separate files, partitionable).
2a. **Multi-session research-brief capability (TODO §4.16, maintainer-timed "tomorrow")**: stand up the INPUT half of the §4.11 track, a `research/`-directory brief convention in `grc_library_scratch` (one brief per work unit, authored from the worker-brief template) + a `/subagent` external-worker entry command (read-only-on-main enforced by the worker account's GitHub permissions, NOT the prompt) + runbook codification. **Gating maintainer action**: provision the least-privilege worker account (read `grc_library` / write `grc_library_scratch` only). Design advised 2026-06-26; the maintainer will direct the timing. In-session `Agent` fan-out works today (the FR-167 batches use it).
3. **Project-gov separation follow-ups (NOT closed by Phase 1)**: the §7.3 directional-dependency gate (mechanical enforcement of §4: no corpus doc links into `.project-governance/`), the **§7.4 directory-scan-scope parity meta-check** (TODO P2, surfaced by the #338 `/retro`: assert every explicit-allow-list content linter includes every audited-not-exempt top-level dir, generalizing #338's two per-gate regression tests), and the §5.3 deferred classifications (`register-coverage-gaps.md`, `register-document-review-schedule.md`). All three pair naturally with the §4.6/§4.10 bookkeeping-parity gate family (gate 50, now shipped, is the first member). (The §6.3-vs-§7.3 audit-scope open decision is CLOSED by #338's spec §7.4.) The directional-dependency gate (TODO:61), the scan-scope meta-check (TODO:62), and the **gate-49 extension** (TODO:88, validate CSF/ISO codes in per-document reference tables, pairs with the DD-12 corpus-wide CSF-1.1 migration TODO:87) were all re-confirmed as real, still-queued gaps by `/guardrails` r1.
4. **Guardrails-routed low items (new this session, `/guardrails` r1)**: **§4.14** (a CHANGELOG-hygiene first-commit pre-flight aid covering BOTH root `CHANGELOG.md` AND the `.working/` detailed mirror, the un-gated surface the #344 em-dash slipped through; the #344 `/retro` sharpened the scope) and **§4.15** (bring the audit-programme spec's functional-category index current with gates 49/50, and the recorded keep-numeric-append-order gate-ordering decision). Both Low; pair with any future audit-programme-spec edit.

**Maintainer actions (non-blocking, the assistant cannot do them):**
- **Seed the scratch reference binaries durably** (or fix the git-proxy scratch-write block). The maintainer re-attached CCM v4.1 / AICM v1.1 / NIST CSWP.29 during this session, but only to the ephemeral session scratchpad (gone at session end); for FR-167 batch 4 in a fresh session, re-attach them at session start or seed the scratch repo. See [`third-party-issues.md`](third-party-issues.md) 2026-06-25.
- **Provision the external-worker account** (read `grc_library` / write `grc_library_scratch` only) to activate the separate-session worker primitive (the in-session subagent primitive already works).
- **Review the §6.3-vs-§7.3 spec-gap decision** (see Open decisions): confirm the audit-set scope and whether to amend spec §7.2/§7.3.

Re-read [`../TODO.md`](../TODO.md) live; it is the source of truth. Older effort-ordered backlog below (still valid where not superseded by the queue above):

- **M content clusters (dispositions recorded in TODO where noted):**
  - The **High-severity content items** FR-73 (AI ethics independence), FR-58 (inheritance vocabulary), FR-145 (two AI-security standards: keep both + crosswalk, verify overlap at action time). (FR-140 closed #321; DD-8 CPPA-as-live scrub closed #320.)
  - The **Medium content items** FR-15, FR-23, FR-24, FR-63, FR-74, FR-99, FR-154 (several are deliberately-thin baselines; calibrate before "fixing", FR-154 explicitly says so).
  - The **P4 process/meta items** (§4.2 effort-sizing convention, §4.3 standard-version-upgrade process, §4.4 pack language coverage, §4.6 QA-cadence mechanical gate, §4.10 TODO/DONE rotation gate, §4.8 retro-log open loops).
- **S remaining (mostly blocked/needs-scoping):** **FR-62** (AI jurisdiction annexes; likely needs scoping, defer if it balloons past S), **Sweep 3 follow-up** (manual term/identifier consistency pass), **B2** + **DD-10** (both egress-gated; attempt the fetch, defer if 403/blocked, NEVER fabricate). **R1** was closed won't-move (#307); **R2** (the 6-file citation-cluster relocation) is deferred with a convention-conflict note in TODO and **needs a maintainer decision** (active-corpus citers + two linters hardcode the paths; a `.working/` move conflicts with the no-`.working/`-link convention).
- **Larger tracks (deliberately scheduled, OUT of the routine batch):** FR-167 (comprehensive matrix; batch 1 Architecture #275, batch 2 Risk #313, batch 3 Dev-security #317 done; **next batch 4 = supply-chain, 18 docs**, proceed directly: the §4.6a control-code-validity gate is now DONE (gate 49 shipped #325, NIST membership closed #326), so the 18 new rows are gated on creation for ISO Annex A + NIST CSF 2.0 membership; CCM is covered by gate 48. Then resilience 22, operations 27, compliance 30, governance 31, security 34, ai 34, privacy 42), the High[critical] net-new privacy/governance docs (FR-30/31/32/34, FR-70/71/72/73), the L/XL items, and the **§4.11 multi-session / multi-worker orchestration codification track** (maintainer-scheduled; default after P1/P2; design record in [`design-decisions.md`](design-decisions.md)).

**The 2026-06-25 overnight redirect (above) SUPERSEDES the 2026-06-23 order**: multi-session capability FIRST (part 1 done #330; remaining = PR-3 SOP + gate family), then FR-167 (project-gov-sep Phase 1 pulled ahead), then the decided priorities. The older 2026-06-23 order was FR-167 → deferred-decision work → smallest-items-in-batches; maintainer direction supersedes at any time. DD-4/5 (#318), DD-8 (#320), DD-2/3/11 (#310) are done; DD-12 was broadened (#329) to all CSF-1.1 carriers and pairs with the queued gate-49-extension; remaining deferred-decision items live in TODO P2/P3.

**Pending maintainer action (not executable in the assistant's environment):** two stale remote branches are verified content-in-main and safe to delete, but the git proxy here rejects ref deletions and the GitHub MCP has no delete-branch tool: `claude/resume-review-h95prg` (`da8e051` recovered to main #316; `9e4fa87` superseded) and `update-contributors` (`AUTHORS.md` already byte-identical on main). Delete via the GitHub UI or `git push origin --delete` from an environment that accepts ref deletions.

**Worker-brief-template guard-rail candidates surfaced this session (queued, not yet applied):** (1) a target doc's OWN self-cited control codes may be mislabeled (verify against the reference, do not propagate); (2) AICM codes are not CCM v4.1; (3) after any per-document Version bump, regenerate taxonomy/portal/scorecard BEFORE the commit (#317/#318 `/retro`s).

## This session's work (2026-06-25): read before continuing

**Latest resume session (2026-06-25 continued #5, overnight authority, closed by handoff PR #345).** Resumed from handoff #342 via `/resume`. Verified live state matched the snapshot (HEAD `c63e68e`/#342, 49/49 green, versions consistent, no drift; clone unshallowed). The maintainer's clarification batch did not deliver (a transient permission-stream error) and the maintainer said "continue"; proceeded on the documented default running order (the pinned §4.11 gate first), reading the reference re-attachment as belt-and-suspenders per the handoff note. Ran the mandatory **Sweep 47** (the #342 loop-break control over the #340/#341 deltas): **clean** (A/B/C all 0; two sub-threshold notes). **Built the §4.11 worker-provenance / bookkeeping-parity gate (gate 50)** per the pinned design and shipped **#343** (`tools/lint-bookkeeping-parity.py` + four-surface wiring + 13-case regression class, 49→50; QA-cadence parity + TODO/DONE rotation + dormant worker stub; guardrail-SKILL word-form forty-nine→fifty, pack `1.49.12`; spec `1.16.6`). The gate's FIRST live run caught a real pre-existing gap (#342's missing handoff-exemption `/validate-pr` row), fixed in-PR; two TODO Check-2 false positives at first run were fixed IN THE GATE (bullet-start + code-span precision) per gate-discipline. Two maintainer interjections mid-session: (1) "review the 50 gates for coherence/order/overlap, check TODO for it", verified no prior TODO item, the mechanism is `/guardrails`; (2) "run `/guardrails` right away". Ran the first **`/guardrails` r1** (#344) over 50 gates + 10 rules + 15 skills: **0 overlap, 0 drift, machinery COHERENT**; 3 gap candidates dedup-confirmed (TODO 61/62/88), 2 new low items routed (§4.14 CHANGELOG-hygiene pre-flight aid, §4.15 functional-category-index currency + the keep-numeric-append-order gate-ordering decision); §4.13 rotated to DONE. Each non-handoff PR (#343, #344) got a formal `/validate-pr` (each 1 Low, fixed: #343 a CHANGELOG-vs-§4.13 schedule slip from the mid-PR "run now" redirect; #344 a detailed-mirror em-dash, gate-uncaught because D3 is root-only) + `/retro`. **Wound down on the SOP's large-series clause**: the §4.11 pin is DONE, the machinery is verified coherent, and FR-167 batch 5 (22-doc multi-worker) is best started fresh. Handoff PR **#345** lands the batched #344 QA rows + the em-dash fix + this refresh; skips its own trailing `/validate-pr`+`/retro` (loop-break), compensated by **Sweep 48** at the next `/resume`. The prior-session narrative (through #342) follows below.

**Prior resume session (2026-06-25 continued #4, overnight authority, closed by handoff PR #342).** Resumed from handoff #339 via `/resume`. Verified live state matched the snapshot (HEAD `203ca06`/#339, 49/49 green at a descendant of green-at-`bac5ddb`, versions consistent, no drift). Batched the resume clarifications: maintainer confirmed **standing unattended green-CI=merge authority** and chose **FR-167 batch 4 FIRST** (use the freshly re-attached references). Extracted the references to the scratchpad. Ran the mandatory loop-break **Sweep 46** (the #339 control over the #338 delta): Subagents B/C **0 findings** (the §7.4 completeness obligation verified to hold across all 11 explicit-allow-list content linters, so #338's fix left no parallel gap), Subagent A surfaced **one Low asserted-expectation inaccuracy**, the gate-10 README-cadence overstatement (gate 10 skips all READMEs by design, so the #339 handoff's asserted-clean claim at `session-handoff.md`:108 + the #338 test docstring over-precisely named the `.project-governance/` README as gate-10-cadence-scheduled). The load-bearing claim (gates 31/10 scan the dir) held; the substance was an over-precise parenthetical, not a coverage gap. Fixed across the carriers in close-out **#340**; its `/validate-pr` caught a **4th carrier** (the assertion-message string in the same test method, missed by the named-line fix) → fixed + a find-every-carrier `/retro` lesson. Then shipped **#341** (FR-167 batch 4, supply-chain): the matrix Supply chain section expanded **3→18 rows** via the research-assistant discipline (5 research workers produced evidence-backed candidate mappings; orchestrator validated every CCM/NIST code against the in-repo reference modules + authored every cell + dropped 4 weak codes + kept AICM out of the CCM column), moved the cross-listed Supplier-Security-and-Privacy-Assurance row to its home domain (#313 de-dup), excluded the supply-chain crosswalk as out-of-scope, matrix `1.3.1`→`1.4.0`; gates 48/49 validated all codes, `/validate-pr` **0 findings**, `/retro` noted the recurring CHANGELOG-hygiene-at-pre-push friction (dash + link, both gate-caught pre-push). **Wound down on Quality > Speed** at this clean green boundary: the §4.11 worker-provenance gate is a high-complexity multi-surface build whose design the prior session PINNED for fresh continuation. Handoff PR **#342** lands the batched #341 `/validate-pr` (0 findings) + `/retro` rows + this refresh; skips its own trailing `/validate-pr`+`/retro` (loop-break), compensated by **Sweep 47** at the next `/resume`. The prior-session narrative (through #339) follows below.

**Prior resume session (2026-06-25 continued #3, overnight authority, closed by handoff PR #339).** Resumed from handoff #337 via `/resume`. Verified live state matched the snapshot (HEAD `b3069d6`/#337, 49/49 green, versions consistent, no drift). Maintainer asked for FR-167 progress (advised: 3 of ~11 domain batches done, batch 4 = supply-chain, references re-attached this session) then chose to run the mandatory Sweep 45 first. Ran the loop-break **Sweep 45** (the #337 control over the #335/#336 deltas): Subagents A/B clean, **Subagent C surfaced a genuine miss**, the #336 Phase-1 migration enumerated the explicit-allow-list content-linter set incompletely, so gate 31 (date-staleness) and gate 10 (review-cadence) silently skipped `.project-governance/`, contradicting #337's asserted-clean §6.3 claim. Escalated as a genuine miss; maintainer decided to include gate 10 + amend the spec. Shipped **#338** (the fix: `.project-governance` → gate 31 `DEFAULT_SCAN_PATHS` + gate 10 `DOMAINS`, two scope-coverage regression tests, separation spec §7.4 enumerating the explicit-allow-list completeness obligation, taxonomy/scorecard regen; closes the §6.3-vs-§7.3 open decision). #338 got formal `/validate-pr` (0 findings) + `/retro` (pattern: multi-surface-incompleteness recurring on the content-linter scan-scope surface → proposed the directory-scan-scope parity meta-check, now TODO P2). Then **started the §4.11 worker-provenance / bookkeeping-parity gate build**: pinned the design + a historical-irregularity map into [`design-decisions.md`](design-decisions.md), but **wound down on Quality > Speed** before building, a late-session table-edit slip in the #338 `/retro` row (caught + repaired) plus heavy context were a degradation signal, and the maintainer chose wind-down. Handoff PR **#339** lands the batched #338 QA rows + the design capture + this refresh; skips its own trailing `/validate-pr`+`/retro` (loop-break), compensated by **Sweep 46** at the next `/resume`. The prior-session narrative (through #337) follows below.

**Prior resume session (2026-06-25 continued #2, overnight authority, closed by handoff PR #337).** Resumed from handoff #334 via `/resume`. Verified live state matched the snapshot (HEAD `9d36f41`/#334, 49/49 green, versions consistent, no drift). Batched the resume clarifications: the maintainer confirmed **standing unattended green-CI=merge authority + recorded running order**, and **re-attached the FR-167 references** (CCM v4.1 / AICM v1.1 / NIST CSWP.29; extracted to scratchpad, batch 4 unblocked). Ran the loop-break **Sweep 44** (the #334 control over the #332/#333 deltas), **clean**, no findings, no asserted-expectations contradiction. Shipped: **#335** (resume bookkeeping: added Philip Veilleux to `AUTHORS.md` at maintainer request, batched the clean Sweep 44 row; `/validate-pr` 0 findings + clean `/retro`) and **#336** (the major milestone: **project-governance separation Phase 1**, the §8.1 citation-verification cluster migration, `git mv` 6 artefacts to the new audited `.project-governance/` dir + README index, severed 7 corpus→project citations, re-pointed 2 linters + added `.project-governance` to 9 content linters, generator comment + artefact regen; `/validate-pr` 0 error/0 warning/1 cosmetic note). Phase 1 was executed via the research-assistant discipline (a read-only research subagent built the exhaustive evidence-backed migration map; the orchestrator verified every claim against live files before applying, with both runners guarding) and resolved the §6.3-vs-§7.3 audit-scope spec gap stricter+evidenced (surfaced to the maintainer in the #336 CHANGELOG-detailed design note + Open decisions). **Wound down deliberately** at this clean boundary on Quality > Speed: the major Phase-1 migration two prior sessions had deferred for freshness is shipped, and the remaining queue (§4.11 worker-provenance gate family, FR-167's 8 batches, the decided-content series) is a large series better served fresh. Handoff PR **#337** lands the batched #336 QA rows + this refresh; skips its own trailing `/validate-pr`+`/retro` (loop-break), compensated by **Sweep 45** at the next `/resume`. The prior-session narrative (through #334) follows below.

**Prior resume session (2026-06-25 continued, overnight authority, closed by handoff PR #334).** Resumed from handoff #331 via `/resume`. Ran the loop-break **Sweep 43** (the #331 control, covering the #329/#330 deltas), **clean**, no asserted-expectations contradiction, no findings routed. The maintainer (present during this run) confirmed **unattended, green-CI=merge, continue the overnight authority** (superseding an earlier "end + clean up" answer; `overnight-pr.md` stays IN-FLIGHT) and **references kept for apply-time only** (scratch binary seed deferred to maintainer/infra). Shipped: **#332** (§4.11 part 2: the partitionable-work light SOP, a §2 subsection of the `ai-assistant-workflow-disciplines` pack rule + byte-identical mirror + the `## Multi-session orchestration` CLAUDE.md section; pack `1.49.10`→`1.49.11`) and **#333** (§4.11 pre-push-runner gate, Option A: folded history-aware gates 40 + 31 into `tools/run-pr-time-checks.sh` alongside gate 45, so a single pre-push command is a complete commit-graph-aware guard protecting the pulled-ahead Phase-1 migration; corrected the runner-composition prose across CLAUDE.md / design-decisions / CHANGELOG). Each non-handoff PR got formal `/validate-pr` (both **0 findings**) + `/retro`; quality held throughout (Subagent A confirmed every load-bearing claim on #333). **Wound down deliberately** at this clean boundary, on Quality > Speed: the next item (project-gov Phase 1) is a migration-class, non-partitionable citation surgery best started in a fresh session rather than on this heavy context. Handoff PR **#334** lands the batched #333 QA rows + this refresh; skips its own trailing `/validate-pr`+`/retro` (loop-break), compensated by **Sweep 44** at the next `/resume`. The prior-session narrative (through #331) follows below.

**Prior resume session (2026-06-25, overnight, closed by handoff PR #331).** Resumed from handoff #327. Ran the loop-break **Sweep 42** (the #327 control; 1 in-window low fixed). The maintainer (pre-sleep) authorized an autonomous overnight run, redirected the running order to **multi-session capability first → FR-167 → priorities**, and iteratively designed the `grc_library_scratch` reference base. Shipped: **#329** (Sweep 42 close-out + resume bookkeeping; its `/validate-pr` caught + fixed an orchestrator from-memory CSF mis-attribution, `RS.RP`→`RS.MA`), **#330** (multi-session orchestration codification part 1: the runbook `.working/multi-session-orchestration.md` + worker-brief Model-B section + the stale-`PR.IP`→CSF-2.0 correction). Set up `grc_library_scratch`: root README, worker onboarding `CLAUDE.md`, and the trust-split `ref/` framework (`standards/` trusted vs `publications/` assess-first, `originals/` manifests), seeded via MCP because the git proxy blocks scratch writes (the ~2.84 MB text extracts are staged, pending the maintainer's binary re-upload). Queued: DD-12 broaden + gate-49-extension (#329) and the §4.12 publications-assessment process. **Wound down deliberately** at this clean boundary (a very long context; the §4.11 gate family + FR-167 are high-complexity and queued for fresh continuation; overnight-pr stays in-flight). Each non-handoff PR got formal `/validate-pr` + `/retro`; quality held (the #329 slip was caught by the formal QA and fixed; #330 clean). The prior-session narrative (2026-06-24/25 through #327) follows below.

**Prior resume session (2026-06-24/25), closed by handoff PR #327.** Resumed from handoff #322 and ran the loop-break **Sweep 41** (the #322 control), which found+fixed the `:102` CPPA-as-live residue in close-out **#323**. Then, on maintainer direction:
- **#324**, codified the **handoff asserted-expectations convention** (the rejected two-run-diff alternative; the asserted-clean + green-at-sha shape) across the handoff file, project CLAUDE.md, and `/resume`.
- **#325**, built **gate 49** (matrix control-code validity): a new `tools/lint-matrix-control-codes.py` validating the matrix's ISO 27001:2022 column (Annex A membership + clause format) and NIST CSF 2.0 well-formedness, wired into all four surfaces; gate count **48→49**. NIST category membership deferred to §4.6a. Building it surfaced (no-fabrication escalation) that the authoritative CSF 2.0 category set was unreachable via WebFetch (403); the maintainer supplied NIST CSWP.29.
- **#326**, closed **§4.6a**: extended gate 49 to validate NIST CSF 2.0 **category membership** (wired `tools/nist_csf_reference.py`, the 22-Category index from CSWP 29) and remapped the 17 CSF-1.1-era cells in the matrix NIST column (`ID.SC`×5→`GV.SC`, `ID.BE`→`GV.OC`, `PR.IP`×11→per-row `PR.PS`/`PR.DS`). Its `/validate-pr` returned 0 in-window findings + 2 out-of-window NOTEs (CSF-1.1 residue in sibling NIST-bearing tables; see Open decisions DD-12 scope).
- **#327** (this PR), session-closing handoff: lands the batched #326 `/validate-pr` + `/retro` rows and this refresh on `main`. Skips its own trailing `/validate-pr`+`/retro` (loop-break); compensating control is **Sweep 42** at the next `/resume`.

Each non-handoff PR (#323/#324/#325/#326) got a formal `/validate-pr` (all 0 in-window) + `/retro`; post-commit audits caught the expected bookkeeping consequences (spec Version bump on an incidental §6 edit at #325; artefact regen) and they were fixed before push, never reaching CI. Retro candidate **E** (pre-AskUserQuestion answered-check, flagged priority from the prior session) did not clearly recur this session but remains the queued mechanical-guard priority for the next integrity-tooling pass.

---

**Update (resume session, post-narrative):** after the two-part day below, the prior session continued to ship #313 (FR-167 batch 2) and #314 (project-governance separation spec, which closed R2), then paused without a session-closing handoff PR. This resume session ran Sweep 39 (the loop-break control covering the #313/#314 deltas) and shipped close-out #315. The narrative below covers the morning continuation through #310 only.

**Further continuation (post-#319 handoff):** after the #319 handoff PR, the session continued under maintainer direction and shipped **#320** (DD-8 CPPA-as-live scrub: the live-regime CPPA→PIPEDA sense corrected across the named docs, US-annex "California Privacy Protection Agency" sense preserved) and **#321** (FR-140: adopter starter-set divergence resolved, the 6-artefact Day-1 floor is now a strict subset of the 17-doc Tier 1 set and of the README Core reference set). Each got a formal `/validate-pr` (0 in-window findings) + `/retro`. Two recovered process slips on the #321 turn, re-asked an already-answered question, and a two-commit README split forced a gate-40 CalVer double-bump (`300`→`301` across the two PRs), are logged in [`improvement-log.md`](improvement-log.md) as retro candidates D (set-membership apply-time verification), E (pre-AskUserQuestion answered-check), F (single-commit for multi-edit versioned-doc changes). This handoff PR **#322** closes the continuation. **Note for the next session:** retro candidate E (pre-AskUserQuestion answered-check) recurred a *third* time during this very handoff-PR turn (a redundant re-ask of the already-answered FR-167 timing decision), after being logged on the #321 turn. Three occurrences in one session, the last after the lesson was written down, is the signal that candidate E needs the *mechanical* guard (scan the turn for an existing answer before issuing `AskUserQuestion`), not just a register note; treat it as the priority process fix in the next integrity-tooling pass.

A two-part day. **Overnight run** (autonomous, maintainer asleep): shipped #301 (Sweep 36 close-out, CCM/AICM citation-residual completion), #302 (gate-count word-form), #303 (Day-1-floor option A), #304 (R3 relocation), and prepared #305 (loop-break generalization into the pack) which could not be merged then because the GitHub MCP server disconnected after #304. **Morning continuation** (this session, resumed via `/resume` with the maintainer awake; the screenshot of the prior session surfaced the unmerged #305):

- Opened and merged **#305** (once the MCP reconnected), the loop-break-generalize pack-layer change, carrying the batched #304 QA rows.
- Ran **Sweep 37** (the #305 loop-break compensating control): corpus clean except the two pre-known operations-file CCM v4.0 domain-name residuals → fixed in **#306** (F1 cloud-baseline "Infrastructure Security"; F2 physical-security cited the wrong control I&S-03="Network Security", resolved by the maintainer to the DCS physical-access trio).
- **#307**: morning-processed the overnight file (reset to stub), closed relocation R1 won't-move.
- **#308 (S4)** + **#309 (S5)**: the gate-48 enhancement pair, S4 section-aware + cross-catalogue title check (catches the I&S-07 confusion); S5 bare-domain-code check (the family-list/crosswalk residual class), precision-first and FP-free (maintainer chose "build now, precision-first" after the FP surface was surfaced).
- **#310 (DD-2/3/11)**: risk-vocabulary harmonization to the canonical ERM scale across four risk-scoring docs (impact-5 → Catastrophic; likelihood Moderate → Medium; rating-top "Critical" preserved; the corpus-wide grep caught a 4th carrier beyond the 3 named).

**Quality held throughout**, every non-handoff PR got a formal `/validate-pr` (all 0 in-window findings after the apply-time fixes) + `/retro`; the post-commit audits caught the expected bookkeeping consequences (artefact regen, TODO sweep-cursor, link-coverage) and they were fixed before push, never reaching CI as a surprise.

## State snapshot (as of 2026-06-26, post-#368; session-closing handoff PR #369)

- **Branch**: substantive work merged through **#368**. This session (integrity-tooling phase) ran the mandatory **Sweep 53** (1 OOW note to DD-12) and shipped three integrity items: **#366** (delta gate **D4**, per-PR Version-Date co-bump, [`tools/check-date-cobump-on-pr.py`](../tools/check-date-cobump-on-pr.py), closing §4.17), **#367** (§4.15 audit-programme spec §5 functional-category currency, gates 49-53 + the numeric-append-order decision), **#368** (§4.14 CHANGELOG-hygiene commit-gating aid [`tools/preflight-changelog.py`](../tools/preflight-changelog.py)). This session-closing handoff PR (**#369**) lands #368's `/validate-pr` (1 note, addressed) + `/retro` rows + the #368 docstring limitation fix + this session's session-metrics row + this refresh, and records its own handoff-exemption row inline. **The standing mode is attended-autonomous; `overnight-pr.md` is `stub`.**
- **HEAD**: `main` at **#369** once this handoff PR merges (verify with `git log -1`). Immediately prior: #368 (§4.14 CHANGELOG-hygiene aid), #367 (§4.15 §5 currency), #366 (D4 Version-Date co-bump gate), #365 (prior session-closing handoff), #364 (durable session-metrics convention).
- **Versions** (post-#368): library `2026.06.347` (will tick to `.348` on the #369 handoff PR's version bump, verify against `README.md`), pack `1.49.17` (unchanged this session; no pack file touched), README `1.9.218` (→ `1.9.219` on #369; README `Date` `2026-06-26`), audit-programme spec `1.16.11` (bumped #366 `.10` then #367 `.11`), project-governance-separation spec `1.0.4` (unchanged this session). `.working/`-ledger Versions post-#368: validate-pr history `1.2.158`, validate-sweeps history `2.0.46`, improvement-log `1.0.122`, session-metrics `1.0.2`.
- **Mechanical baseline (green-at-sha)**: `tools/run_all_audits.sh` reported **53/53 green at `d6dc4fb`** (the #368 merge). The #369 handoff PR carries a gate-identical state (its only non-`.working/` changes are the `tools/preflight-changelog.py` docstring note plus README/CHANGELOG version surfaces, none gate-affecting), so the receiving `/resume` re-runs the audit on `main` (= the #369 merge) and confirms the same **53/53**; a deterministic mismatch is genuine close-vs-start signal, distinct from the non-deterministic `/validate` subagent layer. **Gate count is 53 numbered (no numbered gate added this session; D4 in #366 is a PR-only delta gate, not a §6 gate).**
- **Audit programme**: **53 numbered gates** + **4 PR-only delta gates (D1/D2/D3/D4)**; all passing on `main`. **One delta gate added this session**: **D4** (per-PR Version-Date co-bump, [`tools/check-date-cobump-on-pr.py`](../tools/check-date-cobump-on-pr.py)) in #366, requiring a versioned doc's `Date` to co-bump to the bump commit's UTC date when its `Version` bumps; it lives in spec §6.1 alongside D1/D2/D3, runs in `quality.yml` + `tools/run-pr-time-checks.sh`, is in `lint-audit-gate-parity.py`'s delta-gate exclusion set, and is NOT a §6 numbered gate (so gate 35 parity stays at 53). The spec §5 functional-category list is now current through gate 53 (#367). A new developer aid [`tools/preflight-changelog.py`](../tools/preflight-changelog.py) (#368) gates CHANGELOG-hygiene before the first commit; it is an aid, NOT a gate (absent from all gate surfaces). The pre-push runner (`tools/run-pr-time-checks.sh`) runs D1/D2/D3/D4 + the history-aware corpus gates 45/40/31. **`.project-governance/`** scanned by **11 explicit-allow-list content gates** (incl. gate 31 + gate 10 per §7.4). Gates 48 (CCM/AICM citation-accuracy), 49 (matrix control-code validity), 50 (bookkeeping-parity), 51 (working-tree prose-hygiene), 52 (directory-scan-scope parity), 53 (corpus-to-project directional-dependency). The audited-domain scan run has a **single source of truth** (`lint_common.AUDITED_DOMAIN_DIRS`); gate 52 forbids hardcoding it, gate 53 derives its scan set from it (minus `.project-governance`). Governance rules: **10**. Skills: **15** (6 paired). Slash commands: **8** (a `/subagent` command is QUEUED, not built, TODO §4.16). The **compliance matrix** ([`compliance/matrix-grc-compliance-alignment.md`](../compliance/matrix-grc-compliance-alignment.md), `1.4.0`) has comprehensive **Architecture/Risk/Dev-security/Supply-chain** sections (batches 1-4); **next batch 5 = resilience (22 docs: 19 net-new + 3 existing rows to re-audit; no resilience crosswalk doc to exclude; watch `procedure-security-incident-reporting-and-escalation` + `procedure-cross-domain-incident-coordination` for cross-section duplicates per the #313 lesson)**.
- **Last merged**: **#368** (§4.14 CHANGELOG-hygiene commit-gating aid `tools/preflight-changelog.py`; `.claude/`+`tools/`+`.working/`+version surfaces, no corpus-document change). Before it: #367 (§4.15 §5 functional-category currency, spec `1.16.11`), #366 (D4 Version-Date co-bump delta gate, closed §4.17, spec `1.16.10`, batched the Sweep 53 row + detail file), #365 (prior session-closing handoff), #364 (durable session-metrics convention). (#369 is this session-closing handoff PR.)
- **Scratch repo (`grc_library_scratch`)**: framework seeded (earlier session): root `README.md`, worker onboarding `CLAUDE.md`, the `ref/` trust-model framework. Reference binaries still NOT in the scratch repo (git-proxy blocks scratch writes). **The in-repo reference modules (`tools/ccm_aicm_reference.py`, `tools/nist_csf_reference.py`) that gates 48/49 use are the PERMANENT, authoritative validators**, so FR-167 batch 5+ code validity is gate-protected WITHOUT the scratchpad extracts; re-attaching the binaries is belt-and-suspenders for worker source-text reading only. **TODO §4.16** queues standing up the scratch `research/` brief channel + `/subagent` worker entry; gating action = the maintainer provisions the least-privilege worker account.
- **`/validate` cadence**: **Sweep 53** ran at THIS session's resume (the #365 loop-break control over the #362/#363/#364/#365 deltas; full A/B/C dispatch, **1 out-of-window note (B-1)** apply-time-verified and cross-referenced into DD-12 (a new CSF-1.1 carrier `RC.IM` at `compliance/procedure-capa.md:475`), 0 in-window, 0 audit-programme, no asserted-expectations contradiction; row + detail file [`2026-06-26-sweep53-iter1.md`](validate-sweeps/2026-06-26-sweep53-iter1.md)). **The next `/resume` runs Sweep 54 FIRST**, the loop-break control for this session-closing handoff PR #369, covering the **#366/#367/#368/#369 deltas**. **Note for Sweep 54**: #366 added delta gate D4 (linter + spec §6.1 row/narrative + `quality.yml`/runner wiring + parity-exclusion-set entry + `DateCobumpOnPrTests`); #367 brought spec §5 current with gates 49-53 + regenerated taxonomy/scorecard; #368 added the `tools/preflight-changelog.py` aid (no corpus doc); #369 is `.working/`+version housekeeping + the #368 docstring fix. So the corpus is near-identical to the post-#367 state. Sweep 54's job: confirm D4 four-surface coherence (gate 35 covers the numbered-gate parity mechanically; D4 is a delta gate), the §5/§6.1 narratives match the code, the version cascade, the preflight aid's docstring/CLAUDE.md claims, and the standing re-examination. The **three** known out-of-window CSF-1.1 carriers (DD-12 scope, all in DD-12's known-carriers list: `risk/...`:230 `ID.SC`; `operations/...`:333/336/337 `RS.RP`/`DE.DP`; `compliance/procedure-capa.md`:475 `RC.IM`) remain expected, not new misses. Gates 49-53 are now PRESENT in the spec §5 descriptive list (§4.15 closed #367), so that prior known low item no longer applies.

## Asserted expectations (session close-out convention)

**Purpose.** At session close, the closing session records here its *asserted-clean* claims, scoped to the surfaces it actually touched or verified this session (not a blanket corpus-wide clean bill). The next session's `/resume` runs its corpus-wide `/validate` (the loop-break compensating control) and cross-checks the findings against these claims: a finding that *contradicts* an asserted-clean touched surface is a genuine miss signal (the closing session's self-assessment was wrong), to be escalated rather than triaged as an ordinary out-of-window finding. A finding on a surface NOT in this list is an ordinary finding, handled by the normal in-window / out-of-window triage.

**Why this shape (and not a two-run diff).** A rejected alternative was: the closing session also runs a full `/validate`, passes the result forward, and the receiving session diffs the two. That is wasteful and noisy: the corpus is byte-identical across the boundary (same `main` commit), so there is no corpus delta for two runs to legitimately differ on, and `/validate`'s expensive layer is non-deterministic subagent fan-out, so two runs differ by LLM sampling variance, not corpus facts (a "discrepancy = major issue" rule would fire on noise). It also collides with the loop-break (a closing-session finding can only ride the prompt forward, the lossy state the handoff exists to avoid). The asserted-expectations shape gets the real signal (a contradiction of a claimed-clean touched surface) at the cost of a few lines, no second sweep. The only deterministic close-vs-start diff worth keeping is the mechanical one, captured by the green-at-`<sha>` line in the snapshot above.

**Scope discipline.** Assert clean ONLY what this session mechanically verified (a green gate) or covered with a formal `/validate-pr` or `/validate` subagent pass. Do not assert clean a domain the session did not touch; an unfounded clean claim turns an ordinary pre-existing finding into a spurious "major miss" at the next resume. This functions as a lightweight proxy for the `/validate-pr` the session-closing handoff PR skips (the loop-break): it records what the session's own QA actually covered, for the next `/validate` to test.

**This session's asserted expectations (2026-06-26 Sweep-53 + D4/§4.15/§4.14 integrity session; resumed from #365, re-closes at #369, refresh at close):**
- **Corpus is clean per Sweep 53** (full A/B/C corpus-wide `/validate`, the #365 loop-break control over the #362/#363/#364/#365 deltas): mechanical baseline 53/53 at `4376ec1`/#365. **1 out-of-window note (B-1)**, apply-time-verified and routed to DD-12 (a new CSF-1.1 carrier `RC.IM` at `compliance/procedure-capa.md:475`, cross-referenced into DD-12's known-carriers list, NOT hot-fixed); 0 in-window, 0 audit-programme. Subagent A 0 (gate-53 four-surface parity, #364 session-metrics convention coherent across its 3 surfaces, version cascade), Subagent B 1 (B-1; otherwise counts 53/10/15/8 + versions current, the other 2 known DD-12 carriers present-as-expected), Subagent C 0 (53-gate parity, `AUDITED_DOMAIN_DIRS` 12 members, both new regression classes present, 10-rule mirror). No asserted-expectations contradiction (B-1 on a surface NOT asserted clean). Row + detail file [`2026-06-26-sweep53-iter1.md`](validate-sweeps/2026-06-26-sweep53-iter1.md).
- **#366 is diff-clean per its formal `/validate-pr`** (**0 findings**): delta gate D4 multi-surface-complete and mutually consistent across the gate script, `quality.yml`, `run-pr-time-checks.sh`, spec §6.1 table+narrative, and `lint-audit-gate-parity.py`'s delta-gate exclusion set (gate 35 passes for 53 gates; D4 correctly NOT a §6 numbered gate); docstring/spec-narrative/code agree on scan set + bump-commit-date comparison + exempt set + delta-vs-HEAD rationale + same-day-FP-free claim; `DateCobumpOnPrTests` 3/3; **dogfooded** (D4 validated this PR's own spec Version+Date co-bump); §4.17→DONE rotation complete. Recorded in [`validate-pr/history.md`](validate-pr/history.md).
- **#367 is diff-clean per its formal `/validate-pr`** (**0 findings**): all 53 gates appear in spec §5 exactly once across the 7 categories (gates 49-53 placed 49→Content-drift, 50/52→Programme-integrity, 51→Language-and-style, 53→Reference-integrity), each new description cross-checked against its §6 row; the append-order note's example matches its category list; version cascade consistent (spec `1.16.11`, Library `2026.06.346`, README `1.9.217`, taxonomy+scorecard at `1.16.11`); §4.15→DONE rotation complete. One sub-threshold note (NOT routed): root CHANGELOG lead richer than the detailed-mirror lead (the established project pattern; reconciliation candidate logged to the §4.8 set).
- **#368 is diff-clean-with-one-addressed-note per its formal `/validate-pr`**: the CHANGELOG-hygiene aid [`tools/preflight-changelog.py`](../tools/preflight-changelog.py) is byte-faithful to the gates it mirrors (link-coverage `looks_like_file_reference` + the gate-51 code-span strip), the `added_lines` diff parser correct, exit codes match the docstring, correctly absent from all gate surfaces, §4.14→DONE rotation complete. **1 in-window note (addressed)**: the aid scans isolated added diff-lines with no fenced-code-block tracking, so a path-span inside a ` ``` ` block would over-block where the mirrored link-coverage gate skips it; fails-safe, latent (no CHANGELOG fenced blocks exist), addressed by a docstring limitation note in this #369 PR. Recorded in [`validate-pr/history.md`](validate-pr/history.md).
- **`.working/` prose is em/en-dash-free under gate 51** (the detailed CHANGELOG, DONE, handoff, validate-pr/history, validate-sweeps/history + the sweep-53 detail file, improvement-log, and session-metrics rows added this session are all dash-free).
- **Both local runners green on the #368 state**: `tools/run_all_audits.sh` **53/53** at `d6dc4fb` and `tools/run-pr-time-checks.sh` all-pass (D1/D2/D3/D4 + history-aware 45/40/31). Re-verified on the #369 handoff state before push.
- **Counts / versions**: **53** numbered gates (+**4** delta: D1/D2/D3/D4), 10 governance rules, 15 skills (6 paired), 8 slash commands; library `2026.06.347` (→`.348` on #369) / pack `1.49.17` / README `1.9.218` (→`1.9.219` on #369) / audit-spec `1.16.11` / separation spec `1.0.4` mutually consistent. One delta gate added this session (D4, #366); no numbered §6 gate added.
- **Known soft spots (NOT asserted clean)**: the **gate-49 extension** NOT built (entangled with the L-effort DD-12 corpus-wide CSF-1.1 migration); **§5.3** registers NOT yet moved (DECIDED: both move to `.project-governance/`, queued as the next PR); the **three** CSF-1.1 carriers outside the matrix (DD-12 scope, all in DD-12's known-carriers list) NOT migrated; the `preflight-changelog.py` fenced-block fidelity gap documented-not-fully-fixed (fails-safe); FR-167 batches 5-N + decided-content items not started; §4.16 multi-session + §4.18 session-concurrency capabilities queued not built; scratch `ref/` binaries not seeded. (The prior "gates 49-53 absent from spec §5" soft spot is now CLOSED by #367.)

**Prior session's asserted expectations (2026-06-26 Sweep-52 + gate-53 + durable-session-metrics session; resumed from #361, continued past #363, re-closed at #365):**
- **Corpus is clean per Sweep 52** (full A/B/C corpus-wide `/validate`, the #361 loop-break control over the #359/#360/#361 deltas, run at this session's resume): mechanical baseline 52/52 at `432d581`/#361. **All three subagents 0 findings** (Subagent A: recent-PR #359/#360/#361, gate-52 four-surface parity, #359 behaviour-preservation set-and-order-preserving, version cascade consistent; Subagent B: counts 52/10/15/8 + all versions current, CSF-1.1 residue bounded to the 2 known DD-12 carriers, no new carrier; Subagent C: gate-52 docstring/§6/§7.4 agreement, 12-member `AUDITED_DOMAIN_DIRS`, EXEMPT set accurate, 10-rule mirror). No asserted-expectations contradiction. Row in [`validate-sweeps/history.md`](validate-sweeps/history.md), no detail file.
- **#362 is diff-clean per its formal `/validate-pr`** (**0 findings**): gate 53 wired name-identically across all four surfaces (gate 35 confirms parity for 53 gates), the §6 inventory has exactly 53 rows with an accurate gate-53 narrative, the linter docstring / §6 / §7.3 all agree (deliverable-corpus scan set derived from `AUDITED_DOMAIN_DIRS` minus `.project-governance` plus root specs, pack subtree excluded, fenced/external skipped, target resolved before the `.project-governance` path-component test), the version cascade is consistent (audit-spec `1.16.9`, separation `1.0.4`, pack `1.49.17`, Library `2026.06.341`, README `1.9.212`), `DirectionalDependencyTests` 4/4 pass, TODO rotation complete (no directional-dependency item left, DONE carries the #362 entry, §4.15 updated to "gates 49 through 53"), CHANGELOG root↔detailed leads match with full link-coverage. One sub-threshold non-finding (NOT routed): the §6/CHANGELOG "root deliverable specifications" shorthand while `ROOT_DELIVERABLE_DOCS` also includes README/NOTICE, consistent with the linter docstring's "specifications and front matter". Recorded in [`validate-pr/history.md`](validate-pr/history.md).
- **Gate 53 is corpus-clean**: the deliverable corpus carries zero markdown links into `.project-governance/` (verified pre-build by grep, by the gate's own clean pass, and by `/validate-pr`); the only inbound links are the §4-permitted non-deliverable surfaces (CHANGELOG, pack, backlog, generated indexes), which are out of the gate's scope.
- **#364 is diff-clean per its formal `/validate-pr`** (**0 findings**): the durable-session-metrics convention's three prose surfaces (the gate-exempt ledger [`session-metrics.md`](session-metrics.md), the `/resume` Session-metrics note, the CLAUDE.md close-out-checklist bullet) are mutually consistent on all three convention points (orchestrator tokens `not instrumented` never fabricated; the row is written at the session-closing handoff PR; the metrics stay out of `CHANGELOG.md`); all new-prose markdown links resolve at correct relative depth; root↔detailed #364 leads match with full link-coverage; the inaugural ledger row is honestly marked partial (convention added mid-session); no corpus metadata changed so no artefact regen. Recorded in [`validate-pr/history.md`](validate-pr/history.md). The one pre-push slip (a bare `CHANGELOG.md` code span tripping link-coverage, reworded to "root changelog") was caught locally, never reached CI.
- **`.working/` prose is em/en-dash-free under gate 51** (the detailed CHANGELOG, DONE, handoff, validate-pr/history, validate-sweeps/history, improvement-log, and the new session-metrics rows added this session are all dash-free; gate 51 enforces this going forward).
- **Both local runners green on the #364 state**: `tools/run_all_audits.sh` **53/53** at `41f0dce` and `tools/run-pr-time-checks.sh` all-pass (D1/D2/D3 + history-aware 45/40/31). Re-verified on the #365 handoff state before push.
- **Counts / versions**: **53** numbered gates (+3 delta), 10 governance rules, 15 skills (6 paired), 8 slash commands; library `2026.06.343` (→`.344` on #365) / pack `1.49.17` / README `1.9.214` (→`1.9.215` on #365) / audit-spec `1.16.9` / separation spec `1.0.4` mutually consistent. One gate added this session (gate 53, in #362); #364 added no gate (a `.claude/`+`.working/` convention).
- **Known soft spots (NOT asserted clean)**: **§4.17 Version-Date co-bump check** NOT built; **§4.14 CHANGELOG-hygiene commit-gating pre-flight** NOT built; the **gate-49 extension** (validate CSF/ISO codes in per-document reference tables, pairs with DD-12) NOT built; the **§5.3 deferred classifications** (`register-coverage-gaps.md` / `register-document-review-schedule.md` move-or-stay) UNDECIDED, authorial/maintainer; gates 49/50/51/52/53 absent from the spec §5 descriptive list (§4.15); CSF-1.1 residue in NIST-bearing sibling tables outside the matrix (DD-12, TODO:87/88); FR-167 batches 5-N + decided-content items not started; the §4.16 multi-session capability queued not built; scratch `ref/` binaries not seeded.

**Prior session's asserted expectations (2026-06-26 Sweep-51 + scan-scope-parity gate-52 session):**
- **Corpus is clean per Sweep 51** (full A/B/C corpus-wide `/validate`, the #358 loop-break control over the #357/#358 deltas, run at this session's resume): mechanical baseline 51/51 at `c482bc3`/#357. **All three subagents 0 findings** (Subagent A: gate-51 four-surface parity, version surfaces consistent, #357/#358 internally consistent; Subagent B: counts 51/10/15/8 at sweep time + all versions current, CSF-1.1 residue bounded to the 2 known DD-12 carriers, no new carrier; Subagent C: gates 48/49/50/51 four-surface parity and §6 narratives match the linters, §7.4 met, 10-rule mirror). No asserted-expectations contradiction. Row in [`validate-sweeps/history.md`](validate-sweeps/history.md), no detail file.
- **#359 is diff-clean per its formal `/validate-pr`** (**1 note, resolved-by-construction in #360**): all 11 refactored content linters verified set-preserving (each pre/post scanned-path set reconstructed, missing and added both empty; lint-links keeps its `tools`/`docs` extras), `AUDITED_DOMAIN_DIRS` correct at 12 members, the "five gained the import" claim verified, version chain monotonic, no artefact regen needed. The 1 note (present-tense gate comments forward-referencing the not-yet-built gate) was resolved in #360 by construction (the gate now exists). Recorded in [`validate-pr/2026-06-26-PR-359.md`](validate-pr/2026-06-26-PR-359.md).
- **#360 is diff-clean per its formal `/validate-pr`** (**0 findings**): gate 52 wired name-identically across all four surfaces (gate 35 confirms parity for 52 gates), the §6 inventory has exactly 52 rows with an accurate gate-52 narrative, the linter docstring matches the code (literal-matching cluster detector, `DECLARE_THRESHOLD=6`, lint_common + self + 3-file EXEMPT allowances, 12-member integrity check), the gate's allowances exactly match disk reality (Subagent A independently replicated the literal count: only `lint_common.py`, the gate itself, and the 3 EXEMPT files are at/over threshold; the 11 #359-refactored linters splat and sit below), `ScanScopeParityTests` passes both directions, the version cascade is consistent (spec `1.16.8`, separation spec `1.0.3`, pack `1.49.16`, Library `2026.06.339`, README `1.9.210`) across README + both CHANGELOGs + pack history + taxonomy, generated artefacts in sync, and the §7.4 rewrite matches the gate. Subagent A on the diff + cross-reference check (gate 39 reports 52-gate consistency across 443 files; the only "fifty-one" residue is frozen `.working/` archives + historical pack-README rows). Recorded in [`validate-pr/history.md`](validate-pr/history.md).
- **`.working/` prose is em/en-dash-free under gate 51** (the detailed CHANGELOG, DONE, handoff, validate-pr/history, validate-sweeps/history, and improvement-log rows added this session are all dash-free; gate 51 enforces this going forward).
- **Both local runners green on the #360 state**: `tools/run_all_audits.sh` **52/52** at `b729918` and `tools/run-pr-time-checks.sh` all-pass (D1/D2/D3 + history-aware 45/40/31). Re-verified on the #361 handoff state before push.
- **Counts / versions**: **52** numbered gates (+3 delta), 10 governance rules, 15 skills (6 paired), 8 slash commands; library `2026.06.339` (→`.340` on #361) / pack `1.49.16` / README `1.9.210` (→`1.9.211` on #361) / spec `1.16.8` / separation spec `1.0.3` mutually consistent. One gate added this session (gate 52).
- **Known soft spots (NOT asserted clean)**: the **§7.3 directional-dependency gate** (no corpus doc links into `.project-governance/`) is NOT built (the queued next integrity-tooling item); §4.18 session-state-safety NOT built; gates 49/50/51/52 absent from the spec §5 descriptive list (§4.15); CSF-1.1 residue in NIST-bearing sibling tables outside the matrix (DD-12, TODO:87/88); FR-167 batches 5-N + decided-content items not started; the §4.16 multi-session capability queued not built; scratch `ref/` binaries not seeded.

**Prior session's asserted expectations (2026-06-26 Sweep-50 + gate-51 session):**
- **Corpus is clean per Sweep 50** (full A/B/C corpus-wide `/validate`, the #354/#356 loop-break control over the #352/#353/#354/#355/#356 deltas, run at that session's resume): mechanical baseline 50/50 at the #356 corpus state, then gate 51 added in #357 (51/51 at `c482bc3`). **All three subagents 0 findings** (Subagent A: #352-#356 internally consistent, em-dash conformance landed, version monotonicity clean; Subagent B: counts 50/10/15/8 at sweep time + all versions current, CSF-1.1 residue bounded to the 2 known DD-12 carriers; Subagent C: four-surface gate parity, gates 48/49/50 §6 narratives match the linters, §7.4 met, 10-rule mirror). No asserted-expectations contradiction. Row in [`validate-sweeps/history.md`](validate-sweeps/history.md), no detail file.
- **#357 is diff-clean per its formal `/validate-pr`** (**0 findings**): gate 51 wired name-identically across all four surfaces (gate 35 confirms parity for 51 gates), the §6 inventory has exactly 51 rows with an accurate gate-51 narrative, the linter docstring matches the code (CommonMark code-span strip then dash-flag, `.working/`-only scope), all version surfaces agree (spec `1.16.7`, pack `1.49.15`, Library `2026.06.336`, README `1.9.207`) across README + both CHANGELOGs + the pack version-history row + taxonomy, the generated artefacts are regenerated, the 4 regression cases are coherent both directions, and the Sweep-50 + #356-exemption rows are well-formed and dash-free. Subagent A on the diff + cross-reference check (gate 39 reports 51-gate consistency across 442 files, no broken links, no stale `1.16.6` citation). Recorded in [`validate-pr/history.md`](validate-pr/history.md).
- **`.working/` prose is em/en-dash-free under the new gate 51** (90 files, 0 prose dashes): the detailed CHANGELOG, DONE, handoff, validate-pr/history, validate-sweeps/history, and improvement-log rows added this session are all dash-free; gate 51 enforces this going forward (em/en-dashes allowed only inside inline code spans and fenced blocks).
- **Both local runners green on the #357 state**: `tools/run_all_audits.sh` **51/51** at `c482bc3` and `tools/run-pr-time-checks.sh` all-pass (D1/D2/D3 + history-aware 45/40/31). Re-verified on the #358 handoff state before push.
- **Counts / versions**: **51** numbered gates (+3 delta), 10 governance rules, 15 skills (6 paired), 8 slash commands; library `2026.06.336` (→`.337` on #358) / pack `1.49.15` / README `1.9.207` (→`1.9.208` on #358) / spec `1.16.7` mutually consistent. One gate added this session (gate 51).
- **Known soft spots (NOT asserted clean)**: the **directory-scan-scope parity meta-check** (TODO P2) is NOT built, deferred as a fragile meta-gate (content linters declare scan roots three ways including function-local lists invisible to a constant grep; needs a shared introspectable scan-root declaration as groundwork); §4.18 session-state-safety NOT built; gates 49/50/51 absent from the spec §5 descriptive list (§4.15, now covering all three); CSF-1.1 residue in NIST-bearing sibling tables outside the matrix (DD-12, TODO:87/88); FR-167 batches 5-N + decided-content items not started; the §4.16 multi-session capability queued not built; scratch `ref/` binaries not seeded.

**Prior session's asserted expectations (2026-06-26 integrity-tooling session):**
- **Corpus is clean per Sweep 49** (full A/B/C corpus-wide `/validate`, the #351 loop-break control over the #346-#350 deltas, run at this session's resume): mechanical baseline 50/50 at `a27fc3e`/#351. **All three subagents 0 findings** (Subagent A: #346-#350 internally consistent and dash-free; Subagent B: counts 50/10/15/8 + all versions current, CSF-1.1 residue bounded to the known carriers; Subagent C: 50-gate four-surface parity, gates 48/49/50 §6 narratives match the linters, §7.4 met, 10-rule mirror). No asserted-expectations contradiction. Recorded as a history row in [`validate-sweeps/history.md`](validate-sweeps/history.md) (no detail file, zero-actionable convention).
- **#352 is diff-clean per its formal `/validate-pr`** (0 in-window; 1 within-gate-31-tolerance note, the pack README `Date` staying 2026-06-25 while its `Version` bumped, **fixed in #353**): the DONE/FR header separator migrated to `### PR #N:` colon form consistently in BOTH change-tracking copies (gate 37 byte-identical), the Detail placeholder `none` consistent across the 2 SKILLs + 2 commands, version bumps correct, root + detailed entries dash-free with full link-coverage, corpus-wide grep confirmed zero remaining `### PR #N` em-dash templates outside `.working/`. Subagent A on the diff + cross-reference check; recorded in [`validate-pr/history.md`](validate-pr/history.md).
- **#353 is diff-clean per its formal `/validate-pr`** (**0 findings**): the bulk `.working/` em-dash conformance apply (74 files, ~1299 prose lines) verified clean, all DONE/FR headers in `### PR #N:` colon form with no comma-corruption, history tables pipe-balanced with zero-finding `Detail` cells reading `none`, the Sweep 49 / #352-QA bookkeeping rows mutually consistent, all 10 versioned surfaces bumped, the pack README `Date` corrected to 2026-06-26. Subagent A on the diff + cross-reference check.
- **`.working/` prose is em/en-dash-free EXCEPT the documented code-span literals**: the #353 bulk apply leaves exactly **11 residual em/en-dashes, all inside inline code spans as designed** (regex character classes, frozen historical quotes, the house-style rule's glyph examples, the SARIF format literal); these are content, not prose violations, verified at #353 `/validate-pr` apply-time. The pack-layer conventions (DONE/FR header separator `### PR #N:` colon form, Detail placeholder `none`) and the change-tracking rule's template now match.
- **Both local runners green on the #353 state**: `tools/run_all_audits.sh` **50/50** and `tools/run-pr-time-checks.sh` all-pass (D1/D2/D3 + history-aware 45/40/31). Re-verified on the #354 handoff state before push.
- **Counts / versions**: **50** numbered gates (+3 delta), 10 governance rules, 15 skills (6 paired), 8 slash commands; library `2026.06.332` (→`.333` on #354) / pack `1.49.14` / README `1.9.203` (→`1.9.204` on #354) / spec `1.16.6` mutually consistent (gate 39 + gate 35 green); README `Date` `2026-06-26`.
- **Audit-programme integrity**: 50-gate four-surface parity, 10 governance-rule mirror copies byte-identical, taxonomy/portal/scorecard in sync (50/50 green at `70b93a5`/#353). **No gate added this session** (the Tier-1 prose-hygiene gate was built+validated then reverted; see soft spots).
- **Known soft spots (NOT asserted clean, untouched or only-noted this session)**: CSF-1.1 residue survives in NIST-bearing sibling tables OUTSIDE the matrix (`risk/standard-third-party-and-supply-chain-risk.md`:230 `ID.SC`; `operations/procedure-security-monitoring-and-alert-management.md`:333/336/337 `RS.RP`/`DE.DP`); DD-12 (TODO:87) + the gate-49 extension (TODO:88) are queued, so treat any CSF-1.1-code finding outside the matrix as expected, not a miss. **Gates 49/50 are absent from the audit-programme spec's §5 descriptive functional-category list** (queued as §4.15, low; NOT a defect, §5 is "descriptive, not prescriptive" and not gate-checked). **The Tier-1 `.working/` prose-hygiene gate (gate 51 in the 20-PR plan) was built and validated this session but reverted before this handoff** (only 2 of 4 surfaces were wired; a half-wired gate fails gate-35 parity); its design is captured in Next actions for a clean rebuild, and gate count stays 50. **FR-167 batches 5-N (resilience onward) and the decided-content items NOT started**; the matrix domain sections other than Architecture/Risk/Dev-security/Supply-chain are NOT yet comprehensive. **The multi-session §4.16 capability (the scratch `research/` brief channel + `/subagent` command) is QUEUED, not built.** **Scratch `ref/` reference binaries not seeded** (git-proxy blocks scratch writes).

## Open decisions awaiting maintainer

- **§6.3-vs-§7.3 audit-scope spec gap**: **CLOSED in #338.** Sweep 45 found that #336's "added to all 9 explicit-list content linters" enumeration was incomplete: gate 31 (date-staleness) and gate 10 (review-cadence), two more explicit-allow-list content gates, silently skipped the dir (gate 31 a clear §6.3 violation since §6.3 names "date currency"). #338 added `.project-governance` to both (with a scope-coverage regression test each) and amended the spec with **§7.4**, which enumerates the explicit-allow-list content-linter completeness obligation (the maintainer chose, 2026-06-25, to include gate 10 and to amend the spec). The maintainer-decided spec amendment closes the decision; the **directory-scan-scope parity meta-check** (TODO P2) is the queued mechanical backstop that would have caught this class.
- **R2 relocation**: CLOSED by principle in #314 (the project-governance separation spec classifies the 6-file citation-verification cluster as project governance, so it migrates in Phase 1). The Phase-1 migration, the queued directional-dependency gate (§7.3), and the 2 deferred §5.3 classifications are now forward-looking TODO items, not open decisions awaiting the maintainer.
- **FR-167 mapping gate (asked 2026-06-24)**: RESOLVED, built as gate 49 (#325 ISO + NIST well-formedness; #326 NIST category membership + 17-cell remap closing §4.6a). The control-code-validity gate catches hallucinated/mistyped codes; semantic mapping correctness remains the orchestrator's apply-time job. No longer awaiting.
- **DD-12 scope**: RESOLVED + APPLIED (#329, maintainer-approved "broaden + extend gate 49"). DD-12 in TODO is broadened from `PR.IP`-only to all CSF-1.1 carriers (`PR.IP`/`ID.SC`/`ID.BE`/`RS.RP`/`DE.DP`/`PR.AC`/`PR.PT`/...; `RS.RP`→mainly `RS.MA`), and a **gate-49-extension** TODO item is queued (validate framework codes in per-document reference tables, not only the central matrix; build the scanner first, then gate the migration). Both are now forward-looking TODO items, not open decisions. The migration itself (corpus-wide, non-partitionable) is queued.
- **Publications-assessment process (§4.12, surfaced + queued 2026-06-25)**: a maintainer-directed process to assess `ref/publications/` for useful info AND detect poisoning/false info before it informs corpus work, queued as TODO §4.12 (not yet built). Forward-looking, not an open decision.
- **Maintainer actions still pending**: (a) **seed the scratch reference binaries** durably (git-proxy blocks scratch writes), belt-and-suspenders ONLY: the in-repo reference modules gates 48/49 use are the authoritative validators, so FR-167 batch 5+ code validity is gate-protected without them; re-attach only if a worker wants the source text; (b) **provision the least-privilege external-worker account** (read `grc_library` / write `grc_library_scratch` only), this is the gating action for both the separate-session worker primitive (§4.11) and the queued **§4.16** multi-session research-brief / `/subagent` capability. Neither blocks the next session's queue (in-session `Agent` fan-out works for FR-167 today).
- The larger-track decisions (FR-167 batches 4..N, the High[critical] net-new docs, the L/XL items) remain queued and scheduled deliberately.

## Trust-recovery state

The trust-recovery suite (`/full-qa` + `/fitness` r2) ran and the maintainer **signed off** (2026-06-22). Codification COMPLETE. Not re-triggered since; quality held throughout the 2026-06-24 overnight run, morning continuation, and the resume session (Sweep 38 + close-out #312, both warnings fixed first try, post-commit + pre-push green).

## Known environment behaviours (read before assuming working-tree state)

- **The stop-hook auto-commits AND pushes uncommitted changes on turn-end.** The working tree is auto-persisted to the feature branch; verify `git log` rather than assuming edits are held locally.
- **The squash-merge commit on `main` shows locally as `E noreply@github.com`** (GitHub's own merge commit; Verified in the GitHub UI, not locally verifiable). After syncing the branch to `main` the tip equals `origin/main`; **do NOT `git commit --amend`** it (that rewrites merged `main` history). Set `git config user.email noreply@anthropic.com && git config user.name Claude` so your own feature-branch commits are attributed correctly.
- **A squash-merge makes the local feature branch diverge from `main`** (the squash commit is new; the local pre-squash commit is its content but a different SHA), so `git merge --ff-only origin/main` fails after a merge; use `git reset --hard origin/main` to re-sync the feature branch (the local commit's content is in the squash, preserved in reflog).
- **The GitHub MCP server can disconnect mid-session** (it did 2026-06-24 after #304, blocking #305's open/merge). `git push` over HTTPS keeps working; only the MCP-routed GitHub-API operations are affected. If `mcp__github__*` tools vanish: keep committing and pushing via `git`, record the pending-merge state in the handoff, and resume the PR lifecycle when MCP returns. Do NOT merge by any non-MCP path. Full writeup: [`third-party-issues.md`](third-party-issues.md).
- **The commit-signing server can 503**, distinguish from a real defect (gates 31/40 pass on the real corpus while gate 36's failures are `git commit` subprocess errors); re-run after a pause. Full writeup: [`third-party-issues.md`](third-party-issues.md).
- **The clone may start shallow**; `git fetch --unshallow` before any history-aware audit (gates 31/40).
- **The `Bash` tool's working directory can reset to a non-repo directory between calls** (observed twice this session after long async-subagent gaps: a bare `git ...` failed with "not a git repository"). Prefix git-command sequences with `cd /home/user/grc_library &&` rather than assuming the cwd persisted.
- **After a post-merge sync you are ON `main`; `git checkout -B <feature-branch>` BEFORE building the next PR.** The post-merge sync (`git checkout main && git pull origin main`) leaves you on `main`. If you then build and `git commit` the next PR without switching branches, you commit to local `main` (which the stop-hook flags as an unverified commit ahead of `origin/main`), and a subsequent `git push -u origin <feature-branch>` pushes the STALE feature-branch ref (still at the prior PR's pre-squash commit), so the PR opens with an empty/wrong diff. This happened on the #361 handoff (the commit landed on local `main` as `2b589cd`; recovery was `git checkout -B <feature> <commit>` to repoint the feature branch, `git branch -f main origin/main` to undo the local-main commit, then a force-push of the feature branch). Recovery is clean and on-branch (no `main` push, no shared-history rewrite), but the fix is cheaper than the catch: switch to the feature branch immediately after the sync.
- **Operating mode is now attended-autonomous, not overnight** (set 2026-06-26, codified in [`.claude/CLAUDE.md`](../.claude/CLAUDE.md) #349): green CI = standing merge authority (merge green PRs and proceed without per-merge approval), stricter-is-safer always, and the pending-decisions / 2-minute-timeout graceful-degradation mechanism ([`pending-decisions.md`](pending-decisions.md), currently empty). `overnight-pr.md` is `stub` (overnight mode is OFF); do not treat it as in-flight. The `/resume` command now has a step 7 that checks `pending-decisions.md` before the queue.

## Standing disciplines (do not drift from these)

- **PRIMORDIAL RULE**: Quality > Speed > Cost; integrity absolute. Emit the integrity-check line at task start, before commit, before completion claims, and at tension points.
- **Post-commit audit**: after every commit, run `tools/run_all_audits.sh` standalone before pushing; never chain commit and push. Before push, also `tools/run-pr-time-checks.sh`.
- **`lint-language` pre-flight on new pack prose** before the first commit (catches em-dashes / British `-ise`). (Relevant for DD-4/5's `go.md` rewrite.)
- **Pre-commit dash-grep on new root `CHANGELOG.md` lines** (`grep -nP "\xe2\x80\x93|\xe2\x80\x94"`): the D3 dash gate is PR-time only, so a late-assembled CHANGELOG entry can slip an em-dash that surfaces at pre-push; grep the added lines before the first commit.
- **Grep-after-wiring / convention change**: after editing a restated-across-surfaces value, `grep` the OLD form across the full changed file AND every sibling surface; zero hits before commit.
- **Corpus-wide-grep / special-case-the-edge / find-every-carrier**: a mechanical bulk edit (a rename, a relabel, a title alignment) must verify EVERY adjacent field, special-case the known edge cases (e.g. preserve rating "Critical" while fixing impact-5; the DD-2/3/11 grep found a 4th carrier beyond the 3 named), and search corpus-wide for the OLD form, not apply one convenient rule uniformly.
- **Sweep close-out bookkeeping triplet**: a `/validate` close-out PR regenerates the generated artefacts (after per-doc version bumps), advances the TODO sweep cursor (gate 45 + the gate-36 TodoStaleness regression both enforce it), and bumps `validate-sweeps/history.md`'s own Version. The post-commit audit catches all three; doing them pre-commit avoids the fail-then-fix loop.
- **60-second** paired fallback timer after every `subscribe_pr_activity`.
- **Per-PR QA**: formal `/validate-pr` (Subagent A) + `/retro` after every merge; no skip, no abbreviation. **One standing exception**: the session-closing handoff PR skips both (loop-break); the compensating control is the corpus-wide `/validate` that `/resume` runs first. (This exception is now generalized into the pack layer, #305.)
- **PR close-out checklist** (the degradation guard): prior PR's `/validate-pr` row AND `/retro` row both batched in; closed TODO items rotated to DONE (every path-shaped code span in a CHANGELOG entry linked, changed or merely mentioned); stale-count check if an enumerated collection changed; session-handoff.md refreshed.
- **Self-assess for degradation / session-continuation SOP**: continue while quality holds; wind down on actual drift/hallucination/mistakes (not caught by the QA layer), OR when a very large series remains and we want no mid-operation interruption. Steady-state misses the per-PR `/validate-pr` catches are NOT drift. (This session wound down here, at a clean boundary after a long, productive run, with the large M-cluster series queued for a fresh session.)
- **Compute-don't-ask**: before surfacing a question, apply a "can I compute/verify this myself?" gate; surface only the result or a decision that genuinely needs maintainer judgement. (Counterpart: surface a genuine design tradeoff before building, as S5's FP surface was surfaced with named options rather than guessed.)

## Refresh discipline

This file is refreshed at every PR close-out (post-merge), as part of the recursion-avoidance batch that carries the validate-pr/retro rows into the next PR.
