# /validate-pr Record: PR #218

**Date:** 2026-06-22\
**PR:** #218 ([`Closes FR-46 DPO consolidation`](https://github.com/jposluns/grc_library/pull/218))\
**Merge commit:** `495d6b0`\
**Touched files:** 84\
**Findings:** 2 in-window, 0 out-of-window in scope (1 out-of-scope observation surfaced)\
**Hot-fix PR:** none (per recursion-avoidance rule, fixes bundled into the next substantive PR)

## Trigger and state snapshot

- PR #218 closed FR-46 DPO consolidation (medium, P1.5) by flipping the canonical privacy-lead role from "Chief Privacy Officer" to "Data Protection Officer". Reverses the canonical direction set by PR #210 (Privacy Officer → Chief Privacy Officer rename, merged) and PR #217 (DPO → Chief Privacy Officer rename, closed unmerged after maintainer redirect).
- Mechanical baseline post-merge: `tools/run_all_audits.sh` exits 0 on all 46 gates.
- 84 files touched: 3 canonical surfaces (`governance/register-role-authority.md`, `governance/register-glossary.md`, `privacy/README.md`), 73 corpus-prose-renamed files (across privacy, supply-chain, AI, security, governance, compliance, resilience, risk, dev-security domains plus NOTICE.md and `tools/build-portal.py`), 3 regenerated artefacts (`taxonomy.yml`, `docs/portal.md`, `docs/maturity-scorecard.md`), and 5 plumbing files (CHANGELOG root + detailed mirror, DONE, TODO, README).

## Subagent A return

Subagent A inspected 84 touched files for the 8 failure-mode classes (stale prose references, mis-attributed citations, multi-surface incompleteness, inferred-as-verified assertions, per-document version-bump omissions, generated-artefact lag, stale Python docstrings, cross-document term drift). Two in-window findings:

### Finding 1: `tools/build-portal.py:95` synonym-collapse double-substitution

- **ruleId:** SYNONYM-COLLAPSE-DOUBLE
- **level:** error
- **location:** [`tools/build-portal.py:95`](../../tools/build-portal.py)
- **evidence:** `"The Data Protection Officer or Data Protection Officer needs the privacy programme charter, policy, procedures, jurisdiction annexes, and the cross-border transfer register."`
- **impact:** The rename script's synonym-collapse pre-cleanup list did not include the pattern `"Chief Privacy Officer or Data Protection Officer"` (the script had `"Chief Privacy Officer (or Data Protection Officer)"` with parentheses, `"Chief Privacy Officer / DPO"`, `"Chief Privacy Officer or DPO"`, and `"Chief Privacy Officer or domain DPO"`; the `or`-without-parens-without-`DPO` pattern fell through to the bulk replace). The bulk replace substituted "Chief Privacy Officer" with "Data Protection Officer", producing the double "Data Protection Officer or Data Protection Officer". Propagates to the generated [`docs/portal.md:411`](../../docs/portal.md), which is the adopter-facing portal, adopters now read nonsense in the Privacy audience guidance.
- **recommendation:** Edit [`tools/build-portal.py`](../../tools/build-portal.py):95 to collapse to "The Data Protection Officer needs the privacy programme charter, policy, procedures, jurisdiction annexes, and the cross-border transfer register." Then re-run `python3 tools/build-portal.py` to regenerate [`docs/portal.md`](../../docs/portal.md) (line 411 will refresh from this source).
- **classification:** in-window (PR #218 introduced the double-substitution via the rename script)

### Finding 2: `risk/register-key-risk-indicators.md:142` stale "CPO" acronym

- **ruleId:** STALE-REF (also MULTI-SURFACE-INCOMPLETENESS)
- **level:** error
- **location:** [`risk/register-key-risk-indicators.md:142`](../../risk/register-key-risk-indicators.md)
- **evidence:** `| KRI Dashboard | Monthly | Chief Risk Officer; CISO; CPO |`
- **impact:** PR #218 renamed the KRI Owner column at lines 66-70 (5 rows: "Chief Privacy Officer" → "Data Protection Officer"), but the script's targets did not include the bare acronym "CPO", line 142's reporting-cadence audience cell still says "CPO". Same document, in-scope file; post-PR #218 the canonical role label is "Data Protection Officer" and the canonical acronym is "DPO" per [`governance/register-glossary.md:106`](../../governance/register-glossary.md) and [`governance/register-role-authority.md:39`](../../governance/register-role-authority.md). "CPO" outside the three sanctioned canonical surfaces is a stale adopter-variant reference. Per-doc Version was patch-bumped to 1.0.1 and Date to 2026-06-22 in PR #218, so a reader pulling at 1.0.1 sees an inconsistent acronym between the KRI rows (lines 66-70, "Data Protection Officer") and the reporting-cadence table (line 142, "CPO").
- **recommendation:** Replace "CPO" with "DPO" at [`risk/register-key-risk-indicators.md:142`](../../risk/register-key-risk-indicators.md). Same-day patch under the per-document version-bump discipline (since the file was already at 1.0.1 on 2026-06-22 from PR #218; if shipped on a later UTC day, patch-bump per the rule).
- **classification:** in-window (PR #218 touched this file with intent to flip the privacy-lead role label, missed the bare "CPO" occurrence at line 142)

### Out-of-scope observation (not a finding for this PR)

- [`governance/guideline-minimum-viable-governance-structure.md`](../../governance/guideline-minimum-viable-governance-structure.md) (lines 67 and 114) lists "CPO" in an executive-roles enumeration. NOT touched by PR #218; classification: out-of-window pre-existing. Will surface in the next corpus-wide `/validate` sweep as out-of-window for triage; appropriate fix is a small cleanup PR (rename "CPO" to "DPO" in the two affected lines plus per-doc Version+Date bump).

## Cross-reference check

Spot-checked citers of the canonical surfaces ([`governance/register-role-authority.md`](../../governance/register-role-authority.md), [`governance/register-glossary.md`](../../governance/register-glossary.md), [`privacy/README.md`](../../privacy/README.md)):

- Path-level citations (e.g., `[`governance/register-role-authority.md`](register-role-authority.md)`) resolve cleanly; the canonical surfaces' file paths are unchanged.
- Text-level quotes of the canonical surfaces (e.g., quoted line excerpts) were not found in corpus prose; PR #218's CHANGELOG-detailed self-quoted the register's new note, which is intentional.
- Gate 8 (Owner role audit) confirms the new "Data Protection Officer" role label matches every `**Owner:** Data Protection Officer` metadata field across the renamed jurisdiction annexes and other docs. No mismatches.

## Orchestrator triage

Both in-window findings are real defects and ship as adopter-visible (Finding 1 propagates to [`docs/portal.md`](../../docs/portal.md), the adopter-facing portal; Finding 2 ships in a corpus document at v1.0.1 with mixed canonical-role acronyms). Triage decision: **fix both in the next substantive PR** (PR-2: at-top "Role-name convention" notes) per the recursion-avoidance rule. The PR-2 description will note the bundled fixes.

The out-of-scope `guideline-minimum-viable-governance-structure.md` observation is logged here for the next `/validate` sweep's out-of-window triage and is not part of PR-2's scope unless the maintainer redirects.

## Resulting hot-fix PR (if any)

**None, by design** per the recursion-avoidance rule: `/validate-pr` findings bundle into the next substantive PR (PR-2), not a dedicated hot-fix PR.

## Notes

- The 8-failure-mode-class survey was clean on 6 of 8 (no stale prose references in CHANGELOG; no mis-attributed citations; no inferred-as-verified state assertions; no per-document version-bump omissions; no generated-artefact lag for `taxonomy.yml` or `docs/maturity-scorecard.md`; no stale Python docstring drift in `tools/build-portal.py`'s docstring). Failures clustered on (1) generated-artefact propagation from a buggy generator (Finding 1) and (2) cross-document term drift via a missed acronym pattern (Finding 2).
- Root-cause analysis for the next /retro: the rename script's `REPLACEMENTS` list was scoped to spelled-out forms ("Chief Privacy Officer" + variants), not to the bare acronym "CPO". A future corpus-wide rename script should include both spelled-out and acronym forms in the substitution list, OR run a final corpus-wide grep for the acronym after the script completes. Documented in this record so the next iteration of the worker-brief template can encode the lesson.
