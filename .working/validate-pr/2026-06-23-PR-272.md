# PR-scoped validation sweep, PR #272

## Trigger and state snapshot

- **PR**: #272 (FR-166, corpus listing-surface completeness gate 47 + `suggest-listing-surfaces.py` authoring tool). Squash merge commit `7944abb`.
- **State at merge**: library `2026.06.250`, pack `1.49.3`, README `1.9.121`; audit gates 47 (the new gate 47 included); governance rules 10; skills 15.
- **Mechanical baseline**: `tools/run_all_audits.sh` green post-merge on `main` (47/47).
- **Pre-flight scanner (touched-file filtered)**: one candidate in `dev-security/claude-rules/skills/guardrail-review/SKILL.md` (the "two rules/skills/gates" comparative prose), confirmed legitimate, dismissed.

## Subagent A return

Subagent A (diff-scoped, 8 failure-mode classes + cross-reference check over audit-programme citers) returned **zero in-window findings** and **one out-of-window finding**:

```
tool: subagent-A-pr272
ruleId: stale-gate-count-prose
level: note
location: governance/template-library-health-report.md:63
fingerprint: stale-gate-count-prose:governance/template-library-health-report.md:63
rubric: track-as-follow-up
in_window: no
evidence: Line 63 reads "Aggregated output of the 32 automated audits (see [...] §6 for the canonical gate inventory)". The §6 inventory now has 47 gate rows, so "32" is stale by 15. This escapes gate 39 because gate 39's idiom set matches "N audit gates" / "N gates" / "N-gate" but NOT "N automated audits" (verified against tools/lint-gate-count-consistency.py P1-P7 regexes). git blame shows the line dates to the initial public release (2026-05-31); it is a long-standing pre-existing miss, NOT introduced by PR #272. Surfaced by the PR-272 cross-reference check over audit-programme citers. Fix: count-agnostic phrasing ("the automated audits") matching the style every other citer uses.
```

Subagent A confirmed PR #272 is internally sound: the gate's code matches its documented behavior (docstring, §6 description, §5 category, suggest-tool MECHANICAL/SEMANTIC split all accurate); the 46→47 bump is complete and consistent (gate name byte-identical across all four surfaces; §6 has exactly 47 rows; guardrail "forty-six"→"forty-seven" and TODO "46"→"47" both updated; no stale "46" gate-count prose remains); all version surfaces bumped (audit-programme 1.15.0 + Date, ingestion 1.7.3 + Date, pack 1.49.3, library 2026.06.250 / README 1.9.121, taxonomy + scorecard reflect the bumps with no lag); no em-dashes or British -ise in touched prose.

## Cross-reference check

`governance/specification-audit-programme.md` is heavily cited. The cross-reference check over its citers found that every other citer phrases the gate count count-agnostically ("current count in spec §6") EXCEPT `template-library-health-report.md`:63, which hard-codes "32 automated audits", the finding above. No citer quotes the audit-programme spec's Version in a way the 1.14.2→1.15.0 bump invalidated. The two new tool files (`tools/lint-listing-surface-completeness.py`, `tools/suggest-listing-surfaces.py`) live under `tools/`, which is not an active-document path in taxonomy, so the new gate does not require them in any listing surface (confirmed: the gate keys on taxonomy active docs).

## Orchestrator triage

- **In-window (0)**: none. PR #272 is clean within its own scope.
- **Out-of-window (1)**: `template-library-health-report.md`:63 "32 automated audits" → genuinely stale (47), apply-time-verified by re-reading the line. **Determinable fix** (count-agnostic phrasing, matching every other citer's style, no new authorial decision). Per the `/validate-pr` batching rule, the fix bundles into the **next PR (FR-167)**, not a dedicated hot-fix PR.
- **Gate-coverage gap surfaced**: gate 39 (`lint-gate-count-consistency.py`) does not catch the "N automated audits" idiom. **P4 follow-up candidate**: extend gate 39's idiom set to include "N automated audits" / "N audits" so this class of stale count is caught mechanically. Logged to the `/retro` register and surfaced to the maintainer.

## Resulting hot-fix PR

None (no dedicated hot-fix PR per the batching rule). The count-agnostic fix to `template-library-health-report.md`:63 bundles into the next PR (FR-167). History row records originating PR #272 → closing PR (FR-167).

## Notes

The finding is the second instance of a "free-prose / non-standard-idiom count escapes the count gate" pattern (cf. the recurring "free-prose rule-count" pattern gate 41 misses). The durable fix is mechanical (broaden the count gate's idiom coverage); the immediate fix is the count-agnostic rephrase. Both are tracked.
