# `/validate-pr` record: PR #275 (FR-167 batch 1, Architecture domain)

## Trigger and state snapshot

- **PR**: #275, "FR-167 batch 1, compliance matrix Architecture domain". Merge commit `8a283a1` (squash) on `main`.
- **State at post-merge HEAD**: library `2026.06.253`, pack `1.49.3`, README `1.9.124`, matrix `1.1.0`; 47 gates, 10 rules, 15 skills, 8 commands.
- **Touched files (9)**: `compliance/matrix-grc-compliance-alignment.md` (the substantive change), `taxonomy.yml` + `docs/maturity-scorecard.md` (regenerated), `README.md` (versions), `CHANGELOG.md` + `.working/changelog-details/CHANGELOG-detailed.md` (entries), `TODO.md` (DD-11/DD-12 + sweep cursor), `.working/validate-sweeps/history.md` + `.working/validate-sweeps/2026-06-23-sweep29-iter1.md` (Sweep 29 records).
- **Mechanical baseline (post-merge main)**: `tools/run_all_audits.sh` → all 47 gates pass.
- **Single-iteration cycle**: dispatch Subagent A on the diff + targeted cross-reference check, triage, record.

## Subagent A return

**0 error, 0 warning, 1 note (out-of-window, pre-existing).**

```
tool: subagent-A
ruleId: legend-convention-undocumented-for-NA-cells
level: note
location: compliance/matrix-grc-compliance-alignment.md:36
fingerprint: matrix-legend-na-vs-colon-275
rubric: class (c) multi-surface incompleteness / (h) term drift, OUT-OF-WINDOW (pre-existing; amplified by this PR)
evidence: Line 36 documents only the ": " empty-cell convention ("A cell containing ': '
  indicates no direct mapping..."), but the matrix uses "N/A" as its dominant no-mapping marker
  (75 N/A cells vs 4 ": " cells). PR #275 added many of those N/A cells (8 Architecture rows x the
  5 customs/trade columns). The legend never mentions "N/A", so a reader cannot map "N/A" to the
  documented "no direct mapping" meaning. Out-of-window: the parent commit already had 19 N/A cells
  with the same gap, so the inconsistency predates PR #275; this PR widened it but did not create it.
  Distinct from the DD-11/DD-12 item set. Suggested fix (matrix-local, low): add "or \"N/A\"" to
  line 36, or normalize the 4 ": " cells to "N/A".
```

**Spot-verified 4 rows against source documents** (exceeds the 3 minimum): API design (AIS-01/02/04, IPY-01, A.5.14/A.8.26/A.8.27, supported); data architecture (DSP-01/02/03/05/07, A.5.12/A.5.13/A.5.34/A.8.10, supported, doc self-cites A.5.12/13/34 at :253); integration architecture (AIS-04, IPY-01, IVS-03, DSP-04, A.5.14/A.8.20/A.8.21/A.8.26, supported); ADR (§7.5, A.8.27, GRC-01/CCC-02/CCC-03, supported). Independently corroborated A.5.8, A.5.20, A.8.30 via the source docs' own crosswalks. No fabricated/mis-attributed identifier. Confirmed clean: stale-prose (zero "42" in the matrix; summary fully count-agnostic), citations, multi-surface (8-column key table + intro consistent), inferred-as-verified, version bump (1.0.0→1.1.0 + Date), generated-artefact lag (scorecard shows matrix 1.1.0), term drift (N/A convention + CCM v4.1/PR.IP per the FR-167 decision; DD-12 items not re-reported).

## Cross-reference check

- **Citers of the matrix basename**: ~17 files link to `matrix-grc-compliance-alignment.md` (registers, sector annexes, READMEs, the reverse crosswalk, the supply-chain matrix, portal/scorecard). All are **link references** (navigational), unaffected by adding a section to the target. None quotes a specific matrix section/count that the rewrite would strand.
- **Stale "All 42 documents" count elsewhere**: the only live mention is **TODO.md:77**, the FR-167 task description itself (which asks to soften that very wording). It is an in-flight backlog item, not a stranded citation. `CHANGELOG.md` mentions are historical/working-state, accurate-as-of-time.
- Result: clean; no external citation stranded by the summary rewrite.

## Orchestrator triage

| Finding | Window | Severity | Disposition |
|---|---|---|---|
| `legend-convention-undocumented-for-NA-cells` (matrix:36) | out-of-window (pre-existing) | note | **Bundle the fix into the next PR (FR-167 batch 2)**: update line 36 to document `N/A` (the dominant convention). Trivial, no authorial decision; benefits every future FR-167 batch (each adds N/A cells). |
| TODO.md:77 FR-167 progress | in-flight backlog | n/a | **Refresh in the next PR**: note batch-1 (Architecture) shipped #275 + the summary softened; not a defect. |

Apply-time verification: the orchestrator re-read matrix:36 and confirmed the legend/N-A convention gap (the matrix uses both `N/A` and `": "`, legend documents only `": "`). The finding is real and correctly tagged out-of-window.

## Resulting hot-fix PR

None. Per the `/validate-pr` batching rule, the (out-of-window, trivial) legend fix and the TODO:77 progress refresh bundle into the next substantive PR (FR-167 batch 2), not a dedicated hot-fix PR.

## Notes

- The PR's substantive change (8 Architecture rows + count-agnostic summary) is clean and well-grounded: every spot-checked mapping traces to its source document, and several control identifiers are independently confirmed by the source docs' own framework-alignment crosswalks. This validates the apply-time verification done during authoring (the research-assistant discipline held).
- The single note is a pre-existing legend gap the expansion amplified, a good catch for the next batch, since the FR-167 expansion will keep adding `N/A` cells and the legend should document them.
