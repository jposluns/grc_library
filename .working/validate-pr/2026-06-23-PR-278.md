# PR-scoped validation sweep, PR #278

## Trigger & state snapshot

- **Trigger**: post-merge `/validate-pr` for PR #278 (DD-1: add PR-time delta gate D3 for the no-dash convention on new CHANGELOG entries, new-entries-only).
- **Merge commit**: `5ac0b4f` (squash) on `main`.
- **State at HEAD**: library `2026.06.256`, README `1.9.127`, audit-programme spec `1.16.0`; 47 numbered gates + 3 PR-only delta gates (D1/D2/D3); 10 rules, 15 skills, 8 commands.
- **Mechanical baseline**: `tools/run_all_audits.sh` → 47/47 on the post-merge state.

## Subagent A return

Subagent A (recent-PR deep review scoped to #278's diff): **1 error, 2 warnings, 0 notes.** The mechanical D3 wiring (tool, runner, workflow PR-only step, parity exclusion, spec §6.1 table + paragraph, dual CHANGELOG lock-step, README/version) is correct, complete, and self-consistent; the tool does not flag the `→` arrow; the #278 CHANGELOG entry and the spec D3 paragraph are dash-free (byte-verified). All three findings are in bookkeeping/disposition surfaces:

```
tool: validate-pr-subagent-A
ruleId: stale-todo-disposition-DD1
level: error
location: TODO.md (DD-1 resolution line in the triage block)
fingerprint: stale-todo-disposition-dd1:todo.md:resolution
rubric: a (stale prose reference)
evidence: The DD-1 resolution line records the SUPERSEDED "Extend the dash gate to all of CHANGELOG; fix the ~5 existing P1-P4 en-dash entries" plan. PR #278 shipped the OPPOSITE (new-entries-only): history untouched, the existing entries NOT fixed, per the maintainer's at-action-time re-confirmation after the ~130-dash investigation. A future reader would believe DD-1 still requires whole-CHANGELOG scope. FIX: update the line to the new-entries-only resolution actually shipped.
```
```
tool: validate-pr-subagent-A
ruleId: todo-not-rotated-DD1
level: warning
location: TODO.md (DD-1 original "Decide:" backlog item)
fingerprint: todo-not-rotated-dd1:todo.md:backlog-item
rubric: a (TODO/DONE rotation)
evidence: DD-1 was resolved and shipped in PR #278 (a substantive PR, not the handoff exception), but the open backlog item "DD-1 (from #259 /validate-pr, low): ... Decide: extend the dash gate to CHANGELOG, or keep it unscoped" remains in TODO unremoved. The change-tracking PR-finalization protocol requires closing the item from TODO and rotating a headline to .working/DONE.md. FIX: delete the DD-1 backlog item + its resolution line; add a DONE.md entry keyed to #278.
```
```
tool: validate-pr-subagent-A
ruleId: stale-delta-gate-enumeration
level: warning
location: .claude/CLAUDE.md (PR-workflow delta-gate enumeration)
fingerprint: stale-delta-gate-enumeration:.claude/claude.md:pr-time-checks
rubric: h (multi-surface incompleteness)
evidence: ".claude/CLAUDE.md" enumerates "the PR-only delta gates (D1 CHANGELOG-on-PR, D2 per-PR version-bump) plus gate 45". PR #278 added D3 to the same tools/run-pr-time-checks.sh runner this sentence describes, but the enumeration still reads as a closed set of two. FIX: add D3 (CHANGELOG dash-on-PR) to the enumeration. (The same line's pre-existing em dash is out of scope; .claude/ is gate-exempt.)
```

## Cross-reference check

Orchestrator grep over corpus citers of the delta-gate set and the touched tool/spec files: **no other surface enumerates the delta-gate count in a way D3 stales.** The spec §6.1 is the only D1/D2/D3 enumeration (updated in #278). All other "delta gate" / "PR-time delta gate" references (change-tracking rule + skill, README tree) are generic and unaffected. The `.claude/CLAUDE.md` enumeration (Subagent A finding 3) is the one durable-instruction surface that named D1/D2 explicitly; it is gate-exempt but a live instruction, so it is in-scope for the fix.

## Orchestrator triage

All three findings apply-time-verified (re-read the cited lines). All **in-window** (introduced/exposed by #278's own disposition bookkeeping). Classification:
- Finding 1 (error): C-class stale-reference / paired-surface-lag. The decision was re-scoped mid-session and the triage-block resolution line was not back-updated. Real; fix required.
- Finding 2 (warning): TODO/DONE rotation omission. DD-1 is the first of the 12 triaged DDs to actually ship, so it is the one due for rotation now; DD-2..DD-12 remain queued work and stay in TODO.
- Finding 3 (warning): multi-surface incompleteness in a durable instruction file.

**Action**: all three fixes bundle into the next PR (DD-9, the next Track-1 quick-win) per recursion-avoidance, alongside the #278 `/validate-pr` history row and `/retro` row. No dedicated hot-fix PR.

## Resulting hot-fix PR

None standalone. The three fixes + this record's history row batch into the next PR (DD-9).

## Notes

- The mechanical change (gate D3) shipped clean; the misses were all in the decision-disposition bookkeeping, the recurring paired-surface-lag class. This is the close-out-checklist's exact target; `/validate-pr` is the backstop that caught it.
- Stop-hook git-check flagged the squash-merge commit `5ac0b4f` as Unverified (committer `noreply@github.com`); this is the GitHub merge commit on `main`, correctly NOT amended (amending would force-push a protected branch).
