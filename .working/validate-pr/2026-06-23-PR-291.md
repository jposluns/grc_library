# PR-scoped validation: PR #291 (PR-F security/crypto bundle)

## Trigger and state snapshot

- **Trigger**: mandatory post-merge `/validate-pr` for PR #291 (squash commit `ba8274b` on `main`), PR-F of the XS/S batch: FR-153 (PBKDF2), BYOD MDM/MAM rewrite, FR-90 (browser hardening), FR-84 (regression checklist), FR-85 (breach-assessment leads), FR-86 (recovery-runbook crisis-comms cross-ref).
- **State**: library `2026.06.269`, pack `1.49.3`, 47 numbered gates + 3 delta, 10 rules, 15 skills. Post-merge mechanical baseline `tools/run_all_audits.sh` = **47/47**.
- **Pre-flight scanner** filtered to the 6 touched files: 0 candidates.

## Subagent A return

**0 findings.** Verbatim verdicts:

- **BYOD residual-MAM-only verdict: CLEAN.** Every former MAM-only absolute is now model-conditional or genuinely model-independent. The "What the organisation does not do" block is scoped under "Under the MAM model..." with an MDM counterpoint; governance rows, access-model intro, incident/loss, offboarding, policy-violations, and the NIST 800-124r2 framework row are all dual/conditional; Scope and the Data classification restriction are correctly left model-independent. The access-model table's per-row "by MAM policy" phrasing is the designated MAM control set (per the :31 and :58 framing that MDM also enforces), not a leftover absolute.
- **FR-153 internal-consistency: CORRECT.** Argon2id `m=19456 (19 MiB), t=2, p=1` and PBKDF2-HMAC-SHA256 `600,000` / SHA-512 `220,000` match the live OWASP Password Storage Cheat Sheet (verified against the canonical OWASP CheatSheetSeries GitHub master). The §3 general "Hashing" row naming SHA-512/BLAKE2b while the password row names PBKDF2-HMAC-SHA256 is not a contradiction: they govern different operations (general digest vs password derivation), and SHA-256 with 600,000 iterations is OWASP's recommended PBKDF2 configuration.
- Version+Date: all 6 bumped, Date 2026-06-23. Generated taxonomy/scorecard reflect all 6 new versions. FR-86 cross-reference target `resilience/plan-crisis-communication.md` exists and the link resolves. No stale reference or term drift introduced.

## Cross-reference check

Orchestrator checked which corpus files cite the 6 touched docs and whether any characterization went stale post-merge.

- **1 in-window finding** (note, cross-ref): `governance/register-document-index-and-classification.md`:103 — the BYOD policy's index row relevance descriptor read "...CSA CCM UEM, MAM controls". The #291 rewrite made the policy cover MAM **and** MDM, so "MAM controls" is now an incomplete descriptor. The index register is hand-maintained (only `taxonomy.yml`, `docs/portal.md`, `docs/maturity-scorecard.md` are generated), so the descriptor is hand-authored and was made stale by #291.
  - **Fix**: "MAM controls" → "MAM and MDM controls"; register `1.27.31` → `1.27.32`. Bundles into the session-closing handoff PR per recursion-avoidance.
- No other citer characterizes the BYOD model as MAM-only. The only near-hit, `standard-remote-working-security.md`:177 ("mobile device management where technically feasible"), is consistent with the new dual-model framing (it does not contradict it).
- FR-153, FR-90, FR-84, FR-85, FR-86 touched no section number, title, or version-pinned reference that a citer depends on; no other cross-reference breakage.

## Orchestrator triage

- The single finding is in-window (PR #291 caused the staleness) and note-level (an index-register relevance descriptor, not a normative error). Per the in-window protocol it received a concrete fix; per recursion-avoidance the fix bundles into the next PR (the session-closing handoff PR), not a dedicated hot-fix PR.
- Subagent A's 0-finding result on the substantive corpus content (including the security-critical FR-153 and the BYOD structural rewrite) was apply-time-verified by the orchestrator against the OWASP source and the live BYOD document.

## Resulting hot-fix PR

None. The fix (index-register descriptor + register version bump) bundles into the session-closing handoff PR.

## Notes

The cross-reference check was the load-bearing step here: Subagent A (scoped to the touched files) correctly returned 0 findings on the documents themselves, but the staleness was in a *citing* file (the index register's descriptor), which only the cross-reference pass examines. This is the designed division of labour between Subagent A (touched-file content) and the orchestrator cross-reference check (citers of the touched files).
