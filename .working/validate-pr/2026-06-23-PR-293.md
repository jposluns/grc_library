# PR-Scoped Validation: PR #293

## Trigger & state snapshot

- **PR**: #293 — Sweep 34 `/validate` close-out (correct the false "Day-1 floor nests inside Tier 1" claim across two adopter-onboarding surfaces; reconcile a breach-response role lead-in).
- **Merge commit**: `2068c2f` (squash) on `main`.
- **Library version post-merge**: `2026.06.271`; README `1.9.142`; pack `1.49.3`. Gates **47** (+ D1/D2/D3); rules **10**; skills **15**; commands **8**.
- **Touched files (diff)**: 3 substantive corpus (`docs/adopter-guide.md` `1.3.0`→`1.3.1`, `docs/template-quickstart.md` `3.0.2`→`3.0.3`, `privacy/procedure-data-protection-and-privacy-breach-response.md` `1.4.12`→`1.4.13`); generated `taxonomy.yml` + `docs/maturity-scorecard.md`; `CHANGELOG.md` + detailed mirror; `README.md`; `TODO.md` (sweep cursor); the Sweep 34 history row + detail file.
- **Post-merge mechanical baseline**: `tools/run_all_audits.sh` clean — **47/47**.
- **Pre-flight scanner (filtered to touched files)**: no candidates in the three touched corpus files.

## Subagent A return

Scope: the three touched corpus files, read in full post-merge, against failure-mode classes (a)-(h).

**0 findings** (the #293 fixes are correct and complete). Verified:
- **(d) 4-of-6 overlap, independently re-derived**: the six Day-1-floor artefacts (`template-quickstart.md:34-39`) against the 15-doc Tier 1 table (`adopter-guide.md:121-135`) — info-security policy, security-incident-response procedure, privacy-and-data-governance policy, and enterprise-governance-and-risk-management policy overlap (4); `security/policy-acceptable-use.md` and `security/policy-identity-and-access-management.md` are absent from Tier 1 (the 2 named exceptions). The new "shares four of those six artefacts" claim is correct.
- **(c) multi-surface consistency**: both `adopter-guide.md:115` and `template-quickstart.md:45` now carry the corrected framing and are mutually consistent; neither retains the false "nest/subset" claim; `decision-tree.md:320` correctly frames the ~25-doc path as the outer set that "builds on rather than replaces" the floor and Tier 1.
- **(f) breach-response §4.2**: the new lead-in (`:100` "drawing in Legal where a determination requires it") reconciles with the table's determinations 5/6 led by "DPO, with Legal" (`:108-109`); no residual closed "two roles only" framing for the assessment (the remaining "joint" references at §1.3/§2.2/§10 concern *initiating* breach response, a genuine DPO+CISO joint duty).
- **(e) version bumps**: all three present (adopter-guide 1.3.1, quickstart 3.0.3, breach-response 1.4.13; Date 2026-06-23).
- **(a)(b)(h)**: no stale references, mis-attributed citations, or term drift introduced.

**Out-of-scope observation A surfaced** (not a finding against #293; the file was untouched by the PR): `docs/template-startup-roadmap.md:56` defines its own "core baseline" risk-domain artefact as `risk/procedure-risk-register.md`, whereas the quickstart Day-1 floor (`template-quickstart.md:39`) uses `risk/policy-enterprise-governance-and-risk-management.md`. The two "six-artefact floor" definitions disagree on the sixth artefact.

## Cross-reference check

Citers of the three touched files were enumerated (`grep -rln`). All cite by path/link (validated by the broken-link gate 3) or title, not by quoting the specific sentences #293 changed. No citer pins a section number or quoted phrase that the edits broke. `decision-tree.md:320` (a citer of both quickstart and adopter-guide) was re-read and is consistent with the softened prose. No version-pinned reference except the regenerated scorecard (correct at the new versions).

## Orchestrator triage

- **In-window**: 0 findings. The #293 fixes verified correct and complete (apply-time re-read confirmed the 4-of-6 overlap and the breach-response reconciliation; Subagent A independently re-derived the same).
- **Out-of-window (1, CONFIRMED REAL at apply-time)**: the two-baseline risk-artefact drift. Apply-time verification: `docs/template-startup-roadmap.md:56` core baseline uses `risk/procedure-risk-register.md`; `docs/template-quickstart.md:39` Day-1 floor uses `risk/policy-enterprise-governance-and-risk-management.md`; both files exist; the two baselines agree on the other five artefacts. This is a C8 cross-document drift (two purportedly-canonical "six-artefact floor" definitions disagree on which risk-domain artefact is the floor). Pre-existing (neither file's risk-artefact line was touched by #293; #293 exposed it via adjacency to the floor/Tier-1 reconciliation). **Routed to the maintainer with named options (not auto-fixed, directional):** (A) make the quickstart's enterprise-governance-and-risk-management policy canonical in both (the policy is the posture-setting artefact, matches the Tier 1 starter set); (B) make the startup-roadmap's risk-register procedure canonical in both (a populated register is the more operational Day-1 deliverable); (C) keep both deliberately and add a one-line note explaining why the Day-1 floor and the startup roadmap pick different risk artefacts. Added to `TODO.md` as a new item (no-drop). Recommendation: (A) — the floor is described elsewhere as the posture/policy floor, and the quickstart already cross-links its risk artefact to the Tier 1 starter set, so (A) harmonizes three surfaces; the register-population step can be retained as roadmap guidance without being the named floor artefact.

## Resulting hot-fix PR

None. Per recursion-avoidance, this record + the history row batch into the next PR (PR-H). The out-of-window fix lands in a future PR once the maintainer chooses a direction.

## Notes

The #293 sweep confirms the Sweep 34 fixes are clean (the per-PR sweep is the proof-of-discipline that the close-out actually resolved the corpus-wide sweep's findings). The single out-of-window finding is a genuine pre-existing cross-document drift surfaced by the adjacency of #293's floor/Tier-1 work; it is surfaced and routed, not dropped and not auto-resolved.
