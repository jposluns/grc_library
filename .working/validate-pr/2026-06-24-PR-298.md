# PR-Scoped Validation: PR #298

## Trigger & state snapshot

- **PR**: #298 (Sweep 35 close-out + corpus-wide CCM/AICM citation reconciliation).
- **Merge commit**: `dae2e1c06a80c47253feddebfd1c450d0d22ebf9` (squash).
- **Post-merge mechanical baseline**: `tools/run_all_audits.sh` = **47/47** on `main`.
- **Touched files**: 48 (33 corpus docs version-bumped, 4 pack files, the `.claude` mirror, taxonomy/portal/scorecard, CHANGELOG×2, README, TODO, 3 `.working/` records).
- **Pre-flight (touched-file scope)**: 0 candidates.
- Ground truth: the authoritative CSA CCM v4.1.0 + AICM v1.1.0 catalogues (session scratchpad).

## Subagent A return

Two findings, both in `dev-security/register-compliance-controls-and-gap-register.md`, same class: a code-level fix (IVS→I&S rename; AICM v1.0.3→v1.1 relabel) not carried through to the human-readable control **titles**, which were pre-existing-wrong and are now asserted under authoritative codes/version.

**A-298-1** — `ivs-to-i&s-rename-left-stale-control-titles` / **warning** / `dev-security/register-compliance-controls-and-gap-register.md:198-204` / **in-window**. The seven I&S rows carry titles that do not match the CCM v4.1.0 I&S catalogue: doc `I&S-01 "Audit Logging and Monitoring"` (catalogue: "Infrastructure and Virtualization Security Policy and Procedures"); `I&S-03 "Migration and Asset Ownership"` (cat: "Network Security"); `I&S-04 "Network Architecture"` (cat: "OS Hardening and Base Controls"); `I&S-06 "Network Defense"` (cat: "Segmentation and Segregation"); `I&S-07 "Segmentation and Segregation"` (cat: "Migration to Cloud Environments"); `I&S-08 "Production and Non-Production Environments"` (cat: "Network Architecture Documentation"); `I&S-09 "Vulnerability and Patch Management"` (cat: "Network Defense"). The titles were stale before the PR, but the rename now presents them under an authoritative v4.1.0 `I&S` code.

**A-298-2** — `aicm-relabel-left-stale-mds-titles` / **warning** (raised from the worker's "note" at apply-time, since the whole MDS block is wrong) / `dev-security/register-compliance-controls-and-gap-register.md:280-296` / **in-window**. The AICM section (relabelled `v1.0.3`→`v1.1` by #298) carries titles that do not match the AICM v1.1.0 catalogue: `GRC-09 "Acceptable Use of AI Service"` (catalogue: "Acceptable Use of **the** AI Service"); and all thirteen MDS rows are wrong, e.g. doc `MDS-01 "Model Inventory"` (cat: "Training Pipeline Security"), `MDS-02 "Model Documentation"` (cat: "Model Artifact Scanning"), `MDS-04 "Model Bias and Fairness Assessment"` (cat: "Model Documentation Requirements"), `MDS-06 "Adversarial Robustness Testing"` (cat: "Adversarial Attack Analysis"). The MDS titles read like a plausible-but-fabricated AI-control set, not the real AICM MDS controls. (`HRS-15 "AI Acceptable Use"` to be verified against the catalogue HRS-15 title during the fix.)

**Verified clean** (Subagent A, full list): no live-corpus residual `IVS-`/`NET-[0-9]`/`IPY-05`/`AICM v1.0.3`/`CCM v4.0.12`/`GRM domain`/`GOV`-as-CCM (only `.working/`, append-only CHANGELOG, the resolved TODO note, corpus-internal `MODEL-GOV`/`AI-GOV`, and the intended `lint-citations.py` GRM denylist rule); GOV-01..08→GRC-01..08 correct; GRM→GRC all 6 cells; the A&A add-back titles match the catalogue exactly (A&A-01/02/05); the AICM GRC-10/11/12 add-back matches and its inline text agrees with the table row; NET→I&S network mappings sound; IPY-05→IPY-04 correct; AICM v1.0.3→v1.1 in register + carriers; all 33 version+date bumps present; `build-taxonomy.py --check` and `build-portal.py --check` both in sync; B-1 (AI-log 7-year) fixed with cross-reference.

## Cross-reference check

The touched files are heavily cited (register-document-index by 46, register-canonical by 23, matrix-grc by 17, matrix-cross by 14), but citers link to the *files*, not quote their internal control-code rows. A corpus-wide grep for every old code form (`GOV-NN`-as-CCM, `IVS`, `NET`, `GRM domain`, `GOV domain`, `IPY-05`, `AICM v1.0.3`, `CCM v4.0.12`) returns zero live-corpus hits outside `.working/`/CHANGELOG, so no citer quotes a now-corrected code, version, or section. No cross-reference broken by the version bumps or code corrections.

## Orchestrator triage

Both findings apply-time-verified by orchestrator re-read against the catalogues (the gap-register's "Control Title" column is confirmed to be the CCM/AICM control title, e.g. GRC-01="Governance Programme Policy", IAM-01="IAM Policy and Procedures", so the I&S/MDS title mismatches are genuine defects, not an org-control-label convention). Both **in-window** (the #298 rename/relabel asserted the wrong titles under authoritative codes/version) and both localized to one file. **Fixes bundle into the next PR (the gate PR)** per the recursion-avoidance batching rule: correct the 7 I&S titles and the GRC-09 + 13 MDS titles (+ verify HRS-15) to the authoritative catalogue titles, in the same PR that adds `lint-ccm-aicm-citations.py`. Added to the gate-PR work list. This is the **third occurrence this session-window** of the missed-parallel-surface / incomplete-mechanical-sweep class (cf. #294 retention-ripple, #296 FR-160 third surface): a mechanical prefix/version swap that did not verify the adjacent human-readable field. The forthcoming gate checks codes, not titles, so the title-accuracy gap motivates extending the gate's reference to carry canonical titles (a title-match check) as a follow-on.

## Resulting hot-fix PR

None standalone. Fixes bundle into the gate PR (next PR); this record's history row names that PR as the closing PR once it lands.

## Notes

The finding strengthens the case for the queued `lint-ccm-aicm-citations.py` gate to validate not just domain/range but, where a code carries an inline title, that the title matches the canonical catalogue title (a fair-use reference of code→title). That extension is noted for the gate PR.
