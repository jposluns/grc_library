# PR-Scoped Validation: PR #299

## Trigger & state snapshot

- **PR**: #299 (gate 48 CSA CCM/AICM citation-accuracy audit + full gap-register title alignment).
- **Merge commit**: `c245c0c4fc6d66b3d8bbd9292cb8cb8c4e6b39a7` (squash).
- **Post-merge mechanical baseline**: `tools/run_all_audits.sh` = **48/48** on `main` (gate 48 itself green: 0 findings across 378 files).
- **Touched files**: 18 (the new linter + reference, 4-surface wiring, gap-register full title alignment, spec §6, regression fixture, TODO, generated artefacts, CHANGELOG×2/README, batched #298 QA records).
- **Pre-flight (touched-file scope)**: 0 candidates.
- Ground truth: the authoritative CSA CCM v4.1.0 + AICM v1.1.0 catalogues (session scratchpad).

## Subagent A return

**One in-window finding (error). All other checks clean** (Subagent A mechanically compared all 54 CCM-section + 17 AICM-section rows against the authoritative catalogues; verified the gate logic, the reference's 207 CCM + 247 AICM titles, four-surface parity, gate-count 47→48, version bumps, and generated-artefact sync).

**A-299-1** — `cross-catalogue-title-confusion` / **error** / `dev-security/register-compliance-controls-and-gap-register.md:202` / **in-window**. The row `| I&S-07 | Migration to Hosted Environments |` sits under the "## CSA CCM v4.1 control mapping" section, but "Migration to Hosted Environments" is the **AICM v1.1** title for I&S-07; the **CCM v4.1** title is "Migration to Cloud Environments" (verified: `CCM_V41['I&S-07'] = "Migration to Cloud Environments"`, `AICM_V11['I&S-07'] = "Migration to Hosted Environments"`). I&S-07 is the only control whose title diverges between the two catalogues AND appears in the register; the PR #299 alignment script used the union dict (`ALL_TITLES`, AICM-wins-on-overlap), so it picked the AICM value for a CCM-section row. Gate 48 cannot catch it: the two titles share the content words "Migration"/"Environments", so the conservative zero-shared-word title check does not fire (gate 48 exits 0 corpus-wide). This is exactly the gate-escaping class the PR-scoped sweep targets.

## Cross-reference check

Gap-register cited by 6 corpus files, spec-audit-programme by 19; all citers link to the files, not to internal control-code rows, so the title fix and version bumps break no cross-reference. No stale current-state "47 gates" claim outside `.working/`/CHANGELOG (the `47` grep hits are specific gate-47 references and version-history dates, not counts). Gate 48 present with identical name in all four surfaces.

## Orchestrator triage

A-299-1 apply-time-verified by orchestrator re-read (confirmed I&S-07 is under the CCM section header and CCM_V41 differs from AICM_V11). **In-window**; fixed in the session-closing handoff PR (gap-register `1.0.2` → `1.0.3`: I&S-07 title set to the CCM value "Migration to Cloud Environments"). Per the handoff-PR exception the handoff PR may carry this small corpus fix; the next `/resume`'s corpus-wide `/validate` re-checks. **Gate-enhancement candidate** (routed to `/retro`): make gate 48's title check section-aware (CCM-section rows checked against `CCM_V41`, AICM-section rows against `AICM_V11`) so the two divergent controls (I&S-07, IAM-11) are caught; currently the union-dict check is blind to CCM-vs-AICM title confusion on near-identical titles. Low-frequency (2 controls diverge), so a follow-up enhancement, not a blocker.

## Resulting hot-fix PR

None standalone. The I&S-07 fix bundles into the session-closing handoff PR (which also carries this record's history row and the #299 `/retro` row); the history row names that PR as the closing PR.

## Notes

The finding is the same incomplete-mechanical-sweep meta-pattern (a mechanical bulk operation that did not verify an edge case): here the union-dict alignment was correct for 53 of 54 rows but wrong for the one cross-catalogue-divergent control. The gate-48 section-aware enhancement is the durable fix for the class within the CCM/AICM domain.
