# /validate-pr — PR #320 (DD-8 CPPA-as-live → PIPEDA scrub)

## Trigger and state snapshot

- **PR**: #320, squash-merge commit `47b5f18` on `main`. "DD-8: scrub the live-regime sense of CPPA to PIPEDA across the corpus" + folded-in `go.md` TLS-precision fix.
- **State**: library `2026.06.298`, pack `1.49.9`, README `1.9.169`; 48 gates, 10 rules, 15 skills, 8 commands.
- **Mechanical baseline (post-merge `main`)**: `tools/run_all_audits.sh` → **48/48**.
- **Touched files**: 31 corpus/pack docs (the 29 CPPA scrub targets + `go.md` + pack `README.md`) plus `.working/` records, `CHANGELOG`×2, root `README`, `TODO`, `taxonomy.yml`, `docs/maturity-scorecard.md`.
- **Pre-flight (filtered to touched files)**: none of the 9 corpus-wide pre-flight candidates fall in the touched-file set (they are in the `guardrail-review` / `deep-qa-review` / `trust-recovery` prose, untouched here).

## Subagent A return

**1 finding (warning, class c — multi-surface incompleteness).**

```
tool: subagent-A
ruleId: cross-document-fact-drift
level: warning
location: privacy/annex-privacy-jurisdiction-index.md:133
fingerprint: pr320-c27-lapse-vs-under-study
evidence: The "2026 Update summary" bullet reads "**Canada Bill C-27 (CPPA/AIDA):** Remained under parliamentary study as of early 2026; monitoring required for Royal Assent and transitional provisions." PR #320 touched this file (version 1.0.3 -> 1.0.4; line 45 edited to "CPPA (pending)") and its glossary rewrite (register-glossary.md:90/246) asserts CPPA "lapsed in 2025 (not in force)". "lapsed in 2025" cannot coexist with "remained under parliamentary study as of early 2026." C-27 died on the order paper at the 2025-01-06 prorogation, so line 133 is a stale, internally-contradictory currency note the scrub left behind in a file it otherwise edited.
recommendation: Align line 133 to the corpus consensus (C-27 lapsed at the 2025-01-06 prorogation; not in force; would require reintroduction), matching annex-privacy-canada.md:28 and register-canonical-citations.md:130.
```

**Verified clean** (Subagent A, with evidence): every CPPA→PIPEDA swap is factually correct (`matrix:65` "PIPEDA accountability" = PIPEDA Schedule 1 Principle 1; incident-response §6 PIPEDA row = the Breach of Security Safeguards Regulations standard; ERM:255 full name correct; glossary entries accurate); `annex-ai-risk-methodology.md:149` correctly qualified `CPPA (Canada, proposed)` not replaced; all residual CPPA in touched files is California-agency or qualified-pending/proposed; all per-doc Version+Date bumps present; `go.md` (no metadata block) version-tracked via pack `1.49.9`; taxonomy/scorecard in sync; the go.md TLS reword is accurate per Go `crypto/tls` and internally consistent.

## Cross-reference check

- Corpus-wide `grep CPPA` (orchestrator) confirms the only remaining occurrences outside the preserved set are `privacy/jurisdictions/annex-privacy-united-states.md`:56/:100 — both the **California Privacy Protection Agency** sense (ADMT regulations, CCPA enforcement penalties), correctly preserved.
- `grep "superseded by CPPA" / "being superseded"` corpus-wide → none (no other doc repeats the old claim the glossary fixed).
- The touched registers (matrix, document-index) are cited by ~18 files **by path only**; the version bumps and cell-content edits break no path citation, and no citer quotes the changed cell/glossary text.

## Orchestrator triage

- **F1 (`annex-privacy-jurisdiction-index.md:133`)**: apply-time re-read CONFIRMED real (the line says "Remained under parliamentary study as of early 2026"; the corpus consensus, `annex-privacy-canada.md:28` + `register-canonical-citations.md:130`, is that C-27 lapsed 2025-01-06). **Classification: out-of-window** (the line pre-existed #320 and was not in the scrub's edit set; the "as of early 2026" phrasing was already stale given the Jan-2025 prorogation), **exposed by** the DD-8 scrub. Honest note: it is a **scrub near-miss** — DD-8 edited this file's framework-list line (:45) but its scope was CPPA-as-a-regime-in-lists, not the C-27 legislative-status narrative at :133; "triage softer mentions" should arguably have reached it. Severity warning (Medium); routes to P2 per the convention.
- **Action**: per recursion-avoidance, the fix is **bundled into the next substantive PR** (not a dedicated hot-fix PR), alongside this `/validate-pr` history row and the #320 `/retro` row. Recommended fix: rewrite :133 to "lapsed at the 2025-01-06 prorogation (died on the order paper); not in force and would require reintroduction in a future Parliament; monitoring required for any reintroduction." Surfaced to the maintainer in chat.

## Resulting hot-fix PR if any

None (no dedicated hot-fix PR; the fix bundles into the next PR per recursion-avoidance). Source #320 → next PR.

## Notes

The DD-8 scrub itself was clean on its 29 in-scope edits; the one finding is an adjacent C-27-status currency note in a touched file that the scrub's list-focused scope did not reach. A future corpus-wide-scrub brief should include "also scan narrative/currency-summary surfaces in every touched file for the same fact, not only the structured lists" — the same multi-surface-incompleteness lesson the close-out checklist already tracks.
