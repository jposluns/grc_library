# /validate-pr — PR #321 (FR-140: adopter starter-set divergence)

## Trigger and state snapshot

- **PR**: #321, squash-merge commit `3a57952` on `main`. "FR-140: resolve adopter starter-set divergence (Day-1 floor as subset of Tier 1)".
- **State**: library `2026.06.300`, README `1.9.171`, pack `1.49.9`; 48 gates, 10 rules, 15 skills.
- **Mechanical baseline (post-merge `main`)**: `tools/run_all_audits.sh` → **48/48**.
- **Touched corpus docs**: `README.md`, `docs/adopter-guide.md`, `docs/decision-tree.md`, `docs/template-quickstart.md`, `privacy/annex-privacy-jurisdiction-index.md` (+ generated `taxonomy.yml`, `docs/maturity-scorecard.md`, and `.working/` records).

## Subagent A return

**0 error / 0 warning findings introduced by #321. 1 NOTE (pre-existing, out-of-window).**

```
tool: subagent-A
ruleId: readme-catalogue-label-imprecision
level: note
location: README.md:277
fingerprint: pr321-readme-row277-enterprise-label
evidence: README "Core reference set" row reads "| Governance | Governance and Risk Management Policy |",
  but the floor/Tier-1 artefact it nests is risk/policy-enterprise-governance-and-risk-management.md,
  whose Document Title is "Enterprise Governance and Risk Management Policy". The README label drops
  "Enterprise" and files the row under the Governance domain rather than Risk. PRE-EXISTING (the row
  predates #321; the diff did not touch it). The strict-subset claim still holds at the artefact level
  (the only file matching the name is the enterprise risk policy; no governance/policy-governance-and-
  risk-management.md exists). Low-severity catalogue-label imprecision, surfaced for maintainer triage.
```

**Verified clean** (Subagent A, with evidence): adopter-guide Tier 1 table is **exactly 17 rows** (`:121-137`); all 6 Day-1-floor artefacts present in Tier 1 (info-security `:129`, acceptable-use `:130`, IAM `:131`, incident-response `:133`, privacy `:134`, enterprise-risk `:125`); all 6 present in the README Core reference set (`:289-292`, `:308`, `:277`); the "17" count is consistent across quickstart `:45` / adopter-guide `:115`+`:117` / decision-tree `:320` with no surface left at 15; the C-27 lapse fix at `annex-privacy-jurisdiction-index.md:133` is factually accurate; all per-document Version+Date bumps present (quickstart 3.0.4, adopter-guide 1.3.2, decision-tree 1.0.7, annex 1.0.5, README Library 2026.06.300 / README 1.9.171); taxonomy + scorecard regenerated with no drift; term usage consistent.

## Cross-reference check

- Orchestrator corpus-wide grep for `15-document` / `15 documents` / `these 15` / `four of those six` / `the other two`: zero surviving Tier-1 references in live corpus docs. The one hit (`compliance/financial-services/annex-financial-services-sector-requirements.md:143` "all 15 required areas") is an unrelated cybersecurity-policy coverage count, correctly untouched.
- Adopter-guide Tier 1 table independently counted at **17** document rows; each of the 6 floor artefacts confirmed present exactly once (`grep -c` = 1 each).
- The touched docs are cited by-path by sibling on-ramp docs (quickstart ↔ adopter-guide ↔ decision-tree ↔ README); the version bumps and count edits break no path citation, and the cross-references between them were all updated in lock-step (verified by the consistent "17" and the paired strict-subset / strict-superset framing).

## Orchestrator triage

- **F1 (`README.md:277`, NOTE, out-of-window)**: apply-time CONFIRMED real (the README row label drops "Enterprise" and files the enterprise-risk policy under the Governance domain). Pre-existing, low severity, and the fix involves a domain-reclassification judgement (is the Governance-domain placement intentional?) that is the maintainer's call, so NOT silently fixed. **Action**: routed to TODO as a tiny P3 follow-up (`FR-140-followup / README catalogue label`); surfaced in chat. Not bundled into the next PR's substantive work (a README catalogue-label/domain change is unrelated to the FR-167 supply-chain-matrix work that is next).

## Resulting hot-fix PR if any

None. Source #321 → next PR carries this history row (and the #321 `/retro` row) per recursion-avoidance; the F1 NOTE is tracked in TODO, not bundled as a fix.

## Notes

The FR-140 multi-surface reconciliation (the primary risk class for this PR) was complete and internally consistent on the first apply, including the README-catalogue nesting that an earlier draft had missed (caught and fixed pre-merge in commit 2). The one residual NOTE is a pre-existing catalogue-label imprecision orthogonal to FR-140's count/nesting scope.
