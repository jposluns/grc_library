# /validate-pr record — PR #326

## Trigger and state snapshot

- **PR**: #326 — Close §4.6a: gate 49 NIST CSF 2.0 category-membership + 17-cell remap.
- **Merge commit**: `78c0258` (squash; parent `58b58bb`).
- **Date**: 2026-06-24 (UTC).
- **Touched files (14)**: `tools/lint-matrix-control-codes.py`, `tools/nist_csf_reference.py` (wired), `compliance/matrix-grc-compliance-alignment.md` (`1.3.0`→`1.3.1`), `governance/specification-audit-programme.md` (`1.16.4`→`1.16.5`), `tests/test_linters.py`, `taxonomy.yml`, `docs/maturity-scorecard.md`, `README.md`, `TODO.md`, `CHANGELOG.md`, `.working/changelog-details/CHANGELOG-detailed.md`, `.working/DONE.md`, `.working/improvement-log.md`, `.working/validate-pr/history.md`.
- **Mechanical baseline (post-merge `run_all_audits.sh`)**: **49/49 all gates passed.**
- **Pre-flight scanner (filtered to touched files)**: 0 candidates.

## Subagent A return

**0 findings (clean).** Subagent A ran all eight failure-mode classes plus six PR-specific high-value checks against the touched files and returned zero in-window defects, with evidence:

1. **Remap semantically sound + internally consistent**: a programmatic sweep of every NIST-column token against `nist_csf_reference.py`'s `CSF_CATEGORIES` returned "Invalid CSF categories still in matrix: NONE". All remap targets (`GV.SC`, `GV.OC`, `PR.PS`, `PR.DS`) are real CSF 2.0 Categories.
2. **No CSF-1.1 residue in the matrix**: grep for `PR.IP|ID.SC|ID.BE|PR.AC|PR.PT|DE.DP` → none.
3. **Spec §6 narrative + linter docstring + actual behaviour agree**: both now state membership IS validated against the 22-Category set (CSWP 29); neither retains "deferred"/"well-formedness only"/"4.6a follow-up". Linter exits 0 with the "well-formedness + Category membership" message.
4. **Reference module**: `len(CSF_CATEGORIES)==22`; per-function `{GV:6,ID:3,PR:5,DE:2,RS:4,RC:2}` sums to 22, matching the docstring.
5. **No stale gate-49 description elsewhere** in the touched corpus docs; residual "deferred" hits are unrelated (gate 43, D3).
6. **No generated-artefact lag**: `build-taxonomy.py --check` in sync; scorecard shows matrix `1.3.1` and spec `1.16.5`.
7. Version bumps present (matrix/spec/README/library); 11 `MatrixControlCodeTests` pass; CHANGELOG-detailed per-line claims (L52/53/64/101/105/113/115/118/130/141/159/161/165) each verify against the post-merge file; TODO §4.6a deletion + DONE rotation correct.

## Cross-reference check

Citers of the touched files were enumerated (`grep -rln`) and the NIST-bearing siblings checked for the same CSF-1.1 residue the PR removed from the matrix. **Two out-of-window findings** (pre-existing CSF-1.1 codes presented under a CSF-2.0 context; not introduced by #326, which only touched the matrix):

### X1 (note, out-of-window) — `risk/standard-third-party-and-supply-chain-risk.md:230`

```
| NIST CSF 2.0 | GV.SC Supply Chain Risk Management; ID.SC Supply Chain Cybersecurity |
```

`ID.SC` is a CSF-1.1 code (became `GV.SC` in 2.0) presented under an explicit "NIST CSF 2.0" framework-reference label. Notable because this is the *same document* whose matrix NIST cell #326 just fixed (matrix line 64 dropped `ID.SC`, keeping `GV.SC`): the forward matrix now says `GV.SC`, but the document's own internal framework-reference table still says `GV.SC ...; ID.SC ...`. Internal inconsistency the matrix correction exposed. The row already carries `GV.SC`, so the fix is to drop the `ID.SC` clause.

### X2 (note, out-of-window) — `operations/procedure-security-monitoring-and-alert-management.md:333, 336, 337`

```
| Alert triage and response | §8.16 | Respond: RS.RP-1 | ... |
| Dashboards and reporting   | §8.16 | Detect: DE.DP-5 | ... |
| Rule tuning and MITRE mapping | §8.16 | Detect: DE.DP-3 | ... |
```

`RS.RP` (CSF-1.1 Response Planning, became `RS.MA` in 2.0) and `DE.DP` (CSF-1.1 Detection Processes, redistributed in 2.0) are CSF-1.1 categories cited in a 2.0-era control-mapping table. Subcategory-suffixed (`-1`/`-5`/`-3`), so they need a per-row 2.0 re-derivation, not a mechanical swap.

**Relation to existing backlog**: `TODO.md:87` **DD-12** already tracks "the corpus-wide `PR.IP`-under-CSF-2.0 convention" migration, but its literal text names only `PR.IP`. X1/X2 are the same class (CSF-1.1 codes under a CSF-2.0 label) for *different* codes (`ID.SC`, `RS.RP`, `DE.DP`), which DD-12's `PR.IP`-only wording does not explicitly cover.

## Orchestrator triage

- **Subagent A in-window**: 0 findings. No action.
- **X1, X2 out-of-window**: pre-existing CSF-1.1 residue in sibling files, not introduced by #326. Corpus-wide CSF-1.1→2.0 migration is a scoped effort already on the backlog as **DD-12**; auto-fixing piecemeal here would be out-of-scope scope-creep, and the `operations` subcategory refs (X2) need semantic re-derivation. **Surfaced to the maintainer with named options** rather than bundled silently. Recommended dispositions: (a) broaden DD-12's wording from "`PR.IP`-under-CSF-2.0" to "all CSF-1.1 residue under a CSF-2.0 label (`PR.IP`, `ID.SC`, `ID.BE`, `RS.RP`, `DE.DP`, `PR.AC`, `PR.PT`, ...)"; and/or (b) extend gate 49's membership check to the sibling NIST-bearing framework tables (the risk/operations mapping tables) so the mechanical gate catches this class beyond the matrix.

## Resulting hot-fix PR

None. X1/X2 are out-of-window and routed to the maintainer (DD-12 family). No hot-fix opened per the recursion-avoidance default; the history row + this record batch into the next PR.

## Notes

- This is NOT a handoff PR; full per-PR QA ran.
- The chat reply surfaces X1/X2 inline for maintainer triage.
- The zero-finding Subagent A result + the two out-of-window cross-reference notes batch (as this row) into the next PR per recursion-avoidance.
