# /validate-pr record, PR #329

## Trigger and state snapshot

- **PR**: #329 "Resume session-start: Sweep 42 close-out + resume bookkeeping". Merge commit `86b9d81` (squash); diff vs `6d3d23c` (#328).
- **Class**: substantive/bookkeeping close-out PR (NOT a session-closing handoff PR), so full per-PR QA applies.
- **State**: post-merge `main` at `86b9d81`. Library `2026.06.308`, pack `1.49.10`, README `1.9.179`. 49 gates.
- **Mechanical baseline**: `tools/run_all_audits.sh` → **49/49 green** post-merge (confirmed on synced main).

## Subagent A return

Two findings (1 warning/Medium, 1 note/Low).

```
tool=subagentA-pr329  ruleId=B-misattributed-csf2.0-relocation-target  level=warning
location=TODO.md:87
fingerprint=todo-L87-dd12-rs.rp-relocation-target-rc.rp-should-be-rs.ma
rubric: class=mis-attributed-citation (b); severity=Medium; gate-caught=NO; window=this-PR (DD-12 broaden)
```
Evidence: the broadened DD-12 item asserted `RS.RP` (→`RC.RP` under CSF 2.0). `tools/nist_csf_reference.py`:80 records `"RS.RP": "CSF 1.1 'Response Planning'; redistributed in 2.0 (mainly RS.MA)"`. `RS.MA` ("Incident Management") is in the **Respond** Function; `RC.RP` ("Incident Recovery Plan Execution") is in **Recover**, a different Function, not the redistribution target. The PR's other new targets check out (`ID.SC`→`GV.SC` per :73; `ID.BE`→`GV.OC` per :71; `PR.AC`→`PR.AA` per :76). Isolated mis-attribution, not systemic. As DD-12 is the forward-looking migration instruction, the wrong target would propagate if executed as written.

```
tool=subagentA-pr329  ruleId=C-changelog-detailed-file-list-underclaim  level=note
location=.working/changelog-details/CHANGELOG-detailed.md:51
fingerprint=changelog-detailed-PR329-omits-session-handoff-from-file-list
rubric: class=multi-surface-incompleteness (c); severity=Low; gate-caught=NO; window=this-PR
```
Evidence: the #329 diff touches `.working/session-handoff.md` (the `:84` step-ref fix + Sweep-42/43 cadence), but the CHANGELOG-detailed structured sections do not enumerate `session-handoff.md`. Borderline under-claim: the handoff refresh is a conventional companion edit of the close-out batch (named in the root lead paragraph + commit message), so expected rather than a silent scope expansion.

Subagent A confirmed CLEAN on every other dimension: version cascade (Library `2026.06.308` + README `1.9.179` + pack `1.49.10` + pack-history Library column all agree; contradiction searches for stale `2026.06.307`/`1.9.178`/`1.49.9`-header/`forty-eight` empty); the guardrail fix at `:93`=`forty-nine`; the Sweep 42 detail/history/`#328`-subsumption rows coherent; the `session-handoff.md:84` fix to "steps 3 and 6" accurate vs live `resume.md`; third-party `1.0.2` + overnight in-flight (gate 46) match their CHANGELOG claims; TODO sweep cursor "Sweep 42" matches the latest history row.

## Cross-reference check

Touched files are bookkeeping/version surfaces; their citers are gate-validated. The README/pack version surfaces are checked by gates 35/39 (green post-merge). The `guardrail-review/SKILL.md` growth-narrative count is free prose, not cited by any other surface (the pack README skills tree references the skill by path/title, not its count). No broken citer surfaced.

## Orchestrator triage

- **A-1 (Medium, in-window)**: REAL, apply-time-verified against `tools/nist_csf_reference.py`:80 (`RS.RP` → mainly `RS.MA`, not `RC.RP`). **Fixed** in PR-2: `TODO.md`:87 changed to "`RS.RP` (→ mainly `RS.MA` under CSF 2.0, per `tools/nist_csf_reference.py`)". This is a from-memory mis-attribution the orchestrator introduced in #329; the apply-time-verify discipline caught it. (Logged to `/retro`.)
- **A-2 (Low note)**: ACCEPTED, not fixed. The handoff refresh is a conventional companion edit named in the #329 root lead paragraph + commit message; retroactively editing a merged CHANGELOG-detailed entry to enumerate it would be a worse (retroactive-entry) smell than the borderline under-claim. Recorded here for the audit trail.

## Resulting hot-fix PR

None separate. The A-1 fix bundles into PR-2 (multi-session codification) per recursion-avoidance, alongside this record + history row + the #329 `/retro` row.

## Notes

The A-1 catch is a clean instance of the research-assistant / apply-time-verify discipline working on the ORCHESTRATOR's own output: a plausible-looking CSF-2.0 relocation target written from memory, caught by the formal `/validate-pr` against the project's own reference module. It reinforces the value of `tools/nist_csf_reference.py` as the single source of truth for CSF relocations and is an argument for the queued gate-49-extension (which would mechanically catch a wrong CSF code in a reference table, though not in a TODO backlog line).
