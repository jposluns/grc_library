# /validate-pr record — PR #336 (Project-governance separation Phase 1)

## Trigger and state snapshot

- **PR**: #336, merge commit `b09375d` (squash; carries the migration commit `7275997` plus the batched #335 QA-rows commit `67a99cd`).
- **Trigger**: mandatory post-merge `/validate-pr` (CLAUDE.md PR-workflow step 5a); NOT a handoff PR, so full formal Subagent A dispatch + cross-reference, no abbreviation.
- **State**: Library `2026.06.315`, README `1.9.186`, pack `1.49.11` (unchanged). 49 numbered gates. Post-merge `tools/run_all_audits.sh` = 49/49 on `main` at `b09375d`.
- **PR substance**: the §8.1 citation-verification cluster migration (6 files `governance/` → `.project-governance/`, new audited dir + README index, 7 staying-corpus citer severs, 2 linter re-points, 9 content-linter additions, generator comment, artefact regen).

## Subagent A return

0 errors, 0 warnings, 1 note. Verbatim summary:

- **§4 one-way dependency (CRITICAL) — CLEAN.** No deliverable corpus `.md` contains a markdown link resolving under `.project-governance/`. The only inbound links are from `CHANGELOG.md` and `TODO.md`, the non-deliverable surfaces the separation spec explicitly carves out (`governance/specification-project-governance-separation.md:86`). The spec names movers only as code spans (zero markdown links).
- **6 moved files — CLEAN.** Repository Path `.project-governance/<self>`; staying-governance links `](../governance/<file>)`; within-cluster links bare; `../tools/` unchanged; all targets resolve; no would-dangle bare basename; Version AND Date bumped (Date 2026-06-25): `1.1.1 / 1.0.4 / 1.0.1 / 1.0.2 / 1.0.1 / 1.0.4`.
- **New `.project-governance/README.md` — CLEAN.** 13-field metadata + 12 trailing `\` line breaks pass; 6 residents indexed with resolving same-dir links; back-links use `../governance/` and `../README.md`; no em/en dashes.
- **7 citer severs — CLEAN.** README, document-index, register-canonical-citations, register-ai-security-tooling-landscape, specification-citation-verification, template-citation-verification-worklist, register-coverage-gaps: mover links removed cleanly (plain-text mentions or an illustrative code span); no empty parens / double separators; grammatical; each Version+Date bumped to 2026-06-25.
- **Index + README row removal — CLEAN.** 6 mover rows gone from both surfaces; staying spec + template rows remain.
- **Linters — CLEAN.** `lint-citation-verification-freshness.py` re-pointed (constant :37 + `--root` :117); `lint-citations.py` Q4 path :116 + DEFAULT_PATHS :144; 9 content linters each carry `.project-governance` (count 1); `build-taxonomy.py` DOMAINS excludes it (11 corpus domains + §7.2 comment); all 11 touched .py compile.
- **Generated artefacts — CLEAN.** Zero mover-basename refs in taxonomy/portal/scorecard; `--check` in sync.
- **CHANGELOG + versions — CLEAN.** Root + detailed both carry the PR #336 entry (lock-step); Library `2026.06.315` / README `1.9.186` consistent; zero em/en dashes in #336 added CHANGELOG lines.

### The 1 note (out-of-window, soft-convention)

`ruleId: changelog-detailed-lead-wording-divergence` — `level: note` — `CHANGELOG.md:9` vs `.working/changelog-details/CHANGELOG-detailed.md:11`. The root and detailed #336 lead paragraphs are semantically identical (same Phase-1 headline, same spec reference) but not verbatim-identical in their opening sentence. The change-tracking two-file-split workflow prefers matching lead wording. Cosmetic only: not gated (no audit checks lead verbatim equality), lock-step modification (the D1 delta gate's actual requirement) IS satisfied.

## Cross-reference check

Covered by Subagent A's §4 grep and citer verification. The 6 moved files' former citers were all enumerated and severed (the migration's own scope); no OTHER corpus file references the moved files by their old `governance/` path. The 7 staying citers' Version bumps are not quoted cross-file (no downstream doc pins their versions). No broken or stale cross-reference surfaced.

## Orchestrator triage

- **§4 + all 8 areas**: clean, confirmed by both the orchestrator's direct §4 grep (empty) and Subagent A. No action.
- **The 1 note**: in-window (my own PR wrote both leads) but cosmetic and ungated. Triage: **route to `/retro` as a candidate, do not fix.** Editing a just-merged CHANGELOG-detailed lead to align wording is low-value retroactive churn; the lock-step requirement (what D1 enforces) holds, and both leads convey the same headline. Recorded here and in the history row per the no-discount discipline (routed, not dropped).

## Resulting hot-fix PR

None. The 1 note requires no fix. The history row + this record + the #336 `/retro` row batch into the next PR per recursion-avoidance.

## Notes

This was the highest-stakes change in recent history (a structural migration touching 35 files with a §4 direction-rule invariant the broken-link gate cannot enforce). The clean result reflects the research-map + apply-time-verification + dual-runner discipline: a read-only research subagent enumerated every edit surface, the orchestrator verified each against live files before applying, and both `run_all_audits.sh` (49/49) and `run-pr-time-checks.sh` (D1/D2/D3 + 45/40/31) gated the push. The §6.3-vs-§7.3 spec-scope gap was resolved stricter+evidenced (9 content linters given `.project-governance`) and surfaced to the maintainer in the CHANGELOG-detailed design note for review.
