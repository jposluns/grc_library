# PR-Scoped Validation, PR #340

## Trigger and state snapshot

- **PR**: #340 ("Sweep 46 close-out: correct gate-10 README-cadence overstatement"), the Sweep 46 close-out (NOT a handoff PR; full per-PR QA applies).
- **Merge commit**: `269ab7d94bb23c0c20fafc46291cb994a70beff8` (squash).
- **State**: library `2026.06.319`, pack `1.49.11`, README `1.9.190`; 49 gates, 10 rules, 15 skills, 8 commands.
- **Post-merge mechanical baseline**: `tools/run_all_audits.sh` → **49/49** green (after clearing the untracked `tests/tmp/` fixture leftovers).
- **Touched files**: `tests/test_linters.py`, `.working/session-handoff.md`, `.working/changelog-details/CHANGELOG-detailed.md`, `.working/validate-sweeps/history.md`, `.working/validate-sweeps/2026-06-25-sweep46-iter1.md`, `.working/overnight-pr.md`, `CHANGELOG.md`, `README.md`, `TODO.md`.
- **Pre-flight (filtered to touched files)**: 0 candidates (the 9 corpus-wide candidates are all in `dev-security/claude-rules/`, outside the touched set).

## Subagent A return

**1 finding (in-window, warning).**

```
tool: subagent-A
ruleId: gate10-readme-cadence-overstatement-residual-carrier
level: warning
location: tests/test_linters.py:1594
fingerprint: gate10-readme-cadence-overstatement:tests/test_linters.py:assertion-message
labels: [K],[in-window]
evidence: The assertion failure-message string (lines 1594-1595) read: "gate 10 must scan
.project-governance so its README annual review and the register's 12-month cadence are
scheduled." This is the SAME overstatement PR #340 set out to eliminate. It is materially
false: tools/check-review-cadence.py:100-101 (`if f.name == "README.md": continue`) makes
iter_active_docs skip every README, so .project-governance/README.md's "annually" cadence is
NOT gate-10-scheduled. The string was introduced in #338 and not touched by #340's edits
(which corrected the docstring 17 lines above, session-handoff.md:108, and the #338
CHANGELOG-detailed bullet). It is a FOURTH carrier in the same touched file and the same test
method, missed by the PR's own stated "find-every-carrier" discipline. Dormant (the test
passes because .project-governance IS in DOMAINS, so the message never renders); no runtime
or corpus effect. Raised as warning (not note) because it is a self-contradicting in-file
residual of the exact overstatement this PR claimed to fully correct under a named
completeness discipline.
```

**Verified clean (0 findings) on all remaining checks**: the corrected docstring (1568-1580) accurately describes gate 10; the third-carrier correction in the #338 CHANGELOG-detailed bullet is accurate with no new overstatement; version cascade consistent (README `2026.06.319`/`1.9.190`; CHANGELOG root+detailed #340 leads match; history.md `2.0.39`); #340 root-CHANGELOG link coverage clean (the 4 real file paths linked; bare `tests/`, `.working/`, `.project-governance/`, `/resume`, `/validate` are directory prefixes / slash commands, not file-path code spans); session-handoff.md:108 fix accurate; no stale references, generated-artefact lag, or term drift in the touched set.

## Cross-reference check

- **README.md version bump**: grep for the old library version `2026.06.318` as a live "current" claim → none outside `.working/` + `CHANGELOG.md` (append-only history, expected). No corpus doc hardcodes the README's specific version as current.
- **`tests/test_linters.py`**: not cited by any corpus document (it is a test module); no citer to break.
- **CHANGELOG / `.working/` files**: cited by path link only (README, change-tracking rule, handoff); no quoted-text or version citation broken by #340.
- **Regression suite** after the assertion-message fix: `python3 tools/run-linter-regression.py` → "Ran 146 tests ... OK".

## Orchestrator triage

- **Finding `gate10-readme-cadence-overstatement-residual-carrier`** (in-window, warning, dormant): **confirmed at apply-time** (orchestrator re-read `tests/test_linters.py:1591-1596` and `tools/check-review-cadence.py:100-101`). The finding is real: a fourth carrier of the same overstatement, in the assertion message of the very test method #340 edited. The substance is dormant (no runtime/corpus effect, the message never renders) but it contradicts the corrected docstring 17 lines above and makes the PR's own "find-every-carrier" CHANGELOG claim inaccurate.
- **Action**: fixed at apply-time (the assertion message now reads "gate 10 must scan .project-governance so the register's 12-month re-verification cadence is scheduled (gate 10 skips all READMEs by design, so the directory's README cadence is not scheduled here)"). Per recursion-avoidance, the fix + this row bundle into the **next PR (FR-167 batch 4)**, not a dedicated hot-fix PR.
- **Discipline note (for `/retro`)**: the find-every-carrier discipline was invoked but applied to file-and-line surfaces (docstring, handoff line, CHANGELOG bullet) without scanning the *same method's other strings*. The carrier was a non-comment string (an assertion message) two statements below the corrected comment. Lesson: when correcting a prose overstatement, grep the full enclosing unit (function/method/section) for the offending phrase, not just the named line; a `grep -n "README annual"` across the touched file would have caught all four carriers at once.

## Resulting hot-fix PR

None (per recursion-avoidance, the fix bundles into the next PR, FR-167 batch 4, alongside this row). Source #340 → closing PR = the next merged PR.

## Notes

The Sweep 46 corpus-wide `/validate` (the parent activity) was substantively clean (Subagents B and C 0 findings; §7.4 completeness verified across all 11 content linters). This `/validate-pr` on the Sweep 46 close-out then caught a fourth carrier of the same overstatement that the close-out itself missed: the per-PR QA layer doing exactly its job (catching an orchestrator-side incompleteness the corpus-wide sweep's diff-scoped Subagent A had flagged at the file level but the orchestrator's fix under-applied). No corpus, gate-behaviour, version, or count defect at any layer.
