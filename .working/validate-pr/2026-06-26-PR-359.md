# Validate-PR record: PR #359 (2026-06-26)

## Trigger and state snapshot

- **PR**: #359, "Add AUDITED_DOMAIN_DIRS single source of truth; refactor 11 content linters to derive from it".
- **Merge commit**: `6608b2c` (squash onto `main`).
- **State at run**: Library `2026.06.338`, README `1.9.209`, pack `1.49.15`, spec `1.16.7`; 51 gates, 10 rules, 15 skills, 8 commands.
- **Mechanical baseline**: `tools/run_all_audits.sh` reports 51/51 green on the post-merge `main` state.
- **Pre-flight (touched-file filtered)**: 0 candidates in the touched files (the 9 standing candidates are all in the trust-recovery / deep-qa / guardrail SKILLs, none touched by #359).
- **Touched files**: `tools/lint_common.py` plus 11 refactored linters (`check-review-cadence`, `lint-citations`, `lint-document-date-staleness`, `lint-filename-title-alignment`, `lint-language`, `lint-links`, `lint-metadata-line-breaks`, `lint-metadata`, `lint-roles`, `lint-shall-near-uncertainty`, `lint-standards-currency`); `CHANGELOG.md`, `.working/changelog-details/CHANGELOG-detailed.md`, `.working/validate-sweeps/history.md`, `README.md`, `TODO.md`.

## Subagent A return

**1 finding (note, in-window).**

- `stale-comment-future-gate-present-tense` (note, in-window), fingerprint `stale-comment-future-gate-present-tense:tools/lint_common.py:67`. All 12 touched Python files carry a present-tense comment asserting that "the directory-scan-scope parity gate (`tools/lint-scan-scope-parity.py`) enforces" / "the scan-scope parity gate forbids hardcoding" the domain run, but that gate file does not exist in the merged state (it is PR 2). The authoritative narrative surfaces (commit message, both CHANGELOG entries) correctly frame the gate as future work, so this is a class-(g) documentation-accuracy slip in the in-code comments, not a behavioural defect. Carriers: `lint_common.py:67`, `check-review-cadence.py:43`, `lint-citations.py:140`, `lint-document-date-staleness.py:128`, `lint-filename-title-alignment.py:79`, `lint-language.py:206`, `lint-links.py:94`, `lint-metadata-line-breaks.py:72`, `lint-metadata.py:445`, `lint-roles.py:67`, `lint-shall-near-uncertainty.py:86`, `lint-standards-currency.py:60`.

**Verified clean (0 behavioural findings):**

- **Set-preservation across all 11 linters**: Subagent A reconstructed each linter's pre-refactor scan set from `6608b2c^` and confirmed `missing` and `added` are both empty post-refactor (citations 17, date-staleness 20 retaining `docs`, metadata-line-breaks 22 retaining `docs`, roles 16, shall-near-uncertainty 14, standards-currency 17, filename-title-alignment 12, check-review-cadence 12, plus lint-language and lint-links function-local lists confirmed by direct diff with lint-links keeping its `tools`/`docs` extras). Live `importlib` probe corroborated.
- **`AUDITED_DOMAIN_DIRS` correctness**: exactly 12 entries (11 domain dirs, all present on disk, plus `.project-governance`); docstring accurate including the `docs`-exclusion rationale.
- **CHANGELOG accuracy**: root and detailed #359 entries list exactly the 11 refactored linters; the "five gained the import" claim verified correct.
- **Version and artefacts**: README bumped Library `2026.06.337` to `.338` and README `1.9.208` to `.209`; CHANGELOG version chain monotonic; no taxonomy/portal/scorecard touched (correct, no corpus doc metadata changed).
- **Sweep 51 row and TODO cursor**: internally consistent; history Version `2.0.43` to `2.0.44`.

## Cross-reference check

Citers of the touched linter scripts were checked in the spec, `.claude/`, `dev-security/`, `docs/`, and `tests/` surfaces.

- **One out-of-window prose-imprecision surfaced**: `governance/specification-project-governance-separation.md:195` (§7.3) still describes the explicit-allow-list linters as selecting scan roots "from a hardcoded list of domain directories plus repository-root meta files (a `DEFAULT_SCAN_PATHS` or `DOMAINS` constant)". Post-#359 those linters derive from the shared `AUDITED_DOMAIN_DIRS` rather than a hardcoded list. The §7.4 obligation prose (the linters must include `.project-governance`) still holds because the splat includes it. Out-of-window (the spec was not touched by #359). Fold into PR 2, which revises the §7.3/§7.4 narrative when it adds the parity gate.
- **`tests/test_linters.py`** (the #338 scope-coverage regressions at `:1593` `mod.DOMAINS` and `:2278` `mod.DEFAULT_SCAN_PATHS`) still reference the constants the refactored linters expose; gate 36 green confirms they pass (the splat flattens to a tuple/list still containing `.project-governance`).
- No broken cross-references; the linter script paths the spec §6 inventory cites are unchanged.

## Orchestrator triage

- **A-1 (present-tense gate comments), in-window, note/Low**: resolves in PR 2 by construction. PR 2 adds `tools/lint-scan-scope-parity.py`, which makes the present-tense "the parity gate enforces / forbids hardcoding" comments true. Per recursion-avoidance the resolution is the next PR (PR 2), which is the immediate next action this session; no softening churn and no dedicated hot-fix PR. (Had PR 2 not been imminent, the comments would have been softened to future tense.)
- **Cross-ref (§7.3 "hardcoded list" prose), out-of-window**: fold the spec-narrative update into PR 2 (it revises §7.3/§7.4 to describe the single-source-of-truth model and the new gate).

## Resulting hot-fix PR

None. Both items fold into PR 2 (the parity-gate PR) per recursion-avoidance; PR 2 is the immediate next substantive PR this session.

## Notes

The single in-window finding is the natural artefact of splitting the groundwork (PR 1) from the gate (PR 2): the refactor comments forward-reference the gate the next PR adds. The split itself is sound (behaviour-preserving refactor separated from the behaviour-adding gate, per the always-split-when-in-doubt discipline); the cost is one transient present-tense overclaim that the next PR retires.
