# /validate-pr record: PR #383 (FR-167 batch 7, compliance domain)

## Trigger and state snapshot

Post-merge PR-scoped validation of **PR #383** (merge commit `e79f699`), FR-167 batch 7: the [`compliance/matrix-grc-compliance-alignment.md`](../../compliance/matrix-grc-compliance-alignment.md) Compliance section expanded from 5 rows to 30 (25 net-new), matrix `1.6.0` to `1.7.0`. Library `2026.06.362`, pack `1.49.19`, 54 numbered gates, 10 governance rules, 15 skills. Post-merge mechanical baseline: `tools/run_all_audits.sh` **54/54 green** at `e79f699`. Single-iteration cycle.

## Subagent A return (PR-diff-scoped deep review)

**1 finding (0 error, 0 warning, 1 note).**

- ruleId `title-type-disagrees-with-document-type-metadata`, level note, at `compliance/matrix-grc-compliance-alignment.md:93`. The row-93 Document-Title cell is "Template: Compliance Obligations Register", giving the "Type:" word as "Template", but the source [`compliance/register-compliance-obligations-template.md`](../../compliance/register-compliance-obligations-template.md) carries `Document Type: Register`. The other 24 new rows track the source `Document Type` field for the "Type:" prefix; this is the sole disagreement. The source is a register-template hybrid (metadata title "Compliance Obligations Register Template"), so the cell took the type word from the title's trailing word rather than the Document Type field. Subject, path, and all codes are correct; only the type-word source is inconsistent.

All other checks clean across the 25 rows: all well-formed (11 cells); all 25 link targets resolve; 24/25 titles faithful; every CSF code valid CSF 2.0 with none in `CSF_1_1_RELOCATED`; every CCM code in `CCM_V41` with no AICM-only code in the CCM column; every ISO code a valid 2022 Annex A control or §4-§10 clause (gates 48/49 pass); trade columns coherent (non-customs docs N/A, logistics/customs and the legal-and-regulatory policy grounded; WCO uniformly "Pillar II", zero "Pillar I"); no code/description mismatch; `lint-document-control-codes.py` reports 0 CSF-1.1 carriers corpus-wide.

## Cross-reference check

20 corpus files cite the matrix as a related-document by path; a grep for the new compliance-row source basenames confirms citers reference the matrix by path (related-doc / index / reverse-crosswalk), none quote compliance-row cell values. Adding rows changes no existing row, so no citer breaks. Clean.

## Orchestrator triage

The single note is **in-window** (PR #383 introduced it). Severity note (cosmetic convention inconsistency; no broken link, no invalid code, subject faithful). Per the recursion-avoidance rule, the fix bundles into the **next PR (FR-167 batch 8, governance)**, which edits the same matrix file. Fix: change row 93 to "Register: Compliance Obligations Template" (Type word tracking the `Document Type: Register` field, matching the convention the other 24 rows follow).

## Resulting hot-fix PR

None (per recursion-avoidance, the fix bundles into batch 8, not a dedicated hot-fix PR).

## Notes

Convention point confirmed for future batches: the matrix "Type: Subject" Document-Title cell takes its Type word from the source's `Document Type` metadata field, not from the title's leading/trailing type word. For register-template / hybrid-type documents the two can differ; track Document Type.
