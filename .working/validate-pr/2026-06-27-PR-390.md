# /validate-pr: PR #390

## Trigger and state snapshot

- **PR**: #390, "Gate 49: enforce CCM-vs-AICM catalogue separation in the matrix CCM v4.1 column".
- **Squash commit**: `07ce6d4` on `main`.
- **Post-merge mechanical baseline**: `tools/run_all_audits.sh` 54/54 green at `07ce6d4`.
- **Library** `2026.06.369`, **README** `1.9.240`, **audit-spec** `1.16.13`, gate count **54** (unchanged; this is a scope tightening of existing gate 49, not a new gate).
- Non-handoff PR: full Subagent A dispatch + cross-reference check (no abbreviation).

## Subagent A return

0 errors / 0 warnings / 1 note. Verified clean: (1) docstring/section-5/section-6/code agree on the new CCM-column catalogue-membership check, no surviving "CCM out of scope" stale prose in either the gate docstring or the spec; (2) module split correct (`CCM_DOMAINS` 17, `AICM_DOMAINS` 18 with `MDS` the extra, `VALID_DOMAINS` union unchanged at 18; `is_aicm_only('MDS-01')` True, `is_aicm_only('GRC-01')` False); (3) gate 48 unaffected (exit 0; all imported symbols `VALID_DOMAINS`/`ALL_TITLES`/`CCM_V41`/`AICM_V11`/`KNOWN_BAD_DOMAINS` present); (4) the 3 new regression tests present and `test_ccm_column_not_policed_here` gone, `MatrixControlCodeTests` 13/13; (4b) gate 49 exit 0 on the live matrix; (5) version cascade consistent; (6) generators in sync (`build-taxonomy.py --check` and `build-portal.py --check` both OK); (7) Sweep 60 history row present (0 findings) and TODO cursor at Sweep 60.

The one **note** (level: note, not error/warning):

- **ruleId**: `changelog-prepush-placeholder-in-merged-record`
- **location**: `.working/changelog-details/CHANGELOG-detailed.md:25`
- **evidence**: the #390 detailed entry's Verification section carried a future-tense pre-push placeholder, "Post-commit `tools/run_all_audits.sh` 54/54 and pre-push `tools/run-pr-time-checks.sh` to be confirmed green before push." The PR merged green (re-confirmed: gate 49 exit 0, gate 48 exit 0, `MatrixControlCodeTests` 13/13, both generators in sync), so the future-tense wording is now an inaccurate artefact in the merged audit-trail record.
- **classification**: in-window (introduced by #390), gate-exempt `.working/` cosmetic wording.

## Cross-reference check

- No corpus document cites the old spec version `1.16.12` (grep clean across `*.md` excluding CHANGELOG/`.working/`/version-history).
- The only "gate 49" + "not re-checked" grep hit is the spec's own new gate-49 narrative, which is the correct updated framing (CCM title accuracy stays gate 48's job; CCM-column membership is now checked). No stale "CCM out of scope" framing survives anywhere.
- Citers of `specification-audit-programme.md` reference it by path (not by version/line content), so the Version bump and narrative edit break no citer.

## Orchestrator triage

- The single note is in-window and cosmetic (a gate-exempt working-state wording artefact). Per recursion-avoidance it is corrected in the next PR (PR2, the scratch-content validation work), not in a dedicated hot-fix PR: the Verification line is reworded from the future-tense placeholder to the confirmed post-merge state.
- No error/warning findings; no hot-fix PR required.

## Resulting hot-fix PR

None. The note's fix and this validate-pr row batch into the next PR (PR2) per recursion-avoidance.

## Notes

- This PR was a scope tightening of existing gate 49 (no new gate number); four-surface parity unchanged at 54.
- The crib and every code in the paused FR-167 batch-10 research were cross-validated against the scratch `ref/standards/` source extracts as part of this work; that cross-check is what surfaced the CCM/AICM-confusion hole this PR closes.
