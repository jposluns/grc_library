# `/validate-pr` record, PR #404 (FR-167 batch 11, privacy domain)

## Trigger & state snapshot

- **PR**: #404, FR-167 batch 11 (privacy-domain compliance-matrix expansion).
- **Merge commit**: `9d0507b8fdcc53c1d9d7c948d8940c64f1ca71d7` (squash).
- **Post-merge mechanical baseline**: `tools/run_all_audits.sh` **54/54 green**.
- **Library** `2026.06.383`, **compliance matrix** `1.11.0`, **pack** `1.50.0`, gate count **54**, 10 rules, 16 skills, 9 commands.
- **Touched files**: `compliance/matrix-grc-compliance-alignment.md` (the `## Privacy domain` section 2 to 42 rows + Version `1.10.0` to `1.11.0`), regenerated `taxonomy.yml` + `docs/maturity-scorecard.md`, `CHANGELOG.md` + `.working/changelog-details/CHANGELOG-detailed.md` (#404 entries), `README.md` (version bumps), `TODO.md` (FR-167 line + new P3 source-doc item), `.working/validate-sweeps/history.md` (batched Sweep 65 row).

## Subagent A return

Full diff-scoped review. **2 findings** (both in-window, both gate-blind):

- **Finding 1 (warning, `garbled-edit-duplicated-text`, `TODO.md:51`)**: the FR-167 line's "AICM-column follow-up (surfaced by batch 10, future maintainer decision)" sentence (a 361-char span ending "Whether to add a dedicated CSA ") was duplicated when the batch-11 progress note was appended, because the orchestrator's edit `new_string` re-included a sentence that was still present in the unchanged remainder of the line. Verified introduced by #404: the distinctive phrase appears once in `git show 9d0507b^:TODO.md` line 51 and twice post-merge. Prose duplication is gate-blind.
- **Finding 2 (note, `changelog-overstated-changed-file`, `CHANGELOG.md:9` + `.working/changelog-details/CHANGELOG-detailed.md` #404 Changed bullet)**: the #404 entries said "taxonomy/portal/scorecard regenerated ... to reflect the matrix Version bump", but `docs/portal.md` is not in the PR diff and carries no matrix Version (`build-portal.py --check` exits 0, in sync). Minor accuracy imprecision: portal was re-run by the generator but unchanged.

Everything else verified clean: all 42 Privacy rows well-formed (exactly 11 content cells), all 42 `../privacy/...` path targets exist (incl. all 25 jurisdiction annexes), 9 sampled Document-Title cells match source `**Document Title:**` metadata, all 22 distinct CCM v4.1 codes valid (no AICM-only), all 9 NIST CSF 2.0 categories valid, 8 ISO Annex A codes + 4 clause refs follow convention, semantic spot-check across ~8 rows found no wrong-control mapping (DSP-09/DSP-10 on the corrected PIA row are on-the-nose), trade columns N/A across all 42 except the breach-response BASC cell (intentional, genuine nexus), the 2 corrected existing rows now read N/A, version cascade consistent (matrix 1.11.0, README Library 2026.06.383 / README 1.9.254, scorecard 1.11.0 no lag, CHANGELOG headers descend 404/403/402), the 3 routed source-doc defects (`PRI-05`/`PRI-04`/`SEF-02`) genuinely exist in their source files.

## Cross-reference check

Citers of `compliance/matrix-grc-compliance-alignment.md` checked. **0 findings**: no citer enumerates a Privacy-section row/document count or the matrix's domain list; `docs/maturity-scorecard.md:62` correctly carries matrix `1.11.0` (in sync); the `1.10.0`/`1.11.0` hits in `dev-security/claude-rules/README.md` are unrelated *pack* versions; `governance/register-document-index-and-classification.md` lists privacy documents independently (correct raw titles, no matrix dependency); the "All 42 documents" string in `TODO.md:51` is the FR-167 task-description text (describing the old summary to soften), not a live stale reference. The matrix is cited by path as a related document; the 40 new rows stale no citer.

## Orchestrator triage

Both findings are **in-window** (introduced by #404) and **fixed in the working tree**, batched into the next PR per recursion-avoidance (no dedicated hot-fix PR):

- Finding 1: the 361-char duplicated span deleted from `TODO.md:51`; verified the phrase now occurs once and the junction reads "...closing whole-matrix `/matrix-fit`). **AICM-column follow-up ...** ... Whether to add a dedicated CSA AICM v1.1 column to the matrix...".
- Finding 2: root `CHANGELOG.md` reworded to "taxonomy and scorecard regenerated (the matrix Version does not surface in the portal, which is unchanged)"; the detailed-mirror Changed bullet reworded to list only `taxonomy.yml` + `docs/maturity-scorecard.md` as regenerated, noting `docs/portal.md` was re-run but unchanged and `build-portal.py --check` confirms sync.

No out-of-window findings. The 3 source-doc-prose code defects Subagent A confirmed are already routed to the new `TODO.md` Priority-3 item (not a new finding; expected from the batch's worker research).

## Resulting hot-fix PR

None. Both fixes batch into the next PR (the next FR-167 partial-section batch or the session-closing handoff), per the recursion-avoidance rule.

## Notes

- The matrix is now comprehensive across all **11 per-domain batches** (architecture, risk, dev-security, supply-chain, resilience, operations, compliance, governance, security, AI, privacy). Remaining FR-167 work: the matrix's partial sections, then matrix completion + the closing whole-matrix `/matrix-fit`.
- Pre-existing soft spot (NOT a #404 regression): the 2 original privacy rows keep the older "Type: Name" Document-Title format ("Policy: Privacy and Data Governance", "Procedure: Privacy Impact and Cross-Border Transfer") while the 40 new rows use raw `**Document Title:**` metadata (the current convention); the matrix already mixes these conventions across older/newer sections and no gate enforces title format. Title-convention harmonization is a separate optional sweep, not in batch-11 scope.
