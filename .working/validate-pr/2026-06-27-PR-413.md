# /validate-pr record: PR #413 (2026-06-27)

## Trigger and state snapshot

PR #413 (squash `e2edc92`), the TODO-hygiene pass (resume-metadata relocation to `session-handoff.md` + gate-45 sweep-cursor re-point + FR-167 batch rotation to DONE). Post-merge baseline: `tools/run_all_audits.sh` **54/54** at `e2edc92`/#413. Library `2026.06.392`, README `1.9.263`, pack `1.50.0`, matrix `1.11.1`. Counts 54 gates / 10 rules / 16 skills / 9 commands. Full Subagent A diff-scoped dispatch + cross-reference check (no abbreviation).

## Subagent A return

**2 in-window findings (both Low/warning, the same root cause: the relocation left parallel surfaces describing the old state).** All gate-45 internal-correctness, cursor-currency, FR-167-consistency, removed-section, version, and generated-artefact checks verified clean.

- **A-1 [stale-gate-45-description-in-citer] warning, `governance/specification-audit-programme.md:145` (§6) and `:72` (§5).** The §6 narrative still says gate 45 "scans `TODO.md` for two recurring drift patterns" including the sweep-cursor pattern; the §5 summary likewise frames the sweep-cursor check as scanning TODO. After #413 the sweep-cursor check scans `.working/session-handoff.md` (`SWEEP_CURSOR_FILE`), not TODO. #413 updated the linter docstring + the TODO preamble but missed this parallel spec surface. Gate-blind (gate 35 checks name/script parity, not narrative prose), adopter/auditor-facing (Public corpus doc). **In-window.** Fix: update §6:145 + §5:72 to say the sweep-cursor check reads `.working/session-handoff.md`; the spec carries `Date: 2026-06-27`, so the body edit needs a Version + Date co-bump.
- **A-2 [stale-relocation-claim] note, `.working/design-decisions.md:233`.** Asserts in the present tense that "the 'Session resume metadata' subsection in `TODO.md` is intentionally frozen"; #413 relocated that subsection to `session-handoff.md`, making the claim false. Subagent A classified it out-of-window (the file pre-existed #413), but the staleness was introduced BY #413's relocation, so it is treated here as an in-window parallel-surface miss. Gate-exempt working state (no version bump). Fix: update the claim to reflect the relocation (the cursor now lives in `session-handoff.md`; TODO is forward-looking only).

## Cross-reference check

Citers of `lint-todo-staleness.py`: `specification-audit-programme.md` (→ A-1), TODO.md (preamble accurate), CHANGELOG (accurate). Citers of `session-handoff.md`: TODO.md (§4.23 + preamble, past-tense-correct), `.claude/CLAUDE.md` + `.claude/commands/resume.md` (no dangling reference to the removed TODO section), CHANGELOG. Corpus-wide grep for "Session resume metadata": the only stale present-tense claim is `design-decisions.md:233` (A-2); all other references are correctly past-tense. FR-167 trimmed TODO bullet and the consolidated DONE entry are mutually consistent (11 batches #275..#404, same remaining scope, no double-claim).

## Orchestrator triage

Both findings are in-window (consequences of #413's own relocation that left two parallel surfaces stale) and **bundle into the next PR** per recursion-avoidance (no dedicated hot-fix PR). A-1 needs a spec Version + Date co-bump; A-2 is a working-state prose fix. Both are small and mechanical. Root cause: the close-out "grep the OLD phrasing across every sibling surface before commit" discipline was not run thoroughly for the gate-45 behavior description (docstring + TODO preamble were updated; the spec §5/§6 and design-decisions surfaces were missed). Routed to the #413 `/retro` as a multi-surface-incompleteness lesson.

## Resulting hot-fix PR

None (bundled into the next PR, whatever its substantive purpose, per recursion-avoidance).

## Notes

The miss is the textbook multi-surface-incompleteness class the close-out checklist's grep-the-old-phrasing line exists to prevent; when re-pointing a gate's BEHAVIOR (not just its name), the grep target is every prose surface that DESCRIBES the behavior (linter docstring, TODO preamble, audit-programme §5/§6, design-decisions), not only the docstring. The QA layer caught it; the grep would have caught it first.
