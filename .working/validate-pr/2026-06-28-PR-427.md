# /validate-pr record: PR #427 (2026-06-28)

## Trigger & state snapshot

Post-merge PR-scoped validation of PR #427 ("P3: correct five mis-cited CSA CCM codes in two privacy procedures"), merge commit `129c3de` (squash). Library `2026.06.405`, pack `1.51.0`, 54 gates, 11 governance rules. Post-merge mechanical baseline: `tools/run_all_audits.sh` **54/54** on `main` at `129c3de`. Touched files: the two privacy procedures (corpus), `taxonomy.yml` + `docs/maturity-scorecard.md` (generated), CHANGELOG root + detailed, README, TODO, `.working/` (DONE, improvement-log, validate-pr/history).

## Subagent A, diff-scoped review

**In-scope: 0 findings (clean).** All five corrected codes verified: `PRI-04` to `DSP-11` (both surfaces of the DSR procedure, lines 29 + 374); `PRI-05` to `SEF-08` and `SEF-02` to `SEF-06` (the breach-response procedure, line 29 prose + lines 345/354 table). Zero residual `PRI-04`/`PRI-05`/`SEF-02` in either file. Replacement codes confirmed `is_ccm_v41==True` with context-fitting titles (DSP-11 "Personal Data Access, Reversal, Rectification and Deletion"; SEF-08 "Security Breach Notification"; SEF-06 "Event Triage Processes"). Per-document Version + Date co-bumped (breach-response 1.4.16, DSR 1.5.1, both 2026-06-28). Generated artefacts in sync (`build-taxonomy.py --check` / `build-portal.py --check` exit 0).

**Out-of-window: 4 findings (1 finding class, the same invalid-`PRI-*` defect #427 fixed, persisting in sibling privacy files).** All `is_ccm_v41==False`, gate-blind, pre-existing (NOT introduced by #427):
- `privacy/policy-privacy-and-data-governance.md:144/145/147/149/151`: 5 PRI codes (PRI-01..PRI-05) in the framework-mapping table.
- `privacy/charter-privacy-management-programme.md:218`: the range "CSA CCM v4.1 PRI-01 through PRI-07".
- `privacy/procedure-privacy-impact-and-cross-border-transfer.md:263`: PRI-05 on a PIA row.

## Cross-reference check

Citers of the two #427-edited procedures (30 files) reference the procedures by path, not by the specific control codes; the code corrections break no citer. Clean. The out-of-window finding above came from the sibling-file scan of the same defect class, not from a citer.

## Orchestrator triage

The out-of-window finding was apply-time re-verified (corpus-wide grep for `PRI-0[0-9]` + `is_ccm_v41` confirmation: 7 distinct citations across 3 files, all invalid). Surfaced to the maintainer with named options. Per the standing "fix issues before new content" directive and the fix-first/correctness class, resolved by **fixing it immediately in PR #428** (research-then-apply, same pattern as #427): 3 unambiguous mappings applied (PRI-04 to DSP-11, breach PRI-05 to SEF-08, PIA PRI-05 to DSP-09); 4 judgement-call rows surfaced with recommendations, the graceful-degradation timer fired with no answer, recommended evidence-backed defaults applied (GRC-06 / DSP-12 / DSP-10 / charter Option A) and recorded in [`pending-decisions.md`](../pending-decisions.md) for maintainer confirm/redirect.

## Resulting hot-fix PR

PR #428 (the domain-wide PRI-* correction) absorbs both this record's out-of-window fix and the batched #426/#427 QA rows, per recursion-avoidance.

## Notes

The trust-recovery routing that created the #427 backlog item named only 2 documents; the actual invalid-`PRI-*` class was domain-wide (10 citations across 5 privacy files total, counting #427's 2 procedures). The `/validate-pr` sibling-file cross-check is what surfaced the broader scope. Reinforces the existing find-every-carrier / corpus-wide-grep discipline applied to control-code defect classes (grep the whole domain for the same class before closing), not a new mechanism.
