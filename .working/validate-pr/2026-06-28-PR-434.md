# `/validate-pr` record: PR #434

## Trigger and state snapshot

Post-merge `/validate-pr` for PR #434 (squash commit `97b4532`, "Codify 'never assert a state you cannot observe' + reference-version-currency SOP"). PR #434 codified the un-observable-state discipline family: the new `## Un-observable state, inventory, and external-version currency` subsection in both `evidence-grounded-completion.md` copies, the new `## Reference-version currency` section and wind-down sharpening in `.claude/CLAUDE.md`, the `resume.md` scratch-index-load + currency-order additions, the `multi-session-orchestration.md` §6 version-currency subsection, the pack README 1.51.0->1.51.1 bump, plus bookkeeping.

Mechanical baseline: **54/54 gates pass** (`tools/run_all_audits.sh` standalone on post-merge `main` at `97b4532`).

15 touched files: `.claude/CLAUDE.md`, `.claude/commands/resume.md`, `.claude/rules/governance/evidence-grounded-completion.md`, `.working/DONE.md`, `.working/changelog-details/CHANGELOG-detailed.md`, `.working/improvement-log.md`, `.working/multi-session-orchestration.md`, `.working/pending-decisions.md`, `.working/session-handoff.md`, `.working/validate-pr/history.md`, `CHANGELOG.md`, `README.md`, `TODO.md`, `dev-security/claude-rules/README.md`, `dev-security/claude-rules/governance/evidence-grounded-completion.md`.

## Subagent A return

1 in-window WARNING; 7 of 8 failure-mode classes clean.

- **ruleId c (multi-surface / same-paragraph incompleteness), level warning, `.claude/commands/resume.md:25`.** Evidence: the PR-added sentence "the index, not a guessed directory path, is authoritative for location (MITRE, for example, lives under `ref/frameworks/`, not `ref/standards/`)" sits in the same paragraph as the pre-existing enumeration "`ref/standards/` (one directory per issuing body, NIST / CSA CCM-AICM-CAIQ / ISO / MITRE / OWASP, ...)". The enumeration lists MITRE under `ref/standards/` while the corrective sentence says MITRE lives under `ref/frameworks/`: the paragraph contradicts itself. The correction's explicit purpose is the standards-vs-frameworks location distinction, so the un-updated enumeration directly undercuts it. Class-(c) shape; the project's close-out "grep the OLD phrasing across the full changed file before commit" bullet targets exactly this.

Verified clean (with quoted evidence in the subagent return): (a) stale prose references; (b) mis-attributed citations (`register-canonical-citations.md:169` correctly identifies the ATLAS row; #433 batched-row cross-refs accurate); (c) other multi-surface (the two `evidence-grounded-completion.md` copies are byte-identical per `diff -q`; pack README version-history row matches metadata Version 1.51.1; CLAUDE.md eleven-rule enumeration matches reality; root-vs-detailed CHANGELOG lead divergence matches established practice); (d) inferred-as-verified (the live ATLAS finding is grounded in the CHANGELOG-detailed Verification chain, not memory); (e) per-document version-bump omission (pack-rule files carry no per-doc Version block; all metadata-bearing `.working/` files bumped); (f) generated-artefact lag (no corpus-document metadata changed); (g) stale docstrings (no Python touched); (h) cross-document term drift (the `ref/frameworks/` correction is applied consistently in CLAUDE.md, the handoff, and the runbook; the only un-updated instance is the resume.md:25 enumeration above).

## Cross-reference check

Targeted check on citers of the touched files: clean. Pack README bumped 1.51.0->1.51.1; the only `1.51.0` occurrences elsewhere are historical "added in pack 1.51.0" statements (`.claude/CLAUDE.md:843`, `dev-security/claude-rules/README.md:66`), which remain correct (the eleventh rule was indeed added at 1.51.0; 1.51.1 is a patch). No file claims 1.51.0 as the *current* version. `resume.md` and `multi-session-orchestration.md` §6 citers checked; no quoted-step or section reference broke (the PR added content; it did not rename or remove headings).

## Orchestrator triage

The resume.md:25 finding was re-read at apply-time and confirmed (the enumeration and the corrective sentence are both in the paragraph at line 25). Classified **in-window** (PR #434 introduced the contradiction by adding the corrective sentence without updating the same-paragraph enumeration). Fixed in this batching PR: MITRE removed from the `ref/standards/` enumeration, leaving "NIST / CSA CCM-AICM-CAIQ / ISO / OWASP", so the paragraph agrees with its own authoritative-location sentence. Surgical one-line edit; `resume.md` carries no metadata Version field, so no per-document bump applies.

## Resulting hot-fix PR

No dedicated hot-fix PR. Per recursion-avoidance, the fix and this record's history row batch into the next PR (the MITRE ATT&CK/ATLAS register currency-fix PR).

## Notes

The finding is the same multi-surface/same-file incompleteness shape the close-out checklist's grep-OLD-phrasing bullet is meant to catch; the bullet was not applied to a *pre-existing same-paragraph* enumeration when the corrective sentence was added. No new mechanism is proposed (same-paragraph self-contradiction is not mechanically gate-checkable); the `/retro` row records the reinforcement.
