# `/validate-pr` record: PR #436

## Trigger and state snapshot

Post-merge `/validate-pr` for PR #436 (squash commit `9d26372`, "TODO §4.23 hygiene pass (a+b) + batched #435 QA rows + ATLAS decision close"). Working-state-only PR: TODO §4.23 parts (a) (trim §4.11 to its residue) + (b) (delete shipped/closed clauses), §4.23(c) filed in Priority 7, batched #435 `/validate-pr` (0) + `/retro` rows, recorded the maintainer's "defer the scratch half" answer in pending-decisions.

Mechanical baseline: green (CI-green merge of #436; corpus byte-identical to the pre-merge 54/54 tree).

7 touched files: `.working/changelog-details/CHANGELOG-detailed.md`, `.working/improvement-log.md`, `.working/pending-decisions.md`, `.working/validate-pr/history.md`, `CHANGELOG.md`, `README.md`, `TODO.md`.

## Subagent A return

1 in-window WARNING; 7 of 8 failure-mode classes clean.

- **ruleId c (multi-surface incompleteness) / h (cross-document term drift), level warning, `TODO.md:358`.** Evidence: the Backlog-totals summary line `- **P7 (awaiting decision)**: 0 pending + 2 dropped-decision audit-trail entries.` contradicts the same file's Priority-7 intro at `TODO.md:321` (`One open decision pending (§4.23(c), below).`). PR #436 filed §4.23(c) as a new open decision in Priority 7 (updating the intro and adding the bullet) but left the paired Backlog-totals count at "0 pending". Same count-on-one-surface-stale-on-the-paired-surface shape the close-out checklist's grep-the-OLD-phrasing line targets; introduced by this PR.

Verified clean (with quoted evidence in the subagent return): (a) stale prose references / dangling cross-references from the deleted clauses (the §4.18 `#366` deletion and §4.11 trim strand nothing; the only residual `#366`/`P4.17` mention is the §4.23 description of what was deleted); (b) mis-attributed citations (PR numbers and versions consistent across CHANGELOG / pending-decisions / improvement-log / validate-pr-history); (c) other multi-surface (version cascade `2026.06.413->.414`, README `1.9.284->.285`, both #435 QA rows present and consistent); (d) inferred-as-verified (the #435 retro row owns its inference slip rather than presenting it as verified); (e) per-document version-bump omission (README bumped; TODO/`.working/` are not per-document-Version-metadata docs); (f) generated-artefact lag (no corpus metadata changed); (g) stale docstrings (no Python touched); (h) cross-document term drift (apart from the finding, the §4.23 "Remaining (open): (c)" framing matches the Priority 7 entry; pending-decisions Status "0 pending" matched its body at the time).

## Cross-reference check

Clean. The deleted shipped/closed clauses left no dangling cross-references; greps for `#366` / `P4.17` / `D4 shipped` return only the intended §4.23 description-of-what-was-deleted prose. No corpus document cites the touched `.working/` / TODO files.

## Orchestrator triage

The `TODO.md:358` finding was re-read at apply-time and confirmed (`:321` "One open decision pending" vs `:358` "0 pending"). Classified **in-window** (PR #436 introduced the contradiction). Fixed in this batching handoff PR: `TODO.md:358` changed to "1 pending (§4.23(c)) + 2 dropped-decision audit-trail entries." `TODO.md` carries no per-document Version field, so no per-document bump applies.

## Resulting hot-fix PR

No dedicated hot-fix PR. Per recursion-avoidance, the fix and this record's history row batch into the next PR (this session-closing handoff PR).

## Notes

The finding is the same multi-surface count-parity shape the close-out checklist targets; it was introduced by the very §4.23(c) filing that updated the Priority-7 intro, an instance of "update one surface of a paired count, leave the sibling stale". The Backlog-totals block self-describes as approximate / drift-prone, which is why the gate does not catch it; the close-out grep is the guard.
