# PR #443 PR-scoped validation record

## Trigger and state snapshot

Post-merge `/validate-pr` for PR #443 (squash commit `0697249`, "DD-10: correct addyosmani spot-scanned skill count (18 to 19)"). PR #443 corrected the addyosmani spot-scanned count from 18 to 19 across the addyosmani vetting surfaces, grounded by the upstream `addyosmani/agent-skills` repo holding 24 skills (web-verified 2026-06-28) and the vetting-log's own explicit 19-item list. Post-merge mechanical baseline: 54/54 (`run_all_audits.sh` green at `0697249`). Touched files: `vetting-log.md`, pack `README.md`, `setup-generator-prompt.md`, root `README.md`, `TODO.md`, `.working/DONE.md`, `CHANGELOG.md` + detailed mirror, and the batched #442 QA rows in `improvement-log.md` / `validate-pr/history.md`.

## Subagent A return

**1 HIGH finding (in-window).**

- **ruleId c (multi-surface incompleteness), HIGH, `dev-security/claude-rules/vetting-log.md:80`.** The vetting-log carries the addyosmani spot-scanned count on THREE lines, not two: line 57 (the "remaining skill directories" enumeration), line 78 (the Verdict), and line 80 (the Status field). PR #443 corrected lines 57 and 78 to 19 but missed line 80, which still read "the remaining 18 skill directories". A live published vetting-record surface (not the frozen historical CHANGELOG), so the undercount the PR set out to eliminate persisted on that line, and the same document's Status (18) contradicted its corrected Verdict and list (19). Evidence quote: `**Status:** `Vetted` for the fully-vetted subset above; `Spot-scanned` for the remaining 18 skill directories.`

Seven of eight classes clean (with quoted evidence in the return): the bare-token corpus scan confirmed the only other surviving "18 spot-scanned"/"18 remaining" hits are the new #443 narrative (correct usage) and the frozen 2026-06-19 original-vetting CHANGELOG-detailed entry (deliberately not retroactively edited); the factual core verified (vetting-log:57 enumerates exactly 19 directories; 5 fully-vetted skills + 19 = 24 = the upstream total; LICENSE is not a skill); version bumps complete (vetting-log 1.3.2 + Date 2026-06-28; pack README 1.51.3 + paired history row; root README CalVer + Version co-bumped); no Python touched; no em/en dashes in added lines.

## Cross-reference check

The root-cause of the miss: the orchestrator's apply-time contradiction-search in #443 was phrasing-specific (`grep "18 spot-scanned\|18 remaining"`), which does not match line 80's word order ("remaining 18 skill directories"). A bare-token scan (`\b18\b` in the addyosmani section) would have caught it. This is the bare-token-vs-phrasing-specific-search lesson (TODO 4.8 item a) recurring.

## Orchestrator triage

**In-window, fixed in PR #444** (the next PR, per recursion-avoidance; not a dedicated hot-fix PR). The fix: `vetting-log.md:80` "remaining 18 skill directories" to "remaining 19 skill directories", with the vetting-log metadata `Version` re-bumped 1.3.2 to 1.3.3 (the body changes again). The merged #443 CHANGELOG entries keep their as-shipped wording (no retroactive edits); the #444 CHANGELOG notes the completion.

## Resulting hot-fix PR

None dedicated. The fix bundles into PR #444 (gate-50 check-4 build) per the recursion-avoidance batching rule.

## Notes

Apply-time verification confirmed the finding by reading `vetting-log.md:80` directly and an exhaustive bare-`18` scan of all three live surfaces (the only other bare `18` is README:437 "18 domains", an unrelated CSA CCM domain count, correctly untouched). The finding is the QA cadence working as designed: the formal Subagent A dispatch caught a real in-window miss the orchestrator's phrasing-specific grep let through.
