# /validate-pr record, PR #449

## Trigger and state snapshot

- **PR**: #449 "FR-167: scope the CSA AICM v1.1 column in audit-matrix-semantic-fit.py"
- **Squash commit**: 4c632ec (merged into `main`)
- **Library / pack / counts at HEAD**: library `2026.06.427`, README `1.9.298`; 54 gates / 11 governance rules / 16 skills / 9 slash commands (unchanged by this PR).
- **Mechanical baseline**: `tools/run_all_audits.sh` standalone on post-merge `main` = all 54 gates pass.
- **Touched files (7)**: `tools/audit-matrix-semantic-fit.py` (substantive); `CHANGELOG.md`, `.working/changelog-details/CHANGELOG-detailed.md` (#449 entries); `README.md` (version cascade); `TODO.md` (FR-167 remaining); `.working/validate-pr/history.md`, `.working/improvement-log.md` (batched #448 QA rows + Version bumps).
- **Pre-flight scanner**: 0 candidates in the touched-file set.

## Subagent A return

**0 findings across the 7 touched files.** Subagent A verified, each with result:

1. `--self-test`: exits 0, 7 tests OK (including the two new AICM cases).
2. AICM title truth: `AICM_V11['MDS-02'] == 'Model Artifact Scanning'` matches both new self-test comments and the `assertEqual`; `AICM_V11['MDS-01'] == 'Training Pipeline Security'` confirms the detailed-CHANGELOG discipline-observation (the apply-time title-fabrication self-catch) is accurate, not fabricated.
3. Multi-surface enumeration completeness: every title-source enumeration that names CCM/CSF now also names AICM (docstring lines 42-45, `KNOWN_TITLES` comment, `run()` narrative "CCM v4.1 / AICM v1.1 / CSF 2.0"); no parallel enumeration left omitting AICM.
4. CI-wiring invariant: `audit-matrix-semantic-fit` has zero hits in `run_all_audits.sh`, `quality.yml`, `.pre-commit-config.yaml`, and the spec-audit-programme; the tool stays advisory.
5. Worklist count: `--matrix-only` prints 62 rows, matching the "62 rows" claim in root CHANGELOG, detailed mirror, and TODO.
6. Version cascade: README `2026.06.427` / `1.9.298`; root + detailed both head with #449/`2026.06.427`; detailed cascade line agrees; `.working` bumps `1.2.232` / `1.0.182` match the detailed entry's `→` references.
7. Superset term-drift: the only #449/#447-introduced `superset` hits are negating ("not a 'superset'"); other hits are pre-existing, unrelated entries.
8. Generated-artefact lag: `build-taxonomy.py --check` and `build-portal.py --check` both exit 0; the PR did not touch the matrix.
9. Per-document version bump: README + the two `.working` registers bumped; the tool carries no per-document Version field (gate-exempt).
10. Stale docstring: the docstring correctly enumerates AICM_V11 and the new `scan_matrix` AICM-column behaviour.

## Cross-reference check

Citers of the substantive touched file (`tools/audit-matrix-semantic-fit.py`) were checked for line-number anchors that the diff shifted. The PR added 3 lines to the module docstring (the AICM title-source enumeration), shifting the "ISO 27001:2022 codes are not assessed" text from line 52 to line **54**.

Three citers reference `:52`:
- `TODO.md:67` (live, forward-looking): **FINDING (in-window, see triage).**
- `.working/changelog-details/CHANGELOG-detailed.md:778`: frozen-state archive (moment-in-time record), left verbatim per the skill's frozen-archive rule.
- `.working/validate-pr/history.md:55`: frozen-state archive, left verbatim.

No citer quotes the tool's row content in a way the diff broke; the only impact is the one stale line anchor below.

## Orchestrator triage

**Finding F-1 (in-window, Low/note).**
- ruleId: (a) stale prose reference (line-anchor drift)
- location: `TODO.md:67`
- evidence: `([`tools/audit-matrix-semantic-fit.py`](tools/audit-matrix-semantic-fit.py):52 "ISO 27001:2022 codes are not assessed ...")`
- rubric: PR #449's 3-line docstring addition shifted the cited text from line 52 to 54; the live backlog item's `:52` anchor is now stale.
- classification: **in-window** (the line shift is a direct side-effect of this PR's docstring edit).
- disposition: **fixed in this sweep** (`TODO.md` `:52` → `:54`); the fix batches into the next PR (the standardization track) per recursion-avoidance. The two `.working/` `:52` refs are frozen-state archives and are correctly left verbatim.

## Resulting hot-fix PR

None. Per recursion-avoidance, the one-line `TODO.md` anchor fix batches into the next PR (the high-assurance-verification standardization track), not a dedicated hot-fix PR.

## Notes

The finding is a textbook line-anchor-drift case: a forward-looking citer referencing a line number that a later edit to the cited file shifted. It reinforces the general fragility of `:line` anchors into frequently-edited files, but the fix here is the cheap correct one (`:52`→`:54`). The frozen-archive distinction held: the same stale `:52` in two `.working/` records is correctly NOT touched (moment-in-time artefacts).
