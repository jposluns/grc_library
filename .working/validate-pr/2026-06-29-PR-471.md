# /validate-pr record: PR #471 (close TODO §4.6 as satisfied by gate 50 Check 1)

## Trigger and state snapshot

- **PR**: #471 (merged, squash `057fd16`), "Close TODO §4.6 (QA-cadence enforcement) as satisfied by gate 50 Check 1".
- **Diff**: 9 files, bookkeeping-only, no corpus-content change: [`TODO.md`](../../TODO.md) (§4.6 section deleted; 4 cross-refs reworded to "the bookkeeping-parity gate family, gates 50 and 57, plus the D5 PR-time check"; surviving `4.6` token de-`§`-ed; P4 count 8->7), [`.working/DONE.md`](DONE.md) (#471 row), [`.working/pending-decisions.md`](pending-decisions.md) (§4.6 resolved + S3 park), [`.working/validate-pr/history.md`](history.md) (`1.2.252`->`1.2.253`; carries the batched #470 row), [`.working/improvement-log.md`](../improvement-log.md) (`1.0.200`->`1.0.201`; carries the batched #470 retro row), [`.working/session-handoff.md`](../session-handoff.md) (live cursor refresh), [`CHANGELOG.md`](../../CHANGELOG.md) + [`.working/changelog-details/CHANGELOG-detailed.md`](../changelog-details/CHANGELOG-detailed.md) (terse #471 entry), [`README.md`](../../README.md) (library `2026.06.448`->`.449`, README `1.9.319`->`.320`).
- **Mechanical baseline**: `tools/run_all_audits.sh` **57/57 green at `057fd16`** (post-merge `main`).
- **Pre-flight scanner**: no high-recall candidates in the touched-file set.

## Subagent A return

Refute-briefed, scoped to the 9-file diff, across the eight failure-mode classes. **In-window: CLEAN across all eight classes** (a stale-refs, b mis-attributed citations, c multi-surface incompleteness, d inferred-as-verified, e version-bump omission, f generated-artefact lag, g stale docstrings [no `.py` touched], h cross-document term drift). Specific verifications all passed: `grep '§4\.6'` in `TODO.md` = ZERO (intra-doc-ref gate satisfied); `grep '### 4\.6'` = ZERO (heading deleted); P4 count "7 subsections (4.1-4.5, 4.7-4.8)" correct (headings 4.1-4.5, 4.7, 4.8 all exist, 4.6 absent); gate 50 Check 1 confirmed real and IS the QA-cadence missing-row parity check (`tools/lint-bookkeeping-parity.py:27-48`); DONE / CHANGELOG-root / CHANGELOG-detailed #471 entries agree on substance, PR number, date, and library version `2026.06.449`.

**One out-of-window finding (A-1)** and **one refuted hypothesis** (see triage).

## Cross-reference check

For the touched surfaces, which live (non-frozen, non-`.working`) files cite the closed §4.6:

- **`.claude/CLAUDE.md:297-298`** (gate-exempt but live): "the convention-level companion to the queued **P4.6 mechanical QA-cadence gate**; **until that gate exists**, the checklist is the guard." CONFIRMED stale: §4.6 closed as satisfied with no new gate, so "queued ... gate" / "until that gate exists" misdescribe the state. (Subagent A A-1, re-verified.)
- **`tools/lint-bookkeeping-parity.py:13`** (live source, gate-blind on §-refs): "co-designed with the queued **§4.6** (QA-cadence) and **§4.10** (TODO/DONE rotation) items". CONFIRMED stale: §4.10 closed in #468/#469 (this half was already stale, the #469 cross-ref cleanup missed this cross-FILE citer), §4.6 closed in #471. Lines 27 ("the §4.6 surface") and 66 ("the §4.6 #376 surface") are dangling back-ref labels (lower priority; design-provenance, not a "queued" misstatement).
- **`tools/lint-todo-staleness.py`**: Subagent A cited a "queued §4.6" gate-45 docstring here (from the #469 history row). **REFUTED at apply-time**: `grep '4\.6'` returns ZERO; the file's "queued" hits are the unrelated queued-*PR*-number check (`check_queued_pr`), not a §4.6 reference. Not routed.

## Orchestrator triage

- **In-window findings: 0.** The 9-file diff is internally clean and self-consistent.
- **Out-of-window findings: 2 confirmed** (the same class: closing a numbered TODO §-section leaves cross-FILE "queued §N" references stale in non-TODO live surfaces). Low severity, gate-blind (57/57 green), mechanical to fix:
  1. `.claude/CLAUDE.md:297-298`: reword the "queued P4.6 gate / until that gate exists" to point at gate 50 Check 1 as the shipped mechanism (the checklist remains the convention-level companion for the non-mechanizable abbreviated-marker half).
  2. `tools/lint-bookkeeping-parity.py:13` (and optionally 27, 66): reword "the queued §4.6 ... and §4.10 ... items" to past tense (both shipped/closed: §4.6 = this gate's Check 1, §4.10 = gate 57 + D5).
- **Refuted: 1** (`lint-todo-staleness.py`, no §4.6 reference exists).
- **Resolution**: bundle the 2 cross-file fixes into the **next PR** (the standard `/validate-pr` out-of-window resolution). The next queued item is the **§4.8 apply-time-discipline codifications**, one candidate of which is exactly "closing a numbered TODO §-section needs a §N cross-ref cleanup pass" (these 2 carriers are the live instances), and the codification should be **extended to cover cross-FILE citers** (CLAUDE.md, tool docstrings), not only intra-TODO §N refs. The #469 miss (the §4.10 half of `lint-bookkeeping-parity.py:13`) is the 2nd-occurrence evidence that intra-TODO-only cleanup is insufficient.

## Resulting hot-fix PR

None (no dedicated hot-fix; the 2 out-of-window fixes bundle into the next PR per recursion-avoidance).

## Notes

- The Subagent A false positive (`lint-todo-staleness.py`) is the routine apply-time-verification catch: a subagent finding is a hypothesis until the orchestrator re-reads the cited source. Subagent A inferred the carrier from the #469 history row rather than reading the file.
- This PR carried the batched #470 `/validate-pr` + `/retro` rows. PR #471's own `/validate-pr` (this record + its history row) and `/retro` row batch into the next PR.
