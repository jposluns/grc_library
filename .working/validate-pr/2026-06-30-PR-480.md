# /validate-pr record: PR #480

## Trigger and state snapshot

Post-merge PR-scoped validation sweep on PR #480 (squash commit `a5d7293`, "FR-32: Legitimate Interest Assessment (LIA) template (Article 6(1)(f))"). Run during the assembly of PR #481 (FR-145 AI-standards scope/precedence note + crosswalk), into which this row and the fix batch per recursion-avoidance. Mechanical baseline: all 57 gates green on the post-#480 state. Library Version at trigger: 2026.06.458.

## Subagent A return

Subagent A (diff-scoped, 17 touched files including the new `privacy/template-legitimate-interest-assessment.md`) returned **one in-window finding**:

```
tool:     validate-pr-subagent-A
ruleId:   house-style-ise-orthography / gate-2-pattern-gap
level:    note
location: privacy/template-legitimate-interest-assessment.md:74
evidence: line 74 carried British "recognises" in the same sentence as Canadian "recognized"
          ("Recital 47 recognises certain interests ... naming the interest within a
          recognized category"). The internal inconsistency is the tell that "recognises"
          is an unintended slip, not a deliberate Commonwealth choice. Gate-blind:
          tools/lint-language.py ISE_PATTERN lists "recognise|recognised|recognising" but
          not the third-person "-ises" inflection, so the pattern did not match and gate 2
          passed (full suite 57/57 green). Both harness verifiers also passed over it.
```

Verified clean (no other findings): the LIA is listed consistently across all surfaces (taxonomy.yml, privacy/README.md:45 after DPIA, governance/register-document-index-and-classification.md:121, docs/portal.md, docs/maturity-scorecard.md); every GDPR citation (Art 6(1)(f) quote, the second-subparagraph exclusion, Recital 47/48/39, Art 5(1)(c), Art 9, Art 21 incl. 21(4), Art 5(2), Arts 13/14) accurate and internally consistent; the framework-alignment row correctly scopes "attaches to 6(1)(f); 21(1) also covers 6(1)(e), outside scope"; EDPB Opinion 28/2024 registered and correctly cited (not the unregistered Guidelines 1/2024); the full 13-field metadata block matches the DPIA sibling and all 7 Related Documents links resolve; version bumps internally consistent (LIA 1.0.0, privacy/README 1.2.3, register 1.27.41, README Library 2026.06.458 / README 1.9.329); `build-taxonomy.py --check` and `build-portal.py --check` clean; FR-32 TODO->DONE rotation complete (0 TODO residual, DONE keyed #480); CHANGELOG root/detailed lock-step; the #479 QA rows well-formed (no welded/duplicate rows) and the new `2026-06-30-PR-479.md` record internally consistent. (g) no Python touched; (b) no mis-attributed citations.

## Cross-reference check

The LIA basename is cited only in its three intended surfaces (the document-index register, privacy/README, CHANGELOG); all resolve. No external citer is broken by this PR.

## Orchestrator triage

Finding 1 validated at apply-time (re-read LIA:74; confirmed `recognises` adjacent to `recognized` in the same sentence). In-window (introduced by the new LIA file in #480). Fixed in PR #481: `recognises` -> `recognizes` (LIA Version 1.0.0 -> 1.0.1, Date 2026-06-30 co-bumped). The note also surfaced a gate-2 linter pattern gap (ISE_PATTERN lacks the `-ises` inflection); queued as a separate tooling follow-up in TODO (a gate-logic change is not bundled into this content PR per always-split-when-in-doubt, and gate changes warrant their own focused PR + regression fixture).

## Resulting hot-fix PR

No dedicated hot-fix PR; the one-word fix bundles into PR #481 (FR-145) per recursion-avoidance.

## Notes

The finding is the value of the post-merge sweep working exactly as designed: a slip that the gate-2 linter (pattern gap) and both high-assurance harness verifiers (focused on completeness and citation accuracy, not orthography) passed over was caught by the independent diff-scoped read. Low-severity, no correctness or legal impact.
