# /validate-pr record: PR #554 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #554 (the guardrail-review TODO intake + #553 QA batch + #553 finding fixes). Merge commit `23aebf3` on `main`. Mechanical baseline post-merge: **59/59 gates green**. Pre-flight scanner surfaced one new candidate in the touched set (TODO GR-P5's "the three rules that carry it"), routed to the subagent for triage. Library 2026.07.42; README 1.9.403. Note: a substantive-tier refute-verifier ran two iterations on this PR pre-push (a wrong line pointer and a soft commit count, then a residual descriptor mismatch; all fixed pre-push), so this sweep's value-add was the post-merge state and what the diff-scoped verifier could not see.

## Subagent A return

Verbatim (findings; the verified-clean inventory is summarized in the triage):

> ### Finding 1
> - tool: A
> - ruleId: imprecise-carrier-count-in-todo-item
> - level: note
> - location: TODO.md:325 (GR-P5, closing clause)
> - fingerprint: carrier-count:TODO.md:325:exception-register-three-rules
> - rubric: (a) stale/imprecise prose reference (pre-flight candidate, triaged)
> - evidence: "Also hoist the exception-register 'absent means strict mode' disclosure to the top of the three rules that carry it."
>
> Split verdict. At the mention level the count VERIFIES: exactly 3 governance rules contain "exception register" (gate-discipline.md:144/:148, change-tracking.md:262/:271, artefact-and-branch-discipline.md:152). But the hoistable disclosure itself ("If the project does not have an exception register, the exception is not available") exists in only 2 of the 3; artefact-and-branch-discipline:152 is a cross-reference with no strict-mode sentence of its own to hoist. In-window (line authored by #554).
>
> ### Finding 2
> - tool: A
> - ruleId: stale-rule-trio-in-claude-md
> - level: note (OUT-OF-WINDOW; not touched by #554)
> - location: .claude/CLAUDE.md, Boundaries section
> - fingerprint: wrong-enumeration:claude-md-boundaries:exception-register-trio
> - rubric: (b) mis-attributed citation
> - evidence: "The three pack rules that reference 'the project's exception register' (gate-discipline, change-tracking, evidence-grounded-completion)..."
>
> Surfaced by the Finding-1 grep: evidence-grounded-completion.md contains ZERO exception-register mentions; the actual third mentioner is artefact-and-branch-discipline. Route to the GR-P5 implementation PR (it will touch this exact surface).

Subagent A's verified-clean inventory: full 10-file diff read; bare-token stale-version greps clean (no live surface claims 2026.07.41/1.9.402 current); #554's own "zero stale carriers remaining" Verification claim TRUE at HEAD (bare-token `SR-5`/`NEEDS-UPSTREAM` scans, every hit read to full cell extent; all four handoff carriers correct, no fourth carrier); CHANGELOG root/detailed paired with identical #554 headers, root lead-paragraph-only, claims match the diff, PR-number prediction correct; QA-row/record correspondence exact; Version bumps present (1.0.278, 1.2.332) and none owed elsewhere (headers confirmed field-free); Backlog totals P3=10/P4=7 match the counted headings; GR-1/GR-7 pointer spot-checks accurate; gates 45 and 50 pass standalone (exit 0, with #553 demoted from highest-numbered and its rows present); no corpus doc touched, no .py touched.

## Cross-reference check

Included in the Subagent A dispatch. No broken citation post-merge.

## Orchestrator triage

Both findings re-verified at source by the orchestrator (the two rules' exception-handling sections read; the CLAUDE.md Boundaries line read; the grep count reproduced).

- Finding 1 (note, in-window): FIXED in the next PR (#555): GR-P5's clause now states the disclosure sentence exists in gate-discipline and change-tracking only, artefact-and-branch-discipline carrying a cross-reference.
- Finding 2 (note, out-of-window, protected file): ROUTED, not fixed tonight. The `.claude/CLAUDE.md` Boundaries line's wrong trio is recorded inside the GR-P5 TODO item (which will touch that exact surface), flagged as a protected-file factual touch to log when executed. Deferred per the overnight protected-file discipline; the graceful-degradation routing is the record.

## Resulting hot-fix PR

None (batching rule: the finding-1 fix ships in #555 alongside its substantive purpose, the GR-2 regression-test batch).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
