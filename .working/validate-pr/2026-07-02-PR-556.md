# /validate-pr record: PR #556 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #556 (GR-3 wave 1 + GR-4 tilde half: shared metadata parser, gate-31 fail-loud migration, tilde-fence toggle, spec clause amend, 7 tests). Merge commit `d39c079` on `main`. Baseline post-merge: 59/59 gates green; regression suite 254 OK; gates 45/50 standalone exit 0; both generators check-clean. Library 2026.07.44; README 1.9.405.

## Subagent A return

One finding (verbatim):

> **F1, Low (note): the "338/338" census denominator is not reproducible at HEAD.**
> Carriers: CHANGELOG.md:109 ("338/338 identical, no set differences"), .working/changelog-details/CHANGELOG-detailed.md:30, .working/overnight-pr.md:33.
> Evidence: independent re-run of the old-regex-vs-new-parser diff over gate 31's exact post-filter target set at HEAD: 334/334 agreement, zero old-only/new-only files. No natural scope yields 338 (post-filter 334, +EXEMPT_FILES 336, pre-filter 391, taxonomy 295). The load-bearing claim (zero blast radius, no disposition change) is verified TRUE by the reproduction; only the denominator is a soft count (the #443/#554 soft-count class, here in shipped CHANGELOG prose).

Verified-clean inventory: retired-DATE_RE residual scan clean (every bare-token hit read: the retirement comment, other linters' own same-named symbols, frozen entries); spec section-5 clause matches the code path exactly; tilde and unbalanced-fence censuses reproduced at zero; suite delta +7 matches; root/detailed #556 headers identical with monotonic version sequence; version bumps complete (spec 1.16.29 + Date, ledgers 1.2.334/1.0.280, no field-bearing surface missed); docstrings match new code; TODO residual texts verified at source including the verifier caveats (the leading-whitespace narrowing, gate 31 now stricter than gate 1 for an empty Date); `parse_metadata_block`/`parse_iso_date` consumers = gate 31 + tests only (the followup-ageing linter's same-named private helper is pre-existing, not a consumer); no consumer parses gate 31's OK line; pre-flight candidates all pre-existing pack-file shapes outside the touched set.

## Cross-reference check

Included in the dispatch (helper-consumer and OK-line-consumer greps above). No broken citation.

## Orchestrator triage

F1 re-verified at source: the orchestrator's pre-push blast-radius scan ran `iter_markdown_targets` over the FULL repo tree (338 files with a Date in both parsers), not gate 31's post-filter scan set (334); both scans show zero disagreements, so the substance holds and the denominator was scope-underspecified. In-window disposition:

- The live carrier (`overnight-pr.md` ledger line) reworded in #557 to state both scopes explicitly.
- The shipped #556 CHANGELOG entries are NOT retro-edited; the correction of record is the note in #557's entry.
- Class accounting: this is the soft-count class's THIRD in-window occurrence tonight (the "41 commits" GR-5 draft, the eighteen/246 count sweep, now the 338 denominator), formally meeting the GR-P3 three-occurrence threshold; recorded in the #556 `/retro` row, with GR-12 (residual-scan tooling) and GR-P3 (escalation convention) already queued as the durable fixes.

## Resulting hot-fix PR

None (batching rule: the fix ships in #557 alongside the GR-1 gate-59 ordering extension).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
