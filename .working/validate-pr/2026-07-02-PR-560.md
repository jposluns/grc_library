# /validate-pr record: PR #560 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #560 (C1: the risk methodology's residual-risk formula reconciled to the canonical re-scored Residual likelihood x Residual impact model; section 1.6 item 1 rotation; the #559 QA rows). Squash commit `c44d6f5` on `main`. Baseline post-merge: 59/59 gates green at push time; both generators check-clean per Subagent A. Library 2026.07.48; README 1.9.409.

## Subagent A return

Three findings, all Low (verbatim summaries):

> **F1, Low: `TODO.md`:46 (section-1.6 item 2) carries a line pointer staled by this PR's own edit.** The item cites the methodology at ":145 (\"require ERC approval for High and Critical risks\")", but the 4.3 replacement added two net lines above section 6, so the sentence now lives at :147; :145 is now the 6.2 treatment-plan line. The PR edited the same section-1.6 block (header, intro, item 3) without refreshing item 2's pointer. The companion :38 pointer is still correct.

> **F2, Low: the new 4.3 citation "per the risk-register field model in `risk/standard-enterprise-risk-management.md` Section 7" is a bare code span, not a markdown link.** The reciprocal citation in the standard is linked; this file's metadata block links every reference; the bare span matches only the pre-existing 6.1 style at :134. Gate-blind (the link-coverage gate targets root CHANGELOG.md only). If fixed, fix :134 in the same edit.

> **F3, Low (pre-existing, out-of-window): the root and mirror lead paragraphs do not share the same wording.** The change-tracking pack rule's two-file-split workflow says the root file carries "the same lead-paragraph wording as the detailed entry"; headers match exactly across every entry, wording does not, and every prior entry has the identical divergent shape. A standing repo-convention-vs-pack-rule drift surfaced by the check, not a #560-introduced defect.

Verified-clean inventory: new 4.3 internally consistent with sections 4.1/4.2/5/6/7/8; the Section 7 citation accurate at source (`risk/standard-enterprise-risk-management.md`:161-164 carries the Strong / Adequate / Weak / Untested field model and the re-scored residual fields); corpus-wide discount-model residual scan zero across all corpus dirs; 30+ citers of the methodology contradict nothing; Version 1.2.0 + Date 2026-07-02 present and taxonomy/scorecard match with both generators check-clean; section-1.6 header count matches the remaining items; DONE/history/improvement-log rows internally accurate (the #559 "13 files" count independently reproduced from `git show 5250cc4 --stat`); CHANGELOG root/mirror headers identical, no em/en dashes in added lines, root path spans all linked; README CalVer and Version co-bumped.

## Cross-reference check

Included in the dispatch (citer enumeration plus residual-formula contradiction sweeps). No contradicting citer; closest lines (`risk/template-operational-risk-register.md`:193, `compliance/procedure-control-testing.md`:51) are consistent with re-scoring.

## Orchestrator triage

All three findings re-verified at source before disposition (the :147 sentence, the two bare spans, the divergent lead pairs quoted from live files).

- **F1 (in-window):** resolved structurally in the next PR (C2 closes section-1.6 item 2 and deletes the bullet carrying the stale pointer; no pointer refresh needed).
- **F2 (in-window):** fixed in the next PR (C2 edits the same file: both :109 and :134 spans become markdown links, per the finding's same-edit instruction).
- **F3 (out-of-window):** ROUTED as a convention-reconcile note into the GR-P2 rule-condense TODO item (that work edits the change-tracking rule; the reconcile decision, align the rule text to the long-standing practice or vice versa, is taken there, defaulting to documenting the practice).

## Resulting hot-fix PR

None (batching rule: F1/F2 ship in the C2 PR alongside the section-1.6 item 2 fix).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
