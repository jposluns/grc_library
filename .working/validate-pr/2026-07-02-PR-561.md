# /validate-pr record: PR #561 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #561 (C2: the risk methodology's two pre-FR-171 acceptance-authority lines realigned to the canonical Executive Committee or Board Risk Committee chain with the ERC reviewing and recommending; section 1.6 item 2 rotation; the two #560 in-window Lows fixed; the #560 QA rows). Squash commit `d1e9abc` on `main`. Baseline post-merge per Subagent A: both generators check-clean. Library 2026.07.49; README 1.9.410.

## Subagent A return

One finding, Low, out-of-window (verbatim summary):

> **F1, Low (out-of-window, cross-reference check): two un-routed acceptance-authority carriers in `governance/procedure-grc-programme-management-and-annual-review.md` sit uneasily beside the realigned canonical chain, and #561's completion-scan pattern was too narrow to see them.** :262 "the residual risk must be formally accepted in writing by the CIO or above" (satisfiable by a CIO-alone signature for a High or Critical residual, against the canonical Executive Committee or Board Risk Committee authority); :266 "Residual risks accepted at ERC level are reported in the annual GRC programme report" (asserting an ERC acceptance level the role register does not grant). Why missed: the #561 scan pattern was `ERC` + `approv` + `acceptance`; :266 carries the word-form "accepted" and no "approv", so the pattern could not return it (the pattern-width failure the CLAUDE.md bare-token checklist line warns about). The literal retired phrasings do have zero live carriers, so the shipped root-CHANGELOG claim is accurate as worded; these two lines strain the broader realigned-authority reading, not the shipped claim.

Verified-clean inventory: full 12-file diff matches the declared scope with no undeclared change; the methodology carries no residual ERC-approves line (:126 escalation, :159 reporting are in-model); the new roles-table row is a sound composition of the register grant and policy Section 2.2; all three 6.3 targets exist and say what 6.3 attributes; all added links resolve; Version 1.2.1 + Date 2026-07-02; both generators check-clean; 25 methodology citers contradiction-free; TODO section 1.6 header/intro/single-bullet consistent; the GR-P2 and section-2.13 routed notes quote their sources accurately (verbatim checks against the change-tracking rule and the PIA procedure); the #560 record/history/retro rows mutually consistent with all three dispositions matching what #561 actually shipped; CHANGELOG root/mirror headers identical, added lines dash-free, root spans linked; README both versions co-bumped once.

## Cross-reference check

Included in the dispatch (methodology-citer enumeration plus retired-phrasing corpus greps). The one strained pair of lines is F1.

## Orchestrator triage

F1 re-verified at source (both lines quoted from the live file, section 10.3). Out-of-window (pre-existing prose in an untouched file, surfaced by the cross-reference lens). ROUTED in the C3 PR: the existing section-2.13 PIA-signatory note (same class, same open GDPR-Article-36-to-High-band mapping question) extended to name both lines, so the eventual fix aligns all three carriers in one pass. Class accounting for the `/retro` row: the scan that missed these was pattern-width-deficient (verb-form "approv" without the "accept" word-family), a recurrence of the established narrow-scan class on its pattern-width axis.

## Resulting hot-fix PR

None (out-of-window routing only; the routed-note extension ships in the C3 PR).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
