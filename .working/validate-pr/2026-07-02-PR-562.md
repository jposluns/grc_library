# /validate-pr record: PR #562 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #562 (C3: the risk taxonomy reconciled to the canonical 12 categories across the ERM standard and the register procedure, plus the AI-annex routing, the Medium band rename, and the Untested respelling; section 1.6 fully closed; the #561 QA rows). Squash commit `e402bc0` on `main`. Baseline post-merge per Subagent A: both generators check-clean. Library 2026.07.50; README 1.9.411.

## Subagent A return

Three findings (verbatim summaries):

> **F1, Low: the shipped mirror entry's redistribution count is off by one.** The #562 CHANGELOG-detailed Fixed bullet says "(the verifier reproduced the accounting element-by-element, all 34 traced)"; the pre-PR legacy table carries 35 example subcategories (4+4+4+3+3+5+3+3+3+3). Subagent A independently traced all 35 into the new table, each landing in exactly one row, none dropped, so the substantive "none lost" claim holds; only the audit-trail count is wrong. The miscount originated in the pre-push verifier's own report ("all 34") and was echoed into the entry without an independent recount.

> **F2, Low (in-window): `TODO.md`:464's Backlog-totals P1 line still names the deleted section 1.6 and counts 6 items.** It also contradicts the shipped "zero live forward pointers to section 1.6" claim: the orphan grep keyed on the `§1.6` / "section 1.6" forms and missed the bare "1.6 risk-model consistency" form, a pattern-width recurrence of the narrow-scan class the same PR's #561 retro row logs. Severity stays Low because :462 self-describes the totals as approximate drift-prone counts.

> **F3, Informational: declaration asymmetry in the mapping sentence.** The mirror names one net-new example ("sanctions exposure" added) but not the other net-new examples the reshape introduced (Privacy's "unlawful cross-border transfer", Technology's "legacy-system obsolescence", Physical's three). Arguably covered by the "new rows" clause; documentation-completeness note only.

Verified-clean inventory: full 17-file diff matches the declared scope; the 12 rows match the template's names and order exactly; the independent 35-element redistribution accounting confirms zero dropped; no retired name or old-count claim anywhere in the standard; the CISO-row grammar fix satisfies the ensure-that style; all five Version+Date co-bumps present; the annex's qualified routing is internally necessary and correct against its cluster table; the acceptance band set now matches Low/Medium/High/Critical; cross-corpus scan finds no enterprise-risk-context carrier of any retired name (the KPI-category and obligation-category axes are distinct schemes); TODO sibling numbering untouched; the 2.13 extension quotes verbatim-accurate; the pending-decisions C3 entry's claims match the shipped table element-by-element; all #561 bookkeeping mutually consistent; CHANGELOG headers identical and dash-free; README co-bumped; both generators check-clean.

## Cross-reference check

Included in the dispatch (retired-name category-context sweep plus the named sibling surfaces). Clean except F2.

## Orchestrator triage

All three findings re-verified at source (the 35-count re-added by hand from the legacy table; TODO:464 quoted live; the mapping sentence re-read).

- **F1 (in-window, correction-note class):** the shipped entries are not retro-edited; the correction of record is a note in the C4 PR's CHANGELOG entry. Class accounting: a soft-count echo, the verifier's count adopted without an independent recount, logged in the #562 `/retro` row.
- **F2 (in-window):** fixed in the C4 PR (the Backlog-totals P1 line rewritten without 1.6, and without 1.7 which C4 itself closes; count corrected). The orphan-grep habit extended: the section-close grep must include the bare `N.M` form, not only `§N.M` and "section N.M".
- **F3 (informational):** log-only; the C4 entry declares net-new examples symmetrically.

## Resulting hot-fix PR

None (batching rule: the F2 fix and the F1 correction note ship in the C4 PR).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
