# /validate-pr record: PR #563 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #563 (C4: the section-1.7 retention reconcile: the seven-year audit floor applied to the AI-audit and supplier-audit surfaces, the breach row aligned to the breach procedure's evidence minimum, the PIA and AI-IA values composed whichever-is-longer, the breach procedure's consistency claim re-anchored; the #562 QA rows and the 34-vs-35 correction note). Squash commit `dded94b` on `main`. Baseline post-merge per Subagent A: both generators check-clean. Library 2026.07.51; README 1.9.412.

## Subagent A return

Four findings (verbatim summaries):

> **F1, Low (audit-trail precision): the correction note misattributes the "all 34" miscount to the root #562 entry, which never carried it.** The #563 root and mirror correction texts say "the #562 entries" / "the #562 root and mirror entries" state the verifier traced "all 34"; the root #562 entry contains no "34" at all (it says only "every example subcategory redistributed, none lost"); only the mirror #562 Fixed bullet carries the count. The PR's own per-PR record attributes it correctly, so the correction-of-record contradicts the record it summarizes.

> **F2, Low (residual value mismatch behind a "matches" claim): the AI-IA register row's decommission anchor is "model", the cited Step 6 anchor is "system".** For a model retired inside a still-live system, the register floor (model-decommission-plus-5) is shorter than Step 6's (system-decommission-plus-5), so the row is weaker than the surface it claims to match, and nothing shipped acknowledges the divergence.

> **F3, Low (gate-widening spec omits the fifth reconciled row):** the updated gate-55 TODO item names only the AI-audit, supplier-audit, breach, and PIA rows; the AI-IA composed value, the row F2 shows most needs a lock, is absent from both the row list and the value parenthetical, and the root entry's "now naming the reconciled values to lock" overstates coverage by one row.

> **F4, Informational (adjacent-class, outside the declared scan scope): the retention standard's AI-domain minimum is an unrouted sibling.** `governance/standard-records-retention-and-destruction.md`:80 "AI Systems | 5 years post-decommission" (a domain minimum) vs the register's "AI audit reports | 7 years" flat: neither dominates for all decommission timings, the same composed-elsewhere shape; the line carries no scan-family token so the declared scan scope stays accurate, but unlike the security-incident sibling it was not routed.

Verified-clean inventory: full 465-line, 16-file diff matches the declared scope; the policy 7.1 floor and its AI-and-supplier scope quoted at source; the breach 9.2 re-anchor sound ("no per-record value table" literally accurate: the standard's tables are classification-tier and domain-keyed); the PIA-row composition strictly-not-weaker and verbatim-matched across both surfaces; all four Version+Date co-bumps; zero surviving 5-year statements for the four reconciled record types; the logging standard's example list matches the live register; TODO P1 recount = 4 matching the totals line; the D5 tooling note verified against the actual closure patterns; three-form orphan grep clean; all #562 bookkeeping mutually consistent including the 35-arithmetic; headers identical, dash-free, spans linked; README co-bumped; generators check-clean.

## Cross-reference check

Included in the dispatch (old-value citer sweep plus the logging-standard example list). Clean except F4.

## Orchestrator triage

All four findings re-verified at source before disposition.

- **F1 (in-window, correction-note class):** the #563 entries are shipped and not retro-edited; the C5 entry carries the narrowed correction of record (the "all 34" count lived in the #562 MIRROR bullet only). Class accounting: a scope-attribution imprecision inside a correction note, logged in the #563 `/retro` row.
- **F2 (in-window):** fixed in the C5 PR: the register's AI-IA anchor reconciled to "associated system decommission", matching Step 6 (stricter: the system outlives routine model swaps) and semantically right (an impact assessment attaches to the AI system, not a model artefact; the model-anchored adjacent rows are model-artefact records).
- **F3 (in-window):** fixed in the C5 PR: the gate-55 item's row list and value parenthetical gain the AI-IA row.
- **F4 (out-of-window):** ROUTED in the C5 PR as a sibling line beside L-f in section 3.14 (the retention standard's AI-domain minimum vs the register's AI-audit flat 7, compose-or-declare decision).

## Resulting hot-fix PR

None (batching rule: all dispositions ship in the C5 PR).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
