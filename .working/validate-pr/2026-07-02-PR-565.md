# /validate-pr record: PR #565 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #565 (C6: section-2.12 items 6, 8, 9: the PRI domain drop, the A.5.36 title correction x2 plus the verifier-caught :130 case-variant, the APO12/ISO-31000 matrix corrections; the #564 QA rows). Squash commit `234f872` on `main`. Baseline post-merge per Subagent A: both generators check-clean; the "all 59 gates" claim independently reproduced from the runner. Library 2026.07.53; README 1.9.414.

## Subagent A return

Five findings (verbatim summaries):

> **F1, Low (in-window): TODO's rewritten item-10 bullet retains the pre-confirmation hedge ("the breach-notification regulation is LIKELY Resolucao...") inside its own upstream-CONFIRMED framing.** The head and tail were reconciled; the middle kept the old hedge, so the bullet asserts the same fact as both tentative and confirmed. Same class as the late-adopted-fix propagation facet.

> **F2, Low (in-window): the four-question `/matrix-fit` routed note under-enumerates the exception policy's APO12.03 carrier set.** The same code in the same risk-assessment intent also appears bare at :66 and :144; if the fit pass recodes :130 to APO12.02, those move with it. Nothing shipped is wrong; the routed question's carrier enumeration is narrower than the class.

> **F3, Informational: the frozen #564 record says "523-line" diff; `git show 51bbeb2 | wc -l` returns 522** (15 files, 131+53 confirmed). The write-time-recount habit targets exactly this class.

> **F4, Informational: the mirror's "zero 'Policy on Exceptions' carriers outside frozen records" claim does not cover the root CHANGELOG's own quotation of the corrected-from title.** Not a defect carrier; the claim as literally worded has a hole.

> **F5, Informational (out-of-window, adjacent class): three pre-existing carriers of the non-canonical COBIT OBJECTIVE title "APO12 Manage Risk"** (the ERM standard :251, the risk methodology :184, the governance-and-risk policy :135; canonical is "Managed Risk", used correctly by the GRC programme procedure :336). Outside the practice-title scope this PR declared.

Verified-clean inventory: the full diff maps to the declared 15-file scope; the canonical-title scan reproduced with only canonical titles; zero live PRI / fabricated-title carriers; all edits grammatically integrated; co-bumps mirrored in the generated artefacts; the matrix ISO 31000 column internally consistent; the A.5.36 title re-verified at the extract's :4752 row; the TODO rotation complete with the confirmed values matching all framings; the fit note's four items each verified at their rows; Backlog totals recounted with no update needed; the DONE four-title count internally consistent; the QA trio mutually consistent; pending-decisions three-choice consistency across all three surfaces; headers identical, dash-free, spans linked; README and gate-count claims reproduced.

## Cross-reference check

Included in the dispatch (title scans, PRI scan, ISO column read). Clean except the enumerated findings.

## Orchestrator triage

All five findings re-verified at source.

- **F1 (in-window):** resolved structurally in the C7 PR (item 10 closes and its bullet deletes; the hedge goes with it).
- **F2 (in-window):** fixed in the C7 PR (the routed note names :66 and :144 as carriers that move with any :130 recode).
- **F3 (informational, frozen record):** not retro-edited; this record is the correction of record (the #564 diff is 522 lines by `git show | wc -l`). Class accounting: a count produced by the recount habit itself was off by one, logged in the #565 `/retro` row.
- **F4 (informational):** log-only; the C7 record states scan claims with the quotation carve-out named.
- **F5 (informational, out-of-window):** ROUTED in the C7 PR as a section-3.14 line (three objective-title carriers, "Manage Risk" to "Managed Risk", trivially confirmable and mechanical).

## Resulting hot-fix PR

None (batching rule: F1/F2/F5 dispositions ship in the C7 PR).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
