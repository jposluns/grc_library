# /validate-pr record: PR #567 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #567 (C8: seven section-2.13 cross-document conflicts resolved across nine bumped documents; the #566 QA rows). Squash commit `de07275` on `main`. Baseline post-merge per Subagent A: all 59 gates pass on live main; both generators check-clean; the scorecard mover arithmetic independently reproduced. Library 2026.07.55; README 1.9.416.

## Subagent A return

Six findings (verbatim summaries):

> **F1, Medium: the freshly re-corrected L-d completeness claim is wrong again, by an order of magnitude.** A corpus-wide recount of the verbless "ensure(s) that + noun-phrase" class (multiple patterns, every hit read, grammatical hits excluded) finds SEVENTEEN additional open carriers beyond the three enumerated (list quoted in the sweep output: QA-and-testing x2, GRC-programme procedure, the exception policy itself, digital-twin policy, cloud-baseline, privacy policy, the China annex, the DSR procedure, encryption policy, logging standard x2, crypto-key operations, the infosec policy, acceptance-into-service, supplier due diligence, plus F2's carrier). The class has about 20 open carriers, not 3, and the wrong count shipped on four #567-window surfaces. Likely root cause: a past mechanical "ensure to ensure that" house-style sweep broke "ensure + noun" sentences; the #566 recount grep was itself under-scoped. Second consecutive staleness miss on the same claim.

> **F2, Low (in-window adjacent):** `security/standard-data-classification-and-handling.md`:106 carries the same verbless defect ("...to ensure that data lineage tracking integrity") in the very file #567 bumped while repairing that file's Purpose-line instance of the identical class.

> **F3, Low:** the acceptance procedure's new section uses "ERC" with no expansion anywhere in the document (defined only in the cited exception policy).

> **F4, Low:** the item-19 deletion hunk consumed the blank line between the 2.13 bullet list and the 2.14 heading (CommonMark-safe; breaks the file's blank-line-before-heading formatting).

> **F5, Informational:** the 2.13 header and the DONE entry say routed "notes" (plural); one routed note remains (the intro line is correctly singular).

> **F6, Informational:** the canonical-scheme enumeration varies across the three crosswalk surfaces (the ai-audit 6.1 says "Critical / High / Moderate / Low"; the control-testing note appends "/ Observation"). Harmless for CAPA routing (only nonconformities route); noted as cross-surface texture.

Verified-clean inventory: the full 690-line, 20-file diff maps to the declared scope; all nine edits present, grammatical, internally coherent, and co-bumped; the carve/chain/ceiling coherence checks all reproduce (including the exception policy's identical no-BRC substitution clause and the SOP's out-of-hours rule); no surviving cross-corpus conflict in any of the seven classes; the D5 vacuous-pass claim verified against the actual closure regexes; all bookkeeping mutually consistent; recounts reproduce including the scorecard movers.

## Cross-reference check

Included in the dispatch (word-family scans across all seven conflict classes). Clean except the enumerated findings.

## Orchestrator triage

All findings re-verified at source (the 17-carrier list spot-checked at five members before adoption).

- **F1 (Medium, in-window for the claim; the repair promoted):** the C9 PR rewrites L-d with the verified inventory (22 carriers enumerated to date, the C9 pre-push verifier adding two beyond the sweep's list) and PROMOTES the repair to its own immediate follow-up PR (C10): a scripted corpus-wide verbless-sentence repair with per-file Version+Date bumps and a refute verifier. Class accounting: the under-scoped-recount is the pattern-width family again; the root-cause hypothesis (a past mechanical ensure-that sweep breaking noun objects) is recorded for the C10 verifier to confirm or refute against git history.
- **F2 (in-window):** folded into the C10 inventory.
- **F3 (in-window):** fixed in C9 (first-use expansion in the acceptance procedure).
- **F4 (in-window):** fixed in C9 (the blank line restored).
- **F5 (in-window):** fixed in C9 (both surfaces to the singular).
- **F6 (informational):** log-only (the enumeration difference is benign for the routing the crosswalk governs; harmonizing three surfaces risks new drift for no behavioural gain).

## Resulting hot-fix PR

None (batching rule: the C9 PR carries F3/F4/F5 and the L-d rewrite; C10 carries the corpus repair).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
