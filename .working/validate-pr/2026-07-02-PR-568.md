# /validate-pr record: PR #568 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #568 (C9: the coverage-gaps 56-token positional migration, the five-paths count fix at the generator plus four siblings, the six #567 finding dispositions including the L-d rewrite; the #567 QA rows). Squash commit `43e130c` on `main`. Baseline post-merge per Subagent A (run in an isolated detached worktree because the live checkout carried in-flight C10 edits): all 59 gates pass at the commit; both generators check-clean. Library 2026.07.56; README 1.9.417.

## Subagent A return

Eleven findings (summarized; severities as returned):

> **F1, Medium:** four live positional `TODO P` tokens survive in [`docs/decision-tree.md`] (:275, :324, :332, :336), a file this PR itself edited, and all four resolve to the WRONG topic under current TODO numbering (the :275 OT reference doubly stale: the `operations/ot/` suite shipped). The item-20 defect class, missed in a touched file.

> **F2, Medium:** the `tools/lint-placeholder-leakage.py` docstring rationale ("the coverage-gaps register uses `TODO P5.x` cross-references by design") was made false by this PR; a stale cross-FILE citer of the migrated file (the tool-docstring class).

> **F3, Medium:** the L-d "22 carriers enumerated to date" is a further undercount: wider verbless shapes find ~26 more genuine carriers (~48 total), including nine in files already on the inventory. The item's own hedge ("patterns are the method") holds, but a C10 keyed to the named patterns would repair less than half the class.

> **F4, Medium:** `PORTAL_METADATA_VERSION` stayed 1.2.0 although #568 changed the generator's emitted portal prose; both precedents (#165, #166) bumped the constant on emitted-prose changes, and the portal's own metadata note records that discipline (gate-blind: generated files are exempt from gates 40/31).

> **F5, Medium:** the AWS/Azure/GCP and Kubernetes overlay rows were mapped to `TODO backlog: multi-cloud governance overlay`, but TODO section 6.5 explicitly EXCLUDES per-cloud content (the per-cloud hardening baselines shipped in `dev-security/`) and never mentions Kubernetes; the rows' Coverage "None" also contradicts the shipped baselines.

> **F6, Low:** the PCI DSS 4.0.1, Basel III, and SWIFT CSP cells assert the financial-services country-regulator backlog topic, but none of the three is a country regulator; by the PR's own evidence-accuracy criterion these belong in Backlog-candidate form.

> **F7, Low:** the PR body's "47 cells / 9 cells" split is an occurrence count including the Conventions legend; the true cell split is 46 + 8 plus 2 Notes-prose rewrites (the artefact is complete at zero residual tokens; only the claim is off).

> **F8, Low:** the #568 DONE entry says "20-carrier inventory" against the shipped 22 (the pre-verifier draft count survived in DONE).

> **F9, Low:** the section-3.14 routed note says "Nine register cells... reworded to the Backlog-candidate form"; only eight cells carry the form (the ninth rewording was Notes prose).

> **F10, Informational:** Status "Planned" sits beside "(not currently scheduled in TODO)" on four rows; the re-grade is already routed to the section-3.14 triage.

> **F11, Informational:** the startup-roadmap peer sentence this PR rewrote carries "two-minute on-ramp" against the quickstart's own "10-minute" (a pre-existing conflicting value carried through the rewrite).

Verified-clean inventory (as returned): the pre-image 56-token census reproduces the CHANGELOG's per-token assertion sum; post-image zero tokens; all nine distinct backlog topics resolve to live TODO headings and 8+ spot-checked cells match their row subjects; the portal lists exactly five deeper-dive documents with all four sibling peer-lists correct and none self-listing; both L-d patterns reproduce the 22-carrier claim exactly at the commit and the four grammatical near-misses are genuinely grammatical; all six touched versioned docs co-bumped; generators check-clean and 59/59 at the commit in an isolated worktree; CHANGELOG headers identical and dash-free with spans linked; the #567 QA trio mutually consistent; README as declared; no stale forward pointers to section 2.14 outside frozen records.

## Cross-reference check

Included in the dispatch (citers of the migrated register, `TODO P` forms corpus-wide, tools/ docstrings, `.claude/`). F1 and F2 are its findings.

## Orchestrator triage

All eleven findings re-verified at source before adoption.

- **F1 (in-window):** fixed in the C10 PR: all four decision-tree tokens migrated to stable topic phrases, and the :275 answer rewritten to point at the shipped `operations/ot/` suite (the "Limited coverage today" claim was stale on its own evidence). Decision-tree 1.0.10.
- **F2 (in-window):** fixed in the C10 PR, in BOTH carriers: the docstring and the :64 comment the sweep did not list (the full-file-grep guard applied to the fix itself). Exemption behaviour unchanged.
- **F3 (in-window, converged):** the C10 completeness pass had independently escalated from pattern-match to a full read of all 170 "ensure(s) that" occurrences; the read classified 54 genuine carriers (the sweep's ~48 estimate plus `policy-basc-information-security.md` :21, `standard-records-retention-and-destruction.md` :142, and `procedure-supplier-audit.md` :41), and C10 repairs all 54, not the enumerated 22. Class accounting: the fourth consecutive undercount of the same claim; the durable lesson (counts of an open-ended defect class are lower bounds until a full-set read, not a pattern, closes them) is the #568 retro row's pattern entry.
- **F4 (in-window):** fixed in the C10 PR (`PORTAL_METADATA_VERSION` 1.3.0; portal regenerated).
- **F5/F6 (in-window):** the seven mis-mapped cells reworded to the evidence-accurate Backlog-candidate form in the C10 PR (register 1.1.20); the routed section-3.14 triage note extended to cover them and the Coverage-cell staleness (per-cloud baselines shipped while the rows say "None").
- **F7 (claim-only):** recorded here as the correction of record; no artefact carries the wrong split.
- **F8 (in-window):** fixed in the C10 PR (the DONE count).
- **F9 (in-window):** the routed note reworded in the C10 PR (eight cells plus the Notes-prose ninth).
- **F10 (informational):** already routed; log-only.
- **F11 (in-window):** both "two-minute" carriers aligned to "10-minute" in the C10 PR (startup-roadmap 2.2.5).

## Resulting hot-fix PR

None (batching rule: all in-window fixes ship in the C10 PR, #569).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
