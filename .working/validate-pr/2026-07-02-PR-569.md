# /validate-pr record: PR #569 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #569 (C10: the 54-carrier verbless "ensure(s) that" corpus repair across 36 documents, the eleven #568 finding dispositions, the in-repo root-cause refutation; the #568 QA rows). Squash commit `a3f182e` on `main`. Baseline post-merge per Subagent A (clean checkout at the merge commit): all 59 gates pass; both generators check-clean. Library 2026.07.57; README 1.9.418.

## Subagent A return

Four findings, all Informational (no High or Medium):

> **F1:** the D5 vacuous-pass observation (the L-d prose-named closure did not match D5's detection forms) is recorded only in the PR body; its claimed durable home is the #569 retro row, which the orchestrator must confirm actually carries it. (The durable linter-side fix is already queued at TODO item-24-residual.)

> **F2:** bare unlinked path spans inside the new frozen `2026-07-02-PR-568.md` record (which quotes a subagent return verbatim); `.working/` is gate-exempt, so convention-noise only.

> **F3:** the cloud-baseline repair inserted the article "a" in addition to the verb, so the "each repair supplies the missing verb" claim is minutely loose for that one carrier (grammatically required; meaning preserved).

> **F4:** the two apply scripts live outside the repo (session scratchpad), so the 54/54 re-parse claims are process narrative, not reproducible at the commit; the sweep compensated by independently re-reading all 54 repaired lines in place.

Verified-clean inventory: the 170-occurrence / 88-file census reproduces exactly and ALL 170 hit lines were re-read with zero residual verbless carriers (colon-lists predicated, style-rule mentions quoted tokens, the `ensuring that` sibling clean); the 54-carrier / 36-document / 22+32 / 17+19 / 9-in-7 arithmetic all reproduces from the diff; every repair grammatical, meaning-preserving (the PIPL / EU-AI-Act / POPIA / IPP-12 / PDPA paraphrases checked), none deletes "that"; ~15 spot-checked `:line` references accurate; the root-cause refutation reproduces (`git log -S` bottoms out at `03ca390`); all eleven dispositions verified at artefact (topics resolve, the six `operations/ot/` files exist, register census 39+15+legends+2 = 56, portal constant 1.3.0 both halves, linter behaviour-identical, DONE count fixed); severity split 5/4/2 consistent across the record trio; no dashes, README as declared; zero live `TODO P` tokens beyond the two sanctioned historical carriers; no citer quotes the old verbless text; 59/59 at the merge commit.

## Cross-reference check

Included in the dispatch (OLD-fragment citer greps, `TODO P` and "two-minute" residual scans, completeness-claim scans). Clean.

## Orchestrator triage

- **F1 (confirmed disposition):** the #569 `/retro` row (this batch) carries the D5 vacuous-pass observation explicitly, alongside the guard-bypass pipe-masking slip from the same window.
- **F2 (log-only):** frozen-record convention-noise; the record quotes the subagent verbatim by design.
- **F3 (log-only):** recorded here as the claim's precision correction (one repair supplies an article plus the verb).
- **F4 (log-only):** the scripts are session working files; the compensating control is the sweep's independent in-place re-read of all 54 repaired lines, recorded above.

## Resulting hot-fix PR

None (zero substantive findings; the F1 disposition is a bookkeeping confirmation in this batch's retro row).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
