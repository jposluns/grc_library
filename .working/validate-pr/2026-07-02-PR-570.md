# /validate-pr record: PR #570 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #570 (M1: the ~2050-flip Commonwealth-spelling harmonization across ~360 files under the high-assurance harness, the gate-2 coverage extension, the pseudonymization-standard rename, the pack mirror re-sync; the #569 QA rows). Squash commit `119c6df` on `main`. Baseline post-merge per Subagent A (clean checkout at the merge commit): all 59 gates pass; `lint-language.py` exits 0; the regression suite runs 264 tests OK; both generators check-clean. Library 2026.07.58; README 1.9.419.

## Subagent A return

Nine findings (3 Medium, 2 Low, 4 Informational):

> **F1, Medium:** live Commonwealth-verb residual in a swept file: `operations/ot/standard-ot-ics-security.md:285` "Monthly patching as **practised** in IT environments" contradicts the CHANGELOG's "all Commonwealth forms harmonized" claim. Root cause: "practise" flips irregularly (practise to practice, not "practize"), so it is in neither `_ISE_STEMS` nor any pattern (probe-confirmed gate-blind), the same non-mechanical class as verb-"analyses" but neither fixed nor documented as an editorial carve-out.

> **F2, Medium:** the HA register M1 row is internally contradictory: Status `verified` under an "One item in-flight:" framing with the Harness cell still opening "**Stage 1** (in-flight): research worker enumerating...". Per the register's own convention a `verified` row belongs under Completed items; as merged, the next `/resume` would surface a phantom in-flight sensitive item. The TODO section-3.15 partial-rewrite tension-marker class.

> **F3, Medium:** stale final-state counts in CHANGELOG root, mirror, DONE, and the overnight ledger (the second-verifier-round updates not propagated). Actual final state: 58 stems (40 added), seven forms per stem, six new fixtures, suite 264. Against that: root `CHANGELOG.md:9` "39 further stems / five fixtures / 263"; the mirror :18-:19 "57 stems (four inflections each) / five fixtures / Suite 263" plus a `YSE_PATTERN` "(analyse/analysed/analysing only)" description, internally contradicting the same entry's line 25 which correctly records the sixth fixture and suite 264; DONE "39 new verb stems"; the overnight PR-18 line "57 stems ... five fixtures".

> **F4, Low:** `tests/test_linters.py:289-290` comment states the inverted fact: "the GDPR Article 25(1) official English text spells 'organizational'" (the official OJ English spells "organisational", the entire reason the span is allow-listed; the fixture string itself is correct). The sweep's own flip corrupted the comment's quoted word.

> **F5, Low:** the `tools/lint-language.py` module docstring is stale on three points after the second-round extensions: the `YSE_PATTERN` description omits `analyser(s)`; the allow-span enumeration lists three spans while the tuple holds four (AEO missing); the generator-scan paragraph says "the three prose house-style rules (dash, `-ise`, `ensure that`)" though that path now also runs the `-isation` and `-yse` checks.

> **F6, Informational:** cross-surface approximation drift: the PR title says "~2050 flips", CHANGELOG/DONE "about 2000 / roughly 330 files", the HA register "~2000 / ~300 files".

> **F7, Informational:** the root-README AEO carrier at :111-112 is wrapped across two lines, so the single-line allow-span mask can never match it; it passes gate 2 only because it sits inside a text fence (unwrapping it would flag with no span protection).

> **F8, Informational:** `-yse` coverage is analyse-family only (catalyse/paralyse/hydrolyse/dialyse unmatched, probe-confirmed); zero corpus carriers today, gap-only.

> **F9, Informational (relay):** the overnight ledger records the maintainer's directed PAUSE FOR DISCUSSION after this PR; honour it before the next queued PR.

Verified-clean inventory (as returned): all 59 gates, gate 2 exit 0, 264-test suite OK, generators check-clean at the merge commit; a programmatic diff-to-scope pass over all 374 files found zero out-of-scope changes and confirmed the `.claude/` touch spelling-only; exactly 7 pack rules changed with all 7 `.claude/rules/governance/` mirrors byte-identical; the gate-2 scan set plus root files residual-clean except F1, with the remaining repo `-isation` hits all classified expected (historical CHANGELOG entries, the queued tools-docstring residual, fixture strings, the four KEEP spans); 30 flipped lines sampled across seven domains read clean; the four KEEP spans byte-intact and the ATT row agrees with the glossary; the six new fixtures assert what they claim and ~45 every-dialect FP probes all pass; TODO rotation complete with zero orphan pointers to 7.4/item-25; zero live citers of the old filename; pack README 1.53.11 with exactly one paired history row; the #569 QA trio mutually consistent; README versions as declared; the FN-round 17-residual arithmetic consistent.

## Cross-reference check

Included in the dispatch (old-filename and old-spelling citers, tools constants, the gate-39/50 bookkeeping surfaces). F2 and F3 are its findings.

## Orchestrator triage

All nine findings re-verified at source before adoption; all in-window fixes ship in the overnight-cleanup PR (#571).

- **F1 (in-window):** the carrier fixed ("practiced"; OT standard 1.0.4), and the practise-to-practice irregular-flip family documented as an editorial carve-out in BOTH the linter docstring and the gate-2 audit-programme narrative (1.16.33), the same treatment as verb-"analyses".
- **F2 (in-window):** the register restructured: the M1 row moved under "## Completed items" with the Stage-1 cell in completed tense and the follow-up cell updated to record the executed rotation; the Active-items section restored to "None currently in-flight"; the Completed-items table's missing header separator repaired and a stray mid-table separator removed (register 1.0.14).
- **F3 (in-window):** all four ledgers corrected to the final state (58 stems / seven forms / six fixtures / 264; root 40-further-stems; DONE 40; the mirror's `YSE_PATTERN` description extended with `analyser(s)`); the overnight-ledger carrier was reset to stub by this PR's routing, which removes its copy.
- **F4 (in-window):** the comment corrected to "the official English text spells 'organisational'".
- **F5 (in-window):** the docstring's three stale points fixed (YSE incl. analyser(s); four spans enumerated incl. AEO; the generator-scan wording widened to the Commonwealth-spelling family).
- **F6 (log-only):** correction of record: the classified census was 2024 `-isation`-family hits with 1945 FLIP plus the verb/yse flips and the FN-round residuals (~2050 total flips, ~360 touched files including bookkeeping); the surfaces carry approximations written at different stages, none load-bearing.
- **F7 (in-window):** the README AEO carrier reflowed to a single line ("AEO (EU Authorised Economic Operator)") so the allow-span protects it independent of the fence.
- **F8 (log-only):** gap-only with zero corpus carriers; the generic `ISATION_PATTERN` already covers the corresponding nouns (catalysation-class words are absent too); revisit only if a carrier ever appears.
- **F9 (honoured):** the pause was taken after #570 merged; the maintainer's discussion produced the three 2026-07-02 directives this PR executes.

## Resulting hot-fix PR

None standalone (batching rule: all in-window fixes ship in the overnight-cleanup PR, #571).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
