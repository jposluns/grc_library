# /validate-pr record: PR #571 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #571 (the overnight-cleanup PR: ledger routing + stub reset, the 7.5 keep-tracked decision, the mode-exit priority codification, the nine #570 sweep dispositions, the #570 QA rows). Squash commit `fa2498d` on `main`. Baseline post-merge per Subagent A (re-anchored to a detached worktree at the immutable merge commit because the live checkout carried in-flight next-PR edits): all 59 gates pass; the regression suite runs 264 tests OK; both generators check-clean. Library 2026.07.59; README 1.9.420.

## Subagent A return

Five findings, all Informational (no High, Medium, or Low):

> **I1:** the stub overnight file's lifecycle line still says the stub form "holds only this protocol description and the `Status: stub` line", while the file now (deliberately, per this PR's scope) also carries the closure-note paragraph; a two-word amendment closes the self-description tension. Gate 46 passes; cosmetic.

> **I2:** process-shape note: the change-tracking rule describes the morning-processing PR as "a small focused PR (no other scope)", while #571 bundles the routing with the sweep dispositions, a TODO rotation, and a protected-file codification. Covered by the rule's explicit awake-early exception path and by the maintainer-directed mode-exit ordering this same PR codifies (which defines "overnight cleanup" as exactly this bundle); a documented deviation, recorded for the trail.

> **I3:** the reflowed README AEO line is ~150 characters inside a fixed-width fence whose siblings wrap at ~100, so the block renders with one over-long line; the reflow is the declared F7 fix and the allow-span now matches as intended; cosmetic trade-off.

> **I4:** enumeration drift without contradiction: the design-decisions mode-exit entry says "(to daytime-unattended or attended)" while the codified CLAUDE.md paragraph enumerates three mode names; same meaning, the CLAUDE.md form is the codified one.

> **I5:** environment observation: two early gate/regression failures during the sweep were artefacts of the sweep racing the live checkout's in-flight next-PR edits and the regression suite's transient fixtures; all disappeared in the detached worktree at the merge commit. Running QA in the shared checkout while editing reproduces the false reds; the worktree pattern is the fix.

Verified-clean inventory (as returned): all 59 gates and the 264-test suite green at the merge commit in a detached worktree; the full 647-line diff read hunk-by-hunk with zero out-of-scope changes and the CLAUDE.md touch confirmed as exactly the one 9-line paragraph; all nine #570 dispositions verified at artefact (including `len(_ISE_STEMS)=58`, seven forms, four spans, six named fixtures, suite-264 by run); bare-token residual greps ("practised", "57 stems", "39 further", "five fixtures", "four inflections", "suite 263", "One item in-flight", "Two open decisions") all zero live carriers; cross-surface coherence confirmed across the stub ledger, pending-decisions, design-decisions, TODO, DONE, and the handoff (the keep-tracked story with the same three conditions everywhere; the 18-PR arithmetic; the #570 QA trio mutually consistent); zero live orphan pointers to TODO 7.5; house style clean on every added line; no wrong claims in the CHANGELOG pair, PR body, or QA records.

## Cross-reference check

Included in the dispatch (citers of the HA register conventions, the overnight protocol prose in the change-tracking rule, the CLAUDE.md attended-autonomous neighbours, the cited TODO sections). I2 is its finding; all cited TODO sections resolve.

## Orchestrator triage

- **I1 (in-window):** fixed in the #572 PR (the stub lifecycle line now names the closure note).
- **I2 (log-only):** documented deviation; the awake-early exception and the codified mode-exit ordering are the governing texts.
- **I3 (log-only):** accepted cosmetic trade-off; span protection wins over fence line-width.
- **I4 (log-only):** the CLAUDE.md enumeration is the codified form; the design-decisions entry paraphrases it.
- **I5 (adopted as practice):** QA sweeps and regression runs during in-flight edits are run against a detached worktree at the immutable commit (Subagent A already does this; the orchestrator's own mid-edit gate runs stay on the working tree by design, where drift is the thing being checked).

## Resulting hot-fix PR

None (zero substantive findings; the I1 fix batches into #572 per the normal in-window rule).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
