# /validate-pr record: PR #572 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #572 (the section-2.13 item-11 DSR-clock correction; the #571 QA rows and the I1 stub-wording fix). Squash commit `b184390` on `main`. Baseline post-merge per Subagent A (detached worktree at the immutable merge commit): all 59 gates pass; the regression suite runs 264 tests OK; both generators check-clean. Library 2026.07.60; README 1.9.421.

## Subagent A return

Six findings (2 Low, 4 Informational; no High or Medium):

> **L1:** the CHANGELOG-detailed mirror's #572 entry locates the surviving clarification window in "Section-5"; the sole surviving non-PIPEDA "30 days" carrier lives in section 7.2.2 (the manifestly-unfounded criterion (d)), and section 5 carries no 30-day value at all. The artefact is fine; the QA/CHANGELOG record is wrong.

> **L2:** "45 calendar days" (procedure :76, template :85, and the root entry's "45-calendar-day window") over-attributes the "calendar" qualifier to the cited Cal. Civ. Code text, which says "within 45 days", unqualified; the qualifier is substantively defensible (the implementing regulations say calendar days) but neither supporting source is cited. The third statutory-precision issue of the class the pre-push verifier caught twice.

> **I3:** the Access/Correction cells itemize the GDPR and PIPEDA clocks but are silent on the third cited instrument, PIPL (which carries no fixed statutory day-count); nothing wrong, but the itemized format invites an exhaustive reading.

> **I4:** the root entry says the CCPA window's full parenthetical detail was added "in the policy line"; the policy line carries only the 45-day value.

> **I5:** the template anchors the GDPR clock "from intake" while Article 12(3) runs from receipt; Stage 1 of the template is intake, and the anchor predates this PR.

> **I6:** the TODO section-2.9 pointer into the procedure (":52/:84") drifted further (the same-business-day carrier is at :88 post-PR); pre-existing staleness worsened by the insertion.

Verified-clean inventory (as returned): every changed value re-verified verbatim at the sources (GDPR Art 12(3); CCPA 1798.130(a)(2)(A) and 1798.145(h)(1), with the corpus's allowance-not-cap phrasing confirmed; PIPEDA s.8(1)/(3) grounding both the retained 30-day cells and the Objection row's no-clock note); all 15 files' hunks mapped with zero silent changes; corpus-wide greps found zero surviving GDPR-30-day DSR claims and no citer restating a clock; the post-fix census matches (the only survivors are the PIPEDA cells and the section-7.2.2(d) operational window); co-bumps, generated artefacts, CHANGELOG header parity, the #571 QA trio, README versions, and the TODO/DONE rotation all verified; 59/59 gates and 264 tests at the merge commit.

## Cross-reference check

Included in the dispatch (DSR-citing documents, jurisdiction annexes, glossaries, the compliance matrix, docs/). I6 is its finding; no clock restatement found anywhere.

## Orchestrator triage

All six findings re-verified at source; the in-window fixes ship in the section-3.14 batch-A PR (#573).

- **L1 (in-window):** the mirror sentence corrected in place with a record-correction note (section 7.2.2, not section 5).
- **L2 (in-window):** the unattributed "calendar" qualifier dropped at both artefact carriers (procedure 1.6.4, template 1.1.3) and in the root entry, so the corpus states exactly what the statute states.
- **I3 (log-only):** PIPL is not available, so a "no fixed day-count" corpus claim would be unverifiable against a source; recorded here, not written into the corpus.
- **I4 (in-window):** the root sentence rephrased in place (the detail is in two locations, the value in three).
- **I5 (log-only):** the intake anchor predates the PR and Stage 1 is intake; acceptable equivalence.
- **I6 (in-window):** the TODO section-2.9 pointers refreshed (:52/:88, dated as-of the #572 state).

## Resulting hot-fix PR

None standalone (batching rule: the fixes ship in #573).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
