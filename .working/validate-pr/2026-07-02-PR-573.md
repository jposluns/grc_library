# /validate-pr record: PR #573 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #573 (the section-3.14 mechanical batch A; the #572 QA rows). Squash commit `14ef3c5` on `main`. Baseline post-merge per Subagent A (detached worktree at the immutable merge commit): all 59 gates pass; the regression suite runs 264 tests OK; both generators check-clean; gate-2 exit 0. Library 2026.07.61; README 1.9.422.

## Subagent A return

Three findings (1 Low, 2 Informational; no High or Medium):

> **L1:** the #572 sweep's L2 "calendar"-qualifier fix was not propagated to the paired detailed-mirror #572 entry, which this same PR edited in place for the L1 record-correction: the mirror sentence still reads "45 calendar days ... quote-verified against the full text", the exact over-attribution L2 identified, and root and mirror now diverge on the value. The fix-the-named-carrier-miss-the-sibling shape again; gate-blind (`.working/` is exempt).

> **I2:** the TODO section-3.14 intro's shipped-list said "the COBIT APO12 trio" without the APO10 half (DONE, both CHANGELOG entries, and TODO 3.13 state it correctly).

> **I3:** TODO :170 retains a backticked `APO10 Manage Suppliers` as a historical was-corrected-from quote, the only corpus-wide survivor; noted so a future bare-token census does not mistake it for a live carrier.

Verified-clean inventory (as returned): every hunk in all 42 files mapped with zero silent changes; the 39/19 flip arithmetic diff-confirmed with all apply-time additions verified; the keeps census returned exactly the declared keep set with zero undeclared residuals; all three verifier-loop rounds' fixes embodied in the shipped state (the D2 bump; the refuted claim with APO10 fixed at the source at all three carriers and the TODO clause updated; the bookkeeping-parity word-reference reverts with the queued regex widening grounded against the flipped CLAUDE.md prose; the round-2 matrix carrier at 1.1.9); residual censuses clean ("Manage Suppliers", "APO12 Manage Risk", "45 calendar" outside the L1 carrier, D1-D4-as-current, stale 40/21 counts all zero live); the L-j linter line references, FR-48 13-of-38, TODO 2.9 pointers, CLAUDE.md closure-form sentence, owasp.md mirror-map absence, and pack version pairing all exact; 59/59, 264 OK, generators in sync, house style clean. Not separately verified: the exact COBIT date and the #559 GR-13 attribution (both taken from their own records).

## Cross-reference check

Included in the dispatch (tools keeps census, COBIT carriers corpus-wide, pack TLS census, the QA-trio coherence). L1 and I2 are its findings.

## Orchestrator triage

- **L1 (in-window):** fixed in the #574 PR: the mirror's #572 sentence drops the qualifier with a record-correction note, re-converging root and mirror.
- **I2 (superseded):** the batch-B rotation had already rewritten the 3.14 intro to the batch-split form, which no longer enumerates; nothing to fix at the live state.
- **I3 (log-only):** the backticked historical quote is the sanctioned narrative form; recorded for future census readers.

## Resulting hot-fix PR

None standalone (the L1 fix batches into #574).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
