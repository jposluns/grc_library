# /validate-pr record: PR #575 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #575 (the GR-12 residual-scan aid; the #574 QA rows and sweep dispositions). Squash commit `d8fd3bc` on `main`. Baseline post-merge per Subagent A (detached worktree at the immutable merge commit): all 59 gates pass; the regression suite runs 264 tests OK; both generators check-clean; `lint-language.py` standalone exit 0. Library 2026.07.63; README 1.9.424.

## Subagent A return

Eight findings (2 Low, 5 Informational, 1 process note; no High or Medium):

> **L1:** the three record surfaces mis-describe the scan scope as "tracked", contradicting the tool's own docstring and verified behaviour: `CHANGELOG.md:9`, the mirror's Added bullet, and the DONE #575 entry all say *tracked*, while the tool by design applies no git-tracked filter (untracked planted files were scanned and reported). The docstring is correct; the three records are wrong, and the defect is the PR's own theme (one wrong token, three sibling carriers; the aid itself finds exactly these three).

> **L2:** the session-handoff refresh checklist item has now been unmet for three consecutive PRs (#573, #574, #575): the file's last touch is #572, its PROGRESS block opens "current through #571" with stale versions (2026.07.59/1.9.420 vs shipped 2026.07.63/1.9.424), and its queue still frames the 3.14 batches and GR-12 as upcoming. Continued, not introduced, by #575.

> **I3:** classifier depth limit: a dated per-run file nested one level deeper than an activity directory classifies LIVE, not FROZEN-RECORD (the regex requires the date at depth two). No such nesting exists today and the failure direction is conservative, but the docstring reads broader than the regex.

> **I4:** ledger-like top-level registers outside `LEDGER_PATHS` (session-metrics, design-decisions, verifier-overrides, pending-decisions) label historical hits LIVE; conservative and consistent with the deliberate list, but expect recurring judged-historical LIVE friction.

> **I5:** `--substring` combined with `--regex` is accepted silently, regex winning; no mutual-exclusion guard, undocumented precedence.

> **I6:** the word-boundary lookarounds are stricter than `grep -w` for tokens with a non-word edge character (a leading hyphen or flag token never matches in the default mode); a small undocumented recall edge, covered by `--substring`.

> **I7 (pre-existing):** the rewritten pending-decisions Status line quotes the CLAUDE.md section title with the Commonwealth spelling ("authorise QA abbreviation") while the actual post-harmonization title reads "authorize".

> **P8 (process note):** the PR-body verifier-round cross-check could not be performed: `gh` is not installed and the GitHub MCP token had expired ("MCP server github requires re-authorization"). The verifier-round embodiment claims were judged from the shipped artefacts, which carry the attributed guards.

Verified-clean inventory (as returned): all 13 files in the merge commit map to declared scope with zero silent hunks; the tool exercised standalone (never piped) across live, clean, historical, empty-token, invalid-regex, and no-args cases with correct labels, suppressed-count arithmetic, and exit codes; the classifier verified against the real `.working` layout (100+ dated per-run files FROZEN-RECORD, all five history.md files LEDGER, the documented LIVE set); the #574 dispositions confirmed at artefact (the two charter carriers, the register's two flat-valued sibling rows, the composed AI-audit row, the two-of-four bracket precision, the Item-14 corroboration); the recurrence tallies coherent across CHANGELOG, docstring, and DONE; the QA trio and GR-12 rotation coherent with zero stale forward pointers repo-wide; aid wiring correctly absent from all four gate surfaces (aid convention, matching preflight-changelog); house style clean on the full added-line set.

## Cross-reference check

Included in the dispatch (the aid's record surfaces, the #574-disposition carriers, the rotation citers). L1 and I7 are its findings.

## Orchestrator triage

All findings re-verified at source; the fixes ship in the gates-widening PR (#576).

- **L1 (in-window):** the three "tracked" carriers corrected to the working-tree description (root entry, mirror bullet, DONE entry; record-corrections on the most-recent entries). The aid's own scan confirms zero LIVE residuals of the corrected token.
- **L2 (in-window):** the session-handoff PROGRESS block refreshed to the current state (versions, merged list, queue position).
- **I3 (in-window):** the docstring tightened to "DIRECTLY under ... depth two" with the conservative-direction note.
- **I4 (log-only):** the LEDGER list is deliberate; the friction is documented in the docstring's read-and-judge discipline.
- **I5 (in-window):** a mutual-exclusion guard added (`--substring` with `--regex` now exits 2).
- **I6 (in-window):** the boundary-mode recall edge documented in the docstring (use `--substring` for non-word-edge tokens).
- **I7 (in-window):** both quoted-title carriers corrected to "authorize" (the Status line plus the section-4.6 sibling the bare-token scan found), and the live removal-ledger pointer in `claude-md-considerations.md` updated to the current title (1.0.2); the six LEDGER hits are legitimately historical.
- **P8 (environment):** the GitHub MCP re-authorization is surfaced to the maintainer; retried at the next PR-creation call.

## Resulting hot-fix PR

None standalone (the fixes ship in #576).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
