# /validate-pr record: PR #578 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #578 (L-k, the pre-commit regen-before-check reorder; the #577 sweep dispositions and QA rows). Squash commit `583d246` on `main`. Baseline post-merge per Subagent A (detached worktree at the immutable merge commit, full clone): all 60 gates pass; the regression suite runs 271 tests OK; both generators check-clean; `lint-language.py` exit 0. Library 2026.07.66; README 1.9.427.

## Subagent A return

Three findings (2 Low, 1 Informational; no High or Medium):

> **F1:** the comment above `PRECOMMIT_NON_GATE_HOOKS` in the gate-parity linter still describes the OLD hook order ("are refreshed before the corresponding --check gates run"), the only live non-frozen surface describing the hook's position, not updated by the reorder PR. No behavioural effect (the exclusion keys on the hook name, not position); exactly the cross-reference class the close-out guard names.

> **F2:** the pending-decisions Status-line fix itself shipped a splice seam ("genuinely awaits confirm-or-redirect to confirm or redirect on resume" with the old entry-enumeration parentheticals left dangling after "the fifth"): the register-cell splice-seam class the #577 sweep's L1 named, recurring inside the cell that fixes I1.

> **F3:** four-vs-six granularity across surfaces: the Status line says "four are maintainer-CONFIRMED", the handoff says "all six proceeded defaults CONFIRMED", and no surface states the entries-vs-defaults mapping; the frozen PR-577 record also characterizes the handoff as saying "four", which is not quote-accurate.

Verified-clean inventory (as returned): the reorder is a pure move (sorted-line comparison byte-identical, only position changed; gate 35 green at 60); both routed TODO bullets' factual claims verified at source (the gate-60 file-wide row count and the vanished-file exception path both structurally accurate); the #576 retro-row reword changed only the one cell with the record-correction note; the #577 QA trio consistent across all three surfaces with the 23-file claim reproduced; rotation same-commit; headers identical root/mirror; zero live stale-count carriers; generated artefacts untouched as expected for a no-corpus-doc PR; house style clean on added lines. Stated unverifiable: GitHub-side PR content; transcript-only retro claims; whether F3 is contradiction or granularity (flagged, not asserted).

## Cross-reference check

Included in the dispatch (surfaces describing the pre-commit hook order; the QA ledgers' citers). F1 is its finding; no other live regen-first claim exists repo-wide.

## Orchestrator triage

All findings re-verified at source; the fixes ship in the L-j PR (#579).

- **F1 (in-window):** the gate-parity comment rewritten to the L-k ordering (checks fail loud first, regen auto-fixes after).
- **F2 (in-window):** the proceeded-entries clause rewritten END TO END this time (the convention the #577 retro codified, applied at sentence scope): the duplicate phrase removed and the entry enumeration re-attached to the four confirmed entries it describes.
- **F3 (in-window, granularity clarified):** the rewritten clause now states the mapping in durable terms (the decision round's "confirm all six" answer covered every default logged across the four confirmed entries; counts differ by granularity, some entries logging paired values) without asserting a per-entry decomposition no artefact records. The frozen PR-577 record's imprecise characterization stays as-written per the frozen-record convention; this record notes it.

## Resulting hot-fix PR

None standalone (the fixes ship in #579).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
