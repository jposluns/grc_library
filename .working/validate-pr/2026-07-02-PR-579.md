# /validate-pr record: PR #579 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #579 (L-j, the lifecycle-marker migration across every live carrier; the #578 sweep dispositions and QA rows). Squash commit `bbd67d6` on `main`. Baseline post-merge per Subagent A (detached worktree at the immutable merge commit, full clone): all 60 gates pass; the regression suite runs 275 tests OK; both generators check-clean; `lint-language.py` exit 0; the five affected gates (1, 4, 16, 19, 38) each exit 0 standalone. Library 2026.07.67; README 1.9.428.

## Subagent A return

Six findings (2 Medium, 3 Low, 1 Informational; no High):

> **M-1:** the rewritten ingestion-spec paragraph contradicts itself in its own tail: it states lifecycle is deliberately NOT part of Classification, then the retained closing example still says a fork may hold a document "`Reference Only` on the lifecycle axis" (the pre-migration framing surviving in the rewritten paragraph, the splice-seam class again).

> **M-2:** the retirement/supersession procedure carries two live prose instructions of the OLD convention ("Marked as deprecated in the document index register"; retirement step 1 "Mark document as deprecated..."), the operational sibling of the ingestion instruction's step 20 that WAS migrated; a reader executing it re-creates the retired overload. Gate-blind free prose.

> **L-1:** the lint-structure module docstring still says "the privacy deprecated annex" nine lines above the comment the PR migrated in the same file (the full-file-grep-on-touched-files class).

> **L-2:** lifecycle-vocabulary fork: the marker value is "Superseded" but the corpus lifecycle vocabulary still names the stage "Deprecated" (the governance charter's stage 5, the health-report template's count row, the adopter-guide's marking advice, the build-taxonomy docstring); none instructs the retired mechanism, but the marker value has no anchor in the charter's stage enumeration.

> **L-3:** the ingestion instruction's step 4 ("Apply the canonical 13-field metadata block exactly") and its migrated step 20 (add a 14th Status line) are unreconciled within the one document (the spec reconciles them; the instruction did not).

> **I-1:** the new retro cell claims "the re-read is quoted in the sweep record"; the #578 record describes the rewrite but quotes no re-read (record-claim precision drift).

Verified-clean inventory (as returned): the annex migration exact (sole Status carrier, placement consistent everywhere); all three linters on the identical regex with the no-alias fixture pinning; the new convention internally consistent across annex, spec, register, instruction, README table, docstrings, and fixtures except M-1's one clause; zero non-Public Classification lines corpus-wide, the "Public for both" spec claim now true corpus-wide; the #578 dispositions embodied; QA trio consistent; rotation and version chain clean; generated artefacts correct with the annex properly absent; house style and gate-39-safe phrasing clean. Stated unverifiable: GitHub-side content; transcript-only claims; whether the stage-name-vs-marker-value fork is intended (a maintainer call).

## Cross-reference check

Included in the dispatch (citers of all six touched convention surfaces). M-2 and L-2 are its findings.

## Orchestrator triage

All findings re-verified at source; the fixes ship in the retention-composition PR (#580).

- **M-1 (in-window):** the closing example's axis label corrected to "the handling axis" (spec 1.7.7).
- **M-2 (in-window):** both retirement-procedure instructions rewritten to the Status: Superseded convention with the index-register cross-reference retained (procedure 1.0.5).
- **L-1 (in-window):** the lint-structure docstring aligned ("superseded annex").
- **L-2 (routed):** an authorial naming call (align the four vocabulary carriers to "Superseded" for supersession, or document the stage-name-vs-marker-value distinction); routed to TODO section 3.14 for maintainer triage, per the unavailability-window discipline.
- **L-3 (in-window):** step 4 now names the optional Status line with a pointer to step 20, reconciling the two within the instruction.
- **I-1 (in-window, record-correction):** the retro cell reworded to what the record actually shows, with the correction noted in the cell (improvement-log 1.0.304).

## Resulting hot-fix PR

None standalone (the fixes ship in #580).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
