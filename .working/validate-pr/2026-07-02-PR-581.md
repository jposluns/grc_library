# /validate-pr record: PR #581 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #581 (the coverage-gaps register re-grade; the #580 sweep dispositions and QA rows). Squash commit `830e72d` on `main`. Baseline post-merge per Subagent A (detached worktree at the immutable merge commit, full clone): all 60 gates pass; the regression suite runs 275 tests OK; both generators check-clean; `lint-language.py` exit 0. Library 2026.07.69; README 1.9.430.

## Subagent A return

Five findings (1 Medium, 2 Low, 2 Informational; no High):

> **M-1:** the same staleness class the PR fixed remains across the register tables the verifier only spot-checked (pre-existing, not introduced by #581): under the PR's own re-grade logic, the seven section-2.3 regulator rows, the section-2.4 NHS DSPT / PHIPA / My Health Records rows, the section-2.5 UK AI and US state-by-state rows, the section-2.2 EU AEO row, and the section-3 TOGAF and ISO 9001 rows are graded None while shipped corpus artefacts name or address each (the regulator rows internally inconsistent with the Referenced grades the register already gives the US bank regulators for the same annex shape; ISO 9001 parallels the ISO 14001 re-grade exactly).

> **L-1:** the root CHANGELOG #581 entry describes Kubernetes at its superseded intra-PR grade ("moves None to Referenced ... no dedicated treatment") while the shipped register cell is Partial (the verifier's raise); the DONE #581 entry carries the same superseded grade.

> **L-2:** count drift and lead-pair mismatch: the root lead says "twelve stale Coverage cells" (twelve re-grade events over eleven distinct cells, Kubernetes double-counted), the mirror lead says "seven" (the stale pre-verifier count, mismatching the root contrary to the shared-lead convention), and the DONE and handoff surfaces carry the same seven framing. Single root cause with L-1: the record surfaces were drafted before the verifier round widened the set and only partially refreshed.

> **I-1:** the section-1 Manufacturing row is graded None while the shipped operations OT suite addresses industrial/ICS within broader documents (the Notes cell already discloses the cross-cut); belongs in M-1's triage batch.

> **I-2:** the AWS/Azure/GCP Partial grade sits in tension with the vocabulary's Partial definition (dedicated documents, not broader-doc treatment); the mirror discloses this as a pending vocabulary call, but the Notes cells state the facts without naming the tension. Log-only.

Verified-clean inventory (as returned): all twelve re-grade events grade-accurate against their cited evidence artefacts under the register's vocabulary (every artefact opened; no over- or under-grade found); the routed section-3.15 D5 bullet mechanically verified by importing the actual `CLOSURE_PATTERNS` and running them against the shipped phrasing (no match on any of the six forms, each per-form explanation accurate); the routed section-2.13 N-4 blockquote verbatim at source with the heading and intro co-updated; the #580 L-1 record-correction consistent end to end (1.0.305-at-ship with two-bump provenance, header now 1.0.306); the QA trio consistent; the version chain and generated artefacts exactly the register bump; house style clean; the 15 citing files all generic routing pointers restating no changed grade. Stated unverifiable: transcript-only provenance (the maintainer's re-grade answer; the verifier round's internal sequence) and GitHub-side state.

## Cross-reference check

Included in the dispatch (citers of the coverage-gaps register). All clean of restated grades; one pre-existing imprecision noted un-routed (`docs/decision-tree.md` pointing at register "section 5" for the IEC 62443 depth candidate that lives in section 3; the section-5 OT row does mention IEC 62443).

## Orchestrator triage

All findings re-verified at source; the fixes ship in the GR-P3 aid PR (#582).

- **M-1 + I-1 (routed):** re-verified at file (the register rows' None grades and the cited evidence artefacts each confirmed at routing time); routed to TODO section 3.14 as the second coverage-gaps triage batch, cross-referenced to the pending coverage-vocabulary call (I-2's subject), the same maintainer-triage shape as the bullet #581 closed.
- **L-1 + L-2 (in-window, record-correction cluster):** the root #581 entry's lead and Kubernetes clause corrected to the shipped register (eleven cells across twelve events; Kubernetes Partial with the raise provenance) with the correction noted inline; the mirror lead corrected to match; the DONE #581 entry rewritten to the eleven-cell, Kubernetes-Partial state with a correction note (the orchestrator had independently caught the DONE drift pre-sweep); the handoff PROGRESS framing refreshed. Fourth instance of the at-ship-refresh pattern (versions at #580, counts and grades here); the retro row carries it.
- **I-2 (log-only):** the vocabulary tension is disclosed in the #581 mirror and cross-referenced from the M-1 triage bullet; the coverage-vocabulary call was ANSWERED at the maintainer's same-day return round (widen Partial's definition to cover dedicated-adjacent-artefact cases), with the definition-cell edit shipping in the batch-2 PR.

## Resulting hot-fix PR

None standalone (the fixes ship in #582).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
