# /validate-pr record: PR #585 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #585 (the lifecycle-vocabulary alignment: the four supersession-vocabulary carriers aligned to the `Status: Superseded` marker; the #584 QA rows; the #583 retro-cell scoping fix). Squash commit `292e102` on `main` (parent `1921959`). Baseline post-merge per Subagent A (detached worktree at the immutable merge commit, all exit codes read unpiped): all 60 gates pass; the regression suite runs 276 tests OK; both generators check-clean. Library 2026.07.73; README 1.9.434.

## Subagent A return

Three findings (1 Low, 2 Informational; no High or Medium):

> **L-1:** the TODO section-3.14 intro rewritten by this PR claims a singular "the remaining bullet is the second coverage-gaps triage batch", while the same PR's own SF-1 routing added a second live bullet to the section; a live self-inconsistency inside one file, the improvement-log's #580 counted-section class, invisible to gates (no gate reads free TODO prose).

> **I-1:** the adopter-guide and charter cite the marker in the plain form (`Status: Superseded`) while every gate keys on the bold line form; convention-consistent with the corpus's established citation style (the register's vestigial-Deprecated row uses it too), so log-only.

> **I-2:** the supersession-semantics design observation (a kept-for-option jurisdiction annex is not literally superseded; the marker exempts from three gates but the index exclusion is path-keyed), likely duplicating the pre-push verifier's N-1/N-3, recorded so it is not lost; no change proposed.

Verified-clean inventory (as returned): the four carrier edits faithful and coherent (the six-stage model reads cleanly, Section D maps one-to-one onto stages 5 and 6, the adopter advice is consistent with the ingestion spec and register, the build-taxonomy docstring is accurate against `EXEMPT_FROM_INDEX`); the corpus-wide `deprecat` sense-enumeration found every remaining carrier a different sense or a documented vestigial reference; the #584 QA trio internally consistent (the "9 files" claim matching the squash stat, counts agreeing across all four record surfaces, the 22-entry rotation re-counted); the #584 sweep's L-1 scoping fix independently re-verified against the ledger (#577-#582 all carry findings, the four overnight rows record zero); all eight stale-token hunts clean on live surfaces; house style clean on all 76 added lines; version discipline paired. Stated unverifiable: the maintainer-conversation halves, the pre-push verifier round's original text (only its repo-side effects), the #584 sweep's own dispatch process, and the squashed intermediate commits.

## Cross-reference check

Included in the dispatch (citers of the touched files). No live surface describes the charter's stage 5 as "Deprecated", cites the health-report's old row name, or instructs the old index-marking mechanism; the only stale section-3.14-contents description found was TODO's own intro (L-1).

## Orchestrator triage

- **L-1 (validated, fixed in-window):** re-verified at triage that section 3.14 in the #586 diff holds exactly one bullet (the routed SF-1) and the rotated intro names exactly it; the singular-claim inconsistency does not survive #586.
- **I-1 (log-only):** the plain-form citation is the corpus's established convention; no change.
- **I-2 (already dispositioned):** duplicates the #585 pre-push verifier's N-1/N-3 design observations, which the #585 mirror records as inherent in the maintainer's directive; no change.

## Resulting hot-fix PR

None (the Low is fixed in-window by #586's own rotation edit; the two Informational findings are log-only).

## Notes

`.working/validate-pr/` is a frozen-state archive; `path:line` references are as-of-write-time.
