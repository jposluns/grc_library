# /validate-pr record: PR #587 (2026-07-02)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #587 (gate 61, the COBIT 2019 / ISO 31000:2018 citation-existence audit, both halves of the section-3.13 build, three day-one corpus fixes, the matrix-fit extension, pack 1.53.14, the #586 QA rows). Squash commit `55b911a` on `main` (parent `f117c10`), 29 files. Baseline post-merge per Subagent A (detached worktree at the immutable merge commit, all exit codes read unpiped): all 61 gates pass; the regression suite runs 284 tests OK; both generators check-clean. Library 2026.07.75; README 1.9.436.

## Subagent A return

Ten findings (1 Medium, 4 Low, 5 Informational; no High):

> **M-1:** the routed COBIT recode bullet enumerates "the two adjacent mis-titled MEA01 cells" (MEA01.02, MEA01.05) but the same compliance-management table carries two more non-canonical MEA01 titles it does not name: MEA01.01 "Establish governance" (canonical "Establish a monitoring approach") and MEA01.04 "Report results" (canonical "Analyze and report performance"); the recode PR's worklist under-enumerates by two cells (MEA01.03 title-correct).

> **L-1:** the section-2.13 intro still says the acceptance-authority note is "blocked on" the mapping decision while the same PR's diff annotates both bullets maintainer-ANSWERED (the intro-vs-post-diff-section-contents class of the #586 retro row).

> **L-2:** the HA register's stage-4 cell "eight regression fixtures + the corpus-at-HEAD smoke test" reads as nine; the smoke test is one of the eight (suite 276 to 284, exactly +8).

> **L-3:** neither #587 CHANGELOG half records the new section-1.3 DPO-sweep intake, the two section-2.13 ANSWERED annotations, the routed section-3.15 S-4 bullet, or the protected-tree `.claude/commands/resume.md` touch.

> **L-4:** the "gates 48/49/54" enumeration survives on two live standing surfaces (worker-brief-template DO rail 9, matrix-fit history preamble), and the S-4 bullet names only the docstring carrier of `tools/verify-reference-modules.py` while a second carrier sits in its line-179 runtime message string.

> **I-1 to I-5:** HA register identifier-count precision (271 vs the ISO tree; 38 vs 46 clause entries); gate 60's forward drift arithmetic (2 of 3; the next machinery gate flips it red absent a /guardrails run); the answered section-2.13 bullets retain their original "Decide..." closers; root/mirror lead divergence (established practice); the reference-module docstring's "title checking applies to objective codes only" phrasing.

Verified-clean inventory (as returned): the reference data independently re-derived from the extracts with exact agreement (231 practice codes, per-objective counts and contiguity, all 40 objective titles verbatim, all 46 ISO clause entries, no clause 7); the three corpus fixes verified at extract (MEA01.03 canonical, DSS05.07 exists and fits, ISO/IEC designation corrected); the ISO 6.6/6.7 swap re-verified as exactly three carriers with a corpus-wide hunt finding no more; the DPO sweep sized (9 live documents, ~28 lines); the #586 QA trio consistent; four-surface parity plus the section-6 detailed-prose pair present; stale-token hunts clean on live surfaces; house style clean on the whole squash diff.

## Cross-reference check

Included in the dispatch. The matrix-fit widening's carriers all consistent; the two stale sibling surfaces outside the PR's diff are L-4 above.

## Orchestrator triage

- **M-1 (validated at file and extract, fixed in-window):** both cells recoded to the canonical titles in #588 (the executing recode PR), which the same-table move-together constraint makes the natural carrier; the compliance policy re-bumped for the second body change.
- **L-1 (fixed in-window):** the section-2.13 intro re-anchored to the ANSWERED state with the two executing-PR routings named (#588).
- **L-2 + I-1 (fixed in-window):** the HA register cell now counts the smoke test inside the eight and carries the 317-identifier / 46-clause-entry precision; register 1.0.16.
- **L-3 (fixed in-window):** a dated addendum appended to the #587 mirror entry naming the four unrecorded diff items; the root lead stays frozen per the #582/#584/#586 precedent.
- **L-4 (fixed in-window):** both standing surfaces widened to 48/49/54/58/61 (worker-brief-template 1.3.2, matrix-fit history 1.0.6) and the S-4 bullet widened to name the line-179 runtime-message carrier.
- **I-2 (log-only, standing):** noted for the next machinery PR; a /guardrails run precedes or accompanies the next gate addition.
- **I-3/I-4/I-5 (log-only):** annotation convention, established practice, and phrasing respectively.

## Resulting hot-fix PR

None; all in-window fixes ship in #588 (the recode PR already in flight, which also carries this QA trio).

## Notes

The sweep independently re-derived the gate-61 reference data rather than re-reading the shipped module (the verifier's second-extraction artefact is not in-repo), agreeing exactly; upstream currency of COBIT 2019 / ISO 31000:2018 remains egress-gated per the standing deferral.
