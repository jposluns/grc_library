# /validate-pr record: PR #588 (2026-07-03)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #588 (the COBIT fit-pass recodes, the ISO 31000 clause correction, the MEA01 canonicalization corpus-wide, the gate-2 verbatim-title span mask, the #587 QA trio). Squash commit `78cb1d6` on `main` (parent `55b911a`). Baseline post-merge per Subagent A (detached worktree at the immutable merge commit, all exit codes read unpiped): all 61 gates pass; the regression suite runs 286 tests OK; both generators check-clean. Library 2026.07.76; README 1.9.437.

## Subagent A return

Seven findings (3 Low, 4 Informational; no High, no Medium):

> **L-1:** the #588 mirror lead's "span five corpus documents" was written at commit-2 state and not re-counted after the commit-4 fold-in (ten documents; twelve with the ISO carriers); the root lead and the Changed bullets are accurate.

> **L-2:** the refreshed handoff PROGRESS block is self-contradictory: the header parenthetical and queue item (5) retain the pre-#587-merge state ("current through #586, #587 in flight") while the body records #587 merged and #588 in flight.

> **L-3:** the mirror's supplier bullet mis-attributes the BAI05 objective title to the practice code (BAI05 is Managed Organizational Change; BAI05.02 is Form an effective implementation team); the fit reasoning and the recode stand.

> **I-1:** the #588 DONE entry undercounts the shipped scope ("the two MEA01 title fixes" vs four practice cells plus nine objective-title carriers).

> **I-2:** root/mirror lead wording divergence (established practice, log-only).

> **I-3:** gate-31 boundary: the merge landed 2026-07-03 UTC with touched docs dated 2026-07-02, lag exactly 1 day, which the gate tolerates and which cannot grow.

> **I-4:** the VERBATIM_ENSURE_TITLES mask is a case-sensitive substring span, so a prose sentence reproducing the exact title string would be masked outside a citation context; inherent to the enumerated-span design, one entry, both directions fixture-pinned.

Verified-clean inventory (as returned): every replacement code and title independently re-verified at the held extracts (extract line numbers quoted, including the vacated codes); the 6.7-to-6.6 direction confirmed on all three carriers; residual-token hunts zero on live surfaces across every old code, old title, and old-cell variant; paired description cells consistent with the new codes; the reference-module arithmetic (317 = 271 + 46) checks; the #587 QA trio internally consistent with 29 files verified at the squash; all five claimed in-window fixes present in the diff; TODO surface integrity (intros vs bullets, the fit-pass bullet's line claims) verified live; CHANGELOG root-lead counts all verified against the diff; the gate-2 mask span-scoped on both paths with both fixture directions pinned; stale-count hunts clean.

## Cross-reference check

Included in the dispatch. All live MEA01 mentions outside the corrected carriers are bare-code or gloss citations carrying no title.

## Orchestrator triage

- **L-1 + L-3 (fixed in-window):** a dated addendum on the #588 mirror entry (the #587 L-3 precedent) records the count and the objective/practice attribution correction; shipped in #589.
- **L-2 (fixed in-window):** the handoff header parenthetical and queue item (5) reconciled to the merged state in #589's handoff refresh.
- **I-1 (fixed in-window):** the #588 DONE entry body updated to the full shipped scope (DONE is working state, not a frozen entry).
- **I-2/I-3/I-4 (log-only):** established practice, a boundary that cannot grow, and an inherent design property respectively.

## Resulting hot-fix PR

None; all in-window fixes ship in #589 (the small-fixes PR already in flight, which also carries this QA trio).

## Notes

The sweep verified outcomes rather than scratchpad artefacts (the apply script and verifier transcripts are not in-repo); `verifier-overrides.md` untouched, no overrides made. Upstream currency of COBIT 2019 / ISO 31000:2018 remains egress-gated per the standing deferral.
