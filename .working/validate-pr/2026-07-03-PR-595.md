# /validate-pr record: PR #595 (2026-07-03)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #595 (section-3.15 machinery wave 2a, the #594 QA trio). Squash commit `a76e0e9` on `main` (parent `c35a1ca`). Baseline post-merge per Subagent A (detached worktree, exit codes unpiped): all 61 gates pass (gate 60 in its expected drift-2 warning state); suite 287 OK; both generators check-clean. Library 2026.07.83; README 1.9.444.

## Subagent A return

Two Medium, two Low, two Informational:

> **M-1:** the new gate-39 vanished-file test is order-dependent: it omits the `sys.path` insert the sibling importlib-loading tests carry, so it errors (`ModuleNotFoundError: lint_common`) when its class runs in isolation; the canonical runner passes only because alphabetically-earlier classes insert the path first.

> **M-2:** the audit spec's D5 narrative under-describes the widened forms 3 and 6 (the "narrative updates when detection logic changes" class the close-out checklist names; the PR bumped the same document for the category-3 fix without touching the adjacent paragraph).

> **L-1:** the protected `.claude/CLAUDE.md` D5 six-form description under-describes forms 3 and 6 the same way (gate-exempt tree; incomplete rather than wrong).

> **L-2:** the session handoff's forward queue still lists "the routed gate-60/gate-39/D5 candidates" as remaining machinery while the same line's progress text records #595 closing exactly those three.

> **I-1:** the docstring shorthand `rotated to [the] DONE [ledger]` implies four bracket combinations where the regex matches two. **I-2:** the negation guard covers the immediate-`NOT` shape only (census-bounded by design; "never rotated to DONE" would still match).

Verified-clean inventory (as returned): the D5 census reproduced exactly at the parent (three new form-3 hits, all genuine; 22 short-form hits with exactly the two guarded negations); gate 60's scoped parse with live behaviour unchanged and the fixture decoy genuinely pinning; gate 39's tolerance and de-staled comment; the spec's category-3 example list now byte-matching its header; the three-bullet rotation orphan-free with the GR-6 census recorded; the #594 QA trio internally consistent with the Sweep-33 claims verified at the frozen record; CHANGELOG pair accurate; mechanical baseline green.

## Cross-reference check

Included in the dispatch (behaviour-description carriers of the changed tools screened; M-2/L-1 are its findings).

## Orchestrator triage

- **M-1 (validated by isolated run, fixed in-window):** the `sys.path` insert added in #596; the isolated run now passes.
- **M-2 (validated at file, fixed in-window):** the D5 narrative's forms 3 and 6 descriptions refreshed in #596 (spec 1.16.42).
- **L-1 (deferred, protected tree):** the CLAUDE.md D5 description refresh is recorded in [`pending-decisions.md`](../pending-decisions.md) as a morning item (factual staleness fix needing maintainer authorization for the protected file).
- **L-2 (fixed in-window):** the handoff queue line's closed candidates dropped in #596's refresh.
- **I-1/I-2 (log-only):** shorthand imprecision and the census-bounded guard scope, both documented in the code comments.

## Resulting hot-fix PR

None; the fixes ship in #596 (the FR-48 doc-2 PR, which carries this QA trio).

## Notes

The sweep's stated-unverifiable items (census-worker internals, per-commit gate greenness behind the squash) are process narratives whose outcomes it verified independently.
