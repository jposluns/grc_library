# /validate-pr record: PR #605 (2026-07-03)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #605 (FR-48 document 11 of 13, the secure-development-and-engineering renumber with zero citers; the #604 sweep's carried matrix-legend range fix; the carried-fix annotation for doc 13; the #604 QA trio). Squash commit `958b5aa` on `main` (parent `3d6957f`). Baseline post-merge per Subagent A (detached worktree, all commands foreground, exit codes unmasked): both generators check-clean; root/mirror #605 CHANGELOG headers byte-identical; README 2026.07.93 / 1.9.454; worklist 36 checked / 2 unchecked with the carried-fix annotation present.

## Subagent A return

Zero in-window findings at any severity; one carried adjudication and one new pre-existing Informational:

> **Adjudication (Low, pre-existing), verdict FIX:** the matrix legend row's Framework cell `ISO/IEC 27001:2022 Annex A` should drop ` Annex A`: every sibling legend's Framework cell names the whole framework document and leaves the identifier scheme to the Reference Basis cell (CCM, NIST CSF, and BASC siblings quoted); the ISO row is the only one narrowing to a subset that demonstrably excludes part of the column's content (166 body-clause tokens); and the same document's line-407 coverage table already names the framework whole.
>
> **Observation (Informational, pre-existing):** `dev-security/policy-secure-development-and-engineering.md` line 25 reads "ISO/IEC 27002:2022 Annex A controls"; Annex A belongs to ISO/IEC 27001, and 27002:2022 provides the reference controls themselves. One-clause reword proposed, not merge-blocking.

Verified-clean inventory (as returned): the renumber floor exact (53 removed/added pairs text-identical after number-prefix strip; 5 H2s, 12 H3s, 34 clauses, zero residual); all 16 invariants recounted and matching; the zero-citers claim held under proximity greps; the legend-fix census reproduced column-scoped (with the whole-file-vs-column-scoped distinction noted: the BASC column also carries § tokens); zero residual `§6 to §10`; all bookkeeping counts and the #604 QA trio consistent; both generators check-clean.

## Cross-reference check

Included in the dispatch (per-touched-doc citer verification, the legend-fix census reproduction, and the carried adjudication).

## Orchestrator triage

- **Adjudication (validated at file, fixed in-window):** the Framework cell corrected to `ISO/IEC 27001:2022` in #606 (matrix 1.11.8), the sibling-legend and line-407 evidence re-read before the edit.
- **Observation (validated at file, fixed in-window):** line 25 corrected to "ISO/IEC 27002:2022 controls" in #606 (dev-sec policy, ultimately 1.0.7); the initial zero-residual grep proved pattern-narrow (pipe-defeated by table cells), and the #606 pre-push verifier pair surfaced four more carriers of the class, all fixed in the same PR with widened class greps returning zero residual.

## Resulting hot-fix PR

None; both fixes ship in #606 (the FR-48 doc-12 PR, which carries this QA trio).

## Notes

The sweep ran foreground-railed in a detached worktree (created and removed cleanly). Pre-push, the #605 verifier pair had returned no in-scope findings on either lens (one pre-existing observation, the Framework cell, which became this sweep's carried adjudication).
