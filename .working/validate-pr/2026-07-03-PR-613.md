# /validate-pr record: PR #613 (2026-07-03)

## Trigger and state snapshot

Post-merge PR-scoped sweep for PR #613 (the section-3.15 GR-3 wave-2 migration: gate 40 plus the D2 and D4 delta checks retired their three identical private Version-window parsers for the shared `head_version()` in [`tools/lint_common.py`](../../tools/lint_common.py); D4's Date read rebuilt on `parse_metadata_block()` plus `parse_iso_date()`; three new tests, suite 307; the TODO GR-3 bullet trimmed to its wave-3 residual; the #612 QA trio plus both #612 sweep fixes). Squash commit `d4af399` on `main` (parent `d9f051f`). README 2026.07.101 / 1.9.462.

## Subagent A return

**1 Low + 1 Informational; zero High/Medium.**

- **L-1 (Low, [`tools/check-date-cobump-on-pr.py`](../../tools/check-date-cobump-on-pr.py) module comment and the #613 mirror entry's D4 bullet):** the equivalence claim "the failing exit is the same either way" overstates for one doubly-degenerate corner. The migration reordered the Date-validity check ahead of the defensive `commit_date is None` skip: pre-migration, a shape-valid but calendar-invalid Date (e.g. 2026-02-30) on a diff-listed file no range commit touched hit the silent skip first and exited 0; post-migration `parse_iso_date` rejects it before that skip, so the same file now draws the missing-canonical-Date finding and exits 1. Fail-loud direction, but undocumented.
- **I-1 (Informational, [`tools/check-version-bump-on-pr.py`](../../tools/check-version-bump-on-pr.py)):** cosmetic blank-line irregularities left by the helper removal (one blank line before `def git` against the file's two-line convention; three between `git_show` and `is_exempt`). No gate checks Python spacing.

Verified-clean inventory (as returned): `head_version()` semantics re-derived independently against the retired regex (all five docstring divergence claims accurate; the leading-whitespace narrowing correctly the headline); no import shadowing (the `head_version_value` rename complete including both f-strings); suite 307 OK live; gate 40, D2, and D4 quoted Verification lines reproduced byte-for-byte against the real #612 range; root/mirror leads identical modulo link targets; house style clean; version surfaces coherent; the #612 QA batch and both #612 sweep fixes verified at file; the TODO GR-3 trim correct; whole-repo cross-reference grep for the four retired names clean outside frozen records; the audit-spec gate-40/D2/D4 narratives confirmed accurate (nothing names the retired internals).

## Cross-reference check

Included in the dispatch (the retired-name whole-repo grep; the gate-31 linter, PR-time runner comments, and sweep-preflight scanner checked for stale internals descriptions; the audit-spec narratives re-read).

## Orchestrator triage

L-1 validated at file against both implementations (the old flow's `commit_date is None: continue` preceded its `fromisoformat` try/except; the new flow parses the Date first): CONFIRMED, fixed IN-WINDOW in the next PR as a one-clause amendment to the D4 module comment plus a one-sentence addendum to the #613 mirror entry. I-1 validated and fixed in-window in the same PR (blank-line normalization, zero behaviour change).

## Resulting hot-fix PR

None (both fixes ride in the next PR).

## Notes

The sweep ran read-only on the merged state. The L-1 shape (an equivalence claim scoped to the surfaces examined, defeated by an interaction with an ADJACENT unexamined branch, here the check ORDERING rather than the check itself) is a near-neighbour of the parallel-case re-verification checklist line; logged in the #613 retro row's Friction cell.
