# /validate-pr record: PR #620 (2026-07-03)

Frozen-state archive; `path:line` references are as-of-write and are kept verbatim even if lines later shift.

## Trigger and state snapshot

PR #620 ("RM-10 pipe-guardrail hardening (TODO 1.9 a+b): PreToolUse hook, tail-safe wrapper, guard pipe self-defence"), squash commit `f1bd098` on `main`, parent `ede2e3b` (#619). Touched: the new [`.claude/hooks/block-verification-pipes.py`](../../.claude/hooks/block-verification-pipes.py) and [`tools/tail-safe.sh`](../../tools/tail-safe.sh), `.claude/settings.json` wiring, the guard self-defence in [`tools/pre-push-guard.sh`](../../tools/pre-push-guard.sh), the gate-36 fixture (suite 307 to 308), `TODO.md` section 1.9, both CHANGELOG surfaces, the #619 QA batch, the handoff, the lease, `README.md` (CalVer `2026.07.108`, README `1.9.469`). Mechanical baseline post-merge: all 63 gates pass standalone on `f1bd098` (file-redirect exit capture). Context: two pre-push verifier rounds ran (round 1: 2 defects + 3 nits + 2 observations; the over-blocking defect was fixed by re-anchoring the hook regex to command position rather than documenting around it; gate 36 itself caught the guard-fixture interaction on the first guard run).

## Subagent A return

Full dispatch (diff review plus cross-reference check, including reproducing the behaviour claims). 2 findings, 3 observations:

- **F1 (low, in-window, stale parallel count; fingerprint `hook-selftest-count-third-carrier-620`).** The detailed mirror's Verification bullet still claimed "hook self-test 7 blocked / 7 allowed", contradicting the entry's own lead and Added bullet (both 10/14) and the artefact (reproduced: 10/14). The pre-verifier-round draft count was corrected on two carriers; the third survived, the full-file-grep-for-prose-corrections shape (#443/#320 class).
- **F2 (low, in-window, coverage-pairing miss; fingerprint `coverage-pairing-1.9-rescope-620`).** PR #620 materially rescoped TODO section 1.9, and the close-out checklist's own coverage-pairing line (shipped one PR earlier in #619) required a queued-or-done scratch coverage sync; neither happened in the close-out. The scratch section-1.9 row still described the pre-#620 queued state at stamp #618.

Observations: O1, the deliberate piped invocation ran unblocked inside the sweep itself, re-confirming the hooks-load-at-session-start residual, with a design note that the next-session validation must run from a session rooted at the main repo (the wiring is `$CLAUDE_PROJECT_DIR`-anchored); O2, the hook's pipe alternation also blocks the `||` control operator (fail-closed, consistent with the uniformity stance, but a fifth residual not yet listed; candidate one-liner for the (c) widening); O3, the handoff's scratch-main snapshot line is behind again (declared drift tolerance).

Checks clean: suite count reproduced (308 OK, the 4 guard tests including the new refusal test); hook 10/14 and wrapper 3-check self-tests reproduced; guard refusal (exit 3 via `PIPESTATUS`), override, and probe behaviours reproduced; stale-parallel greps clean on live surfaces; root vs detailed leads identical modulo link targets; no style hits; all guard citers consistent with the refusal behaviour; TODO 1.9 text agrees with the shipped files.

## Orchestrator triage

Both findings validated at the artefacts and IN-WINDOW. **F1 FIXED in this batch's diff** (the Verification bullet corrected to 10/14 with a provenance note). **F2 FIXED immediately** in scratch PR #64 (the section-1.9 row updated to the shipped a+b state and re-stamped at #620). O2 is queued into the section-1.9(c) widening item's notes at its build.

## Resulting hot-fix PR

Scratch PR #64 (the F2 half). Main-repo half: none (recursion-avoidance; F1 batches with this record and the history row).

## Notes

O1 keeps the section-1.9 next-session validation residual open and sharpens it (main-repo-rooted session required). The external worker's delivery cadence continued through this PR's window; deliveries are screened and merged scratch-side and do not interact with this PR's surfaces.
