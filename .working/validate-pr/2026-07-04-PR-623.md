# /validate-pr record: PR #623 (2026-07-04)

Frozen-state archive; `path:line` references are as-of-write and are kept verbatim even if lines later shift.

## Trigger and state snapshot

PR #623 ("Delta gate D6: pack-README version-history co-bump check; overnight-mode transition; #622 QA batch"), squash commit `0eaca27` on `main`, parent `4e9c6d3` (#622). Touched 19 files: the new [`tools/check-pack-readme-cobump-on-pr.py`](../../tools/check-pack-readme-cobump-on-pr.py) plus its four wiring surfaces and four fixtures (suite 312), the D1-D6 range refreshes (guard header; `.claude/CLAUDE.md` under the pre-authorized one-liner class), the TODO 3.15 bullet's TODO-to-DONE rotation, the TODO 3.1 F3 file-naming fix, the #622 QA batch, the handoff snapshot F1 fix, the overnight file to `in-flight`, the CHANGELOG pair, and README `2026.07.111`/`1.9.472`. Context: one pre-push verifier round (1 Medium, the missing DONE rotation, fixed pre-push; 1 nit phrasing, fixed).

## Subagent A return

Full dispatch (diff review plus cross-reference check, re-verifying the spec narrative line-by-line against the tool and re-running the fixtures, parity, and live probes). 1 low + 2 notes:

- **F1 (low, in-window).** `TODO.md`'s section-3.15 orphan-check bullet still carried the r3 review's "D6 candidate" label after this PR shipped the pack-README check AS delta gate D6: a live naming collision (the orphan check, if shipped, would be D7). One-word relabel.
- **F2 (note, in-window).** The considerations-ledger clause "the delta set is D1-D5 since the #469 D5 check" left stale by the CLAUDE.md D1-D6 refresh (the #573 precedent pairs the ledger annotation with the range refresh).
- **F3 (note, protected file, routed).** The close-out checklist's paired-surface bullet now UNDERSTATES instance (a) (D6 mechanizes it); no literal misstatement, so it routes to the next authorized `.claude/CLAUDE.md` touch rather than the one-liner class.

Checks clean: fixtures 4/4; suite measured 312; gate 35 parity green with the step name matching the registry verbatim; the spec 6.1 narrative re-verified against the tool's actual behaviour (trigger, row check, fail-loud, absent-at-head, no opt-out) with the only-in-file-history-table claim surviving refutation; live probes reproduced on the real #622/#623 ranges; rotation integrity (bullet gone, DONE entry present, no dangling live references); D1-D6 consistent on all live carriers; the #622 QA batch present with gate 50 green; the overnight gate green on `in-flight`; brief-freshness clean (10 staged briefs, 0 dead paths/anchors, 0 PRs behind); style clean.

## Orchestrator triage

F1 and F2 validated at the artefacts and FIXED in the working tree immediately (the D7 relabel with its provenance clause; the ledger clause updated to the #623 state); both batch into the next PR per recursion-avoidance. F3 validated and ROUTED to the next authorized CLAUDE.md touch (recorded in the improvement-log row as a carried candidate, the GR-8(b) carrier-scan shape).

## Resulting hot-fix PR

None (F1/F2 batch forward; F3 routed).

## Notes

The naming-collision class (a review-coined candidate label overtaken by a shipped artefact's official number) is new; single occurrence, observation stage. The wave-5 seeding and the #78-#80 delivery merges ran in this PR's boundary window; their records live scratch-side and in the next PR's entries.
