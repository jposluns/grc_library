# /validate-pr record: PR #629 (2026-07-04)

Frozen-state archive per the `.working/` convention; cross-references accurate as-of write-time.

## Trigger and state snapshot

Formal post-merge `/validate-pr` on PR #629 (the maintainer-authorized CLAUDE.md touch bundle: RM-10 widening, seven staged close-out-checklist clauses, D-F4+F6 rewords, hook set widening, 2026-07-04 decision-round bookkeeping, #628 QA batch), squash merge `4ab8f6d289d26a5e313fe06a4a37533a8ee36b00` to `main`, 15 touched files. State at dispatch: library `2026.07.117`, README `1.9.478`, pack `1.53.22`, gate count 65, suite 326, register `1.0.353`, validate-pr history `1.2.407`. Mechanical baseline on post-merge `main`: `tools/run_all_audits.sh` standalone, direct exit capture, exit 0, all 65 gates passing. Pre-flight scanner filtered to the touched files: zero candidates. Two pre-push verifier rounds ran on this PR before merge (round 1: six findings, all fixed in-branch in the amend; round 2: CONFIRMED-FIXED with clean class-width probes), so Subagent A was briefed to hunt beyond those rounds' probes.

## Subagent A return

Two findings (SARIF-lite, verbatim rubric and evidence, both quoted at `path:line`):

- tool subagentA, ruleId a1, level warning, location `.working/session-handoff.md:44`, fingerprint `handoff-snapshot-four-QA-ledger-values-not-in-diff`. Rubric: inferred-as-verified / stale prose: the Current-truth snapshot clause asserts "runbook `1.1.3`, guardrail-history `1.0.5`, validate-pr history `1.2.407` and improvement-log `1.0.353` (all four QA-ledger values are IN the #629 diff, not riding beyond it)", but the diff's 15-file set contains neither `.working/multi-session-orchestration.md` (runbook) nor `.working/guardrail-reviews/history.md` (guardrail-history): both shipped in #628 and carry forward unchanged, so the literal claim is false for two of the four values. Orchestrator apply-time verification: CONFIRMED against `git diff 4ab8f6d^..4ab8f6d --name-only`. This is the FOURTH occurrence of the snapshot-line append-not-reconcile class (#619 F1, #622, #628 F2, now #629 a1), introduced by the very reconcile that shipped the reconcile-not-append checklist widening.
- tool subagentA, ruleId c1, level warning, location `CHANGELOG.md:9` + `.working/changelog-details/CHANGELOG-detailed.md:11` + `.working/DONE.md:35` + `.working/session-handoff.md:36`, fingerprint `eight-staged-1.9c-clauses-overcount-actual-seven`. Rubric: incorrect count with multi-surface spread: all four carriers say "eight staged 1.9(c) clauses" (handoff: "eight-plus") while each enumeration lists exactly SEVEN (#614, #594, #593, #612, #619, #625, #620), and the live CLAUDE.md checklist carries seven distinct staged-clause edits (the #622 routed candidate merged INTO the #619 line's widening rather than landing as an eighth clause, which is the likely miscount source). Secondary same-root observation: `CHANGELOG-detailed.md:15` says "the eleven authorized-bundle edits" while its parenthetical enumerates ten. Orchestrator apply-time verification: CONFIRMED by re-reading all four carriers and counting each enumeration. An instance of the count-drift class the PR's own #612/#620 clauses target, landed by the PR that shipped them.

Classes (b) citation attribution, (e) version bumps, (f) generated artefacts, (g) hook docstring, and (h) term drift verified clean by Subagent A with named checks (staged-clause attributions matched to originating register rows; README CalVer/Version and both QA-ledger bumps present; no corpus metadata in the diff; hook docstring matches the widened sets and the 14 blocked / 17 allowed self-test; `winding-down` consistent across CLAUDE.md and `resume.md`).

## Cross-reference check

Orchestrator pass over citers of the touched files, bare-token width for the closed items: `D-F4` citers are all frozen QA/CHANGELOG/DONE records or the handoff's accurate description of what #629 shipped; `GR-3` citers outside TODO are backward-looking provenance comments on the shipped waves 1-2 work in `tools/lint_common.py`, the date/version linters, and `tests/test_linters.py`, with TODO:183 correctly recording wave 3 as closed keep-as-is; `1.9(c)` citers outside frozen records are the hook's two backward-looking provenance comments describing the shipped widening. `S-a..S-e` survives only in a frozen historical CHANGELOG entry. No stale forward pointers found.

## Orchestrator triage

- a1 (in-window warning): ROUTED to the next PR's batch. Fix: reword the handoff:44 parenthetical to scope the in-diff claim to the two values actually in the #629 diff (validate-pr history, improvement-log), with runbook and guardrail-history already on `main` via #628. Not claimed fixed here; no fix exists in any diff yet (record-vs-diff clause).
- c1 (in-window warning): ROUTED to the next PR's batch. Fix: "eight" to "seven" on all four carriers (the handoff's "eight-plus" likewise), and reconcile the detailed entry's "eleven authorized-bundle edits" to match its own ten-item enumeration (or re-count the diff's edit set and correct the enumeration, whichever the re-count supports). Not claimed fixed here.
- Pattern escalation: a1 is the FOURTH append-not-reconcile snapshot occurrence and c1 is a count-drift instance landed by the PR shipping the count-reflex clause; together they are direct evidence for the TODO 4.7 GR-P3 thesis (checklist prose reduces but does not extinguish a failure class; only a mechanical check does). Handled in the #629 `/retro`.

## Resulting hot-fix PR

None. Both findings batch into the next PR per the recursion-avoidance default.

## Notes

Both post-merge findings are bookkeeping-surface staleness of exactly the two classes this PR codified checklist lines against (append-not-reconcile; count-reflex), caught by the formal sweep despite two pre-push verifier rounds. The retro records the pattern signal and the mechanical-check candidate (a snapshot-line version-value freshness check) so the class graduates beyond prose per GR-P3.
