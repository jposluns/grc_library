# /validate-pr record: PR #630 (2026-07-04)

Frozen-state archive per the `.working/` convention; cross-references accurate as-of write-time.

## Trigger and state snapshot

Formal post-merge `/validate-pr` on PR #630 (S3 PR B: the claim-fit skill, eighteenth, pack 1.54.0, plus the one-time Tier-A adoption pass and the #629 QA batch), squash merge `487a8a2d4c960c8063b0574b266156f5b79081b3` to `main`, 26 touched files. State at dispatch: library `2026.07.118`, README `1.9.479`, pack `1.54.0`, gate count 65, suite 326, register `1.0.354`, validate-pr history `1.2.408`, claim-fit history `1.0.1`. Mechanical baseline on post-merge `main`: `tools/run_all_audits.sh` standalone, direct exit capture, exit 0, all 65 gates passing. Pre-flight scanner on the touched set: two candidate blocks, both adjudicated false positive by Subagent A (the setup-generator-overlay five-skill vetting disclosure; the guardrail-review comparative-prose class). Two pre-push verifier rounds ran before merge (round 1: 4 findings + 3 nits fixed in-branch, including the verifier-caught register:104 sixth misattribution carrier; round 2: CONFIRMED-FIXED with empirical census verification at both tree states); Subagent A was briefed beyond their probes.

## Subagent A return

One finding (SARIF-lite, verbatim rubric and evidence):

- tool subagentA, ruleId c1, level note, location `.working/claim-fit/history.md:17` (related `.working/claim-fit/2026-07-04-tier-a-adoption-pass.md:22`), fingerprint `six-carrier-locations-vs-seven-line-locations-granularity`. Rubric: multi-surface count-label consistency / the round-2-flagged "carrier" granularity residual. Evidence: the history Summary cell states "Five documents corrected across six carrier locations" while its own enumeration lists SEVEN path:line locations (ops 1, due-diligence 2, register 2, breach-response 1, supplier-assurance 1); "six" is the FIX count (the run record's six numbered fixes; the commit message's "6 attribution-phrasing fixes"), not the location count, and the run record's "caught a SIXTH carrier" is sixth at fix granularity but seventh at line-location granularity. Orchestrator apply-time verification: CONFIRMED by re-reading the cell and counting its own enumeration. A new unlabelled use of the granularity the round-2 verifier flagged as an observation.

All other classes clean with named checks: the handoff Current-truth line reconciled with all quoted values matching shipped state (the post-merge "in flight" framing being the designed one-PR lag); the FR-120/#294 provenance and every run-record source reference verified against the texts (Art 33(1)/(2), Arts 18/19, Annex IV, ISO/IEC 42001 :1097); 18 skills on every enumerated surface with the parity linter green at 9 pairs; all five corpus carriers Version+Date co-bumped (the register's two-step 1.0.14 to 1.0.16 matching its two sequential fixes); both generated-artefact checks exit 0; the tool docstring's 8-row census empirically reproduced; the four-verdict vocabulary identical across all six carrying surfaces.

## Cross-reference check

Orchestrator pass over citers of the touched files: the closed TODO section-1.4 whole-repo grep is clean (survivors are frozen records, backward-looking CHANGELOG provenance, and an unrelated document-internal §1.4). ONE in-window finding: the TODO P1-intro cadence enumerations (`TODO.md:17` and `:24`, "the routine `/validate`, `/validate-pr`, and `/matrix-fit` cadences") omit `/claim-fit`, a family this PR's own ship widened (the #614 enumeration-width class, on an enumeration the PR itself extended). ONE out-of-window observation: the protected [`.claude/CLAUDE.md`](../../.claude/CLAUDE.md) carries a `/matrix-fit` cadence section (line 514) with no `/claim-fit` sibling; a candidate clause for the NEXT authorized CLAUDE.md touch (the skill, command, and TODO carry the cadence in the interim), surfaced at the retro.

## Orchestrator triage

- Subagent A c1 (in-window note): ROUTED to the next PR's batch. Fix: reword the history cell to "six fixes across seven carrier locations" (and align the run record's "SIXTH carrier" clause to its fix-list self-label). Not claimed fixed here.
- Cross-ref (in-window Low): ROUTED to the next PR's batch. Fix: add `/claim-fit` to both TODO cadence enumerations.
- Cross-ref (out-of-window): the CLAUDE.md `/claim-fit` cadence-sibling clause staged as a candidate for the next authorized touch; recorded in the retro's Proposed-improvement cell.

## Resulting hot-fix PR

None. Both in-window items batch into the next PR per the recursion-avoidance default.

## Notes

Zero substantive-content findings on a PR that changed five corpus documents' normative-attribution phrasing: the two pre-push verifier rounds and the judge's quoted-passage evidence held up under the post-merge sweep. The two residual items are count-label granularity and an enumeration the PR itself widened, both bookkeeping-surface classes.
