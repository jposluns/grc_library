# /validate-pr record: PR #637 (2026-07-04)

Frozen-state archive; cross-references accurate as-of write-time.

## Trigger and state snapshot

PR #637 (the TODO section 3.19 decision-tree hygiene bundle) squash-merged to `main` at `684c96b` (parent `9f6fbd1`), 13 touched files. Post-merge mechanical baseline: `tools/run_all_audits.sh` standalone, direct exit capture, all 65 gates passed (exit 0). Pre-merge QA context given to the subagent as not-to-re-report: two refute-briefed verifier rounds (six round-1 findings dispositioned, one round-2 wording amendment).

## Subagent A return

One finding (Low, class c enumeration undercount), SARIF-lite summary:

- **F-1 (Low, c)** `TODO.md:214-216` (item 3.25, added by this PR): the "Five acronyms" list omits bare `AI`, which meets the item's own criterion identically to the claimed `ML` residual: decision-tree lines 53/91/99/237 use bare AI with no first-use expansion (`grep -in 'artificial intelligence'` returns nothing), the glossary has no bare `**AI**` row (only AICM/AIDA/AIGC/AIMS/AIS), the register's Purpose section carries no household-term carve-out, and the acronym linter has no AI exemption. Failure scenario: the 3.25 applier fixes exactly the enumerated five and closes the item, leaving ML resolved while its compound-sibling AI is not. Suggested disposition: fold AI into the list, or record the lexicalized-word adjudication (which would also justify striking ML).

All other classes clean, with the notable verifications: D7 re-run on the merge range (9 tokens OK, exit 0); both 3.15 gate-widening bullets' technical claims re-verified against the live linter regexes; the 3.25 five-acronym line claims re-read live (CPRA 83, AI/ML 99, PIA 140, BC/DR 145, BC/CM 146); both expansions consistent with their glossary rows; the size-band note's cue text verbatim-identical to `docs/adopter-guide.md:115`; both generators check-clean; decision-tree confirmed absent from taxonomy/scorecard (glossary-only generated diffs correct). One protocol-honest unverified item: the PFMI attribution grounded in recall plus the recorded pre-merge BIS check (offline sweep), consistent, not a finding.

## Cross-reference check

Orchestrator pass over citers of the two touched documents (portal, adopter-guide, worked-example, quickstart, roadmap templates, registers): no version pins on either doc, and the adopter guide's Tier 1 cue line matches the new note's quotation verbatim. Clean.

## Orchestrator triage

F-1: IN-WINDOW (the PR introduced item 3.25 with the undercount). Independently re-validated (no "artificial intelligence" expansion in the doc; no bare AI glossary row), then FIXED in the next PR's batch per recursion-avoidance: the 3.25 list folded from five to six naming the bare-AI carrier lines, with the alternative lexicalized-word adjudication recorded inline as the applier's call to surface.

## Resulting hot-fix PR

None; the fold batches into the next PR (recursion-avoidance).

## Notes

Third consecutive sweep in which the only surviving finding class is a count/enumeration claim authored in the same diff (the #636 five-vs-four file count, the #637 five-vs-six acronym list): the figure-drift family's evidence base for the EXPEDITE-the-recount-clause proposal (the #636 retro) keeps growing.
