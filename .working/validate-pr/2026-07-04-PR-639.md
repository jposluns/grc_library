# /validate-pr record: PR #639 (2026-07-04)

Frozen-state archive; cross-references accurate as-of write-time.

## Trigger and state snapshot

PR #639 (TODO 3.21's build-only half, with FQ-B1 descoped at the verifier-loop cap by maintainer decision) squash-merged to `main` at `d3131db` (parent `5a91fe9`), 17 touched files. Post-merge mechanical baseline: `tools/run_all_audits.sh` standalone, direct exit capture, all 65 gates passed (exit 0). Pre-merge QA context given to the subagent as not-to-re-report: three refute-briefed verifier rounds culminating in the loop-cap descope.

## Subagent A return

Two findings (both Low, one class: pre-descope phrasing that survived the descope reconcile):

- **F-1 (Low, a/c)** `TODO.md:194-196`: the rescoped 3.21 heading and intro still said "four build-only fixes SHIPPED (the gate-13 misstatement reword, ...)" and "Two decision-parked residuals remain" against the paragraph's own three lettered residuals and every sibling surface's "three"; listing the "gate-13 misstatement reword" as shipped contradicted residual (c) three sentences later. Failure scenario: a future session drops residual (c) or re-closes FQ-B1 as shipped.
- **F-2 (Low, same class)** detailed mirror `#639` entry Changed bullet: "the two decision-parked residuals; the four build-only fixes rotated" against the same entry's own lead ("three") and the DONE entry it cites ("Three validated fixes shipped").

Clean elsewhere, notably: the FQ-B1 revert byte-identical across the merge (spec-master untouched, 1.6.5, by design); D7 re-run on the merge range (9 tokens OK); the corrected gate-13 docstring verified against the tool's actual `current < prior` behaviour with the CHANGELOG-coupling attribution grounded in gates 29/59; the pack history row's CalVer cell matching README; both generators check-clean; the Discipline-observations three-round narrative consistent with TODO 3.21(c).

## Cross-reference check

Orchestrator pass: no surface outside the F-1 paragraph claims the FQ-B1 spec fix shipped; the parked matrix C-TPAT cell is the corpus's only remaining C-TPAT carrier; ERC's first use matches the glossary row. Clean.

## Orchestrator triage

F-1 and F-2: IN-WINDOW (the descope reconcile in the same PR missed these two carriers). Both FIXED in the next PR's batch per recursion-avoidance: the 3.21 heading/intro rewritten to "three build-only fixes SHIPPED" with the docstring fold named as distinct from residual (c), and the Changed bullet aligned to "three ... three"; the contradiction grep for `two decision-parked|four build-only|four validated` returns zero across all five carrier surfaces.

## Resulting hot-fix PR

None; both fixes batch into the next PR (recursion-avoidance).

## Notes

The class is the descope's own append-not-reconcile shape: the reconcile edited the lead, DONE, and handoff but missed the TODO intro (authored pre-descope in the same diff) and one detailed-entry bullet. Same-diff self-consistency between a rescoped item's heading/intro and its residual list is a cheap future checklist half-line; folded to the recount-clause evidence base rather than a new proposal.
