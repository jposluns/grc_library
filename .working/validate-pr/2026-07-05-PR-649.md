# /validate-pr record: PR #649 (2026-07-05)

## Trigger and state snapshot

Formal post-merge PR-scoped validation sweep on PR #649 (merged squash `28aeb26`), the standards-currency linter `v`-prefix widen that closes TODO 3.22. Triggered by the merge, per the mandatory per-PR QA cadence. Post-merge HEAD `28aeb26` on `claude/resume-planning-questions-dd9mxk` (= `main`). Library 2026.07.137, README 1.9.498, 66 gates, pack 1.54.2. Mechanical baseline: `tools/run_all_audits.sh` standalone returned all 66 gates passing on the post-merge state (PIPESTATUS direct-captured 0; the squash created a fresh commit, so the history-aware gates 40/31/45 re-evaluated against the new commit date and stayed green). Pre-flight scanner: no candidates in the 9 touched files. Diff = 9 files: `tools/lint-standards-currency.py` (the `v?` separator-regex widen + inline comment), `tests/test_linters.py` (2 StandardsCurrencyTests fixtures), `TODO.md` (3.22 block deleted, family-label residual moved to 3.21(d), the "Three"->"Four" pre-push count fix), `.working/DONE.md` (#649 entry), `CHANGELOG.md` + `.working/changelog-details/CHANGELOG-detailed.md` (#649 entry), `README.md` (version bumps), and the batched #648 QA rows.

## Subagent A return

Subagent A (general-purpose, refute-briefed, pre-tool verification preamble) scanned the 9 touched files against the eight failure-mode classes, reading each touched file in full, running a whole-repo `3\.22` grep, verifying the canonical register, and running the linter-regression suite. Verbatim result:

**FINDINGS: 1** (one note-level).

### Finding 1

- ruleId: `stale-prose-ref`
- level: `note`
- location: `.working/changelog-details/CHANGELOG-detailed.md:24`
- fingerprint: `stale-prose-ref:.working/changelog-details/changelog-detailed.md:24`
- evidence: The #649 Verification bullet cited the #639 detailed-mirror carriers of the "three residuals" state as `detailed :190 / :202`, but the carriers are actually at `:191` (the #639 lead paragraph) and `:203` (the #639 Changed bullet); `:190` is blank and `:202` is the unrelated fresh-reader-validation Changed bullet. The `root :49` citation is correct. Grep-claim-fidelity class: the line numbers were written from a pre-edit grep and were not re-checked after the bullet's own insertion shifted the #639 entry down by one. Low materiality: a working-state self-verification note pointing at correctly-characterized frozen provenance; no corpus, gate, or decision impact.

Classes (b) mis-attributed-citation, (c) multi-surface-incompleteness / §3.22-orphan, (d) inferred-as-verified, (e) version-bump-omission, (f) generated-artefact-lag, (g) stale-docstring, and (h) cross-document-term-drift were all cleared with quoted evidence. Notably, Subagent A independently corroborated the orchestrator's judgment that the two `TODO 3.22 half b` provenance comments (`tools/lint-standards-currency.py:182`, `tests/test_linters.py:510`) are deliberate provenance consistent with the codebase's `Phase 23.64:` convention, not §N-orphan defects; and that the `session-handoff.md:36` / `session-state.md:9` forward-queue references to 3.22 are expected recursion-avoidance batching lag (the handoff self-documents "drift forward is expected"), not an in-diff #649 orphan.

## Cross-reference check

Orchestrator cross-reference pass:
- Citers of the touched `tools/lint-standards-currency.py`: 7 live citers (`governance/register-canonical-citations.md` :9 / :29, `governance/specification-audit-programme.md:87` the section-6 table row, `governance/procedure-standard-version-upgrade.md:81`, `governance/specification-citation-verification.md:9`, `dev-security/claude-rules/skills/citation-quote-verification/SKILL.md` :11 / :71, `.claude/CLAUDE.md:110`). All cite the gate at purpose level ("standards-currency audit", "citation currency", "detect stale references", the section-6 name, "gate 6 in the audit programme"); the `v?` widen changed neither the gate's purpose nor its section-6 name, so every citer still resolves. Clean.
- §3.22-orphan check (whole-repo `3\.22`, `.md` and `.py`): every surviving reference is a correct closure reference, deliberate provenance (the two `.py` comments), a pack-version false positive (`dev-security/claude-rules/README.md:455-456` = pack `1.53.22`), or a frozen historical record. No live open-item pointer to a closed section. TODO 3.22 heading confirmed deleted; TODO.md:196 3.21(d) correctly reads "(closed in #649)". Clean.

## Orchestrator triage

Finding 1 is IN-WINDOW (introduced by #649's own discipline bullet) and note-level. Fixed in this batch (bundled into #650 per recursion-avoidance, not a dedicated hot-fix PR): the `CHANGELOG-detailed.md:24` bullet was reworded to cite the #639 carriers by content (the root entry's lead and the detailed mirror's #639 lead paragraph plus its Changed bullet) rather than by absolute line numbers, which removes the off-by-one and future-proofs the note against later CHANGELOG prepends. Fix verified present in the working tree (the ":190 / :202" substring is gone; the reworded phrasing is in place). Recorded here as fixed-in-batch (#650).

## Resulting hot-fix PR

None. Per recursion-avoidance, the note-level fix and this record's history row batch into the next PR (#650, the TODO 3.1 ISO/IEC 27001:2022 harmonization), not a dedicated hot-fix PR.

## Notes

- The core #649 change (the gate-6 `v?` detection-logic widen, its guard, the two regression fixtures, the 343-test suite, the clean corpus run, gate-count and section-6 completeness) was confirmed correct by both the pre-push refute-briefed verifier (REFUTED, 1 warning: the "Three"->"Four" count fix, applied pre-push) and this post-merge Subagent A pass (all substantive classes clean).
- The two verifier/sweep findings across #648->#649 are both the same shape: a count/enumeration or line-citation that went stale when text was inserted above it (the "Three"->"Four" pre-push catch; this ":190/:202"->content post-merge catch). Both are grep-claim-fidelity / count-drift class, gate-blind, low-materiality. The retro row for #649 notes the pattern.
