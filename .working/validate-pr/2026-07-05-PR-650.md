# PR-scoped validation sweep: PR #650

## Trigger and state snapshot

- **Trigger:** post-merge `/validate-pr` for PR #650 ("Harmonize ISO 27001 framework-alignment headers in five documents to ISO/IEC form (TODO 3.1 partial)"), merged squash `7d5b37c` (parent `28aeb26`).
- **State at sweep:** local branch synced to merged `main` (`7d5b37c` = `origin/main`); library `2026.07.138`, README Version `1.9.499`; gate count 66.
- **Diff:** `7d5b37c^..7d5b37c`, 15 touched files: 5 corpus docs (each one framework-alignment-table ISO header harmonized to the `/IEC` form + Version/Date co-bump), `README.md`, `TODO.md`, `CHANGELOG.md`, `taxonomy.yml`, `docs/maturity-scorecard.md`, and the `.working/` set (`changelog-details/CHANGELOG-detailed.md`, `pending-decisions.md`, `improvement-log.md`, `validate-pr/2026-07-05-PR-649.md`, `validate-pr/history.md`).
- **Mechanical baseline:** `tools/run_all_audits.sh` standalone on post-merge `7d5b37c` = "All 66 audit gates passed" (exit 0). Pre-flight scanner surfaced no stale-count/number-drift candidates in the touched files.
- **Pre-push verification (already done before merge):** one refute-briefed skeptical verifier on the descoped diff returned REFUTED with one note-level finding (a `pending-decisions.md` overstatement of gate 49's residual coverage), fixed in-branch; all seven substantive claims confirmed clean.

## Subagent A return

Subagent A ran over all 15 touched files and the eight failure-mode classes (a-h) plus four falsification targets. Classes (b) mis-attributed-citation, (e) version-bump-omission, (f) generated-artefact-lag, (g) stale-docstring (N/A, no `.py` touched), (h) cross-document-term-drift, and (a) stale-prose-ref all cleared with quoted evidence; all four falsification targets held (no bare header remains in the 5 docs; CHANGELOG root/detailed agree with no scope overclaim; TODO 3.1 residual internally consistent; taxonomy `version:` entries match the 5 docs' new Version fields). **2 findings, both note-level, both the same underlying issue** (class (c) multi-surface incompleteness, secondarily (d) deferral-rationale precision):

```
tool: subagent-a
ruleId: multi-surface-incompleteness
level: note
location: .working/changelog-details/CHANGELOG-detailed.md:21
fingerprint: multi-surface-incompleteness:.working/changelog-details/changelog-detailed.md:21
rubric: (c) multi-surface incompleteness / (d) deferral-rationale precision
evidence: "makes gate 49 SILENTLY skip ISO-column validation (no error, a false OK the pre-push guard would not catch)". Ground truth (tools/lint-matrix-control-codes.py:102 ISO_HEADER literal; :268-277 sets all four column indices only inside `if ISO_HEADER in cells and NIST_HEADER in cells:`, then `if iso_idx is None: continue` skips every body row): a renamed ISO header blinds ALL FOUR matrix columns, not the ISO column alone. The pre-push verifier flagged this exact "only the ISO column" framing and it was corrected in pending-decisions.md ("the entire matrix's control-code validation ... none of the four columns") and TODO.md 3.1(a) ("blinds the gate"), but the correction was not propagated to this detailed-mirror lead. Materiality low: not strictly false (the ISO column IS among the blinded columns), no corpus/gate/decision impact, the deferral conclusion is unchanged and correct.
```

```
tool: subagent-a
ruleId: multi-surface-incompleteness
level: note
location: CHANGELOG.md:9
fingerprint: multi-surface-incompleteness:changelog.md:9
rubric: (c) multi-surface incompleteness / (d) deferral-rationale precision
evidence: "renaming it silently blinds gate-49 ISO-column validation unless the gate constant, its regression fixtures, its docstring, and the audit-programme spec are updated in lockstep". Same understatement as the detailed-mirror finding; the adopter-facing root-summary compression of the same defect. The corrected parallel surfaces (pending-decisions.md, TODO.md 3.1(a)) describe it correctly.
```

Both findings share one root: the pre-push correction of the gate-49 residual-coverage framing was applied to `pending-decisions.md` and TODO but left un-propagated to the two CHANGELOG surfaces describing the same fact.

## Cross-reference check

The 5 harmonized docs are widely cited (core dev-security standards). Every citation is path-based (markdown links, gate-3-confirmed resolving on the post-merge state) and title-based (titles unchanged). PR #650 altered only header FORM (`ISO 27001:2022`/`ISO 27001/27002` to the `/IEC` form), the 5 docs' Version/Date, README CalVer/Version, the regenerated artefacts, and TODO/CHANGELOG/pending bookkeeping, none of which any citer reproduces verbatim. The compliance matrix and the FIT-8 register cite the 5 docs by path (unaffected) and carry their own deferred bare-ISO headers (the intended partial-harmonization state, not a cross-ref break). Cross-reference check: **clean**.

## Orchestrator triage

- **Both findings: in-window** (introduced by PR #650 itself: the descoping applied the gate-49 residual-coverage correction to 2 of the 4 parallel surfaces and left the two CHANGELOG carriers understated).
- **Disposition: fixed in-batch.** Both CHANGELOG surfaces reworded to match the corrected `pending-decisions.md`/TODO precision:
  - `CHANGELOG.md:9`: "blinds gate-49 ISO-column validation" to "disables gate 49's matrix control-code validation (the bare header is gate 49's machine key, so a rename skips all four matrix columns, not the ISO column alone)".
  - `.working/changelog-details/CHANGELOG-detailed.md:21`: "skip ISO-column validation" to "skip its entire matrix validation (the bare header is the gate's machine key for all four columns, so a rename leaves every column index unset and the `if iso_idx is None: continue` guard skips every matrix body row: ...)".
- **Fix verification (record-asserts-applied-fix guard):** grep confirmed the "ISO-column validation" understatement is gone from both surfaces and the corrected wording is present; validated against the live linter (`tools/lint-matrix-control-codes.py:268-277`).
- The two TODO-3.1 deferrals (matrix gate-49 coupling; FIT-8 register cells) are intentional maintainer-pending decisions, not defects; not triaged as incompleteness.

## Resulting hot-fix PR

None. Per recursion-avoidance, the two in-batch fixes plus this record and the history/`/retro` rows batch into the next substantive PR (no dedicated hot-fix PR).

## Notes

- Both the pre-push verifier and the post-merge Subagent A independently converged on the gate-49 residual-coverage precision issue: the verifier caught the false-affirmative in `pending-decisions.md`; Subagent A caught that the correction was not fully propagated across the parallel CHANGELOG surfaces. The two-layer defence surfaced both the original imprecision and its incomplete-propagation, no escape.
- History row batches into the next PR; closing PR recorded there once known.
