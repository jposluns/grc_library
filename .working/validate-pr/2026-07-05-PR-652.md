# /validate-pr record: PR #652 (2026-07-05)

## Trigger and state snapshot

Post-merge PR-scoped sweep for **PR #652** (squash `cb261cd`, parent `6b78c67`), "Switch to daytime-unattended mode; apply first deferred-protected CLAUDE.md batch". Diff `6b78c67..cb261cd`, 11 files, all `.claude/` / `.working/` / `README.md` / `CHANGELOG.md` / `TODO.md` (no corpus document touched). Post-merge mechanical baseline: `tools/run_all_audits.sh` standalone at `cb261cd` = **66/66 green**. Library `2026.07.140`, README `1.9.501` at HEAD. Subagent A dispatched (general-purpose), scoped to the #652 diff, refute-briefed with the pre-tool verification preamble; the post-#652 `4c7f0c5` handoff-capture commit was explicitly excluded from scope.

## Subagent A return

2 findings, both note-level, both in `.working/` bookkeeping files, both the multi-surface-incompleteness class (a summary surface marked resolved/landed while its paired detailed entries were left in open framing):

- **note** `resolved-items-retained-under-open-pending-heading` at `.working/pending-decisions.md:15`: the Status line (line 3) states the four 2026-07-05 items were RESOLVED, but the four entries below stayed under the "## Pending (open ...)" heading with "ROUTED, not edited" / "deferred-blocked" headings and a "Not edited pending the maintainer's direction" body, contradicting the Status line and liable to re-surface at the next `/resume`.
- **note** `landed-subitem-left-as-open-todo` at `.working/deferred-protected-changes.md:65`: the header note recorded item 5(a) as landed in #652, but item 5's body still listed sub-item (a) as an un-started reword with "surface the two options at daytime", no landed marker.

Clean (verified by Subagent A, quoted `path:line`): TODO 1.11's four Brazilian designations at the exact cited lines (annex :29 / :43, procedure :177 / :321); version-surface agreement (2026.07.140 / 1.9.501 across README, both CHANGELOGs, handoff); mode-switch completeness (every current-mode marker reads DAYTIME-UNATTENDED, residual OVERNIGHT mentions historical); no duplicate version tokens in the reconciled Current-truth line; every #652 CHANGELOG path linked; Status reads "0 pending"; generated-artefact lag correctly N/A.

## Cross-reference check

Touched files are `.claude/` / `.working/` / `README.md` / `CHANGELOG.md` / `TODO.md`; no corpus document cites them under the corpus gates, so the cross-reference surface is the working-state self-consistency the two findings cover. TODO 1.11's citations INTO the corpus (the Brazil annex and breach-response procedure) were verified present at the cited lines by Subagent A check 1.

## Orchestrator triage

Both findings apply-time re-verified against the live files (read `.working/pending-decisions.md:13-38` and `.working/deferred-protected-changes.md:62-73`): both CONFIRMED, both in-window (introduced by #652's incomplete rotation of resolved/landed items). Both note-level, working-state only, no corpus or adopter impact. Per recursion-avoidance both fixes are batched into the next substantive PR (#653, the codification PR), not a dedicated hot-fix PR.

## Resulting hot-fix PR

None (recursion-avoidance): the two fixes are folded into #653 as commit `f543ee0` ("Fix #652 /validate-pr findings: reconcile resolved-but-open-framed entries"), which marks deferred-protected 5(a) DONE/landed and gives each of the four pending-decisions entries an inline RESOLVED-2026-07-05 marker with a no-longer-open closing context line.

## Notes

The two findings share one root cause: when #652 marked a summary surface resolved/landed (the pending-decisions Status line; the deferred-protected header note), the paired detailed entries were not updated to match. This is the same update-one-surface-leave-the-pair-stale class the close-out checklist's paired-surface guards target; recorded in the #652 `/retro` row. Originating PR #652; closing PR #653.
