# /validate-pr record: PR #656

## Trigger & state snapshot

- **PR:** #656 (merged squash `b958fb5`, parent `3cad7fa` = #655). Diff `3cad7fa..b958fb5`, 11 files.
- **Change:** the pack half of TODO 3.15 D-F3, the `evidence-grounded-completion` rule's three corollaries (un-observable state; inventory; external-version currency) named across three pack enumeration surfaces (pack `CLAUDE.md` one-liner, pack `README.md` tree blurb, pack `README.md` scope row), one condensed clause each, plus version bumps and batched bookkeeping.
- **Versions at HEAD:** library `2026.07.144`, README `1.9.505`, pack `1.54.6`; gate count 66; suite 343 (the bookkeeping-parity regression fired then cleared during the pre-push guard's first run, see Notes).
- **Mechanical baseline:** `tools/run_all_audits.sh` standalone on `b958fb5` = exit 0, all 66 gates (captured via file redirect + direct exit code, unpiped).
- Iteration 1 (single-cycle).

## Subagent A return

Subagent A (general-purpose, refute-briefed, pre-tool falsifier preamble) assessed all 11 touched files at post-merge state plus the ground-truth rule section, the parent-commit improvement-log via `git show 3cad7fa`, the 1.51.1 provenance row, and `DONE.md`. Method note: the agent was long-running (a thorough 11-file pass, ~250k subagent tokens); the orchestrator prematurely stopped it at ~5 minutes on a mis-read stall signal, then resumed it with its context intact to complete the one remaining read and compile. (Process lesson captured in the #656 `/retro`.)

**Verdict: CLEAN on the core change. Totals error 0 / warning 0 / note 1 (in-window).**

- Core change clean: the three clauses faithfully name the corollaries against `evidence-grounded-completion.md:46-54` (one deliberate, labelled, non-falsifying condensation: "or memory" dropped from the currency never-list on the pack CLAUDE.md one-liner; "answered only by the upstream source" still excludes memory). Version coherence confirmed (`README.md:4-5` library `2026.07.144` / README `1.9.505`, zero stale-token residual; pack README `Version: 1.54.6` matches its `## Version history` row at `:453`; the "(added ... in 1.51.1)" provenance is accurate against the 1.51.1 row at `:486`). Partial-close correct (`TODO.md:181` D-F3 present and rescoped, not deleted; no `DONE.md` #656/D-F3 entry; project `.claude/CLAUDE.md` correctly not touched). improvement-log repair well-formed (post-merge `:51`/`:52` each exactly 7 columns, monotonic, no lost content).
- **NOTE (in-window), `.working/changelog-details/CHANGELOG-detailed.md:22`, ruleId inferred-as-verified-state-assertion (also stale-prose-reference).** The #656 entry's "Batched" bullet describes the improvement-log repair as fixing a `#654` row "that #655 left malformed", claiming its prefix was dropped and "no row carried `#654` in the PR column" at the pre-state. The merge-base bytes contradict this: `git show 3cad7fa:.working/improvement-log.md` line 51 is a **well-formed 7-column `#654` row** (prefix intact), followed by `#653` at line 52, with **no `#655` row** and Version `1.0.378`. So #655 shipped a correct `#654` retro row; the malformation was introduced during #656's OWN prep (the `#655`-retro-row insertion fused the `#654` cells and dropped its prefix), which gates 50/36 then caught at #656 pre-push. The repair itself is sound; only the narrative's causal attribution is wrong.

## Cross-reference check

Files citing the touched pack surfaces resolve unchanged. The `evidence-grounded-completion` rule is cited by many pack and `.claude/` surfaces by path; the #656 additions are purely additive clauses naming existing corollaries, so no citer's quoted text or section reference is staled. The pack README `Version`/history-row pair is internally consistent. No broken cross-reference introduced.

## Orchestrator triage

- **The note is CONFIRMED (apply-time-verified against `git show 3cad7fa:.working/improvement-log.md`, which shows the well-formed 7-column `#654` row and no `#655` row at the merge base).** It is IN-WINDOW (the inaccurate narrative was authored in #656's diff).
- **Impact:** bounded to frozen `.working/` audit-trail prose (corpus-gate-exempt); no corpus, version, generated-artefact, or adopter effect; the actual repair and the current improvement-log are correct.
- **Disposition: FIX (routed to #657), not surface-not-fix.** Unlike the #653 "headline"/"bare outcome" synonym note (a defensible paraphrase), this is a false causal claim in the audit trail. Per the accuracy discipline (correct an identified error explicitly, do not leave it standing), the #656 detailed bullet is corrected in #657 to describe the actual cause (a #656-prep row-insertion malformation, caught by gates 50/36 at #656 pre-push), with the #657 CHANGELOG entry flagging the correction transparently rather than silently rewriting the frozen entry. The correction batches into #657 alongside its substantive content.

## Resulting hot-fix PR

None dedicated. The narrative correction batches into #657 (the next PR, the `.claude/CLAUDE.md` D-F3-remnant + 3.15 r5 bundle), per recursion-avoidance.

## Notes

- The pre-push guard's FIRST run (on `75999f1`, the pre-amend #656 commit) failed gates 50 and 36 on the malformed `#654` retro row; the orchestrator repaired the improvement-log (split the fused row back into two 7-column rows) and the re-run guard was green (66/66 + all PR-time checks). The suite count moved with the bookkeeping-parity regression toggling; final green state is authoritative.
- Scanner candidate `dev-security/claude-rules/README.md:227` [PF-collection-skills] `captured '5', expected '18'` triaged as a FALSE POSITIVE (the '5' is the addyosmani external-source vetting count, "5 skills in full + 19 spot-scanned", not the pack's 18-skill count; corroborated by the 1.51.3 history row at `:484`) AND out-of-window (L227 not in the #656 diff). No action.
- The `#654` retro row's FR-closed cell was reworded during the repair (from "TODO 3.18 CLOSED (...)" to "TODO 3.18 pack half CLOSED ... across the four `dev-security/claude-rules/ai/` files"); accurate to 3.18's pack half, though a frozen-row reword; noted for completeness, no action.
