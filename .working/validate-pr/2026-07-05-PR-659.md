# /validate-pr record: PR #659

## Trigger and state snapshot

- **PR:** #659 (merged squash `184f727`, parent `0c3fe84` = #658). Diff `0c3fe84..184f727`, 9 files.
- **Change:** two grouped TODO 3.15 r5 close-out-checklist clauses in `.claude/CLAUDE.md` (grouped by logical fit per the maintainer's 2026-07-05 direction): (1) the reconcile bullet names the D7 handoff-snapshot freshness PR-time check as the mechanical backstop for the version-token half of the `Current truth` reconcile (gate-50 naming pattern); (2) a new summary/description-lag close-out-checklist bullet. Plus version bumps and batched bookkeeping.
- **Versions at HEAD:** library `2026.07.147`, README `1.9.508`, pack `1.54.6` (unchanged); gate count 66; suite 343.
- **Mechanical baseline:** `tools/run_all_audits.sh` standalone on `184f727` = exit 0, all 66 gates (file-redirect + direct exit capture, unpiped).
- Iteration 1 (single-cycle). Pre-flight scanner: no candidates in the 9 touched files.

## Subagent A return

Subagent A (general-purpose, refute-briefed, pre-tool falsifier preamble) read all 9 touched files in full plus the D7 source in full. **Verdict: 1 finding (note-level, in-window), otherwise CLEAN (six of seven claims held).**

Six claims held: (1) D7 clause accuracy confirmed against `tools/check-handoff-snapshot-on-pr.py` (fires on handoff-touch L196-198; fails on token-vs-live-header mismatch L230-251; fails on more than one token per surface L223-229; version-tokens-only per docstring L25-27, naming the identical four prose halves). (2) the summary/description-lag bullet is correctly placed at `.claude/CLAUDE.md:394-398`, inside close-out-checklist item 2, after the intact "Paired-surface completeness" bullet and before the intact "Section-close cross-FILE cleanup" bullet. (3) no dashes or British -ise in the two added clauses (the 23 dash lines in CLAUDE.md are all pre-existing, matching the CHANGELOG's own claim). (4) version coherence (README 2026.07.147 / 1.9.508, no stale 2026.07.146/1.9.507; both CHANGELOG surfaces carry the #659 entry). (5) rotation consistent across DONE/TODO/deferred-protected for both clauses. (6) batched #658 rows well-formed (validate-pr history #658 Findings 0, improvement-log #658 retro, no fusion). (7) no stale `#661` /claim-fit pointer (all say #660).

## Finding (1, note-level, in-window)

- **NOTE, `.working/deferred-protected-changes.md:58`, ruleId cross-surface-occurrence-range-drift (classes stale-reference / cross-document-term-drift).** The summary/description-lag bullet body read ``the #652/#653 `/retro` candidate, at four in-window occurrences`` (self-contradictory: two PRs named vs "four"), while the four canonical surfaces #659 authored say `#650-#653` (`.claude/CLAUDE.md:395`, `CHANGELOG.md:9`, `.working/changelog-details/CHANGELOG-detailed.md:16`, `TODO.md:185`). It sits on a diff-context line the batch-B prefix edit reconciled the lead of but not the body of (the multi-surface / full-file-grep discipline at sub-bullet scope). Subagent A honesty caveat: it did not independently enumerate the four source occurrences; the value is established by the four-surface agreement plus the line's own "four" count, and corroborated at apply time by the handoff QUALITY SIGNAL enumeration `#650/#651/#652/#653`.

## Cross-reference check

No file cites a CLAUDE.md close-out-checklist bullet by an ordinal position or a total bullet count the insertion would shift (the checklist bullets are unnumbered). The "Paired-surface completeness" neighbour bullet is intact. The session-handoff Current-truth line is stale-by-three (still says merged-through-#656, #658 = the 3.15 clauses), the known batching lag; its refresh is due at #660 (the small-clause-run boundary), not a #659 defect.

## Orchestrator triage

- **The note is CONFIRMED** (apply-time read of `.working/deferred-protected-changes.md:58` showed the stale ``#652/#653`` token) and **IN-WINDOW** (#659 authored the same clause's range as `#650-#653` on four surfaces in the same PR).
- **Impact:** bounded to a frozen `.working/` staging file (gate-exempt, non-adopter-facing, scheduled to rotate out when #660 clears item 5); every canonical surface is correct.
- **Disposition: FIXED in-window.** Reconciled `#652/#653` to `#650-#653`, matching the four canonical surfaces and self-consistent with "four in-window occurrences". Grep-verified zero genuine residual (the one `#652/#653` grep hit is a substring of the correct `#650/#651/#652/#653` four-PR enumeration in the handoff QUALITY SIGNAL, not a stale token). The fix and this record batch into #660 per recursion-avoidance.

## Resulting hot-fix PR

None dedicated. The one-token fix and the QA rows batch into #660 (the next PR, the /claim-fit cadence clause) per recursion-avoidance.

## Notes

- The finding is the same class as the #659 `/retro` observation (a targeted edit reconciled a bullet's prefix but left an adjacent body value stale that the same PR authored differently elsewhere). The `/retro` records the OBSERVATION-level habit note (re-read the whole bullet body on a targeted prefix edit).
- The commit-subject PR-number-doubling habit fix (from the #658 `/retro`) was validated here: omitting `(#N)` from the #659 subject produced a single `(#659)` merged title, so that observation did not advance to SIGNAL.
