# /validate-pr record: PR #664 (gate-49 matrix ISO-header lockstep)

## Trigger and state snapshot

Post-merge PR-scoped validation sweep for PR #664 (squash commit `a92cf67`), the gate-49-coupled matrix ISO-header lockstep. 18 files touched (matrix + spec versioned docs; the gate-49/58/shared/matrix-fit tool files; the two gate-49 fixtures; generated artefacts; README; and the batched-#663 working-state files). Mechanical baseline: `tools/run_all_audits.sh` 66/66 standalone on post-merge main (exit 0); `run-linter-regression.py` 343 tests (exit 0).

## Subagent A return

Full 7-check sweep (Sonnet Subagent A, completed without stall). CLEAN on six: (1) gate 49 live exit 0 + injection of a bad Annex A code under the new header caught (`iso-annex-theme`, exit 1), so the gate is not silently disabled; (2) 0 residual bare `ISO 27001:2022` in the matrix; (3) `ISO_HEADER = "ISO/IEC 27001:2022"` exactly matches the matrix header cell; (5) version-bump completeness (matrix 1.11.11 / spec 1.16.57 / Date 2026-07-05; artefacts regenerated; live README 2026.07.152 / 1.9.513 matches every CHANGELOG-cited value); (6) gate-58 both-form preservation (exactly 4 bare `ISO 27001:2022` remain in `lint-document-iso-annex-a.py`, all input/both-form documentation); (7) cross-reference clean.

ONE finding (Low, in-window):
- **ruleId** `changelog-fixture-form-mischaracterization`; **level** note/low; **location** `.working/changelog-details/CHANGELOG-detailed.md` (#664 `tests/test_linters.py` bullet); **evidence**: the bullet claimed `DocumentIsoAnnexATests` has "bare-form fixtures test gate 58's both-form input acceptance", but `grep -c '"ISO 27001:2022' tests/test_linters.py` = 0 and the class's `_row_doc(...)` fixtures pass the joint `"ISO/IEC 27001:2022"` label (lines 5540/5547/5555/5571/5581); the bare occurrences at 5501/5509/5536 are test comments, not fixture data. **rubric**: (d) inferred-as-verified state assertion in bookkeeping prose.

## Cross-reference check

Citers of the touched files checked; the header rename + version bumps are form-only/non-breaking. The only whole-repo residual bare `ISO 27001:2022` outside `.working/`, `CHANGELOG.md`, and the gate-exempt pack tree is the deliberate gate-58 both-form clause at `governance/specification-audit-programme.md:68` (intended). No citer depends on the old bare matrix-header string. Orchestrator independently re-ran the residual/constant/gate-58/version/injection checks; all agree with Subagent A.

## Orchestrator triage

The finding is validated (CONFIRMED) and in-window. It is a documentation-accuracy defect in a `.working/` CHANGELOG mirror (no corpus content or gate affected; gate 58's bare-form acceptance is real, via its `ISO_LABEL_RE` regex). Fixed in-window: the bullet reworded to the measured joint-form statement, noting the bare-form input acceptance is provided by the regex rather than a literal-bare fixture, with a post-merge-correction marker. Per recursion-avoidance the fix, this record, the history row, and the `/retro` row batch into the next PR.

## Resulting hot-fix PR

None dedicated. The fix bundles into the next substantive PR per recursion-avoidance (the `/validate-pr` default).

## Notes

Third occurrence of the meta-prose-accuracy pattern (a bookkeeping/CHANGELOG claim characterizing artefact state without measuring it first): #662 (caveat introduced an unverified year), #663 (gap bullet asserted "left bare" without measuring the mixed distribution), #664 (this: "bare-form fixtures" without checking the fixtures are joint-form). Per the #663 retro, the third occurrence warrants a close-out-checklist codification line; surfaced to the maintainer in the `/retro` and proposed for the next protected-file (`.claude/CLAUDE.md`) touch.
