# PR-scoped validation sweep: PR #681

## Trigger and state snapshot

- **PR:** #681 (FR-99 per-control effectiveness metric across four assurance surfaces; TODO 2.4). Applied from the scratch-inbox worker delivery (Option A).
- **Merge commit:** `fb1a744`; merge base `d764d6e` (#680).
- **Mechanical baseline:** `tools/run_all_audits.sh` 66/66 on `main` at `fb1a744` (CI-verified at merge; the post-merge standalone confirms the merged state on `main` is clean).
- **State at HEAD:** library `2026.07.169`, README `1.9.530`, gate count 66, suite 346, rules 12, skills 18, commands 11.
- **Touched files (PR diff):** [`governance/register-digital-trust-and-assurance-metrics.md`](../../governance/register-digital-trust-and-assurance-metrics.md), [`compliance/procedure-control-testing.md`](../../compliance/procedure-control-testing.md), [`governance/framework-continuous-assurance-and-improvement.md`](../../governance/framework-continuous-assurance-and-improvement.md), [`governance/framework-metrics-monitoring-and-performance-reporting.md`](../../governance/framework-metrics-monitoring-and-performance-reporting.md), the regenerated [`taxonomy.yml`](../../taxonomy.yml) and [`docs/maturity-scorecard.md`](../../docs/maturity-scorecard.md), plus the bookkeeping surfaces (CHANGELOG root and detailed mirror, DONE, TODO, the batched #680 validate-pr and retro rows, README, session-handoff, session-state).

## Subagent A return

One finding, note level:

- **ruleId:** cross-document-term-drift (second-line-enumeration class)
- **level:** note
- **location:** `compliance/procedure-control-testing.md:218`
- **evidence:** "the second line (risk and compliance) aggregates and challenges it" abbreviates the canonical Three Lines Model second line (risk, compliance, information security, privacy, AI governance, and legal) to a two-function shorthand.
- **rubric:** in-window. The section 8.1 per-control-band sentence was introduced by #681, so the shorthand is a #681 drafting artefact, not a pre-existing issue. It is the same phrasing class the pre-push skeptical verifier flagged in the register (`:128`, `:123`) and continuous-assurance (`:121`), which #681 fixed at those cited instances but not at this fourth sibling instance: a fix-at-class-width miss (the verifier-loop discipline "fix at the width of the class, not the cited instances").

## Cross-reference check

Citers of the four touched files were re-checked for stale cross-references post-merge:

- The register's DTI-threshold anchor to `framework-governance-performance-and-improvement.md` §2 is intact.
- The control-testing result-class references (Effective, Observation, Deficiency, Material Weakness at section 4.1) and the escalation-response reference (section 4.2) are intact.
- The framework-continuous-assurance section 4.4 cross-reference to the register and the control-testing register resolves.
- 0 additional findings.

## Orchestrator triage

- The one note is in-window and real, verified against the canonical Three Lines Model in [`governance/register-key-terms-and-definitions.md`](../../governance/register-key-terms-and-definitions.md) (second line = risk, compliance, information security, privacy, AI governance, legal).
- **Fix:** change `compliance/procedure-control-testing.md` section 8.1 "(risk and compliance)" to "(oversight functions)", the umbrella term the register subsection and continuous-assurance section 4.4 now use, avoiding an incomplete enumeration. Bundled into **#682** (fr-15) per recursion-avoidance, with control-testing `1.1.0` to `1.1.1`.
- **Root cause:** fix-at-class-width miss; the pre-push verifier fix addressed the three cited instances but not the whole phrasing family. Recorded in the #681 `/retro`.

## Resulting hot-fix PR

No dedicated hot-fix PR. The fix bundles into **#682** (fr-15) per the recursion-avoidance rule; the history row's Hot-fix PR column records #682 as the closing PR.

## Notes

- All three defects of the second-line-enumeration class across #681's build were caught in-window: two by the pre-push skeptical verifier (register and continuous-assurance), one by this post-merge sweep (control-testing §8.1). Nothing escaped to a later session.
- The background-subagent offload stalled repeatedly this session (about five occurrences), so #681 was authored largely direct-in-orchestrator; this post-merge Subagent A dispatch completed normally.
