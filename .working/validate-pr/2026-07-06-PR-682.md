# PR-scoped validation sweep: PR #682

## Trigger and state snapshot

- **PR:** #682 (FR-15 maturity-ladder methodology standard; TODO 2.5). Applied from the scratch-inbox worker delivery.
- **Merge commit:** `fe1d0d6`; merge base `fb1a744` (#681).
- **Mechanical baseline:** `tools/run_all_audits.sh` 66/66 on `main` at `fe1d0d6` (post-merge standalone).
- **State at HEAD:** library `2026.07.170`, README `1.9.531`, gate count 66, suite 346, rules 12, skills 18, commands 11.
- **Touched files (PR diff):** the new [`governance/standard-maturity-assessment-methodology.md`](../../governance/standard-maturity-assessment-methodology.md) (`1.0.0`); [`docs/template-maturity-self-assessment.md`](../../docs/template-maturity-self-assessment.md) (`1.1.0`); [`governance/framework-governance-performance-and-improvement.md`](../../governance/framework-governance-performance-and-improvement.md) (`1.0.5`); [`governance/README.md`](../../governance/README.md) (`1.10.10`); [`governance/register-document-index-and-classification.md`](../../governance/register-document-index-and-classification.md) (`1.27.60`); [`compliance/procedure-control-testing.md`](../../compliance/procedure-control-testing.md) (`1.1.1`, the batched #681 §8.1 fix); the regenerated [`taxonomy.yml`](../../taxonomy.yml), [`docs/portal.md`](../../docs/portal.md), [`docs/maturity-scorecard.md`](../../docs/maturity-scorecard.md); [`README.md`](../../README.md); and the bookkeeping surfaces.
- **Pre-flight scanner:** 0 candidates in the touched corpus files.

## Subagent A return

Verdict: 0 error, 0 warning, 3 note-level observations. Full per-probe confirmations recorded below.

- **Note 1 (cross-surface-term-variance; class h; probe 1):** `governance/framework-governance-performance-and-improvement.md:39-47` presents the ladder as a "five-level scale" with a "Level" column, while the new standard §4, the template's `## Maturity tiers`, and the register DTI line say "Tier"/"5-tier". The five names (Initial, Managed, Defined, Quantitatively Managed, Optimized) and definitions are identical across all four surfaces, and standard §4 bridges the nouns ("the CMMI maturity-level model: five tiers"). Separately, `docs/template-maturity-self-assessment.md:43` attributes lineage to "NIST CSF Tiers, CMMI, and similar frameworks" while standard §4 and register `:65` attribute to CMMI (the five names are CMMI's; NIST CSF Tiers use different names). Both variances are pre-existing in surfaces #682 did not change.
- **Note 2 (scorecard-band-enumeration; class d):** `governance/standard-maturity-assessment-methodology.md:117` (§8) states the generated scorecard assigns "(Mature, Baseline, Draft)"; `docs/maturity-scorecard.md` also defines an "Unknown" catch-all band (currently 0 documents). The statement is accurate for every real document and matches the template's own three-band description at `docs/template-maturity-self-assessment.md:21-23`.
- **Note 3 (asymmetric-related-documents; class c):** `docs/template-maturity-self-assessment.md:6` gained three prose pointers to the new standard (:55, the floor-check line, review question 6) but its Related-Documents metadata field was not updated to include the standard, whereas the framework's Related-Documents field was. Reciprocal Related-Documents is not a corpus norm (3 of the standard's 5 related docs are non-reciprocal) and no gate covers it.

Per-probe confirmations (clean): (1) four-surface ladder/aggregation/limitation/floor-check/DTI consistency confirmed; (2) the new pointers resolve to the standard's real path and real sections (§5, §6, §7); (3) the standard is enumerated in both mechanical listing surfaces with consistent fields; (4) control-testing "(oversight functions)" matches the register umbrella phrasing (`:123`) and framework §4.4, sentence grammatical and meaning preserved; (5) floor-check §7 logic and §6 arithmetic correct (median of ten Tier-4 + one Tier-1 = 4; absolute floor catches Tier-1; relative 4-1=3 >= 2); (6) 13-field metadata block well-formed. Classes (a), (b), (e), (f), (g) clean: citations accurate and current; all six touched docs bumped Version and Date; taxonomy/portal/scorecard reflect the new doc and the bumps (template and governance/README are correctly absent from the generated set, which excludes `docs/` and `README.md` files); no Python touched.

## Cross-reference check

Citers of the touched files re-checked post-merge:

- `governance/register-digital-trust-and-assurance-metrics.md:65` references framework **§2 (Maturity assessment)**; the `### 2. Maturity assessment` heading is intact (framework `:37`). The §2 edit added a pointer paragraph within §2, so the DTI anchor resolves.
- Citers of `compliance/procedure-control-testing.md` reference "section 4" (result classes; e.g. register `:116`) and section 8, not the §8.1 second-line phrasing; the fix breaks no cited section.
- Citers of `docs/template-maturity-self-assessment.md` (the roadmaps, coverage-gaps) resolve; no headings were removed.
- The new standard's citers are exactly the four pointers added (template, framework, governance README, document-index register) plus the generated artefacts; all resolve.
- 0 findings.

## Orchestrator triage

- **Note 1: out-of-window.** The level-vs-tier variance and the NIST-CSF-Tiers-vs-CMMI lineage variance are pre-existing in `framework-governance-performance-and-improvement.md` §2 and `docs/template-maturity-self-assessment.md:43`, both unchanged by #682; the new standard is internally consistent and bridges the nouns. Surfaced to the maintainer as a corpus-wide harmonization decision (harmonize the framework §2 column to "Tier" and tighten the template lineage line, or leave the established prose); not fixed in-window (a corpus-wide harmonization, out of the fr-15 / fr-23 scope).
- **Note 2: defensible, no fix.** The three-band form matches the template's own :21-23 description and is accurate for every real document; the "Unknown" band is an empty catch-all. Enumerating it would diverge from the template's established description for a zero-document band.
- **Note 3: in-window, fixed in #683.** Adding the new standard to the template's Related-Documents metadata is the symmetric, correct completeness move (the standard already lists the template in its own Related Documents, and the template now materially depends on the standard as its authoritative methodology). Bundled into the next PR (#683, fr-23), template `1.1.0` to `1.1.1`.

## Resulting hot-fix PR

No dedicated hot-fix PR. The in-window Note 3 fix and this history row batch into **#683** (fr-23) per recursion-avoidance.

## Notes

- The #682 build's one in-window defect class (the control-testing §8.1 second-line shorthand) was the #681 `/validate-pr` finding, fixed in #682; this #682 sweep confirms it landed correctly ("(oversight functions)").
- Subagent A ran about 7.7 minutes; a mid-run status probe confirmed it alive (message queued for its next tool round), a long run rather than a stall.
