# `/validate-pr` record: PR #685 (Sweep-87 close-out)

## Trigger and state snapshot

- **PR**: #685, "Sweep-87 close-out: /validate over #680..#683, fix framework domain-scope, prune handoff, acquire lease". Merge commit `dc297ea`.
- **Run date**: 2026-07-07 UTC (the owed `/validate-pr` for #685; batched into the session-closing handoff PR #686 per recursion-avoidance). #685 merged 2026-07-06; its per-PR QA was owed into the next corpus/close-out PR.
- **Diff scope**: one corpus body change, `governance/framework-governance-performance-and-improvement.md` (the `## Scope` line: added "architecture", 10 -> 11 domains; Version `1.0.5` -> `1.0.6`, Date `2026-07-06`). Regenerated `taxonomy.yml` and `docs/maturity-scorecard.md`. Version surfaces `README.md` / `CHANGELOG.md`. `.working/` bookkeeping (session-handoff prune, session-state lease acquire, validate-sweeps history + `2026-07-06-sweep87-iter1.md`).
- **Mechanical baseline**: 66/66 gates green on the post-merge state (confirmed at resume; audit suite clean).

## Subagent A return

Refute-briefed skeptical verifier, scoped to the #685 diff plus the domain-enumeration completeness class. **1 finding; all other classes clean.**

### FINDING A-1 (multi-surface incompleteness / cross-document term drift), level: warning, OUT-OF-WINDOW

- **Location**: `governance/procedure-grc-programme-management-and-annual-review.md:29`.
- **Evidence** (quoted): "2.1 This procedure applies to all documents held in the GRC library, spanning the following domains: governance, risk, compliance, information security, privacy, artificial intelligence governance, resilience, supply chain security, IT operations, and development security."
- **Rubric**: this is a THIRD full "spans all governance domains / all documents" enumeration, parallel to the one #685 fixed. It lists 10 domains and OMITS architecture. Mapped to the 11 corpus domain dirs (ai, architecture, compliance, dev-security, governance, operations, privacy, resilience, risk, security, supply-chain), only architecture is absent. Its two sibling enumerations both carry 11 including architecture: `governance/standard-maturity-assessment-methodology.md:34` (the authoritative methodology standard) and `governance/framework-governance-performance-and-improvement.md:27` (fixed by #685). A procedure asserting it spans ALL library documents cannot omit architecture, which is a real corpus domain dir with documents.
- **Refutes #685's own completeness claim**: the Sweep-87 record `.working/validate-sweeps/2026-07-06-sweep87-iter1.md` asserts (around lines 109-110 and 114-115) that the framework Scope fix had "no parallel carrier" and "no parallel enumeration carrier survives". That termination claim is false: `procedure-grc-programme-management-and-annual-review.md:29` is a parallel enumeration carrier and it survives with the omission. The #685 fix was applied and asserted complete without greping the full enumeration family at bare-token width. This is the CLAUDE.md "Full-file-grep and parallel-case re-verification for prose corrections" guard's failure mode recurring: a fix applied to the named carrier, the class asserted closed without the corpus-wide family grep.
- **Orchestrator apply-time verification**: CONFIRMED at source (read line 29: 10 domains, architecture absent). Class boundary CONFIRMED by an independent corpus-wide grep for the enumeration family (`spanning the following domains` / `all governance domains` / `governance, risk, compliance`): exactly three full domain-model enumerations exist (methodology:34, framework:27, procedure:29); only procedure:29 omits architecture. The other grep hits (README:36, procedure-control-testing:21, procedure-continuous-improvement-register:21, matrix-cross-framework-alignment:39, specification-master-project:57) are deliberately-scoped prose or terminology lists, not the "spans all domains" completeness class, so they are correctly out of scope for this fix.

### Other classes

(a) stale prose references: 0. (b) mis-attributed citations: 0. (d) inferred-as-verified: the sole instance is the #685 sweep-record completeness claim, folded into A-1 (the corpus-side defect it points to). (e) per-document version-bump omission: 0 (framework bumped `1.0.5`->`1.0.6` + Date in the body-change commit). (f) generated-artefact lag: 0 (`docs/maturity-scorecard.md` shows the framework at `1.0.6 | 2026-07-06`). (g) stale docstrings: 0 (no Python touched). (h) cross-document term drift: covered by A-1.

## Cross-reference check

All citers of `governance/framework-governance-performance-and-improvement.md` resolve post-merge (Related-Documents lists in `compliance/procedure-control-testing.md`, `risk/procedure-risk-assessment-methodology.md`, `governance/procedure-continuous-improvement-register.md`; the matrix row in `compliance/matrix-grc-compliance-alignment.md`; the DTI-threshold anchor to §2 in `register-digital-trust-and-assurance-metrics.md`; the index/portal/scorecard rows). None cite the `## Scope` line or the domain enumeration, so #685's one-word Scope edit breaks no citation. **0 breaks.**

## Orchestrator triage

- **A-1: OUT-OF-WINDOW** (pre-existing; #685 did not touch `procedure-grc-programme-management-and-annual-review.md`). #685 exposed it via the enumeration-family cross-check that #685's own fix should have run. **Routed**, not fixed in-window: this record batches into the session-closing handoff PR #686, which is bookkeeping-and-version-surface only and takes no trailing `/validate-pr` (the loop-break exception), so a corpus body change added to it would receive no per-PR QA. Routing to a TODO item gives the fix its own `/validate-pr` on the receiving (NUC) session's first corpus PR, which runs after the `/resume` corpus-wide `/validate`.
- **Routing target**: a new Priority 1 (fix errors) TODO item, precisely scoped to `procedure-grc-programme-management-and-annual-review.md:29` (the class is bounded to this one carrier, verified above). Carried in the NUC handoff as the first queued corpus fix.
- **Discipline lesson** (recorded in the `/retro` row): #685 under-scoped the enumeration class it claimed to close, a fresh occurrence of the parallel-case re-verification guard's failure mode. The guard exists; it was not applied to the family grep in #685.

## Resulting hot-fix PR

None in-window. Routed to TODO (P1) + the NUC handoff for the receiving session's first corpus PR.

## Notes

The finding is material because it directly refutes #685's recorded "no parallel carrier survives" termination claim, not because the omission itself is severe (a note-to-warning domain-list completeness gap). The value here is the discipline signal: the same corpus fix should grep the full family before asserting the class closed. The fix, when applied, aligns all three enumerations at 11 domains and bumps the procedure's Version + Date in the same commit (a Procedure body change), regenerating `taxonomy.yml` then the portal/scorecard.
