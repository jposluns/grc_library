# PR-scoped validation, PR #688 (2026-07-07)

## Trigger & state snapshot

- **PR**: #688 (squash sha `6598091`), the reference-library split cutover 3b (the grc_library re-point). Mode: DAYTIME-UNATTENDED (NUC).
- **State at HEAD**: library `2026.07.176`, README `1.9.537`, pack `1.54.7`; gate count 66. Post-merge baseline `tools/run_all_audits.sh` = 66/66 green on main.
- A pre-push skeptical verifier already reviewed #688's diff and caught one defect (a false "neither repo carries CI" claim), fixed before push. This post-merge `/validate-pr` is the independent confirmation.

## Subagent A return (recent-PR deep review, scoped to the #688 diff)

Refute-briefed Subagent A confirmed the re-point is a clean location-only change on every surface the PR touched (the two governance corpus docs with correct Version+Date co-bumps and preserved meaning, CLAUDE.md SOP, resume.md, runbook §6 prose, matrix-fit/claim-fit skills + commands, the `audit-claim-precision.py` tool with its `--scratch`->`--ref-base` rename, TODO SR block); generated artefacts in sync (`build-taxonomy.py --check` and `build-portal.py --check` exit 0); the claim-precision tool works (self-test passes, default resolves to `grc_library_ref`). It found **2 warnings + 1 note (0 error)**, all one root miss:

- **W1 `verify-reference-modules.py-not-repointed` (out-of-window, K)** `tools/verify-reference-modules.py`: a live in-repo maintainer dev-aid names the reference base at the OLD scratch location 5x and assumes the `ref/`-prefix bucket layout the split eliminated. NOT touched by #688. Bounded impact (dev-aid, not a gate; skips cleanly, exit 0, so 66/66 unaffected), but the runbook §6 parity discipline points the maintainer at a tool validating against a base that moved.
- **W2 `runbook-s6-obligation1-scratch-residual` (in-window, K)** `.working/multi-session-orchestration.md` obligation-1 (3 phrases: "scratch base"/"scratch source") left describing the reference-validation tool's source as scratch, contradicting the re-pointed §6.
- **N1 `overstated-completion-claim` (in-window, I)** the #688 CHANGELOG "every LIVE grc_library surface" claim is falsified by the missed tool (the corpus-wide-completion-grep failure mode: the surface-discovery grep was `.md`-only, so it excluded the `.py` tool).

## Cross-reference check

No live doc cites the two governance docs' §6.6 by its old title; no live version pin broken (paths unchanged). `audit-brief-freshness.py` also references `grc_library_scratch` but that is the WORKER-EXCHANGE (staged briefs / `research/COVERAGE.md`), correctly left as scratch (confirmed: not the reference base).

## Orchestrator triage

All three findings are one root miss (a `.py` tool + its runbook description missed because the re-point surface-discovery grep was `.md`-only). Orchestrator apply-time-verified at source (read `verify-reference-modules.py`: 5 scratch refs + the `ref/`-prefix layout assumption confirmed; runbook obligation-1 3 phrases confirmed). Then ran the **corpus-wide completion grep at full file-type width** (not `.md`-only) over `tools/`, `governance/`, `docs/`, all domain dirs, `.claude/`, and the runbook: the ONLY remaining live reference-base residuals are `verify-reference-modules.py` + the runbook 3 phrases (`audit-brief-freshness.py` is worker-exchange, correctly scratch; governance/docs/.claude clean). **FIXED in PR #689** (this record's closing PR): re-pointed `verify-reference-modules.py` to `grc_library_ref` (root layout, `GRC_SCRATCH_REF`->`GRC_REF_ROOT`, default `../grc_library_ref`; a live run confirms it resolves and all modules match the grc_library_ref extracts), fixed the runbook 3 phrases, and corrected the CHANGELOG overclaim honestly. This completes the "every live surface" re-point.

## Resulting hot-fix PR

PR #689 (bundled per recursion-avoidance; not a dedicated hot-fix PR).

## Notes

The pre-push verifier and the post-merge `/validate-pr` are complementary: the verifier deep-reviewed the diff (catching the CI-claim defect within the touched files) but, like the diff itself, did not surface a file the diff never touched; the `/validate-pr`'s independent scope caught the untouched-but-should-have-been-touched tool. The lesson (surface-discovery grep must span all file types, not `.md` only) is recorded in the #688 `/retro` row.
