# PR-Scoped Validation, PR #706

## Trigger and state snapshot

- **PR**: #706 (merged `3bfc3b6`), reference-audit PR A: the reference-breadth advisory tool.
- **Diff**: `tools/audit-reference-breadth.py` (new), `tools/reference-breadth-aliases.json` (new), `.working/reference-audit/{README,history,doc-state}.md` (new stubs), `reference-audit` added to `RECORD_SUBDIRS` in `tools/sweep-working-records-to-scratch.py`, CHANGELOG both surfaces + version bumps (library `2026.07.193` to `2026.07.194`, README `1.9.554` to `1.9.555`), batched #705 `/validate-pr` + `/retro` rows.
- **Substantive element**: the new advisory tool (an orchestrator dev-aid, not a gate). Re-run at apply time and its figures recounted (the manifest flag).
- **Baseline**: `tools/run_all_audits.sh` at the clean merged `3bfc3b6` = 66/66. (A standalone run on the PR-B working tree reports 1/66 failing on gate 60, but that is the reference-audit-skill branch's uncommitted skill+command tipping the guardrail-cadence drift to 4, not a #706 defect; the committed #706 state is 19 skills / 12 commands, drift 2, below the fail bar.)

## Subagent A return

Subagent A (scoped to the #706 diff) ran the eight failure-mode classes and re-executed the tool independently. Result: **1 low/note-level finding**, all other classes clean.

- The tool re-run at apply time reproduces every figure in the CHANGELOG EXACTLY: 343 in-scope reference items (books 22, frameworks 70, legislation 40, programs 15, standards 180, templates 16; sums to 343), 382 corpus documents, 128 well-cited / 62 thin / 131 uncited / 22 no-key (sums to 343), exit 0, zero writes (pre/post working tree identical).
- The SIGPIPE apply-time correction landed correctly: the `signal.signal(signal.SIGPIPE, signal.SIG_DFL)` guard is present at `main()`, the docstring no longer claims "always exits 0", and a truncated-pipe consumer terminates cleanly at exit 141 (no traceback).
- The tool is correctly NOT a gate (absent from the runner, the workflow, the pre-commit config, and spec §6; no regression fixture); `reference-audit` is in `RECORD_SUBDIRS` and the non-dated stubs are excluded from the sweep by the `DATED_FILENAME` filter.
- Gate-59 mirror parity holds; library/README tokens consistent across README and both CHANGELOG headers; the `**Worker provenance:**` line is present; the batched #705 rows consistent with `1ed5524`; no em/en dashes in added lines; all in-repo links resolve.

### The one finding

```
tool:      subagent-A-pr706
ruleId:    changelog-meta-prose-inaccurate-exit-claim
level:     note (low)
location:  CHANGELOG.md:9  and  .working/changelog-details/CHANGELOG-detailed.md:11
rubric:    Meta-prose state-claim measurement / Summary-description-lag completeness.
           Audit-trail accuracy only; no functional impact.
evidence:  Both #706 CHANGELOG leads characterized the tool as "always exit 0", the
           exact phrasing the SAME entry's Verification section documents as corrected
           (the tool exits 141 on a broken pipe and 2 on error, so "always exit 0" is
           literally false). Intra-entry inconsistency with the entry's own Verification
           text (a paired-surface lag: the lead was not harmonized when the docstring
           was softened).
```

## Cross-reference check

Citers of the touched files: the reference-audit stubs forward-reference PR B's skill and command (intentional, gate-exempt, self-documented). No stale cross-reference. The tool is new (no prior citers). `RECORD_SUBDIRS` consumers (the sweep tool) unaffected.

## Orchestrator triage

- The one finding is **in-window** (introduced by #706's CHANGELOG prose) and **fixed in PR B** (the next PR): both leads changed from "always exit 0" to "exit 0 on a clean run and 2 on error". Verified applied at PR-B assembly (`grep -n "always exit 0"` returns zero in both surfaces post-fix).

## Resulting hot-fix PR

None dedicated; the fix bundles into PR B per recursion-avoidance.

## Notes

The figure-recount and read-only-behaviour re-verification both reproduced the brief exactly, confirming the apply-time re-run discipline held. The finding is a phrasing lag, the mildest class, and its fix is a two-clause CHANGELOG edit carried in PR B's diff.
