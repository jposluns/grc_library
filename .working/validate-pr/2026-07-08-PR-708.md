# PR-Scoped Validation, PR #708

## Trigger and state snapshot

- **PR**: #708 (merged `9c17f62`), CHANGELOG restructure PR 2: the initial current-week sweep (TODO 3.19, Option A).
- **Diff**: 214 files (204 dated-record deletions + the detailed-mirror rewrite + root CHANGELOG #708 + version bumps + TODO/pending/handoff/state/next-prs + the batched #707 QA rows). 43 insertions / 27232 deletions.
- **Baseline**: `tools/run_all_audits.sh` at `9c17f62` = 66/66.

## Subagent A return

Subagent A (scoped to the diff) verified the 8 classes + the data-move high-value checks. **1 finding (Medium), the rest clean.**

### The finding

```
tool:      subagent-A-pr708
ruleId:    cross-reference/dangling-index-links-after-sweep
level:     warning (Medium)
location:  .working/validate-pr/history.md (137 links), validate-sweeps/history.md (52),
           guardrail-reviews/history.md (5), fitness-reviews/history.md (4),
           claim-fit/history.md (2), matrix-fit/history.md (1) [201 total]
evidence:  #708 deleted 204 dated per-run record files; 201 of them were linked from the
           sibling history.md index files, which the sweep tool leaves in-repo by design
           (tools/sweep-working-records-to-scratch.py:5-11,64). Those 201 markdown links now
           point at removed files. Gate 3 (broken-link audit) is BLIND (`.working/` is in
           DEFAULT_EXEMPT_DIRS), so the 66/66 pass does not clear it. Data integrity intact
           (losslessness proven; git history + scratch archive hold every swept file); this is
           an audit-trail / in-repo-navigability regression, not data loss. The design record
           states nothing about the dangling-index-link consequence.
```

## Cross-reference check

Losslessness re-proven on merged main: pre-prune mirror 688 headers; live in-repo 42; scratch archive 647 (2+25+145+261+214). 42 + 647 = 689 = 688 + 1 (the +1 is #708, present live, absent from the frozen archive); the only (live ∪ archive) header not in pre-prune is #708; pre-prune-minus-union is empty (no data loss); no duplicate headers. No wrongful deletion (all 204 are dated records in the nine RECORD_SUBDIRS; no history/README/operational file). Versions consistent (library 2026.07.196, README 1.9.557, pack 1.57.0); gate-59 parity holds (#708 in both surfaces, root retains full 692-entry history). The off-by-one count fixed pre-push confirmed resolved (zero surviving "holds 41"; mirror is 42, #667 through #708). Bookkeeping reconciled (pending-decisions 0-pending, TODO 3.19 PR-2 DONE, #707 QA rows accurate, handoff D7 clean).

## Orchestrator triage

- The finding is **in-window** (introduced by #708's prune) and **gate-blind** (the 201 links live in the gate-3-exempt `.working/` tree). Data integrity is intact; it is a navigability regression.
- Remediation is a **design choice**, surfaced to the maintainer with named options (delink / repoint-to-archive / accept-and-note) plus the durable fix (enhance the sweep tool to delink-or-repoint the history.md detail-column links when it prunes, so future weekly sweeps do not recreate the dangling). Not blocking; batches into the next PR once the option is chosen, and the sweep-tool enhancement is a TODO 3.19 residual.

## Resulting hot-fix PR

#709 (bundled): the maintainer chose delink + tool fix. #709 delinks the 217 now-absent history-file links (206 same-dir Detail-column + 11 cross-subdir references) to plain text and enhances `sweep-working-records-to-scratch.py` (a `delink_absent_history_links` step run at the end of `--prune` and exposed as a `--delink-history` mode) so future weekly sweeps self-heal.

## Notes

The mandatory post-merge `/validate-pr` caught what the pre-push data-safety verifier's scope missed: the pre-push verifier proved losslessness and no-wrongful-deletion but did not check whether the RETAINED history.md files link the DELETED dated files. This is the layered-QA value the per-PR cadence exists for.
