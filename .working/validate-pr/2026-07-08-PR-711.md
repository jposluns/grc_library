# PR-Scoped Validation, PR #711

## Trigger and state snapshot

- **PR**: #711 (merged `350958f`), AIQT PR 2: the corpus Integrity and Trustworthiness Principle document + the new "Principle" Document Type registration.
- **Baseline**: `tools/run_all_audits.sh` at `350958f` = 66/66.
- **Diff**: 13 files (the new doc, `lint-metadata.py` type registration, the two index surfaces, the regenerated artefacts, TODO 3.21, CHANGELOG both, versions, batched #710 QA rows + link fix + r6-G1 deferral).

## Subagent A return

Subagent A verified the 8 classes + the new-doc integration. **1 material finding (Medium, in-window, gate-blind), 2 Low notes.** The new-doc index/generated integration (5 surfaces) and the pre-push claim-fit CLEAN verdict both hold.

### Finding 1 (Medium, in-window, gate-blind): doc-type enumeration not propagated

Registering the new "Principle" Document Type in `tools/lint-metadata.py` (ALLOWED_TYPES + TYPE_TO_PREFIX + docstring 17 to 18) was NOT propagated to the other surfaces that DEFINE/ENUMERATE the allowed-type set:

- `specification-master-project.md:151` (§4.4 "Allowed types and canonical filename prefixes" table, 17 rows, the type-selection authority) omits Principle / `principle-`.
- `specification-ingestion.md:172` (bulleted allowed-types list) omits Principle.
- `instruction-ai-document-ingestion.md:5-6` (allowed types + prefix list) omits Principle / `principle-`.
- `governance/charter-governance-library.md:61-77` ("## Document hierarchy" table, levels 1-17) omits Principle.
- `governance/framework-document-architecture-and-interrelationship.md:47-63` ("## Document hierarchy" table) omits Principle.
- `tools/lint-filename-title-alignment.py:82-86` keeps its OWN `DOCTYPES` set (17, "principle" absent), so `principle-integrity-and-trustworthiness.md` is SILENTLY SKIPPED by the filename-title gate, and the docstring invariant (":32-33", "covers all 17 doctypes accepted by ALLOWED_TYPES") is now violated (ALLOWED_TYPES is 18).

No ALLOWED_TYPES-parity gate exists, so 66/66 did not catch it. No substantive defect ships (filename and title do align; the doc is well-formed), but the type registration is incomplete across surfaces and a second linter silently skips the new doc.

### Finding 2 (Low): `tools/lint-required-sections.py:23-24` docstring lists the not-enforced types and should include Principle (it is correctly not in REQUIRED_SECTIONS; docstring enumeration incomplete).

### Finding 3 (Low, appears deliberate): the new doc carries both `## 4. Framework alignment` (analytical) and a trailing `## Framework alignment` (conventional end-table), identical heading text, a minor navigability note.

## Cross-reference check

The new-doc 5-surface index integration all present on merged main (README:30, index register:58, taxonomy:2178, portal:250, scorecard:360; both generator `--check`s exit 0). The apex-rule cross-link resolves. All batched #710 rows + the link fix + the r6-G1 deferral + TODO 3.21 present and accurate.

## Orchestrator triage

- **Finding 1 (Medium)**: in-window, material. Requires a hot-fix propagating "Principle"/`principle-` to the six enumeration surfaces + the second linter, and (Low) the required-sections docstring, plus the duplicate-heading rename. Mostly mechanical; the two "Document hierarchy" tables carry a judgment (level placement + renumber vs append). SURFACED to the maintainer with the placement sub-question and the fix-now-vs-defer choice.
- A proposed improvement surfaced: an ALLOWED_TYPES-to-prose parity gate (the gate-blind class this finding exposes; sibling to the gate-name-parity gate). Routed to the retrospective.

## Resulting hot-fix PR

Pending the maintainer's fix-now-vs-defer + hierarchy-placement decision.

## Notes

The pre-push claim-fit + refute verifier's scope was the new document + the lint-metadata registration; it was not briefed to check the OTHER type-enumeration surfaces, so it (correctly, within its scope) missed this. The mandatory post-merge `/validate-pr` caught it. This is the multi-surface-incompleteness class (register a value in one surface, miss the parallel enumerations) that the close-out checklist's "grep the OLD phrasing across all sibling surfaces before commit" guard targets, here applied to a doc-TYPE registration whose enumeration surfaces I did not fully grep.
