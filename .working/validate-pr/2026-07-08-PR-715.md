# PR-scoped validation, PR #715, 2026-07-08

## Trigger and state snapshot

Post-merge `/validate-pr` for PR #715 (merge commit `0058000` on `main`), the application
of the Fable `resume-pointer` worker delivery: a new repo-root `RESUME.md` manual resume
entry point, plus the README/library version bumps and the batched #714 QA rows. Run on
branch `claude/worker-collision-start-check` (this session's PR 3), attended-autonomous
daytime, NUC. Post-merge mechanical baseline: 66/66 standalone. Library `2026.07.203`,
README `1.9.564` at the #715 head.

## Subagent A return

**2 findings** (1 warning, 1 note). Verify items 1-6 and the pointer-not-copy check passed;
the substantive finding is a recurring dangling-link class in the gate-exempt detailed
CHANGELOG.

- **F1, `detailed-changelog-dangling-relative-link`, warning, in-window.**
  `.working/changelog-details/CHANGELOG-detailed.md` (the #715 entry): four `.working/`
  files were linked with BARE relative targets that do not resolve from the file's own
  directory (`.working/changelog-details/`): `[`.working/session-handoff.md`](session-handoff.md)`,
  `[`.working/session-state.md`](session-state.md)`, `[`.working/validate-pr/history.md`](validate-pr/history.md)`,
  `[`.working/improvement-log.md`](improvement-log.md)`. The correct form is `../<name>`
  (the majority convention in the same file, and the form the immediately-preceding #714
  entry used). Gate-blind: `.working/` is in `DEFAULT_EXEMPT_DIRS`, so the broken-link
  gate never scans this file, which is why the 66/66 run does not catch it. This is the
  same class the #710 retro logged.
- **F2, `worker-provenance-crossrepo-link-unresolved`, note, out-of-window.** The
  `**Worker provenance:**` line's cross-repo link
  `[`inbox/...MANIFEST.md`](../../../grc_library_scratch/inbox/worker-20260708-fable/resume-pointer/MANIFEST.md)`
  does not resolve from `.working/changelog-details/` against a stale local scratch
  checkout. Cross-repo links are inherently un-gate-checkable; gate 50 validates the
  provenance marker SHAPE, not link resolution. Lower confidence (the target is another
  repo). Same form as the #701/#702/#706/#707 provenance lines.

Verify-item coverage (all clean): item 1 (RESUME.md's two links resolve), item 2 (pointer
carries no copied protocol or queue), item 3 (style: no em/en dashes, `ensures that`),
item 4 (versions `2026.07.203` / `1.9.564`, zero residual), item 5 (CHANGELOG accuracy),
item 6 (batched #714 rows well-formed, both Versions bumped).

## Cross-reference check

0 breaks. `RESUME.md` is a new file, cited only by the `.working/` queue files
(`session-handoff.md`, `session-state.md`); no corpus document cites it. Its two link
targets (`.claude/commands/resume.md`, `.working/session-handoff.md`) exist. README carries
no residual `2026.07.202` / `1.9.563` tokens.

## Orchestrator triage

- **F1 (in-window): FIXED in #716.** On validating F1 the orchestrator ran a file-wide
  resolution-checked re-scan of the whole detailed CHANGELOG (not just the #715 entry):
  the same dangling-link class was present in three OTHER entries (#713, #696, #685),
  **16 occurrences across 9 distinct bare targets** total, distributed #715 (4), #713 (3),
  #696 (5), #685 (4) (`improvement-log.md`,
  `overnight-pr.md`, `pending-decisions.md`, `session-handoff.md`, `session-metrics.md`,
  `session-state.md`, `validate-pr/history.md`, `validate-sweeps/2026-07-06-sweep87-iter1.md`,
  `validate-sweeps/history.md`). All 16 were fixed (bare -> `../`), each replacement
  count-asserted, and a re-scan confirmed 0 dangling bare-relative links remain. The
  out-of-window siblings were fixed alongside the in-window instance because every one is
  the same unambiguous, zero-risk, pure link-resolution correction (no prose change) and
  leaving known-dangling links violates the Quality tier; the full scope was reported to
  the maintainer.
- **F2 (out-of-window): TRACKED, not fixed.** The maintainer chose (2026-07-08) to track
  the cross-repo-provenance-link convention as a deliberate review item (TODO 3.22) rather
  than change it now, since it is the established convention across five prior entries and
  the change would affect all of them and the gate-50 interaction.

## Resulting hot-fix PR

Not a dedicated hot-fix PR: per recursion-avoidance, the F1 fix (all 16 links) and these
records bundle into #716 (the worker-collision start-side-check PR).

## Notes

The F1 recurrence (the #710 class) motivates a durable backstop, proposed in the #715
`/retro`: extend `tools/preflight-changelog.py` to check that the detailed mirror's own
bare-relative link targets resolve (the mechanical layer that `.working/`'s broken-link
exemption otherwise leaves open). Routed as a candidate.
