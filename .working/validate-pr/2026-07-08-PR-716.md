# PR-scoped validation, PR #716, 2026-07-08

## Trigger and state snapshot

Post-merge `/validate-pr` for PR #716 (merge commit `8d0e8b1` on `main`), the
maintainer-directed worker-collision START-side-check convention addition (three carriers)
plus the bundled #715 `/validate-pr` F1 fix (dangling links) and F2 tracking (TODO 3.22).
Run on branch `claude/worker-collision-start-check` (this session's PR 3),
attended-autonomous daytime, NUC. Post-merge mechanical baseline: 66/66 standalone. Library
`2026.07.204`, README `1.9.565`, runbook `1.1.8` at the #716 head.

## Subagent A return

**1 finding** (1 warning, in-window). Verify items 1, 2, 3, 4, 6, 7 clean; item 5 (F1
completeness) clean on link/target counts but carried the entry-count defect below.

- **f1-entry-count-off-by-one, warning, in-window.** `CHANGELOG.md:9`,
  `.working/changelog-details/CHANGELOG-detailed.md:17`, `.working/validate-pr/history.md:15`,
  `.working/validate-pr/2026-07-08-PR-715.md:52`. The F1 fix was described as spanning
  "five entries (#715, #713, #696, and two more)" / "four OTHER entries", but the 16 fixes
  fall in exactly FOUR entries: #715 (4), #713 (3), #696 (5), #685 (4). The 16-link and
  9-distinct-target counts are correct and corroborated; only the entry count is off by
  one ("five" -> "four"; "four OTHER" -> "three OTHER"; "two more" -> "one more, #685").
  The CLAUDE.md count-vs-enumeration-granularity class (#630/#631/#633): a count stated
  next to an enumeration at a different granularity, without measuring the enumeration.

Clean items: (1) cross-carrier coherence, the TODO bullet, the CLAUDE.md paragraph, and
the runbook §5 bullet state the same rule with no contradiction; (2) accuracy vs runbook
§5.1's artefact-derived-state model; (3) gate-39-safe TODO phrasing (only "gate 50" /
"gate-50", no P7 adjacency); (4) TODO 3.22 well-formed and correctly numbered; (6) version
consistency (`2026.07.204` / `1.9.565`, runbook `1.1.8`, no stale token); (7) the #716
detailed entry's own `.working/` links all resolve (the very class F1 addressed, correctly
authored this time).

## Cross-reference check

0 breaks / 0 misses. The change is additive (new bullets, paragraph, TODO item, and link
corrections; no renumber), so citers are unaffected. An orchestrator completeness re-scan
independently confirmed the 16-fix / 4-entry distribution (#715 4, #713 3, #696 5, #685 4)
and that the four bare `.md` links NOT fixed all legitimately resolve (three
`CHANGELOG-detailed.md` self-references, which are correctly bare, and one `README.md`
whose bare form resolves), so no genuinely-dangling link was missed. Independent re-scan:
0 dangling bare-relative links remain in the detailed CHANGELOG.

## Orchestrator triage

- **The entry-count finding (in-window): FIXED, bundled into the next PR.** Validated by an
  independent measurement mapping the 16 fixed bare targets to their owning entry headers
  in the pre-#716 image: exactly four entries (#715 4, #713 3, #696 5, #685 4). Corrected
  in all four surfaces ("four entries", "three OTHER entries (#713, #696, #685)", the
  per-entry distribution added). The 16 / 9 counts left unchanged (verified correct).

## Resulting hot-fix PR

Not a dedicated hot-fix PR: per recursion-avoidance, the entry-count fix and these records
bundle into the next grc_library PR.

## Notes

Third caught authoring slip of the completeness/precision family this session (after the
two em-dash-in-detail-file slips), all caught in-window with 0 escapes. Surfaced to the
maintainer as a named caught-slip-density signal for the continue-vs-handoff decision (per
the #712 retro's convention), not an un-instrumented feeling.
