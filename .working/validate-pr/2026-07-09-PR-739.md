# /validate-pr record: PR #739 (eIDAS2 public-sector annex)

## Trigger and state snapshot

- **Trigger:** post-merge `/validate-pr` for PR #739, merge commit `5ea08eb`, diff base `9e89c8a` (#738).
- **State at merge:** library `2026.07.227`, README `1.9.588`, pack `1.59.4`, spec `1.16.62`; gate 67, suite 354, rules 13, skills 21, commands 14, Doc-types 18.
- **PR substance:** new `compliance/public-sector/annex-eidas-requirements.md` (eIDAS Sector Requirements Annex, role-organized eIDAS2 obligations, TODO §5.7 EU eIDAS bullet closed); wired into `compliance/README.md`, `compliance/public-sector/README.md`, `governance/register-document-index-and-classification.md`; fixed the #738 TODO:60 dangling relative link; batched #738's `/validate-pr` + `/retro` rows.
- **Mechanical baseline:** `tools/run_all_audits.sh` on post-merge `main` = 67/67 PASS (unpiped via tail-safe).
- **Method:** Subagent A refute-briefed on the diff + touched-file states + touched-file-filtered pre-flight scanner (no candidates); targeted cross-reference check on citers of the touched files. No gh-GraphQL (exhausted); REST used for PR ops.

## Subagent A return

Two gate-blind multi-surface-incompleteness findings, both in-window (the new annex was added to the gated listing surfaces but not to two hand-maintained sibling-enumeration surfaces the mechanical gates do not scan). No error-level regressions.

**F1 (WARNING, class c multi-surface-incompleteness):** `docs/decision-tree.md:198`
- Evidence: `3. If providing cloud services to US federal agencies: [\`compliance/public-sector/annex-fedramp-requirements.md\`](../compliance/public-sector/annex-fedramp-requirements.md): FedRAMP.`
- The decision tree routes every sector-conditional annex (FedRAMP under §3.6 public sector at `:198`, DORA under §3.2 financial services at `:176`, NIS 2 at `:227`) plus scope-trigger bullets (`:85` DORA, `:87` FedRAMP, `:89` NIS 2). The new eIDAS annex is the direct public-sector peer of FedRAMP but has no routing line and no scope-trigger bullet, so an adopter routing an EU digital-identity / trust-services scenario is not reached to the annex. Gate-blind (gates 3/4/47 cover the doc-index register and READMEs, not the decision tree).

**F2 (NOTE, class c multi-surface-incompleteness):** `governance/register-glossary.md:143`
- Evidence: `| **FedRAMP** | Federal Risk and Authorization Management Program. U.S. federal cloud-service authorization. See [\`compliance/public-sector/annex-fedramp-requirements.md\`](../compliance/public-sector/annex-fedramp-requirements.md). |`
- DORA (`:115`), NIS 2 (`:230`), and FedRAMP (`:143`) each have a glossary entry linking their dedicated annex. eIDAS now has a dedicated annex but no glossary entry, breaking the established parallel pattern.

**Classes checked CLEAN (negative):** (a) stale prose refs (versions/PR/dates internally consistent; no numeric doc/annex counts in the touched surfaces to go stale); (b) mis-attributed citations (internal cross-refs consistent annex/register/CHANGELOG; article citations pre-verified against held text); (d) inferred-as-verified (pending-decisions naming entry real at `:37-39`; DONE #739 present; #738 validate-pr row batched); (e) version-bump (annex 0.0.1/2026-07-09; compliance/README 1.4.8->1.4.9; public-sector README 1.0.1->1.0.2 Date 2026-07-02->2026-07-09; doc-index 1.27.65->1.27.66; README library+per-doc bumped); (f) generated-artefact lag (annex in taxonomy `:1491`, portal `:235`/`:527`, scorecard `:355`; `--check` clean); (g) no Python touched; (h) title byte-identical across all surfaces. TODO:60 dangling-link fix confirmed root-relative and resolving; no new dangling link. §5.7 closure integrity confirmed (EU eIDAS bullet removed; UK/Australia/Canada overlays retained open; `5.7 eIDAS2` off the egress-deferred list).

## Cross-reference check

Citers of the touched files re-checked: the eIDAS annex's own 11 relative link targets resolve on disk; the annex Framework-alignment table (`:284-287`) and the doc-index row (`:315`) are consistent; NIS2-annex / IAM / auth / identity cross-references resolve. No citer's quoted text broken by the new annex. The two findings above are the only sibling-enumeration surfaces the annex did not reach.

## Orchestrator triage

- **F1 (WARNING) and F2 (NOTE): IN-WINDOW.** Both are apply-time verified against the live sources (decision-tree has FedRAMP/DORA/NIS2 routing + scope-trigger lines and lacks eIDAS; glossary has DORA/FedRAMP/NIS2 annex-linked entries and lacks eIDAS). Both are genuine parallel-surface omissions introduced by #739 (the annex is new; these are its sibling-enumeration surfaces).
- **Disposition:** both fixes applied in the immediately-following eIDAS discoverability-wiring PR per recursion-avoidance (no dedicated hot-fix PR): the eIDAS decision-tree §3.6 routing line + a scope-trigger bullet, and the eIDAS glossary entry. NOTE: the sibling FR-62 EU AI Act annex was built this session but HELD from merge pending maintainer (TODO line-48 egress-deferral for currency; a pending "Digital Omnibus on AI" amendment was found upstream this turn); its own decision-tree/glossary wiring lands when authorized, not in this PR.
- **Pattern observation (routed to /retro):** "new sector/regime annex" has MORE parallel surfaces than the gated ones (README + doc-index): the decision-tree routing (line + scope-trigger) and the glossary entry are gate-blind sibling-enumeration surfaces. The new-annex close-out procedure should enumerate them.

## Resulting hot-fix PR

None (fixes applied in the immediately-following eIDAS discoverability-wiring PR per recursion-avoidance; no dedicated hot-fix PR).

## Notes

Rows (this record + the history row) and the F1/F2 fixes are carried by the eIDAS discoverability-wiring PR (the immediate next PR). That PR was originally scoped as the FR-62 EU AI Act annex, but the EU AI Act annex merge is HELD pending maintainer (TODO line-48 egress-deferral; a pending "Digital Omnibus on AI" amendment surfaced upstream this turn), so the next PR carries only the non-blocked eIDAS discoverability fixes + this QA batch. History row records originating PR #739 and the closing PR so the audit trail traces source-to-fix in one hop.
