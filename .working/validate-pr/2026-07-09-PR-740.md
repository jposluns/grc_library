# /validate-pr record: PR #740 (eIDAS discoverability wiring)

## Trigger and state snapshot

- **Trigger:** post-merge `/validate-pr` for PR #740, merge commit `73cc712`, diff base `5ea08eb` (#739).
- **State at merge:** library `2026.07.228`, README `1.9.589`, pack `1.59.4`, spec `1.16.62`; gate 67, suite 354, rules 13, skills 21, commands 14, Doc-types 18. Registers at #740 merge: validate-pr history `1.2.513`, improvement-log `1.0.456`.
- **PR substance:** wired the eIDAS annex into `docs/decision-tree.md` (§3.6 routing + §1.4 scope-trigger) and `governance/register-glossary.md` (eIDAS entry), closing #739's `/validate-pr` F1/F2; batched the #739 QA rows.
- **Mechanical baseline:** `tools/run_all_audits.sh` on post-merge `main` = 67/67 PASS.
- **Method:** Subagent A refute-briefed on the diff, eight classes (a-h) + pending-decisions/TODO coherence. No gh-GraphQL; REST for PR ops; RM-10 pipe-guard fired correctly on an accidental `--check | tail` (re-run unpiped).

## Subagent A return

Two gate-blind QA-provenance inaccuracies in the #739 QA rows this PR authored. No corpus/adopter-facing regression.

**F1 (WARNING, class a meta-prose state-claim measurement):** `.working/validate-pr/history.md:14`
- Evidence: `README 2026.07.227/1.9.588; registers self-bumped 1.2.513/1.0.456`
- The #739 history row states #739's registers self-bumped `1.2.513/1.0.456`, but #739's actual merge-state (`git show 5ea08eb:`) is validate-pr history `1.2.512` / improvement-log `1.0.455`. The cited `513/456` are #740's OWN register versions (the writing PR's). The established per-row convention (six prior rows, unbroken +1, each equal to that PR's own merge-state; #738's row = 511/454 = its merge commit) is that the row records the PR's own merge-state, not the writing PR's. Transcription of the writing-PR figures, the same measured-not-inferred class as #630/#631/#663.

**F2 (NOTE, class a/h CHANGELOG scope-overclaim):** `CHANGELOG.md:9` (mirrored `.working/changelog-details/CHANGELOG-detailed.md:31`)
- Evidence: `Batches PR #738's and PR #739's /validate-pr and /retro rows.`
- Both CHANGELOG halves claim #740 batches #738's rows too, but #738's rows were already batched by #739 (present at `5ea08eb`); #740's diff adds only the #739 rows (`git show 73cc712 -- history.md` = sole added row `| 739 |`). The detailed line's `self-Version 1.2.513/1.0.456` are #740's register-file versions, not #738's-row versions.

**Classes CLEAN (negative):** (a) version tokens consistent (README 2026.07.228/1.9.589, root+detailed+handoff agree; no stale residue; handoff reconciled 2->5 pending, heartbeat re-stamped); (b) glossary alphabetical order correct (eIDAS between EECC/EIOPA); (c) sibling-pattern fidelity (decision-tree §3.6 line + §1.4 bullet + glossary entry all match FedRAMP/DORA/NIS2 form); (d) links resolve (`../compliance/...` from docs/ and governance/); (e) version-bumps (decision-tree 1.0.14, glossary 1.4.10, README both); (f) generated-artefact (taxonomy + scorecard carry glossary 1.4.10; portal correctly unchanged; `--check` clean); (g) acronym consistency (eIDAS expansion standard, no false inline expansion); (h) QA-batch integrity (#738/#739 rows present; the #739 record file present, fold-target correctly = #740). pending-decisions item 1 + TODO §5.1 coherent (5 pending headers = count).

## Cross-reference check

Citers of the touched files: the two new eIDAS links resolve; the decision-tree/glossary sibling patterns intact; no citer's quoted text broken. No further findings.

## Orchestrator triage

- **F1 (WARNING) and F2 (NOTE): IN-WINDOW.** Apply-time verified against `git show` of the #739/#738 merge states (validate-pr history 1.2.512/1.0.455 at #739; the sole #740-added history row is `| 739 |`). Both are QA-provenance prose inaccuracies this PR introduced while authoring the #739 rows.
- **Disposition:** both fixed in the immediately-following post-#740 PR (this record's carrier), per recursion-avoidance: F1 the #739 row register figures corrected to `1.2.512/1.0.455`; F2 the #740 CHANGELOG root + detailed reworded to "Batches PR #739's rows (PR #738's were already batched by #739)".
- **Pattern (routed to /retro):** the meta-prose state-claim measurement class (#630/#631/#663) recurred: a per-PR QA row's "registers self-bumped X/Y" must be MEASURED from the PR's own merge-state (`git show <mergesha>:`), not transcribed from the writing PR's live register state.

## Resulting hot-fix PR

None (fixes applied in the post-#740 PR per recursion-avoidance).

## Notes

Both findings are gate-blind QA-provenance prose (no corpus/adopter impact). The correction to #740's already-merged CHANGELOG entry is a meta-prose accuracy fix, noted in the carrying PR's CHANGELOG.
