# /validate-pr, PR #751 (2026-07-10 UTC)

## Trigger and state snapshot

- **PR**: #751, "TODO 1.5 reference version-currency sweep + corpus-wide cascade".
- **Merge**: squash-merge, commit `4c3acde` on `main` (no merge commit; `git log --merges` correctly found none for this PR, an earlier draft of this sweep mis-read an unrelated old merge commit and was corrected before dispatch).
- **Diff**: the register-currency stamp/correct (41 confirmed + 5 stale + 1 data-defect), the corpus-wide WCO SAFE 2021→2025-edition cascade across 10 docs + NERC/TSA annex citations, `tools/lint-external-link-domains.py` allow-list (+ecfr.gov +pib.gov.in), `governance/specification-citation-verification.md` §7 (US Federal + new India row), regenerated taxonomy/portal/scorecard, version+date bumps, `.working` QA rows for #750.
- **Mechanical baseline (post-merge, live `main`)**: `tools/run_all_audits.sh` = **67/67 all gates passed** (confirmed standalone before this sweep).
- **State**: library `2026.07.239`, README `1.9.600`, pack `1.59.4`, spec-citation `1.2.12`.

## Subagent A return

One warning (in-window, adjudicated to non-defect), two notes (out-of-window). Grep/gate counts grounding the clean bill on the eight classes:

- WCO SAFE corpus grep (`WCO SAFE|SAFE Framework|Framework of Standards`, `--include=*.md`) ≈ 80 hits; every version-bearing hit is on "2025 edition" (one frozen worklist residual excepted, NOTE-1); the rest are generic no-year programme references (correct).
- `lint-external-link-domains.py` → `OK ... scanned 518 files`, exit 0.
- `lint-standards-currency.py` (gate 6) → `OK ... 126 standards across 393 files`, exit 0.
- `build-taxonomy.py --check` / `build-portal.py --check` → in sync (taxonomy, portal, scorecard).
- All 12 cascade corpus docs bumped **both** Version and Date to 2026-07-10 (class e clean).
- `ecfr.gov` + `pib.gov.in` present in BOTH the linter ALLOW_LIST (L131-132) AND the citation-verification §7 (US Federal L229 + new India row L237), class (c) complete.
- CHANGELOG counts match the diff: 49 needs-reconfirm = 41 + 5 + 3; WCO cascade = exactly 10 docs (class d clean).

**Finding 1 (WARNING, in-window), `register-ref-version-discrepancy`**, `governance/register-canonical-citations.md` NERC CIP row cites CIP-015-1 (URL `CIP-015-1.pdf`, FERC-approved 2025-06-26, effective 2025-09-02) while the trusted reference base holds `CIP-015-2` (`grc_library_ref/.../NERC-CIP-015-2--full-text.md`, `authoritative: true`). The existence gates cannot adjudicate a revision-currency question.

**Finding 2 (NOTE, out-of-window), `stale-wco-safe-worklist-residual`**, `.project-governance/worklist-citation-verification-batch-q4-canonical-citations.md:149` records "WCO SAFE Framework of Standards ... 2021" as the verified-against input state. A frozen completed-worklist cell, not a live normative citation; arguably correct as a historical record.

**Finding 3 (NOTE, out-of-window), `wco-safe-naming-variation`**, across the 10 cascade docs the instrument appears in three name forms ("WCO SAFE Framework (2025 edition)", "WCO SAFE Framework 2025 edition", "WCO SAFE Framework of Standards 2025 edition"); the edition TOKEN is consistent everywhere ("2025 edition", never bare "2025"), only the surrounding name form varies. No error; recorded because class (h) was in scope.

## Cross-reference check

The touched files' citers were shallow-checked; no new drift. The register's downstream citers (the 10 cascade docs) all carry the current "2025 edition" token; the two new allow-list domains resolve on both surfaces. No broken citations, no stale cross-references introduced.

## Orchestrator triage

- **Finding 1 (WARNING) → adjudicated NON-DEFECT via upstream verification (this turn).** WebSearch confirmed FERC Order No. 907 (2025-06-26, effective 2025-09-02) approved **CIP-015-1**, the currently-enforceable revision. **CIP-015-2** (Project 2025-02, extending INSM to EACMS/PACS outside the ESP) passed the NERC final ballot 2026-03-05 but is pending NERC Board adoption and FERC filing as of July 2026, so it is not yet FERC-approved. The register correctly cites the FERC-approved revision (CIP-015-1); the reference base holds the newer not-yet-approved CIP-015-2 draft. **No corpus fix.** A ref-side advisory note was added to TODO §1.5 (annotate `grc_library_ref`'s CIP-015-2 as not-yet-approved so the discrepancy is not re-flagged).
- **Finding 2 (NOTE, out-of-window) → no action.** Frozen worklist cell recording the pre-sweep input state; correct as a historical record.
- **Finding 3 (NOTE, out-of-window) → no action.** No error (edition token consistent); left as each doc's pre-existing phrasing.

**Net: 0 in-window corpus defects requiring a fix.** No fix batches into the closing handoff PR.

## Resulting hot-fix PR

None. The single warning adjudicated to a non-defect; the two notes are out-of-window no-action. The ref-side CIP-015 note is tracked in TODO §1.5 (cross-repo, advisory).

## Notes

- Upstream-currency verification (FERC/NERC CIP-015) done this turn per the reference-version-currency SOP (the upstream source, not the register or the ref copy, is the version authority).
- Corroboration limitation recorded honestly: `grc_library_ref` holds only the 2021 WCO SAFE edition, so the register's "2025 edition" rests on WebSearch, not held text, already tracked in TODO §1.5 (ref-side WCO SAFE refresh), so it is a tracked residual, not a defect.
- The git commit-message body for `4c3acde` froze the pre-correction "9 docs" figure; the durable CHANGELOG (root + detailed) correctly says 10. Commit bodies are immutable, ungated, no path:line, informational only, no rewrite.
