# /validate-pr record: PR #793 (2026-07-11)

## Trigger & state snapshot

- **PR**: #793 (squash `c0d5f37`), AI-workstream PR 3 (model-risk cluster): matured 8 AI documents against OSFI E-23, ISO/IEC 23894:2023, ISO/IEC 5338:2023 §6.4.14, and NIST AI 600-1 (framework-ai-model-risk 1.0.2, standard-ai-model-risk 1.1.3, procedure-ai-model-risk-assessment 1.0.4, matrix-ai-model-risk-control-to-lifecycle-mapping 1.0.2, procedure-ai-model-lifecycle-management 1.0.4, register-model-registry 0.0.6, procedure-foundation-model-lifecycle 0.0.5, template-dataset-datasheet 1.0.6). Merged via `gh --admin`.
- **State**: library `2026.07.281`, gates 67. Post-merge `run_all_audits.sh` = **67/67** on `main` at `c0d5f37`.
- **Pre-push coverage**: a refute-briefed skeptical verifier returned FINDINGS (one LOW CHANGELOG-prose over-grouping, fixed pre-push) and refuted every corpus target clean. This post-merge pass is the second independent lens (integration / cross-surface).

## Subagent A return

**2 in-window findings** (1 warning, 1 note); the substantive corpus content verified clean.

```
tool: validate-pr-A
ruleId: multi-surface-incompleteness
level: warning
location: ai/procedure-foundation-model-lifecycle.md:178
rubric: in-window
evidence: The new "Value chain and component integration" row references "the AI System Register dependency fields", but ai/template-ai-system-register.md has NO dependency/constituent/component field (grep clean). This PR added a "Dependencies" field to the *Model Registry* (register-model-registry.md:64), not the AI System Register. So the row points at fields that do not exist. The pre-existing register-model-registry.md:118 ("AI System Register | Each system entry lists the model identifiers it depends on") rests on the same absent-field gap (out-of-window, but the same fix resolves it).
```

```
tool: validate-pr-A
ruleId: stale-prose-reference
level: note
location: ai/procedure-ai-model-risk-assessment.md:35
rubric: in-window
evidence: Step 1 says "Assign a provisional inherent model-risk rating at this step, to be confirmed at Step 10." Step 10's body (line 71) carries no instruction to confirm/finalize the provisional rating, so a reader executing the procedure gets no explicit Step 10 prompt to do so. A one-clause addition to Step 10 closes it.
```

**Verified clean:** all 8 docs bumped Version + Date to 2026-07-11; generated artefacts in sync; ISO/IEC 5338 §6.4.14 cited identically across standard §3.7.4, lifecycle §4.6, foundation framework row; OSFI E-23 (2025, effective 2027) cited consistently and sector-neutrally across framework/standard/risk-assessment/matrix/register/lifecycle; the 12 NIST AI 600-1 classes count to exactly 12; intra-cluster cross-references resolve (datasheet->A2, Related Documents links, Step 10 exists); term coherence held ("risk tier" vs "inherent model-risk rating" deliberately distinct; MRM expanded; independent-review/reviewer/trigger-list consistent across standard §3.1.6 and risk-assessment); new register fields trace to defining requirements (only the value-chain reference target was broken); no inferred-as-verified assertions (all hedged "sector-neutrally"/"illustrative not exhaustive").

## Cross-reference check

Inbound citers of the 8 touched documents resolve (path-only Related-Documents citations, unchanged by the edits). The one broken linkage is intra-cluster (foundation->AI System Register), captured as Finding 1.

## Orchestrator triage

Both findings validated against the live files (apply-time verification): Finding 1 confirmed (AI System Register template has no dependency field; register:118 pre-existing claim rests on the same gap; the foundation:178 reference is in-window from this PR). Finding 2 confirmed (Step 10 body has no confirm-the-rating clause).

- **Finding 1 (`multi-surface-incompleteness`, warning, in-window):** FIX rides PR 4. Add a `Constituent models and dependencies` field to `ai/template-ai-system-register.md` (the system-level inventory genuinely should list the models/components a system depends on; both the in-window foundation:178 reference and the pre-existing register:118 integration-table claim already assert this linkage exists). This one edit makes the in-window reference valid AND closes the latent out-of-window gap. Bumps the AI System Register template Version 1.1.2->1.1.3 + Date.
- **Finding 2 (`stale-prose-reference`, note, in-window):** FIX rides PR 4. Add a clause to Step 10 of `ai/procedure-ai-model-risk-assessment.md` to confirm/finalize the provisional rating at approval. Bumps its Version 1.0.4->1.0.5 + Date.
- **Out-of-window (register-model-registry.md:118):** RESOLVED by the Finding-1 fix without touching the register (the AI System Register field addition validates the claim); no register version bump needed.

## Resulting hot-fix PR

None. Both fixes + this record + the history row + the /retro row batch into **PR 4** (recursion-avoidance).

## Notes

The pre-push verifier (corpus-content lens) and this post-merge Subagent A (integration/cross-surface lens) were genuinely complementary: the verifier refuted every source-attribution target clean, and Subagent A caught a cross-document reference to a non-existent template field that a same-file check would miss. Both findings are the AI-workstream's own new content, caught in-window.
