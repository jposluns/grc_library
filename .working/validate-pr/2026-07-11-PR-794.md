# /validate-pr record: PR #794 (2026-07-11)

## Trigger & state snapshot

- **PR**: #794 (squash `1932eed`), AI-workstream PR 4 (testing, evaluation, transparency, audit and certification): deepened 9 AI documents against ISO/IEC 12792:2025, TS 25058:2024, 25059:2023, NIST IR 8312, TR 24027:2021, and ISO/IEC 42006:2025, and closed the two #793 `/validate-pr` findings (AI System Register dependency field; risk-assessment Step 10 rating confirmation). Merged via `gh --admin`.
- **State**: library `2026.07.282`, gates 67. Post-merge `run_all_audits.sh` = **67/67** on `main` at `1932eed`.
- **Pre-push coverage**: a refute-briefed skeptical verifier checked 34 clause locators against the held texts and returned one warning (an OWASP-naming divergence), fixed pre-push by reverting to the corpus short-form convention. This post-merge pass is the integration/cross-surface lens.

## Subagent A return

**1 in-window warning** (low-severity, gate-clean); every other surface verified clean.

```
tool: validate-pr-A
ruleId: cross-document-identifier-year-drift
level: warning
rubric: in-window
location: ai/template-model-card.md:56 (also :57,:58,:88,:89); ai/template-system-card.md:59,:60,:110,:111
evidence: The two card templates cite ISO/IEC 12792 and ISO/IEC 25059 WITHOUT the edition year, while the three prose docs in the same PR carry the year (ISO/IEC 12792:2025, ISO/IEC 25059:2023). Within template-model-card.md the drift is internal: 12792 cited bare 5x while ISO/IEC TR 24027:2021 carries its year. Gate-clean (the citation gates accept the bare form) and unambiguous, so low severity; fix is adding ":2025"/":2023" in the template field-hint parentheticals to match the PR's own prose convention.
```

**Verified clean:** (a) audit-framework new §6.1 exists and its "Section 7" ref resolves; risk-assessment Step 1->Step 10 loop closed (Step 10 now confirms the provisional rating); transparency §4 restructure purely additive (4.1/4.2/4.3 intact). (b) 25059 clause meanings agree across the standard and the evaluation procedure; the audit-framework §10 row corrected to point at 6.1, the old 4/5/9 mis-attribution gone. (c) the new AI System Register `Constituent Models and Dependencies` field is referenced by BOTH asserted consumers (procedure-foundation-model-lifecycle.md:178 and register-model-registry.md:116/118), resolving the #793 finding; all 9 new alignment rows point at existing sections. (e) all 9 body-changed docs bumped Version + Date. (f) taxonomy and portal `--check` both in sync. (h) OWASP short-form consistent post-fix; SQuaRE expanded on first use; the sole drift is the 12792/25059 year-omission in the templates.

## Cross-reference check

Inbound citers of the 9 touched documents resolve (path-only Related-Documents citations, unaffected by the edits). The intra-cluster references the PR introduced (audit §10 -> §6.1; the AI System Register field's two consumers; the Step 1 -> Step 10 loop) all resolve, confirmed by Subagent A.

## Orchestrator triage

- **Finding 1 (`cross-document-identifier-year-drift`, warning, in-window):** FIX rides PR 5. Add the edition year to the 12792 and 25059 citations in the two card templates (5 in `template-model-card.md`, 4 in `template-system-card.md`) to match the year-form the same PR used in its prose docs. Bumps `template-model-card.md` 1.0.1->1.0.2 and `template-system-card.md` 1.1.2->1.1.3. The broader corpus-wide mixed bare/with-year usage (12792: 8 bare / 5 with-year) is a separate consistency question, not forced here; the fix makes THIS PR's own usage internally consistent.

## Resulting hot-fix PR

None. The fix + this record + the history row + the /retro row batch into **PR 5** (recursion-avoidance).

## Notes

Both #794 verification lenses were complementary again: the pre-push verifier caught the OWASP short-vs-full name divergence (reverted pre-push), and the post-merge Subagent A caught the 12792/25059 year-omission the verifier's source-attribution lens did not target. Both are citation-FORMAT consistency within a multi-doc PR, not source-accuracy defects.
