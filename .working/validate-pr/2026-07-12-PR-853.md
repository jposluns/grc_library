# PR-scoped validation sweep, PR #853, 2026-07-12

## Trigger and state snapshot

- **PR:** #853 (merge commit `1238f8c`), "Fix no-MCP CI-wait discipline (timer sprawl) + absorb #852 /validate-pr findings". `.claude/CLAUDE.md` + `.working/` bookkeeping; no corpus body.
- **Mechanical baseline:** 69/69 (unchanged by the `.claude/`/`.working/`/CHANGELOG/README diff; `.claude/` is gate-exempt).
- **Known-and-being-corrected:** #853's CLAUDE.md paragraph recommended a hand-rolled `gh pr checks <N> --json` loop; `--json` is unsupported by `gh pr checks` here, so that loop spins forever. Corrected in #854 (the `gh pr checks --watch` rewrite). Subagent A was told this and asked to hunt for OTHER defects.

## Subagent A return

**1 warning, 1 note.**

1. `count-fix-residual-word-form` (warning, in-window): `.working/validate-sweeps/2026-07-12-sweep100-iter1.md:47` read "all **five** findings are note-level", a THIRD count carrier in the same file that #853's count fix (which corrected lines 44 and 48) missed. It survived because #853's "zero residual" check used the digit-form grep `5 notes?`, which cannot match the spelled-out "five findings" (the word-vs-digit pattern-width axis). Doubly wrong: "five" (count is 4) and "findings" (the fifth is a refuted flag, not a note). Falsifies #853's own "zero real residual" claim.
2. `bold-lead-referenced-as-h2-heading` (note, in-window): the CI-wait paragraph #853 added referenced the sibling subsection as "`## Background-task check SOP`", but that subsection is a `**bold**`-lead paragraph, not an H2. In-window because #853's own added text introduced the reference. Cosmetic, gate-exempt file.

## Cross-reference check

Version consistency verified across all six surfaces (README `2026.07.341`/`1.9.702`; root + detailed CHANGELOG both #853/`2026.07.341`; validate-pr history `1.2.621`; improvement-log `1.0.560`; validate-sweeps history `2.0.96`). Digit-form "5 note(s)" grep across all carriers returns only legitimate finding-descriptions and false substring hits ("D5 note", "015 note"), no digit residual. The #852 record and history row accurately describe #852's findings and fixes. #853 CHANGELOG added lines dash-free with path-shaped refs linked. The dangling-pointer neutralize is correct and complete (handoff line 42 reworded; line 38's "#833 (sweep98) block below" still valid, that block is present; no other dangling "supersedes ... below"). Prune-discipline bullet coherent. The `--json` defect is confirmed already corrected in-tree by #854's `--watch` rewrite.

## Orchestrator triage

- **Finding 1 (word-form residual):** CONFIRMED, in-window, FIXED in #854 (line 47 reworded to "all four notes are note-level; the ETSI item is a refuted flag, not a finding"). Word-form-tolerant proximity grep `(5|five)[^.0-9]{0,40}(note|finding)` re-run across all carriers to confirm no other spelled-out residual.
- **Finding 2 (`##` mislabel):** CONFIRMED, cosmetic, FIXED in #854 (dropped the `##`, reference now "**Background-task check SOP** below").

Both bundle into #854 per recursion-avoidance. No error-level finding; no dedicated hot-fix PR.

## Resulting hot-fix PR

None dedicated; both note/warning fixes bundle into #854 (the `gh pr checks --watch` correction PR).

## Notes

Two recurring lessons captured in the #853 `/retro` row: (1) the count-fix "zero residual" grep must be run at WORD-FORM width (`(N|word)`), not digit-only, when a count is spelled out anywhere; this is the pattern-width axis the CLAUDE.md close-out checklist already names, recurring. (2) A discipline codified from a tool-doc EXAMPLE (`gh pr checks --json`) shipped a command unverified in THIS environment; commands codified into standing discipline must be verified in-environment first (evidence-grounded-completion at the discipline-authoring boundary).
