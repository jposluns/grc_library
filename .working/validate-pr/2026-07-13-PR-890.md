# PR-scoped validation, PR #890

## Trigger and state snapshot

- **PR**: #890, the deep-assessment r3 Phase-3 `/guardrails` (r10) + the drift-cluster fix + #889 QA.
- **Merge commit**: `8b9b86b` (squash on `main`).
- **Post-merge mechanical baseline**: all 69 gates pass (Subagent A ran `build-taxonomy.py --check` and `build-portal.py --check`, both exit 0; gates 39/50/59/69 standalone rc=0).
- **Diff**: two tool docstrings + a versioned corpus doc (spec §6 narrative, Version 1.17.1->1.17.2) + regenerated taxonomy/scorecard + TODO §3.62 + guardrail-review r10 record/history + #889 QA + CHANGELOG + README.

## Subagent A return

**0 error / 1 warning (in-window, FIXED in the follow-on PR) + 5 notes (all PASS-confirming).**

```
tool=subagentA
ruleId=gate29-docstring-incomplete-drift-remediation
level=warning
location=tools/lint-version-date-consistency.py:9
fingerprint=gate29docstringstaleheaderform
classification=in-window (missed sibling surface of PR #890's own drift-fix scope)
evidence: gate 29's top-of-file docstring (`lint-version-date-consistency.py:9` "matching the pattern `## YYYY-MM-DD, Library Version YYYY.MM.patch`") describes the gate's invariant as reading ONLY the legacy `## ...` heading, while its code matches BOTH forms (CHANGELOG_HEADING_RE + COMPACT_HEADING_RE) and the live root CHANGELOG is 100% compact (`grep -cE '^## [0-9]{4}-' CHANGELOG.md` = 0). This is the identical drift class #890 fixed for the gate-50 docstring, gate-59 docstring, and spec §6 narrative; the r10 drift lens named gate 29's CODE as both-form-matching but did not check gate 29's own docstring, so the "three descriptive surfaces, all fixed" completeness claim is one surface short. Warning, functionally harmless (code correct, CI green), but a real residual carrying the same "narrow the code back to ##-only" risk the r10 finding names.
```

Subagent A's five notes CONFIRM the rest clean: (drift-fix-correctness) both #890-fixed gates' code genuinely matches both forms and the reworded docstrings + spec narrative introduce no new inaccuracy; (positional-token-self-catch) the `TODO 3.16` token is gone from the spec and the tools' `TODO 3.16` comment tokens are out of gate-69 scope (tools/ not in DEFAULT_PATHS); (generated-artefact + version-chain) taxonomy/scorecard `--check` both exit 0, spec taxonomy entry 1.17.2/2026-07-13, README 378/1.9.739, CHANGELOG chain monotonic; (record agreement) the r10 record, history row, and r3 Phase-3 completion marker agree on counts; (dash-free) 0 em/en dashes in the new `.working/` prose.

Item verdicts: 1 (drift-fix correctness) PASS; 2 (spec version + regen) PASS; 3 (no positional-token/shall regression) PASS; 4 (TODO §3.62 well-formed) PASS; 5 (CHANGELOG parity + version chain) PASS; 6 (record/history/r3 agreement) PASS; 7 (dash-free) PASS. One cross-cutting WARNING: incomplete drift remediation.

## Cross-reference check

Clean, and EXTENDED: the orchestrator grepped ALL of `tools/` for the stale legacy-header descriptive form (bare-token width, the find-every-carrier lesson) and found the warning's gate-29 docstring PLUS a SECOND same-class carrier the finding did not name: `tools/check-changelog-on-pr.py:11` ("the root CHANGELOG.md carries lead-paragraph summaries"). The other grep hits are #890's correct both-forms fixes or the both-forms inline comment blocks.

## Orchestrator triage

- The warning (gate-29 docstring) is VERIFIED (line 9 states only the legacy form; the code matches both). **FIXED in the follow-on r3 Phase-3 `/claim-fit` PR** (this branch): gate-29 docstring reworded to name both header forms.
- The cross-reference check surfaced a 2ND same-class carrier (`check-changelog-on-pr.py:11` "lead-paragraph summaries"), also **FIXED here**. So the drift remediation is now complete across FIVE surfaces (gate-50 docstring, gate-59 docstring, spec §6 narrative [#890]; gate-29 docstring, check-changelog-on-pr.py docstring [this PR]).
- No other findings; no separate hot-fix PR (folded into this PR).

## Resulting hot-fix PR

None separate; the drift-completeness fix (2 more surfaces) folds into the r3 Phase-3 `/claim-fit` PR that carries this record.

## Notes

The finding is a completeness miss of #890's OWN fix scope: the r10 drift lens named three surfaces and I fixed exactly those, without grepping ALL gate docstrings at bare-token width for the same stale form. The lesson (find-every-carrier / bare-token-width completeness on a drift/rename fix) is recorded in the #890 `/retro` row. This row and record batch into the r3 Phase-3 `/claim-fit` PR.
