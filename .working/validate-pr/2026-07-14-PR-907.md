# /validate-pr record: PR #907

## Trigger and state snapshot

PR #907 (RB-ETSI see-also + DA-ISO20000 resolved + FR-210 residual completion), squash-merged as `fb8f5a1` on `main`. Highest merged PR at run time. Library `2026.07.395` / README `1.9.756`. Post-merge mechanical baseline: **all 69 gates pass** (single isolated `run_all_audits.sh`, rc 0). Read-only-git throughout (orchestrator on a concurrent working tree). Rows + fixes batch into #908.

## Subagent A return

Two in-window bookkeeping findings + two out-of-window notes; **zero corpus-content defects** (all three body edits accurate and correctly version-tracked).

- **[F1] `todo-rotation-omission` (warning, in-window).** `TODO.md` §3.64 DA-ISO20000 bullet still carried the now-false claim "could not be judged against ground truth ... the held base holds only ISO/IEC 20000-10:2018", while #907 RESOLVED it (ISO/IEC 20000-1:2018 now held at `catalogue.yml:2142`, citation verified+tightened). Subagent A recommended delete-from-TODO + DONE entry per PR-workflow step 7.
- **[F2] `todo-stale-prose-partial-completion` (note, in-window).** `TODO.md` §3.63 RB-ETSI-104128 bullet still read "Add it as a corroborative see-also in [guide]" though that primary engagement shipped (`ai/guide-ai-security-technical-implementation.md:628`); only the secondary target (`ai/standard-ai-security-and-risk.md` alignment table) remains.
- **[N1] `known-corpus-term-drift` (note, out-of-window, already tracked).** Bare "GRC Manager" persists in other docs (`compliance/procedure-capa.md` §2.3, `procedure-audit-planning.md`, `security/policy-encryption-and-key-management.md`, `supply-chain/procedure-supplier-audit.md`, `procedure-supplier-due-diligence.md`); canonical is "GRC Programme Manager" per `register-role-authority.md:64`. Already tracked as TODO §3.71, which correctly scopes internal-audit OUT (Subagent A's independent `grep -oE 'GRC Manager' compliance/standard-internal-audit.md` = 0 bare, confirming §3.71's completeness note).
- **[N2] `ref-catalogue-title-convention` (note, out-of-window, not PR-introduced).** The guide's "Securing AI (SAI)" matches the held ref catalogue verbatim (`catalogue.yml:2488`), but the ref document's own extracted title line reads "Securing Artificial Intelligence (SAI)". The corpus is FAITHFUL to the catalogue, so this is a ref-repo catalogue-title convention question, not a corpus defect.

## Cross-reference check

None of the 3 touched corpus docs was renamed/moved; their 10 / 21 / 28 markdown citers resolve unchanged.

## Orchestrator triage

- **F1 validated, recommendation ADJUSTED to the cluster convention.** DA-ISO20000 is a SUB-BULLET inside the r3 routed-findings cluster §3.64, whose sibling bullets (DA-ASVS, DA-DORA-A12, DA-AIACT-A26) are all marked `RESOLVED #NNN` IN PLACE, not deleted-and-DONE-rotated per bullet; the cluster rotates to DONE only when its last bullet resolves (§3.64 still has the open DA-gate25-scaffold bullet). Subagent A did not see the cluster convention, so its literal delete+DONE recommendation would break cluster consistency. The VALID underlying defect is the stale-false text. **Fix in #908: mark the DA-ISO20000 bullet `RESOLVED #907` in place and correct the now-false ground-truth claim** (consistent with its siblings). Severity as a rotation defect: downgraded to a stale-text correction.
- **F2 validated; fix in #908.** Rescope the RB-ETSI-104128 bullet: mark the guide see-also complete (#907), keep the bullet open for the remaining secondary `standard-ai-security-and-risk.md` alignment-table target (a corpus-body add, handled under §3.67 reference-breadth, not the CHANGELOG-hygiene #908). Same in-place convention as its `RESOLVED #899` sibling RB-FFIEC-CAT.
- **N1:** no action (tracked §3.71). **N2:** noted for the ISO/ETSI reference-base maintainer as an optional catalogue-title exactness question; recorded against the §3.67 ISO 20000 reference-breadth task; corpus is correct as-is.

## Resulting fixes

Batched into #908 (CHANGELOG-hygiene reformat PR): F1 in-place RESOLVED-marking + false-text correction; F2 rescope. Both `.working`/TODO only; zero adopter-facing corpus change from the triage.

## Notes

Fifth completeness-family catch of the session, but a distinct sub-class (stale-text-on-in-place-resolution, not carrier-completeness); every instance caught by the per-PR `/validate-pr`, none adopter-facing. The verifier's cluster-convention blind spot (recommending a rotation the cluster doesn't use) is why the orchestrator VALIDATES rather than auto-applies a verifier recommendation.
