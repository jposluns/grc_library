# /validate-pr record: PR #911

## Trigger and state snapshot

PR #911 (ISO/IEC 20000-1 clause-attribution accuracy pass, closes TODO §3.72), squash-merged as `c26f7cb`. Highest merged PR at run time. Library `2026.07.399` / README `1.9.760`. Post-merge baseline: all 69 gates pass. Read-only-git. Rows + fixes batch into #912.

## Subagent A return

All 11 corpus clause corrections CORRECT against the held 20000-1:2018 source (every new §-number matches its held clause title verbatim; the fabricated "§8.6 Service management" label correctly removed; corpus-wide residual grep = 0 wrong 20000-1 §-citation). 3 Low in-window record-hygiene findings (all `.working`/register bookkeeping, no adopter-facing corpus defect):

- **[F1] stale-count/self-contradicting-changelog (Low, in-window).** `.working/changelog-details/CHANGELOG-detailed.md:15` the "### Fixed" lead said "Six ... corrected across five operations documents", contradicting the same entry's Verification line ("11 across 7"), the root CHANGELOG, DONE, and the diff. The 10/6->11/7 count expansion (after the change-mgmt + register carriers were added) updated most surfaces but left this Fixed-lead survivor: the CHANGELOG count-reflex miss.
- **[F2] stale-count (Low, in-window).** `.working/next-prs.txt` precision note said "the 6 clause fixes" (stale; the pass made 11).
- **[F3] paired-surface-incompleteness (Low, in-window).** `governance/register-document-index-and-classification.md:123` (the change-mgmt procedure's register row) still listed framework "ISO/IEC 20000" (bare) while #911 normalized that same doc's own alignment header and the register's SLM row to "ISO/IEC 20000-1"; the lone bare sibling of the PR's own normalization.

Frozen (unfixable, not a finding): the squash-commit MESSAGE body carries the pre-expansion "6 across 5" text (written at the original commit, --no-edit amended); the authoritative CHANGELOG/records are now 11/7.

## Verified clean

Version+Date co-bumps (all 7 docs 2026-07-14); taxonomy + scorecard propagated (7 bumps); §3.72 closed + DONE + no live orphan; count consistency 11/7 in root CHANGELOG, detailed Verification, DONE, claim-fit history + detail, session-state (only F1/F2 carried stale 6); claim-fit history 1.0.10; #910 QA batched (0-finding validate-pr row + retro); table integrity; README + CHANGELOG parity.

## Orchestrator triage

F1/F3 fixed in #912 (detailed Fixed-lead -> "Eleven ... across seven documents"; register:123 -> "ISO/IEC 20000-1", register 1.27.85 -> 1.27.86 + regen). F2 fixed by the #912 next-prs.txt rewrite (the stale phrase dropped). All three Low, `.working`/register only.

## Notes

Third same-family recurrence on the ISO 20000 cluster: the 10/6->11/7 count expansion (itself a fix for the #911 re-verify's D1/D2) left 2 stale-count survivors (F1, F2), and the register normalization left 1 paired sibling (F3). All caught in-window (the layered verification working), 0 adopter escapes; but the count-reflex on a MID-PR count CHANGE keeps leaving one survivor. Retro records the sharpened habit: when a count changes mid-PR, bare-token grep the OLD count (both digit and word forms) across EVERY surface, including the CHANGELOG entry's own non-lead sentences and next-prs.txt.
