# PR-Scoped Validation, PR #915

## Trigger & state snapshot

- **PR**: #915, "Sweep 103 `/validate` close-out (resume from #914)", squash-merged as `fbe0bd0` on `main`.
- **First PR of the resumed session** `claude/resume-sweep103-validate` (attended-autonomous). Its `/validate-pr` + `/retro` rows and the two findings' fixes batch into the next PR (#916, the P1 §1.2 CHANGELOG reformat) per recursion-avoidance.
- **Post-merge baseline**: `tools/run_all_audits.sh` = all 69 gates pass.
- **Touched files**: `governance/specification-audit-programme.md` (the substantive change: gate-50 §6 fifth-check sentence + Version 1.17.3->1.17.4), `taxonomy.yml`, `docs/maturity-scorecard.md` (regenerated), `CHANGELOG.md` + `.working/changelog-details/CHANGELOG-detailed.md` (#915 entries), `README.md` (library 2026.07.403 / README 1.9.764), and the `.working/` bookkeeping set (session-handoff prune, session-state lease ACQUIRE, pending-decisions bank, next-prs, validate-sweeps history row + detail file).

## Subagent A return

Subagent A (scoped to the #915 diff) confirmed the substantive change and the version/generator surfaces clean, and found **2 in-window issues** in the `.working/` bookkeeping:

- **F1 (warning, `table-row-join` / multi-surface-record-corruption):** `.working/validate-sweeps/history.md:15` the prepended Sweep 103 row merged onto one physical line with the prior Sweep 102 row; the Sweep 102 row lost its leading `| Date | Sweep |` identifier columns (`grep -c '| 102 iter 1 |'` = 0 before fix). Cause: the new-row Edit did not carry a trailing line break before the retained Sweep 102 row. Gate-blind: `.working/` is exempt, gate 59 reads only the CHANGELOG pair, gate 51 checks only dashes, so it escaped to `main`.
- **F2 (note, Low, `paired-surface-value-lag`):** `.working/session-handoff.md:38` still read "coverage-refresh sync (index 171+ PRs behind)" while #915 freshly wrote "185" into `next-prs.txt`, `session-state.md`, and the `pending-decisions.md` bank; the fresh figure was not reconciled to the sibling handoff clause.

Clean (coverage): version consistency (library 2026.07.403 / README 1.9.764 / spec 1.17.4) across README, both CHANGELOG headers, the spec header, `taxonomy.yml`, `docs/maturity-scorecard.md`, and the handoff `Current truth` snapshot line; root #915 and detailed-mirror #915 present and lock-step, and the #914 detailed-mirror header intact (not clobbered by the #915 insertion); both generators `--check` in sync; spec Version AND Date both bumped; the gate-50 §6 fifth-check sentence accurate vs `tools/lint-bookkeeping-parity.py` Check 5 (strictly-ascending run-number, precision-first/FP-free, register-less-fork -> no findings, sibling gates 59/63 correct); the handoff prune clean (keep-current+1-prior, #886 removed, both superseding notes reworded, no dangling "below" pointer); the lease ACQUIRE well-formed (gate-63 shape); the Sweep 103 history/CHANGELOG claims (A:0/B:0/C:1-note-fixed) match the detail file.

An out-of-window observation (NOT a #915 defect): `session-handoff.md:38` carried a duplicated `(3)` list marker inherited from the #914 handoff (present in both the pre- and post-#915 diff lines). Cosmetic.

## Cross-reference check

The only touched corpus file is `governance/specification-audit-programme.md`. The change added a fifth-check sentence to gate 50's §6 prose (no section renumber, no heading change, no removed anchor). Grep for citers plus a corpus-wide check for any prose stating a gate-50 check *count* that "five checks" would make stale: **zero**. The gate-50 internal-check phrasing ("N internal check of gate 50", "fourth internal check") lives ONLY in the spec itself (`grep -rln 'internal check of gate 50'` = the spec alone); no other file cites it. **Cross-reference check CLEAN.**

## Orchestrator triage

- **F1**: in-window, FIXED in #916 (split line 15; restored `| 2026-07-14 | 102 iter 1 |`; verified `| 102 iter 1 |` = 1 row and 2 physical Sweep rows post-fix).
- **F2**: in-window, FIXED in #916 (`171+` -> `185`; the inherited duplicate `(3)` marker relabelled to `(4)`/`(5)` while on the line).
- Both fixes and this row batch into #916 (the P1 §1.2 PR), per recursion-avoidance; no dedicated hot-fix PR.

## Resulting hot-fix PR

None. Fixes bundled into #916 (P1 §1.2 CHANGELOG reformat) per recursion-avoidance.

## Notes

The F1 table-row-join is the recurring mechanical-edit class the project handoff names (a prepend without the trailing line break). Both findings are `.working/` bookkeeping, gate-blind, non-adopter-facing; caught by the mandatory post-merge sweep, in-window, zero adopter escapes.
