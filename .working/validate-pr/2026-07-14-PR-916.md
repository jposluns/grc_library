# PR-Scoped Validation, PR #916

## Trigger & state snapshot

- **PR**: #916, "P1 §1.2: reformat root CHANGELOG #902-#914 to compact plain-language form + strengthen the readability guard (+ #915 QA batch)", squash-merged as `7596f18` on `main`.
- **Post-merge baseline**: `tools/run_all_audits.sh` = all 69 gates pass.
- Its `/validate-pr` + `/retro` rows and the two findings' fixes batch into #917 (the session-closing handoff PR) per recursion-avoidance.
- **Touched files**: `CHANGELOG.md` (reformat + #916 entry), `.working/changelog-details/CHANGELOG-detailed.md` (#916 entry), `tools/audit-changelog-entry-length.py` (longest-sentence signal), `TODO.md` (§1.2 delete, §3.73 add), `.working/DONE.md` (§1.2 entry), `README.md` (library 2026.07.404 / README 1.9.765), and the batched #915 QA set (`.working/validate-pr/history.md` + record, `.working/improvement-log.md`, `.working/validate-sweeps/history.md`, `.working/session-handoff.md`, `.working/session-state.md`, `.working/next-prs.txt`).

## Subagent A return

Subagent A (scoped to the #916 diff) confirmed the substantive change clean and found **2 in-window issues**, both `note`/Low, both meta-prose-measurement class, both in gate-exempt/non-adopter-facing surfaces:

- **F1 (`meta-prose-measurement/cross-surface-stale-figure`):** `tools/audit-changelog-entry-length.py:15,31` still stated the old #902-#914 range as "74-147" after `DONE.md` and the detailed mirror were corrected to 67-140; the correction was not propagated to the tool docstring the same PR edited (multi-surface incompleteness).
- **F2 (`meta-prose-measurement/off-by-one-range`):** the detailed-mirror #916 entry stated the reformatted new range as "62-79", but #912's D2 reword pushed its word count to 80, so the actual max is 80 (62-80); a figure written before the mid-PR reword changed it (count-reflex).

Clean (coverage): version consistency (library 2026.07.404 / README 1.9.765) across README + both CHANGELOG #916 headers + the handoff snapshot line; gate-59 header parity (root headers >=#667 = 250, detailed = 250, symmetric difference empty; #914/#915/#916 present in both, no clobber/merge from the reformat or inserts); reformat faithfulness spot-checked (#904 14/30/90, #905 4096/P-384, #906 Brazil-15/Mexico-20, #911 11-clauses/7-docs; the #912 D2 fix held); F1/F2@#915 fixes verified (sweep-history `| 102 iter 1 |` = 1 own-line row; handoff reads 185); the guard tool `--self-test` 6/6 + live run clean + still advisory (not gate-wired); TODO §1.2 rotated to DONE + §3.73 present; no residual table-row-join in any touched ledger.

## Cross-reference check

No corpus-document body was touched by #916 (the reformat is CHANGELOG-only; the guard tool has no normative citer). **Cross-reference check CLEAN.**

## Orchestrator triage

- **F1**: in-window, FIXED in #917 (`audit-changelog-entry-length.py` docstring "74-147" -> "67-140" x2).
- **F2**: in-window, FIXED in #917 (detailed-mirror #916 entry "62-79" -> "62-80").
- Both fixes and this row batch into #917 (the session-closing handoff PR), per recursion-avoidance.

## Resulting hot-fix PR

None. Fixes bundled into #917.

## Notes

Both findings are the meta-prose-measurement class the close-out checklist governs (a figure written from an earlier state, or a correction not propagated to every sibling surface). Together with the recurring table-row-clobber class caught this PR (the #915 detailed header + the #914 validate-pr row, both caught by #916's post-commit audit), they formed the mid-run precision-strain signal that drove the maintainer-confirmed session-closing handoff at #917.
