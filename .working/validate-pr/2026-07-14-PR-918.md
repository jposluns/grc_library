# PR-Scoped Validation Sweep, PR #918

## Trigger and state snapshot

Post-merge `/validate-pr` for **PR #918** (the Sweep 104 `/validate` close-out, first PR of the 2026-07-14 resumed session), squash-merged as `1ae6e7a` on `main` (parent `9cb42fe` = #917). Run during the assembly of PR #919 (the `§2.4` website build), which absorbs this sweep's row and fixes per recursion-avoidance.

- Library `2026.07.406` / README `1.9.767` at `1ae6e7a`.
- Mechanical baseline: `run_all_audits.sh` 69/69 at HEAD (`1ae6e7a` plus the uncommitted #919 scaffold; linters discover via filesystem `rglob` so untracked files are scanned).
- Subagent A dispatched read-only-git on the #918 diff (`git diff 9cb42fe..1ae6e7a` / `git show 1ae6e7a`), refute-briefed, with the pre-tool verification preamble. The orchestrator was on the concurrent feature branch `claude/cloudflare-public-website-2d7sis` in the shared tree; the subagent used only `git show` / `git diff` / `git log` (no checkout/switch/reset/stash).

## Subagent A return

Counts: **error 0 / warning 1 / note 1**. Verdict: FINDINGS (both in-window).

```
FINDING 1  (warning)  ruleId: fabricated-latent-gate-failure / inferred-as-verified-state-assertion
fingerprint: pr918-handoff-marker-phantom-catch
location: .working/changelog-details/CHANGELOG-detailed.md:18; .working/validate-sweeps/2026-07-14-sweep104-iter1.md:105;
          .working/validate-sweeps/history.md:15; CHANGELOG.md:6; (commit-message body of 1ae6e7a)
```

#918 recorded a 5th "in-window fix": a supposed latent gate-50 Check-1 failure in which PR #917's exemption row "lacked" the recognized marker, "FIXED by rewording #917's Findings cell". Refuted on three independent legs, each re-verified by the orchestrator against the actual files:

1. **Parent-state read** (`git show 9cb42fe:.working/validate-pr/history.md`, field `c[4]`, the Findings cell): #917's Findings cell already read `SKIPPED (handoff-PR exception): the session-closing handoff PR takes no trailing /validate-pr or /retro ...` at the parent, so the recognized marker (both `handoff-PR exception` and `SKIPPED ... handoff`) was present before #918.
2. **The #918 diff** (`git diff 9cb42fe 1ae6e7a -- .working/validate-pr/history.md`): the only change to the #917 row is in `c[3]` (Touched files); `c[4]` (Findings) is byte-identical. So the "fix" edited the wrong cell.
3. **Gate code** (`tools/lint-bookkeeping-parity.py`, `parse_validate_pr_status`): `findings = c[4]` then `if HANDOFF_FINDINGS.search(findings): row_status = "handoff"`. The gate reads `c[4]`; the prose #918 cited ("SESSION-CLOSING HANDOFF PR") lives in `c[3]`, which the gate never reads. #917 was classified handoff-exempt all along.

Conclusion: no latent gate-50 failure ever existed; the "fix" was a no-op in the wrong cell; the recorded rationale is false and had propagated across five surfaces (the four editable ones above plus the immutable commit-message body).

```
FINDING 2  (note, dependent on 1)  ruleId: inflated-fix-count
location: CHANGELOG.md:6; .working/changelog-details/CHANGELOG-detailed.md:18
```

The #918 root entry says "It fixed five in-window audit-tooling accuracy items". Only four are genuine (C-1 the `four`->`five checks` header; A-1 the total-vs-single-sentence reword; A-2 the splitter regex `?`/`!` extension; B-1 the recycled `§1.2`->#916 reference). The 5th is the phantom of Finding 1, so the count overstates by one.

**Refutations that FAILED (the four genuine items are CLEAN):** C-1 confirmed (`git show 1ae6e7a:tools/lint-bookkeeping-parity.py` has exactly five `**Check N,` headers; no live surface still says "four checks"); A-2 confirmed monotonic (adding `?`/`!` to the lookbehind only adds split points, so a measured sentence can only shorten; the "false positive not possible" claim holds; `--self-test` 6/6, exit 0); version bumps (README `2026.07.405->406` / `1.9.766->767`, validate-pr-history `1.2.684->685`, validate-sweeps `2.0.100->101`) all present; gate-59 root/detailed #918 header parity holds; #918 touches no corpus body or generated artefact, so no regen owed. B-2 (`compliance/procedure-capa.md` Source-field) correctly recorded out-of-window and routed, not a #918 defect.

## Cross-reference check

Targeted at the surfaces carrying the phantom claim (corpus-wide grep, all `.md` file types, bare-token and phrase forms `five in-window` / `5th in-window` / `latent gate-50` / `handoff-marker rewording` / `lacked the marker`):

- **6 live carriers** of the false claim: `CHANGELOG.md:6`, `.working/changelog-details/CHANGELOG-detailed.md:18` and `:24`, `.working/validate-sweeps/2026-07-14-sweep104-iter1.md:105` and `:106`, `.working/validate-sweeps/history.md:15`. All corrected in #919.
- The **immutable commit-message body** of `1ae6e7a` also carries it; not rewritable (protected history), noted here and in the #919 CHANGELOG instead.
- **Unrelated hits confirmed untouched** (different PRs, correct as-of-write-time): `validate-pr/history.md:344` (#588 "five in-window fixes"), `validate-sweeps/history.md:40` + `session-metrics.md:104` + `improvement-log.md:488/494` + `validate-pr/history.md:480` (the #452/#459 "handoff-marker 'Summary cell'->'Findings cell'" root-cause work).
- No corpus-document body was touched by #918, so there is no citer-of-a-touched-file cross-reference to re-resolve.

## Orchestrator triage

Both findings are **in-window** (defects in #918's own audit trail) and **corrected in #919** per recursion-avoidance (no dedicated hot-fix PR): the six carriers reworded to withdraw the phantom and state the true four-item count; #917's `c[3]` Touched-files cell reverted to its accurate parent text (`c[4]` untouched, so gate 50 still classifies #917 handoff-exempt). The four genuine fixes stand. The process lesson (inferred-as-verified: characterizing a table cell without reading the actual Findings cell) is logged in the #918 `/retro` row.

## Resulting hot-fix PR

None. Both fixes batch into **PR #919** (the website build) alongside its own work, per the recursion-avoidance rule. History-row source-to-fix trace: originating PR #918, closing PR #919.

## Notes

- Read-only-git respected throughout (Subagent A used only `git show` / `git diff` / `git log`; no checkout/switch/reset/stash on the shared tree).
- Subagent A did not read the uncommitted `.web/` scaffold (out of the #918 diff scope); that change is covered by the separate pre-push website skeptical verifier (verdict SHIP) recorded in the #919 CHANGELOG.
- This is a genuine shipped-escape (the phantom reached merged `main`, including the adopter-facing root `CHANGELOG.md`), caught in-window by the very next PR's post-merge sweep, which is the designed function of `/validate-pr`. The verification layer worked; it is not a degradation signal.
