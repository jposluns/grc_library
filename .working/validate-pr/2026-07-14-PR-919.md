# /validate-pr record: PR #919

## Trigger and state snapshot

- **Triggering PR:** #919, "§2.4 grclibrary.ai website build: static-site generator + landing template + generator-health CI check (+ batched #918 phantom-catch correction)".
- **Merge commit:** `1850276a` (squash); parent `1ae6e7aa` (#918). The PR diff is `git diff 1ae6e7aa..1850276a`.
- **Run context:** post-merge PR-scoped sweep, run from the concurrent feature branch `claude/cloudflare-public-website-2d7sis` (the #920 build), so Subagent A judged the #919-merge state via `git show 1850276a:<path>` and never the #920-modified working-tree copies of the `.web/` files. Read-only git throughout.
- **Mechanical baseline:** 69/69. #919 merged CI-green at `1850276a`; the corpus remains 69/69, confirmed by the #920 pre-push guard (`run_all_audits.sh` + `run-pr-time-checks.sh`) run before the #920 push. #919 touched no corpus-document body and no generated artefact.
- **Recording:** batched into #920 per recursion-avoidance (this record + the history row + the `/retro` row ride #920's diff). Findings-producing (2 notes), so this per-PR file exists.
- **Note on re-dispatch:** the Subagent A dispatch below was run fresh in the #920 build (the pre-compaction dispatch's verbatim return could not be cleanly isolated from a 12 MB session transcript, and pasting a misattributed block into a verbatim-return section would be an integrity violation). A fresh formal dispatch on the immutable #919 merge is a valid `/validate-pr` invocation and eliminates that risk.

## Subagent A return

Verbatim (HTML entities that the task-notification escaped for display are rendered here as the literal characters the subagent emitted):

---

I have completed the sweep with full evidence. Read-only git throughout (`git show 1850276a:<path>`, `git diff`, `git show --stat`); I judged the #919-merge state only and never the #920-tainted working-tree copies of `.web/`. No `checkout`/`switch`/`reset`/`stash`.

---

# Subagent A: `/validate-pr` sweep, PR #919 (`1850276a`)

**Verdict: SHIP.** 0 error / 0 warning / 2 note. The public-site content boundary holds; every citation matches the canonical register; the #918 phantom-catch correction is complete and correct across all live carriers.

## Findings

### note

```
tool: validate-pr-subagent-a
ruleId: meta-prose-count-imprecision
level: note
location: CHANGELOG.md:6
fingerprint: meta-prose-count-imprecision:changelog.md:6
rubric: (c) multi-surface incompleteness / meta-prose state-claim measurement, adopter-facing surface
evidence: "...so the six records carrying it are reworded to the true four-fix count and
  the redundant cell edit is reverted."
```
The paired detailed mirror and the `2026-07-14-PR-918.md` record show the **six carriers** are: four *reworded* files (root `CHANGELOG.md` #918 entry, the detailed #918 entry, `sweep104-iter1.md`, `sweep-history.md`), one *reverted* cell (`validate-pr/history.md` c[3]), and one *immutable/uncorrectable* commit-message body (`1ae6e7a`). So the root summary's "six records … reworded" overstates the reworded set (four) and does not account for the immutable carrier (which was not corrected in place; the CHANGELOG entry is merely "the correction of record" for it). Substance is correct (phantom withdrawn, true count four) and the detailed mirror is precise; this is a low-impact wording imprecision on the adopter-facing surface only. The token "six carriers" is also used with three different denominators across the correction records (root: six; detailed: 4 files + revert + commit-msg; PR-918.md: 6 text-locations across 4 files), each self-consistent but drifting in basis. Suggest tightening the root clause (e.g. "the records carrying it are corrected to the true four-fix count, the immutable merge-commit message excepted, and the redundant cell edit reverted").

```
tool: validate-pr-subagent-a
ruleId: citation-version-form-nicety
level: note
location: .web/templates/landing.html:663
fingerprint: citation-version-form-nicety:.web/templates/landing.html:663
rubric: (b) mis-attributed citations, form-only, within tolerance
evidence: '<li><span class="id">OWASP ASVS 5.0</span><span class="topic">App security verification</span></li>'
```
The register's canonical form is `OWASP ASVS | 5.0.0` (line 221). The page abbreviates to "5.0". This is within tolerance: "5.0" unambiguously denotes the 5.0.0 release, is **not** a superseded edition (register superseded set = 4.0.3, 4.0, 3.0), and the `.web/` tree is not scanned by `lint-standards-currency.py`. Informational only; every other edition/year on the page matches the register exactly.

## Clean-by-class (highest-priority content boundary)

**Content boundary through the generator, CLEAN.** `.web/build.py` reads exactly three inputs (`TAXONOMY = REPO_ROOT/"taxonomy.yml"`, `README = REPO_ROOT/"README.md"`, `TEMPLATE = WEB_DIR/"templates"/"landing.html"`); `compute_figures()` reads only the first two, `render()` only the template; the sole write is `.web/dist/index.html`. No `os.walk`/`glob`/`iterdir` over the repo, no subprocess, no network, no read of `.working/`/`.claude/`/`tools/`/`tests/`/`.github/` or a sibling repo. A repo file cannot leak onto the site through the generator.

**Template leak scan (all 823 lines), CLEAN.** No email (`jeff@posluns.com`/`.ca`/any `mailto:`), no `.working`/`.claude`/`tools/`/`tests/` path, no `grc_library_ref`/`grc_library_scratch`, no absolute path (`/home`, `/Users`, `/tmp`, `/root`). The only personal data is the intended §07 maintainer bio and the `linkedin.com/in/jposluns` + `github.com/jposluns/grc_library` links (expected per brief).

**Placeholder-driven, CLEAN.** The template's `{{…}}` set (CALVER, DOC_TOTAL, DOMAIN_COUNT, DOMAIN_DOC_TOTAL, ROOT_COUNT, DOMAIN_ROWS, TYPE_CHIPS) is exactly build.py's 7 value keys, so `render()` raises no BuildError; no hardcoded corpus-count literal survives (grep for `eleven`/`312`/`11 domains`/`69 gate` → none). The gate suite is described qualitatively ("Continuous", "continuously-expanding suite") with no gate count anywhere.

**CI check is not a corpus gate; gate count unchanged at 69.** `.github/workflows/web-generator-health.yml` runs `python3 .web/build.py --check`, is explicitly labelled "NOT one of the audit-programme gates", is separate from `quality.yml`, uses `contents: read` and SHA-pinned actions. `git show --stat` confirms the diff touches no gate-wiring file (`quality.yml`, `run_all_audits.sh`, pre-commit config, audit-programme §6 all absent). `run_all_audits.sh` at merge has exactly **69** `run_gate` invocations (+1 helper definition).

**Citations (class b), CLEAN.** Every standards edition/year on the page (lines 588-670) matches `governance/register-canonical-citations.md`, including the staleness-prone ones: ISO/IEC 27018:2025, 27701:2025, 38500:2024, 42005:2025, 5259:2024; NIST SP 800-88 Rev. 2, 800-61 Rev. 3, 800-63B Rev. 4; CSA AICM v1.1; CSA CCM v4.1; OWASP Top 10 (2025); PCI DSS 4.0.1. The verifier's asserted figures are exact against the merge-state `taxonomy.yml`: **312** documents = **310** across **11** non-root domains + **2** root; **17** distinct types; build.py's `DOMAIN_SCOPE` covers all 11 non-root domains (no "unknown domain" BuildError). Only nicety = the ASVS 5.0/5.0.0 note above.

**#918 phantom-catch correction, COMPLETE.** The false "5th fix" (a claimed latent gate-50 Check-1 failure) is withdrawn across every live carrier: root `CHANGELOG.md` #918 entry changed "five"→"four in-window … items" with a withdrawal parenthetical; the detailed mirror, `sweep104-iter1.md`, and `sweep-history.md` bullets reworded to "WITHDRAWN as a PHANTOM"; `validate-pr/history.md` c[3] (Touched-files) reverted while c[4] (the Findings cell gate 50 reads) is byte-identical old-to-new (verified in the diff), so gate 50 still classifies #917 handoff-exempt. Final contradiction grep found no surviving assertion of the phantom as real; the one remaining "five in-window fixes" hit is the unrelated #588 historical row (history.md:345), correct and untouched.

**Other classes, CLEAN.** (a) no stale prose reference (§06 uses "across every governance domain", not a hardcoded "eleven"); (d) the only inferred-as-verified failure was #918's, correctly caught and withdrawn here; (e) #919 touches no corpus-document body, so no per-document Version/Date bump owed; (f) no generated artefact (taxonomy/portal/scorecard) touched, none owed; the git-ignored `.web/dist/` is not a committed generated artefact; (g) build.py's module and function docstrings accurately describe the code (three inputs, one output, `--check` semantics, qualitative-not-counted); (h) the six pack rule-ids on the page all match real files in `.claude/rules/governance/`. The new Cloudflare runbook (`.working/cloudflare-pages-setup.md`) carries no secret/token/email (7 `[confirm live]` placeholders; the one "token" mention is a sentence stating *no* API token is placed in the environment).

## Summary

PR #919 is safe to ship. The public-site build cannot leak internal or working-state content: the generator's inputs are an explicit three-file allow-list with no repository walk, the committed template hardcodes no internal path/email/sibling-repo/absolute-path, every corpus figure is a build-time placeholder, and the CI health check is deliberately outside the 69-gate audit programme. All 40-plus standards citations on the landing page match the canonical register (one within-tolerance form nicety, ASVS 5.0 vs 5.0.0), and the verifier's document/domain/type figures reproduce exactly against the merge-state taxonomy. The batched #918 phantom-catch correction is complete and internally consistent, with the gate-50-critical Findings cell provably untouched. The two findings are both notes: an adopter-facing count/verb imprecision in the root CHANGELOG #919 entry ("six records … reworded" where four were reworded, one reverted, one immutable) and the ASVS version-form nicety, neither of which affects the published site or any gate.

---

## Cross-reference check

The `.web/` tree is new in #919, so its citers are few. `git grep` for the touched substantive files:
- `.web/build.py` / `.web/templates/landing.html` / `.github/workflows/web-generator-health.yml`: cited only by [`.working/cloudflare-pages-setup.md`](../cloudflare-pages-setup.md) (the runbook), [`TODO.md`](../../TODO.md) §2.4, and the session handoff / next-prs working state. All references resolve to existing files; none quote a line or version that #919 invalidated.
- No corpus document cites the `.web/` tree (it is build tooling, not corpus content), so there is no citation-currency or quoted-text breakage to check.
- CLEAN.

## Orchestrator triage

Both findings are notes (Low), both in-window (they are on #919's own surfaces), no High/Medium, no hot-fix PR: the fixes and this record batch into #920 per recursion-avoidance.

- **F1 (`meta-prose-count-imprecision`, `CHANGELOG.md:6`, note, in-window, adopter-facing): FIXED in #920.** Validated against the paired records: the six carriers are indeed four reworded + one reverted cell (c[3]) + one immutable commit-message body, so "six records … reworded" overstated the reworded set. The #919 root CHANGELOG entry's clause was reworded to "the records carrying it are corrected to the true four-fix count: four are reworded, the redundant cell edit is reverted, and the immutable merge-commit message is noted as corrected of record here (a #919 post-merge validation note, fixed in PR #920)."
- **F2 (`citation-version-form-nicety`, `.web/templates/landing.html`, note, in-window, within tolerance): DEFERRED to TODO §3.74, not changed piecemeal.** The finding is correct (the page renders "OWASP ASVS 5.0" while the register records 5.0.0). A #920 change to "5.0.0" was made and then REVERTED, because the maintainer (2026-07-14) set the release-year form as the norm for standards references and directed a holistic repo-wide standardization review rather than piecemeal edits. Registered as TODO §3.74; the §05 list is left at its #919 rendering (ASVS 5.0, and ISO/IEC 5259:2024) pending that review. Within tolerance in the interim; the site is not deployed.

Related orchestrator note (not a Subagent A finding): a #920 attempt to render ISO/IEC 5259 as the register's bare Standard-column identifier ("ISO/IEC 5259", no year) was also reverted to the year form ("ISO/IEC 5259:2024") under the same maintainer directive. The corpus house form is bare for series-level references and part-and-year (e.g. 5259-4:2024) for specific parts; harmonizing this is part of TODO §3.74.

## Resulting hot-fix PR

None. F1 fixed in #920; F2 deferred to TODO §3.74. No standalone hot-fix PR (recursion-avoidance: the fixes and this record batch into #920).

## Notes

- Subagent A ran read-only-git on the shared tree (the orchestrator was on the concurrent #920 branch) and judged the #919-merge state via `git show 1850276a:<path>`, so the #920 working-tree edits to the `.web/` files did not perturb its verdict.
- The highest-value check for this PR (the public-site content boundary) is CLEAN: the generator reads a three-file allow-list and walks nothing, the committed template leaks no internal path/email/sibling-repo/absolute path, and the CI health check is outside the 69-gate audit programme. This corroborates the pre-push website skeptical verifier's SHIP verdict recorded in the #919 CHANGELOG-detailed entry.
