# PR #920 post-merge PR-scoped validation sweep

## Trigger and state snapshot

- **PR:** #920, "§2.4 website: two-page About/Contributors restructure (+ #919 QA, compact next-prs, TODO §3.74)".
- **Squash commit:** `8315c78`.
- **Run by:** the resumed VM orchestrator session (branch `claude/website-fixes-pr1`). The cloud session that merged #920 (`claude/cloudflare-public-website-2d7sis`) crashed before persisting #920's post-merge QA, so this sweep and its rows are produced fresh by the resuming session and batched into PR #921, per recursion-avoidance.
- **Baseline:** `tools/run_all_audits.sh` = 69/69 on `main` at #920 (confirmed standalone at resume).
- **Touched files:** `.claude/CLAUDE.md`, `.web/build.py`, `.web/templates/{about,landing}.html`, `.web/templates/partials/{footer,head-style,script,topbar}.html`, `.working/{changelog-details/CHANGELOG-detailed.md, cloudflare-pages-setup.md, improvement-log.md, next-prs.txt, session-handoff.md, session-state.md, third-party-issues.md, validate-pr/2026-07-14-PR-919.md, validate-pr/history.md}`, `CHANGELOG.md`, `README.md`, `TODO.md`.

## Subagent A return

Refute-briefed, read-only-git on `8315c78`. Verdict **SHIP-WITH-NOTES, 0 error / 0 warning / 3 note**.

- **N1 `stale-docstring` (g), note, IN-WINDOW.** `.web/build.py:2` (`"...public landing page..."`), `.web/build.py:29` (`"Only this generator and the template are committed."`), `.web/build.py:330` (`description="Render the grclibrary.ai landing page..."`). The two-page restructure updated the CONTENT-BOUNDARY docstring paragraph to plural but left these three singular carriers. Internal help/docstring text only, never rendered to the published site. **FIXED in #921** (all three pluralized: "landing and about pages" / "templates").
- **N2 `name-form` (h), note, no-action.** `.web/templates/about.html:62` byline "Jeff Posluns, CGEIT, CISSP" (and `about.html:6` meta) vs `AUTHORS.md` "Jeffrey Posluns". The short byline is the maintainer's intended public presentation (the detailed CHANGELOG records the credential line as intended); a bio byline legitimately differs from the source-of-record long form. No defect; surfaced to the maintainer for awareness.
- **N3 `citation-edition-doubt` (b), note, OUT-OF-WINDOW, REFUTED.** Subagent doubted `landing.html:225` ISO/IEC 27018:2025, `landing.html:261` NIST SP 800-88 Rev. 2, `landing.html:298` CSA CCM v4.1 from stale general knowledge; these lines are pre-existing (the #920 standards diff is net-zero). All three MATCH `governance/register-canonical-citations.md` as the current, recently-verified editions (27018:2025 verified 2026-07-09, supersedes 2019; 800-88 Rev. 2 final at csrc.nist.gov/pubs/sp/800/88/r2/final, verified 2026-06-30; CCM v4.1 2024). No defect. The maintainer-directed holistic standards-reference-form review is already TODO §3.74.

**Content boundary (specific ask): CLEAN.** `build.py` reads exactly `taxonomy.yml`, `README.md`, and `.web/templates/` (no `os.walk`, no `.working`/`.claude`/`tools`/sibling-repo read, no subprocess/network). No rendered page leaks an email, absolute path, or internal-tree path. The only `jposluns` occurrences are the intended public links (LinkedIn, GitHub repo, CONTRIBUTING) and the 7 contributor GitHub handles, which match `AUTHORS.md` 7/7.

## Cross-reference check

Citers of the #920-touched `.web/` files and the runbook (live, non-frozen): only `.working/cloudflare-pages-setup.md` and the `.working/validate-pr/` records. No corpus document cites the website tree. **CLEAN** (nothing to break).

## Orchestrator triage

- N1: in-window, **fixed in #921** (verified: `grep "landing page|the template are" .web/build.py` returns 0 post-fix; `build.py --check` rc=0).
- N2: no-action (maintainer's intended byline); surfaced to maintainer.
- N3: refuted against the register (all three current); no action; holistic review is TODO §3.74.

## Resulting hot-fix PR if any

None. The one in-window fix (N1) is bundled into PR #921 per recursion-avoidance (no dedicated hot-fix PR).

## Notes

The sweep confirms the register cross-check catching a stale-general-knowledge false positive (N3): a citation-edition doubt raised from model knowledge was refuted by the corpus's own currency-gated register. The N1 docstring lag is the completeness class (a partial docstring update leaving sibling carriers stale), reinforced in the #920 retro row.
