# PR #921 post-merge PR-scoped validation sweep

## Trigger and state snapshot

- **PR:** #921, "§2.4 website review polish (PR 1 of 2): full-width paragraphs, stat-cell fit, new-tab off-site links, housekeeping (+ #920 QA batch, lease takeover)".
- **Squash commit:** `389e8e9`.
- **Run by:** the resumed VM orchestrator session (branch `claude/website-domain-pages-pr2`), read-only-git on the merge commit.
- **Baseline:** `tools/run_all_audits.sh` = 69/69 on `main` at #921.

## Subagent A return

Refute-briefed, read-only-git on `389e8e9`. Verdict **SHIP-WITH-NOTES, 0 error / 0 warning / 1 note**.

- **N1 `stale-docstring` (g), note, in-window.** `.web/build.py` docstring retained two singular phrasings of the now-multi-page fact ("Every corpus figure on the page is recomputed here"; "the preview HTML that seeded the template") after PR1 fixed the three primary singular carriers. PR1's verification grep was the narrow `"landing page|the template are"`, not a full-carrier sweep, so these survived. Internal docstring only, never rendered; both defensible as generic, but the #920 retro's own full-file-grep completeness lesson points at fixing them. **FIXED in #922** (the PR2 docstring plurality sweep: "landing and about pages" -> "landing, about, and per-domain pages" across the module docstring + argparse description; "figure on the page" -> "on the pages"; "seeded the template" -> "seeded the templates"; "the two pages cannot drift" -> "the pages cannot drift").

Everything else CLEAN: versions agree (README `2026.07.409` / `1.9.770`, both CHANGELOG headers); all 11 off-site links carry `target="_blank"` and every internal anchor lacks it; root vs detailed CHANGELOG consistent; the `history.md` #920 row matches the `2026-07-14-PR-920.md` record; the stat-cell selector `.stat .num:not(.tnum)` targets exactly the two word cells; `.m-creds` grep-confirmed 0 residual; per-document version bumps present (improvement-log 1.0.623->624, history 1.2.687->688, runbook 1.0.1->2); no generated-artefact lag (README bump does not feed the taxonomy; gate 33/34 clean); no term drift; no dashes in added lines.

## Cross-reference check

The `.web/` files and the runbook are cited only by `.working/` records; no corpus document cites them. **CLEAN.**

## Orchestrator triage

- N1: in-window, **fixed in #922** (the docstring plurality sweep; verified `grep` for the singular phrasings returns 0 in `build.py` post-sweep, `build --check` rc=0).

## Resulting hot-fix PR if any

None. The one note is bundled into PR #922 per recursion-avoidance.

## Notes

The note is the full-file-grep completeness lesson (#920 retro) applied a second time: PR1's narrow verification grep left two docstring carriers of the multi-page fact; #922's plurality sweep closes them, and PR2 makes them concretely stale anyway by adding the per-domain pages.
