# PR #922 post-merge PR-scoped validation sweep

## Trigger and state snapshot

- **PR:** #922, "§2.4 website (PR 2 of 2): per-domain on-site pages + register links + SEO (+ #921 QA)".
- **Squash commit:** `34496d0`.
- **Run by:** the resumed VM orchestrator session (branch `claude/website-card-links-pr3`), read-only-git.
- **Baseline:** `tools/run_all_audits.sh` = 69/69 on `main` at #922.

## Subagent A return

Refute-briefed, read-only-git on `34496d0`. Verdict **SHIP-WITH-NOTES, 0 error / 2 warning / 0 note**. Both in-window, both FIXED in #923.

- **F1 `multi-surface-incompleteness` (a + c), warning.** `.working/cloudflare-pages-setup.md:30,36,39` still described a two-page site with a two-file input allow-list ("only inputs are `taxonomy.yml`, `README.md`, and the page templates ... only outputs are the landing page and the about page"), not swept when #922 widened `build.py`'s content boundary to the 11 domain READMEs + 11 domain pages and updated only the runbook's watch-paths section. The runbook was touched and version-bumped (1.0.2->1.0.3) in #922, so a second stale description in the same file is the full-file completeness class. **FIXED in #923** (Overview now describes 13 pages + the domain-README inputs; runbook 1.0.3->1.0.4).
- **F2 `deprecated-api` (code nit), warning.** `.web/build.py:218` `re.split(r"(?<=\.)\s", it, 1)` passed `maxsplit` positionally (DeprecationWarning on Python 3.13+, observed on 3.14 during `--check`; rc=0 but log-polluting and a latent break). Introduced by #922's new `read_domain_purpose`. **FIXED in #923** (`maxsplit=1`).

Everything else CLEAN: the 312-doc / 13-page / 310-domain-doc / 2-root figures match across the CHANGELOG, the render, and the `build --check` summary; per-domain doc-row counts sum to 310 with zero cross-page bleed; zero leaks on the domain/about pages; content boundary confirmed exactly `{taxonomy.yml, root README.md, the 11 <domain>/README.md, .web/templates/*}` with no repo walk; gates 33/34 and the external-link lint pass; per-document version bumps present; no residual singular/two-page docstring carrier in `build.py`; no term drift.

## Cross-reference check

The `.web/` files and the runbook are cited only by `.working/` records; no corpus document cites them. **CLEAN.**

## Orchestrator triage

- F1 and F2: both in-window, **fixed in #923** (verified: runbook Overview no longer says "two-page"; `build.py` emits no DeprecationWarning under `-W always`; `build --check` rc=0).

## Resulting hot-fix PR if any

None. Both warnings bundled into PR #923 per recursion-avoidance.

## Notes

F1 is the third in-window occurrence of the multi-surface / full-file completeness class this session (cf. #920/#921 retros): a change swept one description of a fact but left a sibling description in the same touched file stale. F2 is a new Python-version-currency watch-item for the `.web/` generator (positional-arg deprecation on the CI build image's Python).
