# PR #923 post-merge PR-scoped validation sweep

## Trigger and state snapshot

- **PR:** #923, "§2.4 website (PR3): title-as-link on landing cards + domain doc names, tighter domain spacing (+ #922 QA batch)".
- **Squash commit:** `63b8b42`.
- **Run by:** the resumed VM orchestrator session (branch `claude/website-sidebar-nav-pr4`), read-only-git.
- **Baseline:** `tools/run_all_audits.sh` = 69/69 on `main` at #923.

## Subagent A return

Refute-briefed, read-only-git on `63b8b42`. Verdict **SHIP-WITH-NOTES, 0 error / 0 warning / 2 note**. Both in-window, internal-only, FIXED in #924.

- **F1 `meta-prose-count-imprecision`, note.** The #923 detailed-CHANGELOG entry said "all 16 target files" / "all 16 targets"; the reality is 15 card links to **14 distinct** target files (STRUCTURED and STYLE both point at the master spec). The "16" was the earlier candidate-verification count (I had confirmed 16 candidate files exist, but only 14 distinct are actually linked). The substantive assurance (targets exist) is true; only the count was wrong. Root `CHANGELOG.md` and `README.md` state no count, so nothing adopter-facing was wrong. **FIXED in #924** (corrected to 14 distinct across both lines).
- **F2 `stale-docstring`, note.** `.web/build.py` `render_domain_doc_rows` docstring still enumerated "type tag, title, and a GitHub link" (two row elements) after #923 merged the title and the GitHub link into one anchor (title IS the link; the separate link was deleted). **FIXED in #924** (docstring now describes the single title-link, no separate GitHub link).

Everything else CLEAN: all 15 landing card labels are links, each to its claimed target, all 14 distinct targets exist; the 6 pack-rule cards link to their identically-named rule files; version surfaces agree (README `2026.07.411` / `1.9.772`, both CHANGELOG headers); per-document bumps present (improvement-log, history, runbook, README); zero leftover `doc-link` / "GitHub" row link in any rendered page; the spacing change is scoped to `domain.html`'s page-style; every off-site link carries `target="_blank" rel="noopener"` and no internal anchor does; no generated-artefact lag; no term drift.

## Cross-reference check

The `.web/` files and the runbook are cited only by `.working/` records; no corpus document cites them. **CLEAN.**

## Orchestrator triage

- F1 and F2: both in-window, internal-only, **fixed in #924** (F1: CHANGELOG count 16 -> 14 distinct; F2: docstring rewritten; `build --check` rc=0 after).

## Resulting hot-fix PR if any

None. Both notes bundled into PR #924 per recursion-avoidance.

## Notes

Both notes are the session's dominant recurring class: F1 the measured-not-inferred count (a ship-claim count written from an earlier candidate check rather than the final linked set), F2 the stale-docstring / full-file completeness (a docstring carrier not swept when the code below it changed). The #924 retro reinforces both close-out guards; no new gate (free-form internal prose).
