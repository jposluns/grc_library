# PR-Scoped Validation Record for PR #928

## Trigger and state snapshot

Post-merge PR-scoped validation sweep (`/validate-pr`) on PR #928, squash-merged to `main` as `7479db5145d212fa1ab1ba77c17df430fedf1fec`. Baseline: `tools/run_all_audits.sh` standalone = 69/69 (rc=0, no transient `tests/tmp` failure this run). README 2026.07.416 / 1.9.777. Read-only-git throughout (orchestrator on the concurrent `claude/todo-permanent-numbering` branch in the shared tree).

PR #928 shipped three follow-up fixes to the public grclibrary.ai static site (templates only; `.web/build.py` not touched): (1) landing hero CTAs expanded to five buttons (`#start`, `#build`, `/pack`, `#register`, `#standards`); (2) the six Get-started cards reworked so the `.k` keyword is the link and the `<h3>` is an unlinked statement, matching sections 01/03/04; (3) the pack page's wide-screen `.sidenav` clip (`max-height` / `overflow-y` / `position: sticky`) removed so all 13 rules + 23 skills render inline. Plus bookkeeping and the batched #927 zero-finding QA rows.

## Subagent A return

Subagent A (refute-briefed, read-only-git on `7479db5`) verdict **SHIP-WITH-NOTES**, 0 error / 0 warning / 1 note.

Build and audit integrity: `python3 .web/build.py --check` rc=0, 14 pages (index, about, pack, + 11 domain pages). `tools/run_all_audits.sh` standalone rc=0, all 69 gates. Rendered-output checks: hero exactly 5 CTAs, the four `#` anchor ids each exist once and `/pack` renders; Get-started 6 `.k` links + 0 linked `<h3>`; pack page 13 rule + 23 skill links all resolve to files on disk with zero `max-height` / `overflow-y` / `position: sticky` residual. Classes (f)/(g): `.web/build.py`, `taxonomy.yml`, `docs/portal.md`, `docs/maturity-scorecard.md` confirmed absent from the diff. Version surfaces agree (README 2026.07.416 / 1.9.777 = root CHANGELOG #928 header = detailed-mirror header); both versioned `.working` ledgers bumped with Date 2026-07-15.

**F1 (note) `meta-prose-count-imprecision`** in `.working/changelog-details/CHANGELOG-detailed.md:22`. The #928 detailed entry read "matching sections 01/03/04, all 18 feature cards consistent"; the page has 18 `<div class="feat">` cards but only 15 use the `.k`-link + unlinked-`<h3>` pattern (`#what` 3, `#start` 6, `#build` 6), while the 3 `#pack`-teaser cards deliberately carry no `.k` link. Internal, gate-exempt frozen record; not adopter-facing (the live site is correct and the root CHANGELOG makes no numeric claim). Recurring class (cf. #919 F1, #923 F1). In-window.

## Cross-reference check

The touched templates (`landing.html`, `pack.html`) are cited only by `.web/build.py` (which renders them) and by gate-exempt `.working` records; no corpus document cites them, so no citation broke.

## Orchestrator triage

F1 accepted as an accurate in-window note and **fixed in the batching PR (#929)**: `CHANGELOG-detailed.md:22` corrected to "15 of the 18 feature cards use this `.k`-link + unlinked-`<h3>` pattern, the 3 pack-teaser cards deliberately carry no `.k` link" (re-verified against the rendered output). No adopter surface affected. No hot-fix PR (batched per recursion-avoidance).

## Resulting hot-fix PR

None. The single note is fixed in the batching PR (#929, the permanent-numbering PR), per recursion-avoidance.

## Notes

Third in-session recurrence of the meta-prose-count-imprecision class (a count written from an earlier mental model rather than the final measured set). Already covered by the close-out "Meta-prose state-claim measurement" guard; no new gate proposed (free prose in a gate-exempt record). Reinforcement, not a new failure class.
