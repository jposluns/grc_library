# PR-scoped validation, PR #943 (2026-07-15)

## Trigger and state snapshot

- **PR**: #943, the Sweep 105 `/validate` close-out (resume from #942), squash-merged as `ff14421` on `main`.
- **Touched files**: `.web/build.py` (the A-1 fix), `README.md`, `CHANGELOG.md`, `TODO.md`, and `.working/` files (`changelog-details/CHANGELOG-detailed.md`, `next-prs.txt`, `session-handoff.md`, `session-state.md`, `validate-sweeps/2026-07-15-sweep105-iter1.md`, `validate-sweeps/history.md`). No corpus document body.
- **Baseline**: `tools/run_all_audits.sh` = 69/69 on the post-merge `main`. Versions: library `2026.07.431`, README `1.9.792`, spec `1.17.6`, gate 69.
- **Environment**: gh-CLI (no GitHub MCP), attended-autonomous on the VM. Orchestrator on `main`; Subagent A read-only-git.

## Subagent A return

One note-level finding; everything else adversarially confirmed clean.

```
ruleId: meta-prose-count-inconsistency
level: note
location: .working/validate-sweeps/2026-07-15-sweep105-iter1.md:48
classification: IN-WINDOW (the detail file is new in #943)
evidence: Line 9 says "11 candidates"; the "Verified clean" item (6) said "All 6
  pre-flight FP candidates confirmed false positives". 11 != 6: the "6" conflated the
  candidate COUNT (11) with the candidate-FILE count (6). Accuracy nit in a gate-exempt
  .working/ frozen sweep record; not adopter-facing, not gated, load-bearing conclusion
  (all candidates FP, 0 genuine pre-flight findings) unchanged.
```

Verified clean (with evidence): (1) the A-1 fix `.web/build.py` key `(TYPE_RANK.get(d["type"], len(TYPE_ORDER)), d["title"].lower(), d["path"])` is valid Python and **provably eliminates the divergence** (re-sorting each domain with the new key reproduces `taxonomy.yml`'s order in 0/12 domains diverging; the old case-sensitive key diverged in 6 domains, matching the finding); `d["path"]` has identical repo-relative posix semantics to `build-taxonomy.py`'s `rel` tiebreaker (line 96). The two generators are now consistent. (2) TYPE_ORDER byte-identical (17 types). (3) Version bumps correct (library and README each +1 once; README Date fresh; no corpus-document body touched). (4) CHANGELOG dual-entry parity, header consistent, no em/en dashes, detailed-mirror path refs are markdown links. (5) Handoff prune correct: exactly 2 State-snapshot + 2 asserted-expectations blocks; #901-#914 removed; the "#914" pointer neutralized. (6) Lease acquired (Status active, heartbeat later than #942's release). (7) Sweep-record fidelity: history row and detail file agree (0 error / 1 warning / 0 note, A-1 fixed, loop-break for #942 PASSES).

## Cross-reference check

`.web/build.py` and `tools/build-taxonomy.py` are the paired generators (TYPE_ORDER + per-domain sort key). Post-fix they are consistent (identical TYPE_ORDER; matching secondary key semantics). TODO §3.76 references both and was widened to cover the secondary key. No other citer of the touched files was affected (the `.working/` files are frozen records; README/CHANGELOG/TODO are self-contained).

## Orchestrator triage

- **N-1 (note, in-window, FIXED in #944):** the "All 6" -> "All 11 (across 6 files)" count conflation in the Sweep 105 detail file. Fixed in this PR (#944) per recursion-avoidance. This is the project-tracked "meta-prose state-claim measurement" class (a count stated next to an enumeration at a different granularity); the fix recounts and states both granularities.

No other findings. No hot-fix PR needed (the note bundles into #944).

## Resulting hot-fix PR (if any)

None. The single note is fixed in #944 (the pack-page card-links PR), which this record batches into.

## Notes

The A-1 fix's correctness was independently re-derived by Subagent A (0/12 domains diverging post-fix vs 6/12 pre-fix), a stronger confirmation than the sweep's own render spot-check. No corrections to the A-1 fix itself.
