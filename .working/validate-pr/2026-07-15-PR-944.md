# PR-scoped validation, PR #944 (2026-07-15)

## Trigger and state snapshot

- **PR**: #944, pack-page card links (§2.4 website) + batched #943 QA, squash-merged as `257ac16` on `main`.
- **Touched files**: `.web/templates/pack.html` (the substantive change), `README.md`, `CHANGELOG.md`, and `.working/` files (changelog-details, improvement-log, next-prs, validate-pr/2026-07-15-PR-943.md, validate-pr/history.md, validate-sweeps/2026-07-15-sweep105-iter1.md). No corpus document body.
- **Baseline**: `tools/run_all_audits.sh` = 69/69 post-merge. Versions: library `2026.07.432`, README `1.9.793`.
- **Environment**: gh-CLI (no GitHub MCP), attended-autonomous on the VM. Orchestrator on a concurrent feature branch; Subagent A read-only-git.

## Subagent A return

One note-level finding; everything else adversarially confirmed clean.

```
ruleId: next-prs-first-line-overlength
level: note
location: .working/next-prs.txt:1
classification: IN-WINDOW (line rewritten by #944)
evidence: First line is 150 characters, exceeding the CLAUDE.md ~120-char statusline
  budget; the console `next:` statusline truncates at ~120, dropping the 3rd queue item
  from the glance. Content correct (3 items, current queue, just-closed pack-card item
  dropped); defect is length only. Gate-exempt .working/ file, not adopter-facing.
```

Verified clean (with evidence): (1) all three pack `.m-k` kickers wrap the label in a balanced anchor (whole-file 82 `<a`/82 `</a>`); targets correct (repo / `docs/adopter-guide.md` / `dev-security/claude-rules`), all three exist at `257ac16`; the third card's pre-existing setup-generator link preserved; no other section disturbed; the "no CSS change needed" claim holds (`.mode .m-k` already brass). (2) Version bumps correct (README Library and Version each +1 once; no corpus body touched). (3) CHANGELOG dual-entry parity, headers consistent, 0 em/en dashes, detailed path refs are markdown links. (4) Batched #943 QA all present and consistent (history row + record + retro row; sweep105 count fix applied, consistent with line 9). (5) next-prs content correct (only the length flagged).

## Cross-reference check

The pack page links to `docs/adopter-guide.md` and `dev-security/claude-rules` (both exist); these are stable adopter-facing paths. No corpus document cites `.web/templates/pack.html` (it is a site template, not corpus content). No stale cross-reference introduced.

## Orchestrator triage

- **N-1 (note, in-window, FIXED in #945):** `next-prs.txt` line 1 overlength (150 > ~120). Fixed by the #945 next-prs rewrite (trimmed to <=120, 3 items preserved). Project-tracked as a 1st-occurrence observation in the #944 `/retro` row (behavioural watch, no gate).

No other findings; no hot-fix PR (the note bundles into #945).

## Resulting hot-fix PR (if any)

None. The single note is fixed in #945 (the landing "by domain" text fix PR), which this record batches into.

## Notes

Subagent A verified all three card-link targets exist in the repo tree at the merge commit, a stronger check than the orchestrator's pre-push render (which confirmed the links render, not that the targets resolve). No corrections to the card-link change itself.
