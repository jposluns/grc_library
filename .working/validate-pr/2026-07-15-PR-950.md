# PR-Scoped Validation Record: PR #950

**Version:** 1.0.0\
**Date:** 2026-07-15\
**License:** CC BY-SA 4.0

## Trigger and state snapshot

Post-merge PR-scoped sweep on PR #950 (squash merge `fa5d4ee`), the "License" (s)
site-wide spelling fix plus the `.web/build.py` docstring `(TODO 2.16)`->`(#941)`
provenance reword, batching the #949 QA rows. Session is gh-CLI (no GitHub MCP),
attended-autonomous on the VM. Mechanical baseline `tools/run_all_audits.sh` = 69/69,
`.web/build.py --check` EXIT=0. Subagent A dispatched refute-briefed, read-only-git on
the shared tree (orchestrator on feature branch `claude/website-sidebars-scrollspy`).

## Subagent A return

0 error / 0 warning / 1 low note.

- **N-1 `detailed-mirror/bare-path-code-span` (low, in-window).** Location:
  `.working/changelog-details/CHANGELOG-detailed.md`, the #950 "Changed" bullet
  ("**\"License\" (s) throughout..."). The entry links some touched paths
  (`LICENSE`, `tools/lint-language.py`, `.web/build.py`, `improvement-log.md`) but
  leaves three touched paths as BARE code-spans: `.web/templates/landing.html`,
  `.web/templates/for-ai.html`, `.web/templates/partials/footer.html`. This deviates
  from the change-tracking rule's "every touched file linked from the entry text" and
  from the entry's own mixed practice (measured: landing.html linked ~12x, footer.html
  ~3x, for-ai.html ~1x elsewhere in the mirror). Non-blocking: `.working/` is in
  `DEFAULT_EXEMPT_DIRS`, so the CHANGELOG link-coverage gate does not scan the mirror
  and `run_all_audits.sh` is 69/69 green. Cosmetic convention nicety.

## Cross-reference check

No corpus document body was touched (the diff is `.web/` templates + a `.web/build.py`
docstring + README metadata + CHANGELOG + `.working/` only), so there are no corpus
citers to re-resolve. The spelling fix's internal identifiers (`id="licence"`,
`href="#licence"`, `href="/#licence"`, `.licence-note`/`.licence-fine` classes) were
confirmed left unchanged and all still resolve (a half-rename would have orphaned an
anchor or a class); Subagent A verified 0 visible "Licence"(c) across all 15 rendered
pages, with the shared-footer fix propagating site-wide.

## Orchestrator triage

N-1 accepted as a real, low, in-window convention deviation. Disposition: FIX in the
next PR (#951, the item-2 sidebars PR), which already touches
`.working/changelog-details/CHANGELOG-detailed.md`, by wrapping the three bare paths in
markdown links. No hot-fix PR (batched per recursion-avoidance). No other finding.

## Resulting hot-fix PR

None. The single low finding is folded into #951's diff (the next PR).

## Notes

The finding is the recurring detailed-mirror link-convention class on a gate-exempt
surface; it is caught by the sweep, not by a gate, because the mirror is exempt. Folding
the fix into the next `.working` touch is the standing disposition for this class.
