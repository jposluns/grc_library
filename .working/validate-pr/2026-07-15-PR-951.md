# PR-Scoped Validation Record: PR #951

**Version:** 1.0.0\
**Date:** 2026-07-15\
**License:** CC BY-SA 4.0

## Trigger and state snapshot

Post-merge PR-scoped sweep on PR #951 (squash merge `1e3d47f` on `main`), website
batch item 2: contents sidebars on the For-AI, per-domain (11 pages), and Contributors
pages; the shared sidebar CSS moved into `head-style.html`; landing's inline sidebar
block removed; pack reduced to a non-sticky override; the scrollspy rewritten for
grid-row-peer highlighting. It also folded the #950 `/validate-pr` fix (three bare
`.web/templates/*` paths now linked) and batched #950's QA. Session gh-CLI (no MCP),
attended-autonomous on the VM. Mechanical baseline `tools/run_all_audits.sh` = 69/69;
`.web/build.py --check` EXIT=0. Subagent A dispatched refute-briefed, read-only-git.

## Subagent A return

0 error / 1 warning / 0 note.

- **W-1 `bookkeeping/stale-counter` (warning, in-window).** Location: `TODO.md:101`.
  The PR added `### 3.77 CHANGELOG link gates...` (`TODO.md:311`, verified unique) but
  left the Priority-3 header counter `**Next item number: 3.77.**` unchanged; it should
  read `3.78`. A paired-surface completeness miss: adding a numbered item must advance
  the section's `Next item number` counter, or the next new P3 item collides on 3.77.
  Numbering itself is still sound (sequence 3.74, 3.75, 3.77 with 3.76 rotated to DONE
  in #949, no reuse yet), so the defect is latent, not active.

Categories verified clean (with evidence): stale prose / cross-refs; bookkeeping-table
integrity (all improvement-log and history rows 8 pipes / 7 cells, no concatenation, so
the #949-row corruption caught pre-push in #951 did not ship, the `--amend` absorbed
it); multi-surface completeness (README CalVer 2026.07.439 + Version 1.9.800 each bumped
once, matching the #951 CHANGELOG header; register bumps present; 0 em/en dashes);
link hygiene (17/17 detailed-mirror link targets resolve at `../../` depth; the three
#950 bare paths now linked); TODO §3.77 well-formed and not recycled; generated-artefact
/ template correctness (`--check` EXIT=0, shared CSS once, landing inline block removed,
pack override wins by source order, every new sidebar anchor resolves on-page); no
`(TODO 2.16)` residual in the touched `.web/` files.

## Cross-reference check

No corpus document body was touched (diff is `.web/` templates + `.working/` records +
README + TODO + CHANGELOG), so no corpus citers to re-resolve. The touched `.web/`
templates are consumed only by `.web/build.py`, exercised by the `--check` above.

## Orchestrator triage

W-1 accepted as a real, in-window, low-severity paired-surface miss. Disposition: FIX in
the next PR (#952, website batch item 3), which touches `TODO.md`, by advancing the P3
counter to `3.78`. No hot-fix PR (batched per recursion-avoidance). No other finding.

## Resulting hot-fix PR

None. W-1 folded into #952's diff.

## Notes

W-1 is the "added a numbered item without advancing the section counter" shape, a
consequence of the permanent-numbering counter convention adopted 2026-07-15 (the counter
is a new paired surface). Recorded in the #951 retro for pattern-watch.
