# PR-Scoped Validation Record: PR #953

**Version:** 1.0.0\
**Date:** 2026-07-15\
**License:** CC BY-SA 4.0

## Trigger and state snapshot

Post-merge PR-scoped sweep on PR #953 (squash merge `b82475b` on `main`), website
batch item 4: per-type listing pages (17 `types/<slug>/index.html`) + clickable "By
document type" chips (Option A), with the shared document-list CSS DRY'd into the
head-style partial. Session gh-CLI (no MCP), attended-autonomous on the VM. Mechanical
baseline `tools/run_all_audits.sh` = 69/69; `.web/build.py --check` EXIT=0 (35 pages).
Subagent A dispatched refute-briefed, read-only-git.

## Subagent A return

0 error / 1 warning / 0 note.

- **W-1 `bookkeeping/session-state-prose-staleness` (warning, in-window).** Location:
  `.working/session-state.md:9` and `:11`. #953 re-stamped the lease heartbeat
  (`16:55:48Z`) and `Active-session: claude/website-per-type-pages` (the gate-63 shape
  fields, all fresh/correct), but did NOT refresh the descriptive Current-task /
  Worker-dispatches prose: the Current-task line still read `This PR (#952) is batch
  item 3` and listed merges only through #951, and Worker-dispatches still described
  #952's verifier dispatch. The CLAUDE.md close-out checklist requires the lease's
  Current-task / Worker-dispatches lines to be updated if stale; they were two states
  behind. Non-gated (gate 63 checks only the shape fields), so the audit is green;
  impact is `/resume` narrative accuracy only. An in-prose branch-name reference
  (`claude/website-sidebars-scrollspy`) was already stale before #953.

Categories confirmed clean (with evidence): the feature build (`--check` EXIT=0, 35
pages); the 17 chip links each resolving to a generated page; per-type `<li>` counts
summing to 312 with no `>root<` leak (specification page shows `library`); the CSS DRY
move (head-style carries the 4 `.doc-*` defs, domain.html carries 0, no duplication,
domain pages still styled); multi-surface completeness (README CalVer 2026.07.441 +
Version 1.9.802 each bumped once, matching the #953 header; register bumps present; 0
em/en dashes); link hygiene (all 6 detailed-mirror path spans link and resolve at
`../../`, globs/`sitemap.xml` correctly left as spans); bookkeeping-table integrity
(rows #952/#951/#950 well-formed); the audit suite 69/69; and the #952-retro PR-title
fix confirmed working (#953 subject carries a single `(#953)`).

## Cross-reference check

No corpus document body touched (diff is `.web/` + `.working/` + README + CHANGELOG),
so no corpus citers to re-resolve. The touched `.web/` templates are consumed only by
`.web/build.py`, exercised by the `--check` above.

## Orchestrator triage

W-1 accepted as a real, in-window, warning-tier bookkeeping-staleness finding.
Disposition: FIX in the session-closing handoff PR (#954), which fully refreshes
`.working/session-state.md` (Current-task, Worker-dispatches, in-prose branch name) as
part of its close-out. No hot-fix PR (batched). No other finding.

## Resulting hot-fix PR

None. W-1 folded into #954 (the session-closing handoff).

## Notes

W-1 is a recurrence-adjacent instance of the session-state refresh discipline: a PR that
re-stamps the lease shape fields must also update the Current-task / Worker-dispatches
prose when it has gone stale. Recorded in the #953 retro for pattern-watch.
