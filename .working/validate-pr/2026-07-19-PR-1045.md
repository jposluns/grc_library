# /validate-pr record: PR #1045 (self-guard bundle)

## Trigger and state snapshot

- PR: #1045, "Self-guard bundle: intent-vs-action hooks + evidence-validated degradation-watch".
- Merge commit: `053ca4016cbc481b69a33c2b8e0e31bc436ed4db` (squash), base `86ecebe5` (#1044).
- Touched files (22): the 2 new PreToolUse hooks (`.claude/hooks/block-repeated-tool-failure.py`, `.claude/hooks/_hook_state.py`), 3 sibling-hook `record_block` edits (`block-wrong-repo-tool.py`, `block-verification-pipes.py`, `block-answered-question.py`), `.claude/settings.json`, `.gitignore`, `.claude/CLAUDE.md` (new `## Self-verification` section), `.claude/commands/resume.md`, `tests/test_linters.py`, `tools/audit-brief-freshness.py`, `tools/audit-delivery-status.py`, plus bookkeeping (`README.md`, `CHANGELOG.md`, `.working/changelog-details/CHANGELOG-detailed.md`, `.working/DONE.md`, `TODO.md`, `.working/next-prs.txt`, `.working/session-handoff.md`, `.working/session-state.md`, `.working/validate-pr/history.md`, `.working/improvement-log.md`).
- Mechanical baseline (post-merge `main`): `tools/run_all_audits.sh` = **72/72 OK**.
- Mode: attended-autonomous, VM (gh CLI, no GitHub MCP).

## Subagent A return

VERDICT: 2 findings (1 warning, 1 note); hook logic + wiring CLEAN; self-test 12/12 RC 0.

- **F1** (warning, class-d INFERRED-AS-VERIFIED / measured-not-inferred): `.working/changelog-details/CHANGELOG-detailed.md:14` and `:27` said "4 hook self-tests pass" while the shipped hook `--self-test` has 12 cases (`Ran 12 tests`); L27 self-contradicts within one sentence ("4 pass ... now 12 cases"). Gate-exempt `.working/` file, no corpus/behaviour impact.
- **F2** (note, class-a STALE PROSE REFERENCE / moved-section residual, out of Subagent A primary scope, handed to the cross-ref step): `TODO.md:345` (§3.80, "APPLIED 2026-07-16" phase line) says the `## Credit-offload mode` directive was "wired into ... `.claude/CLAUDE.md`", but #1044 moved that section to `orchestrator-claude.md` A1; bears on the #1045 record's "0 stale pointers remain" claim.

Verified CLEAN (no finding): the 3 repointed targets (A1/A2/A3) correct against `orchestrator-claude.md`; touched tool files clean of any `CLAUDE.md` pointer; multi-surface wiring complete (settings-first on both matchers, all 3 siblings `record_block` with byte-identical subject, `.gitignore` excludes the state dir, fixture wires the self-test, CLAUDE.md references the real filename); hook logic sound (fail-open every path, GUARD 1 gates on a genuine repeat, GUARD 2 escalates only an already-triggered block, steer branches per hook class); version bumps correct + paired (library `2026.07.533` / README `1.9.894`); no generated-artefact lag; threshold phrasing (class-h) consistent; docstrings match code.

## Cross-reference check

Orchestrator-run (read-only). The new hook `block-repeated-tool-failure.py` is cited only where expected (`tests/test_linters.py`, `.claude/settings.json`, `.claude/CLAUDE.md`, the two hook files, `CHANGELOG-detailed.md`); both new hook files resolve; `settings.json` wires the new hook FIRST on both the Bash and AskUserQuestion matchers, `Edit|Write` unchanged; `.gitignore` excludes `.claude/hooks/.state/` (confirmed by `git check-ignore`). No stale citer, all wiring resolves. The cross-ref step adjudicated Subagent A's F2 as a genuine (out-of-window) moved-section residual and routed it to fix.

## Orchestrator triage

- **F1: in-window.** The #1045 record's own inaccuracy. Fixed in-window: L14 and L27 reworded to "the new hook's `--self-test` passes (12 cases)"; the redundant trailing "the hook self-test is now 12 cases" dropped. Both fixes batch into the next PR.
- **F2: out-of-window (#1044-escape) but corrected in-window because it falsifies #1045's own completion claim.** Fixed: `TODO.md` §3.80 repointed to `orchestrator-claude.md` A1 (relocated from `.claude/CLAUDE.md` by #1044); the #1045 detailed-CHANGELOG "0 stale pointers remain" claim honestly softened to record that `/validate-pr` found one further residual, corrected in the next PR. Both fixes batch into the next PR.
- Both findings are gate-exempt prose (`.working/` and forward-looking `TODO.md`); no corpus content, no behaviour, no gate impact. Severity low (warning + note).

## Resulting hot-fix PR

No dedicated hot-fix PR (recursion-avoidance). The two fixes (CHANGELOG-detailed L14/L27/L21, TODO §3.80) plus this record file and the history row batch into the next substantive PR, whose number is filled into the history row's Hot-fix cell at that time.

## Notes

- The guard machinery had already been cleared pre-merge by an independent adversarial verifier (returned CLEAN with one MEDIUM message-coherence finding, GUARD 1's steer hardcoded to the wrong-repo class, fixed in-window before merge and locked by `test_steer_is_class_specific`). Subagent A independently re-confirmed the logic clean and re-ran the self-test (12/12 RC 0).
- F1 is the same self-test count-drift class as #1041 (the "10 vs 12" catch): a count stated in the record prose drifted from the shipped artefact. The lesson is already codified (measured-not-inferred at bookkeeping-authoring time); this occurrence is the recurrence being caught by the post-merge sweep, not a new class.
- No worker source (self-run); no `MAINTAINER_ALERT` raised.
