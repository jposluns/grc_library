# `/validate-pr` sweep record: PR #1158

**Date:** 2026-07-25
**PR:** #1158 (squash `6fea1456`)
**Sweep worker:** `opus-20260725T040036Z-d268` (file-drop transport)

**NOT INDEPENDENT of the gate diff or the pre-push verdict, and SCOPED accordingly.** This worker
authored #1158's gate candidate AND served its pre-push verify, so the order asked it for the two things
no party had examined: the fixes made in RESPONSE to its pre-push findings (which postdate its read), and
a corpus-wide drift hunt across the whole eight-PR overnight run #1151 to #1158. That width was the run's
largest remaining blind spot, since every sweep this run was PR-scoped and most were non-independent.
The drift hunt came back CLEAN on every axis. Its one finding is recorded and fixed in #1159.

---

# Post-merge `/validate-pr` sweep: PR #1158, plus a corpus-wide drift hunt over #1151 to #1158

- **Order:** `validate-pr-1158` (kind `qa`, command `validate-pr`, priority 0)
- **Worker:** `opus-20260725T040036Z-d268` (family `opus`, model `claude-opus-5[1m]`)
- **Subject:** squash merge `6fea1456` on `main`, base `fcec0316`, both reach-checked `commit`.
- **Scoping honoured:** I authored the gate diff this PR applied AND served its pre-push verify, so I
  re-blessed neither. Area 1 is the two fixes made in RESPONSE to my findings (new text I had not
  seen); area 2 is the corpus-wide state, which no party has examined this run.

## RESULT: PASS WITH ONE FINDING

**Both fixes to my pre-push findings verify.** The corpus-wide drift hunt across eight merged PRs is
**clean on every axis I could test**. The one finding is the pattern the order asked me to look for:
the over-scoped bold headline **repeats in a different headline**, and this time it carries a factual
error as well.

| # | Finding | Severity |
| --- | --- | --- |
| **F1** | "The eleventh and last fail-open route": the ordinal contradicts the module docstring shipped in the SAME PR (which says twelfth) and its own arithmetic; and "last" over-claims, since four routes I reported remain open and are tracked nowhere | warning |

---

## 1. Area 1: both fixes to my pre-push findings are correct

**My pre-push F1 (the over-scoped headline) is fixed accurately and completely.** It now reads:

> **No specification edit for gate 59** (gate 44's narrative IS edited, below).

That is both accurate (gate 59 genuinely needs no spec edit, which I verified) and complete: it names
the gate-44 edit and points at it, so the scannable layer no longer misleads.

**My pre-push F2 (the concealed link/image asymmetry) is fixed, on the third attempt.** The order notes
this clause has been rewritten twice and that a third error would be a pattern worth naming. It is not
a third error. The clause now reads:

> a link's visible TEXT is prose and does count, whereas an image is stripped whole including its alt
> text

Verified item by item against the code's eight strips (`iter_non_code_lines`, `HTML_COMMENT_RE`,
`REF_DEFINITION_RE`, `IMAGE_RE`, `LINK_TARGET_RE`, `REF_LINK_LABEL_RE`, `HTML_TAG_RE`, `URL_RE`):
every stripped source is named, nothing is named that is not stripped, and **the asymmetry that both
earlier versions concealed is now explicit**. `git grep` for either superseded phrasing returns **0**,
so no other surface carries an old enumeration.

### F1 (warning). The pattern repeats, and this instance is also factually wrong

The order asked me to check whether any OTHER bold headline over-scopes its supporting sentence the way
the one I found did. **One does**, and it fails in two separate ways.

> **The eleventh and last fail-open route**, after the five closed in #1154 and the six in #1155.

**(a) The ordinal contradicts its own arithmetic and the same PR's code.** Five plus six is eleven
already closed, which makes this one the TWELFTH. And the module docstring this PR ships says exactly
that, at `tools/lint-paired-skill-step-parity.py:267`:

```text
The reference-LABEL strip closes the twelfth such route (2026-07-25): the
eleven closed in #1154 and #1155 left `[visible text][label]` intact ...
```

So the CHANGELOG says eleventh and the code says twelfth, in one commit.

**(b) "last" over-claims.** My `validate-pr-1155` sweep reported four further non-prose routes that
remain OPEN: YAML front matter, a table cell of pure metadata, a link target containing `)`, and an
HTML attribute value after a `>`. I also recorded there that the base rate across rounds was 2, then 5,
then 1, and that the class should not be assumed exhausted. **None of those four is recorded in any
TODO item:** `git grep` of `TODO.md` for `front matter` and `table cell` returns **0**. The entry's only
mention of front matter is inside its #1155 section, discussing a different point.

**Failure scenario.** Two distinct harms. A future maintainer reads "the last fail-open route",
concludes the class is closed, and does not look again, while four known routes sit unrecorded in any
backlog. Separately, anyone reconciling the count between the CHANGELOG and the tool's own docstring
finds them one apart and has to determine which is right.

**Fix:** "The twelfth fail-open route, and the last of the reference-form routes" plus a TODO item
recording the four that remain. The ordinal correction is one word; the "last" correction is the
substantive half, because it is the difference between a closed class and an open one.

**Why I am confident this is not me mis-remembering my own findings:** the four routes are quoted with
their constructions in my `validate-pr-1155` delivery, each verified by running the gate on a
constructed case at that SHA.

---

## 2. Area 2: the corpus-wide drift hunt, clean on every axis

Run across the whole corpus at `6fea1456`, not limited to the diff.

**Bare-token residuals of everything corrected this run: none.**

| Hunted | Result |
| --- | --- |
| The fabricated SP 800-208 titles in a title slot | **none** outside `.working/` narration of the fix |
| The never-shipped `2026.07.641` | 11 occurrences, **all narration**: the DONE record, the changelog-detail describing the correction, `pending-decisions`, my own `/validate-pr` record, a test comment explaining its fixture, and the gate's own docstring citing the evidence. No live value. |
| Superseded gate-44 strip enumerations | **0** |
| Closed TODO 1.24, 1.25, 3.112 still cited as open | **none** in `tools/`, `tests/`, `.claude/`, `governance/`, `docs/` |

**Counts, re-derived from the repo rather than read from prose:**

```text
gates (§6 inventory rows)   77        governance rules   15
pack skills                 24        slash commands     15
Document types (distinct)   17        taxonomy rows      314
```

Gate 39 confirms the cross-file claims: `OK: collection-count references consistent across 557 files
(gates 77, governance rules 15, skills 24; digit and word-form)`. The 24-skills-versus-15-commands gap
is expected, not drift: only 13 skills carry a paired command in the gate-44 `PAIRS` registry.

**Four-surface gate wiring: in parity.** My first crude count read workflow 73, runner 77, pre-commit
73, spec 77, which looked like drift. **It is not**, and I checked before reporting it: exactly four
runner gates have names not ending in "audit" (CHANGELOG file-reference link coverage, taxonomy in
sync, portal and scorecard in sync, linter regression test suite), which accounts for 77 minus 73
precisely, and pre-commit carries the same set under non-`lint-` ids. The mechanical enforcers agree:
gate 77 (gate-name-citation inventory) and gate 39 both pass.

**Dual-tree and generated-artefact sync: clean.**

```text
gate 37 dual-tree     OK: 19 local rule copies are in sync with their pack sources; every local
                          rule file is mapped.
taxonomy --check      OK: taxonomy.yml is in sync.
portal --check        OK: portal and scorecard in sync.
```

**Stale forward pointers in the gate-exempt trees: none found.** The `.claude/` subtree and tool
docstrings are outside the corpus gates, so only a grep finds drift there; the closed-item sweep above
covered them explicitly and returned nothing.

### Mechanical baseline, re-derived unpiped

```text
$ bash tools/run_all_audits.sh                 # exit 1
8 of 77 audit gate(s) failed:                  ... terminal line: "  - Session-state lease audit"

$ python3 tools/run-linter-regression.py       # exit 1
Ran 488 tests in 21.97s
FAILED (failures=6)
```

**488 is the expected count and confirms my own fixtures merged**: 483 at the parent plus the five I
delivered in `draft-gate59-gate44-parity-widening`. The 8 gate failures and 6 test failures are the
standing environmental set in a `git archive` extract (absent `.working/` registers, no git directory),
identical at every commit I have swept this run. **No gate regressed.** No `tests/tmp/*` transient and
no gate-50 flag, so neither concurrent-run artefact arose.

---

## 3. What this drift hunt does NOT cover, stated plainly

The order calls this the widest remaining blind spot, so the residual blind spots matter:

- **Four gates remain environmentally unrunnable** in my read basis, and they are the bookkeeping ones
  (repository-internal link, TODO staleness, bookkeeping parity, CHANGELOG mirror header parity).
  Three of my findings this run came from checking those by hand precisely because the gates could not
  run. The orchestrator must run the runner in a real checkout.
- **I did not re-verify content correctness of the eight PRs' corpus edits**, only drift and
  consistency. The citation values in #1156, for instance, were verified in that PR's own sweep.
- **Non-independence across the run is unchanged by this sweep.** I served a large share of this run's
  QA, so a systematic error in my own reading would survive every pass I ran, including this one. That
  is the argument for the third worker recorded in `MAINTAINER_ALERT.md` 2026-07-24-a, not something
  this delivery can fix.

## 4. Proof of run

Read-only on the shared checkouts: `cat-file`, `show`, `grep <sha>`, `diff`, `archive`. **No
`checkout`, `switch`, `reset`, or `stash`.** All execution in my own extracted copy.

```text
git -C /home/grc/grc_library cat-file -t 6fea1456 / fcec0316
git -C /home/grc/grc_library show 6fea1456:<changelogs, spec, TODO, both gate tools>
git -C /home/grc/grc_library grep -n <each residual token> 6fea1456 -- .
git -C /home/grc/grc_library diff --stat fcec0316 -- governance/specification-audit-programme.md
git -C /home/grc/grc_library archive 6fea1456 | tar -x -C <mine>

python3 tools/lint-gate-count-consistency.py       # gate 39, OK
python3 tools/build-taxonomy.py --check            # OK
python3 tools/build-portal.py --check              # OK
python3 tools/lint-claude-rules-sync.py            # gate 37, 19 copies OK
bash tools/run_all_audits.sh                       # unpiped, exit 1, 8 of 77 environmental
python3 tools/run-linter-regression.py             # 488, exit 1
<count re-derivation across five collections>
<four-surface wiring count, then its explanation before reporting>
```

**Method note.** F1 came from checking a headline's arithmetic against the code shipped beside it, which
is the third time this run that comparing a claim to an adjacent artefact found something a
plausibility read would not. The four-surface count is the counterexample worth recording: it LOOKED
like drift, and the discipline that mattered was explaining the gap before reporting it rather than
after.

## 5. Caveats

- **Nothing was fixed.** F1 is for the orchestrator: one word for the ordinal, and a TODO item for the
  four open routes, which is the part that matters.
- **My independence is partial and the order scoped around it correctly.** Area 1 verifies fixes to my
  own findings, which is the normal loop for a finding's author; it is not independent confirmation
  that the findings were right.
- **F1's "last" half rests on my own earlier delivery.** The four routes are quoted there with their
  constructions, but a third party re-deriving them would make the case stronger than my citing myself.

## 6. Token spend

Approximately 121,000 input tokens and approximately 18,000 output tokens for this order.
Best-effort estimate; the harness exposes no exact per-order counter. Dominated by the corpus-wide
residual greps, the five-collection count re-derivation, four gate and generator runs plus two baseline
runs, and the headline-versus-docstring reconciliation.

Session total across the eighteen delivered orders plus one NOT-RUN notice: roughly 2,467,000 input and
381,000 output tokens. Below the checkout threshold, so I return to the poll loop.
