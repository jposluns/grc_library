# `/validate-pr` sweep record: PR #1166

**Date:** 2026-07-25
**Worker:** `opus-20260725T122030Z-6443`

Offloaded formal pass. Both #1166 and #1167 had their history rows written INLINE by the orchestrator to
break the push-chain recursion; this is the real sweep, and its findings were acted on rather than filed.

---

# Result: validate-pr-1166

- **order:** validate-pr-1166 (kind qa, command validate-pr, priority 0)
- **worker:** opus-20260725T122030Z-6443 (family opus, model claude-opus-5[1m], file-drop transport)
- **grc_library_sha:** 6118ff4d (reach-checked; `#1166: act on the codex deep assessment (M-04 bypass
  logging, M-06 codex contract discovery)`, merged 2026-07-25 08:52:24 -0400)
- **grc_library_ref_sha:** 4a0f2978 (reach-checked, unread; this PR touches no reference-bearing content)
- **independence:** I did not author, review, or advise on #1166, and I read it for the first time in
  this order. I am not the worker whose delivery it acts on.
- **read basis:** an isolated `git clone --shared --no-checkout` of `/home/grc/grc_library` in my own
  scratchpad, checked out at `6118ff4d`. No `checkout`, `switch`, `reset`, `stash`, `fetch`, or
  `worktree` was run against a shared checkout.

## VERDICT: PASS WITH THREE FINDINGS (1 warning, 2 notes). Nothing must-fix-before-merge.

Both mechanical baselines are clean and every one of the order's four weighted axes was exercised.
The PR's central discipline claim, that the merge-bypass backfill is honestly labelled retrospective
rather than presented as contemporaneous, **holds and is unusually well done**. The three findings
are all in the PR's own prose about its own instruments, and the strongest of them is exactly the
class axis (a) was written to catch.

### Findings, SARIF-lite

```text
### Finding F-1: audit-delivery-status.py is mis-described as reading an OUTBOX
- level: warning
- ruleId: claim-vs-code/tool-behaviour-misdescribed
- location: TODO.md:251
- location: .working/changelog-details/CHANGELOG-detailed.md:43
- fingerprint: claim-vs-code/tool-behaviour-misdescribed:todo.md:251
- rubric: R (real), in-window (introduced by this PR)
```

Both places state that [`tools/audit-delivery-status.py`](tools/audit-delivery-status.py)
"reconciles worker **OUTBOX** deliveries". Checked against the tool's CODE, not its docstring, as
axis (a) requires:

```text
tools/audit-delivery-status.py:168      inbox = scratch / "inbox"
tools/audit-delivery-status.py:169      for manifest in sorted(inbox.glob("*/*/MANIFEST.md")):

$ grep -n -i "outbox" tools/audit-delivery-status.py
rc=1        <- the word does not occur anywhere in the tool
```

The tool reads an **inbox**, specifically the `grc_library_scratch` repository's
`inbox/<worker-id>/<work-unit>/MANIFEST.md` tree. Its docstring says so too (`:2`, "worker
deliveries in the scratch inbox"; `:26`, "For every ``inbox/<worker-id>/<work-unit>/`` directory"),
so this is not the aspirational-docstring case: the prose diverges from BOTH the code and the
docstring. The tool that does read an outbox is a different one,
[`tools/audit-worker-saturation.py`](tools/audit-worker-saturation.py), the only file under `tools/`
in which the word appears.

**The conclusion drawn survives; the reasoning stated for it does not.** TODO 3.117's point, that the
tool would not see a maintainer-placed drop, is TRUE, but for a different and sharper reason than the
one given: the tool reads the GIT-SCRATCH repository's inbox tree, which is a different exchange
plane from the file-drop root `/home/grc/grc_working` where the drop actually sat. So the accurate
sentence is stronger than the written one, since it explains that the tool would miss the drop even
though the drop WAS in an inbox.

**Why this is worth a warning rather than a note.** It is not a slip in a closed record: `TODO.md:251`
is a LIVE backlog item that a future session will implement from, and it points that session at the
wrong tree. This project's improvement log tracks the read-before-characterizing class at multiple
occurrences (`.working/improvement-log.md:285`, the #918 "characterized a table cell without reading
the actual cell" instance), and this is the same shape applied to a tool.

**Proposed correction**, both locations: replace "reconciles worker OUTBOX deliveries against the
backlog" with "reconciles worker deliveries in the `grc_library_scratch` INBOX tree against the
backlog, a different exchange plane from the file-drop root".

```text
### Finding F-2: the merge-bypass log claims unbounded scope but backfills only this run
- level: note
- ruleId: record-accuracy/scope-not-stated
- location: .working/merge-bypass-log.md:7
- fingerprint: record-accuracy/scope-not-stated:.working/merge-bypass-log.md:7
- rubric: R (real), in-window
```

Line 7 states the file's scope with no time bound: "Every merge that used the maintainer's
always-bypass path on a protected branch, one row each." The 15 backfilled rows cover **#1151 to
#1165 only**, all dated 2026-07-25. The "Backfill note, stated plainly" section says the rows were
entered retrospectively but never says the backfill is scoped to this run rather than to all history:

```text
$ grep -n -i "this run\|overnight\|1147\|1148\|1149\|1150\|earlier merge" .working/merge-bypass-log.md
rc=1        <- no scoping sentence anywhere in the file
```

The scoping DOES appear elsewhere ("on all 16 merges of this run",
`.working/changelog-details/CHANGELOG-detailed.md:19`), so the run boundary was known and simply did
not reach the log. I verified the count is internally consistent: `git log` shows #1151 as the first
merge dated 2026-07-25 (00:58) after #1150 closed the prior session at 2026-07-24 22:40, and 15 rows
plus the deferred #1166 row is exactly 16.

The ambiguity is not theoretical. `--admin` was a known and available path well before this run:
`.working/validate-pr/history.md:110` records a 2026-07-22 session winding down because
`gh pr merge --admin` was blocked, and `.working/improvement-log.md:126` and `:136` both discuss
declining to `--admin`-override. So a future auditor reading the unbounded scope line will read 15
rows as the complete bypass history, which nobody has claimed.

**Proposed correction:** one sentence in the backfill note stating that the backfill covers the
2026-07-25 run (#1151 onward) and what is known, or not known, about earlier merges.

```text
### Finding F-3: a factual claim about 15 GitHub PRs, in the sentence arguing the backfill is not asserted
- level: note
- ruleId: evidence-provenance/unsourced-outcome-claim
- location: .working/merge-bypass-log.md:36
- location: .working/changelog-details/CHANGELOG-detailed.md:36
- fingerprint: evidence-provenance/unsourced-outcome-claim:.working/merge-bypass-log.md:36
- rubric: I (inferred), in-window
```

The backfill note reads: "Every row's CI state is **recoverable** from the PR's own check history, so
the backfill is verifiable rather than asserted, and **every one of the 15 was green on all three
checks before merge**." The first half is carefully worded (recoverable, verifiable). The second half
is a statement of outcome about 15 GitHub pull requests, and nothing in the repository records that
the recovery was performed.

The rows' own evidence does not establish it either: every Justification cell says "pre-push guard
green (both runners, 77 gates plus D1-D8, unpiped)", which is LOCAL guard greenness, a different
thing from the three named GitHub checks (`Lint markdown corpus`, `Web generator health`,
`Cloudflare Pages`). So the row-level evidence supports a claim the sentence does not make, and the
sentence makes a claim the row-level evidence does not support.

This is axis (c) at its mildest: the prose does not say "I verified", it states the result. I rate it
a note rather than error because the claim is plausible and the surrounding wording is unusually
scrupulous, which is also why the gap stands out.

**Proposed correction:** either state how the 15 were established green (for example "confirmed via
`gh pr checks` on each"), or scope the sentence to what the rows actually record.

## The four weighted axes, one by one

### (a) CHANGELOG claims about what a tool does, checked against CODE

Enumerated every tool-behaviour claim in the PR's added prose and checked each against the tool
source at `6118ff4d`.

| Claim (location) | Verdict |
| --- | --- |
| `audit-delivery-status.py` "reconciles worker OUTBOX deliveries" (`TODO.md:251`, `CHANGELOG-detailed.md:43`) | **WRONG. F-1.** It reads `scratch/inbox/*/*/MANIFEST.md`; `grep -i outbox` over the tool returns rc 1. |
| "the file-drop root is outside every repository, so no audit gate walks it" (`TODO.md:251`) | TRUE. `/home/grc/grc_working` is not inside any of the four repositories, and no gate in `run_all_audits.sh` references it. |
| "the orchestrator's own sense of what needs processing is built from the order queue" (`TODO.md:251`) | Not a tool claim; a description of the assistant's own practice. Not checkable from code, and not asserted as if it were. |
| `Main Protection` config enumeration, "PR required, one approval, stale-review dismissal, thread resolution, strict `Lint markdown corpus`, signatures, deletion prevention, non-fast-forward prevention" (`merge-bypass-log.md:11-13`) | Attributed to the codex assessment's API read, not claimed as self-verified. Correctly attributed. Not independently checkable here, see (d) below. |

### (b) New-tool self-tests, mutated to confirm they fail

**NOT APPLICABLE, and this is measured rather than assumed.** #1166 touches no executable file:

```text
$ git diff --name-only HEAD^ HEAD | grep -E "^tools/|^tests/|\.py$"
rc=1        <- no tool, no test, no Python file in the diff
```

The full diff is ten files, all Markdown: `.claude/CLAUDE.md`,
`.working/alert-and-assessment-triage.md` (new), `.working/changelog-details/CHANGELOG-detailed.md`,
`.working/improvement-log.md`, `.working/merge-bypass-log.md` (new), `.working/session-state.md`,
`.working/validate-pr/history.md`, `CHANGELOG.md`, `README.md`, `TODO.md`. So there is no new
self-test to mutate. I ran the existing regression suite anyway as the baseline (below), and it is
unchanged at 489 tests.

### (c) Prose asserting a verification that was not performed

I scanned every added line for verification vocabulary (`verified`, `confirmed`, `checked`,
`re-derived`, `measured`, `validated`, `recoverable`, `verifiable`) and assessed each hit.

- **One finding, F-3**, the "all 15 were green on all three checks" outcome claim.
- **Everything else is correctly attributed.** "Codex confirmed `Main Protection` is active..."
  (`alert-and-assessment-triage.md:62`) attributes the confirmation to codex rather than claiming it;
  `CHANGELOG-detailed.md:150` says codex "noted carefully that this was not a claim that a bypass
  occurred" and then states separately that it HAD occurred, which is the assistant reporting its own
  conduct, the one thing it can assert first-hand.
- **A point in the PR's favour worth recording:** `CHANGELOG-detailed.md` explicitly credits codex's
  M-06 explanation as superseding the assistant's own banked hypothesis ("codex's explanation is
  better and is the one acted on"). Recording that your own prior diagnosis was wrong, by name, is
  the opposite of the failure axis (c) hunts.

### (d) The two #1166-specific questions

**(d1) Is the merge-bypass backfill honestly labelled retrospective? YES, and exemplarily so.**

The file carries a dedicated section headed "## Backfill note, stated plainly" which states that the
15 rows "were entered RETROSPECTIVELY on 2026-07-25", that "They were not logged at merge time,
because no log existed", and why. Nothing in the table or its headers presents the rows as
contemporaneous: there is no "logged at" column, and the `Date (UTC)` column is the merge date, which
is what it says it is.

The strongest evidence that the labelling is deliberate rather than incidental is the treatment of
#1166 itself:

> **#1166, the change that creates this log, is not yet a row here.** ... a row asserting a completed
> merge cannot honestly be written before the merge completes, so its row is added in the following
> change. ... A log that pre-records its own entries would be exactly the kind of artefact this
> project treats as fabricated rather than evidential.

I verified this is true of the artefact, not just of the prose: `grep` for `#1166` in the row table
returns nothing, and the row count is 15 (#1151 through #1165 inclusive, no gaps, no duplicates).
**This axis passes cleanly.** F-2 and F-3 are about the log's SCOPE and one sourcing sentence, not
about its retrospective labelling, which is the thing the order asked about.

**(d2) Is the corrected PR-workflow step TRUE against the live branch protection? I CANNOT VERIFY IT,
and I will not imply otherwise.**

Establishing it requires querying GitHub's branch-protection API for `main`. I hold no GitHub
credential, this order is `egress_needs: none`, and the safety rails forbid a write credential
entirely. So the truth of "a plain merge attempt does NOT resolve `mergeable_state: blocked`" against
the CURRENT live config is outside what I can establish.

What I can report is the internal evidence, which is consistent:

- The corrected text and the log agree with each other and with the codex finding on the mechanism
  (`REVIEW_REQUIRED`, one required approval, the assistant cannot approve its own PR).
- The correction is in the safe direction: the old text asserted that the merge attempt resolves the
  block, and the new text asserts that it does not and names the bypass. If the live config later
  changes so that a plain merge succeeds, the new text is conservative rather than wrong, and it
  already says "If a future protection change makes a plain merge succeed, prefer it and stop
  bypassing."
- Fifteen recorded merges all reporting `REVIEW_REQUIRED` is consistent behavioural evidence, though
  it is the assistant's own report of its own runs rather than an independent read of the config.

**Recommendation:** the orchestrator should re-confirm the protection config directly at apply time
of any follow-up, since the whole correction rests on it, and record where that confirmation came
from. That is the one load-bearing fact in this PR that no offloaded worker can check.

## Mechanical baseline, independently re-derived

Both run standalone and UNPIPED in my own clone at `6118ff4d`, terminal lines quoted.

```text
$ bash tools/run_all_audits.sh
All 77 audit gates passed.
run_all_audits rc=0

$ python3 tools/run-linter-regression.py
Ran 489 tests in 28.624s
OK
run-linter-regression rc=0
```

**One correction to my own first run, disclosed because it changes how the number should be read.**
My first `run_all_audits.sh` reported `1 of 77 audit gate(s) failed`, on
`lint-library-version-monotonicity.py`: `Library Version decreased: current 2026.7.653 < prior
2026.7.654`. **That was an artefact of my read basis, not a defect in #1166**, and I diagnosed it
rather than reporting it. The gate's `find_prior_ref` (`:93-116`) prefers `origin/main` over `HEAD~1`,
and in my clone `origin/main` pointed at `22dd3aa6` (#1167, Library Version `2026.07.654`), which is
LATER than the pinned SHA. Against the true pre-merge state it passes:

```text
$ git show HEAD~1:README.md > prior-README.md      # #1165, Library Version 2026.07.652
$ python3 tools/lint-library-version-monotonicity.py --prior-readme prior-README.md
OK: library version is monotonic non-decreasing (vs supplied prior).
  Library: current 2026.7.653 >= prior 2026.7.652
rc=0
```

I then removed `origin` from my own clone so `find_prior_ref` falls back to `HEAD~1`, and re-ran the
full suite for the 77/77 quoted above. This is the same concurrent-run artefact class the order
anticipates for gate 50, arriving through a different gate. **It is worth recording as a standing
hazard for offloaded post-merge sweeps:** any worker that clones a shared checkout whose `main` has
advanced past the pinned SHA will see this gate fail spuriously, and a less careful sweep would
report it as a #1166 defect.

## Additional checks beyond the order's list

| Check | Result |
| --- | --- |
| Gate 50 (bookkeeping parity) alone | `OK: bookkeeping-parity audit clean (QA-cadence parity from PR #329; TODO/DONE rotation; version-history parity; worker-provenance attestation; deep-assessment register row-order).` rc=0 |
| The eight PR-time delta gates, run against `HEAD^..HEAD` | **All eight rc=0**: changelog-on-pr, changelog-length-on-pr, changelog-dash-on-pr, version-bump-on-pr, date-cobump-on-pr, todo-rotation-on-pr, handoff-snapshot-on-pr (not triggered), pack-readme-cobump-on-pr (not triggered). |
| D8 ceiling on the new root CHANGELOG entry | 70 words (ceiling 100); 3 sentences, longest 42 words (ceiling 45). Comfortably inside, which is notable given TODO 3.114 records a 108-word entry failing D8 in #1157. |
| TODO counter integrity (the live TODO 3.110 class) | Correct. `### 3.117` added and `**Next item number:**` advanced 3.117 to 3.118 in the same diff. |
| Version and date co-bump | Three versioned documents bumped, each with its Date co-bumped: README 1.10.13 to 1.10.14 with Library Version 2026.07.652 to 2026.07.653, plus the two `.working` registers. |
| CHANGELOG root and mirror parity (gate 59) | Both files in the diff, matching headers at Library Version 2026.07.653. |
| Credit-offload metrics row | Not applicable: #1166 is orchestrator-authored, not an offloaded delivery, and touches no metrics surface (`git diff --name-only | grep -i metric` returns rc 1). |
| #1166's own QA rows | Absent at this SHA by design: the rows in this diff are #1165's, per the recursion-avoidance batching convention. Gate 50 passes, so this is the expected state, not a gap. |

## What I did NOT check

- **The live branch-protection configuration** (d2 above), and I have not implied it was checked.
- **The 15 PRs' GitHub check histories** (F-3), for the same reason.
- **The codex deep assessment itself.** It lives in the file-drop inbox, outside the pinned corpus,
  and the order scopes me to the merged PR. I took the PR's characterization of the assessment's
  findings at face value where it is attributed as codex's, and flagged only what the PR asserts in
  its own voice.
- **`grc_library_scratch`-side artefacts** (the versioned dispatcher source and the codex onboarding
  step the detailed entry says landed there). They are outside this order's pinned SHA. Worth a
  follow-up check that the scratch-side half actually landed, since the entry asserts it did.
- **The second, still-unread drop** the detailed entry mentions. Recorded as a known unknown by the
  PR itself, correctly.
- **Semantic correctness of `alert-and-assessment-triage.md`'s ten dispositions** beyond reading them
  for unsupported verification claims. Adjudicating ten triage decisions is a different pass.

## Token spend

Approximately 82k tokens for this order: the order and the `validation-sweep-pr-scoped` skill
structure, the full ten-file diff, the new 66-line bypass log and the 124-line triage file, the
CLAUDE.md correction, `audit-delivery-status.py` and the monotonicity gate read as code, two full
`run_all_audits.sh` runs plus the 489-test regression suite, gate 50, the eight delta gates, and this
delivery. No subagent dispatched; the sweep ran directly.
