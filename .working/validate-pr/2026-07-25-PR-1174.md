# Result: validate-pr-1174

- **order:** validate-pr-1174 (kind qa, priority 0, **blocking**). **worker:** opus-20260725T122030Z-6443
- **grc_library_sha:** dd28405d (reach-checked). **grc_library_ref_sha:** 4a0f2978 (reach-checked, unread)
- **basis:** my own `git clone --shared` at `dd28405d`, `origin` removed. No shared checkout written to.

## INDEPENDENCE, disclosed before anything else

The order bars `opus-20260725T121943Z-78ff`. It does not bar me, **but it probably should have
considered doing so**: `tools/audit-selftest-discriminability.py`, the PR's headline artefact, is
built from MY `audit-selftest-discriminability` delivery earlier today, so I am grading a tool
productionized from my own research. I served it anyway because the only other live opus worker is
the one the order bars, this is a blocking priority-0 order, and the maintainer's 2026-07-25 decision
(`may_serve_barred`, scratch `84deadff`) is explicitly that work should not stall when nobody else can
serve it. **The finding below is a defect IN that tool, not a defence of it**, which is the direction
that makes a conflict less worrying, but the orchestrator should weigh it.

## VERDICT: FAIL. One error-severity defect, and it is in the PR's headline artefact.

**`tools/audit-selftest-discriminability.py` probes ITSELF, recursively, and spawns runaway
processes.** I measured **159 live orphaned Python processes** and a host load climbing to 4.37 before
I killed them. This also means the four figures the order asks me to confirm **cannot be reproduced by
anyone**, which the order itself pre-declares a finding.

**Baseline (the rest of the PR is fine):**

```text
bash tools/run_all_audits.sh           -> All 78 audit gates passed.   rc=0
python3 tools/run-linter-regression.py -> Ran 502 tests in 29.832s OK  rc=0
```

## Finding F-1 (ERROR): the probe recurses into itself

```text
### Finding F-1: audit-selftest-discriminability.py enumerates itself as a target
- level: error
- ruleId: guard-input/observer-includes-itself
- location: tools/audit-selftest-discriminability.py:387 (target enumeration), :241 (mutant path),
            :304 (directory-wide cleanup)
- fingerprint: guard-input/observer-includes-itself:audit-selftest-discriminability
```

**The mechanism, established at source and then by execution.**

```python
tools/audit-selftest-discriminability.py:387
    targets = [Path(t).resolve() for t in a.tools] if a.tools else sorted(
        p for p in (REPO_ROOT / "tools").glob("*.py") if has_selftest(p))
```

Nothing excludes `__file__`. And the probe satisfies its own predicate:

```text
has_selftest(tools/audit-selftest-discriminability.py) -> True
probe IS in its own target list                        -> True    (of 21 targets)
```

Mutants are written INTO the scanned directory (`:241`, `tool.parent / f"_probe_mutant_{tag}_..."`)
and executed with `--self-test`. Probing the probe therefore runs the probe again, which probes
itself again. **The nesting is visible in the live process names, nine levels deep:**

```text
_probe_mutant_neg__probe_mutant_neg__probe_mutant_neg__probe_mutant_neg__probe_mutant_neg_
  __probe_mutant_neg__probe_mutant_neg__probe_mutant_neg__probe_mutant_sub_
  audit_selftest_discriminability.py --self-test
```

**Three consequences, all measured:**

1. **Runaway processes.** `pgrep -cf "_probe_mutant"` returned **159**; host load 4.37 and rising. I
   killed them and load fell to 2.83.
2. **The figures are irreproducible.** Four runs, four answers for one tool:

```text
run 1  manage-workers  26 guards; 15 detected, 11 not   (1 BLIND, 9 NO-CASE, 1 inherent)
run 2  manage-workers  26 guards; 13 detected, 13 not   (4 BLIND, 8 NO-CASE, 1 inherent)
run 3  manage-workers  26 guards; 14 detected, 12 not   (3 BLIND, 8 NO-CASE, 1 inherent)
run 4  manage-workers  26 guards; 15 detected, 11 not   (1 BLIND, 10 NO-CASE, 0 inherent)
```

3. **A second, compounding defect: cleanup is directory-wide.** `_SiblingWorkspace.__exit__` (`:304`)
   globs `_probe_mutant_*.py` across the WHOLE directory and unlinks every match, not just its own.
   One target's teardown destroys another's in-flight mutant, and the probe then voids itself:

```text
VOID: NEGATIVE control fired ... can't open file '.../tools/_probe_mutant_neg_audit_inbox_drops.py':
[Errno 2] No such file or directory
```

Leftovers also survive the run and are enumerated as TARGETS next time. I observed
`=== tools/_probe_mutant_neg_sweep_working_records_to_private.py === 9 guard(s)` in a later run: the
probe measuring its own debris and reporting it as a tool.

**Minimal fix.** Exclude `Path(__file__).resolve()` from the target list at `:387`, and scope the
`__exit__` cleanup to the mutants that workspace created rather than globbing the directory. The
self-exclusion is load-bearing; the cleanup scoping stops one target corrupting another.

**Blast radius, checked rather than assumed.** Everything stayed inside my disposable clone:

```text
/home/grc/grc_library/tools/_probe_mutant_*  -> 0    shared corpus checkout CLEAN
/home/grc/grc_working/_probe_mutant_*        -> 0    shared exchange CLEAN
my clone after cleanup                       -> 0
```

**A worker running this against a shared checkout would have written mutants into it**, which is the
second reason to fix the self-exclusion before anyone runs the tool again. I have dropped this to the
issue channel as `issue-validate-pr-1174-probe-recurses-into-itself.md` rather than leaving it to be
read here, because it is actively consuming resources on a shared VM.

## (1) The four reported figures: THREE do not reproduce, and now we know why

| tool | order states | my run 4 | reproduces? |
| --- | --- | --- | --- |
| `manage-workers` | 22 / 1 blind / 9 gaps | 26 / 1 / 10 | **NO** (guard count and gaps differ) |
| `collect-deliveries` | 7 / 2 / 4 | 7 / 0 / 3 (+1 inherent) | **NO** (blind and gaps differ) |
| `audit-worker-saturation` | 10 / 0 / 8 | 11 / 3 / 8 | **NO** (guard count differs) |
| `audit-inbox-drops` | 6 / 0 / 6 | 6 / 0 / 6 in run 1; VOID in runs 3 and 4 | **partially** |

I am NOT reporting these as four separate findings. **They are one finding**: the probe is
non-deterministic and self-contaminating, so no figure it produced is reproducible, including the ones
that happen to match. The order's instruction that a non-reproducing figure is a finding is satisfied
by F-1, and re-deriving "correct" figures is impossible until F-1 is fixed.

## (3) The guard-input hunt: F-1 IS an instance of the hunted class

The order names this the primary hunt: an input whose derivation cannot answer the question its guard
asks. **F-1 is exactly that, in the tool built to find it.** The probe's input is "the set of tools to
probe", derived as a glob of `tools/*.py` with a self-test. That derivation cannot answer the question,
because the set it produces includes the prober and, after one run, the prober's own artefacts. The
guard logic is fine; the input is unsound. That the class recurred inside the instrument written to
detect it is the most useful thing this sweep found.

## What I did NOT cover, stated plainly rather than papered over

- **(2), the ledger hook, is NOT COVERED, and I am not reporting on it.** I probed it with six
  malformed rows and every case returned ALLOW, but I could not establish a POSITIVE CONTROL: my
  injected `error`/`open` row also returned ALLOW, and I traced that to appending into the file's
  severity LEGEND table (`.working/open-findings.md:32-35`, `| Severity | Meaning | Blocks |`) rather
  than the findings table. **Without a control that blocks, an ALLOW result proves nothing about the
  parser**, so I am reporting no verdict on the hook rather than a false "it never blocks". The
  fail-open design question is likewise unanswered by me. This needs a re-run with the correct table
  located first.
- **(4), the DONE and CHANGELOG claim verification, is NOT COVERED**, beyond confirming that the
  CHANGELOG credits the probe's origin. F-1 consumed the budget, and I judged an error-severity
  runaway-process defect on a shared VM the better use of it.
- **The cross-reference check on files citing the touched files is NOT COVERED.**
- **I did not re-derive the stall-signal defect** the order explicitly places out of scope.

## Token spend

Approximately 74k tokens: the order, the #1174 diff, four probe runs plus cleanup and process
forensics, source reads establishing the self-inclusion and the directory-wide teardown, a six-case
ledger-parser probe that I then invalidated on my own control, and this delivery. No subagent
dispatched.

<!-- END OF DELIVERY -->
