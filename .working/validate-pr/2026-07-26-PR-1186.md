# validate-pr: PR #1186

**Date:** 2026-07-26 (UTC)\
**PR:** #1186 (squash `dea21c95`, base `ce3ed5cd`)\
**Mode:** offloaded to an exec'd worker (claude / jposluns-work / opus / high), READ-ONLY, refute-briefed\
**Verdict:** SHIP, no blocker (2 LOW, both routed)

## Scope

The sweep `--prune` fail-open fix (TODO 3.129) on all four destructive paths; the
`UnicodeDecodeError` fail-closed hardening; the reality-fixture self-tests (8/8); the 3.139
CLAUDE.md-right-size series + the multi-phase `N.M`/`N.M.Y` numbering convention; the batched #1185
QA rows; README/CHANGELOG/log versions.

## Refutation attempts that FAILED to find a defect

- All four destructive paths are content-verified before prune (weekly changelog-details, roll-up
  rows, records byte-copy, one-off dirs), not existence-checked.
- Emit and verify cannot drift: both call the shared `weekly_archive_body` / `rollup_archive_body`
  helpers over the same `plan()` output.
- The `UnicodeDecodeError` hardening genuinely fails closed (refuses the prune, no traceback).
- The self-test count is dynamic (unittest-counted), not a literal.
- Numbering: 3.129 retired to DONE; P3 counter advanced to 3.140; `.1-.4` children consume no
  top-level number.
- Version + doc parity across README / CHANGELOG root + detailed / the four `.working` logs.

## Findings (both LOW, both routed)

- **F-1 (LOW):** `.working/session-handoff.md` carried stale directives ("do not run sweep `--prune`
  until 3.129 lands", now landed; "exec-worker system not built", now built in #1185). ROUTED to the
  next corrective PR (the handoff sweep).
- **F-2 (LOW):** the sweep tool's `_check_body` is a nested closure inside `main()`, so the two
  generated-body divergence-refusal paths and the `UnicodeDecodeError` branch have no reality
  fixture, while the 8/8 suite reads as full coverage. ROUTED to TODO 3.140 (hoist `_check_body` to
  module scope + add per-path divergent fixtures).

## Proof of run

- `python3 tools/sweep-working-records-to-private.py --self-test` -> 8/8 OK.
- `git diff ce3ed5cd..dea21c95 --stat` -> 12 files, 173 insertions, 53 deletions.
- Version/CHANGELOG parity confirmed (README 2026.07.676 / 1.10.37).

Worker id: `claude-jposluns-work-20260726T194122Z-ee50`. Recorded in `qa-metrics.md` (`_private`).
