# validate-pr: PR #1187

**Date:** 2026-07-26 (UTC)\
**PR:** #1187 (squash `1ac623a9`, base `dea21c95`)\
**Mode:** offloaded to an exec'd worker (claude / jposluns-work / opus / high), READ-ONLY, refute-briefed; ran in parallel with retro-1187 on security-work via the `--account` override this PR added\
**Verdict:** SHIP, no blocker (1 INFO: pre-existing, already-routed, out of #1187 scope)

## Scope

The exec-dispatch `--account` override (`pick_account` resolver, `dispatch()`/CLI wiring, the dry-run
PICK-marker fix, the 17-check self-test) and the batched #1186 close-out rows.

## Refutation attempts that FAILED to find a defect

- `pick_account` returns the auto-pick when `account` is None (byte-identical to `select_account`),
  the exact eligible account when named, and `None` with a correct distinguishing reason (unknown vs
  known-but-ineligible) otherwise; family-scoping is correct where an account name spans two families.
- `dispatch()`/CLI surface a failed resolution loudly (NOT DISPATCHED, exit 1), never a silent
  auto-pick fallback.
- The 6 new self-test cases are discriminating, not vacuous.
- Version/CHANGELOG/README parity holds (README 2026.07.677 / 1.10.38; CHANGELOG root + detailed both
  #1187); the #1186 close-out rows are placed newest-first with correct version bumps.

## Proof of run

- `python3 tools/exec-dispatch.py --self-test` -> OK (17 checks).
- `--dry-run --account security-work` PICK; `--dry-run --account jeff-posluns` REJECTED.

Worker id: `claude-jposluns-work-<validate-pr-1187 exec>`. Recorded in `qa-metrics.md` (`_private`).
