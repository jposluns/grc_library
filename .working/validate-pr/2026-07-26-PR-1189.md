# validate-pr: PR #1189

**Date:** 2026-07-26 (UTC)\
**PR:** #1189 (squash `c2a1db90`, base `2824db7b`)\
**Mode:** offloaded exec'd worker (claude / opus / high), READ-ONLY, refute-briefed\
**Verdict:** SHIP, CLEAN, 0 blocking findings (1 expected trailing-close-out note, actioned in #1190)

## Scope

The token-spend parser fix (TODO 3.128): the `NEGATION` blocklist becomes a gap-connector allowlist
(`gap_is_connector`) + `re.S` DOTALL, so a figure attributable only via a connector-only gap is read
and a non-connector word refuses (UNKNOWN); plus the batched #1188 close-out.

## Refutation attempts that FAILED

- `gap_is_connector` allowlist replaces `NEGATION` with no dangling reference; `re.S` is safe because
  the allowlist rejects prose across a newline; self-test 43/43; fail-closed direction.
- Version/CHANGELOG parity held (README 679/1.10.40; CHANGELOG root + detailed #1189).

## Trailing note (normal pattern)

The open-findings W1/W2 rows and the disposition are stamped FIXED-in-#1189 by the next PR (#1190),
per the trailing close-out convention. Recorded in `qa-metrics.md` (`_private`).
