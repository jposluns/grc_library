# validate-pr: PR #1193

**Date:** 2026-07-26 (UTC)\
**PR:** #1193 (squash `1f9bfa25`)\
**Verdict:** RETURNED, 0 error / 2 warning / 1 note (both warnings hot-fixed in #1194)

Drop `Version`+`Date` from 5 append-only `.working` logs (TODO 3.135): the 5 de-versioned logs, the
D2/D4 EXEMPT_FILES additions, the D7 SURFACES removal, + the #1192 close-out. Run as an in-session
subagent (0 workers live); this followed a separate pre-merge skeptical verifier that also returned
CLEAN.

The change itself is CLEAN, confirmed at source: the 5 de-versioned logs are exactly the 5 paths
added to EXEMPT_FILES in both D2 and D4 (byte-identical, no under/over-exemption); D4's exemption is
load-bearing (the removal PR fails D4 without it); the 3 removed D7 surfaces are exactly the
now-unversioned ones (`claim-fit history` correctly retained); no gate, hook, or parser keys on the
removed fields; version bump correct; no corpus per-document Version touched.

Findings (both are #1193 close-out slips, because it merged during the emergency wind-down):

- **W1 (warning), `governance/specification-audit-programme.md:235`.** The D7 §6 narrative still
  enumerated "the runbook, and the guardrail-review, validate-pr, improvement-log, and claim-fit
  history registers" as D7 surfaces, but #1193 removed the first three from D7's `SURFACES` and
  "the runbook" was never a D7 surface at all. Free-prose drift (type-A row of the change-impact
  surface map), ungated. FIXED in #1194: the sentence now names only the surfaces D7 tracks
  (library CalVer, README Version, pack README Version, this spec's Version, the claim-fit history
  register).
- **W2 (warning), `TODO.md` + `.working/DONE.md`.** #1193 fully closed TODO §3.135 but did not
  rotate it: §3.135 remained in TODO with no DONE entry. FIXED in #1194 (deleted from TODO, DONE
  entry added).
- **N1 (note, pre-existing/out-of-window).** The same sentence's "runbook" mis-name predates #1193
  (absent from D7's code at the base too); folded into the W1 fix.

Recorded in `qa-metrics.md` (`_private`).
