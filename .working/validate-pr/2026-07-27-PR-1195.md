# validate-pr: PR #1195

**Date:** 2026-07-27 (UTC)\
**PR:** #1195 (squash `ab38638e`)\
**Verdict:** RETURNED, 0 error / 1 warning / 1 note (W1 hot-fixed in #1196)

The post-wind-down resume close-out (Sweep 124 rows, #1194 QA rows, TODO 3.128 re-scope, the 1.26 P1
series, the diff-ban + worker-first CLAUDE.md codifications, handoff cursor/snapshot, README/CHANGELOG
bump). OFFLOADED to a worker (the first QA offloaded through exec-dispatch this session); the result was
read and its findings dispositioned at source.

The change is CLEAN: all 78 gates (77 + gate-36 shared-tree-deferred), the 1.26 series is well-formed
(umbrella + four independently-closeable phases, P1 counter advanced to 1.27, gate 78 permanence passed),
the two CLAUDE.md codifications are dash-free / -ize / carry no account names, TODO 3.128's re-scope
matches #1189's fix, the CHANGELOG compact entry is within the length ceiling with consistent version
tokens, and no corpus per-document Version was touched.

Findings:
- **W1 (warning), `.claude/CLAUDE.md`.** The worker-offload rule's lead-in still read "The two-part hard
  rule" after the body was expanded to SIX numbered points, a self-contradiction and the exact
  stale-narrative-after-a-change class the same PR fixed in TODO 3.128. FIXED in #1196 (lead-in now
  "The hard rule ... expanded to six points 2026-07-26").
- **N1 (note).** An uncommitted working-tree CHANGELOG change (the origin-story paragraph) was visible to
  the worker; it is #1196 work, out of #1195 scope, no action.

Recorded in `qa-metrics.md` (`_private`).
