# validate-pr: PR #1196

**Date:** 2026-07-27 (UTC)\
**PR:** #1196 (squash `e9cdc170`)\
**Verdict:** RETURNED, CLEAN, 0 error / 0 warning / 2 note (N1 hot-fixed in #1197)

The origin paragraph + the resume/compaction re-read rule + directive-persistence + the /sitrep &
worker-onboarding TODO items + the batched #1195 QA rows. OFFLOADED to a worker; result read and
findings dispositioned at source.

CLEAN: origin paragraph dash/-ize clean and the pack-README link resolves; the new CLAUDE.md section is
coherent, placed before `## Project`, and carries no account names; the two-part-to-six-points fix is
correct (the rule body has exactly six points); TODO 3.142/3.143 are well-formed with the P3 counter at
3.144 and gate 78 (permanence) OK; no corpus per-document Version was touched; the README/CHANGELOG
version tokens are consistent at 686 / 1.10.47; the #1195 QA rows are all present.

Notes:
- **N1 (note), `.claude/CLAUDE.md`.** The new section used US "behavioral" where the file uses Canadian
  "behaviour" elsewhere. Ungated (`lint-language.py` does not scan `.claude/`), cosmetic. FIXED in #1197.
- **N2 (note).** The worker's read-only tree cannot see `grc_library_private`, so the READ-FIRST mirror
  half of the change was unverifiable from its side. Not a defect (the mirror is correct; the worker
  simply cannot read `_private`).

Recorded in `qa-metrics.md` (`_private`).
