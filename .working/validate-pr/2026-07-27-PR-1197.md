# validate-pr: PR #1197

**Date:** 2026-07-27 (UTC)\
**PR:** #1197 (squash `e2c82cd5`)\
**Verdict:** RETURNED, DEFECTIVE-BUT-REMEDIATED, 1 error (E1) / 1 warning (W1). OFFLOADED to a worker.

The 2026-07-26 daily CHANGELOG roll-up (collapse #1179-#1194 into one daily line + sweep the 16 matching
detailed-mirror entries) + TODO 3.144 (multi-orchestrator, scope-gated) + #1196 QA rows.

Headline verified correct: the 16 per-PR entries collapsed; the 16 mirror entries swept byte-preserved
to the private archive; gate 59 mirror-parity GREEN; D8 length clean; TODO 3.144 well-formed with gate 78
permanence OK; version tokens consistent.

- **E1 (error, CONFIRMED, hot-fixed in #1198).** The root collapse used the distant end-anchor
  `**Week of 2026-07-13**`, so besides the intended #1179-#1194 it also deleted the four already-collapsed
  daily-summary lines (07-22 #1056-#1067, 07-23 #1068-#1107, 07-24 #1108-#1145, 07-25 #1146-#1178),
  folding them into nothing: at `e2c82cd5` the 07-26 daily line was immediately followed by
  `Week of 2026-07-13` with no `Week of 2026-07-20` block and no 07-22..07-25 coverage. A reader-facing
  root history-removal (never-remove violation), ungated. The orchestrator caught it building #1198; this
  sweep independently corroborated it. FIXED in #1198: the `Week of 2026-07-20 (PRs #1056-#1194)` block
  restores the whole week's coverage in aggregate.
- **W1 (warning, not a #1197 defect).** A `run_all_audits.sh` at HEAD reports a Bookkeeping-parity FAIL,
  but that is an artifact of #1198's uncommitted working-tree edits (the #1198 CHANGELOG entry pushes
  `max_pr` to 1198, so #1197 falls inside the gate-50 window); at #1197's own committed state #1197 is
  the highest PR and is exempt. Transparency note, not a code defect.

Recorded in `qa-metrics.md` (`_private`). Lesson (retro-1197): a range-replace must end at the immediate
next entry, never a distant section header; assert the replaced span contains only the intended entries.
