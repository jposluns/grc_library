# validate-pr: PR #1198

**Date:** 2026-07-27 (UTC)\
**PR:** #1198 (squash `8820fbae`)\
**Verdict:** RETURNED, PASS, CLEAN. OFFLOADED to a worker.

The weekly CHANGELOG roll-up (daily lines 2026-07-22..07-26 into one `Week of 2026-07-20 (PRs #1056-#1194)`
block) plus the correction of #1197's over-collapse, plus the batched #1197 QA rows.

Confirmed at the committed merge state: exactly one `Week of 2026-07-20` block covering #1056-#1194 with
zero stray 2026-07-2x daily/per-PR lines; #1197's over-removal FULLY REMEDIATED (07-22..07-26 coverage
restored in aggregate); root history summarized-in-place with all older week blocks and the current-day
#1195/#1196/#1197/#1198 entries intact; gate 59 mirror-parity GREEN; D8 length ok; version tokens
consistent at 2026.07.688; no corpus per-document Version touched; the #1197 QA batch (validate-pr,
retro, bypass, open-findings E1 FIXED) complete.

The two `run_all_audits.sh` failures are both NON-DEFECTS: (1) a Bookkeeping-parity FAIL that is an
artefact of the orchestrator's uncommitted #1199 working-tree edits (gate 50 is GREEN at #1198's own
committed state); (2) the gate-36 linter-regression suite, environment-deferred (fixture writes).

Worker-isolation note (to track): the worker observed the orchestrator's uncommitted #1199 working-tree
state, indicating exec'd workers read the SHARED working tree rather than an isolated pinned clone; the
worker correctly distinguished the artefact, so this QA stands, but the isolation gap is a separate
finding. Recorded in `qa-metrics.md` (`_private`).
