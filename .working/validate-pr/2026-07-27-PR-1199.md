# /validate-pr record - PR #1199 (grc_library)

**Date:** 2026-07-27 · **Offloaded to:** worker (jposluns-work / claude, pinned merged head `e554f1b7`) · **Verdict:** PASS-SHIP, 0 error / 1 warning / 1 note.

**Disposition (orchestrator):** both findings re-verified at source and FIXED in the #1200 handoff PR. W1: TODO 3.145's rationale wrongly stated #1199 removed the wrapper flock / the registry is now the sole per-account cap; the wrapper comment at `tools/exec-dispatch.py` lines 55-56 confirms the wrapper flock still caps same-account concurrency at 1 today, and #1199 does not touch the (out-of-repo) wrapper, so the fail-open is moot at cap 1 because the wrapper still enforces 1, and TODO 3.145 was reworded. N1: the detailed-mirror #1199 entry said the self-test went from 16 to 31; the parent baseline is 17 (re-run read-only), corrected to 17 to 31. Neither affects the merged code or the cap-1 safety posture.

---

## Worker report (verbatim, minus the leading stdin warning)

## Validation Report - PR #1199 (grc_library)

**Target:** exec-dispatch per-account concurrency registry + `--worker-id`
**Merge (squash):** `e554f1b7` · **Parent:** `8820fbae`
**Diff basis:** `git diff 8820fbae..e554f1b7` (read-only; no checkout/switch/reset performed)
**Role:** Subagent A - diff review + cross-reference of touched surfaces

---

### Verification results (per the 6 required checks)

**(1) Registry logic - CORRECT.** `_reserve_slot` (`tools/exec-dispatch.py:279-293`) performs the whole reserve inside ONE `fcntl.flock(LOCK_EX)` critical section on `inflight.lock`: reap -> count-live-for-key -> refuse-if-`>= max_concurrent` (persisting the reap) -> else append + `_write_inflight` -> unlock in `finally`. No check-then-act TOCTOU; the lock is never held across `subprocess.run` (reserve before, `_release_slot` in the `finally` at `:371`).
- pid-liveness reaping via `os.kill(pid, 0)` - `_pid_alive:210-224` (ProcessLookupError->dead, PermissionError->alive, other OSError->dead). [ok]
- 24h absolute `started`-age ceiling `STALE_STARTED_AGE_S = 24*3600`, applied in `_reap:250` as a pid-reuse backstop (deliberately NOT mtime staleness). [ok]
- Key = per-(account,family) config-dir `id`, not bare account - `_inflight_key:319` (`acct.get("id") or acct.get("account")`). [ok]
- Default cap 1 - `_max_concurrent:308-315` (absent / non-positive / unparseable all floor at 1). [ok]
- Atomic writes (`tmp` + `os.replace`) under lock; lock file content never rewritten. [ok]
- Refusal returns without `worker_id` -> CLI `NOT DISPATCHED` loud non-zero path confirmed at `:625-639`. [ok]

**(2) The one known fail-open (`_read_inflight` -> `[]` on ANY error) - routing CONFIRMED, moot-at-cap-1 CONFIRMED (with a caveat, see W1).** It is routed as gated TODO 3.145 (`TODO.md:302-304`) and open-finding (`open-findings.md:47`), correctly gated before `max_concurrent > 1`, with a clear fail-CLOSED-for-RESERVE fix. It is genuinely moot at cap 1 - but *because the wrapper flock still caps same-account concurrency at 1* (`exec-dispatch.py:55-56`), not because "no over-count is possible" in isolation. See W1.

**(3) `--worker-id` threading - CONFIRMED.** `build_dispatch_cmd:330-337` appends `--worker-id worker_id` unconditionally; `dispatch` calls it with `WRAPPER[family]` for BOTH `claude` and `codex` wrappers (`:353`). Self-test checks 24-26 confirm presence, ordering vs `--effort`, and charset `[A-Za-z0-9_-]`.

**(4) Self-test - PASSES.** Ran the merged file read-only: `exec-dispatch self-test: OK (31 checks)`. 14 new `check()` calls added (parent had 17, head has 31; count is dynamic `len(ran)`, so the reported number is authoritative).

**(5) No account names / no secrets - CLEAN.** Added lines contain only pre-existing generic fixture placeholders (`work-a`, `work-b`, `personal-a/b`); no real account names, no `@traffictech`, no keys/tokens/credentials. (The only "token" hit is the word "liveness token" in a docstring.)

**(6) Version + CHANGELOG pair - PRESENT & CONSISTENT.** Root `CHANGELOG.md` `2026-07-27 | 2026.07.689 | PR #1199`; detailed mirror `CHANGELOG-detailed.md:22` `Library Version 2026.07.689, PR #1199`; `README.md` `2026.07.688->689` and README `1.10.49->1.10.50`; `TODO.md` next-item `3.145->3.146` with 3.145 consumed. All aligned.

---

### Findings

**W1 - WARNING (documentation; NON-BLOCKING) - `TODO.md:304` contradicts `tools/exec-dispatch.py:55-56`.**
TODO 3.145 (added by this PR) states the fail-open sits on "what is now the SOLE per-account concurrency cap (the #1199 change **removed the wrapper flock as the backstop**)." But #1199 does **not** touch the wrapper - the root-owned `/usr/local/sbin/run-{claude,codex}-worker` are outside the repo and absent from the diff, and the diff *newly* imports `fcntl` (so no prior in-file flock was removed). Meanwhile this file's own **unchanged, still-shipped** comment at `exec-dispatch.py:55-56` reads: "The per-account flock **currently caps same-account concurrency at 1** … becomes load-bearing once … concurrency > 1 (WIRE-IN)." These two on-main statements are mutually exclusive: the wrapper flock cannot be both "removed by #1199" and "currently capping at 1."
The practical conclusion (`MOOT at cap 1`, gate before `>1`) is **correct** - but *for the opposite reason the item gives*: it is moot precisely because the wrapper flock still enforces 1, not because the registry became the sole cap. Risk: the enable-step operator may misjudge the true trigger for 3.145 becoming load-bearing (loosening the wrapper flock is what makes the registry sole enforcement; enabling `max_concurrent > 1` in config alone would still be blocked by the wrapper flock). Recommend rewording 3.145 to "moves enforcement toward the registry; the wrapper flock still caps at 1 today and must be loosened at the WIRE-IN step, at which point the registry becomes the sole cap and this fail-open becomes live." Does not affect the merged code or the cap-1 safety posture.

**N1 - NOTE (cosmetic count) - `CHANGELOG-detailed.md:25` "self-test extended from 16 to 31 checks."**
The parent self-test actually reports **17** checks, not 16 (verified by running both parent and head read-only). The headline "to 31" is correct and verified; only the baseline is off by one. The new-check comment labels in the diff (numbered 16-27) reflect the same undercount but are purely narrative. Root `CHANGELOG.md` ("self-test 31 checks") and the Verification line ("OK (31 checks)") are accurate.

---

### VERDICT: **PASS - SHIP.** 0 error / 1 warning / 1 note.

The merged registry code is correct and well-tested (31/31), `--worker-id` threads to both wrappers, no account names or secrets leaked, and version/CHANGELOG bookkeeping is consistent. The single fail-open is correctly identified, routed, and gated (TODO 3.145), and is confirmed moot at the shipped `max_concurrent = 1`. W1 is a self-contradiction in that gating item's *rationale text* (wrapper-flock "removed" vs. the file's own "currently caps at 1") that should be corrected before the concurrency-enable step, but does not block the #1199 merge; N1 is a cosmetic off-by-one in the detailed CHANGELOG. Orchestrator to disposition W1/N1.

*(No files edited. Version history inspected read-only via git show/diff/log only.)*
