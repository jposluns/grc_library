# /validate-pr record: PR #1230 (migration PR2b-1)

**Date:** 2026-07-28
**PR:** #1230 - migration PR2b-1: rewire the remaining `.working` consumers
**Merged head:** `e8e26f23` (parent `ad597417`)
**Verdict:** SHIP (dual-family); all findings FIXED/ACCEPTED in PR2b-2a

## Dispatch
Dual-family (claude jposluns-work + codex jeff-mailz), refute-briefed, READ-ONLY.

## Findings and disposition (all in PR2b-2a)
- **LOW** CHANGELOG root "5 tools" (8 rewired) -> FIXED.
- **LOW** detailed:24 nested markdown link `[[...](...)](...)` -> FIXED (de-nested).
- **LOW** detailed:30 duplicated "Pre-push dual-family" lead phrase -> FIXED.
- **LOW** detailed:16,28 "2 tests" (3 added) -> FIXED (2->3).
- **MEDIUM** `audit-cross-repo-references` private-tree walk resolves links relative to
  the private root, so a `.working` file's links to the public corpus become false
  findings post-move -> FIXED (a `link_root` param resolves the private walk's links
  against the public root via the in-repo-equivalent source).
- **MEDIUM/HIGH** `resolve_working_for_write` both-dirs split -> ACCEPTED: correct given
  the complete-copy plan (writes unify in private once every file is copied); residue
  stated in the docstring; the deletion-QA no-in-repo-only-file check is the control.
- **BLOCKER x2 + MEDIUM** `check-changelog-on-pr` / `check-todo-rotation-on-pr` require
  `.working` files in the public diff (fail post-move); D7 silently untriggers post-move
  -> FIXED (all three drop/annotate the `.working` requirement once the in-repo `.working`
  is gone; current pre-move behaviour unchanged).

## Process note
The pre-push codex verifier's findings were under-consumed at push time (the exec-dispatch
`.output` file captures only the delivery TAIL; the full findings were in the worker log).
The post-merge re-verify caught them. Retro codifies: consume from the full worker log,
never the `.output` tail.

<!-- END OF DELIVERY -->
