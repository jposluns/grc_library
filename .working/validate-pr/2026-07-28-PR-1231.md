# /validate-pr record: PR #1231 (migration PR2b-2a)

**Date:** 2026-07-28
**PR:** #1231 - migration PR2b-2a: contained fixes from #1230 post-merge QA
**Merged head:** `0db65053` (parent `e8e26f23`)
**Verdict:** SHIP; 1 LOW fixed in the closing handoff, all else clean

## Findings
- **LOW** (FIXED in the closing handoff): #1231's own detailed CHANGELOG entry reintroduced
  the duplicated "Pre-push dual-family skeptical verify:" lead phrase (the same bug #1231
  fixed in the #1230 entry); the placeholder-fill doubled it.

## Verified clean (post-merge claude, with REAL git fixtures)
- **3 delta-gate re-scopings** tested against real PRE-move (`.working` present) and
  POST-move (`.working` absent) fixtures: post-move each accepts the public half only, and
  check-todo-rotation still FAILs when TODO is not rotated; pre-move each is byte-identical
  to the old behaviour. `_working_in_repo()` resolves `<repo>/.working` correctly.
- **audit-cross-repo `link_root`**: `--self-test` 5/5; the public walk is byte-identical.
- **CHANGELOG corrections + residue note** verified accurate (5->8 tools, etc.).
- **Bookkeeping**: #1230 rows + detail correct (parent `e8e26f23`); README 719->720/1.10.81.
- **No other on-pr gate** hard-requires a `.working` file in the public diff (swept D1-D9).

<!-- END OF DELIVERY -->
