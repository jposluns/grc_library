# Sweep 13 — iteration 1 (2026-06-21)

## Trigger & state snapshot

- **Trigger**: maintainer's standing rule "after every 5 PRs, perform a validate and correct any identified issues before returning to the next 5 PRs" — fired at the close of the FR-21 / FR-45 / FR-35 / FR-19+20 / FR-92 batch (PRs #149-#153).
- **Library version at HEAD**: 2026.06.135 (post-PR-153).
- **Pack version at HEAD**: 1.34.0.
- **Governance-rule count at HEAD**: 7 (`.claude/rules/governance/`).
- **Gate count at HEAD**: 46 corpus gates + D1 / D2 / gate 45 PR-time delta wrappers.
- **Skill count at HEAD**: as declared in the pack README.
- **Iteration**: 1 (initial baseline).
- **Mechanical baseline**: `tools/run_all_audits.sh` — all 46 gates pass.
- **Pre-flight scanner**: zero candidate findings (368 files, 18 candidates suppressed by heuristics + exemptions).
- **Dispatch declaration**: Subagents A, B, and C all dispatched per the full-sweep discipline.

## Subagent A, Recent-PR deep review

```
tool: subagent-A
ruleId: multi-surface-incompleteness
level: warning
location: ai/standard-ai-and-agentic-development-security.md:543
fingerprint: multi-surface-incompleteness:ai/standard-ai-and-agentic-development-security.md:543
rubric: out-of-window
evidence: |
  "**ADTEST-SEC-02:** Test cases may not be removed from the suite without CISO approval."
  PR #150 (FR-45) replaced "may not" with "must not be" in two security/ files
  for RFC 2119 normative-verb compliance. Three parallel occurrences with the
  same MUST-NOT semantics exist in the ai/ domain and were not caught by the
  PR's "corpus-wide grep for `may not` in `security/`" (scope limited the search).
  ADTEST-SEC-02 here is a clear prohibition with override clause: structurally
  identical to the `security/standard-authentication-and-password-management.md`
  password-reuse line that the PR fixed.
```

```
tool: subagent-A
ruleId: multi-surface-incompleteness
level: warning
location: ai/standard-ai-and-agentic-development-security.md:718
fingerprint: multi-surface-incompleteness:ai/standard-ai-and-agentic-development-security.md:718
rubric: out-of-window
evidence: |
  "...may not be embedded in proprietary tooling chains..."
  Same FR-45 class. Reads as an absolute prohibition (licence-imposed) rather
  than a permissive-possibility ambiguity, so RFC 2119 MUST NOT is the intended
  reading. Should be aligned with the PR #150 fix.
```

```
tool: subagent-A
ruleId: multi-surface-incompleteness
level: note
location: ai/guide-ai-adversarial-test-reference.md:164
fingerprint: multi-surface-incompleteness:ai/guide-ai-adversarial-test-reference.md:164
rubric: out-of-window
evidence: |
  "- Test cases may not be removed without CISO approval (AI Security Standard ADTEST-SEC-02)."
  Parallel restatement of ADTEST-SEC-02 in the adversarial-test guide. If the
  ai/standard fix is applied (ADTEST-SEC-02), this guide must be updated in
  lock-step or the two surfaces drift.
```

```
tool: subagent-A
ruleId: multi-surface-incompleteness
level: note
location: compliance/logistics/register-basc-it-compliance-kpis.md:27
fingerprint: multi-surface-incompleteness:compliance/logistics/register-basc-it-compliance-kpis.md:27
rubric: out-of-window
evidence: |
  "| KPI | Metric | Target | Reporting Frequency | Owner |"
  FR-92 (PR #153) added Escalation Owner + Remediation Sign-off columns to all
  KPI tables in `operations/register-it-operations-kpis.md`. The parallel
  KPI register `compliance/logistics/register-basc-it-compliance-kpis.md` has
  the same structural role (per-KPI table with Owner column) but lacks
  Escalation Owner and Remediation Sign-off. If FR-92's principle "each KPI
  must identify a named escalation owner... and a named remediation sign-off
  role" is corpus-normative, this register should follow. (The other two
  candidates, `governance/register-digital-trust-and-assurance-metrics.md`
  and `resilience/register-resilience-metrics-and-testing-log.md`, list metric
  *categories* rather than individual KPIs with per-row breach-response columns,
  so the pattern does not transfer.)
```

```
tool: subagent-A
ruleId: stale-document-history
level: note
location: compliance/logistics/register-basc-it-compliance-kpis.md:63
fingerprint: stale-document-history:compliance/logistics/register-basc-it-compliance-kpis.md:63
rubric: out-of-window
evidence: |
  "| Version | Date | Change |
   | --- | --- | --- |
   | 1.0.0 | 2026-05-27 | Initial release |"
  Frontmatter declares Version 1.1.1 (2026-05-28), but the embedded Document
  history table only lists 1.0.0. Discontinuity between metadata version and
  history table. Not in window for PR #149-153 but surfaced incidentally during
  cross-doc review.
```

**Subagent A summary**: 5 findings total (2 warning, 3 note). All out-of-window. Searched clean: §7.2 / §9.1 / §4.1.1 cross-refs in CAPA resolve correctly; renumbered principles 3-7 in `register-it-operations-kpis.md` have no back-references; no per-doc version pinning across corpus; Article 33(2) framing in `supply-chain/standard-supplier-security-and-privacy-assurance.md:91` already aligns with PR #151; no `ISO/IEC 27701:2019` references remain anywhere.

## Subagent B, Corpus-wide stale-reference sweep

**Subagent B return**: 0 findings.

Classes searched and confirmed clean across all 368 markdown files:

1. **Stale gate counts** — all references say "46 gates" (current) or older counts inside `.working/changelog-details/` / `.working/validate-sweeps/` (frozen-state artefacts, exempt). Clean.
2. **Stale library version pins (2026.06.1XX)** — only present in CHANGELOG history, `.working/DONE.md` historical entries, fitness-review snapshots (with explicit "as-of write-time" framing), and the `r1.md` example block (FR-8 lists 2026.06.107/1.8.63 as illustrative content of the finding itself). Clean.
3. **Stale PR references #1[3-5][0-9]** — all correctly past-tense in CHANGELOG/DONE or correctly forward-looking in historical files. TODO.md line 13 says "synced after PR #153 merge" (current). Clean.
4. **Stale FR backlog totals in TODO** — verified mechanically: High[critical] 14 (match), High 11 (match), 16 closed across PRs #142-#153 (4+2+1+1+1+1+1+1+1+2+1 = 16), 111-16 = 95 open (match). Clean.
5. **Stale skill/rule descriptions** — pack README v1.34.0 lists 7 governance rules; directory tree matches; CLAUDE.md and rollout history consistent. Clean.
6. **Stale dates** — no "As of 2026-06-(01..20)" claims. Clean.
7. **Stale path refs** — none found.

**Cross-check vs. pre-flight scanner**: scanner predicted zero candidates; corroborated.

## Subagent C, Audit-programme integrity reviewer

**Subagent C return**: 0 findings.

Surfaces verified clean across all four parallel surfaces:

1. **Gate count consistency across four surfaces** — all agree on 46 corpus gates: `tools/run_all_audits.sh` (46 `run_gate`), `.github/workflows/quality.yml` (46 `- name:`), `.pre-commit-config.yaml` (47 hook ids = 46 gates + 1 helper hook exempted by gate 35), `governance/specification-audit-programme.md` §6 (rows 1-46). Clean.
2. **Linter docstring accuracy** — spot-checked four (`lint-cross-doc-numbers.py`, `lint-overnight-file.py`, `lint-language.py`, `lint-todo-staleness.py`); docstrings match shipped behaviour. Clean.
3. **Spec ↔ script bidirectional correspondence** — every §6 row 1-46 script exists in `tools/`; every script in runner/workflow documented in spec. Clean.
4. **Audit programme metadata coherence** — `specification-audit-programme.md` carries Version 1.14.0, Date 2026-06-21 (today); §6 row 46 (overnight-file gate) is present. Clean.
5. **Free-form gate-count references** — all `[0-9]+ gates` matches outside the four surfaces fall within `.working/changelog-details/CHANGELOG-detailed.md` (exempt via `DEFAULT_EXEMPT_DIRS`). Clean.
6. **PR-time checks coherence** — `tools/run-pr-time-checks.sh` wraps D1 / D2 / gate 45 correctly, matching spec §6.1. Clean.

**Audit-programme integrity status**: internally consistent. Sweep 13 batch touched no audit-programme file; programme remains in lock-step at 46 gates.

## Orchestrator synthesis

### Dedupe and classification (5.1 / 5.2)

All 5 of Subagent A's findings are unique by `(file_path, normalised_section, claim_type)` tuple. No overlap with B or C (both returned zero findings, so no cross-subagent conflicts to debate). Each finding has explicit `path:line` quoted evidence (rule respected). All five tagged **R** (Real, observed directly with quoted file content).

### Severity adjudication (5.3)

All five findings are **out-of-window** (rubric: out-of-window). Per the in-window vs out-of-window protocol, out-of-window findings are surfaced as questions to the operator with named action options, not auto-deferred to "Low / FYI".

The five findings cluster into two themes:

- **FR-45-class** (3 findings): "may not" used as MUST NOT in `ai/` files (ADTEST-SEC-02 prohibition, AGPL/GPL licence prohibition, parallel restatement in the adversarial-test guide). The original PR #150 scope was deliberately limited to `security/` and did not generalise. These are net-new FR-45 instances on a parallel surface.
- **FR-92-class** (1 finding): a parallel KPI register at `compliance/logistics/register-basc-it-compliance-kpis.md` lacks the Escalation Owner / Remediation Sign-off columns added to the IT-ops register. The other candidate parallel registers (DTI metrics, resilience metrics) operate at the metric-category level rather than per-KPI rows, so the pattern does not transfer; the BASC IT KPIs register is the only true parallel surface.
- **Document-history drift** (1 finding): the same BASC file has a stale Document history table (frontmatter 1.1.1; history shows only 1.0.0). Surfaces a metadata-vs-history-table consistency gap not caught by any current audit gate.

### Failure-mode classification (C1-C8)

- 3× FR-45-class → **C3 multi-surface incompleteness** (scope-limited remediation; parallel surface unaddressed).
- 1× FR-92-class → **C3 multi-surface incompleteness** (same shape: corpus-normative principle introduced in one register; parallel register not propagated).
- 1× document-history drift → **C1 stale prose reference** (per-doc history table not maintained in lock-step with frontmatter version field).

### Cross-check vs false-positive memory

No matches against the sweep-history false-positive register. All 5 findings are net-new.

### Debate (5.5)

No subagent divergence: B and C returned zero, A surfaced findings on areas B and C did not search. No adjacent severity disagreements. No debate round invoked.

### Action decided per finding

All 5 findings are out-of-window. Surfaced to operator for triage; this iteration does not auto-apply fixes. The named action options offered to the operator: (1) bundle into a single follow-up PR closing all five (FR-45 generalisation + FR-92 generalisation + BASC history-table fix), (2) rotate into TODO as net-new FR-style backlog items, (3) dismiss as out-of-scope corpus noise.

### Dispatch declaration (5.6)

Subagents A, B, and C all dispatched per the full-sweep discipline. No thin-sweep exception invoked.

## Resulting PR

[#154 — Sweep 13 iter 1 close-out: FR-45 + FR-92 generalisations](https://github.com/jposluns/grc_library/pull/154).
