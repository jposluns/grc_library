# Sweep 14: iteration 1 (2026-06-21)

## Trigger & state snapshot

- **Trigger**: standing maintainer rule "after every 5 PRs, perform a validate and correct any identified issues", fired at the close of the FR-1 / FR-2 / FR-16 / FR-80 / FR-44 batch (PRs #155-#159).
- **Library version at HEAD**: 2026.06.141 (post-PR-159, pre-close-out).
- **Pack version at HEAD**: 1.34.0 (unchanged this batch).
- **Governance-rule count at HEAD**: 7 (`.claude/rules/governance/`).
- **Gate count at HEAD**: 46 corpus gates + D1 / D2 / gate 45 PR-time delta wrappers.
- **Iteration**: 1 (initial baseline).
- **Mechanical baseline**: `tools/run_all_audits.sh`, all 46 gates pass.
- **Pre-flight scanner**: zero candidate findings (368 files, 18 candidates suppressed by heuristics + exemptions).
- **Dispatch declaration**: Subagents A, B, and C all dispatched per the full-sweep discipline.

## Subagent A, Recent-PR deep review

```
tool: subagent-A
ruleId: multi-surface-incompleteness
level: warning
location: specification-master-project.md:126
fingerprint: multi-surface-incompleteness:specification-master-project.md:126
rubric: in-window
evidence: |
  "2. No directory shall contain non-canonical document types."
  PR #159 (FR-44) added §6.1 Requirement-language register to this very same file (line 277), stating that "shall" is reserved for (a) direct quotation of an external standard and (b) legacy content. Line 126 is normative library-internal text, not a quotation, sitting inside the same specification file that establishes "must / must not" as canonical. The specification contradicts itself: the verb register in §6.1 was added in the same PR that left an authored "shall" in §4.1 #2. Per §6.1 rule 4 this is "legacy content... converted to 'must' opportunistically as documents are revised", but the spec itself was the document being revised in PR #159 and this occurrence was not updated. Note: the rest of the file is predominantly "must" (the §6.1 register cites "must" as the canonical verb), so rule #5 (verb consistency) puts this in the minority side that should have flipped.
```

```
tool: subagent-A
ruleId: multi-surface-incompleteness
level: warning
location: governance/policy-exception-and-risk-acceptance-management.md:88
fingerprint: multi-surface-incompleteness:policy-exception-and-risk-acceptance-management.md:88
rubric: in-window
evidence: |
  "A 4th renewal may not be granted by any authority."
  PR #159 (FR-44, landed same day as PR #157) §6.1 rule #3 in specification-master-project.md:276 states: "Do not use 'may not' as a prohibition... Where the intent is a prohibition, write 'must not'." Line 88 here is clearly a prohibition ("Not permitted under this policy" is the cell's heading). The new normative content authored in PR #157 introduced two "may not" prohibitions that contradict the verb-register convention established in PR #159; both PRs are in the focus window. New normative content must follow the canonical rule even though the surrounding document is shall-predominant.
```

```
tool: subagent-A
ruleId: multi-surface-incompleteness
level: warning
location: governance/policy-exception-and-risk-acceptance-management.md:94
fingerprint: multi-surface-incompleteness:policy-exception-and-risk-acceptance-management.md:94
rubric: in-window
evidence: |
  "Re-baselining may not be used to bypass the ceiling"
  Same class as the line 88 finding. New §3.6 content from PR #157 uses "may not" as a prohibition. Per FR-44 §6.1 rule #3 (added the same day in PR #159), this should be "must not be used to bypass the ceiling".
```

**Subagent A summary**: 3 findings (all warning, all in-window, all `multi-surface-incompleteness` class). All three are FR-44-self-violations from PRs that shipped the same day as the convention. Searched clean: §3.4 / §3.5 / §3.6 cross-references in the exception policy resolve correctly; FR-16 mirror pattern of FR-19 / CAPA §6.3.1 is structurally consistent; FR-80 cross-doc reconciliation is self-contained; RFC 2119 / RFC 8174 / NIST SP 800-37 / CSA CCM v4.1 GRC-12 citations are correct; no other documents are forced to re-bump by the FR-16 / FR-80 changes.

## Subagent B, Corpus-wide stale-reference sweep

```
tool: subagent-B
ruleId: stale-skill-description
level: warning
location: specification-master-project.md:126
fingerprint: stale-skill-description:specification-master-project.md:126
rubric: in-window
evidence: |
  "2. No directory shall contain non-canonical document types."
  This is internal normative content authored within this library's own master specification, not an external-standard quotation. Per §6.1 (lines 277-280) of the same document, added in PR #159 closing FR-44, "shall" / "shall not" is reserved for (a) direct quotation of external standards or (b) legacy content awaiting harmonization. The same document's §4.1 rule 2 was authored internally and is recent enough that it should use "must" / "must not" under the new convention. The spec violates the rule it defines. Even under the legacy-grandfather clause, having the spec itself ship with one "shall" while declaring the convention is the highest-visibility instance of the corpus-wide harmonization debt now tracked as FR-44-generalisation in TODO.
```

```
tool: subagent-B
ruleId: stale-pr-reference
level: note
location: TODO.md:22
fingerprint: stale-pr-reference:todo.md:22
rubric: in-window
evidence: |
  "**Next, PR #N: First fitness-remediation PR (maintainer-directed).** … **No remediation begins until the maintainer directs.**"
  PRs #155-#159 are fitness-remediation PRs (FR-1, FR-2, FR-16, FR-80, FR-44 close-outs); earlier PRs #142-#147 were also remediation work. The framing "first fitness-remediation PR" and the closing assertion "no remediation begins until the maintainer directs" are now stale relative to in-window state: remediation began (with maintainer direction) and many such PRs have shipped during recent windows. The bullet should be reframed to describe the next remediation pick, not the first one.
```

**Subagent B summary**: 2 findings (1 warning, 1 note). 7 stale-reference classes searched (stale gate counts; stale library/README version pins; stale PR references; "as of" dates; FR-44 §6.1 carve-out vs spec's own normative prose; pack-sync drift between `dev-security/claude-rules/` and `.claude/rules/`; TODO backlog-totals arithmetic; FR-16 cross-reference target existence; skill-step counts). Most surfaces clean, pack-sync verified, TODO backlog tier counts arithmetic verified (12 + 8 + 56 = 76), FR-16 cross-reference targets resolve. The master-spec "shall" finding overlaps with Subagent A; the TODO finding is independent.

## Subagent C, Audit-programme integrity reviewer

**Subagent C return**: 0 findings.

All checks clean across the audit-programme integrity surfaces:

| Check | Result |
|---|---|
| Gate count, four surfaces | All agree on 46 corpus gates |
| `tools/run_all_audits.sh` | 46 `run_gate` invocations (lines 68-144) |
| `.github/workflows/quality.yml` | 46 audit script calls + D1/D2/informational detector |
| `.pre-commit-config.yaml` | 47 hook ids = 46 gates + 1 helper hook (`regenerate-derived-artefacts`), expected per gate 35 |
| `governance/specification-audit-programme.md` §6 | Rows 1-46 match runner |
| Spec metadata | Version 1.14.0, Date 2026-06-21 (current) |
| Linter docstrings spot-checked | `lint-cross-doc-numbers.py`, `lint-overnight-file.py`, `lint-followup-ageing.py`, accurate |
| `tools/run-pr-time-checks.sh` | D1, D2, gate 45 wiring correct |
| Free-form gate-count references | All "46 gates" current; older counts only in `.working/` (exempt) |
| Pack version 1.34.0 | Unchanged |

**Future-gate candidates (non-findings, not actioned this iteration)**:

- **Ordinal-ceiling pattern.** Both PR #157 (FR-16) and PR #152 (FR-19) introduced a "1st/2nd/3rd/4th prohibited" ordinal ceiling with re-baselining carve-outs. Second occurrence. Recommendation: defer to a third occurrence before codifying as an invariant (don't-generalise-until-third-use principle from the addyosmani overlay).
- **Numerical-coherence pattern set extension.** `tools/lint-cross-doc-numbers.py` would have caught FR-80 if retention periods were a tracked pattern. Recommendation: a future Phase 23-style empirical analysis to identify retention-related canonical terms before extending the pattern set.

## Orchestrator synthesis

### Dedupe and classification (5.1 / 5.2)

Three findings A surfaced + two findings B surfaced = 5 raw. Dedupe by `(file_path, normalised_section, claim_type)`: A's `specification-master-project.md:126` (ruleId `multi-surface-incompleteness`) and B's `specification-master-project.md:126` (ruleId `stale-skill-description`) target the same line and same claim (the spec violates the convention it defines). Dedupe to one finding, ruleId `multi-surface-incompleteness` (A's framing is more precise about the failure-mode class). **Net unique findings: 4.** All four tagged **R** (Real, observed directly with quoted file content).

### Failure-mode classification (C1-C8)

- 3× FR-44-self-violation (spec line 126 + policy lines 88, 94) → **C3 multi-surface incompleteness** (convention introduced and contradicted in the same multi-PR batch; rule was authored in one PR while new normative content using the disallowed construct was authored in a sibling PR).
- 1× stale-PR-framing (TODO.md:22) → **C1 stale prose reference** (queued-work narrative wrote down state that has since advanced; gate 45 catches sweep-cursor drift but does not cover free-form queued-work prose).

### Severity adjudication (5.3)

All four findings are **in-window** (introduced or surfaced in the focus window). All four are fixable in the close-out PR. Severity: should-fix-this-PR for all four. No findings rise to must-fix-before-merge (none block CI), no findings deferred to track-as-follow-up (all are surgical).

### Subagent provenance (5.4)

- `specification-master-project.md:126`, A and B (deduplicated)
- `policy-exception-and-risk-acceptance-management.md:88`, A
- `policy-exception-and-risk-acceptance-management.md:94`, A
- `TODO.md:22`, B

### Debate (5.5)

No divergence between subagents on the deduplicated finding (A and B independently surfaced the same defect with consistent framing). No adjacent severity disagreements. No real-vs-false-positive disputes. No debate round invoked.

### Dispatch declaration (5.6)

Subagents A, B, and C all dispatched per the full-sweep discipline. No thin-sweep exception invoked.

### Action decided per finding

All four findings fixed in this close-out:

1. `specification-master-project.md:126` "No directory shall contain non-canonical document types." → "Directories must not contain non-canonical document types." (Per-doc 1.6.0 → 1.6.1.)
2. `policy-exception-and-risk-acceptance-management.md:88` "A 4th renewal may not be granted" → "A 4th renewal must not be granted". (Per-doc 1.1.0 → 1.1.1.)
3. `policy-exception-and-risk-acceptance-management.md:94` "Re-baselining may not be used to bypass" → "Re-baselining must not be used to bypass". (Same per-doc bump.)
4. `TODO.md:22` "Next, PR #N: First fitness-remediation PR" framing reworked, narrative now reflects that 21 fitness-remediation PRs have shipped under maintainer direction; the queued sequence describes the next batch, not the first.

### Future-gate candidates (Subagent C non-findings)

Recorded for future consideration; not actioned this iteration.

## Resulting PR

[#160, Sweep 14 iter 1 close-out: FR-44-self-violations + TODO queued-sequence refresh](https://github.com/jposluns/grc_library/pull/160).
