# Sweep 16, Iteration 1, Detail

## Trigger & state snapshot

**Trigger.** Maintainer-directed sweep prior to launching the next FR PR (P1.4b, FR-33 high[critical] GDPR Article 36 prior consultation). The maintainer flagged during PR #179 close-out that "there haven't been many issues found lately" and that the last sweep was Sweep 15 at PR #167, 12 PRs ago (well past the documented 5-PR cadence and 1-8-PR amended cadence).

**State at sweep launch.**
- Library Version: `2026.06.159` (post-PR #180 merge).
- Pack Version: `1.37.0` (post-PR #180 merge).
- Governance-rule count: **8** (gate-discipline, change-tracking, evidence-grounded-completion, clarify-before-acting, artefact-and-branch-discipline, action-before-explanation-of-inaction, validate-inference-before-action, ai-assistant-workflow-disciplines).
- Gate count: **46** (per `tools/run_all_audits.sh`).
- Skill count per `dev-security/claude-rules/skills/`: 13 (verified via `ls` not directly but via Subagent B's review).

**Iteration number.** 1 (initial dispatch; no re-baseline yet).

**Sweep window.** PRs #168 through #180 (13 PRs since Sweep 15's PR #167):
- #168: Sweep 15 follow-up (BASC 5-level classification)
- #169: P1.3 access-control (FR-26+27+28)
- #170: Three-surface version-bump rule
- #171: PR-subscription + 60s-timer discipline
- #172: P1.1 README polish (FR-4+5+6+7+8)
- #173: CHANGELOG backfill for #170/#171
- #174: Retire Changelog: skip; terse-entry; DONE battle-text
- #175: Shorten historical DONE entries
- #176: Workflow-disciplines pack rule (added 8th governance rule)
- #177: Phase 1/2 plan rotation into TODO
- #178: P1.2 ERM (FR-11+12)
- #179: P1.4a small singletons (FR-18+25+79+105+106+110); gate-31 catch
- #180: Extend version-bump discipline to four surfaces (Date)

## Subagent A, Recent-PR deep review

```
- tool: subagent-a
  ruleId: stale-prose-references
  level: warning
  location: TODO.md:22
  fingerprint: stale-prose-references:TODO.md:22
  rubric: cross-document-prose-drift, in-window
  evidence: |
    Line 22: "Fitness-remediation PRs are now in flight under maintainer direction. PRs #142-#176 have closed 34 findings to date (most recently PR #169 FR-26+27+28, PR #172 FR-4+5+6+7+8..."
    
    The TODO.md "Session resume metadata" section (line 13) correctly states "Branch at last refresh: main (synced after PR #180 merge)" but the narrative prose on line 22 still references only PRs #142-#176 despite four additional PRs (177, 178, 179, 180) having shipped. PR #180 updated the library version snapshot (2026.06.159), pack version (1.37.0), and README version (1.9.30) but did not refresh the PR-range citation that follows. The count of closed findings (34) also appears low given the subsequent PRs: #177 (meta), #178 (2 findings), #179 (6 findings), #180 (meta).
```

**Subagent A summary**: 1 finding, warning-level, in-window. The class is `stale-prose-references` (catalogued failure mode C-1) where the narrative prose drifted from the structural session-resume metadata above it. Coverage statement: 6 of 14 PRs reviewed in depth (PRs #180, #179, #178, #177, #176, #172); the rest examined at the stat / cross-reference level.

## Subagent B, Corpus-wide stale-reference sweep

```
- tool: subagent-b
  ruleId: stale-skill-parent-count
  level: error
  location: dev-security/claude-rules/skills/skill-authoring-discipline/SKILL.md:26
  fingerprint: stale-skill-parent-count:dev-security/claude-rules/skills/skill-authoring-discipline/SKILL.md:26
  rubric: stale-count, skill-authoring, governance-rules
  evidence: |
    Line 26: "Every pack skill derives from one of the **seven governance rules** in `dev-security/claude-rules/governance/`."
    
    The skill-authoring-discipline SKILL.md says skills derive from one of seven governance rules, but the pack now ships eight governance rules (gate-discipline, change-tracking, evidence-grounded-completion, clarify-before-acting, artefact-and-branch-discipline, action-before-explanation-of-inaction, validate-inference-before-action, ai-assistant-workflow-disciplines). This is a stale count that slipped past gate 41 (Collection-enumeration consistency audit) because the skill text is not itself a directory enumeration, it's prose narrative. The prose should say "eight governance rules".
```

**Subagent B summary**: 1 finding, error-level, in-window (caused by PR #176's incomplete update). The class is `stale-prose-references` (failure mode C-1) intersecting with multi-surface incompleteness (C-3). Coverage: corpus-wide grep patterns on gate counts, governance-rule counts, skill counts, pack version, library version, citation drift, two-file CHANGELOG lock-step. No discoveries on stale library version (2026.06.159 correct), stale pack version (1.37.0 correct), or stale ISO/IEC / NIST citations.

## Subagent C, Audit-programme integrity reviewer

**Subagent C summary**: 0 findings. High-confidence clean bill (95%). All 46 gates pass; 4-surface gate parity confirmed; 12 rule pairs in MIRROR_MAP synced byte-for-byte; D1 gate's back-compat acceptance of `Changelog: skip` trailer matches the documented state; gate 39 cross-file gate-count consistency clean. No future-gate candidates identified by Subagent C; the future-gate candidate documented below was synthesised by the orchestrator from the A and B findings' shared pattern.

## Orchestrator synthesis

### In-window classification

Both findings (A1 and B1) are in-window:
- A1 caused by PR #177 (TODO rotation), orchestrator updated structural surfaces but missed paired narrative.
- B1 caused by PR #176 (8th governance rule), orchestrator updated inventory tables but missed paired skill narrative.

### Failure-mode class (C-1 through C-8)

Both findings map to **C-1 stale-prose-references** AND **C-3 multi-surface incompleteness**. The two classes overlap here: the prose is stale because a paired surface was updated without updating it. The structural pattern is identical: orchestrator updates the primary surface where a count or PR-range lives, misses the paraphrase elsewhere in the corpus.

### Severity adjudication

Both `should-fix-this-PR`. Per the rubric, when subagents flag findings at warning and error level and the fix is small, the adjudication is `should-fix-this-PR` rather than `must-fix-before-merge` (the gates pass; the corpus ships consistent in everything mechanically checked).

### Dedupe

No dedupe required (A1 and B1 are distinct files, distinct surfaces).

### Debate

No debate triggered. No divergence between subagents (A handled recent PRs, B handled corpus-wide; their findings are independent and complementary).

### Subagent provenance recorded

A1: Subagent A only. B1: Subagent B only. C: 0 findings reported.

### Decided action per finding

- **A1**, fix in this PR. Refresh TODO.md:22 narrative to current state.
- **B1**, fix in this PR. **Maintainer-directed adjustment**: rather than "seven → eight" (re-count), the fix genericizes the prose to "one of the governance rules under `dev-security/claude-rules/governance/`", removes the count entirely. The directory listing is the canonical authority; the count in prose was the drift-prone paraphrase. The broader pattern (count-in-prose where the directory or table is the canonical authority) is queued as a follow-up corpus-wide sweep PR.

### Future-gate candidate (orchestrator synthesis)

A **corpus-wide narrative-vs-current-state cross-reference audit** would catch this class mechanically. The candidate gate would: (a) maintain a registry of "current-state counts" the corpus cites in prose (governance rules, skills, gates, linters, PR range, finding count); (b) scan markdown prose for numeric phrases matching those classes; (c) flag where the prose-cited count differs from the actual count. The cost is moderate (gate logic + registry maintenance); the benefit is closing the C-1/C-3 intersection class that's been the dominant in-window finding shape across recent sweeps.

**Better alternative (maintainer-directed during this sweep)**: rather than building a gate, **genericize counts-in-prose** wherever the directory or table is the canonical authority. This is what the B1 fix demonstrates. Queued as a follow-up corpus-wide PR. The cost is one cleanup pass; the benefit is making the corpus self-maintaining for this class.

### Cross-check against false-positive memory

No false-positive matches in the sweep history register. Both findings are first-time occurrences of these specific drifts.

## Resulting PR

[PR #181, Sweep 16 iter 1 close-out](https://github.com/jposluns/grc_library/pull/181).
