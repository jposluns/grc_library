# /validate Sweep 19, Iteration 1

**Date:** 2026-06-22\
**Trigger:** Maintainer invocation of `/validate` after the merge of PR #219.\
**Scope:** Corpus-wide regression sweep covering changes since Sweep 18 iteration 1 (PRs #186-#214).

## Trigger and state snapshot

- **Library Version** at HEAD: `2026.06.197` (post PR #219 merge `313e362`).
- **Pack version**: `1.45.1`.
- **Governance-rule count**: 8 in [`dev-security/claude-rules/governance/`](../../dev-security/claude-rules/governance/).
- **Audit gate count**: 46 in [`tools/run_all_audits.sh`](../../tools/run_all_audits.sh) and [`governance/specification-audit-programme.md`](../../governance/specification-audit-programme.md) §6.
- **Skill count**: 13 in [`dev-security/claude-rules/skills/`](../../dev-security/claude-rules/skills/) (12 skill subdirectories + 1 PAIRS registry).
- **PR range covered**: PR #215 (record /validate Sweep 18 iter 1), PR #216 (hot-fix PR #215 /validate-pr findings), PR #218 (FR-46 DPO consolidation canonical flip, 84 files), PR #219 (at-top notes in 24 docs + PR #218 /validate-pr fixes). PR #217 was closed unmerged and not in main history.
- **Mechanical baseline**: `tools/run_all_audits.sh` exits 0 on all 46 gates pre-sweep.
- **Pre-flight scanner**: `python3 tools/sweep-preflight-scanner.py` reports 373 files scanned, 0 candidate findings (19 candidates suppressed: 13 by heuristic, 6 by exemption file).
- **Iteration**: 1 (no previous iteration in this sweep). Re-baseline iter 2 verification: corpus-wide `\bCPO\b` grep confirmed clean post-fix (all remaining occurrences are intentional: at-top notes, canonical surfaces, TODO/CHANGELOG historical, `.working/` archives).

## Subagent A, Recent-PR deep review

**Scope:** PRs #215-#219 diffs, looking for the 8 failure-mode classes.

**Return: 2 findings.**

```
- tool: subagent-a
- ruleId: TERM-DRIFT
- level: warning
- location: governance/guideline-minimum-viable-governance-structure.md:67
- fingerprint: term-drift:governance/guideline-minimum-viable-governance-structure.md:67
- evidence: "Named seats: the executive forum should still record decisions against the formal roles the library uses (CISO, CIO, CRO, CCO, CPO, etc.). One person may wear multiple role hats; in that case, record which role's authority a given decision invokes."
- impact: PR #218's stated scope was to flip the canonical privacy-lead role from CPO to DPO "across 73 corpus files". This guideline file lists "CPO" as the canonical privacy-lead abbreviation in an executive role enumeration, the canonical surface now defines DPO as canonical, so the enumeration should read "DPO" (or equivalent). Adopter reading this guideline alongside `register-role-authority.md` sees contradictory canonical statements.
- recommendation: Replace "CPO" with "DPO" in the executive-role enumeration on lines 67 and 114; bump per-doc Version + Date.
- classification: in-window (PR #218's stated DPO consolidation scope missed this corpus file)
```

```
- tool: subagent-a
- ruleId: TERM-DRIFT
- level: warning
- location: governance/guideline-minimum-viable-governance-structure.md:114
- fingerprint: term-drift:governance/guideline-minimum-viable-governance-structure.md:114
- evidence: "| Senior executive | CEO/equivalent, CIO, CISO, CRO, CCO, CPO, CTO, CFO, CHRO, General Counsel, Chief Audit Executive | One or two people wearing several hats |"
- impact: Same as line 67, the canonical privacy-lead in the library is now DPO post-PR #218; this senior-executive enumeration still names CPO, creating cross-document contradiction with `register-role-authority.md`, `register-glossary.md`, and `privacy/README.md`.
- recommendation: Replace "CPO" with "DPO" in the senior-executive row; bump per-doc Version + Date in the same commit. Closing this finding alongside the line-67 fix.
- classification: in-window (PR #218's stated DPO consolidation scope missed this corpus file)
```

### Subagent A negative-space (no-finding) verifications

- At-top note wording byte-identical across 24 docs (spot-checked privacy/, ai/, supply-chain/, security/, governance/).
- Relative path depth correct per directory (`../governance/` for files one level deep; `register-role-authority.md` for governance/ files).
- PR #218 /validate-pr fixes landed correctly: `tools/build-portal.py:95` no double-DPO; `risk/register-key-risk-indicators.md:142` reads "DPO".
- `docs/portal.md:411` regenerated correctly.
- Generated artefacts (portal/scorecard/taxonomy) in sync (`--check` clean).
- Per-doc Version bumps on all 25 corpus files PR #219 touched.
- CHANGELOG root vs detailed mirror in sync for PR #219 entry.
- Three canonical surfaces clean.
- `build-portal.py` docstring not stale.

## Subagent B, Corpus-wide stale-reference sweep

**Scope:** Entire corpus (not just recent PRs); 8 failure-mode classes with focus on cross-document references and counts.

**Return: 0 findings (corpus-wide stale-reference sweep clean).**

### Subagent B verified

- 8 governance rule files in `dev-security/claude-rules/governance/` (matches CLAUDE.md and pack README).
- 13 skills in `dev-security/claude-rules/skills/`.
- 46 audit gates in `tools/run_all_audits.sh` (matches §6 row count in spec, matches non-`.working/` CHANGELOG entries).
- FR-46 DPO consolidation post-PR #218/#219: all spot-checked privacy/ ai/ supply-chain/ security/ governance/ files have CPO/Chief Privacy Officer confined to the at-top adopter-variant note (line 19) as expected.
- PR #219 CHANGELOG entry quoted excerpts verified against the actual file states.
- Canonical citations register at v1.4.23: NIST SP 800-53 Rev. 5, ISO/IEC 27001:2022, ISO/IEC 27701:2025, ISO/IEC 42006:2025 all current.
- Glossary acronyms CISO, DPO, CRO present with appropriate definitions.
- The fitness review `.working/fitness-reviews/2026-06-22-r1.md` says "12 skills" (actual is 13, `pr-retrospective` added in PR #213). NOT a finding because the file is `.working/`-exempt and accurate as-of write-time per `.working/README.md` convention.

## Subagent C, Audit-programme integrity reviewer

**Scope:** Four parallel surfaces of the audit programme (workflow / runner / pre-commit / spec); skill-pack PAIRS registry; linter docstrings.

**Return: 0 findings (audit-programme integrity clean).**

### Subagent C verified

- Gate 35 (gate-name parity) passes: 46 gates across all four surfaces.
- Gate 39 (cross-file gate-count consistency) passes: "46 gates" consistent across 427 files.
- Gate 41 (collection-enumeration consistency) passes: 2 collections, 4 enumeration locations.
- Spec §6 inventory has 46 numbered rows matching the canonical count.
- `tools/run-pr-time-checks.sh` correctly invokes D1, D2, and gate 45.
- PAIRS registry (4 pairs) matches the 4 files in `.claude/commands/` (fitness, retro, validate, validate-pr).
- 13 skills in `dev-security/claude-rules/skills/`, 9 are skill-only (no slash-command counterpart by design), 4 are paired; PAIRS captures the 4 paired ones.
- CLAUDE.md enumerates 8 governance rules; 8 .md files exist in `dev-security/claude-rules/governance/`.
- Recently-modified linters (`tools/build-portal.py`, `tools/lint-internal-references.py`, `tools/lint-pii-in-content.py`, `tools/lint-paired-skill-step-parity.py`), all docstrings still accurate.

## Orchestrator synthesis

### Dedupe + tagging

- Finding 1 (line 67) and Finding 2 (line 114) are **separate occurrences** of the same `(file_path=governance/guideline-minimum-viable-governance-structure.md, normalised_section=executive-role enumeration, claim_type=canonical-role-acronym)` tuple. They are NOT dedupable (different `path:line`), but they are coherent (same root cause, same file, same fix).
- Tags: both findings tagged `R` (Real). Not `I` (Insurance) or `K` (Known issue).

### Severity adjudication

- Both Subagent A findings: **warning** (not error), the canonical surfaces are correct (register, glossary, privacy/README); only the guideline file's enumeration is stale. The drift is real but does not break adopter behaviour at the canonical surface; it would mislead an adopter who reads this guideline as their first reference.
- Per the severity ladder (`must-fix-before-merge | should-fix-this-PR | track-as-follow-up`): both fall in `should-fix-this-PR` since they are in-window per Subagent A's classification (PR #218's stated corpus-wide DPO scope missed this file) and the fix is bounded (1 file, 2 lines).

### Provenance

- Subagent A (Recent-PR deep review): 2 findings.
- Subagent B (Corpus-wide stale-reference sweep): 0 findings.
- Subagent C (Audit-programme integrity): 0 findings.

**All three subagents dispatched (per the discipline; no skips).**

### Cross-subagent debate

No divergence. Subagent B's spot-checks did not include `governance/guideline-minimum-viable-governance-structure.md` (the file's prose enumeration is not in Subagent B's typical scope, it's an internal-corpus reference, not a cross-document citation or external-standard reference). Subagent A's recent-PR-deep-review scope catches it because PR #218's stated corpus-wide scope flagged this file as in-window even though the file itself was not in PR #218's diff.

### In-window vs out-of-window classification

Both findings classified **in-window** by Subagent A on the basis that PR #218's stated scope was corpus-wide DPO consolidation, and this file should have been touched (or the rename script should have included the bare acronym in its substitution list, which would have caught it). Same root cause as PR #218 /validate-pr Finding 2 (`risk/register-key-risk-indicators.md:142`): the rename script's substitution list was scoped to spelled-out forms only.

### Iteration 2 verification (sweep close-out)

After fixing both findings in this iteration's commit, the orchestrator ran a corpus-wide `\bCPO\b` grep to verify no remaining occurrences outside the intentional locations (at-top notes, three canonical surfaces, TODO/CHANGELOG historical, `.working/` archives). Result: clean. The pre-flight scanner had already returned 0 candidate findings; Subagents B and C had already returned 0 findings; the only finding-producing surface was Subagent A's recent-PR deep review, and its 2 findings are now fixed. Re-dispatching all three subagents for iter 2 would have been corroboration-only per the pre-tool-preamble's skip-corroboration rule (no new hypothesis to test that a grep doesn't already answer). **Iter 2 verified via direct grep; 0 new findings; sweep terminates.**

## Resulting PR

PR #220 (link added upon open), Sweep 19 iter 1 close-out. Carries:

- Fix for Finding 1 and Finding 2 in `governance/guideline-minimum-viable-governance-structure.md` (CPO → DPO on lines 67 and 114); per-doc Version `1.0.1 → 1.0.2`, Date `2026-06-02 → 2026-06-22`.
- This per-iteration detail file (`.working/validate-sweeps/2026-06-22-sweep19-iter1.md`).
- Row added to `.working/validate-sweeps/history.md` for Sweep 19 iter 1.
- The PR #219 /validate-pr history row (carried per the recursion-avoidance rule, since PR #220 is the next substantive PR after PR #219).
- The PR #219 /retro register row (same reason).

## Notes

- **Pattern signal (second occurrence)**: Subagent A's two findings here are the SECOND occurrence of the rename-script-bare-acronym-missed pattern (first was PR #218 /validate-pr Finding 2). The /retro register row for PR #219 already flagged the worker-brief template candidate (require both spelled-out AND acronym substitution lists in corpus-wide rename scripts). This sweep's pattern observation strengthens that candidate from "first occurrence" to "second occurrence (signal stage)" per the three-occurrence progression.
- **Out-of-scope observation reconciled**: PR #218's /validate-pr record (`.working/validate-pr/2026-06-22-PR-218.md`) noted these two lines as "out-of-scope but noted, will surface in the next corpus-wide /validate sweep as out-of-window". The /validate sweep has now surfaced them; their classification is in-window per Subagent A's stated-scope reasoning, not out-of-window. The earlier "out-of-window" classification was strict per the PR #218 touched-file set; the /validate sweep applies a broader frame (PR #218's stated corpus-wide scope) which captures more.
