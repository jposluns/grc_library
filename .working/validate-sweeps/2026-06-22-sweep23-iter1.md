# Validation Sweep 23, iteration 1

## Trigger & state snapshot

Triggered by the `/resume` command's step 5: a fresh session's first substantive task is a full corpus-wide `/validate`, the maintainer-authorised compensating control for the session-closing handoff PRs (#248, #249) that intentionally skip their trailing `/validate-pr` to break the post-merge validate-then-PR loop (CLAUDE.md PR-workflow step 5a handoff-PR exception). This corpus-wide sweep is stronger than the skipped per-PR sweeps because it re-examines the whole corpus rather than just the handoff PRs' diffs.

State at HEAD (`1a4da1e`, PR #249 squash merge on `main`, branch `claude/resume-oojneh`):
- Library version `2026.06.227`; README version `1.9.98`; pack version `1.47.0`.
- Governance rules: 9. Skills: 14 (5 paired with slash commands). Audit gates: 46.
- Clone: container started shallow (`git rev-parse --is-shallow-repository` = `true`); `git fetch --unshallow` run before any history-aware audit, per the full-clone methodology rule. Now `false`.
- Mechanical baseline: `tools/run_all_audits.sh` → all 46 gates pass.
- Pre-flight scanner: 376 files scanned, 2 canonical collections tracked, 19 candidates suppressed (13 heuristic, 6 exemption file), 2 candidate findings surfaced (both triaged as false positives below).

This is iteration 1 (not a re-baseline).

## Subagent A, Recent-PR deep review

Scope: the 7 most recent merged PRs (#243, #249), the trust-recovery codification, session-migration protocol, and closing-handoff PRs whose per-PR `/validate-pr` was skipped.

**0 findings (0 error, 0 warning, 0 note).** Both pre-flight candidates confirmed FALSE POSITIVES:
- `dev-security/claude-rules/governance/trust-recovery-escalation.md:26` "a suite of two skills", "two" is the trust-recovery suite size (deep-qa-review + library-fitness-review), orthogonal to the six subagents and ten personas. The three count systems (2 / 6 / 10) are used consistently.
- `dev-security/claude-rules/skills/deep-qa-review/SKILL.md:15` "The two skills run as a suite", same; false positive.

Verified consistent (with evidence): subagent-count / step-identifier (0-6) / routing / sign-off parity across the three trust-recovery surfaces (rule, SKILL, `/full-qa` command); governance-rule count = nine everywhere; the `.claude/rules/governance/trust-recovery-escalation.md` mirror byte-identical to pack source (gate 37 passes); skill count = 14; version chains strictly monotonic and contiguous (Library .220→.227, README 1.9.91→1.9.98, pack 1.45.2→1.47.0) with README bumped in lock-step per PR; all seven PRs carry root + detailed CHANGELOG entries; `A&A-04` CSA CCM domain ID in the new rule's framework table is real and used elsewhere; the #249 loop-break narrative agrees across CLAUDE.md step-5a, the closing-handoff-PR-discipline section, and `resume.md` step 5. High confidence: the deferred per-PR `/validate-pr` skips left no defects detectable in this scope.

## Subagent B, Corpus-wide stale-reference sweep

Scope: the whole corpus, prose-level semantic stale-reference and cross-document drift the mechanical gates do not catch.

**1 finding (1 note):**

```
tool: subagent-B
ruleId: stale-prose-count-self-reference
level: note
location: .claude/CLAUDE.md:263
fingerprint: stale-prose-count-self-reference:.claude/claude.md:263
rubric: stale-count-in-prose (ungated; collection-enumeration gate 41 covers structured enumerations only)
evidence: Line 263 reads `prose counts like "the eight governance rules" are not gated`. Current truth is nine governance rules (verified: 9 .md files in dev-security/claude-rules/governance/; tools/lint-collection-enumeration-consistency.py:5 itself says "the pack's nine governance rules"). The sentence is deliberately illustrating the form of an ungated prose count, but it picks this project's own collection and states a wrong number. Because it is example prose in a gate-exempt tree (.claude/), severity is note. Fix: change "eight" to "nine".
```

Confirmed-but-ALREADY-TRACKED (deduped, NOT re-routed): AI-risk treatment-option vocabulary `ai/register-ai-risk.md:44` (FR-161) and `risk/annex-ai-risk-methodology.md:113` ("Reduce" non-canonical) (FR-163); TLS 1.2-vs-1.3 floor residuals across six surfaces (FR-135); risk-scoring bands / log-and-DSAR retention / CPPA-in-force drift (FR-134, FR-136, FR-137, FR-138, FR-139). All present in the live corpus, all in the backlog.

Verified NOT stale: `tools/lint-collection-enumeration-consistency.py:5` "nine governance rules" (correct); "ten persona" references in `library-fitness-review/SKILL.md` (catalogue genuinely lists 10, co-located); "46 gates" references in TODO.md and `governance/specification-audit-programme.md` §6 (correct); historical "42/32 gates" and "ten skills (was seven)" in CHANGELOG.md (append-only, accurate-to-time).

## Subagent C, Audit-programme integrity reviewer

Scope: integrity and internal consistency of the audit programme across the four parallel surfaces and the recently-touched tooling.

**0 findings.** Verified (with evidence): live gate count = 46 (`run_all_audits.sh` prints [1]…[46]); four-surface script parity exact (`quality.yml`, `run_all_audits.sh`, `.pre-commit-config.yaml` invoke identical script sets); §6 spec inventory has exactly 46 numbered rows ending at gate 46 (Overnight-work file audit), no dangling script paths; governance rules = 9 and skills = 14 consistent across `.claude/CLAUDE.md`, pack `CLAUDE.md`, and pack README tree; `lint-claude-rules-sync.py` MIRROR_MAP (line 91) maps trust-recovery-escalation.md and gate 37 passes; `lint-collection-enumeration-consistency.py:5` docstring corrected to "nine"; `lint-paired-skill-step-parity.py` PAIRS (lines 67-68) includes deep-qa-review ↔ full-qa with matching steps 0-6 and gate 44 passes; `nightly-sweep.yml` references only existing targets (`run_all_audits.sh`, `sweep-preflight-scanner.py`); `lint-gate-count-consistency.py` (gate 39) derives the canonical count dynamically from the §6 table row count, robust against drift.

## Orchestrator synthesis

- **Dedupe**: the only non-deduped, non-tracked finding is Subagent B's `stale-prose-count-self-reference` at `.claude/CLAUDE.md:263`. Subagent A independently noted the same string as an intentional-illustration and refuted it as a defect; the orchestrator adjudicates it a genuine (if trivial) stale count, the string is factually wrong regardless of its illustrative intent. No real-vs-false-positive divergence wide enough to invoke asymmetric debate (both subagents agree on the facts; they differ only on whether "illustrative" excuses the wrong number, and the parent rules it does not).
- **Classification**: C-? stale-count-in-prose. In-window (the close-out checklist containing this line was added in PR #247, by which time the governance-rule count was already nine, so the count was stale at authoring time).
- **Severity adjudication**: `track-as-follow-up` would understate it; `should-fix-this-PR` is right, it is a one-word correction with no downstream coupling. Fixed in this sweep's close-out (the resume bundle).
- **False positives**: 2 pre-flight candidates confirmed-refuted ("two skills" suite size). Recorded so a future sweep does not re-surface them; both are legitimate comparative wording, not count-drift.
- **Already-tracked**: the cross-document drift cluster (FR-134..139, FR-161, FR-163) is in the live corpus but in the P1/P2 backlog from the trust-recovery passes; reported by Subagent B for dedupe, not re-routed.
- **Action decided**: apply the one-word fix (`eight` → `nine`) at `.claude/CLAUDE.md:263`; no backlog routing required (in-window, fixed). Subagents dispatched this iteration: A, B, C (all three, no skips).
- **Termination**: after the fix is committed, the corpus is clean of High/Medium findings (the single finding is note-level and fixed). Empty-delta primary stop applies on re-baseline.

## Resulting PR

Bundled into the session-resume close-out PR (the next substantive PR), per the recursion-avoidance batching rule for findings-producing-but-minor sweeps. Link recorded in the history row.
