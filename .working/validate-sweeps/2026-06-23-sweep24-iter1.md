# Validate Sweep 24, Iteration 1 (2026-06-23)

## Trigger & state snapshot

- **Trigger**: `/resume` step 5 — the full corpus-wide `/validate` that runs as the first substantive task of a new session, the compensating control for the session-closing handoff PR (#255) that intentionally skipped its trailing `/validate-pr` (CLAUDE.md PR-workflow step 5a loop-break).
- **State at HEAD**: library `2026.06.233`, pack `1.47.2`, README `1.9.104` (pre-fix); governance rules 9; audit gates 46; skills 14; slash commands 7.
- **HEAD**: `50e54de` (PR #255 squash merge on `main`). Branch: `claude/sharp-wright-bemtql`.
- **Clone**: started shallow; `git fetch --unshallow` run before any history-aware audit (gates 31/40) per the full-clone methodology rule.
- **Iteration**: 1 (first iteration of Sweep 24).
- **Mechanical baseline**: `tools/run_all_audits.sh` → 46/46 clean.
- **Pre-flight scanner**: 376 files scanned, 19 candidates suppressed (13 heuristic, 6 exemption-file), 2 candidate findings (both `PF-collection-skills-word` "two" vs expected "14"), both triaged as false positives below.

## Subagent A, Recent-PR deep review

Scope: PRs #248–#255 (the focus window). Two findings, both the highest-priority multi-surface-incompleteness class.

```
tool: subagent-A
ruleId: multi-surface-incompleteness
level: error
location: dev-security/claude-rules/README.md:75
fingerprint: multi-surface-incompleteness:dev-security/claude-rules/readme.md:75
rubric: should-fix-this-PR
evidence: The governance/ directory-tree description of the trust-recovery rule reads "...the /full-qa + /fitness suite, every finding to backlog top, maintainer sign-off terminates". "every finding to backlog top" is the OLD pre-#252/pre-1.47.1 single-tier routing convention. PR #252 (pack 1.47.1) revised routing to severity-tiered; the canonical current routing (trust-recovery-escalation.md:40, deep-qa-review SKILL:3, pack CLAUDE.md, .claude/CLAUDE.md) is severity-tiered. Correct: "every finding routed tiered by severity".
```

```
tool: subagent-A
ruleId: multi-surface-incompleteness
level: error
location: dev-security/claude-rules/README.md:96
fingerprint: multi-surface-incompleteness:dev-security/claude-rules/readme.md:96
rubric: should-fix-this-PR
evidence: The skills/ directory-tree description of the deep-qa-review skill reads "...pairs with library-fitness-review; findings route to backlog top, maintainer sign-off terminates". "findings route to backlog top" is the same OLD single-tier convention superseded in 1.47.1. The skill's own frontmatter and body already state the severity-tiered form, so this README description contradicts the artefact it describes. Correct: "findings routed tiered by severity".
```

Dismissed (recorded, not routed): pre-flight candidates `trust-recovery-escalation.md:26` and `deep-qa-review/SKILL.md:15` ("a suite of two skills") — legitimate references to the 2-skill trust-recovery suite, not the 14-skill total. README:443 ("every confirmed finding to the backlog's top priority regardless of severity") — the 1.47.0 version-history row, accurate-to-time, correctly preserved. P1/P2-vs-tier dual naming — intentional and consistent (pack surfaces project-agnostic, project surfaces map to P1/P2). All enumerated counts (10 personas, 6 subagents, 9 rules, 14 skills, 46 gates) internally consistent.

## Subagent B, Corpus-wide stale-reference sweep

One finding.

```
tool: subagent-B
ruleId: stale-prose-enumeration
level: warning
location: dev-security/claude-rules/README.md:32
fingerprint: stale-prose-enumeration:dev-security/claude-rules/README.md:32
rubric: should-fix-this-PR
evidence: The "two areas" item-2 prose enumerates the pack's governance rules exhaustively (the closing "and AI-assistant workflow disciplines" signals an exhaustive list): "gate discipline, change-tracking discipline, evidence-grounded completion, clarify-before-acting, artefact-and-branch discipline, action-before-explanation-of-inaction, validate-inference-before-action, and AI-assistant workflow disciplines". That is 8 rules; the ninth shipped rule, trust-recovery-escalation.md (pack 1.47.0), is OMITTED. The same file's own directory tree (line 75) and version-history table (line 443) both include the ninth rule, so this is an internal inconsistency. Not caught by gate 41 (collection-enumeration), which checks the structured directory tree, not narrative prose. Fix: append the trust-recovery escalation tier as the ninth item.
```

Verified NOT stale (correct against canonical): `.claude/CLAUDE.md:263` "the nine governance rules"; pack CLAUDE.md rule enumeration (all 9); `lint-collection-enumeration-consistency.py` docstring "nine governance rules"; "46 gates" across spec and TODO; "ten persona briefs" / "six subagents" (within-skill counts). Excluded as out-of-scope: `.working/` (frozen archives), CHANGELOG narrative (append-only, accurate-to-time), `register-ai-security-tooling-landscape.md` "69 rules" (describes external promptmap2 ruleset, exempted).

## Subagent C, Audit-programme integrity reviewer

**Zero findings.** Read in full / targeted depth: `governance/specification-audit-programme.md` §5–§6 (gate inventory, gates 33–46 + D1/D2), `tools/run_all_audits.sh` (46 run_gate calls), `.github/workflows/quality.yml` and `.pre-commit-config.yaml` (gate rows 43–46), linter docstrings for gates 43/44 + the PAIRS registry, both trust-recovery SKILLs and the `full-qa`/`trust-recovery` command files against the rule. Confirmations: 46-gate lock-step across all four surfaces (gates 35/39 pass); severity-tiered routing prose uniform across `trust-recovery-escalation.md:40`, both SKILLs, and the command files (no "everything to one tier" residue in the machinery); enumeration consistent (6 deep-qa subagents, 10 fitness personas, 5 PAIRS pairs, `/trust-recovery` correctly absent from PAIRS as a wrapper). One non-finding observation: the two command files reference the rule via different but both-valid paths (pack source vs `.claude/rules/` local copy); gate 3 passes, acceptable layered-source referencing.

## Orchestrator synthesis

- **In-window**: all three findings. README:75 and README:96 are direct residue of the PR #252/#253 severity-tiered routing revision (which named the pack README as a propagation surface). README:32 is residue of PR #246's ninth-rule addition; technically the omission predates the 2-day window, but it is the same file, same fix-shape, and a one-line correctness fix — folded into the same close-out rather than surfaced as a separate question (the anti-auto-defer intent of step 6 is honoured by fixing it now, the strongest form of "not buried").
- **Classification**: README:75/96 = C3 multi-surface-incompleteness; README:32 = C1 stale-prose-reference (sub-shape: stale exhaustive enumeration).
- **Severity adjudication**: all three should-fix-this-PR (Medium). They misdescribe the pack to a reader scanning the README, but the authoritative rule/SKILL text is correct, so none is must-fix-before-merge.
- **Dedupe**: three distinct `(file, section, claim_type)` tuples; no dedupe collapse. No subagent disagreement; no debate round needed.
- **Subagent provenance**: A → README:75, README:96; B → README:32; C → zero.
- **Apply-time verification**: each finding re-read from its cited line before routing; a corpus-wide grep for "backlog top" / "route to backlog" / "regardless of severity" confirmed README:75/96 were the only live-corpus residues (version-history rows 442–444 and CHANGELOG:21/45/53 are accurate-to-time historical narrative, intentionally left intact; trust-recovery-escalation.md:40's "regardless of severity" is the correct text describing what the new form replaced).
- **False-positive memory**: the two pre-flight "suite of two skills" candidates were already dismissed in Sweep 23; re-confirmed here.
- **Action**: all three fixed in this close-out PR; pack version bumped 1.47.2 → 1.47.3.

## Resulting PR

PR #256 (this close-out).
