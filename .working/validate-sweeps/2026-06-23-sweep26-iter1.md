# Validation Sweep 26, Iteration 1

## Trigger & state snapshot

- **Trigger**: maintainer-invoked `/validate` on session resume the morning after the autonomous overnight run (PRs #259, #268). This is the compensating corpus-wide sweep that the session-closing handoff PR #268 deferred under the handoff-PR exception (CLAUDE.md PR-workflow step 5a): #268 skipped its own trailing `/validate-pr`, and the standing compensating control is that the next session runs a full `/validate` first.
- **State at HEAD** (`22c909f`, PR #268 merged): library version **2026.06.246**, pack version **1.49.2**, README version **1.9.117**, **10** governance rules, **15** skills, **8** slash commands, **46** audit gates.
- **Iteration**: 1 (single-iteration cycle; no re-baseline needed, see termination note in synthesis).
- **Mechanical baseline**: `tools/run_all_audits.sh` → all 46 gates pass.
- **Pre-flight scanner**: 378 files scanned, 22 candidates suppressed (16 heuristic, 6 exemption file), 9 candidate findings across 3 files, all 9 the words "two"/"five" in comparative or historical-narrative prose; all confirmed false positives by all three subagents.
- **Focus window**: the overnight run's substantive corpus changes, FR-134 through FR-141 plus Sweep 25 (PRs #259, #268).

## Subagent A, Recent-PR deep review

Scope: the FR-134-141 + Sweep 25 corpus documents. Read 6 documents in full (both FR-134 risk docs, the FR-135 quick-reference, the FR-136 logging standard, the FR-137 DSAR procedure, the guardrail-review SKILL) and grep-verified ~14 named/sibling documents at the changed surfaces, plus two corpus-wide consistency sweeps (`TLS 1.2`, `CPPA`).

**Finding A-1** (the only finding):
```
tool: subagent-A
ruleId: cppa-as-live-residual
level: warning
location: risk/standard-enterprise-risk-management.md:255, "| CPPA (Canadian Consumer Privacy Protection Act, Bill C-27) | As applicable to Canadian personal information |"
fingerprint: cppa-as-live-residual:risk/standard-enterprise-risk-management.md:255
rubric: out-of-window
evidence: The §10 framework-alignment row presents CPPA "as applicable to Canadian personal information" without the lapsed / not-in-force qualifier the canonical state requires (governance/register-canonical-citations.md:130 "Lapsed 2025-01-06 ... Bill C-27 died on prorogation ... never enacted; PIPEDA remains in force"). The file was edited in-window for FR-134 (risk-scoring), but FR-138's scope was locked to three named privacy docs + the ROPA template, with the broader CPPA-as-live sweep explicitly deferred to the maintainer. Classified out-of-window: pre-existing residue in the already-deferred broader-CPPA-sweep set.
```

Classes with NO findings (explicitly confirmed clean): stale prose references (FR-134 bands consistent across §5.2/§8.1/§9.x + register; FR-136 all three docs defer to schedule; FR-137 DSAR 3-year consistent; FR-139 1h RPO + backup cadence aligned); mis-attributed citations (FR-141 deadline cells correct; ROPA PIPEDA Sch 1 Principle 3 correct); multi-surface incompleteness (FR-135 all 5 named files fully migrated; FR-138 all 3 named docs + ROPA complete); inferred-as-verified; per-document version-bump omission (all 12 touched files carry Date 2026-06-23 + matching Version bumps); stale docstrings (guardrail-review growth narrative current); cross-document term drift (no other risk doc uses old Rare/Almost Certain labels; remaining TLS 1.2 hits all in deferred/already-tracked files, owasp/go deferred, cryptography prohibited-column correct, media-handling governed-exception pre-existing + tracked in the PR-261 validate-pr record).

## Subagent B, Corpus-wide stale-reference sweep

Scope: whole-corpus collection-count prose, version-literal references, cross-document term drift, external-standard citations. Confirmed authoritative inventory: 10 rules, 15 skills, 8 commands; pack 1.49.2, library 2026.06.246, README 1.9.117, all current. `lint-standards-currency.py` clean (no registered-standard version drift). Gate-count prose consistent at 46.

**Finding B-1** (dedupe with C-1):
```
tool: subagent-B
ruleId: stale-linter-docstring-collection-count
level: warning
location: tools/lint-collection-enumeration-consistency.py:20, "Phase 1 (this iteration): three hard-coded collections, each with one or more enumeration locations."
fingerprint: stale-linter-docstring-collection-count:tools/lint-collection-enumeration-consistency.py:20
rubric: out-of-window (apply-time-corrected from B's "in-window"; git blame shows line introduced 2026-06-20, commit d333661, gate-41 creation, outside the 2-day focus window)
evidence: The docstring says "three hard-coded collections" and its opening paragraph (L4-11) describes three drift-prone collections (governance rules, skills, external-overlay sources). The COLLECTIONS tuple (L91-132) implements exactly two (pack-governance-rules, pack-skills); the external-overlay collection was never wired. Runtime reports "2 collection(s)"; governance/specification-audit-programme.md:133 says "two hard-coded collections". The docstring's "three" is the stale value.
```

**Finding B-2**, REFUTED at apply-time (false positive):
```
tool: subagent-B
ruleId: stale-matrix-coverage-document-count
level: note
location: compliance/matrix-grc-compliance-alignment.md:151, "| CSA CCM v4.1 | All 42 documents | ..."
fingerprint: stale-matrix-coverage-document-count:compliance/matrix-grc-compliance-alignment.md:151
rubric: out-of-window (file last git-touched 2026-05-31)
disposition: REFUTED, apply-time count confirms "42" is correct.
evidence: Subagent B reported "the rows do not cleanly total 42 (38-43 depending on pattern)" and surfaced it LOW CONFIDENCE. Apply-time count (orchestrator): the matrix has 43 data rows across its 8 domain tables but exactly 42 UNIQUE documents, `risk/policy-enterprise-governance-and-risk-management.md` is mapped in both the Governance domain (L51) and the Risk domain (L63), inflating the raw row count to 43. The column header is "Primary Documents in This Matrix"; the count is of unique documents, which is 42. Every row carries a CSA CCM mapping and the broken-link gate is clean (all 42 paths resolve), so "All 42 documents" for the CSA CCM column is accurate. B's low-confidence flag was the duplicate row plus format variance throwing off its mechanical count. No fix; not routed.
```

## Subagent C, Audit-programme integrity reviewer

Scope: gate-wiring parity across the four surfaces, stale docstrings in recently-touched linters, governance-machinery enumerations, specification-audit-programme §6. Ran gates 35/37/39/41/44 live: 46-gate four-surface parity holds, §6 inventory matches the runner order exactly, §5 category lists cover all 46 with no gaps/dupes, gate-count prose consistent at 46 across 432 files, 10/10 governance rules in sync, 14 local rule copies in sync with pack sources.

**Finding C-1** (deduped with B-1, same fingerprint location):
```
tool: subagent-C
ruleId: stale-docstring-collection-count
level: warning
location: tools/lint-collection-enumeration-consistency.py:20
fingerprint: stale-docstring-collection-count:tools/lint-collection-enumeration-consistency.py:20
rubric: out-of-window
evidence: Identical to B-1. Docstring "three hard-coded collections" contradicts the code's two-entry COLLECTIONS tuple, the runtime "2 collection(s)" output, and spec §6 "two hard-coded collections". C additionally confirmed L5 correctly says "the pack's ten governance rules" (10 rule files present), so only the collection count is stale.
```

C confirmed the 9 pre-flight candidates are all comparative/narrative wording, not stale live counts.

## Orchestrator synthesis

- **Dedupe** (Rule 5.1): B-1 and C-1 are the same finding (`(tools/lint-collection-enumeration-consistency.py, docstring, stale-collection-count)`), reported independently by two subagents, corroboration, counted once. Net distinct candidate findings: 3 (A-1 CPPA-as-live, B-1/C-1 stale docstring, B-2 matrix count). After apply-time verification, **B-2 is refuted as a false positive** (the "42" is correct: 42 unique documents, 43 rows because one document is dual-domain-mapped), leaving **2 confirmed findings** (A-1, B-1/C-1).
- **R/I/K tags** (Rule 5.2): A-1 = R (real, verified at apply-time against canonical-citations:130). B-1/C-1 = R (verified against code+runtime+spec). B-2 = refuted (apply-time count confirmed 42 correct).
- **Severity** (Rule 5.3): the two confirmed findings are **track-as-follow-up** (A-1) and **should-fix** (B-1/C-1, trivial). None must-fix-before-merge.
- **In-window vs out-of-window**: **all three out-of-window.** A-1 file touched in-window (FR-134) but the CPPA row is in the maintainer-deferred broader-CPPA-sweep set; B-1/C-1 docstring predates the window (2026-06-20); B-2 file last touched 2026-05-31.
- **Rubric divergence** (Rule 5.5): B classed the docstring in-window, C classed it out-of-window. Adjudicated **out-of-window** via git blame (line introduced 2026-06-20). No asymmetric debate needed, divergence is a one-axis rubric disagreement, not a real-vs-false-positive or wider-than-one-severity divergence; the apply-time git-blame observation settles it.
- **Subagents dispatched** (Rule 5.6): A, B, C, all three, full briefs, parallel dispatch. No subagent skipped.
- **Pre-flight false-positive memory**: the "two skills / two rules / five-to-ten" comparative-and-historical candidates are the same class dismissed in Sweeps 23, 24, 25; consistent with the accept-list.
- **Key result**: **zero in-window regressions.** The overnight run (FR-134-141 + Sweep 25, PRs #259, #268) introduced nothing the sweep catches. The three findings are all pre-existing issues surfaced incidentally.

**Triage decision** (maintainer-directed on resume): per the out-of-window protocol (surface to operator with named options, do NOT auto-defer to Low/FYI), all three surfaced to the maintainer. Outcomes: (A-1) folds into the already-deferred broader-CPPA-sweep, left in that queue, not fixed piecemeal so the maintainer's scoping decision on how far to extend the CPPA-as-live cleanup is not pre-empted. (B-1/C-1) fixed in the cleanup PR (docstring "three" → "two", with a clarifying clause that the external-overlay sources are enumerated but not yet wired). (B-2) refuted at apply-time when the maintainer challenged the low-confidence count, the orchestrator counted unique documents (42; 43 rows, one dual-domain duplicate) and confirmed "42" is correct; no fix, recorded as a false positive.

**Termination**: single-iteration cycle complete. Empty-delta primary stop applies, zero in-window High/Medium findings, and all out-of-window findings surfaced and dispositioned. No fix-and-re-baseline loop required (B-1/C-1 is a tooling docstring with no corpus-gate effect).

## Resulting PR

Cleanup PR (this branch): B-1/C-1 docstring fix + the Sweep 26 history row + this detail file (with B-2 refuted). A-1 stays in the deferred broader-CPPA-sweep queue. Per recursion-avoidance, the sweep records batch into this same cleanup PR.
