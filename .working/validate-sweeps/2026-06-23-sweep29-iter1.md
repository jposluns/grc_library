# Validation Sweep 29 — iteration 1

## Trigger & state snapshot

- **Trigger**: `/resume` compensating-control corpus-wide `/validate` — the standing control for the session-closing handoff PR **#274** (FR-166 / small-items session), which skipped its trailing `/validate-pr` per the handoff-PR loop-break (CLAUDE.md PR-workflow step 5a). This sweep re-examines the whole corpus and sees the #271/#272/#273/#274 deltas (Sweep 28 had validated through #270).
- **State at HEAD** (`a14e13c`, PR #274 squash-merge on `main`; this session develops on `claude/awesome-planck-u5c9ag`, created at that commit):
  - Library version **2026.06.252**; pack **1.49.3**; README **1.9.123**.
  - Audit gates **47** (gate 47 = listing-surface completeness, #272); governance rules **10**; skills **15** (6 paired); slash commands **8**.
- **Clone**: started shallow; `git fetch --unshallow` run before any history-aware audit (full-clone methodology rule).
- **Mechanical baseline**: `tools/run_all_audits.sh` → **All 47 audit gates passed**.
- **Pre-flight scanner**: `python3 tools/sweep-preflight-scanner.py` → 378 files scanned, 22 candidates suppressed (16 heuristic, 6 exemption file), **9 candidate findings across 3 files** — all the comparative/historical "two skills" / "two rules, two skills, or two gates" / "grew from five rules to ten ... to fifteen ... to forty-seven" phrasings, same class dismissed in Sweeps 22–28. Passed to all three subagents.
- **Iteration**: 1 (no re-baseline; terminated this iteration — see synthesis).
- **Dispatch declaration (Rule 5.6)**: all three subagents (A, B, C) dispatched on this full sweep. No abbreviation, no skip.

## Subagent A, Recent-PR deep review

Scope: deep-read of the four most-recent merged PRs (#271 DR Tier-2 fix; #272 FR-166 gate 47 + `suggest-listing-surfaces.py`; #273 count-gate P8; #274 handoff). Verbatim return:

**1 finding by severity: 0 error, 0 warning, 1 note.**

```
tool: subagent-A
ruleId: spec-prose-lags-docstring
level: note
location: governance/specification-audit-programme.md:130
fingerprint: spec-prose-lags-docstring:governance/specification-audit-programme.md:130
rubric: C3 (multi-surface incompleteness / documentation drift), out-of-window
evidence: PR #273 added gate-39 pattern P8 ("<N> automated audits") to the linter's
docstring (tools/lint-gate-count-consistency.py:31-38) and pattern table, but did NOT
update the §6 prose description of gate 39, which enumerates the idiom set as
'... prose references to gate counts ("N-gate audit programme", "N audit gates",
"gates 1-N", "all N gates", and similar idioms)' (governance/specification-audit-programme.md:130).
PR #273 did not touch the spec file at all (git show 2f09299 --stat). The §6 prose
closes with "and similar idioms" (open-ended catch-all), so the description is not
factually WRONG — P8's "automated audits" idiom is plausibly subsumed — and no count
is incorrect. Low-severity documentation-completeness drift; note-level because the
hedge keeps the prose accurate and no mechanical gate is affected.
```

Triaged all 6 pre-flight candidates → **all FALSE POSITIVES** (trust-recovery suite "two skills"; guardrail-review overlap-lens "two rules/skills/gates"; the L93 growth narrative "five → ten rules, → fifteen skills, → forty-seven gates" verified on-disk current).

Verified clean (with evidence): C8 DR four-tier coherence (#271 — Tier 1 continuous/1h, Tier 2 every-4h/4h, Tier 3 daily/24h, Tier 4 daily/72h; cadence/gap/RTO-RPO/P2-event clauses all agree, no tier trapped in P2); C3 gate-47 four-surface wiring; C1 no stale 46/32 counts in active content; C5 all five per-doc version bumps on-disk with Date 2026-06-23 (DR 1.3.1, audit-programme spec 1.15.0, ingestion 1.7.3, health-report 1.0.2, pack README 1.49.3); C6 taxonomy + portal `--check` in sync; C7 both new/changed linter docstrings match behaviour; C4 CHANGELOG state claims spot-checked true; full audit 47/47, regression 121 OK.

## Subagent B, Corpus-wide stale-reference sweep

Scope: corpus-wide drift the mechanical gates do not catch, with priority focus on the gate-47 SEMANTIC carve-out surfaces (framework matrices/crosswalks, glossary, Related Documents). Verbatim return:

**1 finding by severity: 0 error, 0 warning, 1 note.**

```
tool: subagent-B
ruleId: operational-risk-register-impact5-label-drift
level: note
location: risk/template-operational-risk-register.md:135
fingerprint: operational-risk-register-impact5-label-drift:risk/template-operational-risk-register.md:135
rubric: C2 (cross-document term drift), out-of-window, [adjacent-to / extends-DD-11; NOT covered by DD-2 or DD-3]
evidence: The corpus carries one conceptual 1-5 impact scale defined three ways at the
impact-5 (top) tier. The canonical risk standard risk/standard-enterprise-risk-management.md:103
defines impact-5 as "Catastrophic" and at :83 claims the methodology procedure and the
enterprise-risk-register template "match" it. The methodology procedure
risk/procedure-risk-assessment-methodology.md:105 and the enterprise-risk-register
template risk/template-enterprise-risk-register.md:58/142 use "Critical" (pinned by DD-3).
But the operational-risk-register risk/template-operational-risk-register.md:135 uses a
THIRD label: "| Severe | Existential impact; significant regulatory or reputational
consequence |". This impact-5 "Severe" label is NOT named by any deferred decision: DD-2
names the "Severe" variant only in supply-chain/register-concentration-risk.md:95 (a
different file); DD-3 names the enterprise template as "Critical"; DD-11 explicitly scopes
the operational-risk-register to its LIKELIHOOD scale (123-127) and RATING label (:222),
not the impact table (129-135). Most accurately an enumeration gap in DD-11's coverage of
that Section-E table; line 119 self-scopes the table ("adopting organisations may calibrate
or replace them"). Recommendation: fold into DD-11 / the consolidated DD-2/DD-3/DD-11
risk-vocabulary triage.
```

Triaged all pre-flight candidates → all REFUTED (legitimate comparative/historical phrasing). Verified clean: prose counts (10/15/8/47 current everywhere in live content; "46/44/32" hits confined to `.working/` archives + append-only CHANGELOG); TLS-floor (every live `TLS 1.2` accounted for by DD-4/DD-5 or migrated to 1.3); external citations (OWASP Top 10:2025, ISO/IEC 27001:2022 uniform); the generated maturity-scorecard count is a `--check`-gated artefact; the SEMANTIC matrices carry family-level (not per-doc-count) mappings and the two governance matrices' reciprocal Related-Documents links are consistent; RTO/RPO table internally consistent. Did NOT re-report the known compliance-matrix incompleteness (FR-167, tracked).

## Subagent C, Audit-programme integrity reviewer

Scope: the audit programme's own coherence — gate inventory, four wiring surfaces, enumeration metadata, with focus on gate 47 (#272) and gate 39 P8 (#273). Verbatim return:

**Zero real findings. 0 error, 0 warning, 0 note.**

Verified clean (with evidence): (1) gate 47 wired identically in CI (`quality.yml:162-163`), runner (`run_all_audits.sh:145`), pre-commit (`.pre-commit-config.yaml:348-353`), spec §6 (`specification-audit-programme.md:128`); docstring matches behaviour; root meta-spec exemption documented + implemented (`is_register_required` L135-143). (2) Gate 39 P8 (`\b(\d+)\s+automated\s+audits?\b`) exists as the 8th PATTERNS entry (L145-148), docstring lists it, regression fixture `test_stale_automated_audits_count_flagged` exercises it, zero corpus matches now (canonical 47, 434 files). (3) Gate count = 47 across runner banner, spec §6 rows 1..47, gate-39 canonical, gate-35 report, `guardrail-review/SKILL.md:93` "forty-seven"; README states no hard count. (4) Spec §6 inventory gapless 1..47, no dupes, script links match. (5) Gate 41 docstring "two hard-coded collections" matches the 2-entry COLLECTIONS tuple + runtime + spec §6. (6) `run-linter-regression.py` → 121 tests OK; gate 47 + gate-39 P8 covered. Gate 35 mechanical confirmation: "gate-name parity confirmed for 47 gates across all four audit-programme surfaces." All pre-flight candidates triaged clean.

## Orchestrator synthesis

**Dispatched (Rule 5.6)**: A, B, C — all three, no abbreviation.

**Synthesised finding-set (Rule 5.1 dedupe by `(file_path, normalised_section, claim_type)`)**: two findings, distinct files/claim-types, no dedup collision. C contributed zero.

| # | ruleId | Subagent (5.4) | Tag (5.2) | Level | Severity adjudication (5.3) | Window |
|---|---|---|---|---|---|---|
| 1 | spec-prose-lags-docstring | A | R | note | track-as-follow-up → **dismissed at apply-time, no action** | borderline (#273-adjacent) |
| 2 | operational-risk-register-impact5-label-drift | B | R | note | track-as-follow-up → **routed to DD-11** | out-of-window |

**Debate (Rule 5.5)**: none required — no divergence wider than one severity level, no real-vs-false-positive disagreement among subagents (A and B each surfaced one distinct note; C concurred on the audit-programme baseline). All 9 pre-flight candidates unanimously triaged as false positives.

**Apply-time verification (orchestrator re-read of each cited source before routing):**

- **Finding 1 (`spec-prose-lags-docstring`)** — re-read `specification-audit-programme.md:130` and `lint-gate-count-consistency.py:1-45`. **Dismissed; no action.** The §6 prose is a *description with representative examples + an explicit "and similar idioms" catch-all*, not an exhaustive pattern enumeration: it never listed P6 or P7 either, so the absence of P8 is consistent with the prose's design rather than drift, and P8 ("<N> automated audits") is genuinely subsumed by the hedge. The prose is accurate as written; forcing P8 in while P6/P7 stay unlisted would be *less* internally consistent. Recording as verified-clean-on-reread. No corpus change, no version bump. (Severity-tiered routing under trust-recovery would route every confirmed finding; this is **routine `/validate`, not trust-recovery mode**, so apply-time dismissal of a refuted/no-action note is in-scope. The note is preserved here in the record for audit-trail completeness.)
- **Finding 2 (`operational-risk-register-impact5-label-drift`)** — re-read `risk/template-operational-risk-register.md:115-139` (Section E). **Confirmed; routed to DD-11.** Line 135 labels the impact-5 tier "Severe", a third corpus label for the conceptual top tier (canonical standard "Catastrophic"; DD-3 surfaces "Critical"). It carries an authorial decision (which label is canonical — not mechanically determinable) and line 119 self-scopes the scale as replaceable, so it is a backlog item, not an in-window fix. DD-11 previously enumerated only the operational-risk-register's likelihood scale (123-127) and rating label (222); the impact-table gap (129-135) is now folded into DD-11 with explicit cross-reference to DD-2 (the other "Severe" surface) and DD-3 (the impact-5 label question). No-drop discipline satisfied.

**Termination**: empty-delta / no-action primary stop. **0 error, 0 warning, 2 note**; no High/Medium; one note dismissed at apply-time, one note routed to an existing deferred-decision cluster. No corpus fix required, so no re-baseline iteration. The corpus carries a clean coherence bill for the #271–#274 deltas (and the whole corpus) on top of the green 47/47 mechanical baseline.

## Resulting PR

No corpus fix produced (finding 1 dismissed; finding 2 routed to the backlog). Per recursion-avoidance, this sweep's history row + this detail file + the DD-11 routing edit in `TODO.md` batch into the **next substantive PR** (the queue's top item is FR-167). The sweep does not warrant its own close-out PR (2 notes, no fix — below the "numerous or coherent" threshold).
