# Sweep 30, iteration 1

## Trigger & state snapshot

- **Trigger**: `/resume` compensating-control corpus-wide `/validate` — the standing control for the session-closing checkpoint handoff PR #276 (post FR-167 batch 1), which skipped its trailing `/validate-pr` per CLAUDE.md PR-workflow step 5a's handoff-PR exception. Re-examines the whole corpus + the #275-#276 deltas (Sweep 29 covered through #274).
- **HEAD**: `4ec284a` (PR #276 squash-merge on `main`).
- **State at HEAD**: library `2026.06.254`, pack `1.49.3`, README `1.9.125`; **47 gates**, **10 governance rules**, **15 skills** (6 paired), **8 slash commands**.
- **Iteration**: 1 (no re-baseline yet).
- **Clone**: started shallow (`git rev-parse --is-shallow-repository` = `true`); `git fetch --unshallow` run before any history-aware audit, per the full-clone methodology rule.
- **Mechanical baseline**: `tools/run_all_audits.sh` → **47/47 passed**.
- **Pre-flight scanner**: 378 files scanned, 22 suppressed (16 heuristic, 6 exemption), 9 candidates — ALL confirmed comparative/historical prose by the subagents (same class dismissed in Sweeps 22-29: "two skills" = trust-recovery suite size; "five rules to ten" = guardrail-review growth narrative).

## Subagent A, Recent-PR deep review

Dispatched on PRs #270-#276 (primary focus: #275 compliance-matrix Architecture-domain expansion; secondary: #271 DR Tier-2 cadence fix).

**Total findings: 0** (0 error, 0 warning, 0 note).

- **PR #275 matrix accuracy (PRIMARY) — clean.** All 8 linked Architecture documents exist on disk. All control identifiers in the 8 new rows are corpus-consistent (AIS-01/02/04, IPY-01, IVS-03, DSP-04, GRC-01/02/03, CCC-01/02/03, STA-01 all in established use). Cross-checked each source doc's own framework-alignment section: ISO mappings agree (technology-radar A.5.20/A.8.30 → matrix row 156; api-design A.8.26 → row 151; data-architecture A.5.12/A.5.13/A.5.34 → row 153). Apply-time correction held: `IPY-02` and `DSP-10` (worker-proposed, corrected to `IPY-01`/`DSP-04`) and `PR.PS` appear NOWHERE in the corpus except `.working/` audit-trail records describing the catch. Count-softening confirmed (no stale "All 42 documents"; living-document framing at matrix L162). CCM coverage-summary row extended with "AIS, and IPY" — multi-surface touch present.
- **PR #271 DR consistency — clean.** Tier-2 4-hour cadence internally consistent across RTO/RPO table (`resilience/plan-it-disaster-recovery.md:50`), cadence clause (:97), gap clause (:99), P2-escalation rule (:101). "At least daily" correctly scopes to Tier 3/4 only.
- **Version/date bump integrity — clean.** Matrix 1.1.0 / 2026-06-23; DR doc 1.3.1 / 2026-06-23; `docs/maturity-scorecard.md` in sync.
- **Pre-flight candidates — all confirmed false positives** by direct read.

Files read in full: `compliance/matrix-grc-compliance-alignment.md`, `resilience/plan-it-disaster-recovery.md`, `dev-security/claude-rules/skills/guardrail-review/SKILL.md`, plus architecture source framework-alignment sections.

## Subagent B, Corpus-wide stale-reference sweep

**Findings: 5 warning locations (one finding class — CCM version drift); 0 error.** Tasks 1 (collection counts), 2 (matrix coverage refs), 4 (internal pointers) clean.

The substantive finding: 5 locations cite a nonexistent "CSA Cloud Controls Matrix v5" against the corpus-wide v4.1 convention (`register-canonical-citations.md:154`; `tools/lint-citations.py:42` "CSA's current Cloud Controls Matrix version is v4.1"). The mechanical citation gate passes because it keys on the abbreviated "CCM v5" idiom, not the spelled-out "Cloud Controls Matrix v5" form.

> **Apply-time correction (orchestrator).** Subagent B's location set was **incomplete and partly wrong**: it reported `dev-security/standard-api-security.md:233`, `security/standard-saas-security-posture-management.md:196`, `supply-chain/procedure-supplier-audit.md:173`, `supply-chain/procedure-supplier-due-diligence.md:160` (4 correct), but **missed `security/procedure-key-escrow-and-recovery.md:217`** (a real `v5` instance) and instead flagged `security/standard-endpoint-hardening.md:225` as a "note-level sibling" — which is a *versionless* "CSA Cloud Controls Matrix" form, NOT a `v5` hit. The orchestrator's corpus-wide grep (`grep -rn "Cloud Controls Matrix v5"`) found the complete, correct set of **5 v5 instances** (the 4 B got right + key-escrow:217). This is logged in `hallucination-metrics.md`.

The orchestrator-verified set of 5 `Cloud Controls Matrix v5` instances:

```
tool: subagent-B (apply-time-corrected by orchestrator)
ruleId: stale-standard-version-ccm-v5
level: warning
location: dev-security/standard-api-security.md:233
fingerprint: stale-standard-version-ccm-v5:dev-security/standard-api-security.md:233
rubric: C2 (cross-document term/version drift)
evidence: "| CSA Cloud Controls Matrix v5 | AIS, IAM, IVS, LOG | Cross-walk |" — cites nonexistent v5; corpus convention is v4.1 (register-canonical-citations.md:154). lint-citations.py:41-42 explicitly declares "CSA CCM v5"/"CCM v5" hallucinated but does not catch the spelled-out "Cloud Controls Matrix v5". Fix: v5 → v4.1.
```
```
tool: subagent-B (apply-time-corrected by orchestrator)
ruleId: stale-standard-version-ccm-v5
level: warning
location: security/standard-saas-security-posture-management.md:196
fingerprint: stale-standard-version-ccm-v5:security/standard-saas-security-posture-management.md:196
rubric: C2
evidence: "| CSA Cloud Controls Matrix v5 | Multiple domains | Cloud control baseline |" — same v5 drift. Fix: v5 → v4.1.
```
```
tool: subagent-B (apply-time-corrected by orchestrator)
ruleId: stale-standard-version-ccm-v5
level: warning
location: supply-chain/procedure-supplier-audit.md:173
fingerprint: stale-standard-version-ccm-v5:supply-chain/procedure-supplier-audit.md:173
rubric: C2
evidence: References line: "- CSA Cloud Controls Matrix v5, STA-05 ...". INTRA-DOCUMENT CONTRADICTION: same doc cites "CSA CCM v4.1 STA-05" at :23 (overview) and :91 (alignment). Fix: v5 → v4.1.
```
```
tool: subagent-B (apply-time-corrected by orchestrator)
ruleId: stale-standard-version-ccm-v5
level: warning
location: supply-chain/procedure-supplier-due-diligence.md:160
fingerprint: stale-standard-version-ccm-v5:supply-chain/procedure-supplier-due-diligence.md:160
rubric: C2
evidence: References line: "- CSA Cloud Controls Matrix v5, STA-01 ...". INTRA-DOCUMENT CONTRADICTION: same doc cites "CSA CCM v4.1 STA-01" at :23. Fix: v5 → v4.1.
```
```
tool: orchestrator (apply-time grep; MISSED by subagent B)
ruleId: stale-standard-version-ccm-v5
level: warning
location: security/procedure-key-escrow-and-recovery.md:217
fingerprint: stale-standard-version-ccm-v5:security/procedure-key-escrow-and-recovery.md:217
rubric: C2
evidence: "| CSA Cloud Controls Matrix v5 | EKM domain | Cloud cryptography |" — same v5 drift; surfaced only by the orchestrator's corpus-wide grep, not by subagent B. Fix: v5 → v4.1.
```

Low-priority sibling note (versionless, NOT v5 — stylistic only): `security/standard-endpoint-hardening.md:225`, `compliance/procedure-capa.md:469`, `compliance/standard-internal-audit.md:438` use the versionless "CSA Cloud Controls Matrix" form. Not a hallucinated version; optional consistency tidy. Not routed as a defect.

All other Subagent B tasks clean: collection counts 10/15/8/47 current; no stale matrix-coverage count outside the in-flight `TODO.md:77` FR-167 item; no broken prose pointers.

## Subagent C, Audit-programme integrity reviewer

**Total findings: 0** (0 error, 0 warning, 0 note).

- Gate-count consistency: §6 inventory (`governance/specification-audit-programme.md:82-128`) has exactly 47 data rows; gate 47 wired in all four surfaces (`quality.yml:163`, `run_all_audits.sh:145`, `.pre-commit-config.yaml:348-350`, spec §6 L128). The "three enforcement surfaces" vs "four name-parity surfaces" framing is coherent (the fourth is the §6 declaration table; gate 35 prose reconciles them).
- Gate 39 P8 coherence: docstring lists P8 ("N automated audits"); compiled pattern matches; regression test `tests/test_linters.py:1006-1018` matches; rephrase landed in `governance/template-library-health-report.md:63` (no stale "32 automated audits").
- Gate 47 spec coherence: §6 L130 honestly describes the mechanical/semantic split, matching the linter's behaviour.
- Collection-enumeration docstring: `tools/lint-collection-enumeration-consistency.py:20` says "two" matching the 2-entry COLLECTIONS tuple.
- Pre-flight candidates confirmed false positives.

Files read in full: `tools/lint-gate-count-consistency.py`, `tools/lint-listing-surface-completeness.py`, `tools/lint-collection-enumeration-consistency.py`, `governance/specification-audit-programme.md`, `governance/template-library-health-report.md`.

## Orchestrator synthesis

Full three-subagent dispatch (A, B, C — no abbreviation). Subagents A and C: 0 findings. Subagent B: 1 substantive finding class.

**Synthesis rubric applied:**
- **Dedupe**: the 5 v5 locations are one finding class (`stale-standard-version-ccm-v5`), 5 locations. The 3 versionless-CCM locations are a distinct, lower-priority stylistic class — noted, not routed as a defect.
- **Tag**: `R` (real, verified at apply-time by orchestrator grep + per-file re-read of the contradiction context).
- **Severity**: `should-fix` (warning). A hallucinated framework version that the corpus's OWN `lint-citations.py` declares invalid; two instances are intra-document contradictions. Not `must-fix-before-merge` because it is out-of-window and not a regression of any in-flight PR.
- **Window classification**: **OUT-OF-WINDOW**. `git log -L` blame shows all 5 v5 lines date to the initial public release (`03ca390`, 2026-05-31). None introduced or touched by focus-window PRs #270-#276. Pre-existing, never caught by Sweeps 22-29 (a true gate blind spot).
- **No debate** (subagents did not diverge; B's set was incomplete, corrected at apply-time rather than debated).
- **False-positive memory cross-check**: the 9 pre-flight candidates match the comparative/historical class accepted in Sweeps 22-29; no new accept-list entry needed.

**Apply-time worker-correction logged**: Subagent B's location set was incomplete (missed key-escrow:217) and partly mis-classified (listed a versionless instance as a v5 sibling). The orchestrator's corpus-wide grep produced the authoritative set. Recorded in `hallucination-metrics.md`.

**Gate blind-spot observed (P4 candidate)**: `tools/lint-citations.py` flags `CSA CCM v5` / `CCM v5` but not the spelled-out `Cloud Controls Matrix v5`. Broadening the gate to the spelled-out form would have caught all 5 instances and prevents recurrence. Routed as a process-improvement candidate alongside any fix.

**Action decided**: out-of-window correctness defect — surfaced to the maintainer with named action options per step 6 (do NOT auto-defer to Low/FYI; do NOT silently jump the FR-167 batch-2 queue). The fix is deterministic and unambiguous (v5 → v4.1, matching each document's own body and the corpus convention); the sequencing (own near-term PR vs fold into FR-167 batch 2 vs backlog) is the maintainer's call. Recommended: fix as a small near-term PR + broaden gate 27/lint-citations.

## Resulting PR

**PR #277.** The maintainer triaged the out-of-window CCM v5 finding (sequencing decision) and chose "fix now as own PR + broaden the gate". PR #277 corrects all five `v5 → v4.1` citations (plus the consequent `EKM → CEK` v3-domain-code fix on the key-escrow row), broadens `tools/lint-citations.py` with a `Cloud Controls Matrix v5` denylist entry + regression test, and carries this history row + detail file + the hallucination-metrics catch.
