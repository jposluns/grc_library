# Validation Sweep 31, iteration 1

## Trigger & state snapshot

- **Trigger**: `/resume` compensating-control corpus-wide `/validate`, the standing control for the session-closing handoff PR #280 (post DD-triage + Track-1-start session), which skipped its trailing `/validate-pr` per CLAUDE.md PR-workflow step 5a's handoff-PR exception. Re-examines the whole corpus + the #277/#278/#279/#280 deltas (Sweep 30 covered through #276).
- **HEAD**: `1b150a6` (PR #280 squash merge on `main`).
- **Library version**: `2026.06.258`; **pack**: `1.49.3`; **README**: `1.9.129`.
- **Counts at HEAD**: 47 numbered gates + 3 PR-only delta gates (D1/D2/D3); 10 governance rules; 15 skills (6 paired); 8 slash commands.
- **Clone**: started shallow (`git rev-parse --is-shallow-repository` = `true`); `git fetch --unshallow` run before any history-aware audit, per the full-clone methodology rule.
- **Iteration**: 1 (first iteration; empty-delta termination after this iteration).

## Subagent A, Recent-PR deep review

Scope: full diffs + touched corpus files in their entirety for the six in-window PRs (#275 FR-167 matrix Architecture domain, #276 handoff, #277 Sweep 30 citation fixes + gate broadening, #278 D3 delta gate, #279 DD-9 DR header + #278 close-out, #280 handoff).

**Findings: NONE.** Verification performed:

- **#277 citation fixes**: all five "Cloud Controls Matrix v5" → v4.1 corrections complete; corpus-wide grep found zero residual v5 forms outside exempt CHANGELOG/working/test/denylist locations; the EKM→CEK domain fix on `procedure-key-escrow-and-recovery.md:217` is honest (CCM v4.1 has no EKM domain; CEK is correct); the two supplier docs' intra-document contradictions are resolved; the new denylist entry + regression test are well-formed (test `test_spelled_out_cloud_controls_matrix_v5_flagged` passes).
- **#275 matrix Architecture domain**: all 8 architecture source files exist; ISO 27001:2022 Annex A controls and clauses cited are real; CCM domains (AIS/IVS/IPY/DSP/GRC/CCC/STA) and the honest-N/A-on-five-customs-frameworks pattern are consistent with the matrix's declared independent-assessor methodology (not a transcription of source-doc tables); `PR.IP`-under-CSF-2.0 is the known convention routed to DD-12; the stale "All 42 documents" summary was correctly softened.
- **#278 D3 gate**: wired consistently into all four surfaces (CI, runner, spec §6.1, gate-parity allowlist); step name matches between CI and the parity allowlist; script logic correct and fail-loud.
- **#279 DR header + bookkeeping**: "All Tier 1 and Tier 2 systems" → "All systems" justified (bullets span all four tiers); per-doc Version bumped 1.3.1→1.3.2 in the same commit; the CLAUDE.md D3 enumeration update is complete; the DD-1 TODO→DONE rotation is clean.
- **Version monotonicity**: library CalVer contiguous across the window (253→257 with the 253→255 gap accounted for by handoff #276).
- **Pre-flight candidates**: all confirmed false positives (comparative/historical/structural prose).

## Subagent B, Corpus-wide stale-reference sweep

Scope: corpus-wide hunt for stale counts, term drift, renamed references, mis-attributed citations, stale docstrings.

**Findings: NONE.** Verification evidence:

- **CCM "v5" recurrence**: Sweep 30's fix holds; grep across all corpus document directories returned zero matches; all surviving hits are in exempt locations (denylist, test fixtures, CHANGELOG, `.working/`); all ~60 live CCM citations read v4.1.
- **Canonical counts**: 10 governance rules, 15 skills, 8 slash commands, 47 numbered gates + 3 delta gates, all verified against the filesystem and spec §6; free-prose count claims using non-"gate" nouns swept, all GRC content-domain references not pack-machinery counts; the three pre-flight candidates confirmed false positives.
- **External-standard citations**: no stale ISO 27001:2013, CSF 1.1, OWASP Top 10:2021, ASVS v4, PCI DSS 3.x in citation position; ASVS v5, MASVS v2, AICM v1.0.3, Top 10:2025 all match the canonical register.
- **Document counts**: no stale corpus-total document-count claims in prose.
- **Renamed/moved references and delta-gate phrasing**: D1/D2/D3 correctly enumerated in spec §6.1; `D1`/`D2` strings in `docs/template-*` are an unrelated namespace (maturity-assessment item IDs).

## Subagent C, Audit-programme integrity reviewer

Scope: the four-surface audit programme (CI `quality.yml`, runner `run_all_audits.sh`, pre-commit, spec §6) plus the PR-time runner; highest-value check = D3's wiring.

**Findings: NONE.** Verification:

- **Gate count agreement**: 47 numbered gates + D1/D2/D3 across all four surfaces; gate 39 (count consistency) and gate 35 (name parity) both confirm 47.
- **D3 wiring**: present and consistent in `run-pr-time-checks.sh`, `quality.yml` (PR-only, alongside D1/D2), spec §6.1; correctly excluded from the gate-35 parity audit and `run_all_audits.sh` because §6.1 documents delta gates as inventory-exempt.
- **Spec version bump**: spec metadata `1.16.0` matches the detailed CHANGELOG record for #278.
- **Gate broadening (#277)**: correctly placed in `tools/lint-citations.py` (gate 5) with the CHANGELOG exemption and a regression test.

**Clarification surfaced by C (orchestrator-verified)**: in this corpus the citation-hallucination gate carrying the CCM denylist is **gate 5** (`lint-citations.py`); **gate 27** is `lint-citation-verification-freshness.py`. The Sweep-31 subagent briefs (and #277's CHANGELOG narrative) referred to the hallucination gate as "gate 27", which is a mislabel.

## Orchestrator synthesis

Mechanical baseline clean (47/47). All three subagents returned zero corpus findings. The pre-flight scanner's 9 candidates (in three pack files) were all confirmed comparative/historical/structural prose by A and B, consistent with the same class dismissed in Sweeps 22-30.

Apply-time verification of Subagent C's gate-numbering clarification (read `governance/specification-audit-programme.md` §6: gate 5 = `lint-citations.py`, gate 6 = `lint-standards-currency.py`, gate 27 = `lint-citation-verification-freshness.py`; the spec §6 cross-programme section lines 224-225 label these correctly) surfaced two Note-level gate-number mislabels in live working state, classified C1 (stale/incorrect prose reference). Neither was introduced by the in-window PRs:

- **Finding 1 (out-of-window, note, C1)**, `TODO.md:254`: "Confirm gate 27 (`tools/lint-standards-currency.py`) flags the supersession." The named tool `lint-standards-currency.py` is **gate 6**, not gate 27, an internal number/tool mismatch in the §4.3 forward-looking standard-version-upgrade deliverable sketch. Pre-existing (not in the #275-#280 window). Trivial fix: "gate 27" → "gate 6". Surfaced to the maintainer with named options per the out-of-window protocol; not auto-deferred.
- **Finding 2 (in-window narrative, note, C1, self-resolving)**, `TODO.md:18` (the rolling "Last validation sweep" status describing #277): "+ gate 27 broadened with a 'Cloud Controls Matrix v5' denylist entry". The broadened gate is **gate 5** (`lint-citations.py`), not gate 27. This status line is rewritten each sweep; corrected when this Sweep 31 status is recorded.

**Not routed (frozen history)**: the same "gate 27" mislabel for the hallucination gate also appears in the `CHANGELOG.md` #277 entry and `.working/changelog-details/CHANGELOG-detailed.md`. Per the change-tracking and artefact-and-branch-discipline rules, historical CHANGELOG entries are append-only and not retroactively rewritten; noted, no fix.

**Severity**: both findings Note (0 High/Medium). **Termination**: empty-delta primary stop after iteration 1 (zero High/Medium; the two Note findings are gate-number labels, not corpus-content defects).

**Subagents dispatched**: A, B, C (full three-subagent dispatch, no abbreviation).

## Resulting PR

PR #281 (the Sweep 31 close-out plus the maintainer-directed TODO restructure). Both note-level fixes (the `TODO.md`:254 "gate 27" to gate 6 correction, and the sweep-status "gate 27" to gate 5 correction in the session-resume metadata) land in #281's TODO rewrite, alongside this history row and detail file.
