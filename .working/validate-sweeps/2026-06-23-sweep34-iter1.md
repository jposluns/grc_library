# Validation Sweep 34, iteration 1

## Trigger & state snapshot

- **Trigger**: the `/resume` compensating-control corpus-wide `/validate` — the standing loop-break control for the session-closing handoff PR #292 (post-#291 resume session), which skipped its trailing `/validate-pr` + `/retro` (CLAUDE.md PR-workflow step 5a's handoff-PR exception). Re-examines the whole corpus plus the #289-#292 deltas (Sweep 33 covered through #288).
- **State at HEAD** (`d204495`, PR #292 on `main`): library version `2026.06.270`, pack version `1.49.3`, **10** governance rules, **47** numbered audit gates (+ 3 PR-only delta gates D1/D2/D3), **15** skills (6 paired), **8** slash commands.
- **Iteration**: iteration 1 (first iteration of Sweep 34).
- **Clone**: started shallow (`git rev-parse --is-shallow-repository` = `true`); `git fetch --unshallow` run before any history-aware audit, per the full-clone methodology rule (gates 31/40 mis-fire on a shallow clone).
- **Mechanical baseline**: `tools/run_all_audits.sh` clean — **All 47 audit gates passed**.
- **Pre-flight scanner**: 378 files scanned, 22 candidates suppressed (16 heuristic, 6 exemption file), **9 candidate findings** across 3 files — all in the same comparative/historical/pairing-prose class confirmed false-positive in Sweeps 22-33 ("a suite of two skills", "do two rules cover the same ground", "grew from five rules to ten ... a handful of skills to fifteen ... a dozen gates to forty-seven"). Passed to all three subagents as a known-likely-false-positive candidate list.
- **Subagent dispatch**: full three-subagent dispatch (A, B, C), no abbreviation.

## Subagent A, Recent-PR deep review

Scope: the 15 corpus-content files changed in PRs #289-#292, read in full, plus their cross-reference surfaces, plus apply-time verification of the high-risk external citations against live sources.

**Verbatim return (3 findings: 2 warnings paired, 1 note):**

Key verifications (clean):
- **C2 crypto (FR-153)**: verified against the live OWASP Password Storage Cheat Sheet raw GitHub source. Argon2id m=19456/t=2/p=1, PBKDF2-HMAC-SHA256 >=600,000, PBKDF2-HMAC-SHA512 >=220,000 all match current OWASP guidance exactly. (A WebSearch summary initially suggested "210,000" but the authoritative source confirms 220,000 — apply-time re-verification refuted the false positive.) FIPS 203/204 (Aug 2024) untouched and correct.
- **C2 browser-hardening (FR-90)**: `require-trusted-types-for 'script'`, HSTS preload, `X-Content-Type-Options: nosniff`, `frame-ancestors` all syntactically/technically correct.
- **C3 stale "310,000" / "MAM-only"**: no corpus doc carries the old PBKDF2 figure or describes BYOD as MAM-only; index register line 103 already corrected to "MAM and MDM controls".
- **C2 CIS benchmarks (#289)**: §4/§6 -> Cloud Foundations, §5 -> Cloud Productivity Foundations matches the framework table (v2.0/v3.0); the generic benchmark names are a pre-existing provider-agnostic abstraction the window did not introduce.
- Cross-doc nesting numbers (6 / 15 / ~25) are stated consistently across quickstart, decision-tree, adopter-guide; all new cross-reference target files exist.

```
tool: subagent-A
ruleId: C4-false-subset-claim
level: warning
location: docs/adopter-guide.md:115
fingerprint: c4-false-subset-claim:docs/adopter-guide.md:115
evidence: "This Tier 1 set includes the six-artefact Day-1 floor named in [`docs/template-quickstart.md`](template-quickstart.md)" — The #290 window introduced (in two surfaces) an explicit claim that the six-artefact Day-1 floor is a subset of the 15-document Tier 1 set ("includes the six-artefact Day-1 floor", "the three sizes nest rather than compete"). This is factually wrong: the Day-1 floor (template-quickstart.md:34-39) names six artefacts, but TWO of them — security/policy-acceptable-use.md and security/policy-identity-and-access-management.md — do not appear anywhere in the adopter-guide Tier 1 table (lines 121-135). The floor is therefore NOT a subset of Tier 1.
```

```
tool: subagent-A
ruleId: C3-false-subset-claim-parallel-surface
level: warning
location: docs/template-quickstart.md:45
fingerprint: c3-false-subset-claim-parallel-surface:docs/template-quickstart.md:45
evidence: "These six artefacts are the Day-1 floor; they sit inside the larger starter sets the other paths describe (the adopter guide's 15-document Tier 1 set, ...) rather than competing with them." — Parallel surface of the same defect: asserts the six Day-1-floor artefacts "sit inside" the 15-document Tier 1 set, but acceptable-use and IAM (floor items 2 and 3) are absent from the Tier 1 table. Multi-surface incompleteness pattern; fix in lock-step with adopter-guide.md:115.
```

```
tool: subagent-A
ruleId: C6-two-roles-vs-three-contributors
level: note
location: privacy/procedure-data-protection-and-privacy-breach-response.md:100
fingerprint: c6-two-roles-vs-three-contributors:privacy/procedure-data-protection-and-privacy-breach-response.md:100
evidence: "the Data Protection Officer and CISO jointly conduct an initial assessment. The two roles conduct it jointly, but each determination has a named lead accountable for the answer:" — The lead-in says "The two roles conduct it jointly," but the #291-introduced table assigns determinations 5 and 6 to "Data Protection Officer, with Legal" (a third role). The "two roles" phrasing reads as a closed set of two. Low severity (Legal Counsel is a defined role; not a hard contradiction).
```

## Subagent B, Corpus-wide stale-reference sweep

Scope: whole-corpus stale-reference sweep, with extra attention to surfaces that should have followed the #289-#292 changes.

**Verbatim return: 0 findings.** Searches run:

- `310,?000` and `PBKDF2` corpus-wide — the encryption policy (`security/policy-encryption-and-key-management.md:92`) carries only the NEW value (Argon2id preferred / PBKDF2-HMAC-SHA256 >=600,000 / SHA-512 >=220,000); L56 agrees. Every `310,000` hit is in `.working/` frozen archives or append-only `CHANGELOG.md` documenting the historical FR-153 defect. No corpus doc cites the stale figure.
- `BYOD|MAM|MDM|mobile device/application management` corpus-wide — no doc characterizes BYOD as MAM-only. The one stale descriptor (`register-document-index-and-classification.md:103`) was already fixed to "MAM and MDM controls" by PR #292 (verified live).
- `Cloud Foundations|Cloud Productivity|CIS Benchmark` — `operations/standard-cloud-security-configuration-baseline.md` carries "Cloud Foundations Benchmark v2.0" and "Cloud Productivity Foundations Benchmark v3.0" consistently (L191/192/197). No sibling mis-cites.
- Starter-set sizes (`6 / 15 / about-25`) and `DPO` — the #290 reconciliation is landed; the three sizes are cross-referenced. (B did not catch the false-nesting claim that A surfaced; A's deeper subset-membership check found it.)
- Collection counts (`47 gates / 10 governance rules / 15 skills / 8 commands`) — all current in non-exempt prose. Every stale-count hit lives in `.working/` (frozen) or append-only CHANGELOG (accurate-as-of-time).
- Pre-flight candidates — all confirmed FALSE POSITIVES (comparative/historical prose): `guardrail-review/SKILL.md:93` growth narrative endpoints match live counts; "suite of two skills" refers to the trust-recovery suite; generic "rules, skills, gates" noun usage carries no numeric claim.

## Subagent C, Audit-programme integrity reviewer

Scope: four-surface parity, spec ↔ implementation drift, collection-count prose, runner ↔ spec numbering.

**Verbatim return: 0 findings.**

- **Four-surface parity**: enumerated gates in all four surfaces — `run_all_audits.sh` (47 `run_gate` calls, [1]..[47]); `quality.yml` (same 47 named steps in order + D1/D2/D3 + the informational collection-candidate detector, `if: pull_request`-gated); `.pre-commit-config.yaml` (same 47 hooks + the non-gate `regenerate-derived-artefacts` helper); spec §6 (47-row inventory table matching the runner verbatim and in order). Names match line-for-line; count is a stable **47** (confirmed via `git log` that #289-#292 touched no `tools/`).
- **Full audit run green**: "All 47 audit gates passed."
- **§5 category coverage**: the seven functional-category enumerations partition gates 1-47, no missing numbers, no true cross-category duplication.
- **Spec ↔ implementation drift**: the three most-recently-touched linters — gate 39 (`lint-gate-count-consistency.py`, P#273), gate 47 (`lint-listing-surface-completeness.py`, P#272), gate 40 (`lint-version-bump-recency.py`) — have docstrings matching their §6 prose.
- **Canonical collection counts**: 10 rules / 15 skills / 8 commands confirmed by directory/file counts; every stale-prose candidate resolved to legitimate prose (addyosmani upstream vetting counts, append-only version-history rows, trigger thresholds, comparative growth prose). The spec L142 "1 PR-only delta gates" is a regex artifact from "§6.**1** ... delta gates" heading text; the actual D-gate table correctly enumerates D1/D2/D3 = three.
- **Pre-flight candidates**: all confirmed false positives (same as B's reading).

## Orchestrator synthesis

- **Apply-time verification** (orchestrator re-read each cited source, per the research-assistant discipline):
  - **F1 (`docs/adopter-guide.md:115`) + F2 (`docs/template-quickstart.md:45`)** — CONFIRMED REAL. The Day-1 floor's six artefacts are `policy-information-security` (in Tier 1), `policy-acceptable-use` (**absent**), `policy-identity-and-access-management` (**absent**), `procedure-security-incident-response` (in Tier 1), `policy-privacy-and-data-governance` (in Tier 1), `policy-enterprise-governance-and-risk-management` (in Tier 1). Overlap = 4 of 6; `grep -c` for the two missing policies in `docs/adopter-guide.md` returns 0. The "includes / subset / nest" claim is false. Classification **C4/C3 multi-surface incompleteness** (the in-window cross-reference work asserted a relationship that does not hold, across two parallel surfaces). In-window (introduced by #290, FR-69). Severity: **should-fix-this-PR** (warning).
  - **F3 (`privacy/procedure-data-protection-and-privacy-breach-response.md:100`)** — CONFIRMED REAL but low. Determinations 5 and 6 are led by "Data Protection Officer, with Legal"; the "two roles conduct it jointly" lead-in reads as a closed set. Classification **C6 internal tension** (defensible but improvable). In-window (introduced by #291, FR-85). Severity: **note / track-or-fix-this-PR**.
- **False positive refuted at apply-time**: Subagent A's WebSearch-suggested PBKDF2-HMAC-SHA512 "210,000" was refuted against the authoritative OWASP source (the corpus's **220,000** is correct). Not routed (the corpus is correct, not defective). This is the research-assistant discipline catching a worker-side over-correction at apply-time before it reached the corpus.
- **Dedupe**: F1 and F2 are the same defect on two parallel surfaces; fixed in lock-step (one logical finding, two locations). No overlap with B/C (both 0 findings).
- **Subagent provenance**: A surfaced all three; B and C corroborated 0 corpus-wide / audit-programme findings (and B independently confirmed the BYOD/PBKDF2 ripples were clean, which bounds F1/F2 to the nesting-claim defect rather than a wider count drift).
- **Debate**: none required (no cross-subagent divergence; A's findings are non-overlapping with B/C's clean scopes).
- **Maintainer directional choice**: F1/F2 is a reconcile-direction choice (soften the prose vs expand Tier 1 to 17 documents). Surfaced to the maintainer with a recommended option per the standing per-item-ask instruction. **Maintainer chose: soften the prose** (preserve the curated 15-doc Tier 1; state the accurate 4-of-6 overlap; drop the false subset/nest claim). F3 (a note with an unambiguous improvement) fixed without a separate ask.
- **Actions decided**: F1 + F2 fixed in `docs/adopter-guide.md` and `docs/template-quickstart.md` (accurate-overlap prose). F3 fixed in `privacy/procedure-data-protection-and-privacy-breach-response.md` ("drawing in Legal where a determination requires it"). Per-doc Version bumps: adopter-guide `1.3.0 -> 1.3.1`, template-quickstart `3.0.2 -> 3.0.3`, breach-response `1.4.12 -> 1.4.13`. Derived artefacts regenerated (taxonomy/portal/scorecard; only breach-response is taxonomy-tracked).
- **Termination**: empty-delta primary stop at iteration 1 (the three findings were the only ones; all fixed; no new High/Medium would surface on a re-baseline since the fixes are self-contained prose corrections with no parallel surfaces beyond the two already fixed in lock-step).

## Resulting PR

Sweep 34 close-out PR (this branch, `claude/peaceful-mendel-kdahvm`) — carries the three fixes, the per-doc version bumps, the regenerated artefacts, this detail file, and the history row.
