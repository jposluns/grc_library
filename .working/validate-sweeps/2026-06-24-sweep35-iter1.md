# Sweep 35, iteration 1

## Trigger & state snapshot

- **Trigger**: the `/resume` compensating-control corpus-wide `/validate`, the standing loop-break control for the session-closing handoff PR #297 (2026-06-24 resume session), which skipped its trailing `/validate-pr` + `/retro` per CLAUDE.md PR-workflow step 5a. First substantive task of the new session.
- **State at HEAD**: library `2026.06.275`, pack `1.49.3`, README `1.9.146`; 47 numbered gates + 3 PR-only delta gates; 10 governance rules; 15 skills (6 paired); 8 slash commands. HEAD `0a25551` (#297).
- **Clone**: started shallow; `git fetch --unshallow` run before any history-aware audit (gates 31/40), per the full-clone methodology rule.
- **Mechanical baseline**: `tools/run_all_audits.sh` = **47/47** clean.
- **Pre-flight scanner**: 378 files, 22 suppressed (16 heuristic, 6 exemption-file), 9 candidates, all "two skills" / "two rules" / "five rules to ten" comparative/historical/pairing prose (same class as Sweeps 22-34); confirmed false by all three subagents.
- Iteration 1 (no re-baseline).

## Subagent A, Recent-PR deep review

Dispatched over the substantive #293-#297 corpus deltas (asset-inventory, the #296 cross-doc files, #294 governance/ESG, #293 adopter-guide/quickstart/breach-response) plus the three mandated consistency checks (Business Essential/Standard DR-tier labels; the 7-year AI-log retention carriers; the P1 PIR deadline).

**One finding (A-1):**
- `stale-ccm-domain-abbreviation` / **warning** / `governance/matrix-cross-framework-alignment.md:64` (and parallel cells 65-68, 72). Six cells under the "CSA CCM v4.1" column read `GRM domain`; CCM v4.1's governance domain is `GRC` (`GRM` was the v3.0.1 code). Same error class as #294's FR-155 (which fixed the policy/standard/methodology surfaces but did not enumerate this matrix). **Out-of-window** (matrix not touched by #293-#297). Apply-time confirmed against the authoritative CCM v4.1.0 catalogue (GRC-01..08).

**Verified clean**: Business Essential/Standard DR-tier labels agree across `register-asset-inventory.md` + `plan-it-disaster-recovery.md` (no `Essential`/`Important` survives); all five named 7-year AI-log carriers agree; P1 PIR deadline (5 business days) matches the per-severity table. Pre-flight candidates all false.

## Subagent B, Corpus-wide stale-reference sweep

**One finding (B-1):**
- `stale-ai-log-retention-figure` / **warning** / `ai/standard-ai-access-and-agent-permissions.md:193`. The agent tool-invocation audit-log retention read "minimum AI-specific retention of **12 months**"; the canonical figure set by DD-6/7 (#294) is **7 years** (`register-data-retention-schedule.md:108`; sibling checklist C6 already cites 7 years). **In-window** (the DD-6/7 reconcile is in the #294 focus window; this was the missed fourth carrier). A corpus-wide grep confirmed it was the SOLE remaining stale 12-month AI-log carrier.

**Verified clean**: collection counts (47/10/15/8); risk vocabulary (Open/Closed status, six-option treatment set, no retired options); Service Level Management tier labels; all other "12 months"/"5 years" hits are non-AI-log and correct in context.

## Subagent C, Audit-programme integrity reviewer

**Zero findings.** Four-surface gate parity verified line-for-line (47 numbered gates + D1/D2/D3 identical across workflow, runner, pre-commit, spec §6); governance rules = 10; skills = 15 (6 paired); PR #296's `build-portal.py` glossary change coherent with the regenerated `docs/portal.md`; spec §6 docstrings semantically match the linters spot-checked.

## Orchestrator synthesis

Two confirmed findings, both apply-time-verified by orchestrator re-read; both the **missed-parallel-carrier** class the handoff flagged as the active failure mode.

- **A-1** (out-of-window): `GRM` → `GRC` in the cross-framework matrix (6 cells). A factual standards-accuracy correction (one correct answer per CCM v4.1.0); applied without a directional ask (same class as #277 v5→v4.1 and #294 FR-155).
- **B-1** (in-window): AI-log retention `12 months` → `7 years`. Reconcile-direction confirmed by the maintainer (up to 7 years, matching DD-6/7 and the "AI-specific retention" framing).

**Escalation that expanded the sweep.** Apply-time verification of A-1 surfaced an adjacent `GOV domain` reference (matrix:64; also `risk/policy-enterprise-governance-and-risk-management.md:136`). Investigation found `GOV` is **not a CCM v4.1 domain code**, and a corpus-wide grep found a systemic `GOV-NN`-cited-as-`CSA CCM v4.1` pattern across ~9 documents, plus `IVS-NN` (the superseded v4.0 code; v4.1.0 uses `I&S`), `NET-NN` (not a CCM domain), `IPY-05` (out of range), and AICM cited at the superseded `v1.0.3`. The maintainer supplied the **authoritative CSA CCM v4.1.0 and AICM v1.1.0 catalogues** (uploaded; kept in scratchpad, not committed, per the catalogues' no-redistribution licence) and directed a full corpus-wide CCM/AICM citation reconciliation, grounded entirely in those files. A validator built from the catalogues' domain codes + per-domain control-ID ranges confirmed the complete defect set and zero residual after fixes (the only remaining `GOV-NN`/`NET` matches are corpus-internal `MODEL-GOV`/`AI-GOV` IDs and the DD-12(a) TODO note, all expected).

**Action**: A-1, B-1, and the full CCM/AICM reconciliation (GOV→GRC with A&A/AICM add-backs where a real control fits else remove; IVS→I&S corpus-wide; NET→I&S network controls; IPY-05→IPY-04; AICM v1.0.3→v1.1; CCM v4.0.12/"CCM v4"→v4.1) all fixed in this Sweep 35 close-out PR. DD-12(a) (NET) resolved and rotated to DONE. A follow-up gate (lint-ccm-aicm-citations.py) backed by a fair-use reference of the authoritative domain codes + control ranges is queued as the next PR per the maintainer.

## Resulting PR

PR #298 (Sweep 35 close-out + CCM/AICM citation reconciliation).
