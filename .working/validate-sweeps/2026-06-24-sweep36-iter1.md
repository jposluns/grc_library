# Validation Sweep 36, iteration 1

## Trigger & state snapshot

- **Trigger**: the `/resume` loop-break corpus-wide `/validate` after the session-closing handoff PR #300 (which skipped its trailing `/validate-pr` + `/retro` per CLAUDE.md PR-workflow step 5a). First substantive task of the maintainer-authorized 2026-06-24 overnight mission.
- **State at HEAD** (`2089f0c`, = `origin/main`): library `2026.06.278`, pack `1.49.4`, README `1.9.149`. Audit programme: **48** numbered gates + 3 PR-delta gates. Governance rules: **10**. Skills: **15** (6 paired). Slash commands: **8**.
- **Clone**: started shallow; `git fetch --unshallow` run before any history-aware audit (gates 31/40).
- **Mechanical baseline**: `tools/run_all_audits.sh` → **all 48 gates passed**.
- **Pre-flight scanner**: 378 files, 22 suppressed (16 heuristic + 6 exemption), 9 candidates — all confirmed comparative/historical/pairing prose (the "suite of two skills", "do two rules cover the same ground", "grew from five rules to ten ... handful of skills to fifteen" class, same as Sweeps 22-35). All false; one sub-token on the same line as a candidate (the gate-count word-form, see Subagent B) was a true finding.
- Iteration 1 (no re-baseline).

## Subagent A, Recent-PR deep review

Verbatim return: **0 error, 3 warning, 0 note.** All three are the `incomplete-mechanical-sweep` class: PR #298's reconciliation corrected control codes/titles corpus-wide but left three AICM/CCM *version-currency strings* stale (codes/titles vs version strings — gate 48 checks the former, not the latter).

- `stale-aicm-version-citation` (warning, out-of-window): `supply-chain/procedure-third-party-ai-due-diligence.md:25,97,223` cite "AICM v1.0.3" three times; file untouched in the #294-#300 window; `governance/register-canonical-citations.md:155` records v1.0.3 as a superseded prior version.
- `stale-aicm-version-citation` (warning, out-of-window): `dev-security/standard-security-baseline-and-standards-reference.md:49` lists AICM at "v1.0.3" while the CCM row directly above is correct at "v4.1.0" (the same-table internal inconsistency is the tell).
- `stale-ccm-aicm-version-in-pack-readme` (warning, in-window): `dev-security/claude-rules/README.md:427,432,434` — "CCM v4.0", "AICM v1.0.3", and "13 AI-specific security controls" (the 13 is the MDS subset; AICM v1.1.0 has 247 controls). #298 touched this file's version-history but left the external-projects section stale, contradicting the sibling pack `CLAUDE.md` (which #298 corrected to "CSA CCM v4.1 / AICM v1.1").

Verified clean by A: the gap register's 153 CCM/AICM control rows (0 mismatches vs the reference and the catalogue XLSX); no residual stale CODE (`IVS-NN`, `NET-NN`, `IPY-05`, `GOV-NN`-as-CCM); version-bump discipline on #298/#299/#300 sampled docs; #300's I&S-07 fix correct against the catalogue.

## Subagent B, Corpus-wide stale-reference sweep

Verbatim return: **0 error, 4 warning, 1 note.**

- `stale-gate-count-wordform` (warning, in-window): `dev-security/claude-rules/skills/guardrail-review/SKILL.md:93` growth narrative reads "a dozen gates to forty-seven"; live count is 48 (gate 48 added by #299 after Sweep 34). Word-form, invisible to gate 39's digit-form check. → ships in PR-B (separate theme).
- `stale-ccm-domain-code-IVS` (warning, in-window): 10 live bare-`IVS`-as-CCM-domain mentions survive (`governance/register-glossary.md:177` defines it as current; `register-document-index-and-classification.md:102,108`; `matrix-reverse-framework-control-crosswalk.md:149`; `compliance/matrix-grc-compliance-alignment.md:166`; `dev-security/standard-api-security.md:233`; the three cloud-hardening baselines; `security/policy-information-security.md:90`). v4.1.0 code is `I&S`. Gate 48 is token-only (`<DOMAIN>-<NN>`), blind to bare mentions.
- `stale-ccm-domain-code-NET` (warning, in-window): `compliance/matrix-grc-compliance-alignment.md:166` lists `NET` as a CCM v4.1 family; no such domain (network controls are I&S).
- `stale-ccm-domain-code-GOV` (warning, in-window): `matrix-reverse-framework-control-crosswalk.md:145` and `operations/framework-it-service-management.md:97` cite `GOV` as a CCM domain; governance is `GRC`.
- `erc-acronym-expansion-drift` (note, out-of-window, KNOWN/tracked): ERC = "Enterprise Risk Committee" vs "Executive Risk Committee" drift; pre-existing tracked item, not new.

All 6 pre-flight candidates confirmed false positives. Counts verified: 10 rules / 15 skills / 8 commands; gate count = 48 (contradicting the frozen Sweep-34 "47"). AICM-version currency: only live "v1.0.3" is the documented prior-versions cell at `register-canonical-citations.md:155` (correct).

## Subagent C, Audit-programme integrity reviewer

Verbatim return: **CLEAN. 0 error, 0 warning, 0 note.**

- Gate 48 four-surface wiring consistent (`quality.yml:165-166`, `run_all_audits.sh:146`, `.pre-commit-config.yaml:355-357`, `specification-audit-programme.md:129` row 48). Gate 35 parity rc=0 at 48 gates; gate 39 rc=0 at 48 gates / 437 files.
- `lint-ccm-aicm-citations.py` docstring accurate. **S4 limitation confirmed present** (TRACKED, not a finding): the gate uses the AICM-wins union `ALL_TITLES`, no CCM-section vs AICM-section awareness (the I&S-07 / IAM-11 divergence).
- `ccm_aicm_reference.py` verified against the authoritative XLSX: **CCM 207/207, AICM 247/247, zero title diffs**. `KNOWN_BAD_DOMAINS` mappings accurate.
- `tests/test_linters.py` `CcmAicmCitationTests`: 5 real tests exercising the gate, all pass.
- No stale gate-count in `tools/`; "47 gates" hits live only in `.working/` (frozen) and append-only CHANGELOG.

## Orchestrator synthesis

**Apply-time verification (orchestrator re-read + corpus-wide grep) CONFIRMED the finding-class and EXPANDED it beyond what the subagents reported** — the incomplete-mechanical-sweep lesson applied to the sweep itself. A standalone-token grep for all eight `KNOWN_BAD_DOMAINS` codes (`GOV`/`IVS`/`NET`/`GRM`/`AUD`/`EKM`/`MOS`/`DSI`) in CCM/CSA context found:

- The numbered `GOV`/`AUD` COBIT/CCM crosswalk in `compliance/policy-compliance-and-audit-management.md:114,128-133` (subagents had not surfaced this) — `GOV 01..06` → `GRC 01..06`, `AUD 01..05` → `A&A 01..05`. Verified against the catalogue: GRC has 8 CCM controls, A&A has 6; all cited ranges valid; governance→GRC and audit→A&A preserve intent.
- The `EKM` row in `matrix-reverse-framework-control-crosswalk.md:144` (subagents had not surfaced this) — `EKM` → `CEK`. My first filtered grep (`grep -i ccm|csa`) MISSED it because the row text contains neither token; only the unfiltered standalone-token grep caught it.
- `MOS`/`DSI`/`GRM` have zero live CCM-context occurrences.

The I&S domain full name was extracted from the authoritative CCM v4.1.0 worksheet ("Infrastructure Security - I&S"), confirming the v4.0 "Infrastructure and Virtualization Security" full name was also shortened (the I&S-01 *control* title retains the long phrase, per the catalogue).

**Classification**: C-class = fix-completeness cascade (the #298 reconciliation claimed completeness its token-scoped validator could not deliver) + gate-coverage gap (gate 48 is token-only). **Severity**: in-window warnings (the reconciliation that should have caught them landed today, #298/#299), adjudicated should-fix-this-PR. **Dedupe**: the IVS/NET/GOV/AUD/EKM residuals are one finding-class; the gate-count word-form is a distinct theme.

**Actions decided**:
- All bare-domain-code + version-currency residuals → fixed in close-out PR #301 (13 corpus docs + pack README), catalogue-grounded.
- Gate-count word-form (`forty-seven`→`forty-eight`) → separate PR-B (different theme).
- Durable mechanical fix (extend gate 48 to flag bare known-bad domain codes in CCM/CSA context) → queued as TODO §4.5 **S5**.
- ERC drift → KNOWN/tracked, not re-routed.
- **Out-of-window surfaced for maintainer**: `operations/standard-physical-security-of-it-infrastructure.md:108` cites "I&S-03 ... Physical Security" but I&S-03 = "Network Security" (physical/datacenter security is the `DCS` domain) — a probable control mis-citation whose correct replacement needs judgment; left for morning review (overnight-pr.md morning-review list), not guess-fixed. The two operations files' "Infrastructure and Virtualization Security" domain-*name* descriptors (alongside correct I&S codes) are bundled into that note.

Termination: empty-delta primary stop deferred to post-fix re-baseline (the close-out PR's `run_all_audits.sh`). No new High/Medium beyond the one finding-class.

## Resulting PR

PR #301 (this branch) — the Sweep 36 close-out carrying the CCM/AICM residual completion, this detail file, and the history row. The gate-count word-form ships in a following PR (PR-B).
