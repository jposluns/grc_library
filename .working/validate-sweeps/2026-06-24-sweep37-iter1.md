# Validation Sweep 37, iteration 1

## Trigger & state snapshot

The `/resume` loop-break corpus-wide `/validate` for the **session-closing handoff PR #305** (which skipped its trailing `/validate-pr` + `/retro` per CLAUDE.md PR-workflow step 5a, the handoff-PR exception). PR #305 was prepared by the prior overnight session but could not be opened/merged at the time because the GitHub MCP server disconnected; this morning session merged it (the MCP having reconnected), then ran this sweep as the compensating control.

- **Trigger**: `/resume` → merge #305 → Sweep 37 (the handoff plan's ordered first-actions).
- **HEAD**: `33cd359` (#305).
- **Versions at HEAD**: library `2026.06.283`, pack `1.49.7`, README `1.9.154`.
- **Counts at HEAD**: 48 numbered gates (+ 3 PR-only delta gates), 10 governance rules, 15 skills (6 paired), 8 slash commands.
- **Iteration**: 1 (re-baseline not required; empty-delta termination at iter 1).
- **Clone**: unshallowed (`git fetch --unshallow`) before any history-aware audit.
- **Scope**: the whole corpus plus the #301-#305 deltas (the overnight window: CCM/AICM residual completion #301, gate-count word-form #302, Day-1-floor option A #303, R3 relocation #304, loop-break pack generalization #305).
- **Mechanical baseline**: `tools/run_all_audits.sh` = **48/48** on the committed `33cd359` state.
- **Pre-flight scanner**: 377 files scanned, 22 candidates suppressed (16 heuristic, 6 exemption), 9 candidates surfaced across 3 files — all word-form "two skills" / "two rules" / "five rules to ten" comparative/historical/pairing prose; all confirmed false positives by Subagents A and B.

## Subagent A, Recent-PR deep review

Window `2089f0c..33cd359` (#301-#305). Ground truth `tools/ccm_aicm_reference.py` (read in full).

**IN-WINDOW (#301-#305): zero defects.** Verified clean across all eight failure-mode classes:
- #301 CCM/AICM fixes correct against the reference module, each touched cell checked (`IVS`→`I&S`, `GOV`→`GRC`, `AUD`→`A&A`, `EKM`→`CEK`, `NET` removed, AICM `v1.0.3`→`v1.1`): `compliance/matrix-grc-compliance-alignment.md:166`, `compliance/policy-compliance-and-audit-management.md:114,128-131`, `governance/matrix-reverse-framework-control-crosswalk.md:144-149`, `governance/register-document-index-and-classification.md:102,110`, `governance/register-glossary.md:170`, `dev-security/standard-api-security.md:233`, `-aws.md:235`, `-azure.md:239`, `-gcp.md:239`, `dev-security/standard-security-baseline-and-standards-reference.md:49`, `operations/framework-it-service-management.md:97`, `security/policy-information-security.md:90`, `supply-chain/procedure-third-party-ai-due-diligence.md:25,97,223`.
- Every changed corpus doc bumped both Version AND Date (no class-e omission).
- #302 word-form gate count "forty-eight" correct (`guardrail-review/SKILL.md:93`).
- #303 risk-floor row correctly re-pointed (`docs/template-startup-roadmap.md:54`).
- #305 loop-break exception text byte-identical across the pack rule and `.claude/` mirror, consistent with the new `validation-sweep-pr-scoped/SKILL.md:25` paragraph; pack README AICM coverage line correct ("247 controls across 18 domains").
- Generated artefacts in sync (`build-taxonomy.py --check`, `build-portal.py --check` both clean).
- All 9 pre-flight candidates confirmed false positives.
- CHANGELOG/TODO well-formed (three closed items rotated out, S5 added, FR-140 scope updated).

**OUT-OF-WINDOW (surfaced for triage, NOT in the #301-#305 delta):**
- `misattributed-citation` / **error** — `operations/standard-physical-security-of-it-infrastructure.md:108`: "I&S-03: Infrastructure and Virtualization Security: Physical Security". Two defects: (1) I&S-03 canonical title = "Network Security", not physical security; physical/datacenter security is the `DCS` domain; (2) "Infrastructure and Virtualization Security" is the superseded v4.0 IVS domain name.
- `cross-doc-term-drift` / **warning** — `operations/standard-cloud-security-configuration-baseline.md:195`: "I&S-01, I&S-04 | Infrastructure and Virtualization Security". Codes correct, but the description carries the superseded v4.0 domain name (the #301 rename to "Infrastructure Security" missed this file).
- **Non-defect guard**: `dev-security/register-compliance-controls-and-gap-register.md:198` "Infrastructure and Virtualization Security Policy and Procedures" is the correct canonical I&S-01 control title — do NOT over-correct.

## Subagent B, Corpus-wide stale-reference sweep

Whole-corpus, verified against `tools/ccm_aicm_reference.py`.

- **CCM/AICM token-level reconciliation COMPLETE**: `grep \b(IVS|NET|GOV|GRM|AUD|EKM|MOS|DSI)\b` over `*.md` — every live-corpus hit legitimate (`.NET`/`ASP.NET`, currency `AUD`, corpus-internal `MODEL-GOV`/`AI-GOV`, the glossary rename-note, the I&S-01 control title). Zero surviving bad CCM domain codes in CCM context on live surfaces.
- **2 genuine `stale-domain-name` residuals (warning)** — the same two `operations/` files Subagent A flagged: `standard-cloud-security-configuration-baseline.md:195` and `standard-physical-security-of-it-infrastructure.md:108`, both using the superseded v4.0 domain name "Infrastructure and Virtualization Security" under a "CSA CCM v4.1" label (gate-48-blind: it sees `<DOMAIN>-<NN>` tokens and code-to-title rows, not bare domain-name prose).
- Version-currency: no live `AICM v1.0.3` / `CCM v4.0` / `v4.0.12` residuals (all hits `.working/`/CHANGELOG/`tools` denylist/`tests`). Pack README now correctly CCM v4.1.0 / AICM v1.1.0 / 247 controls.
- Counts (48 gates / 10 rules / 15 skills / 8 commands) and versions (library `2026.06.283`, pack `1.49.7`, README `1.9.154`) all consistent on live surfaces; no stale-count, stale-version, or broken cross-reference defects.

## Subagent C, Audit-programme integrity reviewer

**0 findings — CLEAN.** All six checks pass:
- Gate-count parity: 48 numbered gates, gate 48 (`lint-ccm-aicm-citations.py`) present in all four surfaces (`run_all_audits.sh:146`, `specification-audit-programme.md:129`, `quality.yml:165`, `.pre-commit-config.yaml:355`).
- Gate-name parity: gate 48 + neighbours 46/47 names/order match across surfaces.
- #305 loop-break edit: the two `ai-assistant-workflow-disciplines.md` copies byte-identical (`diff` exit 0); rule count 10, skill count 15 undisturbed; gate 41 clean.
- Generated-artefact sync: `build-taxonomy.py --check` and `build-portal.py --check` both in sync.
- Pack version coherence: README Version `1.49.7` equals newest version-history row `1.49.7`.
- CCM/AICM module + gate 48: reference internally consistent (18 domains, 247 controls, `KNOWN_BAD_DOMAINS` of 8 superseded/fabricated codes present); gate 48 exit 0 across 377 files.

## Orchestrator synthesis

Dedupe by `(file_path, normalised_section, claim_type)`: Subagents A and B independently converged on the **same two findings** (the two `operations/` files). Subagent C returned clean. Synthesized finding-set:

- **F1** (`cross-doc-term-drift` / warning, OUT-OF-WINDOW) — `operations/standard-cloud-security-configuration-baseline.md:195`: stale v4.0 domain name "Infrastructure and Virtualization Security" beside correct `I&S-01, I&S-04` codes. **Unambiguous** completion of the settled #301 IVS-domain rename. **Action: FIXED in this close-out PR** → "Infrastructure Security" (per-doc `1.4.7 → 1.4.8`).
- **F2** (`misattributed-citation` / error, OUT-OF-WINDOW) — `operations/standard-physical-security-of-it-infrastructure.md:108`: compound defect — stale v4.0 domain name AND wrong control code (I&S-03 = "Network Security"; physical/datacenter security is the `DCS` domain). A content-mapping judgment call (partial name-only fix would leave a still-wrong code), so **surfaced to the maintainer with named options**; the maintainer chose the **DCS physical-access trio**. **Action: FIXED in this close-out PR** → "DCS-04, DCS-08, DCS-10: Secure Area Policy, Controlled Physical Access Points, Secure Area Authorization" (parallels the row's NIST PE-2/PE-3/PE-6 triple; per-doc `1.3.2 → 1.3.3`).

Both findings are the gate-48-blind bare-domain-name class; both were the pre-known Sweep 36 morning-review item #1 (deferred for the judgment call), now resolved. The token-level CCM/AICM reconciliation (#298/#301) is confirmed complete corpus-wide. No in-window defects; the overnight window (#301-#305) is clean. Refuted/non-routed: none (no subagent false positives this sweep beyond the pre-flight word-form candidates, all dismissed). Non-defect guard honoured: the I&S-01 control title at `register-compliance-controls-and-gap-register.md:198` left untouched.

Termination: empty-delta primary stop at iter 1 (0 in-window High/Medium; the two out-of-window findings fixed in the close-out; no re-baseline needed since the fixes are isolated table-cell edits with no cross-document ripple — confirmed by a post-edit corpus grep showing only the two legitimate "Infrastructure and Virtualization Security" hits remain).

## Resulting PR

PR-A (Sweep 37 close-out, this branch `claude/resume-review-gtx8fo`): F1 + F2 fixes + this detail file + the history row + CHANGELOG×2 + version bumps.
