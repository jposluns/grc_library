# Validation Sweep 45, Iteration 1

**Date:** 2026-06-25\
**License:** CC BY-SA 4.0

This is a frozen-state archive: `path:line` references are accurate as of write-time (HEAD `b3069d6`/#337) and are not maintained against later corpus changes.

## Trigger & state snapshot

- **Trigger**: the `/resume` loop-break compensating control for the session-closing handoff PR **#337**, which skipped its trailing `/validate-pr` + `/retro` (CLAUDE.md PR-workflow step 5a). Covers the #335 (resume bookkeeping + AUTHORS +Philip Veilleux) and #336 (project-governance separation Phase-1 migration) deltas.
- **State at HEAD**: library `2026.06.316`, pack `1.49.11`, README `1.9.187`; **49** numbered gates (+3 PR-only delta gates); **10** governance rules; **15** skills (6 paired); **8** slash commands.
- **Clone**: started shallow; `git fetch --unshallow` run before any history-aware gate.
- **Mechanical baseline**: `tools/run_all_audits.sh` **49/49** green at HEAD `b3069d6`, matching the #337 handoff's green-at-`b09375d` (HEAD is the byte-identical descendant; #337 changed only `.working/` + README/CHANGELOG version surfaces).
- **Iteration**: iteration 1 (full A/B/C dispatch). Iteration 2 was a mechanical re-baseline plus focused diff re-read (see Orchestrator synthesis).

## Subagent A, Recent-PR deep review

**0 findings.** Every check the #337 handoff asserted CLEAN independently re-confirmed CLEAN; no contradiction of a claimed-clean touched surface.

1. **§4 one-way-dependency invariant, CLEAN.** Two independent greps returned zero deliverable-corpus markdown links resolving under `.project-governance/`: (a) `](.project-governance/` across the 7 citers = none; (b) `\]\([^)]*\.project-governance/[^)]*\)` across all corpus `*.md` (excluding the dir itself, `.working/`, `.claude/`, CHANGELOG, TODO) = zero. The only `.project-governance/` text in corpus docs is prose / code spans (e.g. `governance/register-document-index-and-classification.md:54` "the .project-governance/ destination convention"). `lint-links.py` exit 0.
2. **All 6 movers' link targets resolve, CLEAN.** `Repository Path` = self bare-filename link; staying-file links use `](../governance/...)` (all resolve); within-cluster links bare; no would-dangle basename links.
3. **7 citer severs, CLEAN.** No empty parens, no empty markdown links, no dangling "see " across all 7; each citer bumped both Version and Date in #336 (`b09375d`).
4. **6 mover rows gone from the corpus index, CLEAN** (`governance/register-document-index-and-classification.md`, `governance/README.md`).
5. **`.project-governance/README.md` shape, CLEAN** (13-field metadata block, dash-free; `lint-metadata.py` and `lint-language.py` exit 0).
6. **AUTHORS.md (#335), CLEAN** (Version `1.1.3`, Date `2026-06-25`, both bumped; entry well-formed).
7. **CSF-1.1 residue, CONFIRMED PRE-TRACKED (not a finding)**: only the DD-12 carriers `risk/standard-third-party-and-supply-chain-risk.md:230` (`ID.SC`) and `operations/procedure-security-monitoring-and-alert-management.md:333/336/337` (`RS.RP`/`DE.DP`).

## Subagent B, Corpus-wide stale-reference sweep

**0 findings.** (1) Collection counts current (governance rules 10, skills 15, commands 8, gates 49); the sole live word-form gate-count surface `dev-security/claude-rules/skills/guardrail-review/SKILL.md:93` reads "five rules to ten ... a handful of skills to fifteen ... a dozen gates to forty-nine", all endpoints correct. (2) No live corpus cross-reference still points at the old `governance/register-citation-verification*` or `governance/worklist-citation-verification*` paths; every grep hit was `.working/` history, root CHANGELOG describing the move, or a relative link FROM a `.project-governance/` file back to the two specs that genuinely stayed in `governance/`. (3) ERM / CCM / CSF term-drift clean (only the pre-tracked DD-12 CSF-1.1 carriers). (4) Library `2026.06.316` / pack `1.49.11` / README `1.9.187` mutually consistent. All 9 pre-flight candidates dismissed as comparative / growth-narrative prose.

## Subagent C, Audit-programme integrity reviewer

**3 findings**, all in-window, one root cause (the #336 migration enumerated the explicit-allow-list content-linter set incompletely):

```
ruleId: gate31-project-governance-scan-gap
level: warning   location: tools/lint-document-date-staleness.py:121
evidence: "DEFAULT_SCAN_PATHS: tuple[str, ...] = ( ... \"governance\", \"operations\", ... \"supply-chain\", )"
impact: Gate 31 (Document Date staleness) silently skips all 7 .project-governance/ files; default run scans 225, scans 232 when the dir is added. Section 6.3 explicitly mandates "per-document version and date currency" for the dir, and gate 31 is exactly that check.
recommendation: Add ".project-governance" to DEFAULT_SCAN_PATHS (mirroring lint-metadata.py:448) + a regression confirmation.
```
```
ruleId: gate31-docstring-behaviour-contradiction
level: note   location: tools/lint-document-date-staleness.py:26
evidence: "The set of paths the linter scans is the same default-paths set the metadata audit uses ..."
impact: The docstring asserts parity with lint-metadata.py's default-paths set, but lint-metadata.py includes .project-governance (line 448) and this file did not, so the docstring was false. Same root cause; the path fix makes it true again.
```
```
ruleId: gate10-review-cadence-project-governance
level: note   location: tools/check-review-cadence.py:37
evidence: "DOMAINS = [ \"ai\", ... \"governance\", ... \"supply-chain\", ]"
impact: Gate 10 (per-document review cadence) omits .project-governance; the dir's README annual cadence and the verifications register's 12-month cadence go unscheduled. Section 6.3 names date/version/language/citation/links but NOT review-cadence, so inclusion is a maintainer classification call.
recommendation: Maintainer decision: include (schedule the dir's cadences) or document as corpus-only.
```

**Verified clean**: 49-gate four-surface parity (gate 35 + gate 39 green); the 9 content linters (`lint-metadata.py:448`, `lint-metadata-line-breaks.py`, `lint-language.py`, `lint-links.py`, `lint-roles.py`, `lint-filename-title-alignment.py`, `lint-shall-near-uncertainty.py`, `lint-standards-currency.py`, `lint-citations.py`) all carry `.project-governance` in their scan-roots; the 2 path-targeted linters re-point correctly; `build-taxonomy.py`/`lint-structure.py` `DOMAINS` correctly exclude the dir; taxonomy/portal/scorecard carry zero mover-file references and are `--check` in sync; the 10 governance rules byte-identical to their `.claude/` mirror. **§6.3-vs-§7.3 enumeration result**: of the three explicit-domain-list content linters omitting the dir, `lint-structure.py` is correctly excluded (structural membership is corpus-only); gate 31 is the clear gap (date currency is §6.3-named); gate 10 is the classification call.

## Orchestrator synthesis

- **Dedupe**: C-1 and C-2 share a root cause (gate 31's allow-list omission); C-2 self-resolves when C-1 is fixed. C-3 is the same enumeration-gap class on a different gate. A and B independently clean; no A/B/C overlap to dedupe.
- **Classification (C1-C8)**: all three are **C3/C4 multi-surface-incompleteness** (the migration changed one surface set, the 9 content linters, but the obligation spanned a larger set that was not fully enumerated).
- **Severity adjudication**: C-1 should-fix-this-PR (warning); C-2 note (self-resolving); C-3 note (classification, resolved by maintainer to "include").
- **Apply-time verification (re-read of each cited source before routing)**: confirmed gate 31's `DEFAULT_SCAN_PATHS` (lines 122-140) omits the dir; proved the skip by running gate 31 explicitly on `.project-governance` (scans 7 files, passes, so the files satisfy the check, the wiring just never points there); confirmed `lint-metadata.py:448` is the anchor that already included it; read §6.3 and confirmed it explicitly names "per-document version and date currency"; confirmed gate 40 (version-bump-recency) uses an exempt-list walk and already covers the dir (so only the date-currency side was gapped); confirmed gate 10's `parse_frequency_months` maps the README's "annually" and the register's "every 12 months" to 12-month periodic (so inclusion genuinely schedules them) while the bundle + 4 worklists are correctly event-driven.
- **Genuine-miss escalation**: C-1 contradicts the #337 handoff's asserted-clean "the `.project-governance/` directory is genuinely audited (§6.3)" claim, so per the `/resume` protocol it was escalated as a genuine miss (the #336 session's "added to all explicit-list content linters" enumeration was incomplete), not triaged as an ordinary out-of-window finding.
- **Maintainer decisions** (batched `AskUserQuestion`): (a) **include gate 10** (schedule the dir's stated cadences); (b) **amend the spec** (add §7.4 enumerating the explicit-allow-list content-linter completeness obligation, closing the §6.3-vs-§7.3 gap class).
- **Actions decided / applied**: `.project-governance` added to gate 31 `DEFAULT_SCAN_PATHS` and gate 10 `DOMAINS`; one scope-coverage regression test added per gate (`DocumentDateStalenessTests.test_project_governance_in_default_scan_paths`, `ReviewCadenceTests.test_project_governance_in_domains`); spec §7.4 added (`specification-project-governance-separation.md` `1.0.1`→`1.0.2`, Date `2026-06-25`); taxonomy/portal/scorecard regenerated after the spec Version bump.
- **Iteration 2 (termination)**: a mechanical re-baseline (`tools/run_all_audits.sh` 49/49, gate 36 now exercising the 2 new regression tests) plus a focused re-read of the fix diff (the 2 linter edits, the 2 tests, the spec §7.4 prose, dash-grep + `lint-language.py` clean on the spec). A full A/B/C re-dispatch was judged disproportionate: the iteration-1 findings were a single mechanically-scoped root cause fully addressed, Subagents A and B were clean, and the edits touch no corpus content and add no gate (gate count stays 49, four-surface parity unaffected). Empty-delta termination: no new High/Medium.

## Resulting PR

This Sweep 45 close-out PR (the first substantive PR of the 2026-06-25 continued-resume session). Carries the C-1/C-2/C-3 fixes, the spec §7.4 amendment, the two regression tests, the regenerated artefacts, this detail file, and the history row.
