# Sweep 46, iteration 1

## Trigger & state snapshot

- **Trigger**: the `/resume` loop-break corpus-wide `/validate` for the **session-closing handoff PR #339** (which skipped its trailing `/validate-pr` + `/retro` per CLAUDE.md step 5a; #339 landed the #338 `/validate-pr` + `/retro` rows, the §4.11 bookkeeping-parity gate design capture, and the handoff refresh on `main`). This is the compensating control covering the **#338 delta** (the Sweep 45 close-out: `.project-governance` wired into gate 31 + gate 10, two scope-coverage regression tests, separation spec §7.4). #335/#336/#337 were covered by Sweep 45.
- **State at HEAD**: `main`/feature `203ca06` (#339). Library `2026.06.318`, pack `1.49.11`, README `1.9.189`. Governance rules **10**, gates **49** (+3 PR-only delta), skills **15** (6 paired), slash commands **8**.
- **Mechanical baseline**: `tools/run_all_audits.sh` → **All 49 audit gates passed**. Clone unshallowed (`git fetch --unshallow`) before any history-aware gate.
- **Iteration**: iteration 1 (and the terminating iteration; empty-delta primary stop after the apply-time fixes, the two findings being a single mechanically-bounded root cause fully addressed and B/C clean).
- **Pre-flight scanner**: 379 files, 2 canonical collections, 22 suppressed (16 heuristic, 6 exemption-file); 9 candidates across 3 files, all in `dev-security/claude-rules/` ("two skills / two rules / five→ten→fifteen→forty-nine" comparative-and-growth prose), all dismissed by the subagents (current-state endpoints 10/15/49 verified; the recurring `guardrail-review/SKILL.md`:93 word-form carrier confirmed CURRENT, the Sweep 42 fix holding).

## Subagent A, Recent-PR deep review

**1 warning + 1 note, same root cause; everything else clean.**

```
tool: subagent-A
ruleId: gate10-readme-cadence-overstatement
level: warning
location: .working/session-handoff.md:108
fingerprint: gate10-readme-cadence-overstatement:.working/session-handoff.md:108
labels: [K], [in-window]
evidence: Line 108 asserts as proven-clean: "gate 10: the dir's README annual + register 12-month
cadences scheduled, none overdue". Contradicted by the live gate. tools/check-review-cadence.py:100-101
— `if f.name == "README.md": continue` — makes iter_active_docs() skip every README, and ROOT_DOCS
(lines 62-65) does not list .project-governance/README.md. Direct execution confirms the README is not
in gate-10 active docs. The .project-governance/README.md carries `Review Frequency: ...annually` but
gate 10 does NOT schedule it; only the 6 non-README files (registers/worklists) are scheduled. The
README's date-currency IS covered by gate 31; its review-cadence is scheduled by no gate. The asserted
claim names the README's annual review as gate-10-scheduled, which it is not.
```

```
tool: subagent-A
ruleId: test-docstring-readme-cadence-overstatement
level: note
location: tests/test_linters.py:1573
fingerprint: test-docstring-readme-cadence-overstatement:tests/test_linters.py:1573
labels: [K], [in-window]
evidence: The #338-added test docstring (lines 1573-1574) states gate 10 schedules "the README's annual
review; the verifications register's 12-month re-verification". The README's-annual-review clause is
inaccurate for the same reason as the finding above: check-review-cadence.py:100 skips all README.md
files. The test's actual assertion (assertIn(".project-governance", mod.DOMAINS)) is correct and passes;
only the explanatory comment overstates the effect. Note (not warning): a code comment with no runtime
or corpus effect.
```

**Verified clean (0 findings)**: both scan constants (`lint-document-date-staleness.py`:135 `DEFAULT_SCAN_PATHS`, `check-review-cadence.py`:43 `DOMAINS`) contain `.project-governance`, syntactically correct (quoted, comma-terminated, no duplicate); both regression tests (`test_linters.py`:1568, :2246) assert the constant membership against the live module and pass; gate 31 enumerates 7 `.project-governance` files, gate 10 enumerates 6 (README skipped by design); §7.4 internally consistent with §6.3 (reproduces the obligation list) and §7.3 (accurate cross-reference to the queued/not-built directional-dependency gate, no over-claim), spec Version `1.0.1`→`1.0.2` + Date bumped same change; §7.4 prose has no em/en-dashes, no British `-ise`, `lint-language.py` exit 0; `build-taxonomy.py --check` and `build-portal.py --check` exit 0 (no generated-artefact lag, scorecard reflects spec `1.0.2`); CHANGELOG root + detailed #338 leads accurate; counts 49/10/15/8 and versions `2026.06.318`/`1.49.11`/`1.9.189` confirmed. (The initial `run_all_audits.sh` exit-1 in the subagent's own environment was an artefact of leftover fixture files in untracked `tests/tmp/`; clearing it produced 49/49. Not a corpus defect; the orchestrator's main-tree run was clean at session start.)

**Asserted-expectation cross-check**: the load-bearing claim "Gates 31 and 10 NOW scan `.project-governance/`" HOLDS (gate 31: 7 files; gate 10: 6 non-README files). The contradiction is confined to the parenthetical naming the README's annual review as gate-10-scheduled.

## Subagent B, Corpus-wide stale-reference sweep

**0 actionable findings.**

1. **Counts current.** 49 gates / 10 rules / 15 skills / 8 commands. The recurring gate-39-blind word-form carrier `dev-security/claude-rules/skills/guardrail-review/SKILL.md`:93 reads "grew from five rules to **ten**, from a handful of skills to **fifteen**, and from a dozen gates to **forty-nine**" — all current. Every stale word-form count lives only in `.working/` frozen archives or append-only CHANGELOG/version-table history.
2. **Versions current.** `README.md`:8 = `1.9.189`; no stale-as-current version string in live prose. `TODO.md`:16 (`2026.06.284`/`1.49.7`/`1.9.155`) is the explicitly-labelled "as-of-session-pause snapshot, not 'current HEAD' claim" per `TODO.md`:13 — dismissed.
3. **`.project-governance/` migration clean** (§4 one-way-dependency holds). No deliverable corpus `.md` links into `.project-governance/`; the 6 moved artefacts are referenced at their NEW paths; no old `governance/`-path live link survives.
4. **CSF-1.1 inventory still BOUNDED** to the two known DD-12 carriers (`risk/standard-third-party-and-supply-chain-risk.md`:230 `ID.SC`; `operations/procedure-security-monitoring-and-alert-management.md`:333/336/337 `RS.RP`/`DE.DP`); no new carrier. `security/standard-remote-working-security.md`:256 is CSF-2.0-correct (`PR.AA` live; only an explanatory "was the CSF 1.1 subcategory" note mentions `PR.AC`). Matrix itself CSF-2.0-clean (gate 49 holding).
5. **ERC term drift** confirmed still the tracked pre-existing P3 state, not a new regression; not re-flagged.

## Subagent C, Audit-programme integrity reviewer

**0 findings.** No genuine miss, no parallel-gap miss, no asserted-expectation contradiction.

1. **Four-surface gate parity (49 gates).** `run_all_audits.sh` → "All 49 audit gates passed"; gate 35 (name parity) OK, gate 39 (cross-file gate-count consistency) OK, gate 31 OK, gate 36 OK. Spec §6 has 49 gate rows. Gate 39 passing is the authoritative cross-surface agreement check.
2. **§7.4 COMPLETENESS question — no parallel gap.** All 11 explicit-allow-list content-audit linters include `.project-governance`: `lint-citations.py`:144, `lint-document-date-staleness.py`:135 (gate 31), `lint-filename-title-alignment.py`:81, `lint-language.py`:211, `lint-links.py`:97, `lint-metadata-line-breaks.py`:72, `lint-metadata.py`:448, `lint-roles.py`:70, `lint-shall-near-uncertainty.py`:91, `lint-standards-currency.py`:65, `check-review-cadence.py`:43 (gate 10). The only two include-list omitters are the generators `build-taxonomy.py` (DOMAINS, comment lines 42-48 document the deliberate exclusion) and `lint-structure.py` (a corpus-index/README-membership checker, NOT a §6.3 content-audit obligation; exclusion correct-by-design per §7.1/§4). No third content linter omits the directory. #338's fix left no parallel gap of its own class.
3. **The two regression tests** assert `.project-governance in mod.<CONST>` against the live module and pin membership directly; `run-linter-regression.py` → "Ran 146 tests ... OK"; gate 36 OK.
4. **§7.4 internal consistency**: spec Version `1.0.2` (line 5), Date `2026-06-25`; §7.4 (lines 190-197) partitions the two discovery shapes, names the completeness obligation, names review-cadence as included by maintainer decision, and does NOT over-claim the §7.3 directional-dependency gate (recorded as queued/not-built, lines 184-188).
5. **Gates 31 and 10 scan the directory NOW**: gate 31's `iter_markdown_targets(DEFAULT_SCAN_PATHS)` returns all 7 `.project-governance/` files; gate 10's `iter_active_docs()` returns 6 (README excluded by its own line-100 README-skip, by design). Both standalone OK.

**Asserted-clean cross-checks all UPHELD**: 49-gate four-surface parity (gates 35/39 pass); `git diff --stat bac5ddb 203ca06` over `tools/`, the spec, tests, and `.project-governance/` is EMPTY, so the audit surfaces are byte-identical between #338 and HEAD #339 (green-at-`bac5ddb` holds at HEAD); gates 31/10 scan the dir; both local runners green.

## Orchestrator synthesis

- **Dedupe**: A-1 (`.working/session-handoff.md`:108, warning) and A-2 (`tests/test_linters.py`:1573, note) are two surfaces of ONE root cause (the gate-10 README-cadence overstatement). Distinct `(file_path, section, claim_type)` tuples, so two synthesised findings, one root.
- **Classification (C-class)**: C7 (stale/inaccurate docstring-and-prose characterization of a gate's behaviour). The asserted-clean claim and the test docstring both over-precisely describe gate 10's effect.
- **Severity adjudication**: A-1 `should-fix-this-PR` (warning; it is a `main`-resident asserted-clean claim that is wrong); A-2 `should-fix-this-PR` (note; a tracked-file comment, no runtime/corpus effect, but same-PR fix is cheap and removes the matching overstatement). No High/Medium. No corpus, gate-behaviour, wiring, generated-artefact, version, or count defect.
- **Provenance**: A surfaced both; B and C independently corroborated the surrounding surfaces clean (B: counts/versions/migration/CSF-1.1; C: the §7.4 completeness question + four-surface parity).
- **Debate**: none needed (no real-vs-false-positive or >1-severity-level divergence; B and C found nothing in A's locus to disagree with).
- **Apply-time verification (mandatory before routing/fixing)**: the orchestrator independently re-read `check-review-cadence.py`:92-107 (confirming the `if f.name == "README.md": continue` README-skip and the two-entry `ROOT_DOCS`) and `tests/test_linters.py`:1562-1583 (confirming the docstring text and the correct passing assertion). Both findings CONFIRMED real, Low severity, no behavioural impact.
- **Asserted-expectations cross-check (resume protocol)**: A-1 contradicts the #339 handoff's asserted-clean claim on line 108 — a contradiction of a claimed-clean touched surface, which the protocol treats as a genuine-miss signal to surface (not auto-defer to Low). Surfaced to the maintainer. BUT the substance of the asserted claim (gates 31 and 10 NOW scan the directory) held; only the over-precise parenthetical naming the README was wrong. The underlying behaviour (gate 10 skipping all READMEs) is universal, intentional, and pre-existing, not a coverage gap. So the "miss" is a prose-accuracy slip in the closing session's self-description, not a substantive QA failure (unlike Sweep 45's genuine coverage-gap miss).
- **Actions decided** (both fixed in the close-out PR under the stricter-is-safer + evidence unattended authority): (1) corrected the `tests/test_linters.py`:1568 test docstring to not name the README's annual review as gate-10-scheduled (gate 10 skips all READMEs; the README's date-currency is covered by gate 31); (2) corrected `.working/session-handoff.md`:108's parenthetical to accurately describe gate-10 coverage (6 non-README artefacts; README skipped corpus-wide by design), with an inline note that Sweep 46 corrected it.
- **FYI (no action, surfaced for maintainer awareness)**: no README's review cadence is gate-scheduled anywhere in the corpus (universal gate-10 design choice — READMEs are index/navigation files). The `.project-governance/README.md` carrying a `Review Frequency` field that no gate enforces is consistent with every other corpus README, not a `.project-governance`-specific gap. If the maintainer wants README review cadences gate-enforced, that is a new feature, out of scope for this fix.
- **Termination**: empty-delta primary stop at iteration 1 (the single root cause fully addressed; A/B/C otherwise clean; a re-baseline confirms 49/49 with the corrected docstring).

## Resulting PR

Sweep 46 close-out PR (this branch `claude/resume-6a5su8`): corrects the two overstatement surfaces, adds this detail file + the history row, batches the 2026-06-25 resume decision record (`overnight-pr.md`). Library `2026.06.318`→`2026.06.319`; README `1.9.189`→`1.9.190`.
