# Validation Sweep 53, iteration 1

## Trigger & state snapshot

- **Trigger**: the `/resume` loop-break corpus-wide `/validate`, the compensating control for session-closing handoff PRs #363 and #365 (both skipped their trailing `/validate-pr` + `/retro` per CLAUDE.md step 5a; the 2026-06-26 session continued past the #363 handoff to ship #364, then re-closed at #365). Covers the #362/#363/#364/#365 deltas.
- **State at HEAD**: `main` at `4376ec1`/#365. Library `2026.06.344`, pack `1.49.17`, README `1.9.215`, audit-programme spec `1.16.9`, project-governance-separation spec `1.0.4`.
- **Counts**: 53 numbered audit gates (+3 PR-only delta gates D1/D2/D3), 10 governance rules, 15 skills (6 paired), 8 slash commands.
- **Mechanical baseline**: `tools/run_all_audits.sh` **53/53 green** at `4376ec1`/#365 (a descendant of green-at-`41f0dce`/#364; #365 changed only `.working/` and version surfaces, no corpus content). Clone unshallowed before the history-aware gates ran.
- **Iteration**: iteration 1 (and only iteration; empty-delta primary termination, the single finding being below High/Medium).
- **Pre-flight scanner**: 379 files scanned, 23 candidates suppressed (17 heuristic + 6 exemption file), 9 surfaced candidates, all comparative/historical-growth prose ("a suite of two skills", "two rules, two skills, two gates", "grew from five rules to ten ... to fifteen ... to fifty-three"), all dismissed; collection endpoints 10/15/53 verified current.

## Subagent A, Recent-PR deep review

**0 findings.** Verified across the #362/#363/#364/#365 deltas:

1. Gate 53 four-surface coherence: `tools/lint-directional-dependency.py` behaviour (scan set = `AUDITED_DOMAIN_DIRS` minus `.project-governance` plus `ROOT_DELIVERABLE_DOCS`; pack subtree excluded by prefix; fenced blocks + external targets skipped; target resolved before the `.project-governance` path-component test) matches the docstring, the audit-programme spec §6 row 53 narrative, the separation-spec §7.3 narrative and §4 cross-reference, and the `DirectionalDependencyTests` regression class (four cases). `AUDITED_DOMAIN_DIRS` holds exactly twelve members. Gate 53 runs clean (exit 0).
2. Version cascade consistent: README `2026.06.344` / `1.9.215`; pack `1.49.17`; audit-spec `1.16.9`; separation-spec `1.0.4`; CHANGELOG monotonic #362(.341) to #365(.344); root and detailed CHANGELOGs agree.
3. #364 session-metrics convention coherent across all three surfaces (`.working/session-metrics.md`, `.claude/commands/resume.md`, `.claude/CLAUDE.md`): orchestrator main-loop tokens `not instrumented` (never fabricated); row written at the session-closing handoff PR; kept out of the citable root CHANGELOG. Inaugural row honestly marked partial.
4. CHANGELOG root-detailed lead match + full link coverage for the #362-#365 entries.
5. DONE/TODO rotation for #362 complete (directional-dependency item absent from TODO as open; DONE carries the #362 entry; §4.15 updated to "gates 49 through 53").

One sub-threshold non-finding (not routed): the root vs detailed CHANGELOG lead-paragraph wording diverges slightly for #362 and #364 (the detailed leads carry extra context); a pervasive pre-existing project pattern, not introduced by these PRs, and the lock-step delta gate is satisfied.

## Subagent B, Corpus-wide stale-reference sweep

**1 note (B-1).**

```
tool: stale-reference-sweep
ruleId: csf-1-1-residue-new-carrier
level: note
location: compliance/procedure-capa.md:475
fingerprint: csf-1-1-residue-new-carrier:compliance/procedure-capa.md:475
rubric: [correctness, citation-accuracy, NIST-CSF-2.0-currency]
```

`compliance/procedure-capa.md:475` is a "Framework / Standard" mapping-table row labelled "NIST Cybersecurity Framework 2.0": `| NIST Cybersecurity Framework 2.0 | RC.IM (Recovery: Improvements) | Incorporating lessons learned into recovery strategy and GRC programme improvement |`. `RC.IM` is a CSF-1.1 code, not a CSF 2.0 category: the project's own authoritative reference `tools/nist_csf_reference.py:82` records it as `"RC.IM": "CSF 1.1 'Improvements'; became ID.IM in 2.0"`, and the CSF 2.0 Recover function contains only `RC.RP` and `RC.CO`. This is a NEW carrier: it is not among the two known DD-12 carriers, and DD-12 (TODO:85) names neither this file nor `RC.IM`.

Everything else verified current: collection counts 53/10/15/8; versions current; the two known DD-12 carriers confirmed present-as-expected; `security/standard-remote-working-security.md:256` confirmed CSF-2.0-correct (`PR.AA`, only an explanatory `PR.AC` note); pre-flight candidates all benign.

## Subagent C, Audit-programme integrity reviewer

**0 findings.** Verified:

1. Four-surface gate parity for all 53 gates: §6 has exactly 53 numbered rows (no gate 54); gate 53 name-identical across `run_all_audits.sh`, `quality.yml`, `.pre-commit-config.yaml`, spec §6.
2. Gate 53 spec narrative matches the linter (no drift); §4 one-way-dependency cross-reference accurate.
3. `AUDITED_DOMAIN_DIRS` single-source-of-truth: 12 members; gates 52 and 53 both derive from it; gate 52's 3-file `EXEMPT` set (`lint-structure.py`, `build-taxonomy.py`, `detect-collection-candidates.py`) accurate.
4. 10-governance-rule mirror byte-identical (incl. `project-integrity.md`).
5. `tests/test_linters.py` has `ScanScopeParityTests` (gate 52) and `DirectionalDependencyTests` (gate 53), both with both-direction cases.
6. Gates 49-53 absent from the §5 *descriptive* functional-category list is the known §4.15 low item (§5 is descriptive, not gate-checked), not a finding.

## Orchestrator synthesis

- **B-1** (`compliance/procedure-capa.md:475`, `RC.IM` under a CSF-2.0 label): **apply-time verified** by re-reading lines 470-478 of the file and `tools/nist_csf_reference.py:47/82` (`RC.IM` → `ID.IM`, `ID.IM = "Improvement"`). Confirmed real. Classification: C2 (mis-attributed/stale citation). Severity: note (out-of-window; the file was not touched this session and was not asserted clean by the #365 handoff, so it is an ordinary out-of-window finding, not a genuine-miss contradiction). **Action**: dedupe by class into the existing backlog item **DD-12** (the corpus-wide CSF-1.1 migration, deliberately deferred and non-partitionable) by cross-referencing the new carrier into DD-12's known-carriers list with the correct target (`RC.IM` → `ID.IM`); NOT hot-fixed piecemeal (per the dedupe-against-existing-backlog routing discipline, and because a one-off edit of this file outside the planned migration would be a partial, ungated change of exactly the class DD-12 exists to do once, mechanically gate-verified, via the queued gate-49 extension).
- All other subagent returns 0 findings. No High/Medium findings; empty-delta primary termination at iteration 1.
- Cross-check against the #365 handoff Asserted expectations: no contradiction.

## Resulting PR

Batched into the §4.17 (D4 Version-Date co-bump gate) PR per recursion-avoidance: this history row, this detail file, and the B-1 cross-reference into DD-12's known-carriers list.
