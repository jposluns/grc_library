# Sweep 70, iteration 1 (2026-06-28)

## Trigger & state snapshot

- **Trigger**: the `/resume` loop-break corpus-wide `/validate`, the compensating control for session-closing handoff PR #425 (which skipped its trailing `/validate-pr` + `/retro` per CLAUDE.md step 5a), covering the **#423/#424/#425** deltas (the guard-rails-prompts session: the eleventh governance rule `surface-counterproductive-instructions` + the abandoned-#422 fold-in; PR-E FR-178/194/199; the handoff).
- **State at HEAD**: library `2026.06.403`, pack `1.51.0`, **11 governance rules** (the 11th added #423), 54 gates, 16 skills, 9 commands, compliance matrix `1.11.1`, canonical-citations register `1.5.4`. Corpus doc count 311.
- **Mechanical baseline**: `tools/run_all_audits.sh` **54/54** at HEAD `574902c`/#425 (= `origin/main`; gate-identical descendant of green-at-`9dadbb3`/#424 per the #425 handoff). Clone unshallowed before history-aware gates.
- **Iteration**: iteration 1 (first and only; empty-delta termination after the two in-window fixes, which are in gate-exempt free prose so re-baseline is not gate-affecting).
- **Pre-flight scanner**: 382 files, 24 suppressed (18 heuristic + 6 exemption), 9 candidates, all comparative/growth prose ("a suite of two skills", "grew from five rules to eleven", "two rules, two skills, two gates") in pack files untouched by the window; all dismissed by all three subagents.

## Subagent A, Recent-PR deep review

**0 findings.** Reviewed the #423/#424/#425 deltas in full. All three closing-session asserted-clean claims verified as hypotheses against live state and held:
- **11th-rule wiring consistent**: byte-identical mirror (125 lines line-for-line); pack README tree `dev-security/claude-rules/README.md:77`; MIRROR_MAP `tools/lint-claude-rules-sync.py:93`; collection-enum docstring `tools/lint-collection-enumeration-consistency.py:6` "the pack's eleven governance rules"; `guardrail-review/SKILL.md:93` "grew from five rules to eleven"; pack `1.51.0` metadata at `dev-security/claude-rules/README.md:5` with a paired `## Version history` row at line 445 (the #372-class metadata-without-row miss does NOT recur).
- **FR-178/194/199**: `grep '\bshall\b'` returns 0 matches in both `security/policy-information-security.md` and `privacy/policy-privacy-and-data-governance.md`; `governance/register-glossary.md:123` `| **ELT** | Executive Leadership Team. |` alphabetical between EIOPA (122) and ERC (124); `governance/register-canonical-citations.md:286` (WP216) and `:290` (EDPB Opinion 28/2024) "Current version" cells = "Original (no later revision)" NOT a fabricated "1.0".
- **adopter-guide casing**: `docs/adopter-guide.md:60` and `:83` both sentence-case "Core reference set"; zero title-case residue.
All four pre-flight candidates dismissed as legitimate comparative prose. No stale references, mis-attributed citations, multi-surface incompleteness, version-bump omission, or generated-artefact lag in the reviewed window.

## Subagent B, Corpus-wide stale-reference sweep

**2 findings (both in-window, same class: free-prose governance-rule count stale 10 -> 11 after #423).**

```
tool: subagentB
ruleId: stale-collection-count-in-prose
level: warning
location: TODO.md:160
fingerprint: stale-collection-count-in-prose:todo.md:160
evidence (verbatim): "- **Distillation source**: the ten `governance/` pack rules form the discipline core; ..."
impact: a live present-tense count of the governance rules, now stale (11 .md files in dev-security/claude-rules/governance/, the 11th added pack 1.51.0). Free-prose, gate-41-blind.
recommendation: "the ten" -> "the eleven". (Applied this PR.)
```

```
tool: subagentB
ruleId: stale-collection-count-in-prose
level: note
location: .claude/CLAUDE.md:283
fingerprint: stale-collection-count-in-prose:.claude/claude.md:283
evidence (verbatim): "... prose counts like \"the ten governance rules\" are not gated)."
impact: borderline; the phrase is an illustrative example of an ungated prose count inside the close-out-checklist bullet about prose-count staleness, but it now reads as a concrete stale count (ironically inside the very bullet warning about that drift).
recommendation: "the ten" -> "the eleven" to keep the example current and self-consistent. (Applied this PR.)
```

Gate 54 (`tools/lint-document-control-codes.py`) **0 CSF-1.1 carriers / 311 corpus documents** (CSF-1.1-clean state holds). Version consistency intact: README Library `2026.06.403`, README `1.9.274`, pack `1.51.0` mutually agree (at the pre-fix HEAD). All four pre-flight candidates and all other "ten/nine/10/9 rules" hits (pack README version-history rows, CHANGELOG append-only narrative, `.working/` frozen archive) confirmed legitimate historical records, not findings.

## Subagent C, Audit-programme integrity reviewer

**0 findings.** Four-surface gate parity stays at **54** (`governance/specification-audit-programme.md` §6 carries exactly 54 numbered rows, last = gate 54 at L135; `surface-counterproductive-instructions` absent from `quality.yml` and `run_all_audits.sh`, confirming #423 added a RULE not a gate; gates 35 + 39 would pass). 11th-rule wiring confirmed: MIRROR_MAP entry at `lint-claude-rules-sync.py:93` (gate 37 enforces the byte-identical mirror); collection-enum docstring advanced 10->11 at `lint-collection-enumeration-consistency.py:5-6`; the governance directory holds exactly 11 `.md` files (gate-41 canonical source = 11); present in all three gate-41 enumeration surfaces (pack README tree L77, pack CLAUDE.md L141, project `.claude/CLAUDE.md`). §6 unaffected (a rule is not a gate). All five pre-flight candidates dismissed as legitimate comparative/growth prose (the gate-count tool derives its canonical count dynamically from §6's row count, so it stays self-consistent at 54).

## Orchestrator synthesis

- **In-window findings (2)**: B-1 (`TODO.md:160`, warning) and B-2 (`.claude/CLAUDE.md:283`, note) are the same failure-mode class (C-class: stale prose count after an enumerated-collection change), caused by #423 advancing the governance-rule count 10->11 on the gate-enforced surfaces while these two free-prose counts (gate-41-blind) went stale. **Neither contradicts an asserted-clean surface**: the #425 handoff asserted the rule "wired consistently across every *gate-enforced* surface" and explicitly flagged free-prose count surfaces as a known soft spot (the pack README "When to use each rule" table, TODO §4.24); TODO.md and `.claude/CLAUDE.md` free prose are gate-exempt and not in the asserted-clean set. So these are ordinary in-window findings, not genuine-miss escalations.
- **Action**: both fixed in this PR (`ten` -> `eleven`), the in-window protocol. Apply-time-verified against live file state before editing (the orchestrator re-read both exact lines).
- **Dedupe / debate**: no dedupe needed (distinct locations); no severity divergence (both subagents agreed warning/note, A and C clean); no debate invoked.
- **Subagents dispatched**: A, B, C (full three-subagent dispatch, no abbreviation).
- **Cross-check against #425 asserted expectations**: no contradiction. The three flagged asserted-clean claim-sets (11th-rule consistency, FR-178/194/199, adopter-guide casing) were each independently re-verified by Subagent A and all held.

## Resulting PR

PR landing this row + the two fixes: the Sweep-70 close-out PR on branch `claude/resume-6vgttb` (library `2026.06.404`).
