# Validation Sweep 75, iteration 1 (2026-06-29)

## Trigger & state snapshot

Triggered by `/resume` as the loop-break corpus-wide `/validate` (the compensating control for session-closing handoff PR #457, which skipped its trailing `/validate-pr`). Delta window: the overnight correction-batch session #451..#457 (Sweep-74 close-out #451, handoff-marker fix #452, high-assurance executable skill #453, pack-README rule-table #454, FR-44 `shall`->`must` #455, ERC `Executive`->`Enterprise` reconcile #456, handoff PR #457).

State at HEAD `a0c8697` (= `main`, #457): library `2026.06.435`, README `1.9.306`, pack `1.53.1`, audit-spec `1.16.16`. Gate count 54, governance rules 12, skills 17, commands 10, corpus doc count 311. Mechanical baseline: `tools/run_all_audits.sh` 54/54 green, matching the handoff green-at-`0f69844` line (#457 is a descendant at 54/54). This is iteration 1.

Subagents dispatched: **A, B, C** (all three; full sweep).

## Subagent A, Recent-PR deep review

Verbatim return (SARIF-lite):

```
tool: subagent-A
ruleId: fr44-miss
level: error
location: dev-security/policy-secure-development-and-engineering.md:75,83,95,109,115,125,131,135,139,147,149,151
fingerprint: fr44-miss:dev-security/policy-secure-development-and-engineering.md:75
labels: C2 (FR-44 sweep), in-window
evidence: This Policy-doctype document (line 4 Document Type: Policy) carries 12 bare normative `shall` statements that the #455 FR-44 corpus-wide shall->must sweep MISSED. Quoted: L75 "2.1 All source code shall reside in approved version control repositories"; L83 "3.1 Developers shall follow OWASP ASVS v5"; L95 "4.2 All AI-generated code shall undergo static and dynamic security scanning"; L109 "5.4 A Software Bill of Materials (SBOM) shall be maintained"; L115 "6.2 Penetration testing shall occur annually"; L125 "7.2 Peer reviews shall verify functionality"; L131 "8.1 CI/CD pipelines shall include automated security scanning"; L135 "8.3 Build environments shall be isolated"; L139 "9.1 Development environments shall mirror production"; L147 "10.1 Development teams shall track metrics"; L149 "10.2 Annual secure coding training shall be mandatory"; L151 "10.3 Lessons learned ... shall drive updates". None are external-standard quotations; all are the document's own normative requirements, several sitting beside `must` in the same numbered section (L135's own second clause "signed artefacts must be validated"). The file is NOT among the 17 documents the #455 CHANGELOG-detailed enumerates, is NOT one of the 3 deliberately-preserved files, and was never bumped by #455 (metadata still Version 1.0.2 / Date 2026-06-19). The #455 verification claim "Post-apply corpus grep: 0 normative shall remain" was run against the sweep's own input set rather than a true corpus-wide scan, so it self-corroborated the miss. Domain-dir contradiction grep confirms this is the ONLY genuine miss.

tool: subagent-A
ruleId: within-doc-shall-must-mix
level: warning
location: dev-security/policy-secure-development-and-engineering.md:75
fingerprint: within-doc-shall-must-mix:dev-security/policy-secure-development-and-engineering.md:75
labels: C1 (convention drift), in-window
evidence: Same root cause as fr44-miss. Because the 12 shall were missed, the document violates master-spec specification-master-project.md:278 ("A single document should use one of 'must' or 'shall' throughout its normative text, not both"). Fixing fr44-miss resolves this; it is not a second edit.
```

Summary: one genuine in-window error (FR-44 sweep missed this file) with a paired convention-drift note sharing its root cause. All other hunt classes verified clean (ERC reconcile confined to the deferred carve-out, glossary reads "Enterprise Risk Committee"; 17th-skill/command parity intact; stale-count scan clean; #455/#456 Version+Date co-bumps present).

## Subagent B, Corpus-wide stale-reference sweep

Verbatim return (SARIF-lite):

```
tool: subagent-B
ruleId: fr44-shall-residue
level: error
location: dev-security/policy-secure-development-and-engineering.md:75
fingerprint: fr44-shall-residue:dev-security/policy-secure-development-and-engineering.md:75
labels: cross-document-drift, out-of-window
evidence: FR-44 (PR #455) recorded in CHANGELOG-detailed L34 with the post-apply claim "all 125 normative shall/shall not occurrences converted" across an enumerated 17-document list. This file is NOT in that list yet carries 12 bare normative shall in authored numbered policy clauses (lines 75, 83, 95, 109, 115, 125, 131, 135, 139, 147, 149, 151), none backticked, none quoted. L135 "8.3 Build environments shall be isolated and signed artefacts must be validated" mixes shall and must in one sentence. Gate-9 passes (none sit adjacent to an uncertainty marker), so the class is gate-blind. The glossary "Policy" doctype entry was corrected in #455 to "Uses 'must'.", so this document now violates the register its own doctype prescribes.
```

Summary: one genuine finding (same file/class as Subagent A's, deduped). Verified clean: collection counts live-correct (12 rules, 17 skills, 10 commands); ERC residue confined to the deferred `guideline-minimum-viable-governance-structure.md`; glossary `register-glossary.md:124` reads "Enterprise Risk Committee"; no stale current-tense library/pack version strings on live surfaces; pre-flight candidates all the historical/comparative FP class.

## Subagent C, Audit-programme integrity reviewer

Verbatim return: **NO ISSUES FOUND.** All seven scope items verified clean at HEAD `a0c8697`:
- C1 four-surface gate parity (gate 35): identical normalized script sets across the three executable surfaces; gate 35 live `OK ... 54 gates across all four ... surfaces`; spec §6 enumerates gates 1-54 no gaps/dupes.
- C2 gate-count consistency (gate 39): `OK ... 54 gates ... across 456 files`; spec Version 1.16.16; no stale 53/55.
- C3 collection enumeration (gate 41): `OK ... all consistent`; pack README rule-scope table has rows for all 12 rules (rules 9-12 from #454 present).
- C4 paired-skill parity (gate 44): `OK: 8 paired ... matching step-identifier sets`; high-assurance SKILL `### 1.`-`### 7.` map to `/high-assurance` items 1-7.
- C5 bookkeeping-parity (gate 50): reads the Findings cell (field index 4); #457 handoff row carries `SKIPPED ... handoff` there; docstring + spec §6 say "Findings cell".
- C6 spec internal consistency: §6 exactly 54 gates; spec carries a Version field but no `## Version history` table, which gate 50 check-4 explicitly tolerates.
- C7 docstring staleness: `lint-matrix-control-codes.py:6` reads "nine frameworks" (the #451 fix), `lint-collection-enumeration-consistency.py:5` reads "twelve governance rules"; gate 36 regression `Ran 189 tests ... OK`.

Pre-flight candidate dismissed: guardrail-review SKILL L93 growth-narrative "five rules to twelve ... to seventeen" carries correct current values.

## Orchestrator synthesis

- **Dispatched: A, B, C** (all three).
- **Deduped finding-set: 1.** Subagent A's `fr44-miss` and Subagent B's `fr44-shall-residue` are the same finding (dedupe key `(dev-security/policy-secure-development-and-engineering.md, normative-shall, FR-44-completeness)`). Subagent A's `within-doc-shall-must-mix` shares the root cause and is resolved by the same fix.
- **Classification**: C2 (FR-44 sweep incompleteness) with a C1 (convention-drift) facet.
- **Severity**: error / must-fix. In-window (A) vs out-of-window (B) disagreement resolved per synthesis rule 5.3 (take higher): treated **in-window**: it is the incomplete completion of the in-window #455 sweep, and it **contradicts an asserted-clean expectation** (the #457 handoff asserted "FR-44 sweep complete across corpus prose ... a residual bare normative `shall` would be a genuine miss"). Escalation case: the closing session's self-assessment was wrong.
- **Apply-time verification**: the orchestrator re-read the file (confirmed 12 bare normative `shall`, Version 1.0.2/Date 2026-06-19, gate-9 green) and ran an independent corpus-wide bare-token grep confirming this is the only residual file (the other `shall` hits are the gate-9 filename token in 2 files and the one backticked `` `shall` `` word-reference at `guideline-minimum-viable-governance-structure.md:145`, all 3 correct to keep).
- **Action**: fixed in this close-out PR (#458). Deterministic `\bshall\b`->`must` on the file (12 conversions, L135's pre-existing `must` preserved), Version 1.0.2->1.0.3, Date->2026-06-29, regenerated `taxonomy.yml` + `docs/maturity-scorecard.md`. Re-parse: corpus-wide grep 0 residual normative `shall`; 54/54 gates green; gate-9 green. The frozen #455 CHANGELOG "0 normative shall remain" claim is left as-shipped (frozen record); this PR's entry records the correction and that the #455 claim was scoped to its own input set.

Iteration 2 (re-baseline): corpus-wide post-fix `shall` grep returns 0 residual; 54/54 gates green; no fix-induced findings. **Primary-stop termination** (zero new High/Medium, finding-set empty).

## Resulting PR

PR #458 (Sweep 75 close-out + FR-44 completion fix).
