# Sweep 78, iteration 1

## Trigger & state snapshot

- **Trigger**: the `/resume` loop-break corpus-wide `/validate`, the compensating control for session-closing handoff PR #502 (which skipped its trailing `/validate-pr` + `/retro` per the handoff-PR exception). First substantive task of the resumed session.
- **Delta window**: #476..#501 (the prior session's 26-PR fix-issues-first-then-net-new run that CLOSED FR-167 and FR-58, shipped 9 net-new docs via the high-assurance harness, plus the `-ises` gate widening (#499), the TODO renumber (#500), and the D5 rotation-check broadening (#501)).
- **Operating mode**: attended-autonomous. Resume clarifications maintainer-answered: FR-24 = keep the SOX result-classification scheme + the CAPA crosswalk as shipped (#492); §1.4 S3 = stays parked; mode = attended-autonomous, self-merge green PRs, `.claude`/pack edits pre-approved for this session.
- **State at HEAD**: library `2026.06.480`, README `1.9.351`, pack `1.53.6`, audit-spec `1.16.24`, compliance matrix `1.11.4`; 57 gates, 12 governance rules, 17 skills, 10 commands. Clone unshallowed before history-aware gates.
- **Mechanical baseline**: `tools/run_all_audits.sh` -> **All 57 audit gates passed** at HEAD `fe88442`/#502 (= `origin/main`), matching the #502 green-at-`d86f90b` 57/57 baseline (no close-vs-start drift).
- **Iteration**: iter 1.
- **Pre-flight scanner**: 392 files, 25 suppressed (21 heuristic + 4 exemption), 11 candidates, all comparative/growth/suite-of-two prose (the trust-recovery "suite of two skills"; addyosmani "24 engineering-workflow skills" + "5 vetted skills"; the guardrail-review growth narrative "five rules to twelve ... handful of skills to seventeen ... to fifty-seven", all current). All dismissed by A+B+C.

## Subagent A, Recent-PR deep review

Reviewed the #476..#501 deltas, focused on FR-167 matrix gap-fill, the FR-58 Adoption Disposition column, the 9 net-new docs, the #492 FR-24 deepening + #493 SOX §404 correction, and the #495 OT safety note.

**1 finding (REFUTED at apply-time by the orchestrator, NOT routed):**

```
tool: subagent-A
ruleId: stale-reference
level: warning
location: CHANGELOG.md:29 (root, #497 entry); echoed in .working/session-handoff.md and .working/DONE.md
fingerprint: stale-reference:changelog.md:29
rubric: in-window; asserted-expectations-contradiction: claimed-yes
evidence (as reported): the #497 entry states the Adoption Disposition column classifies "all 292 active documents ... template (282)"; subagent A recounted the register as 275 rows (8 library-internal + 265 template + 2 reference) and claimed 292/282 is a stale prose count contradicting asserted-expectation #2.
```

**Orchestrator apply-time refutation**: `grep -coE '\| (library-internal|template|reference) \|' governance/register-document-index-and-classification.md` returns **292**, with distribution `library-internal 8, reference 2, template 282`, exactly matching the #497 CHANGELOG. Subagent A under-matched the register rows (its 275/265 figure is wrong). The asserted "292 active documents ... template 282" is CORRECT; asserted-expectation #2 stands. A-1 is the false positive and is NOT routed (recorded here per the refutation-trail discipline).

**Clean re-verifications** (asserted-expectations #1/#2/#3 and adjacent surfaces): FR-167 matrix uniform (292 data rows, NF=14, no ragged/short rows; the 7 net-new-doc matrix rows present with resolving Path links; gate 23 green); FR-58 column uniform (292 rows, only the 3 defined vocabulary labels, defined in the Key Terms register, register `1.27.49`/`2026-06-30`, gates 33/34 confirm taxonomy+scorecard co-bump in sync); the 7 enumerated net-new docs present + in taxonomy/register/matrix + co-bumped + links resolve (gates 33/34/47 green); #493 attributes material weakness to SOX §404 (correct); #495 OT safety-priority note correct against NIST SP 800-82r3; the FR-24 result-classification-scheme note is the known maintainer pending-decision (now resolved keep-and-crosswalk), not a finding.

## Subagent B, Corpus-wide stale-reference sweep

Ground-truth counts established (not assumed): gates 57, governance rules 12, pack skills 17, slash commands 10.

**5 findings, all out-of-window, 0 errors** (4 warning + 1 note), all on gate-exempt surfaces (which is why the 57 mechanical gates pass over them):

```
tool: subagent-B | ruleId: stale-ize-verb-gate-blind | level: warning
location: tools/build-portal.py:343 (rendered into docs/portal.md:49)
fingerprint: stale-ize-verb-gate-blind:tools/build-portal.py:343
rubric: out-of-window
evidence: generator hard-codes "customised" in an emitted prose string ("... forked and customised."); `customise` is an enumerated verb in lint-language.py ISE_PATTERN, so it is in the house `-ize` convention's scope and should read "customized". Doubly gate-2-blind: lint-language.py scans only `.md` (the `.py` source is never scanned), and the rendered docs/portal.md is in GENERATED_DOCS (excluded). Exactly the gate-blind `-ise` class #499 set out to close, surviving in a generator source string.
```
```
tool: subagent-B | ruleId: stale-todo-section-pointer | level: warning
location: .claude/CLAUDE.md:468
fingerprint: stale-todo-section-pointer:.claude/claude.md:468
rubric: out-of-window
evidence: "The version-currency register is TODO §4.26." After the #500 renumber that item is `### 1.5 ... (was 4.26)`; the live forward reference should read §1.5. Cross-FILE §N-orphan class (.claude is gate-39/gate-3 exempt).
```
```
tool: subagent-B | ruleId: stale-todo-section-pointer | level: warning
location: .claude/CLAUDE.md:430
fingerprint: stale-todo-section-pointer:.claude/claude.md:430
rubric: out-of-window
evidence: "This is the PR B half of TODO §4.20; the PR A advisory tool shipped in #394." No `(was 4.20)` tag exists; the matrix-fit PR B item shipped (#399), so the pointer is doubly stale (dangling section number + closed item described as live).
```
```
tool: subagent-B | ruleId: stale-closed-FR-in-live-todo | level: warning
location: TODO.md:252
fingerprint: stale-closed-fr-in-live-todo:todo.md:252
rubric: out-of-window
evidence: §4.4's partitionability paragraph names FR-58 (CLOSED #497) and FR-167 (COMPLETE #490-494) as live partitionability candidates; both closed, should be dropped or relabelled.
```
```
tool: subagent-B | ruleId: stale-present-tense-illustration | level: note
location: tools/lint-todo-marked-done.py:31
fingerprint: stale-present-tense-illustration:tools/lint-todo-marked-done.py:31
rubric: out-of-window
evidence: docstring present-tense "FR-167's '... column SHIPPED in PR-B' while FR-167 itself stays open" as a design illustration; FR-167 is now complete; recast to past tense.
```

Dismissals (with evidence): all 5 pre-flight candidates false-positive (README "5 vetted skills" = addyosmani vetting depth; "two skills" = the full-qa+fitness suite; "24 skills" = the addyosmani upstream total; guardrail-review growth narrative all current at twelve/seventeen/fifty-seven). No stale collection count on any live surface. ERC canonicalization holds clean (0 lowercase "executive risk committee" survivors). `organisation`/`authorisation` noun derivatives are deliberately outside gate-2's enumerated-VERB scope (the corpus's established accepted convention, observation only).

## Subagent C, Audit-programme integrity reviewer

**0 findings.** Four-surface gate parity at 57 (gate 35 `OK ... 57 gates across all four surfaces`; gate 39 `OK ... 57 gates ... across 467 files`; §6 inventory table 57 rows). Gate 57's §6 detailed-prose pair is present (the Sweep-77-A1 regression, fixed in #476): the "Gate 57 is a backlog marked-done audit ..." description plus the "Gate 57 is appended ..." sentence. Gates 55 and 56 also carry both halves. §5 grouped-list includes gate 57. D5 registered in `WORKFLOW_DELTA_GATE_STEPS` (lint-audit-gate-parity.py) and wired into run-pr-time-checks.sh; the §6.1 D5 prose matches the linter's three closure forms (canonical `clos...§`; `FR-N CLOSED`; prose-named `clos... the ... item/directive`), with bare lowercase `Closes FR-N` deliberately excluded. Linter docstrings (check-todo-rotation-on-pr.py, lint-bookkeeping-parity.py gate 50, lint-todo-marked-done.py gate 57, lint-bare-normative-shall.py gate 56, lint-retention-consistency.py gate 55) accurate. No gate-39 P7 adjacency in the scanned surfaces.

## Orchestrator synthesis

- **A-1 (CHANGELOG 292/282 count)**: classified C1 (stale-count) by subagent A but **REFUTED at apply-time** by a direct recount (292, template 282, matching the CHANGELOG). The asserted-expectation #2 ("classifies all 292 active documents") is correct; subagent A's recount under-matched. Recorded as a refuted finding, not routed. This is the apply-time-verification-before-routing discipline working as designed (a subagent finding is research, not a finding, until the orchestrator re-reads the source).
- **B-1 (`customised`)**: C5 editorial / gate-blind `-ize`. Out-of-window (a pre-existing generator-source string, not introduced by #476..#501; the #499 gate-2 widening is scoped to `.md` authored prose, so this `.py`-source + generated-output surface was never in gate-2's reach). Genuine; fixed this PR (customized + portal regenerated; both `--check` clean).
- **B-2 / B-3 / B-4 / B-5**: C-term-drift / stale-section-pointer, all out-of-window, all on gate-exempt surfaces (`.claude/`, `TODO.md`, a tool docstring). Fixed this PR (§4.26->§1.5; §4.20 reworded to "shipped across #394/#399"; TODO.md exemplars updated to FR-59/FR-60 + a generic single-session caveat; docstring "stays open"->"was still open").
- **Asserted-expectations cross-check**: NO contradiction of any #502 claimed-clean surface. A-1, the only would-be contradiction, is refuted (the asserted count is correct). B-1..B-5 are out-of-window pre-existing.
- **Termination**: empty-delta primary stop at iter 1 (A-1 refuted by deterministic recount; B-1..B-5 enumerated exhaustively and deterministically fixed, verified by re-read + 57/57 green; no new High/Medium findings remain).

## Resulting PR

The Sweep-78 close-out PR (this PR): the 5 B-finding fixes, the FR-24 pending-decision resolution (keep-and-crosswalk, confirmed), the handoff prune (keep current + 1 prior), this history row + detail file.
