# Sweep 87, iteration 1

## Trigger & state snapshot

Triggered by the `/resume` from session-closing handoff PR #684, on branch
`claude/grc-acquisition-reconfirm-brief-k8fppo`. This is the loop-break corpus-wide
`/validate`, the compensating control for the session-closing handoff PR (which skips its
trailing `/validate-pr` + `/retro`), covering the **#680..#683 deltas** of the 2026-07-06
resumed ATTENDED-AUTONOMOUS session (`claude/resume-chptc7`): #680 Sweep-86 close-out, #681
FR-99 (per-control effectiveness metric), #682 FR-15 (the new
[`governance/standard-maturity-assessment-methodology.md`](../../governance/standard-maturity-assessment-methodology.md)),
#683 FR-23 (audit-evidence assembler-verification). #684 is the working-state-only handoff.

State at HEAD `ba58958` (= `origin/main`, #684): library version `2026.07.172`, pack
`1.54.6`, governance rules 12, gates 66, skills 18, commands 11, suite 346. Mechanical
baseline **66/66** at `ba58958`, a descendant of the #683 green-at-`8af65ea` baseline (no
close-vs-start drift). Clone unshallowed before the history-aware gates. Iteration 1.

Pre-flight scanner: 395 files scanned, 31 candidates suppressed (25 heuristic, 6 exemption),
9 surfaced candidates across 3 files, all the standing comparative/generic-word class
(`trust-recovery-escalation.md` "two skills"; `deep-qa-review/SKILL.md` "two skills";
`guardrail-review/SKILL.md` "two"/"five" comparatives) and all confirmed false positives by
Subagent B. Full three-subagent dispatch (A, B, C); no abbreviation.

## Subagent A, Recent-PR deep review

Read all eleven in-scope target files in full plus the two derivation/consumer surfaces
(framework section 2 ladder, the maturity self-assessment template). All cross-checks against
the closing session's asserted-clean claims held (FR-15 five-tier ladder / median-of-medians
/ floor-check consistent across the methodology standard, framework section 2, and the
template; FR-99 three-lines membership identical across register, framework-continuous-assurance,
and procedure-control-testing; FR-23 independent-test linkage consistent; the two pre-existing
known soft spots observed and not re-reported). One finding:

```
tool: subagent-A
ruleId: cross-doc-domain-scope-mismatch
level: note
location: governance/standard-maturity-assessment-methodology.md:34
fingerprint: cross-doc-domain-scope-mismatch:governance/standard-maturity-assessment-methodology.md:34
labels: {in-window: yes, class: C5}
evidence: The new FR-15 methodology standard (line 34) enumerates 11 governance domains
including architecture ("... operations, resilience, supply chain, architecture, developer
security, and AI"), matching the maturity self-assessment template's Architecture domain
section (docs/template-maturity-self-assessment.md:188 "### Section 9: Architecture domain").
The parent framework it derives from enumerates only 10 domains, omitting architecture
(governance/framework-governance-performance-and-improvement.md:27 "... supply chain,
operations, and developer security"; a grep for "architecture" over that framework returns
zero matches). The methodology standard names itself the authoritative methodology reference
behind the framework's maturity-assessment section, so the two domain lists should agree. The
mismatch is a pre-existing omission on the framework Scope line (architecture is a genuine
corpus domain dir, so the 11-domain list is the correct one), surfaced rather than introduced
by FR-15. Note-level because the ladder/aggregation/floor-check themselves are consistent;
only the enumerated domain list diverged.
```

**1 finding (note).**

## Subagent B, Corpus-wide stale-reference sweep

All pre-flight candidates confirmed false positives (prose comparatives, not stale collection
counts). No stale current-tense collection count (66 gates / 12 rules / 18 skills / 11
commands) found on any scanned corpus surface; the numeric hits resolved to third-party tool
descriptions, the Three-Lines-of-Defence model, or exempt pack-README version-history rows.
The new FR-15 doc's reciprocal links are all present and bidirectional (cited by the framework
at :51, the template at L9/L55/L235, `governance/README.md`:49, and the generated portal and
scorecard; its own Related-Documents list reciprocates). Its document-index register row
(`register-document-index-and-classification.md`:77) is internally consistent with the doc's
metadata. No broken intra-corpus references introduced by the delta; no generated-artefact
generation-lag. Cross-check of the closing session's "second-line-enumeration fully resolved
corpus-wide, 0 surviving instances": CONFIRMED, the only corpus hit
(`compliance/procedure-control-testing.md`:218) reads the corrected form; the stale two-function
shorthand survives only in `.working/` records. **0 findings.**

## Subagent C, Audit-programme integrity reviewer

Four-surface gate parity CONFIRMED at 66 (workflow 66 gate steps, runner 66 `run_gate` lines,
pre-commit 66 gate hooks, spec section 6 table 66 rows; the section 5 grouped list and section 6
detailed-prose enumeration both terminate at gate 66; gates 35/39/64 exit 0 standalone).
Collection counts CONFIRMED (12 rules / 18 skills / 11 commands, directory = prose). The FR-15
methodology standard carries no gate obligation (its floor-check is a manual assessor procedure,
not a mechanizable invariant) and is fully enumerated in taxonomy, portal, scorecard, and the
index register. Generated-artefact freshness CONFIRMED (`build-taxonomy.py --check` and
`build-portal.py --check` both in sync). Asserted-clean cross-check ("four-surface parity at
gate count 66; rules 12 / skills 18 / commands 11") fully corroborated. Documentary note only:
the subagent searched for `specification-master-project.md` under `governance/` and did not
find it; the file is at the repository root (it exists and was in the #680 delta), so this is a
subagent scoping artifact, not a corpus defect. **0 findings.**

## Orchestrator synthesis

Three subagents dispatched (A, B, C); no abbreviation. Total synthesized findings: **1**, a
note-level cross-document domain-scope mismatch from Subagent A.

- **A-F1 (cross-doc-domain-scope-mismatch, note, in-window, C5): VALIDATED at source and FIXED
  this PR.** Independently confirmed: `standard-maturity-assessment-methodology.md`:34 lists 11
  domains including architecture; `framework-governance-performance-and-improvement.md`:27 lists
  10, omitting architecture; the template has a `### Section 9: Architecture domain`; `architecture/`
  is a real corpus domain dir; the framework had zero mentions of architecture. The authoritative
  surface is the methodology standard (and the template, which already sections architecture), so
  the framework Scope line is the under-enumerated one. Fix: added "architecture" to the framework
  Scope line (`... operations, architecture, and developer security`), framework
  `1.0.5`->`1.0.6`, Date `2026-07-06`; taxonomy regenerated first, then portal/scorecard
  (scorecard row picked up the version bump); both `--check` clean. In-window fix per the sweep
  in-window protocol (FR-15 #682 surfaced the inconsistency; the fix aligns the derived framework
  to its authoritative methodology standard). Not a contradiction of any asserted-clean surface
  (the asserted-clean claims were about the ladder/aggregation/floor-check, which hold), so not an
  escalated miss; a genuine pre-existing consistency defect fixed at the width of the finding
  (single enumeration, no parallel carrier: the only two domain-list enumerations, the methodology
  doc and the template, already include architecture).

Subagents B and C: 0 findings; both asserted-expectations cross-checks (the second-line-enumeration
corpus-wide resolution; the four-surface 66-gate parity and 12/18/11 counts) CONFIRMED HOLD, so the
closing session's self-assessment is corroborated, no escalated miss. Termination: iteration 1
complete; the single fix does not re-open the window (no parallel enumeration carrier survives).

## Resulting PR

The Sweep-87 close-out PR (the first PR of the resumed session): the framework Scope fix +
regen, this sweep record + history row, the resume-protocol handoff prune (keep current + 1
prior), the lease acquisition + heartbeat, and the version bumps.
