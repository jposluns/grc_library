# Sweep 90 iter 1, 2026-07-08

## Trigger & state snapshot

The `/resume` loop-break corpus-wide `/validate`, the compensating control for the
session-closing handoff PR #713 (which, per the standing handoff-PR exception, skipped its
trailing `/validate-pr` + `/retro`). Covers the #700..#712 delta window of the 2026-07-08
resumed session (`claude/resume-sweep89-deep-assessment`). Run on branch
`claude/resume-sweep90-validate`, attended-autonomous daytime, on the NUC.

- Baseline: HEAD `011c219` (#713); mechanical baseline **66/66** at `011c219` (=
  `origin/main`), a descendant of the closing session's asserted green-at `bc01515`/#712
  (no close-vs-start drift). Clone non-shallow.
- Versions at HEAD: library `2026.07.201`, README `1.9.562`, pack `1.57.0`,
  audit-programme spec `1.16.60`.
- Counts: gates **66**, suite **347**, governance rules **12**, skills **20**, slash
  commands **13**, Document-types **18**.
- Registers at resume: pending-decisions 0, verifier-overrides 0, high-assurance 0 active,
  deep-assessment no in-progress run.
- Pre-flight scanner: 399 files scanned, 33 candidates suppressed (27 heuristic + 6
  exemption file), 9 candidate findings across 3 files, all in the standing
  comparative/historical phrasing class ("two skills"/"two rules"; the "five to twelve /
  handful to twenty" growth narrative in `guardrail-review/SKILL.md`). Passed to all three
  subagents; all confirmed FALSE POSITIVES.
- Iteration 1 (no re-baseline).

## Subagent A, Recent-PR deep review

**0 escalated (error/warning) findings; 1 out-of-window note.** All three asserted-clean
claims independently verified against source.

- **Claim 1, AIQT reframe (#705) complete: VERIFIED.** `grep -rniE 'quality *> *speed'`
  over the pack + `.claude/` + `governance/` + root `.md` returned 9 hits, every one
  whitelisted-historical: the provenance paragraph in `project-integrity.md:106` (both the
  pack copy and the `.claude/` mirror), the rollout narrative in
  `dev-security/claude-rules/CLAUDE.md:144`, three `dev-security/claude-rules/README.md`
  version-history rows (488/546/551), and dated CHANGELOG entries. Zero live carrier.
- **Claim 2, gate 42->44 correction (#703/#704) complete: VERIFIED.** Every live `gate 42`
  (both forms) refers to the external-overlay licence audit; the paired-skill step-parity
  gate is consistently gate 44. No surface asserts the paired-skill parity gate is gate 42,
  and no over-correction relabels the licence audit gate 44.
- **Claim 3, principle-doc §4 AI-assurance mappings accurate: VERIFIED against
  `grc_library_ref`.** NIST AI RMF 1.0: the 7 trustworthiness characteristics match; the
  "Integrity has no direct NIST AI RMF characteristic" caveat is correct. ISO/IEC TR
  24028:2020: the §5.3 anchor for the integrity definition is correct, and the 3.21
  "false-friend" caveat (integrity as the CIA property) is exactly right; definitions 3.1
  accountability, 3.41 trust, 3.42 trustworthiness, 3.47 verification all match. ISO/IEC
  42001:2023 Annex A: A.6.2.4 / A.7.4 / A.6.2.2 / A.6.2.3 / A.6.2.8 / A.8 / A.3 / A.5 /
  A.10.2 all match their cited labels.
- **Principle Document Type propagation (#712): CLEAN** across every enumeration surface
  (both linters, the two hierarchy tables renumbered contiguous 2->18 with Principle at
  level 2, the register, `specification-master-project.md` §4.3, README, CONTRIBUTING,
  the ingestion spec + instruction, taxonomy). No stale numeric cross-reference survives.

**Note-level observation (out-of-window enhancement, NOT a #700-#713 regression):**
- ruleId `doctype-disambiguation-gap`, level note, `specification-master-project.md:176-180`.
  §4.4 "Type selection guidance" disambiguates only Procedure/SOP, Plan/Roadmap,
  Guideline/Guide. The new **Principle** type has semantic adjacency to Charter ("Mandate,
  scope, **principles**, approval model") and Policy ("Mandatory **principles** and
  governance commitments") but §4.4 carries no Principle-vs-Charter/Policy rule. Low risk
  (the hierarchy tables' Function/Purpose columns distinguish the types). Recommendation:
  add a bullet, or dismiss; overlaps the proposed ALLOWED_TYPES-parity gate.

## Subagent B, Corpus-wide stale-reference sweep

**1 finding.**

- **B-1**, ruleId `stale-inventory-count-comment`, level note,
  `tools/lint-guardrail-cadence.py:74`. The `INVENTORY_RE` documentation comment read
  `# carries, e.g. "inventory 66 gates / 12 rules / 18 skills / 11 commands".`; the live
  counts are **20 skills / 13 commands** (skills 18->20 and commands 11->13 landed this
  session in #706/#707). An `e.g.` comment (gate-exempt; the regex matches any digits, so
  linter behaviour is unaffected), but a genuine stale reference that has now re-staled
  twice (Sweep 88 refreshed it 59/17/10 -> 66/12/18/11 on 2026-07-07; #706/#707 re-staled
  it the next day). In-window (drift introduced this session). Recommendation: replace the
  live counts with placeholder tokens so it stops re-staling on every inventory change.

Coverage (0 other findings): generated artefacts in sync (`build-taxonomy.py --check` and
`build-portal.py --check` both exit 0); the Document-Type enumeration is complete and
consistent at 18 across 11 surfaces plus a corpus-wide scan (0 suspects lacking Principle;
both hierarchies levels 1-18 contiguous); zero live non-dated "Quality > Speed" carrier;
counts recounted bare-token from live reality all match (gates 66, rules 12, skills 20,
commands 13, Document-types 18, suite 347).

## Subagent C, Audit-programme integrity reviewer

**0 findings.**

- Suite green at 66/66; the four parity/count meta-gates (35 four-surface parity, 39
  cross-file count consistency, 60 guardrail-review cadence, 64 audit-spec detailed-prose
  presence) all pass.
- Spec §5 grouped list enumerates gates 1-66 with zero missing/duplicate; §6 table has 66
  contiguous rows; the §6 detailed-prose "Gate N is ..." enumeration covers 35-66 with no
  gap (gate 64 now backstops its presence).
- The gate-42/44 mislabel (#703/#704) is fully resolved: every surviving "gate 42" denotes
  the external-overlay licence audit; the paired-skill step-parity gate is consistently
  gate 44 across the linter, spec §5/§6, the worker-brief DO-rail 14, and the r6 record.
- Advisory tools (`audit-gate-blindspots.py`, `audit-gate-mutation.py`,
  `audit-reference-breadth.py`) appear in neither the spec §6 nor any runtime surface;
  nothing falsely claims them as gates.
- Skill (20) and command (13) counts consistent across the pack README / prose; gate 44's
  PAIRS registry includes the new `deep-assessment` and `reference-audit` pairs.
- The new Principle doctype is consistent across the touched linters (`lint-metadata.py`
  ALLOWED_TYPES 18 members + `principle-` prefix; `lint-filename-title-alignment.py`
  DOCTYPES 18 members; `lint-required-sections.py` acknowledges Principle as not-enforced).

## Orchestrator synthesis

Two findings after dedupe (distinct files, distinct classes; C 0 findings). No subagent
divergence, so no debate round. All three subagents dispatched (A, B, C); full fan-out, no
abbreviation.

- **B-1** (in-window, C-class: stale docstring/comment). Fixed this PR. Chose the durable
  fix B recommended (count-free placeholder tokens) over a numeric refresh, because the
  line has re-staled twice; a placeholder stops the recurrence and aligns with the
  Accuracy/Quality tier (an always-accurate comment beats a periodically-stale one).
- **A-1** (out-of-window enhancement). Surfaced to the maintainer with three named options
  (fold into the parity-gate PR / fix now / dismiss). Maintainer chose **fix now in this
  close-out PR**; the §4.4 Principle-vs-Charter/Policy disambiguation bullet was added,
  `specification-master-project.md` `1.6.10`->`1.6.11`, and taxonomy/portal/scorecard
  regenerated.

**Loop-break compensating control result: PASSES.** No finding contradicts any
asserted-clean touched surface from the #700-#712 session. Every asserted claim
(AIQT-reframe-complete, gate-42/44-corrected, Principle-propagated, counts-consistent
66/12/20/13/18, principle-doc §4 claim-fit-verified) was independently corroborated by A/B/C.
B-1 is an ordinary in-window finding (the comment was never in the asserted-clean set);
A-1 is an out-of-window enhancement.

## Resulting PR

This `/resume` Sweep-90 close-out PR (branch `claude/resume-sweep90-validate`): the B-1 and
A-1 fixes, the sweep cursor advance to Sweep 90, the handoff prune to current + 1 prior, the
lease acquire, the version bumps (library `2026.07.202`, README `1.9.563`,
`specification-master-project.md` `1.6.11`, this history `2.0.85`), and the regenerated
taxonomy/portal/scorecard.
