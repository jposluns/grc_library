# Sweep 104, iteration 1

## Trigger & state snapshot

- **Trigger**: the `/resume` from session-closing handoff PR #917 (loop-break compensating control). Corpus-wide `/validate` over the **#915..#917** delta window (the 2026-07-14 resumed session's closing PRs: #915 Sweep 103 `/validate` close-out + gate-50 Check-5 §6 prose fix; #916 P1 §1.2 root-CHANGELOG #902-#914 compact reformat + readability-guard strengthening; #917 session-closing handoff).
- **State at HEAD**: library `2026.07.405`, README `1.9.766`, pack `1.61.6`, spec `1.17.4`; gate **69**, rules **13**, skills **23**, commands **14**, Document-types **18**.
- **Mechanical baseline**: `tools/run_all_audits.sh` = **69/69** at `9cb42fe`/#917 (a descendant of the closing session's asserted green-at `7596f18`/#916; no close-vs-start drift). Clone non-shallow (unshallowed this resume).
- **Pre-flight scanner**: 421 files scanned, 33 candidates suppressed (27 heuristic + 6 exemption), **11 candidates** surfaced, ALL the collection-count-word false-positive class; passed to all three subagents.
- **Iteration**: iteration 1 (this is the sole iteration; findings are note/warning only, no High/Medium, so no re-dispatch cycle).

## Subagent A, Recent-PR deep review

Verdict: **substantively clean** (2 note, 0 warning, 0 error). All five verification targets confirmed:

- **Reformat faithfulness (#916)**: all six sampled compact root entries (#902, #905, #911, #912, #913, #914) compared against the detailed mirror; no material fact altered (PR numbers, dates, version tokens, control codes ASVS 4.0.3->5.0.0 / TLS 1.3 / 4096-bit RSA / P-384 ECDSA / ISO/IEC 20000-1 "11 clauses across 7 docs" / SLA 14/30/90, and counts). Every `**date | version | PR #N**` header byte-identical on both sides (gate-59 parity holds); compression dropped detail only, never a fact.
- **Readability guard (#916)**: two-signal logic sound; live run exit 0, 867 entries, densest sentence PR #909 at 60 words (confirms the handoff's "densest 60"); `--self-test` 6/6.
- **gate-50 Check-5 §6 prose (#915)**: present and correct at `governance/specification-audit-programme.md:152`; spec 1.17.4 confirmed.
- **Version / generated-artefact sync**: README `2026.07.405` / `1.9.766`; spec `1.17.4`; `build-taxonomy.py --check` and `build-portal.py --check` both exit 0; version sequence #915->#916->#917 monotonic; the #917 "74-147"->"67-140" F1 correction propagated (only historical `.working/` strings retain the old figure).
- **#917 asserted-expectations**: match what shipped; the one `.working/` F1 table-row-join escape is honestly disclosed, not asserted clean; Sweep 104 correctly named as the compensating control. No asserted-clean surface contradicted.

Findings (both NOTE, rubric K, advisory-tool docstring, gate-blind, non-behavioural):

```
tool: subagent-A
ruleId: guard-docstring-sentence-vs-total-conflation
level: note
location: tools/audit-changelog-entry-length.py:31
fingerprint: guard-docstring-sentence-vs-total-conflation:tools/audit-changelog-entry-length.py:31
rubric: K
evidence: Line 31 deployed the 67-140 TOTAL-word range as a single-SENTENCE figure ("one 67-140-word ... sentence") and paired it with "under the 130-word total ceiling", though PR #911 (140) and PR #912 (132) exceed 130; a "140-word sentence under the 130-word total ceiling" is internally contradictory. Measured per-single-sentence range was 24-83 (max PR #909=83). Meta-prose-measurement class (same as the #916 F1/F2), left un-tightened when #917 corrected the numbers but not the sentence-vs-total framing.
```

```
tool: subagent-A
ruleId: sentence-splitter-omits-question-exclaim-terminators
level: note
location: tools/audit-changelog-entry-length.py:79
fingerprint: sentence-splitter-omits-question-exclaim-terminators:tools/audit-changelog-entry-length.py:79
rubric: K
evidence: The splitter regex split only on . ) " ' followed by whitespace; ? and ! were absent, so "Foo? Bar." merges into one measured sentence (the false-POSITIVE direction the docstring said impossible). Cannot fire on the declarative CHANGELOG summaries (live run scanned 867 clean); advisory tool (exit 0), so at worst a spurious compress candidate, never a build failure.
```

## Subagent B, Corpus-wide stale-reference sweep

Verdict: **CLEAN** (0 error, 0 warning, 2 informational notes).

- **11 pre-flight candidates: 11/11 confirmed false-positive** (collection-count-word class; each a prose word enumerating a local set, never a canonical-count claim). Examples: `compliance/healthcare/annex-healthcare-united-states.md:27` "the three [HIPAA] rules"; `dev-security/claude-rules/governance/session-lifecycle.md:24` "Two rules hold in every mode"; `dev-security/claude-rules/governance/trust-recovery-escalation.md:26` "a suite of two skills"; five overlap-lens examples in `guardrail-review/SKILL.md`. 0 escalated.
- **Count drift**: CLEAN. Live counts verified against source-of-truth (`run_all_audits.sh` 69 `run_gate`; spec §6 69 rows; governance dir 13; skills 23; `.claude/commands/` 14; `lint-metadata.py` 18). No live SCANNED-surface prose states a stale total; numeric near-misses (external promptmap2 "69 rules", domain content, fixtures, thresholds) triaged and dismissed.
- **§1.2 closure orphans**: CLEAN. TODO headings 1.1 -> Priority 2, no orphan `### 1.2`; all live `§1.2` carriers resolve (closure records; the intentional recycled-number-hazard note at `TODO.md:225`; `procedure-capa.md` pointing at the Internal Audit Standard §1.2 Scope table, unrelated to the TODO renumber).
- **Term drift / date-currency (window)**: CLEAN.

Findings (both NOTE, informational):

```
tool: subagent-B
ruleId: recycled-section-in-tool-docstring
level: note
location: tools/audit-changelog-entry-length.py:4
rubric: I
evidence: "strengthened by the P1 §1.2 reformat PR" carried a RECYCLED §-number provenance reference; naming the PR (#916) is more durable once §1.2 recycles again. Backward provenance, not a dangling forward pointer.
```

```
tool: subagent-B
ruleId: capa-source-field-points-at-domain-table
level: note
location: compliance/procedure-capa.md:126
rubric: I  (LOW confidence, OUT-OF-WINDOW, pre-existing)
evidence: The "Source | ...(see Section 1.2 table)" field of the Internal Audit Standard points at a Domain/Description table, not a source enumeration; the parallel "Domain" field (line 125) points there correctly, so the "Source" target is a possible semantic mismatch. Reference resolves (a §1.2 table exists), gates 62/65 green; semantic-fit is not gate-checkable and out of this window.
```

## Subagent C, Audit-programme integrity reviewer

Verdict: **audit-programme integrity essentially CLEAN** (1 warning, 0 error, 0 note).

- **Four-surface parity at 69: HOLDS** (workflow 69 corpus gate steps; runner 69 invocations; pre-commit 69 numbered gates + the non-gate `regenerate-derived-artefacts` helper, legitimately excluded by gate 35; §6 table 69 rows). §5 grouped list enumerates gates 1-69 once each; §6 detailed prose runs through gate 69 with correct appended-ordering ranges.
- **gate-50 Check-5 §6 fix (#915): COMPLETE** (line 152 carries both the "Gate 50 is a..." + "Gate 50 is appended..." sentences and describes the fifth check consistently with checks 3-4).
- **Fixtures**: four Check-5 regression fixtures present in `tests/test_linters.py` (`test_register_row_order_ascending_passes`, `_misordered_flagged`, `_duplicate_flagged`, `_empty_passes`).
- **Readability tool correctly NOT a gate** (absent from all four surfaces; keeping it out preserves the 69 count).

Finding (WARNING, rubric I, gate-blind docstring):

```
tool: subagent-C
ruleId: stale-docstring-check-count
level: warning
location: tools/lint-bookkeeping-parity.py:26
fingerprint: stale-docstring-check-count:tools/lint-bookkeeping-parity.py:26
rubric: I
evidence: Line 26 reads "The four checks:" but the docstring enumerates FIVE (Check 1 line 28, Check 2 line 53, Check 3 line 63, Check 4 line 81, Check 5 line 95). The header count is stale; introduced by #913 (Check 5 + four fixtures added, header count not bumped) and not caught by #915's §6-prose fix. Gate-invisible (no gate checks a per-gate check-count in a .py docstring); the parallel spec §6 prose is NOT stale. Internal count-vs-enumeration contradiction, the "Audit-gate change completeness" free-prose class.
```

## Orchestrator synthesis

- **Loop-break control for #917: PASSES.** No in-window escaped adopter-facing defect; all findings are docstring/rationale accuracy in two `tools/` files (one gate module docstring, one advisory tool). Every #917 asserted-clean surface was independently corroborated by Subagent A (reformat faithfulness, guard logic, gate-50 §6 fix, version sync); **0 asserted-expectation contradictions**, so no escalated miss.
- **Dedupe**: A-1, A-2, B-1 all land on `tools/audit-changelog-entry-length.py` (distinct lines/claims, not duplicates); C-1 on `tools/lint-bookkeeping-parity.py`. B-2 is a distinct pre-existing surface.
- **Severity adjudication**: C-1 should-fix-this-PR (a genuine count-vs-enumeration contradiction, the exact class the CLAUDE.md "Audit-gate change completeness" checklist names). A-1, A-2, B-1 track-as-follow-up by default but fixed-in-window here because they are cheap accuracy fixes coherent with the sweep, in files the close-out already touches. B-2 track-as-follow-up, out-of-window, routed to the maintainer.
- **In-window fixes applied (4), all verified**:
  - C-1: `tools/lint-bookkeeping-parity.py:26` "The four checks:" -> "The five checks:". Gate 50 re-run standalone: OK.
  - A-1: `tools/audit-changelog-entry-length.py` line 31 reworded to frame 67-140 as a total-word range (not a single-sentence figure) and drop the false "under the 130-word total ceiling" universal.
  - A-2: same file, splitter regex `[.)\"']` -> `[.?!)\"']` (adds `?`/`!` terminators), with the comment (lines 75-78) and docstring (lines 34-38) reconciled; the change can only add split points (false-negative-safe), so the docstring's "false positive not possible" claim is now true. `--self-test` 6/6; live run unchanged (densest sentence still PR #909 at 60 words).
  - B-1: same file, docstring line 4 recycled `§1.2` -> "PR #916, the root-CHANGELOG reformat".
- **Routed (out-of-window)**: B-2 (`compliance/procedure-capa.md:126` Source-field semantic mismatch) surfaced to the maintainer; low-confidence, pre-existing, gates green, semantic-fit not gate-checkable. Default: leave/track.
- **Pre-flight candidates**: 11/11 dismissed as the standing collection-count-word FP class (no accept-list churn; this class recurs and is re-dismissed each sweep).
- **Close-out re-baseline "catch" (WITHDRAWN as a PHANTOM; corrected in PR #919)**: before adding the #918 CHANGELOG entry, the orchestrator believed PR #917's handoff row lacked the gate-50-recognized marker and "fixed" it. The #918 `/validate-pr` (Subagent A) later refuted this, re-verified by the orchestrator: at the parent `9cb42fe`, #917's **Findings cell** (`c[4]`, the cell gate 50 reads) already carried `SKIPPED (handoff-PR exception)`; the orchestrator had read the **Touched-files cell** (`c[3]`, which begins "SESSION-CLOSING HANDOFF PR" and which gate 50 does not read), and the edit landed in `c[3]`, leaving `c[4]` byte-identical. `parse_validate_pr_status` reads `c[4]`, which matched all along, so #917 was always handoff-exempt: no latent gate-50 failure ever existed. The redundant `c[3]` edit is reverted in #919. The genuine Sweep 104 findings are the 1 warning + 4 note above (this withdrawn item is NOT one of them). Lesson: an inferred-as-verified state assertion (characterizing a cell without reading the actual Findings cell) is the failure class, not a "latent-gate" catch; logged in the #918 `/retro`.
- **Re-baseline**: `run_all_audits.sh` re-run on the fixed state = 69/69 (the genuine fixes are a gate-module docstring + an advisory-tool docstring/regex; no gate behaviour changed), verified WITH the #918 CHANGELOG entry present so gate 50 Check 1 sees #917 demoted from highest-numbered (which passes on #917's pre-existing Findings-cell marker, per the withdrawn item above).

## Resulting PR

The Sweep 104 `/validate` close-out PR (this resume's first PR): the four in-window fixes above, this detail file, the history row, the handoff prune (kept #917 + #914, dropped #900), the lease ACQUIRE, the `next-prs.txt` refresh, and the version bump + CHANGELOG entry.
