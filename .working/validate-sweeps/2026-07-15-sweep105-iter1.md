# Validation Sweep 105, iteration 1 (2026-07-15)

## Trigger & state snapshot

- **Trigger**: the `/resume` loop-break corpus-wide `/validate` for the 2026-07-15 long resumed session's session-closing handoff PR #942 (and the intervening #918..#942 window). The session-closing handoff PR skips its trailing `/validate-pr` + `/retro`; this corpus-wide sweep is the compensating control.
- **Window**: **#918..#942** (git range `9cb42fe`..`7651a1b`, 25 PRs). Substantive surface: the `.web/` static-site generator + templates (#919-#928, #940, #941), the one corpus-document edit `dev-security/guideline-ai-coding-assistant-security.md` (#939 AICM matrix-fit), the fence-predicate consolidation (#937, `tools/lint_common.py` + 8 linters), and assorted `tools/` / spec / `.claude/CLAUDE.md` / generated-artefact changes.
- **Mechanical baseline**: `tools/run_all_audits.sh` = **69/69** at HEAD `7651a1b`/#942, a descendant of the closing session's asserted green-at `3fc2a0c`/#941 (no close-vs-start drift). Clone non-shallow.
- **Versions at HEAD**: library `2026.07.430`, pack `1.61.6`, spec `1.17.6`; gate **69**, rules **13**, skills **23**, commands **14**, document-types **18**.
- **Pre-flight scanner**: 421 files, 33 suppressed (27 heuristic + 6 exemption), **11 candidates, ALL the collection-count-word false-positive class** ("the three [HIPAA] rules", "Two rules hold in every mode", "a suite of two skills"), dismissed by all three subagents.
- **Iteration**: 1 (no re-baseline needed; the single finding is a non-gated `.web/` file, fixed in this close-out).
- **Environment**: gh-CLI (no GitHub MCP), attended-autonomous on the VM.

## Subagent A, Recent-PR deep review

**One finding (warning, CONTRADICTS-ASSERTED-CLEAN); everything else verified clean at source.**

```
tool: sweep-105-subagent-A
ruleId: generator-secondary-sort-divergence
level: warning
location: .web/build.py:306
fingerprint: generator-secondary-sort-divergence:.web/build.py:306
CONTRADICTS-ASSERTED-CLEAN
evidence:
  The #940 "two generators" domain-doc ordering is NOT consistent between the two
  generators on the CURRENT corpus, despite TYPE_ORDER being byte-identical.
  `.web/build.py:306` sorts each domain's docs case-SENSITIVELY:
    `ddocs = sorted(ddocs, key=lambda d: (TYPE_RANK.get(d["type"], len(TYPE_ORDER)), d["title"]))`
  while `tools/build-taxonomy.py:96` sorts case-INSENSITIVELY with a path tiebreaker:
    `return (domain, TYPE_RANK.get(doc_type, len(TYPE_ORDER)), title.lower(), rel)`
  A same-file reproduction using both files' exact keys shows divergent per-domain
  document order in 6 of the domains (operations, dev-security, ai, privacy, security,
  compliance). Concrete materialized example, confirmed in a temp `.web/build.py` render:
  the compliance domain page lists "eIDAS Sector Requirements Annex" LAST (case-sensitive:
  leading lowercase 'e' sorts after all uppercase-initial titles), whereas taxonomy.yml /
  portal.md place it among the E-annexes (case-insensitive). Similarly the ai page orders
  "AI and Agentic Development Security Standard" last while taxonomy orders it first.
  This CONTRADICTS session-handoff.md "## Asserted expectations": "#940 is lossless +
  consistent ... TYPE_ORDER IDENTICAL". PRECISION: the narrow sub-claim "TYPE_ORDER
  IDENTICAL" is TRUE; the contradicted claim is the header "consistent" (the two surfaces
  present different reading progressions within a type). Root cause is an unsynchronized
  SECONDARY sort key, a companion to (not covered by) the deferred TODO 3.76 "TYPE_ORDER
  DRY guard" soft spot, which is scoped to the primary rank only. Severity cosmetic
  (ordering only; no data loss, no wrong doc, no broken link) but user-visible. Fix: make
  `.web/build.py` sort key `(rank, d["title"].lower(), d["path"])` to match.
```

**Verified clean** (no findings): (1) AICM matrix-fit #939, all 7 replacement codes (GRC-09 "Acceptable Use of the AI Service", DSP-07 "Data Protection by Design and Default", AIS-10 "Output Validation", AIS-15 "Prompt Differentiation", AIS-11 "Agents Security Boundaries", TVM-13 "Guardrails", SEF-08 "Security Breach Notification") real AICM v1.1.0 controls and plausible semantic fits for their rows; zero fabricated `AI-GOV/AI-DATA/AI-SEC/AI-INC` codes remain; Version co-bumped 1.3.5->1.3.6, Date 2026-07-15. (2) Fence-predicate consolidation #937 behaviour-preserving; 8 linters route through `is_fence_line()`; only `lint-unbalanced-fences.py` retains a private (already tilde-aware) check, out of scope. (3) TYPE_ORDER tuples byte-identical in both generators (17 types); all three `--check` exit 0; 312 docs preserved. (4) Landing-nav #941, all 6 `std-*` anchors resolve, 11 domains + 6 Standards subs nest, Contributors -> /about, page-scoped. (5) Generated-artefact sync clean. (6) All 11 pre-flight candidates (across 6 files) confirmed false positives.

## Subagent B, Corpus-wide stale-reference sweep

**Zero findings.** All five focus classes clean; every asserted-clean surface CORROBORATED, 0 contradicted.

- **Class 1 (fabricated AICM codes)**: `grep -rnE '\bAI-(GOV|DATA|SEC|INC)-[0-9]'` over corpus bodies = **0 hits**; #939 remap complete; legitimate `OFFAI-SEC-*` family (different construct) not flagged.
- **Class 2 (closed-section / renumber orphans)**: whole-repo grep for `§2.5`/`§3.8`/`§3.10`/`§3.50`/`§3.47`/`§3.57`/`§3.62`/`§3.63`/`§3.68` incl. gate-exempt carriers (`.claude/CLAUDE.md`, spec, tool docstrings) = **0 stale TODO-section pointers**. The two `§2.5` hits are external-standard section refs (`ai/standard-ai-inference-cost-governance.md:209` NIST AI 600-1 §2.5; `TODO.md:181` a different document's own section). No duplicate `### N.M` numbers.
- **Class 3 (count drift)**: no live stale counts; README version-history rows ("18 to 19 skills") legitimate historical; independently counted skills 23 / commands 14 / rules 13 / gates 69, all match.
- **Class 4 (term drift)**: every `governance/` "GRC Manager" already canonical "GRC Programme Manager"; residual bare form only in TODO §3.71 (the routed fork's own description) + the docs it lists (tracked/expected, not a miss).
- **Class 5 (website content-boundary)**: `.web/build.py` inlines only titles/types/paths + each domain README `## Purpose` intro; no corpus BODY prose embedded; no boundary violation.
- Pre-flight candidates all confirmed FP.

## Subagent C, Audit-programme integrity reviewer

**Zero findings.** Every focus check clean; all asserted-clean surfaces corroborated, 0 contradicted.

1. Four-surface parity at gate 69, gate 35 standalone EXIT=0.
2. Gate 39 collection-count consistency across 529 files EXIT=0 (gates 69, rules 13, skills 23).
3. Gate-69 #933 widening reflected in all gate-35-blind free-prose surfaces (§6 detailed prose `spec:178`, §5 grouped-list `spec:67`, module docstring, two regression fixtures).
4. #937 fence consolidation: `is_fence_line()` at `lint_common.py:368`, consumed by 8 linters + lint_common; no §6 drift.
5. #934 preflight link-resolution is an AID, not a gate (not wired into workflow/runner/pre-commit/§6). Correct.
6. `web-generator-health.yml` a separate CI check, not counted as an audit gate. Correct.
7. Regression suite: 381 tests EXIT=0 (the "FAIL: claude-rules sync" lines are expected fixture output; overall pass).
8. `build-taxonomy.py --check` and `build-portal.py --check` both EXIT=0.
9. Collection counts: commands 14, skills 23, rules 13, document-types 18, all match prose + gate 39.
10. Asserted-expectations: spec 1.17.6, pack 1.61.6, gate 69, rules 13 / skills 23 / commands 14, doc-types 18 all CORROBORATED; green-at `3fc2a0c`/#941=69/69, HEAD `7651a1b`/#942 working-state only, live 69/69.
11. Pre-flight FP candidates all confirmed false positives. No concurrency artefacts.

## Orchestrator synthesis

- **Dedupe / provenance**: one finding total (A-1), from Subagent A alone; B and C did not touch the `.web/build.py` sort-key claim (their scopes did not cover the generator secondary key). No duplicates, no cross-subagent divergence, no debate needed.
- **Classification**: A-1 is failure-mode class **C2 (multi-surface incompleteness) / generated-artefact-parity**, a paired-generator secondary sort key left unsynchronized. In-window (#940). Tag **R** (real; the orchestrator confirmed it at source: `.web/build.py:306` uses `d["title"]`, `tools/build-taxonomy.py:96` uses `title.lower(), rel`; a temp render placed "eIDAS ... Annex" last vs among the E-annexes canonically).
- **Asserted-expectations cross-check**: A-1 **CONTRADICTS** the #929-#942 handoff's asserted-clean claim that "#940 ... is lossless + consistent". The narrow "TYPE_ORDER IDENTICAL" sub-claim is TRUE, but the "consistent" header claim is false on the current corpus, so this is a **genuine miss of the closing session's self-assessment**, escalated as such (surfaced to the maintainer in chat), not triaged as an ordinary out-of-window finding. B and C found 0 contradictions.
- **Severity adjudication**: `should-fix-this-PR`. Cosmetic (ordering only; no data loss, wrong doc, or broken link) but user-visible on the live site, in-window, trivial + safe to fix, and a website change (this session's stated priority). Not a High/Medium block-merge finding.
- **Action**: **FIXED this close-out.** `.web/build.py:306` sort key aligned to `(TYPE_RANK..., d["title"].lower(), d["path"])`, matching `tools/build-taxonomy.py:96`. Verified: `.web/build.py --check` EXIT=0; a temp render now orders "eIDAS Sector Requirements Annex" among the E-annexes (DORA -> eIDAS -> Energy), matching the canonical taxonomy/portal order. No committed generated artefact changes (`.web/dist/` is git-ignored, rebuilt on deploy; `taxonomy.yml` already sorted correctly). Follow-up: TODO §3.76 (the TYPE_ORDER DRY guard) should be extended to cover the secondary sort key, not only the primary rank; routed to the backlog.
- **Termination**: iteration 1 produced 0 error / 1 warning / 0 note (no High/Medium). Per the termination rule the sweep completes after this iteration; the one warning is fixed in-window and re-baselined (`run_all_audits.sh` stays 69/69, since `.web/build.py` is not an audit gate). **Loop-break control for #942 PASSES**: one in-window user-visible defect found and fixed, no escaped adopter-facing defect beyond it.

## Resulting PR

The Sweep 105 `/resume` close-out PR (this PR): the A-1 fix, this detail file, the history row, the resume-cursor advance, the handoff prune, and the lease acquire.
