# TODO

Forward-looking backlog of planned enhancements for the GRC Documentation Library. Items are added when identified and rotated out when completed. Completed items move to [`.working/DONE.md`](.working/DONE.md) (closed-TODO ledger, keyed by original backlog ID); historical change detail lives in [`CHANGELOG.md`](CHANGELOG.md). This file holds only pending and queued items.

This file is informational and is not subject to the library's metadata-block, audit-conformance, or version-tracking conventions, with one narrow exception: [`tools/lint-todo-staleness.py`](tools/lint-todo-staleness.py) (gate 45) scans this file for the queued-PR-already-merged drift shape (its companion sweep-cursor-behind-history check reads the resume cursor from [`.working/session-handoff.md`](.working/session-handoff.md)). The intra-document section-reference gate also scans it. Other audit gates skip this file.

**How items are numbered and formatted.** Items are grouped by priority (P1-P7), tiered by work type (maintainer's 2026-06-30 leaning): P1 fix errors and prevent recurrence, P2 fill significant gaps, P3 clean up and tooling, P4 adopter experience, P5-P6 expand the corpus (overlays, then new domains), P7 awaiting decision. **Every item is a `### N.M` subsection whose leading digit is its priority** (so a P3 item is `3.x`); within a section items run lowest-effort-first. The `N.M` number is a position, not a permanent id, so it changes if an item is re-tiered or resequenced; the **stable id is the `FR-N` / descriptive identifier in the heading**, and a `(was X.Y)` tag preserves the prior number for one cycle so older references (CHANGELOG, handoff, retro log, tool docstrings) stay resolvable. Each heading carries `(id, severity, effort)` where severity is `H[critical]` / `H` / `M` / `L` / `FYI` and effort is `XS` / `S` / `M` / `L` / `XL` (scale in 3.1). A `⚠` marks a persona-quoted finding to verify at action time.

---

## Queueing rules

- Orchestrator picks the next batch from **Priority 1 first, then Priority 2**, in highest-severity order; within a chosen section the effort ordering helps assemble like-effort batches.
- **Start-side worker-collision check (before starting any item).** Before starting to build any TODO item, check the scratch `claims-ledger.md` and `research/COVERAGE.md` for an in-flight claim or a pending inbox delivery covering it; a claimed or delivered item is apply-work (validate-then-apply on the delivery), not build-work, so starting to build it would duplicate a worker's effort or collide with a pending delivery. This is the start-side complement to the close-out worker-brief coverage-pairing obligation, and it fires whenever the queue is resumed mid-session, not only at `/resume`. The operational form (where to look, what an in-flight claim row versus a delivered inbox row means) is in the multi-session-orchestration runbook.
- **1-8 PRs per batch** (logical grouping); `/validate` after each batch.
- Maintainer direction supersedes the orchestrator's pick at any time.
- Lower priorities (P3-P7) are picked deliberately, not from the routine batch queue, unless the maintainer triggers them.
- **Maintainer-directed running order (2026-06-30 work-type re-tier)**: work the tiers in order. **P1** first (the cheap fix/prevent items: the §1.5 integrity tooling; the former audit-gate-candidates section is COMPLETE, S3's `/claim-fit` skill shipping as the last of them, 2026-07-04; FR-48 is COMPLETE, 2026-07-03, #596 through #607); then **P2** gaps (FR-59 and FR-60 deepenings and the deepen-baselines cluster first, then **FR-70**, the XL crypto-asset domain, last); then **P3** clean-up-and-tooling; then **P4** adopter experience. **P5 / P6 expansion waits** (maintainer: expansion can wait). The standing **fix-issues-first** directive (2026-06-27) governs within each tier, and the routine `/validate`, `/validate-pr`, `/matrix-fit`, and `/claim-fit` cadences are the reactive half of P1. FR-167, the NIST SP 800 ingestion, and the OT post-ingestion validation are COMPLETE (2026-06-30; ingestion in `grc_library_scratch`, validation in #495).
- **Integrity-tooling items** (the former "guard-rails phase"): the reference version-currency residuals (§1.5) live in **P1** (fix errors and prevent recurrence; the sibling S3 citation-precision instrument is COMPLETE, the `/claim-fit` skill and Tier-A pass shipped 2026-07-04, closing the former audit-gate-candidates section); the QA-cadence and TODO/DONE-rotation surfaces are already covered by the bookkeeping-parity gate family (gates 50 and 57, plus the D5 PR-time check), and the retro guard rails (the former retro-log consolidation item) closed across #510 (the new-skill-drafting checklist) and #511 (the word-form count gate). Research fan-out (workers produce verified research from [`.working/worker-brief-template.md`](.working/worker-brief-template.md); the orchestrator re-verifies every claim at apply-time and authors all final prose) is the standing method for partitionable batches.

---

## Priority 1 — Fix errors and prevent recurrence

Correctness fixes and the **error-prevention tooling** that keeps the corpus from regressing. The routine `/validate`, `/validate-pr`, `/matrix-fit`, and `/claim-fit` cadences are the reactive half of this tier; the items below are the standing preventive half.

### 1.5 Reference version-currency residuals (from the register version-currency build, #505) (S each)

The §1.5 reference version-currency register **shipped in #505** (the `Upstream check location` + `Last verified (UTC)` columns across all 16 register tables, the advisory staleness cadence in [`specification-citation-verification.md`](governance/specification-citation-verification.md) §12.3, the scratch-is-storage / upstream-is-authority principle, the §7 publisher allow-list extension, and 7 upstream-confirmed register corrections), and **all three citation version-upgrade follow-ups have now shipped** (ISO/IEC 27033 -> 27033-1:2015 #506; ISO/IEC 27036-2 2014 -> 2022 #507; NIST SP 800-88 Rev. 2 re-point + IEEE 2883 introduction #508). Remaining residuals:

**The full `needs-reconfirm` currency sweep ran in #751** (applying the `worker-20260709-fable/currency-ledger-sync` delivery, all 49 register rows verified upstream 2026-07-09): 41 confirmed-current rows stamped with their upstream URL + verified date; 5 stale rows corrected to the current instrument (US CMMC + the 48 CFR/DFARS rule, WCO SAFE 2025 edition, NERC CIP-015, ISO 16484 2024/2025 parts, TSA SD Pipeline-2021-02F) with the downstream corpus citations migrated corpus-wide; and 1 data-defect corrected (NIST SP 1900 re-specified as a subseries). Remaining residuals (per the correct-or-TODO SOP):

- **ICAO Doc 10026 mis-citation (error, maintainer decision):** the register row and [`compliance/logistics/annex-logistics-sector-requirements.md`](compliance/logistics/annex-logistics-sector-requirements.md) cite "ICAO Doc 10026" as the Manual on Aviation Security, but Doc 10026 is the Report of the Legal Commission; the aviation-security manual is ICAO Doc 8973 (Restricted, not publicly citable). Decide the intended reference (Doc 8973, Annex 17 to the Chicago Convention, or drop the citation) and correct both the register row and the logistics annex together. Held for the maintainer's decision because Doc 8973 is Restricted.
- **EN 54 per-part currency enumeration:** the multi-part CEN EN 54 fire-detection series row stays `needs-reconfirm` (no single fetchable edition page); enumerate the cited parts and confirm each upstream, then stamp the row.
- **Register-currency sync-tool (P3 tooling):** build the advisory register-currency drift-check tool per the delivery's `sync-tool-spec.md`, keeping the corpus register in sync with the `grc_library_ref` currency ledger (sibling to the GR-GAP-1 require-registration gate; advisory, not a gate).
- **Ref-side WCO SAFE refresh (cross-repo):** `grc_library_ref` holds the superseded 2021 SAFE edition; refresh it to the 2025 edition via the superseded-archival workflow (a `grc_library_ref` PR).

The 51-row deferral (maintainer 2026-07-02, pending the egress instance) is discharged; egress and WebSearch currency-confirmation were available this session.

## Priority 2 — Fill significant gaps

Deepening thin-but-present content to operational sufficiency, and the significant missing capabilities. The deepen-baselines cluster (FR-15 / 23 / 63 / 74 / 99 / 154 / 41) was decided for operational deepening (maintainer 2026-06-25).

**Egress-deferred legislation deepenings (Option B, maintainer 2026-07-06; SUPERSEDED IN PART 2026-07-09):** originally the deepenings citing primary legislation whose currency could not be confirmed upstream were deferred pending the maintainer's egress instance, like the 1.5 item: 2.1 (FR-59 remaining batch 2), and the jurisdiction annexes (Colorado and EU AI Act, and 5.8 Mexico). **Update 2026-07-09:** WebSearch confirms upstream currency in this environment (EUR-Lex full-text remains unfetchable), so the maintainer directed proceeding with the held annexes via WebSearch-currency + held-source + a pending-amendment caveat. The EU AI Act annex was APPLIED in #743 (with a Digital-Omnibus-pending caveat); 1.11 Brazil closed in #746 against the held DOU text. The Colorado AI Act annex (FR-62) was APPLIED in #749, closing FR-62; the remaining delicate builds (5.8 Mexico, EU MDR/IVDR) were sequenced for a fresh session's context. Only 1.5 (reference version-currency) and FR-59's remaining source-gated jurisdictions still genuinely wait on the maintainer's egress/source drops. The non-legislation deepenings proceeded this session: FR-99 (2.4) closed in #681, FR-15 (2.5) in #682, FR-23 (2.6) in #683, FR-63 (2.7) in #697.

### 2.1 Privacy jurisdiction annex operational deepening (FR-59, H, L)

Privacy jurisdiction annexes are too shallow for operational sufficiency; deepen the 25 country annexes to operational level. **Reference-base aid (2026-06-27 scratch-review S-7)**: `grc_library_ref/legislation/` now holds primary-source full-text for Japan APPI, Virginia VCDPA, Colorado Privacy Act, and the LATAM laws (Mexico, Brazil, Argentina, Colombia, Uruguay); author the deepening against those held sources. Honest caveat: APAC beyond Japan (Singapore, Australia, Korea, China, India) remains maintainer-drop (absent from the ref base). **Progress (2026-07-04, held-source batch PR 1)**: the Japan, United States, Canada, and Brazil annexes are deepened to operational level from the held texts (upstream-confirmed current at apply time). Remaining: the Latin America annex correction-plus-deepening with the paired index update and the standalone Mexico annex (batch PR 2, delivery in hand), then the 18 source-gated annexes, which wait on maintainer source drops (gap analysis delivered at `inbox/worker-20260703-a/fr-59-privacy-jurisdiction/research-source-gated-gaps.md`). Out of this item's remaining set by design: the EU annex (fr-74-owned) and the UK annex (already operationally deep; further held-source pinning available-for-later per the delivery manifest). **Deferred pending egress (Option B, maintainer 2026-07-06):** the remaining Latin America correction-plus-deepening and the standalone Mexico annex (fr-59 batch 2, delivery in hand at `inbox/worker-20260703-a/fr-59-privacy-jurisdiction/`) cite primary legislation whose currency cannot be confirmed upstream this session, so they wait on the egress instance. **Accepted-unverified (fr-59 finding): DISCHARGED in #750.** The Latin America annex Mexico entry, the paired index summary row, and the enforcement figure were corrected to the 2025 LFPDPPP (DOF 20-03-2025, Secretaría Anticorrupción y Buen Gobierno authority, UMA fines), and a dedicated standalone [`privacy/jurisdictions/annex-privacy-mexico.md`](privacy/jurisdictions/annex-privacy-mexico.md) was added, verified against the held text with WebSearch upstream-currency confirmation.

### 2.3 Crypto-asset / blockchain governance domain (FR-70, H[critical], XL)

New domain for crypto-asset / blockchain governance — digital-asset custody, staking, smart-contract risk, blockchain platform vetting. Regulatory references: DORA, MiCA, NYDFS BitLicense. (Cross-references P6.x for domain-level shaping.)

---

## Priority 3 — Clean up and tooling

Cross-document consistency cleanup and routine development / quality tooling: lower-priority than gaps, not error-prevention or adopter-facing. Picked deliberately into batches, not from the routine P1/P2 queue.

### 3.1 Sweep 3 follow-up: cross-document term/identifier consistency (L, S)

Cross-document term-and-identifier consistency gap (the prose-and-numbering C3 surface mechanical gates 35/39/41 don't cover). Candidate for a future mechanical gate; a manual sweep works the items meanwhile. **Both original bullets (the edition-explicit `ISO/IEC 27001:2022` header harmonization, 9 headers found and fixed; the stray-punctuation cells, all 7 recorded cells plus the supply-chain matrix's mangled legend line fixed) SHIPPED in the 2026-07-04 sweep PR**; the section stays open on the residual below.

- Sibling `/IEC`-form inconsistency (surfaced by the 2026-07-04 sweep census). **#650 (2026-07-05) shipped the 5 individual-document framework-alignment-table headers**: the 2 single `ISO 27001:2022` headers (`dev-security/guideline-ai-coding-assistant-security.md`, `operations/standard-production-security-requirements.md`) and the 3 combined-form `ISO 27001/27002` headers (`dev-security/standard-developer-security-requirements.md`, `dev-security/standard-devops-security-requirements.md`, `dev-security/standard-security-baseline-and-standards-reference.md`) were harmonized to the full `ISO/IEC` form (form only; values were already correct). **Superseded by the maintainer's accuracy-principle redirect (2026-07-05, #663).** The maintainer reframed the `/IEC`-form question from form-consistency into an accuracy requirement: a joint ISO/IEC standard must be listed as joint, a single-body standard must list only the one body, and accuracy is critical to the project's integrity. Under that principle the maintainer directed option A, a corpus-wide sweep of the base-confirmed joint standards. #663 executed it (232 `ISO NNNNN` to `ISO/IEC NNNNN` conversions across 31 files, the 13 base-verified joint standards 27001, 27002, 27004, 27005, 27017, 27018, 27019, 27035, 27036, 27701, 42001, 23894, 20000; the compliance matrix and the gate-49-keyed / counter-example surfaces were excluded and handled elsewhere). This **subsumes the FIT-8 register-cell variant (former part b)**: the register's bare joint-ISO cells are now uniformly `/IEC`, row 274 is internally consistent (`ISO/IEC 42001 §8, ISO/IEC 42005:2025`, both genuinely joint), and the corpus-wide conversion dissolves option B's objection (that harmonizing 2 cells would clash with bare siblings, since the siblings are no longer bare). One thing remains OPEN: **(b)** the base-unverified joint-standard gaps the #663 sweep left unreconciled in their existing (mostly mixed bare-and-joint) state (next bullet). Former part **(a)** the FR-167 compliance matrix's 11 per-domain `ISO 27001:2022` headers plus its line-29 legend row was **EXECUTED in #664** as the gate-49-coupled lockstep PR (option A): the 11 headers + legend cell moved to `ISO/IEC 27001:2022` in lockstep with gate 49's `ISO_HEADER` constant, the gate-49 regression fixtures, the gate-49 docstring / OK message, the shared `tools/iso_27001_reference.py` finding messages, gate 58's own diagnostic messages (the ISO-gate pair fully harmonized, with only gate 58's deliberate both-form-INPUT documentation retaining the bare form), the `/matrix-fit` worklist tool docstring, and the gate-49 references in the audit-programme spec. The change was gated on proving gate 49 still fires post-rename (an injected `A.5.99` under the new header is caught; the old bare header is no longer recognized), verified by two independent adversarial verifiers. The gate-exempt `dev-security/claude-rules/` pack tree carries its own bare `ISO 27001` headers and is deliberately outside this census frame.

- Base-unverified ISO/IEC designation gaps (the #663 accuracy-sweep tail). The #663 corpus-wide sweep converted only the 13 joint standards **confirmed against the held `grc_library_ref` base**; a further set of standards were NOT base-confirmed this pass, so the sweep did not migrate their remaining bare occurrences on inference (accuracy over form). Most of these already appear predominantly as `ISO/IEC` in the corpus from prior authoring, so they sit in a MIXED bare / `ISO/IEC` state rather than uniformly bare; the sweep left that existing state untouched. Candidates observed, with their corpus bare / `/IEC` counts as of 2026-07-05: `ISO 27033` (network security; 3 / 15), `ISO 27034` (application security; 3 / 2), `ISO 27559` (privacy de-identification; 1 / 2), `ISO 29184` (online privacy notices; 1 / 4), `ISO 20889` (de-identification techniques; 1 / 2), `ISO 25010` (SQuaRE quality model; 2 / 1), `ISO 30134` (data-centre KPIs; 2 / 2), `ISO 5259` (data quality for analytics and ML; 1 / 9), `ISO 38505` (data governance; 1 / 2), `ISO 23247` (digital-twin manufacturing framework; 3 / 0, the only uniformly-bare one), and `ISO 12207` (software life cycle, which is tripartite ISO/IEC/IEEE, not a simple `/IEC` migration; 2 / 1). For each, verify the correct issuing-body designation against the held `grc_library_ref` base (ingesting the source if absent, per the reference-currency SOP) or a primary source, then in a follow-up PR reconcile every occurrence to the confirmed form (migrating bare occurrences to `ISO/IEC`, or to the correct compound form for tripartite standards) and correct any that are single-body. Until verified, the existing mixed state stands as tracked debt. This is the accepted-unverified tracker for the #663 sweep's deliberate scope boundary.

- **Authoritative standards register + designation-correctness gate (maintainer-directed 2026-07-05; egress-gated, scheduled for when egress is addressed next week).** The durable MECHANICAL solution to the whole ISO/IEC-designation-accuracy class the #663 sweep and the gap bullet above cover: build an authoritative register of every standard the project uses or is aware-of-as-relevant, recording each standard's correct issuing-body designation (`ISO` / `IEC` / `ISO/IEC` / `ISO/IEC/IEEE`) verified against the primary source, plus a project norm for the case where other sources write `ISO NNNNN` but the correct designation is `ISO/IEC NNNNN` (the norm: the corpus uses the correct designation; a direct quote of another source's differing usage is the only sanctioned exemption, marked as a quote). Then add a check (a gate/lint, sibling to the existing GR-GAP-1 require-registration citation-currency gate, and ideally sharing its register) that, wherever a registered standard number appears in any document, verifies the designation prefix matches the register's authoritative designation, failing on a mismatch. This converts designation accuracy from convention-guarded (the #663 sweep + the manual gap tracker + the per-PR skeptical verifier) to gate-guarded corpus-wide. Egress-gated because populating the register's authoritative designations requires per-standard primary-source verification (the same iso.org / IEC-webstore egress the §1.5 needs-reconfirm rows wait on). Assistant assessment (surfaced to the maintainer, agreed): this is the right generalization; design notes: (a) the designation axis is orthogonal to GR-GAP-1's version-year axis, so the two gates can share the register but check different columns; (b) the norm must handle sole-ISO standards (e.g. 27799, 31000, 9001) that must stay bare, so the register's designation column is the single source of truth the gate keys on, not a blanket "add /IEC" rule; (c) tripartite standards (12207, 42010) need the compound `ISO/IEC/IEEE` form, so the register column is free-text designation, not a boolean.
- **Generic-family `ISO 27001` reference carve-out (Sweep-86 F4, 2026-07-06, S).** Sweep 86 surfaced bare generic-family `ISO 27001` references (framework-family names alongside `NIST CSF`, not edition-pinned control citations) at [`compliance/register-compliance-obligations-template.md`](compliance/register-compliance-obligations-template.md) line 117, [`NOTICE.md`](NOTICE.md) line 34, and [`NOTICE.md`](NOTICE.md) line 60. The #663 accuracy sweep deliberately scoped to control CITATIONS, not generic family names; adjudicate whether generic family references belong in the designation-accuracy class (likely an acceptable carve-out) when the authoritative-standards register above is built. Provenance: Sweep-86 B findings F4a/F4b/F4c.

Backlog items now carry `(severity, effort)`; this item formalizes the convention. Proposed scale:

| Label | Per-item effort | Bundleable per PR |
|---|---|---|
| **XS** (single-line / single-cell) | 5-15 min | 5-10 items |
| **S** (single-doc section add) | 30-90 min | 2-4 items |
| **M** (multi-doc, bounded) | 2-4 hrs | 1 item |
| **L** (new artefact, multi-doc propagation) | 4-8 hrs | 1 item |
| **XL** (new domain, library-wide reshape) | 1-3 days | 1 item, may split |

**Surfaces to update when the convention formally lands**: `library-fitness-review/SKILL.md`; `validation-sweep/SKILL.md`; this file (already in use); `.working/DONE.md` heading shape; future fitness-review templates and sweep detail files. Schedule: after the current FR backlog closes.

### 3.12 CLAUDE.md removal-ledger review cadence (standing) — added 2026-06-28, PR #441 (was 4.27)

The PR #441 condense of [`.claude/CLAUDE.md`](.claude/CLAUDE.md) moved the cut rationale, war-stories, and provenance into [`.working/claude-md-considerations.md`](.working/claude-md-considerations.md) (the removal ledger), each entry carrying an "evidence the removal was wrong" signal. Standing activity (not a one-time task): each `/retro` does a quick scan of the ledger's open RM entries and the periodic hallucination-metrics pass does a deeper one; if an entry's signal appears, advise the maintainer to restore the cut text or make a new CLAUDE.md change, and record the disposition in that entry's Status. Wired into [`.working/improvement-log.md`](.working/improvement-log.md)'s Convention section. This item is the durable tracker so the cadence is not lost; it stays open by design.

### 3.13 Audit-surfaced gate / tooling extensions (audit 2026-07-02) (S)

The citation-coverage extension shipped as gate 61 in its own PR; the remaining bullet is the optional positional-token lint below.

- **(from item 20) Positional-backlog-token lint.** Optional: a lint flagging `TODO P?N.M` positional tokens in gate-scanned corpus prose (they are renumber-fragile), nudging references toward stable FR-ids / topic names. Same class as the CLAUDE.md §N-orphan guard.

### 3.15 Guardrail-review machinery extensions (guardrail review 2026-07-02) (M mostly, S-M each)

Machinery findings from the 2026-07-02 guardrail review (gates, rules, skills, and toolchain assessed as a system; the corpus-audit siblings are section 3.13 above, still open on one optional bullet, and the former section 3.14, now closed; nothing below duplicated them). Every claim re-verified at source at intake; one lens-agent claim (a "gates 32 and 33" comment alleged in the date-staleness linter) was REFUTED at intake (zero grep hits) and is not carried. Each gate-shaped item is built the project way (four-surface wiring + regression fixture).

- **(GR-8 follow-on A, DECIDED 2026-07-04: build, L, S) Register-ageing advisory tool.** The GR-8(b) design round's option B, maintainer-chosen at the 2026-07-04 morning round: a small advisory `tools/audit-*.py` (not a gate) reporting improvement-log Proposed-improvement cells still pending after N PRs, the register-side analogue of the brief-freshness tool. Queue behind the P2 applies. **Attempted + deferred 2026-07-08 (#698) with a design finding:** a prototype was built and tested, but the core heuristic (a non-empty Proposed-improvement cell with no disposition token equals pending) OVER-FLAGS: it cannot distinguish a genuine pending FORMAL candidate (a proposed gate / rule / template awaiting codification) from an adopted-in-place habit / convention / observation note (which needs no token by design), so it reported roughly 78 July-onward cells (mostly false-positive habit-notes) even with a 2026-07-01 cutoff. To be useful it needs either a formal-candidate classifier (a reliable phrase signal for actual proposals) OR a register-format change (a structured candidate marker the `/retro` skill appends when it records a formal candidate); both are design refinements. The prototype was NOT shipped (an over-flagging advisory erodes trust in its output). Revisit once the classifier / register-format decision is made.
- **(GR-8 follow-on B, IN PROGRESS 2026-07-04, M) Expiry-tail one-pass batch review.** Maintainer-chosen mode for the aged pending candidates (236 pre-July rows): a drafted triage proposal (research subagent, orchestrator-verified) listing each row with a proposed CODIFIED/EXPIRED/ROUTED/keep verdict, reviewed by the maintainer in one round; tokens land only on the maintainer's dispositions (rejection and expiry are maintainer calls per the register convention).
- **(GR-10, L, S) History-aware gate subprocess batching.** Gate 31 spawns a git subprocess per document (200-plus spawns, run twice per pre-push via the guard) and gate 40 is heavier; a batched `git log --name-only` pass would trade `--follow` fidelity for one subprocess. Optimization only; correctness unaffected; queue behind the functional items.
- **(QA-2026-07-04 GR-GAP-1, M-H, M) Require-registration citation-currency gate.** Both currency gates are enumeration-scoped: `lint-standards-currency.py` flags only register-recorded patterns and `lint-citations.py` is a hand-curated denylist, so a standard absent from both is structurally ungated; the corpus's own metrics record the ISO/IEC 29134 wrong-year escape (#162) and recurrence (#482) with exactly this diagnosis. Proposed: a gate extracting every `ISO/IEC NNNNN:YYYY`-shaped citation and failing on any standard-year pair not present in the canonical register (require-registration, not deny-known-bad). Validated from the worker QA run; DECIDED 2026-07-04 morning round: BUILD, queued after the P2 applies (the maintainer accepted the register-population and gate-27 re-verification costs). Boundary clause (r4 O-3): "present in the canonical register" is fixed as current-OR-superseded, so a superseded-but-registered standard-year pair stays gate 6's finding (the standards-currency audit already flags it) and this gate never double-fires on it; and every pair the register gains carries gate-27 citation-verification-freshness re-verification cost, named here so the scoping weighs it alongside population cost. **Register-gap VALIDATED (2026-07-06, before building):** a require-registration check over the corpus's 49 distinct cited `ISO(/IEC) NNNNN:YYYY` pairs against the canonical register found 17 pairs with NO register row (e.g. `ISO 19011:2018`, `ISO/IEC 20000-1:2018`, `ISO/IEC 27004:2022`, `ISO/IEC 29184:2020`), plus a live `ISO/IEC 29134:2017`-vs-`:2023` corpus inconsistency (exactly the wrong-year class this gate targets). So the gate cannot ship clean until those 17 rows are populated, and populating them accurately requires confirming each standard's current version UPSTREAM (egress-gated, the same egress the 51 needs-reconfirm rows and TODO 1.5 wait on); guessing versions would violate the no-fabrication rule. NOT built this session for that reason. A future session with egress: populate the 17 rows (needs-reconfirm acceptable for the verification state, but the version needs upstream confirmation), resolve the 29134 conflict, then build the gate. Gap detail in [`design-decisions.md`](.working/design-decisions.md).
- **(r5, [guardrails], M, S) Fence-predicate consolidation.** The fence-toggle predicate exists in at least ten carriers in three variants, and six private copies are TILDE-BLIND (pre-GR-4 semantics: gates 54 and 58's linters, `lint-links.py`, `lint-changelog-link-coverage.py`, `lint-shall-near-uncertainty.py`, `lint-directional-dependency.py`), so a file pairing one backtick fence with one tilde fence passes gate 66 (even shared-toggle count) while a tilde-blind linter sticks in code mode to EOF, the GR-4 silent-suppression class surviving in the copies. Consolidate on a shared `is_fence_line()` predicate (or the iterator itself) in `lint_common.py`, making gate 66's same-statement equivalence structural; latent today (zero tilde fences in the corpus census).
- **(r5, [guardrails], L-M, S) Count-vs-enumeration advisory candidate.** The figure-drift family's mechanizable subset: a delta-scoped advisory (not a gate) over added CHANGELOG/TODO lines flagging a spelled-or-numeral count that directly introduces a parenthesized enumeration whose marker count disagrees (the #636 five-vs-four shape). Honest limits: adjacent-enumeration subset only; dependent-clause and fold-omission drift stay with the staged recount clause. Full-history FP census before adoption; maintainer scopes (or adjudicates the subset too thin and rests on the EXPEDITE). **ATTEMPTED and census-VETOED (2026-07-06):** the advisory was built and the required full-history FP census run; it flagged 63 candidates with ZERO true positives (worst class: a parenthetical that is a breakdown summing to the count, e.g. `5 findings (1 Critical, 2 Medium, 2 Low)`), because the count-vs-adjacent-enumeration match is not mechanically separable from correct prose. Per this bullet's reserved outcome the tool was discarded and we REST on the EXPEDITE (the #676 `Meta-prose state-claim measurement` recount clause); close candidate on the maintainer's confirm. Census evidence in [`design-decisions.md`](.working/design-decisions.md).
- **(r5, [guardrails], L, XS) Gate-66 default-population shape call.** The gate's default walk skips `.claude/`, but the mandated new-pack-prose `lint-language.py` run scans `.claude/` files via explicit paths with no exempt filter, so an unbalanced fence there suppresses that invocation's tail unseen (zero current findings; the docstring now records the residual honestly). Options: widen the default population to include `.claude/` (keep `.working/` exempt), or add the checklist half-line to run gate 66 on the same explicit paths.
- **(r6, [guardrails], M, M) `/reference-audit` per-touch staleness backstop.** The gate-50-analogue for the per-touch reference-breadth obligation the [`.claude/CLAUDE.md`](.claude/CLAUDE.md) close-out checklist added (a corpus-body-touching PR runs the per-touch tool and refreshes [`.working/reference-audit/doc-state.md`](.working/reference-audit/doc-state.md)). Nothing detects a body-touching PR that omits the per-touch run or the state refresh, exactly the class gate 50 backstops for `/validate-pr` rows. Proposed: a PR-time delta check (a Dn) failing when a PR's diff touches a corpus-domain `.md` body without a matching `doc-state.md` row update or a recorded empty-set note. **This is the item the CLAUDE.md per-touch bullet refers to as "a queued TODO item"** (the r6 gap lens caught that the claim was unbacked when written; this item backs it). Convention-guarded meanwhile. **DEFERRED (maintainer 2026-07-08) until AFTER the first FULL `/reference-audit` run** establishes the `doc-state.md` delta-anchor baseline: building the hard D8 gate before the baseline exists would fire on the next corpus-body PR and force a full per-touch judge run (everything reads as "new" against an empty state) instead of the intended near-zero steady-state. Sequencing: first FULL `/reference-audit` run (populates `doc-state.md`), THEN build this D8. The sibling r6 finding G2 (the worker-brief gate-number rail) was codified in #710.
- **(r7, [guardrails], M, M) Bidirectional See-Also parity gate.** [`skill-authoring-discipline`](dev-security/claude-rules/skills/skill-authoring-discipline/SKILL.md) step 6 requires a new skill's `## See Also` "Related skill" links to be reciprocated by the named siblings (uni-directional cross-references rot; bidirectional ones survive maintenance), but no gate enforces it: gate 32 checks only `derives_from` resolution, gate 41 the README tree, gate 3 link depth, gate 44 PAIRS step-parity. The r7 gap lens caught the `publication-screening` delta reproduce the exact miss (its forward links to `reference-audit` and `claim-fit` were un-reciprocated; fixed in-window in the 2.11 build PR), the same recurring class as improvement-log #213/#257. Proposed: a parity gate that, for each skill A whose See Also links a sibling skill B as a "Related skill", asserts B's See Also links back to A. Directly mechanizable (parse the `## See Also` "Related skill" links, check reciprocity); it closes the one required new-skill surface still riding on authorial discipline alone. Maintainer-decision.

---

### 3.16 ETSI Securing-AI alignment map (L, M) — added 2026-07-04 (ad-hoc brief staged first per the convention)

Map the held ETSI SAI family (EN 304 223 plus the GR/TR set) against the corpus `ai/` security content: which requirements have no corpus carrier, which corpus claims an ETSI citation would strengthen, and a proposed alignment shape (options surfaced for maintainer scoping). Research DELIVERED (scratch inbox `inbox/worker-20260703-a/etsi-sai-alignment-research/`, 2026-07-04); research-only, the apply is a later decision. Citation form UNBLOCKED 2026-07-04: the maintainer supplied a fresh EN 304 223 V2.1.1 copy (identical page text to the held original), refuting the 2026-07-02 scratch-audit mis-designation finding (its TODO item is closed), so the map may cite the adopted EN as authoritative (scratch PR #100 records the confirmation).

### 3.17 MITRE ATLAS 2026.06 alignment and currency map (L, M) — added 2026-07-04 (ad-hoc brief staged first per the convention)

Coverage and identifier-currency map of the held ATLAS 2026.06 refresh against the corpus adversarial-AI content (which techniques lack carriers; which cited identifiers changed across the v5.6.0-to-2026.06 refresh), with a proposed update shape surfaced. Research DELIVERED (scratch inbox `inbox/worker-20260703-a/atlas-adversarial-alignment-research/`, 2026-07-04); research-only. **Pack-tree semantic-fit note (#654 verifier, 2026-07-05):** a future `/matrix-fit`-style pass over the pack `dev-security/claude-rules/ai/` LLM02 rows should weigh the kept `AML.T0024` (Exfiltration via AI Inference API) against `AML.T0057` (LLM Data Leakage), which may fit output-disclosure rows better; `AML.T0024` is current and correctly glossed, so #654 kept it within its currency-refresh scope, and this is a fit judgment rather than a currency defect.

### 3.19 CHANGELOG restructure: current-week model + compact root format (maintainer-directed 2026-07-08) (M, M)

Maintainer-directed restructure to make the changelog readable and the working tree tidy. Design decided 2026-07-08: the detailed-CHANGELOG mirror and the per-run records keep only the CURRENT WEEK in-repo; completed weeks sweep to `grc_library_scratch` weekly Monday-dated archives; the root `CHANGELOG.md` keeps its full history but each entry is compressed to 1-2 sentences in the compact `**date | version | PR** - summary` format; `.gitattributes export-ignore` on `.working/` for fork-clean exports (the 3.19-supersedes note below).

- **PR 1 (machinery, done in the same PR as this item):** gate-59 dynamic cutoff, `tools/sweep-working-records-to-scratch.py` (data-safe emit / verify / prune), `.gitattributes export-ignore`, the `.working/changelog-details/README.md` model write-up, and regression fixtures. No data moved.
- **PR 2 (DONE, grc_library #708 + scratch #113):** the initial sweep ran, the scratch `archive/` weekly Monday-dated files are populated and verified (647 detailed entries across 5 weeks + 204 dated records, lossless 647 == 647), and the in-repo detailed mirror is pruned to the current week (the prune kept 41 entries; with this PR's #708 the mirror now holds 42, week of 2026-07-06, #667 through #708). The scratch `archive/` was exempted from scratch's Check 5 (watermark PII) + Check 6 (house style) per the maintainer's Option A. REMAINING from this bullet: wire the per-PR sweep follow-up step into the PR close-out checklist (a small CLAUDE.md convention edit; folds into the protected-file batch).
- **PR 3+ (remaining):** mechanical reformat of the roughly 678 root entries to the compact format, then compress each to 1-2 sentences.
- **Deferred protected apply:** the change-tracking rule/skill and CLAUDE.md descriptive edits, plus the per-PR sweep-step wiring, are staged in [`.working/deferred-protected-changes.md`](.working/deferred-protected-changes.md) item 7 (daytime, maintainer-authorized).

Note: the sweep tidies the working tree and shrinks the live files; removed content stays in git history, so true clone / fork cleanliness still needs the deferred history-collapse. Supersedes the earlier full-`.working`-migration idea (assessed 2026-07-08 as too costly and gate-breaking; export-ignore plus the current-week model chosen instead).

### 3.20 `/reference-audit` build residuals (post-build, 2026-07-08) (S each)

Follow-ups the `/reference-audit` skill build (TODO 2.14, closed by PR B) left as scope boundaries. (The per-touch mechanical staleness backstop, the third residual, is tracked as a `[guardrails]` item in section 3.15 because the r6 guardrail gap lens surfaced it.)

- **Publications-bucket inclusion after the screening process (screening-process dependency).** The skill excludes the `grc_library_ref/publications/` bucket by default (screen-first tier, `--include-publications` only under an explicit screening decision). The publications-screening process now EXISTS (the `publication-screening` skill / `/screen-publications`, shipped with the reference base's `publications/SCREENING.md` register, closing the former TODO 2.11), so the remaining triggers are the screening wave (§3.24, working the 13 `pending` register rows to `screened`) plus the maintainer's inclusion decision: revisit whether `/reference-audit` should include screened publications in its default candidate set (and how the tier ceiling treats them, likely recommendation-tier like books until a per-source trust decision).

### 3.22 Cross-repo worker-provenance link convention review (post-#715 `/validate-pr`, 2026-07-08) (S)

The detailed-CHANGELOG `**Worker provenance:**` lines link the scratch-side delivery manifest via a cross-repo relative target (`../../../grc_library_scratch/inbox/<worker-id>/MANIFEST.md`), used by #701/#702/#706/#707/#715. That target resolves against a fresh scratch checkout at `main` but not against a stale local scratch working tree, and cross-repo links are inherently un-gate-checkable (gate 50 validates the marker SHAPE, not link resolution; the broken-link gate does not scan `.working/`). Surfaced by the #715 `/validate-pr` as an out-of-window note; the maintainer directed tracking it as a convention-review item (2026-07-08). Decide the durable convention: keep the cross-repo link as-is (accept expected-fragile), convert future worker-provenance manifest references to plain backticked text (no link, so nothing can dangle), or another form; the decision affects all prior entries and the gate-50 interaction. Not urgent (the provenance pointer is a convenience, not load-bearing).

### 3.24 Publications screening wave (post-2.11-build, 2026-07-09) (S each, M total)

Work the 13 `pending` EDPB / WP29 rows in the reference base's `publications/SCREENING.md` register through `/screen-publications` (the process shipped closing the former TODO 2.11): the structured per-item screen (provenance and integrity, the instruction-content scan, corroboration of load-bearing claims against the held GDPR / trusted texts) flips each `pending` row to `screened` (or `quarantined` / `discard-candidate` on a finding), recorded as reference-base PRs through the ref validation gate. Partitionable worker research per the coverage index's promised `publications-screening-wave` (apply through validate-then-apply as `grc_library_ref` PRs). Effort S per item, M total. Cross-references §3.20 bullet 2 (the `/reference-audit` publications-inclusion decision follows the wave plus the maintainer's inclusion call).

## Priority 4 — Adopter experience

Capability and guidance for organizations adopting the library, and the operator-experience tooling for running the project. Scheduled deliberately, after the fix/gap/cleanup tiers.

### 4.1 Corpus-management discipline as a shareable skill (M, XL)

Package the cumulative documentation-and-corpus discipline this project has accumulated as a standalone Claude Code skill that anyone managing a documentation corpus with an AI coding assistant could install.

- **Distillation source**: the thirteen `governance/` pack rules form the discipline core; `validation-sweep` and `library-fitness-review` form the periodic-review surface; the audit-programme architecture forms the mechanical-enforcement surface.
- **Generalization**: carry the patterns and discipline without the GRC-specific control-set citations; adopters supply their own document-type model and metadata-field set.
- **Open questions resolved 2026-06-22**: skill **family** (not omnibus); **prescriptive-only** (no linter scaffolds); **existing pack 1.x bump** (not a new pack). Sequencing: after the FR backlog closes.

### 4.2 Pack: dev-security/claude-rules language coverage review (M, M) (was 4.4)

Review the language-specific security rules in [`dev-security/claude-rules/`](dev-security/claude-rules/). The pack's `languages/` tree already ships 11 language baseline files (including the decided subset; the earlier "Python-focused guidance only" premise was stale, corrected 2026-07-03), so the live work is the REVIEW of the existing subset files against `python.md`'s shape plus the positioning stance, not greenfield authoring. Decided 2026-06-22: baseline subset = **JS/TS + Go + Java** (others deferred); **explicitly position the pack as pointing to OWASP cheat sheets / dedicated technical-security sources rather than duplicating them**. Scope: one or more small PRs each bringing a subset baseline file up to `python.md`'s shape (adding a file only where the review finds a genuine gap) OR updating the pack README's positioning.

### 4.3 Overnight unattended-run driver (M, L) (was 4.7)

Deferred to a future session (maintainer-directed 2026-06-22). For longer unattended runs, a single overnight session is the wrong shape (it degrades like any long session). The sound architecture is an **external driver loop** (cron / CI / Agent SDK script, outside the corpus) that launches a **fresh `claude -p` or SDK session per task-unit**, each reading `.working/session-handoff.md` + the TODO/DONE queue, doing one unit, committing, advancing the queue, and exiting. The durable-state layer already exists (`session-handoff.md`, `/resume`, the green-merge-as-last-act + loop-break disciplines); the missing piece is the driver plus an overnight runbook.

- **Design questions**: where the driver runs; merge authority for unattended worker sessions; the stop condition; how a worker signals "needs maintainer" vs "safe to continue"; interaction with the `## overnight-work protocol` in the change-tracking rule.
- **Building blocks confirmed** (2026-06-22): `claude -p` fresh-by-default; Agent SDK fresh-session-per-call; no built-in overnight/auto-reset scheduler in CLI or web.

### 4.4 Worker-ready brief staging (whole eligible backlog) + `/subagent` external-worker entry (M, L) — maintainer-requested 2026-06-26; expanded per maintainer direction 2026-07-03 (was 4.16)

Stand up the INPUT half of the multi-session capability (its codification closed as section 3.6 in the DONE ledger) so separate-session / separate-account workers can pick up research tasks without waiting on the orchestrator. The 2026-07-03 maintainer direction expanded the original stage-on-dispatch design to STANDING WHOLE-BACKLOG COVERAGE: every TODO item has, in scratch, either a worker-ready brief or a recorded not-eligible verdict, kept current through the normal PR close-out. The settled design (the maintainer's three answers plus the accepted adjustments: eligible-only briefs with verdicts for the rest, the pointer-heavy content-light brief shape, a `Verified-against:` freshness stamp keyed on the main-repo PR number, artefact-derived state with no status fields, TODO-authoritative with briefs as a wipeable derived projection, TODO-delta-batched sync, targeted-first refresh with an advisory cross-repo freshness check) is recorded in [`.working/design-decisions.md`](.working/design-decisions.md) under "Worker-ready brief staging" and operationally in the runbook [`.working/multi-session-orchestration.md`](.working/multi-session-orchestration.md) subsection 5.1 (both shipped with slice 1, 2026-07-03). Build state: slices 1, 2, 3, and 5 SHIPPED (slice 2 as scratch PR #50 plus scratch `validate.py` checks; slice 5 as the wave-1/2/3 seeding scratch PRs #51/#52/#53, whole backlog covered, 13 staged briefs and a verdict for every other item; slice 3 as the close-out pairing line, the `/resume` step-3 freshness check, and [`tools/audit-brief-freshness.py`](tools/audit-brief-freshness.py)). Remaining build slice:

1. **Slice 4, the `/subagent` slash command** as the external-worker entry point (read the assigned brief, claim it in `claims-ledger.md`, read the named main-repo files read-only, produce findings, deliver to `inbox/`, stop; **read-only-on-main MUST be enforced by the worker GitHub account's permissions, not by the prompt**, a prompt is not a security boundary per [`.claude/rules/secrets.md`](.claude/rules/secrets.md)) plus the maintainer-facing quick start in the runbook (provision the account, point the worker session at scratch, assign a brief, collect from `inbox/<worker-id>/`; maintainer-suggested 2026-07-02).

**Gating maintainer action**: provision the least-privilege worker account (read `grc_library` / write `grc_library_scratch` only). In-session `Agent` fan-out works today (shares the orchestrator's credentials, spends its budget); the external-worker path needs the account; slices 2 and 5 are useful before the account exists (maintainer-launched second-account sessions per the scratch `WORKER-ONBOARDING.md` can consume briefs today). **Partitionability**: unchanged; a corpus-wide sweep, rename, or single matrix stays single-session and gets a verdict, never a brief. **The apply stage stays single-session with full QA regardless** (the validate-then-apply no-bypass invariant: worker provenance never reduces the QA a change receives). **Seed-gap re-assessment (standing, maintainer-adopted 2026-07-04)**: at every close-out coverage sync, any not-eligible coverage row whose recorded reason names a decision or source that has since resolved gets a fresh verdict or a staged brief in the same sync; the operative convention text lives in the scratch coverage index header (first applied in the wave-4 seeding, scratch PR #70, which staged seven decision-unlocked briefs from the maintainer's 2026-07-04 overnight-prep round). Pairs with the closed section-3.6 codification (see the DONE ledger) and the publications-screening process (the `publication-screening` skill, closing the former TODO 2.11 item).

### 4.5 Fork-facing guidance + scripts for building an own reference base (L, L) — maintainer-directed 2026-06-27 (was 4.21)

Enable people who fork this library to assemble their **own** reference knowledge base (the authoritative source texts that authoring and citation work draws on) instead of depending on the maintainer's private one. The library is CC BY-SA 4.0 prose, but the source standards it cites (ISO/IEC 27001:2022 and the wider ISO catalogue, CSA CCM v4.1 / AICM v1.1 / CAIQ, NIST CSF 2.0, MITRE, OWASP, jurisdiction legislation) are proprietary or licence-restricted and are **not** redistributed in the corpus. The maintainer keeps those source texts in the separate `grc_library_ref` repo (buckets at its root; trust-bucketed `standards/` trusted, `legislation/` trusted-but-version-sensitive, `publications/` assess-first; plus an `ingest/` staging area, a `catalogue.yml`, and PDF-to-text / XLSX-to-CSV extracts for AI-readability). **That reference repo is the maintainer's own private usage-and-reference and is NOT to be shared / not redistributed** (it holds third-party reference works); this item is explicitly NOT "publish our reference repo", it is "give a forker the method and tooling to build their own equivalent from sources they are entitled to hold".

Maintainer note (2026-06-27): `grc_library_ref` now holds the ISO references and is being expanded across the other source families; it stands as the maintainer's reference for content. The fork-facing capability mirrors its proven structure without exposing its contents.

Deliverables to scope at build (none built yet, this item only records the work):

1. **A fork-facing guidance doc** (candidate home: a `docs/` adopter guide, scope-and-confirm at build): where to obtain each cited standard from a known reputable / authoritative source (the issuing body's own store or an authorized distributor), the licensing posture (what a forker may hold privately vs redistribute), and how to upload their own copies (e.g. purchased ISO PDFs) into a local reference tree.
2. **The trust-model and tree convention** a forker should adopt, mirroring the `grc_library_ref` model: `standards/` (trusted ground truth, one dir per issuing body) vs `publications/` (assess-first, screen for bias / poisoning before use, per the `publication-screening` process) vs `legislation/` (authoritative but version-sensitive), with an `ingest/` staging area and a machine-readable catalogue.
3. **Helper scripts** (`tools/`-style, scope precision at build): scaffold the `ref/` tree; fetch-from-reputable-source where a licence and a stable URL permit (never bypass paywalls / licensing); extract text for AI-readability (PDF to `--full-text.md`, spreadsheet to per-sheet CSV) mirroring the `grc_library_ref` extraction form; build / refresh the catalogue. Honour the security rules: no credential-bearing fetches in tracked config; treat downloaded `publications/` content as untrusted until screened.
4. **Wiring notes**: how a forker's reference base feeds the in-repo validator modules ([`tools/ccm_aicm_reference.py`](tools/ccm_aicm_reference.py), [`tools/nist_csf_reference.py`](tools/nist_csf_reference.py), [`tools/cobit_iso31000_reference.py`](tools/cobit_iso31000_reference.py)) that gates 48/49/54/58/61 build on, and the `/matrix-fit` semantic-fit audit (the [`matrix-fit`](dev-security/claude-rules/skills/matrix-fit/SKILL.md) skill, shipped #399), so a fork can validate control-code citations against its own ground truth.

Low urgency (maintainer flagged it as deferred, "a priority 5 item"); it is filed here in Priority 4 (adopter experience / tooling) because it is fork-facing tooling + guidance, the same class as the other Priority 4 tooling items, rather than the country / regulator content overlays Priority 5 enumerates. Pairs with the publications-screening process (the `publication-screening` skill / `/screen-publications`, closing the former TODO 2.11 item) and the standing reference-base discipline the `/resume` Reference-knowledge-base step and the [`multi-session-orchestration`](.working/multi-session-orchestration.md) runbook §6 describe.

### 4.6 Adopter-experience enhancements (S-a..S-f; FYI/L, XS-L) — suggestions and maintainer-requested items

S-a through S-e are fresh-reader suggestions from the 2026-07-02 audit's persona pass (factual basis re-validated); S-f is a maintainer-requested item added 2026-07-04. The suggestions are enhancements (not defects) the maintainer triages; the maintainer-requested item is already accepted by construction. **Triage state 2026-07-04: S-c and S-d ACCEPTED by the maintainer** (research briefs staged in scratch, `research/s-c-adopter-direction-demo/` and `research/s-d-multi-entity-adoption/`, each owning one NEW `docs/` document; the builds apply through the normal validate-then-apply pipeline when the deliveries land); S-a, S-b, and S-e applied 2026-07-09 (the adopter-experience-sabe batch: the README decision-tree routing link, the glossary Governance-Library-Maintainer legend, and the portal Board/CEO audience section + inline maturity tags). **S-d (multi-entity / group adoption guidance) applied 2026-07-09 (#742): new [`docs/adopter-guide-multi-entity.md`](docs/adopter-guide-multi-entity.md), three group topologies with per-topology versioning / role / jurisdiction / trade-off guidance; rotated to DONE.** **S-c CLOSED as satisfied 2026-07-09 (#744, maintainer decision): the gap is already filled by [`docs/worked-example-adoption.md`](docs/worked-example-adoption.md) (adopter-direction role-substitution walk); no new doc built; rotated to DONE.**

- **(S-f, maintainer-requested 2026-07-04, M-L) Fork update-assessment tooling (upstream-change applicability report).** When an adopter's customized fork pulls upstream `grc_library` updates, nothing today helps THEIR AI assistant assess each upstream change against the fork's local customizations and present a decision matrix (accept / adapt / skip per change, with rationale), so cleanly understanding what upstream changed and selectively integrating the applicable components is manual. Design a fork-side instrument: likely a shareable skill (a prescriptive workflow: diff upstream release-to-release, classify each change by carrier class, document body, gate or linter, convention or pack rule, map each against the fork's recorded local divergences, and produce the per-change accept/adapt/skip report for the fork owner), plus upstream-side enablers where cheap (the structured CHANGELOG already carries the what and why per PR; consider machine-readable change-class tags or per-entry applicability hints as an upstream convention). Pairs with the delivered fork reference-base guidance (see the DONE ledger) and extends section 4.1's adopter framing; shape and upstream-vs-fork-side split are maintainer-scoped at design time.

### 4.7 Pack design improvements (guardrail review 2026-07-02) (M/L, S-L each)

Pack-architecture findings from the 2026-07-02 guardrail review; design-tier work scheduled last in the overnight sequence per the maintainer's interleave-by-risk direction.

- **(GR-P2, M, L) Rule operative-core condense.** The 13 governance rules total roughly 31,000 words always-on, with fixed per-rule scaffolding (framework tables, why-sections, AI-assistant closers, repeated restatements of the audit-trail axiom) that is rationale, not operative instruction. Two-layer split per the PR #441 CLAUDE.md method: an always-on operative core plus on-demand rationale/provenance, with a removal ledger; per-rule PRs, `lint-language` run on each before first commit. **TRANCHE 1 SHIPPED 2026-07-09** (the condense method + the removal ledger [`.working/claude-rules-considerations.md`](.working/claude-rules-considerations.md) + the `gate-discipline.md` exemplar condensed, 1559->1173 words, why-section moved / operative core + framework table kept). **Tranches 2-12 are HELD pending the maintainer's framework-table direction** (recorded in [`.working/pending-decisions.md`](.working/pending-decisions.md)): tranche 1 sets the pattern the other 12 replicate, and whether framework tables are kept-in-rule (the applied stricter-safe default) or cut-to-a-distributed-location reshapes every remaining tranche, so they are not mass-applied unattended on an unconfirmed default. The tranche-2/3/4 worker deliveries exist in `grc_library_scratch`. Routed note (#560 `/validate-pr`, out-of-window): the change-tracking rule's two-file-split text says the root CHANGELOG entry carries "the same lead-paragraph wording as the detailed entry", but every entry in practice pairs matching headers with divergent lead wording (root long-form, mirror short-form); reconcile the rule text to the practice (or vice versa) when this rule is condensed. **Folded-in class-scope fix (from TODO 3.21, maintainer overnight authorization #3):** migrate the pack-wide `LOG-02, LOG-08` audit-trail-row convention to `LOG-02, LOG-04, LOG-10` (LOG-08 is "Audit Logs Sanitization", a mis-fit for audit-trail trust; LOG-04 "Audit Logs Access and Accountability" + LOG-10 "Audit Records Protection" fit) across the pack rules' framework-alignment tables (change-tracking, evidence-grounded-completion, and sibling rows; bare-token census before the sweep), and swap the apex rule's Integrity-row `A.8.34` to `A.5.33` "Protection of records". The AIQT principle document's own cells were fixed in the 3.21 PR; this is the pack-rule half deferred to the condense so the tables are touched once.
- **(GR-P5 residual, L, S) Derived-skill coverage gap.** `validate-inference-before-action` and `surface-counterproductive-instructions` have no derived skill (the strongest candidates for one, if the pack wants a workflow wrapper for each). This is the un-actioned remainder of the GR-P5 batch: GR-P5a (the `deep-qa-review` / `library-fitness-review` `derives_from` re-point to `trust-recovery-escalation`) shipped in the GR-P3/4/5 PR; GR-P5b (the exception-register strict-mode hoist) is folded into the GR-P2 condense (it collides with the gate-discipline and change-tracking condenses, so it is applied when those rules are condensed, not standalone); GR-P5c (the CLAUDE.md Boundaries-trio premise) was validated already-correct at #713 and is a confirmed no-op.

---

## Priority 5 — Expand: country / regulator / programme overlays

Adding new coverage to existing domains. Each subitem is a separate small or medium PR; the maintainer schedules deliberately.

### 5.2 Logistics country / programme expansion (was 5.1)

The WCO AEO Compendium identifies ~94 trusted-trader programmes globally; the library covers 4 (BASC, CTPAT-US, AEO-UK, PIP-Canada). Highest-priority additions: EU AEO (covers 27 member states under EU UCC Art 38), Mexico NEEC / OEA, Australia Trusted Trader (ATT), Singapore STP / STP-Plus, Japan AEO, Korea AEO, New Zealand Secure Exports Scheme (SES), Brazil OEA, China AEO.

### 5.3 Financial-services country regulator overlays (was 5.2)

Within `compliance/financial-services/`: UK PRA / FCA (`annex-uk-pra-fca.md`); US OCC / FRB / FDIC / SEC / FINRA; Canada OSFI; Australia APRA; Singapore MAS; Japan FSA.

### 5.4 Healthcare country regulator overlays (was 5.3)

Within `compliance/healthcare/`: UK NHS DSPT; EU MDR / IVDR; Canada PHIPA and provincial frameworks; Australia My Health Records Act. **Closed the US HIPAA bullet of section 5.4 in #733** (the new US annex `compliance/healthcare/annex-healthcare-united-states.md`, applying the `worker-20260703-a/us-hipaa-healthcare-deepening` delivery: role determination, the Security Rule safeguard families, the Privacy Rule use-and-disclosure core, the Breach Notification mechanics and timelines, the four-tier enforcement structure, and the NIST SP 800-66r2 crosswalk; HITECH covered in Framework-alignment). The remaining bullets stay source-gated (EU MDR/IVDR full text is now held in the ref base; the rest pending).

### 5.5 Energy and utilities country regulator overlays (was 5.4)

Within `compliance/energy-and-utilities/`: US NERC CIP standards; US TSA pipeline cybersecurity directives; UK Ofgem cyber requirements; EU ENISA sectoral guidance.

### 5.6 Telecommunications country regulator overlays (was 5.5)

Within `compliance/telecommunications/`: EU EECC; UK Ofcom telecom security framework; US FCC regulations; Australia ACMA requirements.

### 5.7 Public-sector country / regulator overlays (was 5.6)

Within `compliance/public-sector/`: UK Government Cyber Security Strategy and GovAssure; Australia ISM and PSPF; Canada IT Standards for federal departments. **Closed the EU eIDAS bullet of section 5.7 in #739** (the new `compliance/public-sector/annex-eidas-requirements.md`, applying the `worker-20260703-a/eidas2-public-sector-annex` delivery: role determination and by-role obligations for the wallet-relying party, trust-service provider, public-sector body, and wallet provider, grounded in the held eIDAS2 Regulation 2024/1183 with the wallet/relying-party phase-in clocks framed as relative to the implementing acts). The other bullets stay source-gated.

### 5.8 Privacy jurisdiction gaps (was 5.7)

Existing privacy domain covers 25 country annexes. Known gaps or stale entries: Argentina (PDPA 2025 update pending); Saudi Arabia PDPL (recent updates pending); re-review of EU member-state derogations where applicable. (Mexico's standalone annex shipped in #750 against the held 2025 LFPDPPP, so Mexico is no longer a gap here; see the privacy jurisdiction index.) The Argentina and Saudi bullets stay source-gated.

### 5.9 AI jurisdiction overlays (was 5.8)

The library cites EU AI Act extensively but lacked a dedicated `ai/jurisdictions/` subdirectory parallel to `privacy/jurisdictions/`. **The subdirectory shape and its first two annexes (EU AI Act, applied #743; Colorado AI Act, applied #749) were built under the former FR-62 (now closed, see the DONE ledger); this section is the ongoing home for the remaining source-gated AI jurisdictions.** Remaining candidates, all source-gated pending maintainer drops: Canada AIDA; UK AI policy framework; NYC bias audit law; China generative AI rules; Korea AI framework.

---

## Priority 6 — Expand: new domains

Entirely new domains, multi-week scope each. The maintainer schedules deliberately. Ordered lowest-effort-first.

### 6.1 Identity-specific content depth (L) (was 6.2)

Customer Identity (CIAM) governance, workforce identity governance, identity federation patterns, passwordless adoption playbooks. The library has IAM policy and procedure but no dedicated content for these patterns. **Research state (2026-07-04): the p6-1 scoping research is DELIVERED (scratch inbox, pending apply), and its three proposed documents are each briefed as follow-on work-units in scratch** (`research/ciam-governance-research/`, `research/identity-federation-patterns-research/`, `research/passwordless-adoption-research/`, grounded in the held NIST SP 800-63-4); the BUILD stays maintainer-schedule-gated.

### 6.2 Quantum cryptography readiness deepening (L) (was 6.3)

The library has a phase-level PQC roadmap ([`security/roadmap-post-quantum-cryptography.md`](security/roadmap-post-quantum-cryptography.md)) but not detailed implementation content. Pending: a PQC migration playbook (operational steps per system class), crypto-agility patterns (key abstraction, algorithm switching, hybrid schemes), and post-quantum-ready CA / PKI management. **Research state (2026-07-04): the corpus-internal scoping half is DELIVERED (scratch inbox `inbox/worker-20260703-a/quantum-pqc-readiness-scoping/`, pending apply; deepening options and a source-needs list surfaced, no regime claims)**; the regime half stays source-gated (no PQC standard held) and the BUILD schedule-gated.

### 6.3 Cross-framework matrix expansion (L) (was 6.4)

Current matrix ([`governance/matrix-cross-framework-alignment.md`](governance/matrix-cross-framework-alignment.md)) covers major frameworks; expand to additional sectoral and regional frameworks as the P5 country/sector content grows.

### 6.4 CMMI capability levels alongside maturity levels (L) (was 6.5)

Per maintainer direction 2026-06-22 (low priority, after the FR backlog completes). The corpus uses the 5-tier maturity-level scale at both organization-wide and per-domain granularity; CMMI distinguishes the two (maturity levels 1-5 org-wide; capability levels 0-3 per practice area). Introducing capability levels would: (1) add a capability-level scheme to [`governance/framework-governance-performance-and-improvement.md`](governance/framework-governance-performance-and-improvement.md) §2; (2) update [`docs/template-maturity-self-assessment.md`](docs/template-maturity-self-assessment.md) to use capability levels per domain and aggregate to maturity levels at the programme rollup; (3) possibly extend [`governance/register-digital-trust-and-assurance-metrics.md`](governance/register-digital-trust-and-assurance-metrics.md) DTI thresholds with a capability-level surface. **Research state (2026-07-04): the research-only integration mapping is DELIVERED (scratch inbox `inbox/worker-20260703-a/cmmi-sei-maturity-integration/`, pending maintainer decision; four integration-shape options surfaced, grounded in the CMMI V3.0 + SEI AI Adoption Maturity Model ingestions)**; the BUILD stays sequenced after the FR backlog completes.

### 6.5 Multi-cloud governance overlay (XL) (was 6.1)

Per-cloud hardening baselines for AWS, Azure, and GCP exist in `dev-security/`. The remaining gap is multi-cloud governance (cross-cloud risk taxonomy, cross-cloud incident coordination, cloud-portfolio-level controls). Could live in `operations/` or warrant a new `cloud/` domain. **Research state (2026-07-04): the research-only scoping is DELIVERED (scratch inbox `inbox/worker-20260703-a/multi-cloud-governance-scoping/`, pending maintainer decision; proposed document set and placement options surfaced)**; the BUILD stays P6 schedule-gated.

---

## Priority 7 — Awaiting maintainer decision

No open decisions pending. The FR-104 / FR-130 entries are dropped-decision audit-trail records (see [`.working/design-decisions.md`](.working/design-decisions.md)). The former 7.1 (do the audit-trail-only sections belong in TODO?) was decided 2026-07-03: all three sections KEEP, by design.

### 7.2 Per-regulation context (FR-104)

Per-regulation context not pursued.

### 7.3 Portal reorder (FR-130)

Portal reorder not pursued (README stays at decision-tree item 1).

---

## Reference-base work (`grc_library_ref`)

Validated defects in the reference repo from the 2026-07-02 audit. **The reference base was split out of `grc_library_scratch/ref/` into the dedicated `grc_library_ref` repo (buckets at its root) on 2026-07-07**; the CLOSED items below were done on scratch's `ref/` pre-split (their scratch-PR provenance is retained as history) and are migrated into `grc_library_ref`; the one remaining OPEN item (SR-1) now targets `grc_library_ref` and ships as a PR to it via `gh` (not this repo, not scratch). Item 33 (tracked reference files) was decided 2026-07-02: keep tracked under the private-forever promise (see [`.working/design-decisions.md`](.working/design-decisions.md)). Item 32 (the ETSI designation question) closed 2026-07-04: the maintainer-supplied fresh EN 304 223 copy refuted the mis-designation premise (scratch PR #100). SR-4 (catalogue/extract hygiene, items 30-31) closed 2026-07-05: item 31's report-PDF and KEEP-verdict count corrections (scratch PR #104) and item 30's drop of the WP-BCR failed-conversion stub (scratch PR #105); the SR-4 re-scoping surfaced a new finding (`publications/originals/README.md` is a stale 2026-06-27 delivery snapshot making false present-tense claims), scoped as a maintainer decision in [`.working/pending-decisions.md`](.working/pending-decisions.md).

### SR-1 `last_checked` currency mechanism is inert (item 26, P2, S)

As of 2026-07-05, 5 of 240 `grc_library_ref` `catalogue.yml` items carry a `last_checked` field (the 2026-07-04 currency-check batch stamped ATLAS plus four others; done pre-split as scratch #101), and `grc_library_ref`'s `tools/validate.py` checks the field's FORMAT only if present (no presence requirement), so the 7-day-throttle currency discipline has no on-disk footprint for the unstamped items. **Direction DECIDED** (maintainer, 2026-07-02, pending-decisions line 55): presence + backfill (backfill stamps on the currency-sensitive buckets, then add a presence/due-item check). **Execution egress-gated:** an honest backfill needs an upstream currency check per document (the reference-version-currency SOP), the same egress the 51 needs-reconfirm register rows and the ISO-31000 refresh wait on; stamping without checking would defeat the discipline. Held for the maintainer's egress instance.

### Reference-base acquisition and assessment queue (`grc_library_ref`; maintainer-directed 2026-07-07)

Standing queue of sources the maintainer has directed be assessed for reference-base benefit and (where useful) ingested into `grc_library_ref` (Mode A curation: dedupe against holdings, route by trust, extract, catalogue with vocabulary topics, keep extracts organization-neutral, confirm currency upstream). Egress is available on the NUC. Each is its own batch/PR; a zero-value assessment still records a verdict.

- **RB-1 PCI DSS v4.0.1 + supporting docs (IN PROGRESS this session)**: the maintainer-uploaded PCI SSC set (the v4.0.1 standard, QRG, guidance, ROC/AOC/DESV templates, summary-of-changes) ingested to `frameworks/PCI/`; the fillable forms kept as originals-only; the Prioritized-Approach xlsx to per-sheet CSV.
- **RB-2 staging_ref batch (IN PROGRESS this session)**: 5 ISO/IEC PDFs (24760-1/-2/-3 identity management, 29146 access management, 30111 vulnerability handling) to `standards/ISO/`, verified organization-neutral (no identifying detail); CIS Controls v8 (PDF + 2 xlsx) to `frameworks/CIS/`; NIST SP 800-207 (deduped, extract already held).
- **RB-3 NIST CSRC harvest**: the maintainer-pasted planning-session task (all project-relevant FIPS/SP-800/SP-1800/SP-500/IR/CSWP/AI publications), a background, per-series-batched, high-assurance ingest. Assess the prompt, then proceed or advise (task tracked; assess after RB-1/RB-2 land).
- **RB-4 OWASP projects assessment (maintainer-directed 2026-07-07)**: assess ALL OWASP projects for reference-base benefit, notably [OWASP Threat Dragon](https://github.com/OWASP/threat-dragon) (a threat-modelling tool, assess whether it benefits the project) plus the doc/standard projects (Top 10, ASVS, MASVS, SAMM, cheat sheets; the LLM Top 10 v2.0 and the Top 10 for Agentic are already held). Ingest the useful documentation-type projects to `frameworks/OWASP/`, then keep current.
- **RB-5 MITRE projects assessment (maintainer-directed 2026-07-07)**: assess ALL MITRE projects for reference-base benefit, notably [mitre/cti](https://github.com/mitre/cti) (the ATT&CK STIX/CTI data). Maintainer expectation: ingest into `grc_library_ref` (ATLAS 2026.06 is already held under `frameworks/MITRE/`; add ATT&CK and other MITRE catalogues), then update on the upstream release cadence.

**Completion status (2026-07-07 overnight):** RB-1 (PCI) and RB-2 (staging_ref ISO/CIS) landed earlier this session. RB-3 (NIST CSRC harvest) COMPLETE via `grc_library_ref` PRs #5-13 (~65 net-new NIST across Tier A + Tier B). RB-4 (OWASP) COMPLETE via ref PRs #14-19 + #22 (Top 10:2025, API, Proactive Controls, ASVS, SAMM, WSTG, Cheat Sheets, MASVS, MASTG, CycloneDX at 1.7.1). RB-5 (MITRE) COMPLETE via ref PR #20 (CWE/CAPEC/D3FEND distilled). FedRAMP COMPLETE via ref PR #21 (2026 Consolidated Rules + rules JSON + KSI into `programs/FedRAMP/`); the standalone RFC-0006 draft-as-published is in flight (maintainer-directed). Threat Dragon assessed and SKIPPED (tool, not citable text; its methodology is in the ingested Cheat Sheet Series). These rotate to DONE at the overnight morning-processing (cross-repo: keyed by `grc_library_ref` PR, not this repo's PR numbers).

- **RB-6 reference-base currency + draft watch (NIST-harvest-surfaced 2026-07-07)**: standing watch on `grc_library_ref` items whose upstream has a pending revision, so a future currency pass re-checks them. (a) **Planned-but-unpublished NIST revisions**: FIPS 202/203/204/205 (planning notes for future revisions), FIPS 198-1 (proposed conversion to SP 800-224, draft), SP 800-131A (Rev 3 IPD), SP 800-38D (revision announced), SP 800-46 (Rev 3 draft), SP 800-90A (Rev 2 draft) — all ingested as the current final; re-check on finalization. (b) **Draft-watch (ingested-as-draft or watch-only)**: NIST IR 8596 (Cyber AI Profile), SP 800-53 COSAiS AI control overlays, NIST Privacy Framework 1.1 (CSWP 40 IPD) — near-certain future citations once final. (c) **Controlled-vocabulary gap**: `grc_library_ref/topics.md` has no `cryptography` tag, so the ~15 held crypto standards map to `cybersecurity` only; assess adding a `cryptography` tag (topics.md + normalize_topics.py + the gate) if crypto-citation precision warrants it. (d) **FedRAMP 2026 evolving-preview**: the held FedRAMP 2026 Consolidated Rules are a daily-updated Public Preview snapshot (SHA-anchored); establish a re-snapshot cadence when it stabilizes to a release.

## Standing conventions

Durable behavioural guidance from the maintainer. NOT actionable items; reference material for the orchestrator and future contributors.

- **"More PRs, keep each one clean"** — favor small focused PRs.
- **"I prefer /validate, not /validation-sweep"** — short slash commands; skill names stay descriptive.
- **"Don't explicitly name or link `.working/`"** in template-content files that adopters see.
- **"Inference must be validated before committing or before anything else uses that information"** — operationalized in [`validate-inference-before-action.md`](dev-security/claude-rules/governance/validate-inference-before-action.md).
- **Activity directories should be self-contained** — operationalized in the canonical `.working/<activity>/` layout.
- **Zero-finding sweeps still need history rows but no detail files** — operationalized in the validation-sweep [`SKILL.md`](dev-security/claude-rules/skills/validation-sweep/SKILL.md) step 9.
- **Sweep history is project-application, not template content** — operationalized by keeping the history file in `.working/`.
- **TODO is forward-looking; historical state rotates to DONE.md** — operationalized in [`change-tracking.md`](dev-security/claude-rules/governance/change-tracking.md) PR-finalization-protocol section.
- **After completing a merge, list the upcoming next 5 planned PRs from TODO** — operationalized in [`.claude/CLAUDE.md`](.claude/CLAUDE.md) PR-workflow section and the same pack rule.
- **Validate cadence is 1-8 PRs per batch, not strictly 5** — the 5-PR cadence is default; the batch boundary is chosen at the natural seam.
- **DONE format mirrors TODO format** — DONE H3 headings carry `FR-N (severity)` so the two ledgers are scannable in the same shape. Harmonized in PR #163.
- **Compute-don't-ask** (maintainer-flagged 2026-06-23) — before surfacing a question, apply a "can I compute/verify this myself?" gate; surface the result, not the raw question. (Codified 2026-06-30 into the `clarify-before-acting` rule's compute-first gate, #504.)

---

## Backlog totals

Approximate active counts after the 2026-06-30 work-type re-tier and the 2026-07-02 audit intake (the priority sections themselves are the source of truth; these drift).

- **P1 (fix errors and prevent recurrence)**: 1 item (1.5 reference version-currency; 1.11 the Brazil ANPD-restructuring and breach-rule citation verification CLOSED in #746, the doubling sub-clause upgraded to a primary-source confirmation against the held DOU text of Resolution CD/ANPD No. 15/2024 once ref #35 landed, discharging the accepted-unverified tracker; 1.9 the RM-10 pipe-guardrail hardening CLOSED in #677 as a documented harness limitation (root cause pinned: child sessions leave `CLAUDE_PROJECT_DIR` unset so the PreToolUse hook path fails to resolve; compensating controls active, recorded in [`third-party-issues.md`](.working/third-party-issues.md)); the Quebec 72-hour confabulation sweep closed same-day, #631 catch to #633 fix; the audit-gate candidates closed with S3's `/claim-fit` ship 2026-07-04; the 2026-07-02 audit P1 cluster is fully closed; FR-48 completed in #596 through #607; the section-1.6 D5/CLAUDE.md codifications closed same-day).
- **P2 (fill significant gaps)**: 2 open items (2.1, 2.3; the FR deepenings FR-59 / 60 / 70 / 99 / 15 / 23 / 63 / 74 / 154 / 41, of which 2.4 / 2.5 / 2.6 / 2.7 (FR-99 / 15 / 23 / 63) closed in #681 / #682 / #683 / #697, 2.2 (FR-60 HIPAA) closed in #731 via the new `compliance/healthcare/procedure-hipaa-operational-compliance.md`, 2.8 (FR-74 Schrems II) closed in #734, 2.9 (FR-154 operational-vagueness cluster) closed across #738 (5 of 6 sub-items) and #745 (sub-item 5, the whistleblower feedback ceiling, once the EU Whistleblower Directive was held), 2.10 (FR-41 unified ADM/AI/FRIA workflow) closed in #735 via the new `ai/procedure-integrated-ai-and-privacy-assessment.md`, 2.12 (DORA) closed in #736 via the deepened `compliance/financial-services/annex-dora-implementation.md`, and 2.13 (NIS2) closed in #737 via the deepened `compliance/annex-nis-2-implementation.md`; 2.11 publications-assessment closed (the `publication-screening` skill / `/screen-publications` + reference-base register, ref #29) and 2.14 the `/reference-audit` cadenced skill closed in #707).
- **P3 (clean up and tooling)**: 10 items (3.1, 3.12, plus 3.13 audit tooling extensions, 3.15 the 2026-07-02 guardrail-review machinery extensions, 3.16 / 3.17 the alignment maps, 3.19 the CHANGELOG restructure, 3.20 the `/reference-audit` build residuals, 3.22 the cross-repo worker-provenance link review, and 3.24 the publications-screening wave; sections 3.6, 3.7, 3.8, 3.10, 3.14, 3.18, 3.21, and 3.23 fully closed).
- **P4 (adopter experience)**: 7 items (4.1-4.5, plus 4.6 adopter-experience enhancements and 4.7 the 2026-07-02 guardrail-review pack-design improvements).
- **P5 (expand: country / regulator / programme overlays)**: 8 items (5.2-5.9; FR-62, the two founding AI-jurisdiction annexes, closed in #749: EU #743 + Colorado #749).
- **P6 (expand: new domains)**: 5 items (6.1-6.5).
- **P7 (awaiting decision)**: 0 pending + 2 dropped-decision audit-trail entries (7.2 / 7.3; the former 7.1 decided 2026-07-03, keep-by-design).
- **Reference-base work (`grc_library_ref`)**: 1 open item (SR-1 last_checked, decided but egress-gated), plus the 2026-07-05 `originals/README.md` stale-snapshot finding (scoped in pending-decisions). SR-5 (validate.py cosmetic polish) closed via ref #31 (dead TEXT_EXTS entry removed, EPUB note widened, MuPDF stdout noise suppressed). SR-3 (validate.py binary-scan gaps) closed via ref #30 (check 12 widened to CSV directory-coverage orphan detection; check 13 widened from PDF-only to every tracked binary via PyMuPDF + zipfile + raw-byte scan). SR-2 (publications screening-record check) closed via ref #29 (`validate.py` check 14 + the `SCREENING.md` register, the reference-base half of the former TODO 2.11). SR-4 (catalogue/extract hygiene) closed 2026-07-05 (pre-split, scratch PRs #104, #105).

---

## Notes on maintenance

- Add new items at the appropriate priority; within a section keep the lowest-effort-first ordering. Move items between priorities as context changes.
- When an item is completed, delete it from this file (no strikethroughs, no `[done]` suffixes) and add an entry to [`.working/DONE.md`](.working/DONE.md) in the same PR. The rotation discipline is documented in the PR-finalization-protocol section of [`.claude/rules/governance/change-tracking.md`](.claude/rules/governance/change-tracking.md).
- Design decisions belong in [`.working/design-decisions.md`](.working/design-decisions.md), not in TODO. The P7 "dropped-decision" entries cross-reference those decisions for audit-trail completeness without duplicating the rationale.
- This file is the source of truth for what's queued; conversation history is not.
- Fitness-review backlogs remain the authoritative per-finding evidence source; this file is the action-organized view, kept in sync at fitness-review-cycle time.
